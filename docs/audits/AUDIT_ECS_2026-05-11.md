# AUDIT_ECS 2026-05-11

Re-audit of the ECS in `crates/core/src/ecs/` and its cross-crate
consumers. Baseline: prior `AUDIT_ECS_2026-05-06.md` closed most of
the surface (only D5-NEW-01 / D5-NEW-02 were filed there, both
documentation-class). The 2026-05-04 hot-path perf batch (#823 /
#824 / #825 / #828 / #466 E-03) is the load-bearing regression
surface; this re-audit verifies each anchor is still wired correctly.

## Executive Summary

| Result | Value |
| --- | --- |
| Test suite | **PASS** — 305 tests, `cargo test -p byroredux-core` |
| Dim 8 regression anchors | **5/5 PASS** |
| Dims 1-7 invariants | spot-checked PASS (no exhaustive walk this pass) |
| **Total new findings** | **0** |

## Test Suite

`cargo test -p byroredux-core` — 305 tests PASS. No regressions
since the 2026-05-06 baseline.

## Regression Check (Dim 8 — 2026-05-04 perf batch anchors)

| Issue | Anchor | Location | Status |
| ----- | ------ | -------- | ------ |
| **#823** ECS-PERF-01 | `lock_tracker::held_others` Vec allocation gated on `cfg(debug_assertions)` | `crates/core/src/ecs/lock_tracker.rs:83, 125` (`#[cfg(debug_assertions)]`-block wrapping the `Vec` collection, both read + write paths) | **PASS** |
| **#824** ECS-PERF-02 | `NameIndex.map` in-place refill via `clear()` + `reserve()` + reinsert (not `HashMap::new()` + swap) | `byroredux/src/systems.rs:353-359` (`idx.map.clear(); idx.map.reserve(current_name_count); for ... { idx.map.insert(...) }`) | **PASS** |
| **#825** ECS-PERF-03 | `transform_propagation_system` root-set cache keyed on `(GlobalTransform::len, Parent::len, next_entity_id)` | `byroredux/src/systems.rs:1104, 1157-1166` (key construction + comparison) — plus regression tests `root_cache_invalidates_on_topology_change` (line 1992) and `root_cache_steady_state_still_refolds_parent_bounds` (line 2058) | **PASS** |
| **#828** ECS-PERF-06 | `animation_system` scratches (`events` / `seen_labels`) hoisted out of the per-entity loop; uses `clone()` not `mem::take` to preserve capacity | `byroredux/src/systems.rs:417-421, 550, 593, 681-688` (`events.clone()` insertion sites + `seen_labels.clear()` reuse) — inline doc comments cite #828 | **PASS** |
| **#466** E-03 | `World::despawn` poisoned-lock panic uses type-name side-table | `crates/core/src/ecs/world.rs` (`storage_lock_poisoned` / `storage_lock_poisoned_erased` / `resource_lock_poisoned` helpers; each panic message embeds the component / resource type name) | **PASS** |

All five anchors PASS. The hot-path perf gains from the 2026-05-04
batch are still in place.

## Verified Invariants (Dims 1-7, spot-checks)

| Dim | Invariant | Verified |
| --- | --------- | -------- |
| 1   | Storage correctness (sparse-set swap-remove, packed binary-search) | Indirect (test suite covers; not re-walked this pass) |
| 2   | Query safety / TypeId-ordered locking / Deref soundness | Indirect (test suite + #823 anchor) |
| 3   | `find_by_name` integer-FixedString comparison only | **PASS** at `world.rs:316` (`names.iter().find(|(_, n)| n.0 == sym)` — both operands are `FixedString` symbols) |
| 4   | Resource access via `&self` (interior mutability) | Indirect (test suite) |
| 5   | Scheduler `Fn(&World, f32)` blanket | Indirect (test suite) |
| 6   | Unsafe code review | Not re-walked this pass — covered by 2026-05-03 audit |
| 7   | Streaming / scripting / new-component lifecycles | Indirect (305 tests cover the touch-points) |

Dims 1-2 and 4-7 were not exhaustively re-walked this round; the
test-suite-green outcome is the proxy for "no behaviour drift."
The 2026-05-06 audit covered Dims 1-6 in depth; nothing in the
intervening week's diff suggests a regression there.

## New Findings

**None.** No file:line was verified to a violation. Per
`feedback_audit_findings.md`, no speculative findings filed without
ground-truth.

## Items Already Filed / Closed

From the prior audits:

- **D5-NEW-01** (2026-05-06, doc-only) — per-system panic propagation
  contract documentation gap. Still open; no new info this pass.
- **D5-NEW-02** (2026-05-06, doc-only) — schedule re-entry guard is
  structural, not asserted. Still open; no new info this pass.
- All 2026-05-04 hot-path perf items (#823 / #824 / #825 / #828 /
  #466 E-03) confirmed PASS above.

## Caveats

- This pass did not exhaustively re-walk Dims 1-2 / 4-7. The
  2026-05-06 audit covered them in depth and the intervening week's
  diff is dominated by the NIF / renderer / Vulkan-lifecycle work
  (see commit `git log --since=2026-05-06 --oneline -- crates/core/`)
  — none of which touches ECS hot paths.
- Cross-crate consumer audits (M40 streaming, M41 NPC spawn,
  scripting events, animation controller, debug-drain, AudioWorld)
  were not re-walked; their 305-test-green status is the proxy
  signal.

## Prioritized Fix Order

Nothing to prioritize — zero new findings.

---

Suggest: no `/audit-publish` needed — empty findings list. This
report doubles as a regression-verification record for the 2026-05-04
perf batch and the 2026-05-06 baseline.
