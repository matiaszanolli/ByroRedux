# NIF Parser Audit — 2026-08-20

Deep audit of the NIF binary-format parser (`crates/nif/src/`) across all 6
dimensions: Stream Position Integrity, Version Gating, Block Dispatch Coverage,
Geometry Extraction & Import Handoff, Collision & Shader Block Parsing, and
Allocation Hygiene.

**Scope**: the parser *as a parser*, across its full supported version range
(Oblivion v10.x / v20.0.0.5 → Starfield). No `--game` restriction.

**Method note (differs from the 2026-08-16 sweep)**: this run is part of a
25-audit suite and is explicitly barred from invoking cargo (a concurrent
process holds the target lock). **No corpus probe was re-run this cycle.**
Every claim below is static analysis against HEAD (`bb0b92f2`) cross-checked
against `/mnt/data/src/reference/nifxml/nif.xml`, plus the *checked-in
baselines* — which were themselves regenerated against real game data on
2026-08-19 (`17cb417d`) and are therefore fresher than the 08-16 report's own
measurements. Where a number is inherited rather than re-measured, it is
labelled as such.

## Executive Summary

`crates/nif/` was **not** quiet this cycle despite the session-70 water focus:
46 files, +3 274 / −388 lines across 22 commits. Nine of those commits are
parser fixes with real byte-level consequences (`#2562`/`#2563`, `#2598`,
`#2622`, `#2578`, `#2320`, `#2319`, `#2580`, `#2549`, `#2862`), and the three
MEDIUM findings from 2026-08-16 were all fixed (`86c41022`, `7218f543`).

The **headline improvement**: Oblivion is now at **100% clean, zero
truncations** (8 032 / 8 032). `e32e2b1f` read the `until="10.1.0.103"` `Data`
ref on nine controller types, which recovered `meshes\marker_map.nif` — the last
remaining Oblivion truncation — and `17cb417d` regenerated
`oblivion_truncations.tsv` from real data to `truncating=0 parsed=8032`. Both
`docs/engine/nif-parser.md` and `ROADMAP.md` still carry the stale
`99.93% / 6 truncations` row (tracked OPEN as **#2564**, whose own "stale by 5"
framing is now itself stale — the correct delta is 6).

Three new findings, none of them in the code the delta touched *incorrectly* —
two are **incompleteness in the delta's own fixes** and one is a stale
test baseline the delta invalidated:

1. `#2562`/`#2563` fixed the missing `until="10.1.0.103"` `Data` ref on nine
   `NiSingleInterpController` subclasses. nif.xml declares the same field on
   **four more**, all in the `NiPSys*Ctlr` family, and their shared parsers in
   `particle.rs` additionally read the `since="10.1.0.104"` interpolator ref
   *unconditionally* — a compound desync below that version, covering 22
   dispatch names.
2. Giving `NiKeyframeController` / `NiTransformController` / `NiVisController` /
   `NiAlphaController` real RTTI moved them off the `NiSingleInterpController`
   row of the per-block histogram. The six checked-in
   `per_block_baselines/*.tsv` were **not** regenerated, so
   `compare_histograms` will now report `ParsedShrank` on every one of them —
   the opt-in regression gate is red across the board.
3. The `#2632` / `#2828` degenerate-tangent guard produces a **zero** tangent
   and bitangent for exactly the input its own comment names as the motivation
   (`N` with all-equal components), and the Z-up producer never normalizes `N`
   at all — an asymmetry with its Y-up sibling that is reachable on every
   BSTriShape mesh lacking `VF_TANGENTS`.

**Total new findings**: 4 (0 CRITICAL, 0 HIGH, 2 MEDIUM, 2 LOW).
**Total stream-position mismatches found**: 1 (finding -01, latent).
**Critical coverage gaps**: none — 253 live top-level dispatch arms.

### Per-game clean/recoverable rate

| Game | clean | recoverable | `NiUnknown` | source |
|---|---|---|---|---|
| Oblivion | **100%** (8 032 / 8 032) | 100% | 0 | re-baselined `17cb417d`, real data 2026-08-19 |
| Fallout 3 | 100% | 100% | 0 | `block_coverage_baselines/fallout_3.tsv` (526 109 blocks, 0 unknown) |
| Fallout NV | 100% | 100% | 0 | `fallout_nv.tsv` (662 102 blocks, 0 unknown) |
| Skyrim SE | 100% | 100% | 0 | `skyrim_se.tsv` (758 733 blocks, 0 unknown) |
| Fallout 4 | 100% | 100% | 0 | `fallout_4.tsv` (740 562 blocks, 0 unknown) |
| Fallout 76 | 100% | 100% | 0 | `fallout_76.tsv` (1 548 202 blocks, 0 unknown) |
| Starfield | 100% | 100% | 0 | `starfield.tsv` (770 322 blocks, 0 unknown) |

Oblivion's improvement from the 08-16 report (1 truncation) to 0 is real and
attributable: `e32e2b1f`'s `NiKeyframeController.Data` fix recovered 8 of
`marker_map.nif`'s 13 blocks, including both `NiTriShape` subtrees.

## Block Type Coverage Matrix

Dispatch table: `parse_block_inner` in `crates/nif/src/blocks/mod.rs` —
**253 top-level match arms** (counted this cycle with a brace-depth-aware scan
that excludes the two nested `match` blocks; 252 at 08-16, +1 from the new
`NiPreSplitDataController` arm). `NiUnknown` fallbacks are 0 on all seven
games per the checked-in `block_coverage_baselines` (`unknown_blocks 0` on all
six sized games; Oblivion is gated by truncation-set parity instead, now empty).

| Game | `NiUnknown` blocks | total blocks | gate |
|---|---|---|---|
| Oblivion | n/a (sizeless) | 8 032 files | truncating=0, parsed=8032 |
| Fallout 3 | 0 | 526 109 | PASS |
| Fallout NV | 0 | 662 102 | PASS |
| Skyrim SE | 0 | 758 733 | PASS |
| Fallout 4 | 0 | 740 562 | PASS |
| Fallout 76 | 0 | 1 548 202 | PASS |
| Starfield | 0 | 770 322 | PASS |

Two nif.xml `niobject`s reachable through `NiSingleInterpController` have **no
dispatch arm at all** — `NiFloatsExtraDataController` and `NiRollController`.
Neither is a finding: zero `NiUnknown` on every game means neither appears in
any shipping corpus. Recorded here so the next sweep does not re-derive it.

---

## Findings

### MEDIUM

#### NIF-D1-2026-08-20-01: the `NiPSys*Ctlr` family has the exact `until="10.1.0.103"` / `since="10.1.0.104"` split that `#2562`/`#2563` just fixed on nine siblings — 22 dispatch names, compound desync

- **Severity**: MEDIUM (stream-position mismatch; escalates to HIGH on any
  sizeless file that actually carries one, since `read_string` then reads a
  garbage length prefix)
- **Dimension**: Stream Position Integrity
- **Game Affected**: any NIF below file-version **10.1.0.104**. On shipping
  content this is the Oblivion-era NetImmerse band (`bsver` ≤ 11, file versions
  v3.3–v10.1.0.103) — the same band `#2562`/`#2563` describes as "latent … but
  a real trap for mod/legacy NetImmerse content". FO3+ (`v20.2.0.7`) and every
  later game are above the boundary and unaffected.
- **Location**: `crates/nif/src/blocks/particle.rs:938-947`
  (`parse_modifier_ctlr`), `:950-963` (`parse_emitter_ctlr`), `:965-978`
  (`parse_multi_target_emitter_ctlr`); dispatched from
  `crates/nif/src/blocks/mod.rs:1135-1169`. Gate helper that already exists and
  should be reused: `NifVersion::has_keyframe_controller_data`
  (`crates/nif/src/version.rs:262`).
- **Status**: NEW. Not covered by `#2562`/`#2563` — their fix touched
  `blocks/controller/mod.rs` and `blocks/controller/shader.rs` only, and
  `version.rs:247-262`'s enumerating doc comment lists exactly the nine types
  they fixed. No OPEN or CLOSED issue in `/tmp/audit/issues.json` mentions the
  `NiPSys*Ctlr` half.
- **Description**: nif.xml declares a `Data` ref gated `until="10.1.0.103"` on
  four more `NiSingleInterpController` descendants than `#2562`/`#2563` fixed:

  | type | `Data` template | reaches |
  |---|---|---|
  | `NiPSysEmitterCtlr` | `NiPSysEmitterCtlrData` | `parse_emitter_ctlr` |
  | `NiPSysModifierActiveCtlr` | `NiVisData` | `parse_modifier_ctlr` |
  | `NiPSysModifierFloatCtlr` (abstract base of 19 dispatch names) | `NiFloatData` | `parse_modifier_ctlr` |
  | `NiFloatsExtraDataController` | `NiFloatData` | *no dispatch arm* |

  The defect in `particle.rs` is worse than a plain missing field, because
  these three functions do **not** delegate to
  `NiSingleInterpController::parse` (which gates its interpolator ref
  correctly at `controller/mod.rs:257`). They open-code the chain and read the
  interpolator ref *unconditionally*:

  ```rust
  // particle.rs:938-943 — parse_modifier_ctlr
  let _base = parse_interp_controller_base(stream)?;
  let _interpolator_ref = stream.read_block_ref()?; // NiSingleInterpController
  let _modifier_name = stream.read_string()?;       // NiPSysModifierCtlr
  //  ↑ no gate                                     ↑ and no trailing Data ref
  ```

  Below v10.1.0.104 that produces a three-stage desync: (1) 4 bytes of a
  non-existent interpolator ref are consumed, (2) `modifier_name` is then read
  from a 4-byte-shifted offset — and since the string table only exists at
  `STRING_TABLE_THRESHOLD` and above, `read_string` here reads a `u32` length
  prefix, so a shifted read yields an arbitrary length, and (3) the real
  trailing `Data` ref is never read.
- **Evidence**:
  - nif.xml field declarations (extracted mechanically from
    `/mnt/data/src/reference/nifxml/nif.xml`):
    ```
    NiPSysEmitterCtlr:        <field name="Data" type="Ref" template="NiPSysEmitterCtlrData" until="10.1.0.103" />
                              <field name="Visibility Interpolator" type="Ref" template="NiInterpolator" since="10.1.0.104" />
    NiPSysModifierActiveCtlr: <field name="Data" type="Ref" template="NiVisData"   until="10.1.0.103" />
    NiPSysModifierFloatCtlr:  <field name="Data" type="Ref" template="NiFloatData" until="10.1.0.103" />
    NiFloatsExtraDataController: <field name="Data" type="Ref" template="NiFloatData" until="10.1.0.103" />
    ```
  - **The inconsistency is visible inside one function.**
    `parse_emitter_ctlr` already gates the *visibility* interpolator on
    `>= V10_1_0_104` (`particle.rs:958`, the `#1544` fix) and its own comment
    even names the missing field — *"the pre-10.1.0.104 `Data` ref is the
    mutually-exclusive legacy slot"* — while the *primary* interpolator two
    lines above it (`:952`) is read with no gate at all and the legacy slot is
    never read.
  - Neither `niobject` carries a `since=` attribute in nif.xml, so the schema
    itself considers the pre-split form reachable; that is why the `Data` ref
    is declared at all.
  - The dispatch blast radius is 22 type names:
    `NiPSysEmitterCtlr`, `BSPSysMultiTargetEmitterCtlr`,
    `NiPSysModifierActiveCtlr`, and the 19 `NiPSysModifierFloatCtlr`
    descendants listed at `blocks/mod.rs:1139-1168` (`NiPSysEmitterSpeedCtlr`,
    `NiPSysGravityStrengthCtlr`, `NiPSysAirFieldSpreadCtlr`,
    `NiPSysRotDampeningCtlr`, …). Notably `blocks/mod.rs:1156-1164`'s own
    comment asserts the trailing ref "is gated `until="10.1.0.103"` so FO76
    (v20.2.0.7) skips it via the same NiTimeController base" — but no code path
    implements that gate; the field is simply never read at any version.
- **Impact**: Latent on vanilla content (0 truncations on Oblivion today
  proves no vanilla sub-10.1.0.104 file carries a `NiPSys*Ctlr`), exactly as
  `#2563` characterised its own eight latent types. Reachable on mod / legacy
  NetImmerse particle content. On a sizeless file there is no `block_size`
  anchor, so the desync cascades through every subsequent block — the same
  failure mode that cost `marker_map.nif` 8 of its 13 blocks.
- **Related**: Direct continuation of `#2562` / `#2563`. `NiFloatExtraDataController`
  (singular) *was* fixed by them; `NiFloatsExtraDataController` (plural) has no
  dispatch arm and so is out of scope until one is added — but the two names
  are one character apart and will be easy to conflate.
- **Suggested Fix**: In `particle.rs`, gate the interpolator ref on
  `stream.version() >= NifVersion::V10_1_0_104` (or, better, delegate to
  `NiSingleInterpController::parse` so the gate cannot drift), and add the
  complementary `has_keyframe_controller_data()`-gated `Data` ref after
  `modifier_name` in all three functions — the field sits at the same offset
  for `NiPSysModifierActiveCtlr` (via `NiPSysModifierBoolCtlr`, no own fields)
  and `NiPSysModifierFloatCtlr`, so one read covers both. Extend
  `version.rs:247-262`'s enumerating doc comment with the four new types so the
  next reader does not re-derive the same list. Pin with synthetic v10.1.0.103
  fixtures matching the convention `dispatch_tests/controllers.rs` already
  established for the nine sibling types.

#### NIF-D3-2026-08-20-02: `#2562`'s RTTI restoration invalidated all six `per_block_baselines/*.tsv` — the opt-in gate now fails `ParsedShrank` on every game

- **Severity**: MEDIUM (the regression gate that exists specifically to catch
  silent per-type parse loss is red on all seven games, so a *real* regression
  is now indistinguishable from this known-benign reclassification)
- **Dimension**: Block Dispatch Coverage
- **Game Affected**: all six sized games with an `NiSingleInterpController`
  baseline row (Oblivion, FO3, FNV, Skyrim SE, FO4, FO76). Starfield has no
  such row and is unaffected.
- **Location**: `crates/nif/tests/data/per_block_baselines/{oblivion,
  fallout_3,fallout_nv,skyrim_se,fallout_4,fallout_76}.tsv`; comparison logic
  at `crates/nif/tests/common/mod.rs:809-815`; keying at
  `crates/nif/tests/common/mod.rs:670-683`.
- **Status**: NEW. Same *class* as the 2026-08-16 report's `#2574` note
  (Oblivion TSV stale after a benign reclassification), but a different cause
  and a much wider blast radius — `#2574` was one game and was resolved by
  `c41e87d8`'s regeneration; this was introduced afterwards, by `e32e2b1f`
  (2026-08-19), and covers six.
- **Description**: `PerBlockHistogram::record_scene_blocks` keys parsed blocks
  on the **struct's** `block_type_name()`:
  ```rust
  // tests/common/mod.rs:677-682
  } else {
      self.counts
          .entry(block.block_type_name().to_string())
          .or_default()
          .parsed += 1;
  }
  ```
  Before `e32e2b1f`, `NiKeyframeController` / `NiTransformController` /
  `NiVisController` / `NiAlphaController` all parsed into a bare
  `NiSingleInterpController`, so every one of their blocks was counted on the
  `NiSingleInterpController` row — the RTTI erasure the commit message itself
  calls out. `e32e2b1f` gave them a dedicated `NiPreSplitDataController` whose
  `block_type_name()` returns their real name (`blocks/controller/mod.rs:310-313`,
  dispatch at `blocks/mod.rs:772-784`). Those blocks now land on four new rows,
  and the `NiSingleInterpController` row shrinks by exactly that count.
  `compare_histograms` treats any shrink as a hard regression:
  ```rust
  // tests/common/mod.rs:809
  if cur_counts.parsed < base_counts.parsed {
      regressions.push(BaselineRegression::ParsedShrank { … });
  ```
  The four new rows are absent from every baseline (they never existed), which
  is harmless; the shrink is not.
- **Evidence**:
  ```
  $ grep -nE "NiSingleInterpController|NiAlphaController|NiVisController|NiKeyframeController|NiTransformController" *.tsv
  fallout_3.tsv:55:NiSingleInterpController   7138  0
  fallout_4.tsv:64:NiSingleInterpController  11086  0
  fallout_76.tsv:62:NiSingleInterpController 17439  0
  fallout_nv.tsv:58:NiSingleInterpController 11598  0
  oblivion.tsv:43:NiSingleInterpController   3034  0
  skyrim_se.tsv:59:NiSingleInterpController   5640  0
  # zero rows for any of the four renamed types on any game — they were all
  # folded into the row above.
  ```
  The Oblivion case is unambiguous: the only three types that still parse as a
  bare `NiSingleInterpController` are `BSMaterialEmittanceMultController`,
  `BSRefractionStrengthController` and `BSFrustumFOVController`
  (`blocks/mod.rs:786-791`), and all three are Skyrim-era Bethesda extensions
  absent from Oblivion. So Oblivion's entire 3 034 must come from the four
  renamed types, and that row drops to ~0 — a guaranteed `ParsedShrank` of
  ~3 034 on the next `--ignored` run.
  `block_coverage_baselines` is unaffected (it gates on `NiUnknown` rate and
  the truncating-file set, neither of which is RTTI-keyed) and
  `oblivion_truncations.tsv` *was* correctly regenerated by `17cb417d` — so the
  omission is specific to the `per_block_baselines` sibling.
- **Impact**: The gate whose entire purpose is *"a parser regression where a
  previously-handled type starts under-consuming registers as a successful
  recoverable parse … the geometry is silently missing; the gate stays green"*
  (`per_block_baselines.rs:3-8`) now fails on six of seven games for a known-benign
  reason. Until it is re-baselined, a genuine regression in any of those six is
  invisible in the noise. Note this cannot be caught by `cargo test` — the
  tests are `#[ignore]`d and need game data, which is exactly why it slipped.
- **Related**: `#2574` (same class, resolved by regeneration, one game).
  Caused by `e32e2b1f` (`#2562`/`#2563`), which is otherwise a correct fix.
- **Suggested Fix**: Run
  `BYROREDUX_REGEN_BASELINES=1 cargo test -p byroredux-nif --test per_block_baselines -- --ignored --nocapture`
  on the machine with game data and check the six regenerated TSVs in. Then
  add a line to the `## How the rows are keyed` section of
  `per_block_baselines.rs` noting that **any change to a `block_type_name()`
  return value requires regeneration**, alongside the existing aliased-type
  caveat — that section already documents the aliasing direction
  (`bhkSPCollisionObject` → `BhkPCollisionObject`) but not the de-aliasing one.

### LOW

#### NIF-D4-2026-08-20-03: the `#2632`/`#2828` degenerate-tangent guard emits a zero tangent for exactly the case its own comment names as the motivation

- **Severity**: LOW (the shader has a documented, legitimate fallback for a
  zero tangent, so the observable effect is "synthesis silently declines" —
  not corruption and not NaN)
- **Dimension**: Geometry Extraction & Import Handoff
- **Game Affected**: all — both producers. Z-up (`synthesize_tangents`) serves
  Oblivion / FO3 / FNV `NiTriShape` and Skyrim SE+ `BSTriShape` without
  `VF_TANGENTS`; Y-up (`synthesize_tangents_yup`) serves Starfield `BSGeometry`
  and SSE-reconstructed geometry.
- **Location**: `crates/nif/src/import/mesh/tangent.rs:275-292` (Z-up, added by
  `8075133c` / `#2828`) and `:481-502` (Y-up, added by `#2632`)
- **Status**: NEW. `#2828` and `#2632` are both CLOSED; this is not a
  regression of either — the code is exactly as they landed it — it is an
  incompleteness in the fix they both implement.
- **Description**: Both degenerate branches take a cyclic permutation of the
  normal as a seed tangent, Gram-Schmidt it against `N`, and normalize:
  ```rust
  let t_y_raw = [n_yup[1], n_yup[2], n_yup[0]];               // :489
  let dot_nt  = n_yup[0]*t_y_raw[0] + n_yup[1]*t_y_raw[1] + n_yup[2]*t_y_raw[2];
  let mut t_y = [ t_y_raw[0] - n_yup[0]*dot_nt, … ];
  normalize_inplace(&mut t_y);
  let b_y = cross(n_yup, t_y);
  ```
  The `#2632` comment directly above states the reason for the projection:
  *"a raw cyclic permutation of N's components is NOT generally orthogonal to
  N (e.g. **any N with all-equal components permutes to itself**)"*. But for
  precisely that input the projection removes the entire vector: `t_y_raw == N`
  ⇒ `dot_nt == 1` ⇒ `t_y == [0,0,0]`, and `normalize_inplace` maps a
  below-`1e-12` vector to `[0.0, 0.0, 0.0]` (`tangent.rs:550-560`) rather than
  picking a different seed. `b_y = cross(N, 0) = 0` follows, and
  `bitangent_sign(N, 0, 0)` returns `+1.0` via `clamp_sign(0.0)`
  (`crates/nif/src/types.rs:154-162`). The vertex ships
  `[0.0, 0.0, 0.0, 1.0]`. The Z-up sibling has the identical algebra: the
  coordinate swap is orthogonal, so `dot(n_yup, t_y_raw) == dot(n_zup, t_z)`
  and the trigger condition is unchanged.
- **Evidence**: `N = ±(1,1,1)/√3` is the trigger, and it is representable in
  every source encoding the branch sees. `BSTriShape` normals are three
  independent `byte_to_normal` reads (`blocks/tri_shape/bs_tri_shape.rs:1194-1197`),
  so any vertex whose three normal bytes are equal — the ordinary encoding for
  a diagonal/corner-chamfer normal — lands exactly on it. The branch is
  additionally gated on `vec3_is_zero(&tangent_zup) || vec3_is_zero(&bitangent_zup)`,
  i.e. degenerate UVs, so the two conditions must coincide; that makes it rare,
  not unreachable.
  Pre-`#2828`/`#2632` the branch returned the raw permutation, which is
  non-orthogonal but never zero — so the guard traded a slightly-wrong basis
  for no basis at all in this one case.
- **Impact**: Bounded. `triangle.frag`'s documented contract
  (`crates/renderer/shaders/triangle.frag:446-451`) is that a zero
  `fragTangent.xyz` falls back to the screen-space derivative TBN (Path 2), so
  the affected vertices get a valid — just not authored-quality — basis. No
  NaN, no corruption. The cost is that `synthesize_tangents`' whole purpose is
  defeated on those vertices, silently.
- **Related**: `#2828` (CLOSED, Z-up half), `#2632` (CLOSED, Y-up half).
  Distinct from OPEN **#2815** (`perturbNormal` Path 1 NaN when tangent is
  *parallel* to normal) — that is a renderer-side guard for a non-zero
  degenerate tangent; this is a producer-side zero.
- **Suggested Fix**: When `t_y` collapses below the `normalize_inplace`
  threshold, fall back to a second, non-parallel seed — the standard choice is
  the world axis least aligned with `N` (pick the smallest `|N.x|/|N.y|/|N.z|`
  component, cross with `N`), which is orthogonal by construction and cannot
  degenerate. Add the `N = (1,1,1)/√3` case to
  `crates/nif/src/import/mesh/tangent_convention_tests.rs`, whose existing
  `#2632` guard tests (`:229`, `:290`) use normals that do not trigger it.

#### NIF-D4-2026-08-20-04: `synthesize_tangents` (Z-up) never normalizes `N`, while its Y-up sibling does — and it is the producer that receives quantized `BSTriShape` normbyte normals

- **Severity**: LOW
- **Dimension**: Geometry Extraction & Import Handoff
- **Game Affected**: Skyrim SE / FO4 / FO76 `BSTriShape` meshes that set
  `VF_NORMALS | VF_UVS` but not `VF_TANGENTS`. Oblivion / FO3 / FNV
  `NiTriShape` normals are authored `f32` and are unit in practice, so the
  legacy path is unaffected in practice.
- **Location**: `crates/nif/src/import/mesh/tangent.rs:261-262` (Z-up, no
  normalize) vs `:462-463` (Y-up, explicit normalize). Caller that supplies the
  quantized input: `crates/nif/src/import/mesh/bs_tri_shape.rs:199-204`.
- **Status**: NEW.
- **Description**: `#2632` added an explicit per-vertex normalize to the Y-up
  producer with a comment stating the reason plainly:
  ```rust
  // tangent.rs:454-463
  // #2632 / SF2D2-D2-04 — `normals_yup` is unit-length only to
  // quantization for a UDEC3-decoded source …; the Gram-Schmidt
  // projection below (and the degenerate branch's permutation +
  // cross product) is only correct for `|n| == 1`.
  let mut n_yup = normals_yup[i];
  normalize_inplace(&mut n_yup);
  ```
  The Z-up producer does no such thing — it converts the raw normal and uses it
  directly:
  ```rust
  // tangent.rs:261-262
  let n_zup = normals_zup[i];
  let n_yup = byroredux_core::math::coord::zup_to_yup_pos([n_zup.x, n_zup.y, n_zup.z]);
  ```
  and then runs the same `T - N·(N·T)` projection at `:305-311` and `:317-323`.
  The stated precondition (`|n| == 1`) is not established on this path.
- **Evidence**: The Z-up producer's third caller is
  `bs_tri_shape.rs:199`, which passes `shape.normals` — decoded as three
  independent `byte_to_normal` reads with no vector renormalization
  (`blocks/tri_shape/bs_tri_shape.rs:1194-1205`). That is the same class of
  quantized, non-unit input `#2632` fixed for UDEC3, arriving at the sibling
  that was left unpatched. `#2632`'s own scope note ("Starfield BSGeometry …
  SSE-reconstructed BSTriShape") explains why: the Z-up caller was not in view.
- **Impact**: Small and systematic rather than catastrophic. With
  `|N|² = 1 + ε`, the Gram-Schmidt step over-subtracts by `ε·(N·T)·N`, so the
  emitted tangent is not exactly orthogonal to the shading normal and the
  derived bitangent sign can flip on near-perpendicular cases. Normbyte
  quantization keeps `ε` under about ±1.5%, so the angular error is sub-degree
  — visible, if at all, as slight normal-map shading drift on Skyrim+/FO4
  meshes that ship no authored tangents. No corruption.
- **Related**: `#2632` (CLOSED) fixed exactly this on the Y-up half. Sibling of
  NIF-D4-2026-08-20-03 (same function pair, same divergence between the two
  producers).
- **Suggested Fix**: Mirror `:462-463` — bind `n_yup` mutably and
  `normalize_inplace` it once per vertex before the branch, and carry the same
  `#2632` comment so the two producers document one shared precondition. Cheap
  (one `sqrt` per vertex on a cold path) and a no-op for already-unit input.

---

## Verified-Clean Dimensions (no new findings)

- **Dimension 1 — Stream Position Integrity** (beyond finding -01): every named
  regression guard re-derived from source against nif.xml this sweep.
  `#2562`/`#2563`'s nine controller fixes match nif.xml field-for-field (the
  `until="10.1.0.103"` `Data` declaration was extracted mechanically for all 76
  `NiSingleInterpController` descendants and cross-checked against the fix
  list; nine of the thirteen declaring types are covered — see finding -01 for
  the other four). `#2622`'s Starfield wetness/luminance re-split is
  corpus-verified in its own commit and consistent with nif.xml's
  `#BS_FO4_2#` = `130..=139` / `#BS_F76#` = `155` bands. `#2598`'s
  `min_vertex_bytes` mirrors `decode_bs_vertex_stream`'s read sequence
  correctly (`VF_UVS_2` / `VF_LAND_DATA` / `VF_INSTANCE` deliberately excluded,
  matching the loop). `#1606`'s `starfield_tail` still consumes *to
  `block_size`*, not a hardcoded length. `#688`/`#698` truncate-don't-`Err`
  recovery intact.
- **Dimension 2 — Version Gating**: `impl NifVariant` is still exactly
  `detect` + `bsver`. **Zero raw `bsver` integer literals remain in production
  parsers** — a mechanical scan for `bsver <op> <int>` outside `version.rs`
  returns only comments and `#[cfg(test)]` fixtures, so the three literals the
  08-16 report tracked (`#2424`, `#2425`, `#2597`) appear to have been
  resolved; recommend those three be verified and closed. All six per-game flag
  vocabularies in `shader_flags.rs` were **bit-for-bit diffed against nif.xml's
  `<bitflags>` tables** this sweep (`BSShaderFlags`, `BSShaderFlags2`,
  `SkyrimShaderPropertyFlags1/2`, `Fallout4ShaderPropertyFlags1/2`): every
  constant's bit index matches its nif.xml `<option bit=…>`, with the only
  divergences being naming normalization (`PARALLAX` vs
  `Parallax_Shader_Index_15`, `DECAL` vs `Decal_Single_Pass`,
  `GREYSCALE_TO_PALETTE_COLOR` vs `GreyscaleToPalette_Color`). `#2319`'s
  `fo3nv_f1` bit-22 correction and `#2580`'s Skyrim SLSF1 correction both hold.
- **Dimension 3 — Block Dispatch Coverage** (beyond finding -02): 253 live
  top-level arms, 0 `NiUnknown` on all seven games per the checked-in
  `block_coverage_baselines`. The four new `NiPreSplitDataController` dispatch
  names route correctly and their RTTI reaches `anim/entry.rs`'s
  string-dispatched embedded-controller extraction (`entry.rs:348-414`), whose
  `NiAlphaController` / `NiVisController` arms were previously unreachable dead
  code.
- **Dimension 5 — Collision & Shader Block Parsing**: **dispatch ↔ resolve
  parity re-verified in both directions** — 17 `bhk*Shape` dispatch names map
  onto 16 `downcast_ref` arms in `import/collision/shape.rs`
  (`bhkConvexTransformShape` is the deliberate alias onto `BhkTransformShape`),
  with no unreachable extras. `#2549`'s new non-collidable-layer decode is
  correct: value 15 is `OL_NONCOLLIDABLE` / `FOL_NONCOLLIDABLE` /
  `SKYL_NONCOLLIDABLE` in all three per-game `*Layer` enums nif.xml declares
  (verified mechanically), `HavokFilter`'s layout is `Layer:byte, Flags:byte,
  Group:ushort` in every band so the low-byte extraction is
  correct-by-construction, and FO4/FO76/Starfield use `SkyrimLayer`
  (`#BS_GT_FO3#`) so no fourth branch is needed. `BSLightingShaderProperty`'s
  three-way band dispatch keeps per-variant field isolation, and its FO4
  `shader_type == 1` arm reads `Environment Map Scale` *and* the two
  `#BS_FO4_2#` SSR bools (`shader.rs:1464-1484`), matching nif.xml.
  `#1652`'s `hkMotionType` full canonical mapping, `#1721`'s Starfield
  `!name.is_empty()` stub discriminator, `#1592`'s FO4 MSN + alpha-test
  OR-consumption, and the `CollisionAuthoring` 5-way discriminator (still
  correctly declining to claim `BhkSystemBinary` as decoded) all intact.
- **Dimension 6 — Allocation Hygiene**: 22 `#[must_use]` attributes present in
  `stream.rs` covering `allocate_vec` / `allocate_vec_sized` /
  `allocate_vec_min_bytes` / `read_pod_vec` / all twelve `read_*_array`
  wrappers. `bytemuck` re-confirmed **not** a workspace dependency. Nothing in
  this cycle's delta touched `stream.rs`, `ragdoll.rs`, the `Arc<str>` block-name
  interning, or `pre_parse_cell`'s serial/rayon split, and the 08-16 sweep
  verified all 13 guards against source; no re-derivation attempted here.
  OPEN **#3062** (`read_pod_vec` zero-init pre-fill is dead work) covers the one
  known remaining item — noted and skipped per dedup.

## Issue Tracker Status Changes (not new findings)

- **#2564** ("The Oblivion truncation baseline and the ROADMAP parse-rate row
  are stale by 5 files") — **still open, and its own number is now stale**. The
  live truncating set is **0**, not 1 and not 6:
  `crates/nif/tests/data/block_coverage_baselines/oblivion_truncations.tsv`
  reads `truncating=0 parsed=8032`, regenerated against real Oblivion mesh data
  by `17cb417d` on 2026-08-19. `docs/engine/nif-parser.md:7-9` and `:667` and
  `ROADMAP.md:413`, `:566`, `:1295` all still say `99.93% (8 026 / 8 032)` and
  "remaining 6". The correct row is **100% (8 032 / 8 032), 0 truncations**.
  Recommend updating the issue body with the new figure before closing it out.
- **#2997 / #2998 / #3085** (the three MEDIUM findings from the 2026-08-16
  sweep — FO4 slot 3 → POM over a palette LUT, FO4 slot 7 smooth-spec dropped,
  FO76 slot 6 dropped) — **all CLOSED and verified fixed at HEAD**.
  `86c41022` gave `slot_to_role` a `TextureSlotLayout` context
  (`slot_role.rs:101-120`), added `TextureRole::GreyscaleLut`, routed FO4/FO76
  slot 3 to it, made FO4 slot 7 unconditional `Specular`, and routed FO76 slot 6
  to `Specular` on measured `_s.dds` evidence. `7218f543` added the FO4
  FaceTint slot-4/5 cubemap + `TextureRole::Wrinkle` arms. `canonical_shader_type`
  now translates FO76's `BSShaderType155` at the import boundary, and
  `record_unrouted_texture_slot` makes future table gaps observable instead of
  silent — which is the structural half of the fix the 08-16 report asked for.
- **#2424 / #2425 / #2597** (raw `bsver` literals in production parsers) —
  a mechanical scan finds **zero** raw-integer `bsver` comparisons outside
  `version.rs` and test fixtures. Recommend verifying and closing all three.
- **NIF-D2-2026-08-07-02** (LOW, two sweeps old): this skill's Dimension-2
  helper enumeration is **now correct** — `.claude/commands/audit-nif/SKILL.md:72`
  explicitly names `has_skin_data_vertex_weights_flag` and the `#2168` addition.
  Resolved; not re-filed.

## Candidates Investigated and Disproved

Recorded so the next sweep does not re-derive them.

1. **`NiTexturingProperty` missing bump-map / decal version gates.** A
   mechanical nif.xml-vs-source scan flagged `Has Bump Map Texture`,
   `Bump Map Luma Scale/Offset` and the four `Has Decal N Texture` fields as
   unread. Disproved: `blocks/properties.rs:234-248` reads the bump TexDesc plus
   all six trailing floats, and `:300-304` implements both the `> 8/9/10/11`
   (v20.2.0.5+) and `> 6/7/8/9` (Oblivion) decal-slot formulas with an explicit
   4-slot ceiling (`#2004`). The scan false-positived on field-name spelling.
2. **`NiCamera.Use Orthographic Projection` / `NiSwitchNode.Switch Node Flags`
   (`since="10.1.0.0"`) unread.** Disproved: `blocks/node.rs:768-772` and
   `:409-413` both gate on `V10_1_0_0` explicitly.
3. **FO4 `BSLightingShaderProperty` `Use Screen Space Reflections` /
   `Wetness Control: Use SSR` unread.** Disproved: `blocks/shader.rs:1476-1482`
   reads both bools inside `FALLOUT4..FO4_DLC_UPPER`, matching nif.xml's
   `#BS_FO4_2#` = `130..=139` exactly.
4. **`NiFloatsExtraDataController` / `NiRollController` missing from the
   dispatch table.** True but not a finding: both would surface as `NiUnknown`,
   and `NiUnknown` is 0 on all seven games' checked-in baselines, so neither
   appears in any shipping corpus.
5. **Per-game shader-flag bit vocabularies drifting from nif.xml.** Disproved
   by exhaustive bit-index diff of all six `<bitflags>` tables against
   `shader_flags.rs` — every live constant matches; the only differences are
   name spelling.
6. **`#2549`'s single-check non-collidable layer needing a per-game branch.**
   Disproved: value 15 is `*_NONCOLLIDABLE` in `OblivionLayer`,
   `Fallout3Layer` and `SkyrimLayer` alike, and FO4+ uses `SkyrimLayer`.

## Prioritized Fix Order

Rendering-blocking first, then the gate, then animation/geometry polish:

1. **NIF-D3-2026-08-20-02** (re-baseline the six `per_block_baselines/*.tsv`).
   Cheapest fix in the list — one command on the machine with game data — and
   until it lands, *every other* NIF regression is masked. Do this first even
   though its own impact is indirect.
2. **NIF-D1-2026-08-20-01** (`NiPSys*Ctlr` `Data` ref + unconditional
   interpolator ref). The only stream-position defect found this cycle. Latent
   on vanilla, but it is the same class of desync that truncated
   `marker_map.nif`, and the fix is mechanically identical to the one
   `#2562`/`#2563` just landed next door.
3. **NIF-D4-2026-08-20-04** (normalize `N` in the Z-up producer). One line;
   completes `#2632` across both producers.
4. **NIF-D4-2026-08-20-03** (non-degenerating fallback seed). Bounded by the
   shader's Path-2 fallback, so lowest urgency — but land it with -04, same
   function pair.

No collision-parsing, version-gating, dispatch-coverage or allocation-hygiene
work is indicated this cycle.

## Scope Note

Per-game **material classification** (the `Imported* → translate_material`
boundary in `byroredux/src/material_translate.rs`, +315 lines this cycle,
almost entirely WATAL mesh-water routing) is owned by `/audit-nifal`, not this
audit. `byroredux/src/scene/nif_loader.rs`'s new mesh-water spawn block
(`is_water_shader` → `WaterPlane` / `WaterVolume`) consumes
`MaterialInfo::is_water_shader`, which `crates/nif` sets at exactly two sites
(`import/material/dedicated_shader.rs:612` for Skyrim+ `BSWaterShaderProperty`,
`import/material/legacy_properties.rs:639` for the FO3/FNV
`WaterShaderProperty`); both were verified correct against nif.xml this sweep,
and everything downstream of that flag belongs to `/audit-nifal` and
`/audit-renderer`.

## Reproduction

No corpus probe was run this cycle (cargo is barred for this suite — see the
method note at the top). To confirm findings -01 and -02 on a machine with game
data:

```sh
# Finding -02: expect ParsedShrank on NiSingleInterpController for 6 games
cargo test --release -p byroredux-nif --test per_block_baselines -- --ignored --nocapture
# then, to fix:
BYROREDUX_REGEN_BASELINES=1 \
  cargo test -p byroredux-nif --test per_block_baselines -- --ignored --nocapture

# Finding -01 needs a synthetic v10.1.0.103 fixture, not a corpus file —
# follow the convention in crates/nif/src/blocks/dispatch_tests/controllers.rs
```

TALLY: CRITICAL=0 HIGH=0 MEDIUM=2 LOW=2
