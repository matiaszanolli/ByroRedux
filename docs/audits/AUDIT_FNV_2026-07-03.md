# Fallout: New Vegas Compatibility Audit — 2026-07-03

**Command:** `/audit-fnv` (reference-title regression sweep, part of a `comprehensive`
21-audit suite run)
**Repo:** ByroRedux · branch `main` · HEAD `8498e559`
**Prior report:** `docs/audits/AUDIT_FNV_2026-07-02.md` (HEAD `1b4e8e84`, 32 commits ago)
**Scope:** all 8 dimensions — cell load, NIFAL translation, RT lighting, ESM parser,
NIF parser, animation/skinning/particles, PHYSAL ragdoll, real-data validation.
**Method:** single-session direct sweep (no sub-agent fan-out per task instructions).
Diffed `1b4e8e84..HEAD` for anything touching FNV-relevant code paths, re-verified
every regression guard and every finding from the prior report against current
source, ran the plugin + nif crate test suites, and cross-checked all findings
against `gh issue list` before reporting.

---

## Executive Summary

This is a **one-day delta** on top of a very recently comprehensive FNV sweep
(2026-07-02). Only 32 commits landed in the interim, and — critically for this
audit — **none of them touch the code paths behind the prior report's open
findings** (`crates/nif/src/import/collision.rs`, `crates/nif/tests/ragdoll_import.rs`,
`byroredux/src/cell_loader/exterior.rs` water-doc, `nif_import_registry.rs`
clip-handle path). One commit *did* land squarely in this domain:

- **`ffe9a816` (Fix #1718)** closes the bone-name-miss silent ragdoll drop
  (`FNV-D7-01` from an earlier sweep) — verified in place: `template_from_imported`
  now `log::warn!`s both a dropped-body list and a dropped-constraint list, mirroring
  the sibling #1539 diagnostic. Confirmed correct.

Everything else checked — the SCOL/SCPT/XCLL ESM dispatch guards, `havok_motion_type`
full-enum mapping, the dynamic BLAS budget (`device_local_bytes / 3`, floor 256 MB),
the Disney-BSDF gate (unreachable for FNV), the `skyTint` interior-ambient miss gate
(both raytrace.glsl sites), the single `translate_material` NIFAL boundary, and the
`MAX_NIF_NODE_DEPTH` walker guard — all held unchanged. `cargo test -p byroredux-plugin
--release` and `cargo test -p byroredux-nif --release` both pass clean with no new
failures.

**No CRITICAL or HIGH findings. No NEW findings this cycle.** The five other findings
from the 2026-07-02 sweep (`FNV-D7-02`, `FNV-D7-03`, `FNV-D7-04`, `FNV-D1-01`,
`FNV-D1-02`, `FNV-D1-03`) were already filed as GitHub issues **#1850–#1855** and
remain accurately described, unfixed, and OPEN — re-verified below rather than
re-reported. FNV's status as the reference title is intact; nothing broke a shipped
foundation in this window.

### Findings by severity

| Severity  | Count | IDs |
|-----------|-------|-----|
| CRITICAL  | 0     | — |
| HIGH      | 0     | — |
| MEDIUM    | 0 new (1 existing: #1850) | — |
| LOW       | 0 new (4 existing: #1851–#1855 excl. #1850) | — |
| **Total (new)** | **0** | |

No new report content beyond re-verification is warranted; see "Existing Findings
Re-Verified" below for the full carry-forward with current-source citations.

---

## Baseline Comparison (vs ROADMAP FNV row)

No GPU available in this session (static-analysis sweep, consistent with the prior
report's Dimension 8 method). ROADMAP baseline unchanged since 2026-07-02:

| Metric              | ROADMAP baseline (`1c26bc25`, R6a-stale-14, 2026-06-03) | This audit |
|---------------------|--------------------------------------|------------|
| Prospector entities | 3516                                 | not re-measured (no GPU) |
| Prospector draws    | 1224                                 | not re-measured |
| Prospector FPS      | 76.2 FPS / 13.11 ms                   | not re-measured |
| Prospector fence    | 11.12 ms                             | not re-measured |
| FalloutNV.esm parse | 100% (14 881 forms)                  | dispatch guards re-verified intact (SCOL/SCPT/XCLL/MOVS-PKIN-MSWP gating, no `unreachable_patterns` warnings on `cargo build -p byroredux-plugin`) |
| Plugin test suite   | 522 passed / 0 failed (2026-07-02)   | clean pass, no new failures (`cargo test -p byroredux-plugin --release`) |
| NIF test suite      | not run in prior report              | clean pass (`cargo test -p byroredux-nif --release`) |

> ROADMAP flags the bench-of-record as 437 commits stale as of Session 53 close
> (2026-07-01) pending an R6a-stale-15 refresh — unchanged this cycle, not a fresh
> finding of this audit.

---

## Existing Findings Re-Verified (carried forward, not re-reported)

All six findings from `docs/audits/AUDIT_FNV_2026-07-02.md` were published as issues
**#1850–#1855** (confirmed via `gh issue list`, all six still `OPEN`). Re-checked each
against current source (`HEAD 8498e559`); none of the underlying files changed in the
32-commit window, so each is reconfirmed byte-for-byte accurate:

- **#1850 (FNV-D7-02, MEDIUM)** — `bhkBreakableConstraint` still invisible to
  `extract_ragdoll`'s constraint loop (`crates/nif/src/import/collision.rs:357-360`
  downcasts only `BhkConstraint`); silent edge drop confirmed unchanged.
- **#1851 (FNV-D7-03, LOW)** — `crates/nif/tests/ragdoll_import.rs` real-data test
  still asserts only self-consistency (`ragdoll_joints + hinge_joints ==
  constraints.len()`), no floor pinning FNV's measured 18-body/17-joint reference.
- **#1852 (FNV-D7-04, LOW)** — `byroredux/src/ragdoll.rs:224` (seed) vs `:361`
  (writeback, line shifted +34 from the prior report's `:327` due to the #1718 warn
  blocks added above it, same logic) still de-composes with live `gt.scale` rather
  than a snapshotted activation-time scale.
- **#1853 (FNV-D1-01, LOW)** — `byroredux/src/cell_loader/exterior.rs` doc comment
  on `default_water_height` still stale (not touched this window).
- **#1854 (FNV-D1-02, LOW)** — `byroredux/src/cell_loader/references.rs:764` inserts
  before `:770-771` commits `pending_clip_handles` via `set_clip_handle` with no
  residency check; unreachable on FNV (no single cell nears `BYRO_NIF_CACHE_MAX`).
  Confirmed unchanged (the same-file #1798 commit today only added NPC-spawn timing
  earlier in the function, disjoint from this code).
- **#1855 (FNV-D1-03, LOW)** — `exterior.rs:309, 335` terrain/water spawn results
  still discarded via `let _ = ...` with no per-cell correlated warning.

None of these findings has regressed further or been fixed; none is newly discovered.
No action needed beyond what's already tracked on those issues.

---

## Regression Guards Re-Verified This Cycle

- **#1718 fix confirmed** (`ffe9a816`) — `template_from_imported`
  (`byroredux/src/ragdoll.rs:81-152`) now warns on both dropped-body (bone-name miss)
  and dropped-constraint conditions, phrased consistently with the sibling #1539
  diagnostic in `collision.rs::extract_ragdoll`.
- **SCOL FNV/FO3 dispatch (#1538)** — `is_scol_era = is_fo4_plus || Fallout3NV`
  (`crates/plugin/src/esm/records/mod.rs:198`) unchanged; `cargo build -p
  byroredux-plugin` emits zero `unreachable_patterns` warnings.
- **SCPT SCHR flags u16 (#1654)** — `crates/plugin/src/esm/records/script.rs:145`
  still reads via `u16_or_default`.
- **`havok_motion_type` full enum (#1652)** — unchanged at
  `crates/nif/src/import/collision.rs:145`, test `havok_motion_type_maps_full_enum`
  present.
- **Dynamic BLAS budget** — `compute_blas_budget`
  (`crates/renderer/src/vulkan/acceleration/predicates.rs:573-579`) still
  `(device_local_bytes / 3).max(MIN_BLAS_BUDGET_BYTES)`, floor 256 MB
  (`constants.rs:61`) — not a stale "1 GB" figure.
- **Disney-BSDF gate (#1248–#1252)** — `MAT_FLAG_PBR_BSDF = 32u`
  (`shader_constants.glsl:74`) unchanged; still unreachable for FNV (no BGSM authored).
- **skyTint interior gate (#1125)** — `raytrace.glsl:88` `missCol` ternary unchanged
  at both miss sites (`:133`, `:148`).
- **NIFAL single boundary** — no second `translate_material`/`Material` construction
  site introduced; `git log` shows no touches to `material_translate.rs` or
  `crates/core/src/ecs/components/material.rs` in the window.
- **`#1798` timing addition** (today, `crates/cell_loader/references.rs`) is disjoint
  from the #1854 clip-handle finding — it only wraps the NPC-spawn dispatch calls
  earlier in `load_references` with `Instant::now()` timing; does not touch the
  `pending_clip_handles` commit ordering.

---

## Commits Reviewed for FNV Relevance (`1b4e8e84..HEAD`, 32 total)

Touched FNV-adjacent paths and individually checked: `crates/plugin/src/esm/reader.rs`
(#1731, VWD flag parse/expose — general LOD flag, not FNV-specific, correctly scoped
to "parse only, wiring is LC-D7-01's job"); `byroredux/src/ragdoll.rs` (#1718, see
above); `byroredux/src/cell_loader/references.rs` (#1798, NPC-spawn timing, disjoint
from open findings); `crates/renderer/src/vulkan/acceleration/blas_static.rs` +
`tlas.rs` (#1793, documentation-only, no fix). Remaining 28 commits touch Starfield
BSGeometry (#1828/#1829), SpeedTree billboard PBR (#1819), the pex decompiler
(#1740/#1815/#1816), Vulkan renderer hot-path items (#1782/#1790–1799/#1804), and
skill-file refresh (`8498e559`) — none intersect FNV's live code paths.

---

## Method Note

No sub-agent fan-out was used this cycle (single-session direct sweep per task
instructions). Given the 2026-07-02 report's comprehensiveness and the narrow
32-commit window with no touches to any of its open-finding files, this audit's
value-add is the re-verification pass plus a fresh commit-by-commit relevance check
— both completed with no new findings surfaced.

**Suggested next step:** no new issues to publish this cycle;
`docs/audits/AUDIT_FNV_2026-07-02.md`'s findings (#1850–#1855) remain the actionable
backlog for this domain.
