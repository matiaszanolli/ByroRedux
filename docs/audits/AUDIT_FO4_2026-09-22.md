# Fallout 4 Compatibility Audit — 2026-09-22

**HEAD**: `ee6d3fb39` · **Baseline**: `docs/audits/AUDIT_FO4_2026-09-16.md`
(HEAD `7996edf61`) · **Audited**: all 5 dimensions of the current
`.claude/commands/audit-fo4/SKILL.md` · **Unchanged since baseline
(skimmed)**: Dimension 5 (Archives + real-data — no commits touched
`crates/bsa/src/ba2.rs`, `crates/nif/tests/parse_real_nifs.rs` or
`crates/nif/examples/nif_stats.rs` in the delta window).

**Method**: one auditor, no sub-agents, static reading plus targeted `cargo
test` runs (delta-first: budget spent on the four dimensions with commits
since 2026-09-16, per `_audit-common.md`). No engine launch, no smoke scripts,
no `--release`/workspace-wide builds. `/mnt/data/SteamLibrary/steamapps/common/Fallout 4/Data/`
confirmed present.

**Dedup baseline**: `/tmp/audit/issues.json` (4,569 issues, max #4681, cached
read-only) plus `docs/audits/` including today's/yesterday's sibling reports.
Cited per the task brief, not re-investigated (owned by other audits):
`AUDIT_NIFAL_2026-09-21b.md` NIFAL-D1-2026-09-21b-01 (HIGH, `.btr` distant
normal maps), `AUDIT_EXTERIOR_2026-09-21.md` EXT-D5-2026-09-21-01 (HIGH, WATR
wind angle), `AUDIT_CHARACTER_2026-09-21.md` CHAR-D4-01/02, `AUDIT_ESM_2026-09-21.md`
ESM-D2-02 (MEDIUM, terminal text), `AUDIT_PARSERS_2026-09-21.md`
PAR-D5-2026-09-21-02 (HIGH, BGSM `specular_enabled=false`), `AUDIT_NIF_2026-09-21.md`
NIF-D4-2026-09-21-02 (skin bone transform sanitizer). Also checked today's
`AUDIT_AUDIO/_FNV/_FO3/_LEGACY_COMPAT/_PAPYRUS/_REGRESSION/_SAVE/_SCRIPTING/_SPEEDTREE/_TECH_DEBT/_TOOLING_2026-09-22.md`:
none contain a new FO4-owned finding this dimension set would duplicate
(`AUDIT_REGRESSION`'s FO4 guard-test list corroborates the fixes below;
`AUDIT_SPEEDTREE`/`AUDIT_FNV`/`AUDIT_LEGACY_COMPAT`/`AUDIT_AUDIO` mention FO4
only in passing, on paths this skill does not own).

## Executive Summary

This is a **clean regression pass**: zero new findings across all 5
dimensions. The heavy activity in the 22-commit delta window is almost
entirely fixes to the *previous* FO4 audit's own findings, landing within
2-5 days of that report:

- **HIGH #4424** (`_s.dds` bound as both gloss and specular-colour, zeroing
  the blue channel of every FO4 direct highlight) — **fixed** by `ee47452ca`:
  FO4 texture-set slot 7 now routes to a new `SmoothSpec` role alongside the
  BGSM `smooth_spec_texture`, exactly as the baseline's suggested fix asked.
- **HIGH #4425** (BGEM merge replaced the NIF's `BsEffectShaderData`
  wholesale, erasing SLSF1 palette bits on 362 vanilla effect shapes) —
  **fixed** by `e9b165f25`: the arm now mutates the existing payload in
  place and OR's in its own enable bits instead of overwriting/skipping.
- **LOW #4434** (REFR overlay resolver routed BGSM roles back through
  `BSShaderTextureSet` wire slots, so a modded displacement map could land
  as the palette LUT) — **fixed** by `523c0d908`: the overlay now carries
  four named-role fields independent of the slot table.
- **Related #4402 / #4400** — both fixed (`ce65cd8d0`, `efb6d9921`),
  restoring #2108's greyscale-slot precedence rule and the MSWP re-merge
  texture snapshot respectively.
- **LOW #4436 (skill doc-rot)** — fixed: Dimension 5's path now points at
  `crates/nif/src/blocks/shader/lighting.rs`; `_audit-validate.sh` is clean
  for `audit-fo4`.

Every one of the baseline's 5 findings is now closed and verified fixed in
current code. Guard tests grew from 265 to **277** passing under the same
`cargo test -p byroredux --bin byroredux -- bgsm merge overlay precomb scol
pkin attach pack_imported material_translate csg gloss` filter.

**One MEDIUM remains open, unchanged, correctly not re-reported**: **#4428**
— the BGSM merge still binds `envmap_texture` unconditionally
(`merge.rs:691-697`), ignoring `environment_mapping`, wasting RT reflection
rays and zeroing reflections on conductors. Re-read against current code;
still present exactly as described.

**Positive note for issue-tracker reconciliation**: **#4248** (FO4-D9-01,
"`attach_container_inventory` never expands leveled-list entries") is listed
OPEN in `/tmp/audit/issues.json` but its fix appears to already be in code —
`3cbf3674a` added `expand_leveled_loot` and wired it into
`attach_container_inventory`, game-agnostically, with a test
(`container_loot_resolves_lists_using_live_player_level`) matching #4248's
own completeness checklist. See Dimension 4 below. This is not a new
finding; flagged so the closeout pass can reconcile the tracker.

Parse rate and BA2 reader are unchanged (no commits touched the reader or
the real-data test since 2026-09-16); the 235,082/235,082 100.00% clean
figure from the baseline stands un-re-measured this session (delta-first
budget spent on the 4 changed dimensions — see Dimension 5 coverage note).

## Severity Summary

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 0 |
| **Total new/regression findings** | **0** |

Matched-existing (found and verified, not re-reported): 1 open (#4428,
MEDIUM). Confirmed fixed since baseline: 5 (#4424, #4425, #4434, #4402,
#4400) plus 1 skill-doc LOW (#4436-equivalent path fix). Positive-resolution
note for tracker reconciliation: 1 (#4248, currently OPEN but appears fixed).

## Dimension Results Overview

| # | Dimension | Commits since baseline | Result |
|---|---|---|---|
| 1 | M49 precombines (CSG + decode + spawn) | 11, none precombine-specific | HOLD. All named guard tests present; opaque-architecture blend restore intact after the #4514/#4529 `MaterialTextureHandles` producer unification. 0 findings. |
| 2 | BGSM/BGEM consumption + metalness | 3 | HOLD, with 3 of the baseline's findings fixed in place (#4425 HIGH, #4402/#4400 related-open) and #4428 MEDIUM confirmed still open. 0 new findings. |
| 3 | NIF BSVER 130 / half-float / collision | 4 | HOLD. Tangent pre-size perf fix correct and bounded (#4206); `BhkPlaneShape` comment fix is Skyrim-scoped; detail-neutral feature (#4422) explicitly excludes FO4 by test. Both FO4 collision arms present, 143/143 collision tests green. 0 findings. |
| 4 | ESM architecture records + cell expansion | 22 (mostly cross-cutting) | HOLD. SCOL/PKIN/attach-point guards green (49+9+3+17 tests). TXST TX* catch-all arm correctly ordered after FO4's TX00-TX07, no `unreachable_patterns`. #4248 appears fixed (see above). 0 new findings. |
| 5 | Archives + real-data + forward scope | 0 (core paths untouched) | HOLD, unchanged since baseline — skimmed. BA2 reader untouched; fast test suite green. Skill doc-rot (path to `blocks/shader/lighting.rs`) already fixed. 0 findings. |

**Tests run this session**: `cargo test -p byroredux-bgsm` (30 passed + 2
doctests, 2 real-data `--ignored`), `cargo test -p byroredux-bsa` (2
doctests, 1 real-data `--ignored`), `cargo test -p byroredux-nif --lib
collision::` (143 passed), `cargo test -p byroredux --bin byroredux -- bgsm
merge overlay precomb scol pkin attach pack_imported material_translate csg
gloss` (277 passed), `-- scol_expansion pkin_expansion attach_points
refr_texture_overlay` (49+9+3+17 passed), `-- container_inventory
container_loot` (10 passed), `cargo build -p byroredux-plugin -j 4` (clean,
no warnings). All green, 0 failures.

---

## Dimension Findings

No new or regression findings this session. Full per-dimension detail
(commits reviewed, guards re-verified, fix-in-place evidence) is in
`/tmp/audit/fo4/dim_1.md` through `dim_5.md`.

---

## BGSM Consumption Table (updated)

Texture columns name `MaterialTextureSet` roles, not FO4 slot numbers.
Changes from the 2026-09-16 table are **bold**.

| BGSM field (v2 unless noted) | Into `ImportedMaterial` (`merge_external_material`) | On canonical `Material` / GPU (post-NIFAL) | Notes this audit |
|---|---|---|---|
| `diffuse_texture` | `textures.base_color` (first-wins; NIF slot 0 usually wins) | `baseColor` handle | OK |
| `normal_texture` | `textures.normal` | normal handle; MSN bit via `forward_bgsm_phase1_flags` | OK |
| `smooth_spec_texture` | `textures.smooth_spec` | **`glossMapIndex`; FO4 slot 7 now also lands in `SmoothSpec`, not `Specular`** | **FIXED (#4424)** — see dim_2.md / dim_1.md |
| `specular_texture` (v>2) | `textures.specular` | `specularMapIndex` | 0 vanilla |
| `greyscale_texture` | `textures.greyscale_lut` + `bgsm_greyscale_lut_*` (**precedence restored to #2108's winner-takes-the-bit rule, #4402**) | `greyscaleLutIndex` + `EFFECT_PALETTE_COLOR` | OK |
| `glow_texture` | `textures.emissive` (ungated by `glowmap`) | `glowMapIndex` | **overlay routing now named-role (#4434 fixed)** |
| `envmap_texture` (v≤2) | `textures.environment` (**still ungated — #4428 open, unchanged**) | `envMapIndex` → explicit env, strength = `env_map_scale` | **Existing: #4428 (MEDIUM, OPEN)** |
| `inner_layer_texture` (v≤2) | `textures.inner_layer` | used only on `MATERIAL_KIND_MULTI_LAYER_PARALLAX` | unchanged |
| `wrinkles_texture` | `textures.wrinkle` | preserved, unsampled | unchanged |
| `displacement_texture` (v≤2) | `textures.height` → POM | `parallaxMapIndex` | **overlay routing now named-role (#4434 fixed)** |
| `lighting_texture` / `flow_texture` (v>2) | `textures.lighting` / `textures.flow` | preserved, unsampled | unchanged |
| `distance_field_alpha_texture` (v≥17) | not forwarded (#2642) | — | unchanged |
| Specular colour/mult | `specular_color` / `specular_strength`; metalness from saturation (non-pbr) or luminance (pbr) | `Material::metalness` | #1476 holds |
| Smoothness | `roughness_override = (1 − s).clamp(0.04, 1)`; `glossiness = s·100` | `Material::roughness` | #3639/#3905 hold |
| Emittance | gated on `emit_enabled` | emissive lane | OK |
| Translucency (v≥8) | subsurface suite | `TRANSLUCENCY*` bits | 0 v≥8 files in vanilla |
| `environment_mapping` / mask scale | `env_map_scale` (gated) | `multi_layer_envmap_strength` | texture still not gated (#4428) |
| Alpha blend / test, two-sided, decal | child-first / OR | blend state, markers | precombine path restores NIF blend, intact |
| **BGEM** base/normal/glow/env(+mask, gated)/specular/lighting/glass overlays | named roles | handles | env gate OK (#2643) |
| **BGEM** palette bits / soft / falloff | **updates the existing `effect_shader` in place; NIF SLSF1 bits preserved; own bits OR'd in (#4425 fixed)** | `EFFECT_PALETTE_*`, `EFFECT_SOFT`, falloff | **FIXED (#4425)** |

## Forward Scope Chain

Unchanged from baseline, except #4248 (see below):

1. `_precomb.nif` / `_physics.nif` collision. Blocked on Havok `hknp` decode
   (the `4dca737e2` Havok-branding rename is naming-only, no functional
   change to this blocker).
2. `.uvd` occlusion payload (#3810, open).
3. MOVS physics runtime (parse-only, unwired).
4. LIGH power state.
5. **CONT leveled-item expansion — appears fixed by `3cbf3674a`
   (`expand_leveled_loot`), game-agnostic, tested. Issue #4248 is still OPEN
   on the tracker; recommend closing/verifying at the next publish pass
   rather than continuing to list it as forward scope.**

Do not list the CSG reader, the BGSM parser, SCOL/PKIN expansion, or the
FO4-D5/D2/D9 baseline findings as pending — all confirmed fixed this session.

## Coverage Notes

- One auditor, no sub-agents, per the task's explicit constraint.
- Delta-first scoping: Dimension 5's core paths (BA2 reader, real-data parse
  test) had zero commits since baseline, so the full 235,082-NIF census was
  not re-run this session (unchanged reader + unchanged on-disk data gives
  no expected drift); fast/cheap tests for that crate were run as a spot
  check instead. Dimensions 1-4 got full commit-by-commit review with
  targeted `cargo test` verification.
- Scaleform FO4 HUD (`--hud`, `byroredux/src/scaleform_hud.rs`) shipped this
  window (`62fc0bf22`) — explicitly out of scope per the skill (owner:
  `/audit-ui`).
- Un-owned subsystems (gameplay slice beyond the container/leveled-loot spot
  check, SDK, launcher, mod runtime) were not examined.

## Suggested Publish

```
/audit-publish docs/audits/AUDIT_FO4_2026-09-22.md
```
No new findings to label/file this session. Recommend a manual check on
whether #4248 should be closed (fix appears already merged).
