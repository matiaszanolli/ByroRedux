# AUDIT — SCALEFORM / SWF UI (R4 + M48) · 2026-08-27

Full comprehensive sweep (no `--focus`), depth **deep**, run solo (no
sub-agent fan-out — nested-agent result relay is unreliable in this repo).
Part of a `/audit-suite --preset comprehensive` run. HEAD `969d81c8`.

`cargo test -q -p byroredux-ui`: **53 passed, 0 failed, 2 ignored** (55
total). The 2 ignored are `installed_skyrim_hudmenu_loads_with_avm1_profile`
and `installed_fallout4_representative_menus_obey_host_object_lifecycle`
(`crates/ui/src/host/tests.rs:571`, `:589`). Note that the *default* suite
already requires a real Vulkan/wgpu adapter — `resource_loads_tests` and
`render_failure_tests` both call `SwfPlayer::new`, which goes through
`shared_descriptors()`. On a GPU-less CI box those fail rather than skip.

Prior report: [`AUDIT_UI_2026-08-24.md`](AUDIT_UI_2026-08-24.md) (2 LOW
findings, 6 fixed-but-open, 6 still-open). Delta scoped with
`git log --since=2026-08-23 -- crates/ui/ byroredux/src/{ui_input,app_events,app_frame,scene}.rs docs/engine/ui.md docs/smoke-tests/`.
Four commits touch this surface:

- `7390fe4c` — `Fix #2722` (failed capture no longer publishes stale
  pixels), `Fix #2723` (documented fixed-at-load viewport, deleted dead
  `UiManager::close`), `Fix #2965` (AVM1 request-ID strip now gated on real
  `GameDelegate` evidence).
- `ee03f5c6` — `Fix #3272`: refresh UI test inventory (closes the 08-24
  `UI-D4-04` doc-rot finding).
- `fdba763d` — `Fix #3273`: `docs/smoke-tests/m48-menu-load.sh`, the
  archive-backed `--menu` gate (closes the 08-24 `UI-D5-04` finding).
- `3aebf414` — smoke-test fixture refactor (m48 was **not** migrated; see
  UI-D5-2026-08-27-08).

---

## 1. Executive summary

**8 NEW findings: 0 CRITICAL · 1 HIGH · 4 MEDIUM · 3 LOW.**

Both 08-24 findings are **closed**. Three tracker issues (#2722, #2723,
#2965) closed properly this cycle. Four (#3149, #3151, #3155, #3156) remain
OPEN in the tracker but are still verified fixed in code — the 08-24
recommendation to close them as a paperwork gap stands, unchanged.

| Dim | Area | New | Severity |
|---|---|---|---|
| 1 | Profile & VM Selection | 1 | LOW |
| 2 | Host Bridge Transport | 1 | LOW |
| 3 | AVM2 Adapter Injection | 3 | 2 MEDIUM, 1 LOW |
| 4 | Catalog Fidelity & Drift | 0 | — |
| 5 | Resource Navigator | 1 | LOW |
| 6 | Render & Device Lifecycle | 2 | 1 HIGH, 1 MEDIUM |
| 7 | Engine Wiring & Input Routing | 0 | — |

Plus one MEDIUM that spans Dim 5/6/7 (the `--menu` gate blind spot) — counted
under Dim 5.

### Re-derived catalog counts (Phase 1 step 6 — counted, not trusted)

```
SKYRIM_SKYUI_METHODS              74 entries  (62 command, 12 request)
FALLOUT4_BGS_CODE_OBJECT_METHODS 269 entries (236 command, 33 request)
```

Both match every number quoted in `docs/engine/ui.md`
(:17, :19, :402, :422, :467, :468) and `ROADMAP.md` (:47, :769, :770,
:1116). **Dimension 4 is clean this cycle** — no catalog drift.
`docs/engine/ui.md`:531's "53 default tests plus 2 ignored" now matches the
measured 53+2 exactly (#3272).

**Correction to this skill's own text**, not a code finding:
`audit-ui/SKILL.md` Dim 4 says the catalog is "documented as case-preserving
with case-insensitive lookup". It is not. `ScaleformHostCatalog::find`
(`crates/ui/src/catalog.rs:81-86`) is a case-**sensitive**
`binary_search_by(|method| method.name.cmp(name))`, and that is *required*:
the FO4 array deliberately carries `CloseMenu`/`closeMenu`,
`PlaySound`/`playSound`, `GetButtonFromUserEvent`/`getButtonFromUserEvent`
as distinct entries. `docs/engine/ui.md`:427 says only "The catalog
preserves case" — the doc is right and the skill line is the drift. There is
exactly one normalization point because there is none.

### Host Contract Matrix

| | Skyrim AVM1 | Fallout 4 AVM2 |
|---|---|---|
| Detection | ✅ movie evidence only (`is_action_script_3()`); no `--game`/path inference in any of the three constructors | ✅ same |
| Forced-profile override | ✅ hard `anyhow!` on mismatch (`player.rs:188-193`, `:214-219`) | ✅ same |
| Host object | ✅ n/a by design (`NotRequired`) | ⚠️ four-state; **but the state never reaches the engine** — UI-D3-…-02 |
| Transport | ✅ `ExternalInterface` + request-id + `respond` re-entry; #2965 gate verified live | ✅ `BGSCodeObj.X` normalized in one helper (`host.rs:533-547`) |
| Catalog | ✅ 74/12, matches docs | ✅ 269/33, matches docs + ROADMAP |
| Corpus-measured | ❌ still never (#3103 — no AVM1 bytecode scanner exists anywhere in `crates/ui/src/`) | ✅ 311 movies, both directions, **not** `#[ignore]`d (self-skips) |
| Live engine consumer | ⚠️ loose-file `--swf`, plus `--menu` (profile-agnostic) | ✅ `--menu`/`--menu-archive`, now smoke-gated (#3273) |
| Composited correctly | ❌ **UI-D6-…-01** — tone-mapped/fogged/upscaled (both profiles) | ❌ same |
| Diagnostic dedup | n/a | ✅ `(menu_name, method)` keyed + capped (`app_frame.rs:33-35`, `:283-308`) |

### Profile(s) actually traced end-to-end

**Fallout 4 / AVM2**, statically, archive → pixels → GPU:
`scene.rs::archive_menu_args` → `Archive::open` → `archive.extract` →
`ScaleformProfile::detect` → `UiManager::load_swf_from_resource_provider` →
`SwfPlayer::from_resource_provider` → `inject_host_object_adapter`
(`build_adapter_abc` + `patch_root_constructor`) →
`ScaleformNavigatorRuntime::create` → `drive_archive_preload` →
`app_frame.rs` tick → `drain_host_calls` → `UiFrame` three-state →
`TextureRegistry::update_rgba` → `geometry_pass.rs:620` UI quad →
composite → bloom → TAA → FSR → `presentation.frag`.
Every operand-stack depth in the four generated method bodies was
hand-verified against its declared `max_stack` (forwarder peak 3/declared 3;
installer 3/3; destroy 2/2; init 3/3; ready 1/1) — no headroom defect found.

**Skyrim / AVM1** traced through the transport half only; the only installed
Skyrim evidence is an `#[ignore]`d profile-detection assertion (#3103 stands).

---

## 2. Findings

### HIGH

#### UI-D6-2026-08-27-01: the Scaleform overlay is blended into the render-resolution HDR G-buffer, so every menu is fogged, bloomed, exposure-scaled, ACES tone-mapped, TAA-accumulated and FSR-upscaled

- **Severity**: HIGH
- **Dimension**: Render Path & Device Lifecycle
- **Profile**: both
- **Location**: `crates/renderer/src/vulkan/pipeline.rs:930-956` ·
  `crates/renderer/src/vulkan/context/geometry_pass.rs:606-620` ·
  `crates/renderer/src/vulkan/context/helpers.rs:148-190` ·
  `crates/renderer/shaders/presentation.frag:160`
- **Status**: NEW
- **Description**: The UI quad is drawn at the tail of the *main geometry
  render pass*, alpha-blended into **color attachment 0 — the HDR
  direct-lighting G-buffer target**, at render (not output) resolution. Every
  post-process stage downstream therefore operates on the menu as if it were
  world radiance: height-fog / volumetric transmittance (keyed off the world
  depth still present under the UI quad, since the UI pipeline sets
  `depth_write_enable(false)`), underwater god rays, the M58 bloom add, TAA
  temporal accumulation with a **zero** motion vector and **no** FSR reactive
  or transparency mask, FSR upscaling, and finally
  `aces(graded * params.exposure)` in `presentation.frag`.
- **Evidence**: The blend state is unambiguous —
  ```rust
  // crates/renderer/src/vulkan/pipeline.rs:930
  let ui_hdr_blend = vk::PipelineColorBlendAttachmentState::default()
      .color_write_mask(vk::ColorComponentFlags::RGBA)
      .blend_enable(true)
      .src_color_blend_factor(vk::BlendFactor::SRC_ALPHA)
  ```
  and the code already names the fix it does not implement:
  ```rust
  // crates/renderer/src/vulkan/pipeline.rs:949-952
  // The Scaleform overlay writes no FSR mask. Marking it reactive
  // would only paper over the real fix, which is moving the overlay
  // out of the render-resolution pass entirely so it is composited
  // after upscale and never enters temporal reconstruction.
  ```
  The tone-map is unconditional on the whole upscaled image:
  `vec3 presented = aces(graded * params.exposure);`
  (`presentation.frag:160`). The Narkowicz ACES fit maps linear `1.0` to
  `2.54/3.16 ≈ 0.80`, so — before any exposure scaling — **pure white menu
  text and white UI chrome reach the swapchain at ~80 % grey**, and the
  0.18 mid-grey a font renderer emits lands at ~0.267. The upload compounds
  this: `TextureRegistry::update_rgba` → `Texture::from_rgba`
  (`crates/renderer/src/vulkan/texture.rs:88`) declares
  `vk::Format::R8G8B8A8_SRGB`, so Ruffle's sRGB-encoded `Rgba8Unorm` capture
  is linearised by the sampler on the way in and then re-encoded by a curve
  (ACES) that is not the sRGB OETF.
- **Impact**: Every Bethesda menu this layer can load renders with wrong
  luminance and saturation, ghosts under TAA when it animates (no motion
  vector, no reactive mask), blurs under FSR reconstruction, and — in an
  exterior cell with fog — is attenuated by the fog term belonging to the
  world geometry behind it. This is not a cosmetic nicety: it is the concrete
  blocker under the "font fidelity" row `docs/engine/ui.md` lists as Pending,
  and it makes any future menu-fidelity comparison against vanilla
  meaningless until it moves. It also means the `--menu` smoke gate can only
  ever prove "a menu loaded", never "a menu looks right".
- **Related**: none filed — a full-text search of the 300-issue list and of
  `docs/audits/` finds no issue or prior finding for the overlay's
  compositing stage. `docs/engine/ui.md`:286 ("The UI is drawn at the tail of
  the main render pass") states the fact but none of its consequences.
  Cross-audit: the *pass ordering* belongs to `/audit-renderer`, but the
  defect is UI-only, so it is reported once here per `_audit-common.md`.
- **Suggested Fix**: Move the UI quad out of the geometry pass into its own
  post-`presentation.frag` composite (native output resolution, no
  tone-map, no temporal history), and upload the Ruffle capture as
  `R8G8B8A8_UNORM` once it is no longer being linearised for an HDR target.
  This is the fix `pipeline.rs:949-952` already describes.

### MEDIUM

#### UI-D3-2026-08-27-02: `ScaleformHostObjectState` has no engine consumer — a menu that loaded with no host object at all logs identically to one that injected cleanly

- **Severity**: MEDIUM
- **Dimension**: AVM2 Adapter Injection
- **Profile**: `Fallout4Avm2`
- **Location**: `crates/ui/src/avm2_host.rs:104-106` ·
  `crates/ui/src/player.rs:500-502` · `byroredux/src/scene.rs:1560-1568`
- **Status**: NEW
- **Description**: `/audit-ui` Dim 3's checklist requires: "A movie that
  never creates `BGSCodeObj` must land in `NotPresent` **and be visible to
  the engine** — not silently look identical to success." It lands in
  `NotPresent` correctly, and it is not visible to the engine. The
  `NotPresent` return is completely silent:
  ```rust
  // crates/ui/src/avm2_host.rs:104
  if !declares_contract {
      return Ok((swf_data.to_vec(), ScaleformHostObjectState::NotPresent));
  }
  ```
  no `log::`, no counter. And `SwfPlayer::host_object_state()` has **no
  caller outside `crates/ui`** — `grep -rn "host_object_state" byroredux/
  crates/renderer/ tools/` returns nothing. `UiManager` does not re-export it
  either, so the engine could not read it without a new accessor.
- **Evidence**: The `--menu` route's only success observable is
  `byroredux/src/scene.rs:1560-1566`:
  ```rust
  log::info!(
      "ui.menu: loaded path={} archive={} profile={:?} texture={:?}",
      menu_path, archive_path, profile, handle
  );
  ```
  — profile and texture handle, no host-object state. An AVM2 menu that
  reaches `NotPresent` prints exactly this line, loads, renders pixels, and
  answers no host call ever, with zero log evidence of why. The sibling
  states *are* logged (`avm2_host.rs:222-225` for
  `AdapterInjectedWithoutDestroyHook`, `:188-194` for uncataloged
  forwarders), which makes the silent one the odd case out.
- **Impact**: The single hardest UI failure to diagnose — "the menu draws but
  nothing responds" — is exactly the one with no signal. It is also the shape
  a future DLC/mod menu will most likely take.
- **Related**: #3149 (the same class of invisibility for the destroy hook,
  fixed by making the state distinguishable — this is the half that was left
  unwired), UI-D5-2026-08-27-05.
- **Suggested Fix**: Add `UiManager::host_object_state()` and fold it into
  the `ui.menu: loaded` line (`state={:?}`), or `log::warn!` on
  `NotPresent` for an AVM2 profile inside `inject_host_object_adapter`.

#### UI-D3-2026-08-27-03: a lifecycle-class scan miss is a hard menu-load failure, while the sibling scan over the same ABC tags is deliberately non-fatal

- **Severity**: MEDIUM
- **Dimension**: AVM2 Adapter Injection
- **Profile**: `Fallout4Avm2`
- **Location**: `crates/ui/src/avm2_host.rs:99-153` (esp. `:127-129` and
  `:150-151`) vs `crates/ui/src/avm2_host.rs:175-185`
- **Status**: NEW
- **Description**: `inject_host_object_adapter` decides a movie "declares the
  contract" with a raw byte scan (`contains_bytes(abc, b"BGSCodeObj") &&
  contains_bytes(abc, b"onCodeObjCreate")`, `:99-103`), then requires an
  `abc.instances` entry declaring *both* as traits. Two ways that second
  step can miss on legal ABC, and both are a hard `Err`:
  1. `:150-151` — `"Fallout 4 BGSCodeObj lifecycle class was not found"` when
     the byte scan hit but no instance carries both traits (strings present
     only as constants / in a different class / declared as **static class**
     traits, since the loop walks `abc.instances` and never `abc.classes`).
  2. `:127-129` — `Reader::new(data).read().map_err(…)?` propagates, so a
     single ABC tag anywhere in the movie that this reader cannot parse
     aborts the whole injection.
  Every `SwfPlayer` constructor turns that `Err` into a refused menu
  (`player.rs:174-175`, `:195-196`, `:221-222`).
- **Evidence**: The identical parse over the identical tags, ~25 lines later,
  is explicitly non-fatal:
  ```rust
  // crates/ui/src/avm2_host.rs:175-185
  let referenced = match referenced_host_methods_in_tags(&movie.tags) {
      Ok(referenced) => referenced,
      Err(error) => {
          log::warn!("Fallout 4 host-call inventory scan failed ({error}); …");
          BTreeSet::new()
      }
  };
  ```
  with the reasoning "refusing to load a menu because its bytecode has a
  shape the scanner cannot walk would be strictly worse than the pre-fix
  behaviour" (`:172-174`). The hard-failure behaviour is currently *pinned*
  by `marker_without_a_lifecycle_class_is_rejected` (`:1746-1770`), which
  asserts `.unwrap_err()`.
- **Impact**: This is the same failure shape as #2963/#3149 one level up. There,
  an over-strict three-trait class predicate made four real shipped FO4 menus
  (DialogueMenu, MultiActivateMenu, SPECIALMenu, Terminal) fail to load
  entirely; the fix relaxed the predicate but left the *policy* — miss means
  refuse — untouched. The `NotPresent` state that exists precisely for
  "this movie has no host object" is unreachable from either branch. The FO4
  base-game corpus (311 movies) passes today, so this is latent, not live:
  DLC, Creation Club and mod menus are the exposure.
- **Related**: #2963, #3149, UI-D3-2026-08-27-02.
- **Suggested Fix**: Degrade both branches to `ScaleformHostObjectState::NotPresent`
  with a `log::warn!` naming the movie, matching `:175-185`'s policy; keep the
  hard `Err` only for `patch_root_constructor` failures, where a *partial*
  rewrite really would hand Ruffle a corrupt SWF. Update
  `marker_without_a_lifecycle_class_is_rejected` to assert the degraded state
  rather than the error.

#### UI-D6-2026-08-27-04: an animating menu allocates a fresh full-viewport `VkImage` and blocks on a fence every frame, ahead of `draw_frame`

- **Severity**: MEDIUM
- **Dimension**: Render Path & Device Lifecycle
- **Profile**: both
- **Location**: `crates/renderer/src/texture_registry.rs:1596-1645` →
  `crates/renderer/src/vulkan/texture.rs:71-99` → `:114-131` ·
  `byroredux/src/app_frame.rs:316-333`
- **Status**: NEW
- **Description**: `TextureRegistry::update_rgba` does not update an image
  in place. It builds a **new** `Texture` via `Texture::from_rgba` →
  `from_dds_with_mip_chain`, whose own doc says it "[r]ecords the upload into
  a one-time command buffer, **submits + fence-waits ONCE**, then releases
  the staging buffer", swaps it in, and pushes the previous image onto a
  `pending_destroy` ring drained after `MAX_FRAMES_IN_FLIGHT` (2) frames.
- **Evidence**:
  ```rust
  // crates/renderer/src/texture_registry.rs:1620-1631
  let new_texture = Texture::from_rgba(ctx, width, height, pixels, …)?;
  if let Some(prev) = entry.texture.replace(new_texture) {
      entry.pending_destroy.push_back((current_frame_id, prev));
  }
  ```
  #2719 fixed the *unconditional* dirty flag, so a static menu no longer pays
  this. But `SwfPlayer::render` returns `Some` whenever the captured pixels
  differ from the last upload (`player.rs:470-484`), and a real Bethesda HUD
  (compass, health/AP bars, radiation ticks) differs every single frame.
- **Impact**: Per frame, per animating menu: one full-viewport `VkImage` +
  `VkImageView` create/destroy pair (8.3 MB at 1920×1080, three live copies
  under the 2-frame destroy ring), a staging upload, and a **blocking
  `vkWaitForFences` on the main thread ahead of `draw_frame`**. This is
  allocator churn and a serialisation point in the frame, not a leak — the
  ring does drain — but it is the single most expensive thing the UI does
  and `/audit-performance` has no dimension that would surface it.
- **Related**: #2719 (fixed the always-dirty half of the same cost),
  UI-D6-2026-08-27-06.
- **Suggested Fix**: Give the registry a genuine in-place path for
  dynamic RGBA entries — persistent image + per-FIF staging ring +
  `cmd_copy_buffer_to_image` recorded into the frame's own command buffer —
  instead of recreate-and-defer-destroy. Pairs naturally with
  UI-D6-2026-08-27-01's move of the overlay out of the geometry pass.

#### UI-D5-2026-08-27-05: the new `--menu` smoke gate passes a menu that loaded with every `ImportAssets` dependency missing or with a stalled preload

- **Severity**: MEDIUM
- **Dimension**: Resource Navigator / Engine Wiring
- **Profile**: both
- **Location**: `docs/smoke-tests/m48-menu-load.sh:105-126` ·
  `crates/ui/src/player.rs:251-265`, `:543-566` ·
  `crates/ui/src/navigator.rs:205-218`
- **Status**: NEW
- **Description**: `#3273`'s gate (added `fdba763d`, the newest code in this
  audit's scope) asserts exactly two things: that `ui.menu: loaded` appears,
  and that none of the five `--menu`-route failure arms in `scene.rs` logged.
  Every failure mode *inside* the UI crate is invisible to it. By design
  (#2720), a dependency that cannot be fetched is **not** fatal — it is
  answered with `placeholder_movie()` and recorded — and an unsettled preload
  is advanced anyway after a 60-frame grace. So a menu whose entire font and
  symbol graph is missing (wrong archive, cross-archive import — the
  navigator holds exactly one archive, and `--menu-archive` takes exactly one
  path) loads, prints `ui.menu: loaded`, draws an empty stage, and the gate
  reports PASS.
- **Evidence**: The gate's failure vocabulary
  (`docs/smoke-tests/m48-menu-load.sh:110-126`) is
  `"Failed to open UI archive"`, `"Failed to extract archive menu"`,
  `"Failed to detect Scaleform profile"`, `"Failed to load archive menu"`,
  `"Failed to register UI texture"` — all five from `scene.rs`. It never
  greps for `record_resource_errors`' `log::error!("{error}")`
  (`player.rs:563`), which is where
  `"Scaleform resource {archive_path:?} was not found in the configured
  archive"` (`navigator.rs:255-257`) surfaces, nor for
  `"Scaleform archive preload for {movie_path:?} did not settle"`
  (`player.rs:258-262`). Both are already on stderr at `error`/`warn`; only
  the greps are missing. The ignored FO4 corpus test does assert
  `player.resource_error() == None` (`host/tests.rs:634`) — but it is
  `#[ignore]`d and FO4-only, which is precisely the gap #3273 existed to
  close.
- **Impact**: The gate that converts #3147's HIGH claim from "unverified" to
  "verified" verifies less than its own rationale header claims ("#2968's
  redundant-decompress path and the whole `ScaleformNavigatorRuntime` preload
  loop are also exercised for the first time by a real caller on this gate").
  It exercises them; it does not check their outcome.
- **Related**: #3273, #3147, #2720, UI-D3-2026-08-27-02.
- **Suggested Fix**: Add two greps to the failure-arm loop —
  `"was not found in the configured archive"` and
  `"did not settle after"` — and, once UI-D3-2026-08-27-02 lands, assert the
  `state=` token on the `ui.menu: loaded` line is `AdapterInjected*` for the
  FO4 case.

### LOW

#### UI-D6-2026-08-27-06: the Ruffle wgpu device, its `TextureTarget` and the UI texture ring are entirely absent from `docs/engine/memory-budget.md`

- **Severity**: LOW
- **Dimension**: Render Path & Device Lifecycle
- **Profile**: both
- **Location**: `docs/engine/memory-budget.md` (no occurrence of `ruffle`,
  `wgpu`, `scaleform` or `ui`) · `crates/ui/src/player.rs:93-109`, `:284-289`
- **Status**: NEW
- **Description**: `/audit-ui` Dim 6 asks explicitly for the second GPU
  device to be quantified "against `docs/engine/memory-budget.md`". The doc
  has no row for it. Measured against
  `feedback_vram_baseline` (budget total under ~4 GB) the omission is small
  but not nil, and it is process-lifetime: `shared_descriptors()` stores the
  `Arc<Descriptors>` in a `static OnceLock`, which is never dropped, so the
  second `VkInstance`/`VkDevice`/`VkQueue` and every Ruffle pipeline object
  are released by the OS at exit rather than by wgpu.
- **Evidence**: Per `SwfPlayer`, `TextureTarget::new`
  (`ruffle_render_wgpu`) allocates one `Rgba8Unorm` render texture plus one
  `MAP_READ` readback buffer at `padded_bytes_per_row × height`. At
  1920×1080 that is 8.29 MB + 8.29 MB. Add `SwfPlayer::pixel_buffer`
  (8.29 MB host), the engine-side UI `VkImage`, and up to
  `MAX_FRAMES_IN_FLIGHT` (2) deferred-destroy copies of it
  (UI-D6-2026-08-27-04) — ≈ 25–42 MB plus one whole extra logical device.
  The `shared_descriptors` doc comment honestly records the trade ("one idle
  logical device is retained after the last menu's player is dropped") but
  understates it: the `OnceLock` never releases it at all.
- **Impact**: Doc gap only. Nothing here is a leak that compounds.
- **Related**: #2733 (created the singleton), UI-D6-2026-08-27-04.
- **Suggested Fix**: One `### Scaleform UI` section in
  `docs/engine/memory-budget.md` with the three numbers above and the
  process-lifetime note.

#### UI-D6-2026-08-27-07: `docs/engine/ui.md` still describes a 6-attachment main render pass; it has 8 (+depth)

- **Severity**: LOW
- **Dimension**: Catalog Fidelity & Drift (doc rot)
- **Profile**: both
- **Location**: `docs/engine/ui.md`:297-301 vs
  `crates/renderer/src/vulkan/context/helpers.rs:148-190` and
  `crates/renderer/src/vulkan/pipeline.rs:941-955`
- **Status**: NEW
- **Description**: The doc reads "The main render pass has 6 color
  attachments (HDR + normal + motion + mesh-id + …). The UI pipeline writes
  RGBA to slot 0 (HDR) only; **the other five** attachments use a no-op blend
  state". The pass has **8** color attachments plus depth — slots 6
  (`fsr_reactive`, `R8_UNORM`) and 7 (`fsr_transparency`, `R8_UNORM`) landed
  with the FSR3 work — and `create_ui_pipeline` correctly masks **seven** of
  them. The masking itself is right; only the count is stale.
- **Evidence**: `helpers.rs:148` — "Main render pass writes to 8 color
  attachments + depth", enumerated 0–7 plus 8 = depth.
  `pipeline.rs:941-955` — one `ui_hdr_blend` + seven `ui_noop_blend`.
- **Impact**: Cosmetic, but it is a G-buffer contract number in the doc that
  owns the UI's render integration, and the same doc's next paragraph
  correctly pins the 20-byte `UiVertex` — so a reader has no cue that one
  number is maintained and the other is not.
- **Related**: #2730/#3088/#3153/#3272 (the same drift class).
- **Suggested Fix**: `6` → `8`, `five` → `seven`, and name slots 6/7.

#### UI-D2-2026-08-27-08: `MAX_DISTINCT_HOST_METHOD_NAMES` can lock out the adapter's own `__byroBGSCodeObj*` callbacks, and the constant's rationale does not say so

- **Severity**: LOW
- **Dimension**: Host Bridge Transport
- **Profile**: `Fallout4Avm2` (mechanism is profile-agnostic)
- **Location**: `crates/ui/src/host.rs:37-52`, `:194-217`, `:598-607` ·
  `crates/ui/src/player.rs:606-613`, `:658-666`
- **Status**: NEW
- **Description**: `on_callback_available` funnels every
  `ExternalInterface.addCallback` name through `insert_bounded` into the
  1024-entry `callbacks` set. Untrusted movie content chooses those names
  (the constant's own doc says so). Once the cap latches, a *later*
  registration is dropped — including the injected adapter's
  `__byroBGSCodeObjReady` and `__byroBGSCodeObjDestroy`, which are registered
  from the installer running out of the movie's own constructor, i.e. after
  arbitrary movie code has had a chance to run. `has_callback` is the sole
  gate on both `SwfPlayer::invoke_callback` (`:611-613`) and the destroy hook
  in `Drop` (`:660`), so a capped set silently disables the readiness probe
  and the `code_object_destruction_count()` acknowledgement.
- **Evidence**: `insert_bounded` (`host.rs:201-217`) drops any *new* name
  once `set.len() >= MAX_DISTINCT_HOST_METHOD_NAMES`; there is no reserved
  band or prefix exemption for the engine's own `__byro`-namespaced names,
  even though `HELPER_PREFIX`/`READY_CALLBACK`/`DESTROY_CALLBACK`
  (`avm2_host.rs:24-31`) already give them a distinguishable namespace.
- **Impact**: Requires a hostile or pathological movie registering 1024+
  distinct callbacks before its own lifecycle installer runs — not a shape
  any vanilla menu produces. The finding is that the cap converts a *memory*
  hardening measure into a *functional* one without saying so; the doc
  comment at `:37-52` lists only the growth rationale.
- **Related**: #2964 (added the cap).
- **Suggested Fix**: Exempt names beginning with `__byro` from the cap (they
  are engine-authored and finite: 3), and add one sentence to the constant's
  doc comment recording what the cap costs when it engages.

#### UI-D1-2026-08-27-09: minor hygiene — a substring-overlapping state probe, a saturating request-ID bound, and two smoke-test nits

- **Severity**: LOW
- **Dimension**: Profile & VM Selection / AVM2 Adapter Injection / Resource Navigator
- **Profile**: both
- **Status**: NEW
- **Description**: Three unrelated one-liners, grouped so they are not
  three findings:
  1. `crates/ui/src/avm2_host.rs:89-95` — the re-injection probe separates
     `AdapterInjected` from `AdapterInjectedWithoutDestroyHook` with
     `contains_bytes(abc, DESTROY_CALLBACK.as_bytes())`.
     `DESTROY_CALLBACK` (`"__byroBGSCodeObjDestroy"`) is a strict **prefix**
     of `DESTROYED_EVENT` (`"__byroBGSCodeObjDestroyed"`), so the probe
     cannot distinguish them. It is correct today only because
     `build_adapter_abc` emits all four destroy strings together or none
     (`:747-754`) — i.e. correct by an invariant held two functions away,
     with no test pinning it.
  2. `crates/ui/src/host.rs:558-564` — `numeric_request_id`'s upper bound is
     `value <= u64::MAX as f64`; `u64::MAX as f64` rounds *up* to 2^64, so
     the value 2^64 passes the guard and then saturates to `u64::MAX` in
     `value as u64`. Cosmetic (a `GameDelegate` ID is a small counter), but
     the guard does not do what it reads as. `value < 2f64.powi(64)` is exact.
  3. `docs/smoke-tests/m48-menu-load.sh:48` defines
     `PORT="${BYRO_DEBUG_PORT:-9876}"` and never uses it except inside a
     failure message — it is neither exported nor passed to `byro-dbg`. And
     `:45` reads `BYROREDUX_SKYRIM_DATA` while the crate's own Skyrim corpus
     test (`crates/ui/src/host/tests.rs:574`) and `README.md`:378 read
     `BYROREDUX_SKYRIMSE_DATA` for the same directory, so an operator who
     sets one silently skips the other. (The `_SKYRIM_` spelling is the
     smoke-test-wide convention — 7 scripts plus
     `docs/smoke-tests/lib/fixture.sh` — so this is a repo-wide split, noted
     here because it bites the UI gate specifically.) `m48-menu-load.sh` was
     also not migrated to the `lib/fixture.sh` harness that `3aebf414`
     introduced for `p0`/`p1`/`p2`.
- **Impact**: None observable today; all three are hygiene.
- **Suggested Fix**: (1) match on `DESTROYED_EVENT` instead, or add a test.
  (2) exact bound. (3) drop `PORT` or export it; pick one Skyrim env-var
  spelling repo-wide.

---

## 3. Prior-cycle findings — status at HEAD

| Finding / Issue | 08-24 status | Status now |
|---|---|---|
| `UI-D4-04` (ui.md test count 48+2 vs 51+2) | NEW LOW | **CLOSED** — `docs/engine/ui.md`:531 now reads "53 default tests plus 2 ignored"; measured 53+2 (`ee03f5c6`, `Fix #3272`) |
| `UI-D5-04` (no end-to-end gate for the `--menu` route) | NEW LOW | **CLOSED** — `docs/smoke-tests/m48-menu-load.sh` exists and is referenced from `CLAUDE.md` (`fdba763d`, `Fix #3273`). But see UI-D5-2026-08-27-05 for what the gate does not check |
| #2722 (failed capture published stale pixels) | open | **FIXED + CLOSED** — `player.rs:451-474` returns `None` on all three failure paths with `dirty` still set; pinned by `render_leaves_dirty_set_and_returns_none_on_a_size_mismatch` (`7390fe4c`) |
| #2723 (no resize; dead `UiManager::close`) | open | **CLOSED as documented-not-fixed** — `close()` deleted (`lib.rs:265-273`), the fixed-at-load viewport documented (`lib.rs:63-70`). The *behaviour* is unchanged: nothing drives `set_viewport_dimensions` or re-registers the UI texture on `recreate_swapchain` (`app_events.rs:330-334`, `app_frame.rs:588-596`), so the overlay still stretches on resize. That is now a deliberate, recorded limitation, not a finding |
| #2965 (AVM1 request-ID over-strip) | open | **FIXED + CLOSED** — `host.rs:500-531` gates the strip on `catalog.find(transport_method).is_some() \|\| has_callback("respond")` (`7390fe4c`) |
| #3149 (`AdapterInjected` hid a missing destroy trait) | fixed in code, open in tracker | **still fixed, still OPEN** — `avm2_host.rs:43-58`, `:219-227` |
| #3151 (`InitCodeObj`/`ReleaseCodeObj` dropped from inventory) | fixed in code, open in tracker | **still fixed, still OPEN** — no skip in `referenced_host_methods_in_tags` (`:306-350`); `init_and_release_code_obj_calls_are_reported_by_inventory` passes |
| #3155 (unknown-method warn deduped per process) | fixed in code, open in tracker | **still fixed, still OPEN** — `host_method_diagnostic_key` (`app_frame.rs:33-35`) |
| #3156 (`import_asset_paths` unbounded) | fixed in code, open in tracker | **still fixed, still OPEN** — `MAX_IMPORT_ASSET_PATHS = 512` + `extend_import_asset_paths` (`navigator.rs:31-116`) |
| #2968 (redundant decompress on the archive route) | open, LOW | **still open — and larger than the issue says.** See below |
| #2969 (per-frame drain never reads `dropped_calls()`) | open, LOW | **still open, unchanged** — `grep -rn "dropped_calls" byroredux/` returns nothing; `app_frame.rs:267` is a bare `for call in ui.drain_host_calls()` |
| #3103 (no AVM1 corpus scanner) | open | **still open** — no AVM1 bytecode scanner anywhere in `crates/ui/src/`; the Skyrim corpus test asserts profile detection only |

**#2968 magnitude correction.** The issue text says the route "decompresses
the same SWF four times and fully tag-parses it twice". Since #3147 added the
`scene.rs` `--menu` route, the live chain is **two archive extractions of the
same entry** and **five full SWF decompressions**:
`scene.rs:1524` `archive.extract` → `scene.rs:1525` `ScaleformProfile::detect`
→ `player.rs:210` `provider.load` (extract #2) → `player.rs:214` `detect` #2 →
`avm2_host.rs:69-71` `decompress_swf`+`parse_swf` → `navigator.rs:501-503`
`decompress_swf`+`parse_swf` → `player.rs:227` `SwfMovie::from_data`.
Full tag-parses remain two. Still LOW (load-time only), but the issue's
numbers should be amended rather than re-derived by the next auditor.

---

## 4. Verified clean this cycle (checked, no finding)

- **Dimension 1** — `profile.rs` is byte-identical since 08-16. Detection is
  movie evidence only (`is_action_script_3()`); no `--game`, archive-name or
  path inference exists in any of the three `SwfPlayer` constructors, and the
  `--menu` route feeds `detect`'s answer straight through
  (`scene.rs:1525-1534`). Forced-profile mismatch is a hard `anyhow!` in both
  `new_with_profile` and `from_resource_provider`.
  `external_interface_id` is used at exactly one site
  (`BridgeProvider::get_id`, `host.rs:609-611`) — no id-mismatch surface.
- **Dimension 2** — `MAX_QUEUED_CALLS` eviction is `VecDeque::pop_front`
  (O(1)), the counter increments once per eviction, and `overflow_warned`
  latches (`host.rs:445-458`). Re-entrancy is safe: the response handler is
  cloned out from under a *temporary* borrow (`:396-402`) and invoked with no
  borrow held, and `BridgeProvider::call_method`'s re-entrant
  `callback.call(context, "respond", …)` (`:590-594`) runs after `record_call`
  has fully returned. `MissingResponse` and `Unknown` are correctly separated
  (`:419-439`). The bridge is `Rc`/`RefCell`, so `!Send` is compiler-enforced;
  `UiManager` is still not an ECS `Resource` (no `impl Resource for UiManager`
  anywhere in the tree).
- **Dimension 3** — Constant-pool discipline holds: every index in
  `build_adapter_abc` is derived from its own `add_string`/`add_namespace`/
  `add_multiname` push, and `Index::new(0)` appears only as the ABC "any"
  sentinel. Both #2970 idempotency guards are live (outer tag scan
  `:81-97`, `patch_root_constructor`'s own `INSTALL_HELPER` scan `:526-545`).
  Branch-crossing validation before the splice (`:572-617`) and exception-range
  fixups (`:653-663`) are correct for both jump-relative (end-relative) and
  `LookupSwitch` (start-relative) offsets. Every generated body's declared
  `max_stack`/`num_locals` was hand-verified (see §1). `BGSCodeObj.X` → `X`
  normalization is one shared helper (`host.rs:533-547`), not 269 copies.
- **Dimension 4** — clean; see §1.
- **Dimension 5 (trust boundary)** — **a menu cannot escape the archive.**
  `resolve_url` (`navigator.rs:372-374`) joins onto the `file:///…` movie URL,
  so `..` traversal is normalised away by `Url::join` and an absolute or
  non-`file` URL is refused by `archive_path_from_url`
  (`:463-495`, which also rejects `ParentDir`/`Prefix` components). Even a
  fully-resolved `file:///etc/passwd` becomes the archive key `etc\passwd` and
  is looked up through `ScaleformResourceProvider::load` — the provider only
  ever consults the BSA/BA2 file table (`archive.rs:75`, `navigator.rs:50-68`).
  No filesystem or network reach exists on this path. `raw_tag_records`
  (`:570-605`) is bounds-checked throughout with `checked_add` + a
  `end <= data.len()` filter. `resource_loads` is bounded and hit-counted
  (`player.rs:577-600`, `MAX_RECORDED_RESOURCE_LOADS = 64`), closing the
  08-24 concern. `prepare_import_asset_swf`'s synthetic `[0x40, 0x00]`
  ShowFrame splice was verified against the pinned Ruffle rev
  (`0dde9813`): `loader.rs:2214` really does `set_cur_preload_frame(0)` and
  `movie_clip.rs:4123` really does index `cur_preload_frame - 1`, so the
  workaround is necessary and correctly placed. Not incrementing the header's
  `num_frames` is harmless — `MovieClip::preload` walks to the end of the tag
  stream and is not bounded by `header_frames`.
- **Dimension 6 (device lifecycle)** — creation failure is non-fatal and
  **not** cached (`get_or_try_init`, `player.rs:119-128`, pinned by
  `a_failed_init_is_not_cached_and_the_next_call_retries`); `scene.rs` logs
  and leaves `ui_manager` at `None`. `UiFrame::Unchanged` correctly reuses
  `self.ui_texture_handle` and `Hidden` composites nothing
  (`app_frame.rs:335-339`). `capture_frame` → `into_raw` → `copy_from_slice`
  is an owned round-trip with no borrow crossing the FFI boundary; stride is
  tight-packed on both sides. **Nothing in `crates/ui` is `unsafe`.**
- **Dimension 7** — `is_debug_overlay_key` is checked *before* the
  `ui_manager`/focus gate (`main.rs:545-547`), so F3 stays reachable under a
  modal menu. `cache_window_state` runs ahead of every early return
  (`:543`) — the #2973 fix is intact. `dispatch.captured` drives
  `release_world_input_for_ui` (`:577-579`), which clears `keys_held`,
  `mouse_buttons_held` and `mouse_captured` (`ui_input.rs:58-67`), so an
  `InputAction` edge cannot leak into `combat_input_system` on a menu-open
  frame. `set_mouse_in_stage` is driven from `CursorEntered`/`CursorLeft`
  (`ui_input.rs:87-91`). `crates/ui/src/input.rs` is winit-free (it re-exports
  `ruffle_core::events` types only). Dropping the horizontal wheel axis
  (`ui_input.rs:109-110`) is **correct, not a bug** —
  `ruffle_core::events::MouseWheelDelta` is single-axis by construction.
  `UiManager::tick`/`render` both early-out on `!visible`, so a hidden menu
  does not tick Ruffle.

### Ordering fact for `/audit-concurrency` Dim 6

`App::drop` (`byroredux/src/main.rs:337-345`) explicitly drops
`VulkanContext` **first** (`self.renderer.take()`), so `SwfPlayer` and its
wgpu device drop *after* the engine's Vulkan context — the opposite of the
ordering `/audit-ui`'s Dim 6 checklist asserts is required. It is harmless
here and no finding is filed: the Ruffle device is a separate `VkInstance`/
`VkDevice` created by `create_wgpu_instance` (`player.rs:97-98`), the UI
texture lives in `VulkanContext`'s own registry and dies with it, and
`SwfPlayer::drop`'s `call_internal_interface(DESTROY_CALLBACK)`
(`player.rs:658-666`) touches no engine GPU object. The skill's checklist
premise ("must drop before the Vulkan context tears down its allocator")
predates the #2733 shared-descriptor singleton and should be reworded.

---

## 5. Pending-Row readiness

- **Host-method handlers** — the mechanism is sound and self-retiring
  (`register_method`/`set_response*` remove from `unknown_methods`), and the
  diagnostic is cleanly per-menu (#3155). What is *not* ready is the
  observability around it: `dropped_calls()` (#2969),
  `unknown_methods()`/`unanswered_methods()`, `preload_stalled()` and
  `host_object_state()` all have **zero** engine callers, so the engine can
  see individual calls but not the state of the bridge that carried them
  (UI-D3-2026-08-27-02).
- **Menu stack / focus policy** — `UiManager::close()` is now gone by
  decision (#2723). `--menu`/`--swf` still load exactly one menu for the
  process lifetime, so `install_player`'s swap path (`lib.rs:126-138`) and
  the engine's single `ui_texture_handle` have never run against a second
  menu. Anyone landing a stack must re-register the texture on swap.
- **Papyrus ↔ UI bridge** — unchanged; `invoke_callback` still has no
  non-test caller.
- **Font fidelity** — this is the row UI-D6-2026-08-27-01 blocks outright.
  No amount of font work is measurable while every menu pixel goes through
  ACES, fog and FSR.
- **Resize** — still fixed-at-load by decision (#2723); a real menu pack
  needs it.

## 6. Cross-audit pointers

- Overlay compositing stage (UI-D6-…-01) — the *pass* is
  `/audit-renderer`'s; the defect is UI-only and is reported once, here.
- Dynamic-texture recreate churn (UI-D6-…-04) — mechanism lives in
  `crates/renderer/src/texture_registry.rs`; hand to `/audit-performance`
  if the UI fix is deferred.
- GPU teardown ordering — `/audit-concurrency` Dim 6; the fact is recorded in
  §4, no finding.
- Ruffle/wgpu `unsafe` — `/audit-safety`: still **none** in `crates/ui`
  (verified by grep this cycle, not carried over).
- `--menu`/`--swf` texture-registration duplication in `scene.rs`
  (:1536-1568 vs :1606-1624) — `/audit-tech-debt` Dim 2, unchanged since
  08-24, still not picked up.
- `BYROREDUX_SKYRIM_DATA` vs `BYROREDUX_SKYRIMSE_DATA` — repo-wide;
  `/audit-tech-debt`.

---

TALLY (new findings this cycle): CRITICAL=0 HIGH=1 MEDIUM=4 LOW=3
Additionally: 2 prior findings closed, 3 issues fixed+closed, 4 issues still
verified-fixed-but-open, 3 issues re-verified still open (one with a
corrected magnitude).
