# ECS Audit — Dimension 5 (System & Scheduler) — 2026-05-06

## Executive Summary

- **Scope**: Dimension 5 only (focus arg = 5). Dimensions 1–4 and 6–8 of the ECS spec are out of scope for this run.
- **Findings**: 2 total — 0 CRITICAL, 0 HIGH, 0 MEDIUM, 2 LOW (one DOC, one DOC/contract). Pre-existing OPEN issue **#848** (footstep stale-read) overlaps the "stage ordering correctness" checklist item but is already filed and out of scope to re-report.
- **Baseline**: `cargo test -p byroredux-core` → **291 passed, 0 failed, 0 ignored** (also 2 doc-tests passing). The CLAUDE.md figure of 162 / system-instructions figure of 68 are stale; suite has grown.
- **Verdict**: Dimension 5 is materially clean. No correctness bugs in the scheduler; the two LOWs are documentation/contract gaps (panic propagation semantics, re-entry guard) that don't affect runtime behaviour today.

## Verified Invariants

Each checklist item from the spec, with the file:line evidence used to verify it:

- **Blanket impl for `Fn(&World, f32)`** — `crates/core/src/ecs/system.rs:49` reads
  `impl<F: FnMut(&World, f32) + Send + Sync> System for F`. The `FnMut` (not `Fn`) bound
  is intentional and correct: lets stateful closures with captured mutable state
  (counters, accumulators) act as systems. `Send + Sync` gates rayon dispatch.
  Lifetime bounds: `&World` borrowed for the duration of `run()`, which is exactly
  the dispatch-task lifetime under `par_iter_mut`. **PASS.**
- **System N → System N+1 visibility (same `run()`)** — within a stage, the parallel
  batch in phase 1 (`scheduler.rs:278-281`) ends before phase 2 exclusives run
  (`scheduler.rs:289-291`); across stages, the `BTreeMap<Stage, _>` iteration in
  `scheduler.rs:274` is sequential, and every `RwLockReadGuard`/`RwLockWriteGuard`
  in phase N is dropped before phase N+1 begins (no borrow leak — guards are local
  to each system body). Verified by `mutation_visible_across_stages`
  (`scheduler.rs:557-587`) and `exclusive_runs_after_parallel`
  (`scheduler.rs:614-638`). **PASS.**
- **Empty scheduler runs without panic** — `scheduler.rs:273-293` iterates an empty
  `BTreeMap` as a no-op. Test `empty_scheduler_runs_cleanly` (`scheduler.rs:661-666`)
  + `empty_stages_skipped` (`scheduler.rs:670-688`) confirm both the "zero stages"
  and "zero systems in a stage" cases. **PASS.**
- **`system_names()` returns registration order** — `scheduler.rs:298-300`
  flat-maps `self.stages.values()` (BTreeMap → stage discriminant order
  `Early=0..Late=4`) over `parallel.chain(exclusive)`. Within a stage,
  insertion order is preserved by `Vec::push` in `add_to`/`add_exclusive`.
  Test `system_names_in_stage_order` (`scheduler.rs:692-703`) pins the
  contract. **PASS.**
- **Stage ordering (Update → Late, audio_system Late, transform_propagation
  between)** — engine wiring at `byroredux/src/main.rs:310-357`:
  - `Stage::Early`: `fly_camera_system` (with `Access` decl), `weather_system`,
    `timer_tick_system`.
  - `Stage::Update`: `animation_system`, `footstep_system`, `spin_system`.
  - `Stage::PostUpdate`: `make_transform_propagation_system()` (parallel),
    then exclusives `particle_system`, `billboard_system`,
    `make_world_bound_propagation_system()` (in that registration order).
  - `Stage::Physics`: `physics_sync_system`.
  - `Stage::Late`: `audio_system`, `log_stats_system`, then exclusive
    `event_cleanup_system`. Debug build adds `DebugDrainSystem` exclusive
    via `crates/debug-server/src/lib.rs:28`.
  All ordering claims in the checklist hold. **PASS** (but see Out-of-Scope
  Notes for #848: `footstep_system` running in `Stage::Update` reads
  GlobalTransform before propagation — already filed).
- **Same-system double-registration: documented behavior** — `add_to`
  warns + accepts duplicates (`scheduler.rs:108-135`); `try_add_to`
  returns `Err(name)` (`scheduler.rs:180-198`). The doc comment at
  `scheduler.rs:111-117` explicitly documents the closure-friendly
  rationale (closures share `type_name`). Cross-stage de-dup verified by
  `try_add_to_rejects_duplicate_across_stages` (`scheduler.rs:739-750`).
  Lax-mode regression covered by `add_to_still_accepts_duplicate_with_warning`
  (`scheduler.rs:752-762`). **PASS.**
- **Per-system error/panic propagation** — see finding **D5-NEW-01** below
  (LOW — contract is correct in practice but undocumented, and within-batch
  cancellation semantics differ from what an operator might assume).
- **Schedule re-entry guards** — see finding **D5-NEW-02** below (LOW —
  current architecture makes re-entry physically impossible because the
  scheduler is owned by `App` and not exposed to systems via `&World`,
  but the contract isn't asserted anywhere).
- **Stage-exclusive systems contract** — `crates/debug-server/src/system.rs:48-144`
  defines `DebugDrainSystem`; `crates/debug-server/src/lib.rs:28` registers
  it via `scheduler.add_exclusive(Stage::Late, drain_system)`. The
  exclusive-after-parallel contract is enforced at `scheduler.rs:288-291`
  (phase 2 is a plain `for` loop after the rayon `par_iter_mut` join
  point). Test `exclusive_runs_after_parallel` (`scheduler.rs:614-638`)
  confirms exclusives see writes from the parallel batch. The contract
  is **real, not aspirational.** **PASS.**
- **`Scheduler::access_report()` (R7)** — `scheduler.rs:320-362` builds the
  report on demand from declared access; coverage by
  `declared_systems_produce_conflict_in_report` (`:787-822`),
  `undeclared_closure_pairs_show_as_unknown` (`:824-838`),
  `declared_disjoint_components_have_no_conflict` (`:840-859`),
  `override_at_registration_beats_trait_method` (`:861-879`),
  `exclusive_systems_are_listed_but_not_paired` (`:881-900`). **PASS.**

## Findings

### D5-NEW-01: Per-system panic propagation contract is undocumented

- **Severity**: LOW
- **Dimension**: 5 (System & Scheduler)
- **Location**: `crates/core/src/ecs/scheduler.rs:272-293`
- **Status**: NEW
- **Description**: `Scheduler::run` does not document what happens when an
  individual system panics. With the default `parallel-scheduler` feature
  on, rayon's `par_iter_mut().for_each(...)` (`:278-281`) propagates the
  first worker panic to the calling thread *after* all sibling tasks in
  the batch finish (rayon does not cancel sibling work on panic — see
  rayon-core panic policy). With `parallel-scheduler` off (`:283-287`),
  the plain `for` loop short-circuits on the first panic, leaving any
  remaining parallel-phase systems and *all* exclusive-phase systems in
  the same stage un-run for that frame. There is no per-system
  `catch_unwind`, no panic-isolation, and no doc comment on `Scheduler::run`
  explaining either contract.
- **Evidence**:
  ```rust
  // scheduler.rs:273-293
  pub fn run(&mut self, world: &World, dt: f32) {
      for (_stage, data) in &mut self.stages {
          #[cfg(feature = "parallel-scheduler")]
          { data.parallel.par_iter_mut()
                 .for_each(|entry| entry.system.run(world, dt)); }
          #[cfg(not(feature = "parallel-scheduler"))]
          { for entry in &mut data.parallel { entry.system.run(world, dt); } }
          for entry in &mut data.exclusive { entry.system.run(world, dt); }
      }
  }
  ```
  Compare with `byroredux/src/streaming.rs:260,364` and
  `crates/core/src/ecs/world.rs:1780`, which use
  `std::panic::catch_unwind(AssertUnwindSafe(...))` for isolation in
  failure-tolerant call sites.
- **Impact**: Behavioural — none today (no engine system panics in
  steady state). Diagnostic — when a panic *does* happen (e.g. an
  invariant violation in a third-party plugin system), the operator
  has to know the rayon contract to predict whether sibling systems
  ran or not. This is an operability gap, not a correctness bug.
- **Related**: Issue **#848** (footstep stale-read; same dimension).
  No prior audit has flagged the panic contract.
- **Suggested Fix**: Add a doc comment to `Scheduler::run` describing
  (a) feature-gated panic propagation order, (b) that exclusives in
  the same stage are skipped on a panicking parallel phase under
  `cfg(not(parallel-scheduler))`, and (c) that the engine contract is
  "panics escape — there is no recovery." No code change needed
  unless we want a `panic_policy: enum { Propagate, Isolate }` field
  on `Scheduler`, which is a separate design call.

### D5-NEW-02: Schedule re-entry guard is structural, not asserted

- **Severity**: LOW
- **Dimension**: 5 (System & Scheduler)
- **Location**: `crates/core/src/ecs/scheduler.rs:273` + `byroredux/src/main.rs:774,1111`
- **Status**: NEW
- **Description**: `Scheduler::run` takes `&mut self`. Systems take
  `&World` (interior-mutable via RwLock), so a system body cannot reach
  the `Scheduler` instance through any safe path the engine exposes —
  `Scheduler` is owned by the `App` struct (`byroredux/src/main.rs:240,388`)
  and is **not** stored as a `Resource`. So reentry is structurally
  impossible. However, the contract is implicit. If a future maintainer
  adds `Scheduler` as a resource (or wraps it in `Arc<Mutex<_>>` to
  enable hot-reload), `system → scheduler.run` would compile — and
  panic at `BorrowMutError` with a confusing call site, since the
  outer `&mut self` is held across the inner call.
- **Evidence**: `scheduler.rs:273` `pub fn run(&mut self, world: &World, dt: f32)`.
  No `running: bool` flag, no `Cell<bool>` re-entry guard, no
  documentation that says "this method must not be called from within
  a System." Engine call sites: `byroredux/src/main.rs:774,1111`
  (window-resize and per-frame draw). No system calls `scheduler.run`.
- **Impact**: None today — design is sound. Latent risk if the
  invariant is silently broken by a future refactor (e.g. M27 dynamic
  scheduling, hot-reload).
- **Related**: Aligns with the "no Scheduler-as-Resource" architectural
  invariant implied by the system-instructions contract.
- **Suggested Fix**: Either (a) add a one-line doc comment on
  `Scheduler::run`: "Must not be called recursively. Systems cannot
  reach the `Scheduler` because it is owned by `App` and not exposed
  via `World`. If you need ad-hoc dispatch, use a `CommandQueue`
  resource and drain in a Late-stage exclusive system (the
  `DebugDrainSystem` pattern)." or (b) add a `Cell<bool>` re-entry
  guard with a hard-fail at the top of `run`. (a) is sufficient and
  matches the rest of the codebase's hygiene level.

## Out-of-Scope Notes

These were observed during the Dimension 5 sweep but belong to other
dimensions or are already filed:

- **Stage assignment correctness — `footstep_system`** is registered
  in `Stage::Update` (`byroredux/src/main.rs:323`) but reads
  `GlobalTransform`, which is updated by `make_transform_propagation_system`
  in `Stage::PostUpdate` (`:329`). This is OPEN as **#848**
  (`AUD-D6-NEW-07`, MEDIUM) and is acknowledged in a doc comment at
  `byroredux/src/main.rs:313-322` ("acceptable for footstep position
  accuracy at human movement speeds"). Not re-reporting.
- **Stage assignment correctness — debug `audio_system`** runs in
  `Stage::Late` (`byroredux/src/main.rs:348`), where it reads final
  world transforms after PostUpdate propagation. Correct per design.
- **Test count drift** — system instructions / `CLAUDE.md` mention
  68 / 162 unit tests; current pass count is **291 passed** (plus 2
  doc-tests). Not a finding, but the figure should be refreshed in
  the docs to keep the regression baseline accurate.
- **`has_system` is O(N)** on every `add_to` (`scheduler.rs:302-306`).
  Acceptable at N ≈ 13 systems; flagged in `AUDIT_ECS_2026-04-14.md`
  as "Not a finding." Confirmed still correct for current scale.
- **`SchedulerAccessReport` snapshot is taken once at startup**
  (`byroredux/src/main.rs:369-371`). If runtime registration is ever
  added (e.g. plugin loader), the report goes stale. Out of scope —
  Dimension 7 (Diagnostics) territory.
