# FO4 Compatibility Audit — 2026-06-14

**Branch**: main @ 435e265d
**Auditor**: Multi-agent /audit-fo4 (9 parallel dimension agents, Opus 4.8 1M)
**Scope**: All 9 dimensions — M49 CSG precombines, BGSM/BGEM consumption, BA2 reader, NIF BSVER 130 + half-float + collision, FO4 shader flags + BGSM PBR routing, ESM architecture records (SCOL/MOVS/PKIN/TXST/CONT), NIFAL canonical material translation, FO4 cell-load end-to-end (SCOL/PKIN expansion), real-data validation + forward scope.
**Game data**: FULLY PRESENT at `/mnt/data/SteamLibrary/steamapps/common/Fallout 4/Data/` — base archives (`Fallout4 - Meshes.ba2`, `MeshesExtra.ba2`, `Materials.ba2`, `Textures1..9.ba2`, `Fallout4 - Geometry.csg`, `Fallout4.esm`) plus all DLC `.esm`/`.csg`. All real-data harnesses were exercised.

---

## Executive Summary

FO4 compatibility remains in **strong shape** across the whole pipeline. Every structural investment is verified intact: BSVER-130 NIF parse (half-float, BSTriShape, BSSubIndexTriShape segments, skinning, collision), the exhaustive-`match` BA2 reader (v1/2/3/7/8, GNRL + DX10), the external BGSM/BGEM material parser, SCOL/PKIN expansion, and the **M49 CSG precombine pipeline** (CSG read → PSG decode → Y-up convert → spawn → REFR de-dup). The #1476 metalness-from-spec-chromaticity fix and the #1352 BGSM→PBR_BSDF routing are both present with no reversion.

**Re-measured parse rate** (this audit, `parse_rate_fo4_all_meshes`, ~15 s): **100.00% clean / 0 truncated** across BOTH vanilla mesh archives — `Fallout4 - Meshes.ba2` 34,995/34,995 + `Fallout4 - MeshesExtra.ba2` 124,871/124,871 = **159,866 NIFs, 0 truncated, 0 failed, 0 NiUnknown**. The ROADMAP compat-matrix figure (96.46%) is stale (see FO4-D9-MEDIUM-01).

| Area | State |
|------|-------|
| NIF BSVER 130 (half-float, BSSubIndexTriShape, DLC trailing fields, collision) | PASS — 5/5 checklist |
| BA2 reader (v1/2/3/7/8, GNRL byte-exact, DX10 DDS recon) | PASS — exhaustive dispatch, no v1-fallback |
| BGSM/BGEM consumption + #1476 metalness | PASS — 5/5, #1476 intact verbatim |
| FO4 shader flags (u32 pair) + BGSM PBR routing | PASS — 5/5, FO4 bit vocab nif.xml-pinned |
| ESM architecture records (SCOL/PKIN/MOVS/TXST) | PASS — TXST DODT+DNAM intact |
| NIFAL canonical material translate | PASS — single boundary, resolve-once, glass-after |
| M49 CSG precombine pipeline | PASS — 7/7 (base game); DLC CSG resolution gap noted |
| SCOL/PKIN cell-load end-to-end | PASS — 4/4, depth-capped, de-dup gate correct |

**New actionable findings**: **0 CRITICAL · 0 HIGH · 6 MEDIUM · 6 LOW** (excluding cross-dimension duplicates, INFO, and findings deduped against open issues). The two record-parsed-but-unconsumed gaps (CONT #1359, BSConnectPoint attach) and the DLC CSG resolution gap are the highest-value items; none block vanilla base-game content.

**Most severe**: FO4-D1-MED-01 — DLC / multi-master cells can resolve the **wrong** `Geometry.csg` blob (CSG keyed off loaded-plugin stem, `filename_hash` ignored; `_oc.nif` path built from the load-order-remapped form id). Base game is correct; DLC precombines can read corrupt geometry or silently fall back to per-REFR.

---

## Dimension Findings

### Dimension 1: M49 Precombined Geometry (CSG + Decode + Spawn) — 7/7 PASS

Vertex stride matches spec (the conditional `runtime − 8` on `VF_FULL_PRECISION` is equivalent to "always half4 on disk"); Y-up convert + per-instance transform compose without double-apply (`spawn.rs` adds `cell_origin` with identity ref rot/scale); missing/failed CSG returns `None` → per-REFR fallback (no panic); REFR de-dup gate honors `absorbed_refs` only when `pc_spawned > 0` (interior `load.rs` + exterior `exterior.rs`); single finest-LOD tier selected; one-material-per-packed-block verified on 4,000 `_oc.nif` (no_owner=0, multi_owner=0); exterior `cell_origin = cell_grid_to_world_yup(gx, gy)`.

#### FO4-D1-MED-01: DLC / multi-master precombines resolve the wrong CSG (filename_hash ignored) and a remapped path form-id
- **Severity**: MEDIUM
- **Dimension**: M49 Precombined Geometry
- **Location**: `byroredux/src/cell_loader/precombined.rs:95-99,264-297`; cross-ref `byroredux/src/cell_loader/load_order.rs:41-77`
- **Status**: NEW
- **Description**: Two coupled multi-master assumptions break for DLC cells. (a) `open_geometry_csg` keys the CSG purely off the loaded plugin's stem (`<esm stem> - Geometry.csg`) and never consults the authoritative `BSPackedGeomObject.filename_hash` (BSCRC32 cross-check is an explicit follow-up in both code and `docs/engine/fo4-csg-format.md:170-172`). The Data dir holds seven distinct CSGs (`Fallout4`, `DLCCoast`, `DLCNukaWorld`, `DLCRobot`, `DLCworkshop01/03`). A DLC cell whose precombined objects reference the base `Fallout4 - Geometry.csg` would be read from the DLC CSG instead. (b) The `_oc.nif` path is built from `cell.form_id`, which `build_remap_for_plugin` has rewritten to the global load-order index (top byte → `plugin_index`), whereas the on-disk filename uses the cell's plugin-local form id — so the remapped top byte no longer matches the baked filename for DLC cells.
- **Evidence**:
  ```rust
  // precombined.rs — path uses the remapped global form_id
  let path = format!("meshes\\precombined\\{:08x}_{:08x}_oc.nif", cell.form_id, hash);
  // open_geometry_csg — CSG named for the loaded plugin, hash ignored
  let csg_path = dir.join(format!("{stem} - Geometry.csg"));
  ```
  `decode_shared_geom_object` performs no index-range validation (raw u16 → u32), and `ImportedMesh::from_geometry` does not either, so a wrong-blob read yields garbage vertices or out-of-range indices flowing toward mesh/BLAS upload.
- **Impact**: Base-game (`Fallout4.esm` alone, plugin_index 0, hash 0xddf19a67, unchanged top byte) is correct — the dominant, validated case. DLC / multi-master interior+exterior precombine loads can read the wrong blob (corrupt/dropped geometry) or miss the `_oc.nif` (path mismatch → REFR fallback, which is at least safe). Confined to FO4 DLC content.
- **Related**: `docs/engine/fo4-csg-format.md:79-81,170-172`; FO4-D1-LOW-01; #1446 (stale CSG doc).
- **Suggested Fix**: Resolve the CSG by `geom.filename_hash` (reproduce BSCRC32 over candidate `<Plugin>` stems in the load order, cache a `hash → CsgArchive` map) instead of the single loaded-plugin stem; build the `_oc.nif` path from the cell's plugin-local form id (pre-remap); add a `max_index < num_verts` guard in `decode_shared_geom_object` so a wrong-blob read fails closed.

#### FO4-D1-LOW-01: Decoded precombine triangle indices are never range-checked against num_verts
- **Severity**: LOW
- **Dimension**: M49 Precombined Geometry
- **Location**: `crates/nif/src/import/precombine.rs:139-145`
- **Status**: NEW
- **Description**: `decode_shared_geom_object` reads `tri_count` u16 triples and pushes them verbatim as u32 indices with no `max_index < num_verts` assertion. The spec validated `max == nv−1` for 38/38 vanilla objects, so vanilla is fine; combined with FO4-D1-MED-01 (wrong-blob read) or a future corrupt CSG, an out-of-range index reaches the GPU mesh/BLAS build. The inline `BsTriShape` path has a `data_size`-derived stride sanity net (#359); this fresh CSG decode path has none.
- **Evidence**: `let tris = stream.read_u16_triple_array(tri_count)?;` then pushed with no comparison of `max(a,b,c)` against `num_verts`.
- **Impact**: No effect on vanilla base-game content (indices in range). Defense-in-depth gap that would let a mis-resolved/corrupt CSG emit wrong AS geometry rather than fail closed.
- **Related**: FO4-D1-MED-01; #359 (analogous inline-path guard).
- **Suggested Fix**: Track `max(a,b,c)` after the triple read; if `>= num_verts` return `InvalidData` so `build_precombine_meshes` skips the object (it already `continue`s on decode error).

---

### Dimension 2: BGSM / BGEM Consumption + Metalness-from-Chromaticity — 5/5 PASS

`merge_bgsm_into_mesh` (`asset_provider.rs:957-1523`) is the single FO4 material merge, invoked at NIF-import (cell + partial paths) BEFORE `translate_material` at spawn. The #1476 fix (commit `08ed03be`) is present verbatim: non-pbr branch uses spec-color saturation `(mx-mn)/mx`, luminance only on `leaf.pbr==true`. Empirically confirmed: `paintpeelingconcrete02` spec `[1,1,1]` → metalness 0.0 (was chrome), `metallocker01` → 0.30. `smoothness→roughness` applied exactly once; magic wins over extension with mismatch log; template cycle-break + `DEPTH_LIMIT` both fire (vanilla `defaultTemplate_wet.bgsm` self-ref confirmed). Validation: `cargo test -p byroredux-bgsm` 27/27; FO4 corpus 100% clean (6,616 BGSM + 283 BGEM).

#### FO4-D2-MEDIUM-01: Conductor diffuse-tint blend folds in `specular_mult`, contradicting the #1476 mult-invariance rationale
- **Severity**: MEDIUM
- **Dimension**: BGSM/BGEM Consumption
- **Location**: `byroredux/src/asset_provider.rs:1147-1149,1174-1185`
- **Status**: NEW
- **Description**: When saturation-derived `metalness > 0.5`, the merge biases `mesh.diffuse_color` 50/50 toward the authored conductor spec — but it blends toward `spec_r/g/b = specular_color * specular_mult` (the mult-scaled values). The #1476 fix deliberately excludes `specular_mult` from the metalness derivation because `mult` only scales highlight strength and is not an albedo/F0 quantity. Folding `mult` into the diffuse tint reintroduces exactly that error on the conductor population: for `mult < 1.0` the blend darkens diffuse toward black; for `mult > 1.0` it overshoots past 1.0 (no clamp at translate or upload — `diffuse_color` flows verbatim into `GpuMaterial.diffuse_r/g/b`).
- **Evidence**: Real vanilla strong-metal BGSMs sampled via `dump_bgsm`: `spec=[1.0,0.255,0.255] mult=0.25` → blend target `[0.25,0.064,0.064]` (near-black); `spec=[1.0,0.467,0.318] mult=1.29` → blend target `[1.29,0.60,0.41]` (channel > 1.0, unclamped). The chromaticity the comment intends to recover is `specular_color`, not `specular_color * mult`.
- **Impact**: Wrong conductor albedo/F0 tint for the ~0.3% strong-metal + part of the 3.7% mid-metal BGSM population (brass/gold/copper/painted-metal trim with non-unit `specular_mult`). Half-weight blend + F0-only use bounds the visual error; not a chrome-scale regression. Loose-NIF and cell paths both affected. FO4 BGSM conductor content only.
- **Related**: #1476 (08ed03be) metalness mult-invariance.
- **Suggested Fix**: Blend toward the mult-free chromaticity — use `leaf.specular_color` (already `[0,1]`-bounded) rather than `spec_r/g/b` at `:1180-1184`, and/or clamp the blended diffuse to `[0,1]`. Keep the mult-bearing `spec_*` for the `leaf.pbr==true` F0-luminance path where mult-as-scale is correct.

---

### Dimension 3: BA2 Reader (GNRL + DX10, Exhaustive Version Match) — PASS

`open()` dispatches version via a single `match` over `{1,7,8}→Zlib, 2, 3 (method-switched), _→Err}`. No cascading if/else, no silent v1 fall-through; guarded by `unknown_version_rejected` + `v3_unknown_compression_method_rejected`. GNRL byte-exact extract verified empirically (brute-force sweep of >30k v8 NIFs from `Fallout4 - Meshes.ba2`, zero errors). DX10 DDS reconstruction correct (real v7 cubemap arraySize=6 test passes; mip-chunk order + per-chunk raw-vs-codec axis correct). The 2026-06-02 "D2-LOW-01 (49/88/65)" claim was partially stale: formats 49 (R8G8) and 65 (A8) have ZERO occurrences in the entire FO4 corpus; only 88 (B8G8R8X8) is real.

#### FO4-D3-LOW-01: B8G8R8X8_UNORM (DXGI 88) absent from BA2 DDS pitch table
- **Severity**: LOW
- **Dimension**: BA2 Reader
- **Location**: `crates/bsa/src/ba2.rs:942-950` (`pitch_or_linear_size_for` bpp table)
- **Status**: NEW (partially supersedes prior-audit 2026-06-02 D2-LOW-01; no GitHub issue exists)
- **Description**: `pitch_or_linear_size_for` lists uncompressed bpp only for DXGI 28/29/87/91 (4bpp), 56 (2bpp), 61 (1bpp). DXGI 88 = B8G8R8X8_UNORM (4bpp) is missing, so it falls into the unknown-format fallback which writes `total_bytes` with `DDSD_LINEARSIZE` instead of row pitch `width*4` with `DDSD_PITCH`. Formats 49/65 from the prior audit do NOT occur in the FO4 corpus and are excluded.
- **Evidence**: Corpus DXGI scan over every `*.ba2` DX10 archive in the FO4 Data dir: `fmt88: 1482` occurrences — present in `unofficial fallout 4 patch - textures.ba2` (12×), `ss2extended` (1263×), settlement overhaul, etc. Vanilla base `Textures1..9.ba2` carry only 71/74/77/83/87/61/98 (all handled). Format 49/65: zero anywhere.
- **Impact**: BA2-side only: the synthesized DDS header has a wrong `dwPitchOrLinearSize` + flag. The in-engine loader ignores this field (DX10 extended header disambiguates), so this header bug is export-tooling-only (texconv/DirectXTex). The in-engine content blocker is the sibling renderer gap — see FO4-XCUT-MEDIUM-01.
- **Related**: FO4-XCUT-MEDIUM-01 (renderer `map_dxgi_format` gap); #1074 (added 56/61/87/91 to the same tables).
- **Suggested Fix**: ba2.rs:943 — add `88` to the 4-bpp arm: `28 | 29 | 87 | 88 | 91 => Some(4)`; add a unit test mirroring `pitch_bgra8_unorm_srgb_*` for format 88.

---

### Dimension 4: NIF BSVER 130 + Half-Float + Collision — 5/5 PASS

`half_to_f32` (`decode.rs:18`) handles every IEEE 754 binary16 class (zero/denormal/Inf/NaN-payload/normal); `full_precision = bsver<FALLOUT4 || (attrs & VF_FULL_PRECISION)` is correct (default-half on FO4+). BSSubIndexTriShape segment table + shared SSF walked, surfaced as `bs_sub_index`; skin weights/indices honor packed VF_SKINNED layout with partition-local→global remap. DLC trailing fields gated 130..=139 (subsurface/rimlight/backlight) and `FALLOUT4..FO4_DLC_UPPER` (SSR/skin-tint); Next-Gen BSVERs 132/139 dispatch. **#1242 rename complete** — zero `FO4_ENV_SCALE` hits in live `crates/` source. Both `BhkMultiSphereShape` and `BhkConvexListShape` have explicit `downcast_ref` arms producing `CollisionShape` before the "unsupported" fall-through; neither drops. 811 nif lib tests pass.

#### FO4-D4-LOW-01: No half_to_f32 IEEE 754 edge-case unit test (regression guard gap)
- **Severity**: LOW
- **Dimension**: NIF BSVER 130 + Half-Float + Collision
- **Location**: `crates/nif/src/import/mesh/decode.rs:18` (`half_to_f32`)
- **Status**: NEW
- **Description**: `half_to_f32` decode is correct today for every binary16 class, but no unit test exercises the NaN / denormal / Inf edge cases. The half-float path is the core of FO4 vertex decode (positions/normals default to half on BSVER ≥ 130); a future refactor of the bit-twiddling could silently regress denormal/NaN handling with no test to catch it.
- **Evidence**: grep for half-float tests finds normal-value round-trips only; no NaN/denormal/Inf assertion. All `half_to_f32` consumers route through the one canonical impl (no drift), so a single test pins all of them.
- **Impact**: Code-correctness today, missing regression guard. No content impact (decode is correct).
- **Related**: FO4 half-float vertex path (`bs_tri_shape.rs`).
- **Suggested Fix**: Add a unit test asserting `half_to_f32` against known binary16 → f32 values incl. +0/−0, smallest denormal, largest denormal, ±Inf, a NaN payload, and a normal mid-range value.

---

### Dimension 5: FO4 Shader Flags & BGSM PBR Routing — 5/5 PASS

Flags read as two distinct u32 at bsver 130 (CRC arrays at 132+); FO4 bit vocabulary (`shader_flags.rs::fo4_slsf1/fo4_slsf2`) fully nif.xml-pinned with tests. BGSM Name stopcond is FO76+-only (FO4 reads the full inline body + `root_material_path`); `material_path` flows to `MaterialInfo`. Per-shader-type texture-slot map (SkinTint/HairTint) in lockstep with the frag variant ladder (#563/#1350). PBR scalars consumed; vanilla FO4 BGSM correctly routes through PBR_BSDF (#1352 CLOSED). #1244 BSShaderPropertyBaseOnly wired. #1384 confirmed benign (three bitfields = 128u in different u32 words). #1500 (NORMAL_ALPHA_SPEC_BIT lockstep) is a Dim-7/renderer concern, cited not re-filed.

#### FO4-D5-MEDIUM-01: FO4 NIF shader-flag bits (model-space-normals / alpha-test / glow-map) parsed but never consumed at import
- **Severity**: MEDIUM
- **Dimension**: FO4 Shader Flags & BGSM PBR Routing
- **Location**: `crates/nif/src/import/material/walker.rs:126-339` (BSLightingShaderProperty arm); constants `crates/nif/src/shader_flags.rs:114-209` (`fo4_slsf1::MODEL_SPACE_NORMALS`=bit12, `fo4_slsf2::ALPHA_TEST`=bit25, `fo4_slsf2::GLOW_MAP`=bit6)
- **Status**: NEW
- **Description**: For an FO4 `BSLightingShaderProperty` (bsver 130) the parser reads the full F4SF1/F4SF2 u32 pair into `shader_flags_1/2`, but the walker only consumes decal, two-sided, and effect-shader bits. Three render-affecting FO4 bits are never tested: `MODEL_SPACE_NORMALS` (F4SF1 bit12), `ALPHA_TEST` (F4SF2 bit25), `GLOW_MAP` (F4SF2 bit6). These attributes are sourced exclusively from the companion BGSM/BGEM file.
- **Evidence**: grep for `model_space`/`0x1000`/`GLOW_MAP`/`fo4_slsf2::ALPHA_TEST` in `walker.rs` returns no consumption site (only a comment at :505). `model_space_normals` setters live only in `asset_provider.rs` (BGSM merge), never in the NIF property path.
- **Impact**: Vanilla FO4 is always BGSM/BGEM-backed (BGSM is authoritative), so vanilla content is unaffected. The gap bites inline-authored / loose / modded FO4 NIFs that set these bits in the NIF and ship no matching BGSM: an object-space normal map decoded as tangent-space (wrong shading), an inline alpha-tested cutout rendering opaque, a flag-only glow signal dropped. Visual-only, bounded to non-vanilla content.
- **Related**: #1352 (CLOSED — BGSM authoritative for MSN/PBR); #1384 (MODEL_SPACE_NORMALS bit value, benign); #1241 (PBR-scalar consumption pattern).
- **Suggested Fix**: In the BSLightingShaderProperty walker arm, OR the NIF flag-derived signals into MaterialInfo as a LOWER-priority source than the BGSM merge (`if shader_flags_1 & fo4_slsf1::MODEL_SPACE_NORMALS != 0 { info.model_space_normals = true; }`, likewise alpha-test/glow), keeping BGSM as the override. Handle the CRC32 path for bsver≥132 DLC content and gate the bit vocabulary on the FO4 variant so a Skyrim property isn't read with FO4 semantics.

---

### Dimension 6: ESM Architecture Records (SCOL/MOVS/PKIN/TXST) — 5/5 PASS

SCOL (per-instance placements), PKIN (CNAM bundles), MOVS (parse-only by design), TXST (8 slots + MNAM) all typed into expandable structs. TXST DODT (#813) + DNAM (#814) via `DecalData` intact in `cell/support.rs` — all 11 DecalData fields decoded with correct xEdit FO4 layout, DODT-only records survive the insertion gate. All five architecture maps surface in `categories()`. The prompt's "TXST arm in walkers.rs / unreachable_patterns" premise is stale — TXST parsing lives in `support.rs`; `cargo check` is warning-clean. Real-data harness on vanilla Fallout4.esm: total=84,751, scols=2,617 (40,330 placements), packins=872, movables=0, material_swaps=2,537, texture_sets=380, containers=471.

#### FO4-D6-MEDIUM-01: FO4 CONT (container) typed record parsed but never queried by the cell loader
- **Severity**: MEDIUM
- **Dimension**: ESM Architecture Records
- **Location**: `byroredux/src/cell_loader/references.rs:822-831` (REFR spawn looks up `index.statics` only); `crates/plugin/src/esm/records/mod.rs:406-413` + `crates/plugin/src/esm/records/container.rs:30-92` (CONT fully parsed into `index.containers`)
- **Status**: Existing: #1359
- **Description**: CONT records are dual-routed at parse time: `extract_records_with_modl` captures the MODL mesh in `index.statics` (container renders), and `parse_cont` captures `contents: Vec<InventoryEntry>` into `index.containers`. The cell loader's REFR-spawn path resolves base forms exclusively against `index.statics`; `index.containers` is referenced only in a comment. A container therefore renders as an inert static mesh with no inventory, activation, or interaction layer.
- **Evidence**: `grep -rn '\.containers' byroredux/src --exclude='*_tests*'` → only the comment, zero production consumers. Real-data: `containers=471` parsed, all unqueried.
- **Impact**: No container interaction on FO4 (or any game — game-agnostic gap). Loot/storage/quest-item containers non-functional. Mesh still renders via statics overlap, so it is a missing-feature gap (record parsed but unconsumed), not a parse failure.
- **Related**: #1359 (this exact issue, OPEN); FO4-D6-LOW-01 (MOVS, same shape); FO4-D9-MEDIUM-02 (BSConnectPoint, same shape).
- **Suggested Fix**: None new — #1359 tracks it. When categorised non-STAT spawn lands, route CONT REFRs through `index.containers` to attach an Inventory component.

#### FO4-D6-LOW-01: MOVS (movable static) records parsed into `index.movables` but never queried
- **Severity**: LOW
- **Dimension**: ESM Architecture Records
- **Location**: `crates/plugin/src/esm/cell/mod.rs:896-912` (`movables` map); `byroredux/src/cell_loader/` (no consumer)
- **Status**: NEW
- **Description**: `parse_movs` decodes EDID/MODL/LNAM/ZNAM/DEST/VMAD into `MovableStaticRecord`; records land in `EsmCellIndex.movables`, merged across plugins and surfaced in `categories()`. No production code reads `index.movables`. MOVS REFRs still render via the MODL-only catch-all into `index.statics`, so the mesh appears, but the loop/activate-sound IDs, destruction flag, and script flag are inert.
- **Evidence**: `grep -rn '\.movables\|MovableStatic' byroredux/src` outside tests → nothing. Real-data: `movables=0` for vanilla Fallout4.esm — zero vanilla records.
- **Impact**: None on vanilla FO4 (count 0). On DLC/mod content, movable statics render as immobile statics — the documented design state (MOVS is "parse-only; physics runtime not wired"). Known-gap / code-quality, not a bug.
- **Related**: #1359 (CONT — same pattern); #588 (MOVS parser); M28 Rapier bridge (eventual consumer).
- **Suggested Fix**: No urgent action. Optionally fold into #1359 so the unqueried-typed-record set (CONT, MOVS, activators/doors/npcs) is tracked as one categorised-spawn work item.

---

### Dimension 7: NIFAL Canonical Material Translation — PASS (all checklist + GPU layout)

`translate_material` is the only production `ImportedMesh→Material` site; both cell-loader REFR-spawn (`spawn.rs:857`) and loose-NIF (`nif_loader.rs:796`) route through it (no bypassing `Material {` literals outside `cornell.rs`/tests). `effect_shader_flags` is the exact OR of `pack_effect_shader_flags | pack_bgsm_material_flags | extra_material_flags`; `is_pbr→PBR_BSDF` set on all FO4 BGSM content. metalness/roughness are plain f32, NaN-seeded, `resolve_pbr` fills+clamps idempotently with no per-draw classifier. Glass classified after `resolve_pbr` (forced roughness 0.10 wins). GpuMaterial 300 B, offsets pinned by green tests (27 + 5 + 5).

#### FO4-D7-LOW-01: Stale "PBR_BSDF is a dead gate" / "set on BgsmFile.pbr == true" docs contradict the #1352 fix
- **Severity**: LOW
- **Dimension**: NIFAL Canonical Material Translation
- **Location**: `crates/renderer/src/vulkan/material.rs:452-455` (PBR_BSDF doc), `crates/renderer/src/vulkan/material.rs:516-520` (BGSM_AUTHORED doc)
- **Status**: NEW (doc-rot sibling of CLOSED #1352 / #1504)
- **Description**: Two doc comments still describe pre-#1352 routing: `PBR_BSDF` (452-455) "Currently set by the BGSM translator on `BgsmFile.pbr == true`"; `BGSM_AUTHORED` (516-520) "so `PBR_BSDF` alone is a dead gate." As of #1352, `merge_bgsm_into_mesh` sets `mesh.is_pbr = true` for all `from_bgsm` content unconditionally, and `pack_bgsm_material_flags` ORs `PBR_BSDF` whenever `mesh.is_pbr` is set — so PBR_BSDF is now set on every FO4 BGSM material, the opposite of a "dead gate."
- **Evidence**: `asset_provider.rs:1095` `mesh.is_pbr = true;` (unconditional); `cell_loader.rs:215-217` `if mesh.is_pbr { flags |= PBR_BSDF; }`; `triangle.frag:1013,3050` gate the Disney/Burley diffuse on `MAT_FLAG_PBR_BSDF`. #1504 revised the BGSM_AUTHORED doc but left the "dead gate" sentence intact.
- **Impact**: Documentation only — runtime is correct. Maintenance risk: a future engineer could wrongly conclude FO4 BGSM uses Lambert and regress #1352, or prune PBR_BSDF packing as dead.
- **Related**: #1352 (CLOSED, the fix this doc lags), #1504 (CLOSED, touched the same comment but missed this).
- **Suggested Fix**: Update material.rs:452-455 to state PBR_BSDF is set for all BGSM/BGEM-sourced content (via `is_pbr` → `pack_bgsm_material_flags`); delete the "so `PBR_BSDF` alone is a dead gate" sentence at 520.

---

### Dimension 8: FO4 Cell Load End-to-End (SCOL/PKIN Expansion) — 4/4 PASS

Expanders fan out to per-instance synthetic REFRs with composed parent×child transforms; `MAX_PKIN_DEPTH=4` is shared across PKIN→PKIN, PKIN→SCOL, SCOL→SCOL recursion (cycles terminate at a bounded leaf). `references.rs` fires `expand_pkin_placements` first, falling back to `expand_scol_placements`. 9 SCOL + 9 PKIN tests are real assertions (18/18 pass). De-dup is the `absorbed_refs` set (from XPRI, keyed on REFR placement form_id), checked before expansion: an absorbed SCOL/PKIN REFR's entire fan-out is suppressed when `pc_spawned>0`, and `EMPTY_ABSORBED` is substituted when `pc_spawned==0` so it renders normally (`bUseCombinedObjects=0` fallback). Composition order correct TRS parent∘child: `final_pos = outer_rot*(outer_scale*local_pos)+outer_pos`, `final_rot = outer_rot*local_rot`.

#### FO4-D8-LOW-01: SCOL transform composition only tested with identity outer rotation
- **Severity**: LOW
- **Dimension**: FO4 Cell Load End-to-End
- **Location**: `byroredux/src/cell_loader/scol_expansion_tests.rs:34,79,140,186,247,306,347` (all `outer_rot = Quat::IDENTITY`); code under test `byroredux/src/cell_loader/refr.rs:498-499`
- **Status**: NEW
- **Description**: Every SCOL expansion test passes `Quat::IDENTITY` as the outer REFR rotation, so `outer_rot * (outer_scale * local_pos)` and `outer_rot * local_rot` are never exercised with a non-identity rotation. With identity outer_rot the rotation-of-position degenerates to identity and the rotation product to `local_rot` alone — a transform-order regression would slip through. The PKIN multi-CNAM test uses a non-identity rot but PKIN children inherit the outer position verbatim, so it doesn't cover SCOL composition either.
- **Evidence**: grep `outer_rot`/`from_rotation` in scol_expansion_tests.rs shows `Quat::IDENTITY` at every SCOL call site; no assertion checks a rotated child position or a composed non-identity quaternion.
- **Impact**: A future composition-order swap would mislocate/mis-orient every multi-axis-rotated mod-added SCOL instance and the suite would stay green. Vanilla FO4 ships all SCOLs with cached CM*.NIF so the expansion branch only runs on mod content / previs-bypass — limited blast radius today.
- **Related**: #585 (SCOL expansion), #1182 (SCOL recursion).
- **Suggested Fix**: Add one test with non-identity `outer_rot` + non-zero `outer_pos` and a SCOL part with non-zero local pos + non-identity local rot, asserting the composed `final_pos`/`final_rot` to a small epsilon — pinning the parent∘child order.

---

### Dimension 9: Real-Data Validation + Forward Scope

**Measured parse rate: 100.00% clean / 0 truncated** — `Fallout4 - Meshes.ba2` 34,995/34,995 + `Fallout4 - MeshesExtra.ba2` 124,871/124,871 = 159,866 NIFs, 0 NiUnknown across 70 + 11 block types. Static import trace confirmed for all four content categories (settlement / creature / power-armor / modular weapon) — every relevant block (BSTriShape, BSSkin, BSConnectPoint, bhk*, BSSubIndexTriShape, CSG-shared geom) dispatches clean. `docs/feature-matrix.md` is clean (defers parse rates to ROADMAP). M49 CSG / BGSM / SCOL-PKIN confirmed shipped (FO4-D9-INFO-01). BSBehaviorGraphExtraData correctly parse-only (FO4-D9-INFO-02).

#### FO4-D9-MEDIUM-01: ROADMAP FO4 parse-rate row stale (96.46% → measured 100.00%)
- **Severity**: MEDIUM
- **Dimension**: Real-Data Validation + Forward Scope
- **Location**: `ROADMAP.md:201` (compat matrix), `ROADMAP.md:69` (95-97% band prose), `ROADMAP.md:733` (project-stats line)
- **Status**: NEW
- **Description**: The ROADMAP FO4 compat-matrix row reads `96.46% (33,757 / 34,995) · recover 100%` with the note "FaceGen NIFs dominate the truncation tail (1,235 of 1,238 truncated files)". The live re-run measures 100.00% clean / 0 truncated on BOTH base mesh archives. The 34,995 total matches Meshes.ba2 exactly — ROADMAP measured only the single base archive AND that figure is now stale (test floors recalibrated to 0.995 on 2026-06-14 per #1457). L69 and L733 carry the same stale number.
- **Evidence**: `parse_rate_fo4_all_meshes` → both archives 100.00% clean, 0 truncated. `nif_stats --tsv` → 0 NiUnknown. The FaceGen truncation tail (1,238) cited in the matrix is gone.
- **Impact**: Doc-rot misrepresenting capability. A reader/future audit would under-state FO4 coverage by ~3.5% and chase a non-existent FaceGen truncation tail. No open issue covers the FO4 figure.
- **Related**: #1457 (test floor calibration, CLOSED), #1218 (matrix drift, CLOSED), #1225 (FaceGen zero-mesh, CLOSED).
- **Suggested Fix**: Update the three ROADMAP sites to 100.00% clean / 100% recoverable (Meshes 34,995 + MeshesExtra 124,871, 159,866 total, 0 truncated), drop the FaceGen-truncation note, refresh the L69 band prose to exclude FO4, refresh the sweep date.

#### FO4-D9-MEDIUM-02: FO4 BSConnectPoint attach-point data parsed + lifted to ImportedScene but never consumed
- **Severity**: MEDIUM
- **Dimension**: Real-Data Validation + Forward Scope
- **Location**: `crates/nif/src/import/mod.rs:212-242` (lift); `crates/core/src/ecs/components/attach_points.rs` (`AttachPoints` / `ChildAttachConnections` defined + unit-tested only)
- **Status**: NEW
- **Description**: BSConnectPoint::Parents (1,124) and ::Children (1,055) parse cleanly and are lifted into `ImportedScene.attach_points` / `child_attach_connections` (the #985 work). The ECS components exist. But a repo-wide grep finds NO call site that spawns these components onto entities or reads them to drive attachment — the chain dead-ends at the import boundary (parse → ImportedScene → nothing). The code comment at `mod.rs:212-216` itself notes the OMOD/material-swap subsystem (#973) "can't function without these reaching the ECS."
- **Evidence**: `grep -rn '\.attach_points|\.child_attach_connections'` outside `crates/nif/src/import/*` → zero hits; `AttachPoints`/`ChildAttachConnections` referenced only in their own def + tests. 2,179 ConnectPoint blocks in Meshes.ba2 alone produce data dropped after import.
- **Impact**: Record-parsed-but-unconsumed. Modular weapons and power-armor frames import as their base receiver/frame only; mod parts (barrels, stocks, armor plates) attached via connect points are not positioned/spawned. Functional gap, not a crash.
- **Related**: #985 (lift to ImportedScene — done), #973 (OMOD subsystem — downstream); FO4-D6-MEDIUM-01 (CONT, same parsed-but-unconsumed shape).
- **Suggested Fix**: Add a cell-loader / npc-spawn step that materializes `ImportedScene.attach_points` into the `AttachPoints` ECS component on the spawned root entity, plus a system that resolves `child_attach_connections.point_names` to attach child meshes at the named parent connect point. File as the consumer half of the #985/#973 arc.

#### FO4-D9-LOW-01: #1446 (OPEN) describes CSG-deferred doc rot that has already been fixed in code
- **Severity**: LOW
- **Dimension**: Real-Data Validation + Forward Scope
- **Location**: `byroredux/src/cell_loader/load.rs:228-235`; `byroredux/src/cell_loader/precombined.rs:11-33`
- **Status**: Existing: #1446 (recommend CLOSE — premise stale)
- **Description**: #1446 cites `load.rs:231-239` and `precombined.rs:11-33` as still saying the CSG reader is deferred / "spawns zero entities". Reading those exact lines today: `precombined.rs:11` says "Current state (M49 — complete)" and `load.rs:231` says "resolved via `Fallout4 - Geometry.csg` (M49 complete)". Both comments are refreshed; the doc rot #1446 describes no longer exists. The remaining "Deferred sub-items" (`_precomb.nif` collision, `.uvd` occlusion) are genuinely deferred and correctly labeled.
- **Evidence**: Read of precombined.rs:11-33 + load.rs:228-247. The 2026-06-02 audit that filed #1446 said "Refresh both comments when #1351 is closed"; #1351 is CLOSED and the comments were refreshed, but #1446 was never closed alongside it.
- **Impact**: Stale issue-tracker entry. No code impact.
- **Related**: #1351 (M49 CSG render loop, CLOSED), #1446 (this — OPEN, stale).
- **Suggested Fix**: Close #1446 with a note that both cited comments are already refreshed. No code change.

---

## Cross-Cutting Findings

#### FO4-XCUT-MEDIUM-01: DXGI 88 (B8G8R8X8_UNORM) unhandled in the renderer DDS loader → format-88 textures render as missing-texture checker
- **Severity**: MEDIUM
- **Dimension**: BA2 Reader → Renderer (cross-cutting)
- **Location**: `crates/renderer/src/vulkan/dds.rs:205-242` (`map_dxgi_format`, no arm for 88 → `bail!("Unsupported DXGI format: 88")`)
- **Status**: NEW
- **Description**: `map_dxgi_format` has no arm for DXGI 88, so any format-88 DX10 texture fails the DDS load. The texture registry catches the `Err` and substitutes the magenta-checker fallback (verified `texture_registry.rs:109-119`), so it does not crash — but every format-88 texture renders as the missing-texture checker (the "Chrome → Missing Textures" symptom). DXGI 88 is absent from vanilla base archives but present in `Unofficial Fallout 4 Patch - textures.ba2` (12×) and several popular mods (SS2Extended 1263×, settlement overhaul, America Rising 2, Institute Reformation, etc.) — 1,482 occurrences in the scanned corpus.
- **Evidence**: `dds.rs:241 _ => bail!("Unsupported DXGI format: {}", dxgi)`. `texture_registry.rs:109` documents the magenta-checker substitution on load failure. Corpus DXGI scan: `fmt88: 1482`.
- **Impact**: Vanilla base-game FO4 is unaffected (no format-88 textures). Modded installs that include UFO4P (near-ubiquitous) get checker-rendered surfaces wherever a format-88 texture is referenced — a visible content gap, but bounded to a single format with a one-line fix. MEDIUM because vanilla is clean and the fix is trivial.
- **Related**: FO4-D3-LOW-01 (the BA2-side sibling — pitch table); #1074 (added 56/61/87/91 to the renderer + BA2 tables — the analogous prior fix).
- **Suggested Fix**: dds.rs:213 — add `DXGI_FORMAT_B8G8R8X8_UNORM (88) => Ok((vk::Format::B8G8R8A8_UNORM, 4, false))` (the X channel is "ignore"; B8G8R8A8 reads the same 4-byte BGRX layout, alpha sampled as 1.0). Pair with the FO4-D3-LOW-01 ba2.rs pitch-table fix and add a unit test for format 88.

---

## BGSM Consumption Table

Field × merged-into-`ImportedMesh`-by-`merge_bgsm_into_mesh` / surfaced-on-canonical-`Material` (post-NIFAL). NIF fields take precedence (every `fill()` only writes empty slots). Selected high-signal rows; full enumeration in `/tmp/audit/fo4/dim_2.md`.

| BGSM field | Merged into ImportedMesh | Surfaced on Material |
|---|---|---|
| `alpha`, `u/v_offset`, `u/v_scale` | `mat_alpha`, `uv_offset`, `uv_scale` | `alpha`, `uv_offset`, `uv_scale` |
| `alpha_test` + `alpha_test_ref` | `alpha_test` + `alpha_threshold` (/255) | `alpha_test`/`alpha_threshold` |
| `two_sided`, `decal` | `two_sided` (OR), `is_decal` (OR) | TwoSided marker / decal path |
| `diffuse/normal/smooth_spec/glow/...texture` | `texture_path`/`normal_map`/`gloss_map`/`glow_map`/... (fill) | corresponding `Material` slots |
| `specular_color` | metalness deriv (`pbr?lum:saturation`) + `specular_color` + conductor diffuse tint | `metalness` (resolved), `specular_color` |
| `specular_mult` | metalness (pbr only) + `specular_strength` + diffuse tint (**FO4-D2-MEDIUM-01**) | `specular_strength` |
| `smoothness` | `roughness_override = 1-smoothness` + `glossiness = smoothness*100` | `roughness` (clamp-only), `glossiness` |
| `pbr` | metalness branch select; `is_pbr=true` (unconditional, #1352) | PBR_BSDF flag |
| `emit_enabled`+`emittance_color/mult` | `emissive_color/mult` (gated) | `emissive_color/mult` |
| `model_space_normals` | `model_space_normals` (OR) | MODEL_SPACE_NORMALS flag |
| `fresnel_power` (#1454), `grayscale_to_palette_scale` (#1455) | `fresnel_power`, `grayscale_to_palette_scale` | fresnel / palette scale |
| `translucency` suite (#1147) | `has_translucency` + subsurface/transmissive/turbulence/thick/mix | TRANSLUCENCY(+THICK/MIX) flags |
| `root_material_path` | drives template chain (resolve_bgsm) | — |
| `wetness_control_*`, `custom_porosity`, `lum_emittance`, `aniso_lighting`, `hair`/`tree`/`facegen`/`skin_tint`, `tessellate`, `terrain`+ | NOT consumed | — |
| **BGEM**: `base_texture`/`normal`/`glow`/`grayscale`/`envmap`/`envmap_mask`/`specular`/`lighting` | corresponding `*_map`/`env_*` slots (fill) | corresponding `Material` slots |
| **BGEM**: `base_color`+`base_color_scale` | `emissive_color/mult` + `emissive_source = Effect` (#1358) | `emissive_*`, EmissiveSource::Effect |
| **BGEM**: `glass_enabled` (v≥21) | `bgem_glass = true` (#1280) | glass classify path |
| **BGEM**: glass overlay textures, `emittance_color`, `effect_pbr_specular`, falloff suite | NOT consumed (falloff read NIF-side) | — |

---

## Forward Scope Chain

Do NOT re-file as blockers — these are legitimate forward scope, ordered by dependency:

1. **Precombine collision + `.uvd` occlusion** — `_precomb.nif` collision and `.uvd` occlusion volumes are not read (the CSG render pipeline is shipped; physics/occlusion companions are not).
2. **Record-consumer arc** — CONT inventory/interaction (#1359, FO4-D6-MEDIUM-01), BSConnectPoint attach (FO4-D9-MEDIUM-02), MOVS physics/sound/destruction runtime (FO4-D6-LOW-01) — all parsed, none consumed. Best tracked as one categorised-non-STAT-spawn work item.
3. **OMOD / material-swap assembly (#973)** — depends on (2)'s BSConnectPoint consumer for modular weapons / power-armor.
4. **Deeper cell coverage** — LIGH power state, CONT leveled items, NPC_ face morph.
5. **Havok behavior-graph driving** — BSBehaviorGraphExtraData is correctly parse-only (FO4-D9-INFO-02); driving it depends on the M41.x `.hkx` runtime.

> The M49 CSG reader, the BGSM/BGEM parser, and SCOL/PKIN expansion are **all shipped** — never list them as pending.

---

## Finding Index

| ID | Severity | Status | Title |
|----|----------|--------|-------|
| FO4-D1-MED-01 | MEDIUM | NEW | DLC/multi-master precombines resolve wrong CSG (filename_hash ignored) |
| FO4-D2-MEDIUM-01 | MEDIUM | NEW | Conductor diffuse-tint blend folds in specular_mult (#1476 mult-invariance) |
| FO4-D5-MEDIUM-01 | MEDIUM | NEW | FO4 NIF shader-flag bits (MSN/alpha-test/glow) parsed but unconsumed |
| FO4-D6-MEDIUM-01 | MEDIUM | Existing: #1359 | FO4 CONT typed record parsed but never queried |
| FO4-D9-MEDIUM-01 | MEDIUM | NEW | ROADMAP FO4 parse-rate row stale (96.46% → 100.00%) |
| FO4-D9-MEDIUM-02 | MEDIUM | NEW | BSConnectPoint attach data lifted to ImportedScene but never consumed |
| FO4-XCUT-MEDIUM-01 | MEDIUM | NEW | DXGI 88 unhandled in renderer DDS loader → checker (UFO4P + mods) |
| FO4-D1-LOW-01 | LOW | NEW | Precombine triangle indices never range-checked vs num_verts |
| FO4-D3-LOW-01 | LOW | NEW | DXGI 88 absent from BA2 DDS pitch table |
| FO4-D4-LOW-01 | LOW | NEW | No half_to_f32 IEEE 754 edge-case unit test |
| FO4-D6-LOW-01 | LOW | NEW | MOVS records parsed into index.movables but never queried |
| FO4-D7-LOW-01 | LOW | NEW | Stale "PBR_BSDF is a dead gate" docs contradict #1352 |
| FO4-D8-LOW-01 | LOW | NEW | SCOL transform composition only tested with identity outer rotation |
| FO4-D9-LOW-01 | LOW | Existing: #1446 | #1446 describes CSG-deferred doc rot already fixed (recommend CLOSE) |

**Counts (NEW + Existing, excluding INFO):** 0 CRITICAL · 0 HIGH · 7 MEDIUM · 7 LOW · 14 TOTAL.
Of these, 12 are NEW; 2 are Existing (FO4-D6-MEDIUM-01 → #1359, FO4-D9-LOW-01 → #1446).

---

*Suggested next step:* `/audit-publish docs/audits/AUDIT_FO4_2026-06-14.md`
