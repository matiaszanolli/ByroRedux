# ESM / Plugin Parser Audit — 2026-08-27

**Command**: `/audit-esm` (all 8 dimensions, `--depth deep`), run as part of an
`/audit-suite --preset comprehensive` sweep. Performed **solo — no sub-agent
fan-out** (per explicit dispatch instruction; nested audit sub-agent result
relay is unreliable in this environment, see *feedback_audit_suite_nested_agent_relay*).
**Subject**: `crates/plugin/` — the ESM/ESP parser: header + GRUP walk,
sub-record byte accounting, per-record schema dispatch, FormID load-order
remap, CELL/WRLD walkers, localized strings, `EsmIndex` → ECS handoff.
**Tree**: `main` @ `969d81c8`.
**Delta basis**: 24 commits touching `crates/plugin/` since the previous ESM
sweep's baseline (`048a8bd8`, `docs/audits/AUDIT_ESM_2026-08-24.md`) —
**+3,026 / −499 across 36 files**. Dominated by an FNV audit burst (`#3313`–
`#3325`), the `#3300` NAVM door/cover/grid decode, the `#3356` OTFT `INAM`
array fix, `#3357`'s multi-ARMA armor resolution, `#3384`'s `character_rules`
merge guard, and `#3314`'s CELL/WRLD/LAND FormID-remap sweep.
**Dedup basis**: `docs/audits/AUDIT_ESM_2026-08-24.md` read in full;
`AUDIT_ESM_2026-08-20.md` / `AUDIT_ESM_2026-08-13.md` grepped for every
finding keyword below; `gh issue list --limit 300` (139 open) plus targeted
`gh issue view` / `gh search issues` on `#3237`, `#3270`, `#3300`, `#3314`,
`#2611`, `#2636`, `#3395`, `#3397`, and keyword searches for
`decompressed_size`, `peak RSS`, `EsmIndex RAM`, `catch-all GRUP`.
**Method**: `cargo test -p byroredux-plugin` and
`cargo test -p byroredux-plugin --test parse_real_esm -- --ignored` both run
green. Real-data evidence was gathered with the crate's own probe binaries
(`esm_dim8_bench`, `esm_dim8_coverage`) plus two standalone Python walkers
(`hedr.py`, `mast.py`) that read the TES4 header **outside this parser**, so a
parser bug cannot hide inside the measurement. No windowed engine instance was
launched (*feedback_no_parallel_engine_launch*).

---

## Executive Summary

**7 new findings: 0 CRITICAL, 3 HIGH, 2 MEDIUM, 2 LOW.**

The delta itself is high quality — every one of the eight `#33xx` fixes I
re-derived is correct and carries real-data evidence in-code. The findings
below are almost all **residue**: two of the three HIGHs are the unfinished
halves of remediations that were declared complete (`#3237`'s GRUP depth
bound, `#3314`'s FormID-remap sweep), and one is a long-standing
untrusted-input hardening gap in the compressed-record path that no prior
sweep has looked at.

The single most important disposition point: **`#3237` is now CLOSED on
GitHub, and eight recursive GRUP walkers are still unbounded.** The
2026-08-24 report said explicitly *"Do not close #3237 on this pass's fix
alone"*; it was closed anyway, and the eight walkers it named are byte-for-byte
unchanged at HEAD.

| Dim | Area | HIGH | MED | LOW | Note |
|---|---|---:|---:|---:|---|
| 1 | Header & GRUP Walk | **2** | 0 | 1 | `#3237` incompletely fixed but closed; compressed-record prefix trusted for allocation; FO76 HEDR sample value is doc-rot |
| 2 | Sub-Record Byte Accounting | 0 | 0 | 1 | new `VATS` / `NVDP` / `NVGD` / `INAM` decodes all length-guarded and evidence-backed; `rows()` silently tolerates a stride remainder |
| 3 | FormID Remap & Load Order | **1** | 1 | 0 | `#3314` fixed `cell/`; `records/` still has ~12 un-remapped embedded-FormID sites, two of them live |
| 4 | Record Schema Dispatch | 0 | 0 | 0 | `SECH`/`AOPF` added correctly; coverage matrix refreshed below |
| 5 | CELL / WRLD Walkers | 0 | 0 | 0 | XCLW tri-state intact; `#3300` NAVM decode reconciles exactly; `#3270` fix verified present |
| 6 | Localized Strings | 0 | 0 | 0 | re-derived this pass (untouched since 2026-08-20): every blob offset bounds-checked, no panic path found |
| 7 | `EsmIndex` → ECS Handoff | 0 | 1 | 1 | `merge_from`'s `game` field has the same empty-index hazard `#3384` just fixed for `character_rules` — flagged by the fixing commit's own comment; `resolve_armor_mesh` now dead |
| 8 | Real-Data Validation | — | — | — | 7 masters parsed; coverage matrix + parse-cost table refreshed (first time since 2026-08-13) |
| | **Total** | **3** | **2** | **2** | |

---

## Findings — HIGH (3)

### ESM-2026-08-27-D1-01: `#3237` was closed with eight GRUP walkers still recursing without a depth bound — a crafted plugin still stack-overflows the process

- **Severity**: HIGH
- **Dimension**: Header & GRUP Walk
- **Record / Sub-record**: `GRUP` (`STAT`/`LTEX`/`TXST`/`SCOL`/`PKIN`/`MOVS`/`MSWP`/`REFR` labels)
- **Location**: `crates/plugin/src/esm/cell/support.rs:351` (`parse_modl_group`,
  fn at `:342`), `:384` (`parse_ltex_group`, `:375`), `:442`
  (`parse_txst_group`, `:432`), `:564` (`parse_scol_group`, `:555`), `:633`
  (`parse_pkin_group`, `:624`), `:698` (`parse_movs_group`, `:689`), `:759`
  (`parse_mswp_group`, `:751`); `crates/plugin/src/esm/cell/walkers.rs:665`
  (`parse_refr_group`, fn at `:653`)
- **Status**: **Regression of #3237** (CLOSED) — the issue is closed, the
  defect is not fixed
- **Description**: `#3237` ("SAFE-D2: ESM/ESP GRUP-tree recursion has no depth
  bound — crafted plugin stack-overflows the process", labelled `bug` /
  `high` / `esm-plugin`) was partially fixed by `06f86742`, which added
  `EsmReader::bounded_group_content_end` (`reader.rs:768-786`) and wired it
  into `grup_walker.rs`'s four recursive extractors plus `parse_cell_group`
  and `parse_wrld_children`. Eight *other* self-recursive GRUP walkers were
  never converted and still call the raw, unbounded
  `reader.group_content_end(&sub)`. The 2026-08-24 audit enumerated all eight
  by name and wrote *"Do not close #3237 on this pass's fix alone"*; the issue
  is nevertheless CLOSED at the time of this audit, and `git log --grep=3237`
  returns no commit at all — nothing since `06f86742` has touched these sites.
- **Evidence**: current HEAD, `crates/plugin/src/esm/cell/support.rs:348-356`:
  ```rust
  while reader.position() < end && reader.remaining() > 0 {
      if reader.is_group() {
          let sub = reader.read_group_header()?;
          let sub_end = reader.group_content_end(&sub);
          parse_modl_group(reader, sub_end, statics)?;
          continue;
      }
  ```
  Identical shape at all seven `support.rs` sites and at
  `walkers.rs:662-669` (`parse_refr_group`, which recurses into itself with
  no `depth` parameter anywhere in its signature). Contrast
  `grup_walker.rs:94-100`, the converted form:
  ```rust
  let Some(sub_end) =
      reader.bounded_group_content_end(&sub_group, depth, "extract_records")
  else {
      continue;
  };
  extract_records_inner(reader, sub_end, expected_type, f, depth + 1)?;
  ```
  A minimal `GRUP` header is 24 bytes (20 on Oblivion), so a ~50 KB plugin
  nesting one of these eight labels ~2,000 levels deep exhausts the stack.
  `parse_modl_group` alone is the walker for `STAT` / `MSTT` / `FURN` /
  `DOOR` / `ACTI` / `CONT` / `LIGH` / `MISC` — it runs on every plugin a user
  loads, including third-party mod content.
  I separately confirmed `parse_wrld_group` (`wrld.rs:28`) is **not** a
  regression case: its `group_content_end` result feeds only the non-recursive
  type-1 arm that calls the already-bounded `parse_wrld_children`.
- **Impact**: Identical to `#3237`'s original impact — unrecoverable process
  termination (stack overflow abort, not a catchable `Err`) on hostile or
  corrupt plugin input, on a code path reached by every ESM/ESP load. No
  workaround.
- **Related**: `#3237` (CLOSED, should be reopened);
  `docs/audits/AUDIT_ESM_2026-08-24.md` Prior-Findings row for `#3237`;
  `docs/audits/AUDIT_SAFETY_2026-08-23.md` (SAFE-D2-2026-08-23-01).
- **Suggested Fix**: Mechanical — thread a `depth: u32` through each of the
  eight walkers exactly as `grup_walker.rs` does (public wrapper at depth 0,
  private `_inner` recursion at `depth + 1`), routing `sub_end` through
  `bounded_group_content_end`. Then reopen or re-file `#3237` rather than
  leaving a closed issue over live code.

### ESM-2026-08-27-D1-02: the compressed-record path trusts the 4-byte uncompressed-size prefix for allocation and puts no ceiling on the inflated output

- **Severity**: HIGH
- **Dimension**: Header & GRUP Walk (record decompression)
- **Record / Sub-record**: any record with `FLAG_COMPRESSED` (`0x00040000`)
- **Location**: `crates/plugin/src/esm/reader.rs:630-647`
- **Status**: NEW
- **Description**: `EsmReader::read_sub_records` correctly checks
  `data_size >= 4` before subtracting the prefix (that half of the skill's
  checklist is satisfied, and `compressed_record_too_small_returns_error`
  pins it). It then takes the record's own 4-byte prefix — a value entirely
  under the file's control, range `0..=0xFFFF_FFFF` — and passes it straight
  to `Vec::with_capacity`, and inflates with `read_to_end`, which has no
  output limit at all. Neither the reservation nor the inflation is bounded
  by anything derived from the actual compressed length.
- **Evidence**: `crates/plugin/src/esm/reader.rs:630-647`:
  ```rust
  let raw_data = if header.flags & FLAG_COMPRESSED != 0 {
      // First 4 bytes = uncompressed size, rest is zlib.
      ensure!(header.data_size >= 4, "Compressed record too small");
      let decompressed_size = self.read_u32() as usize;
      let compressed_len = header.data_size as usize - 4;
      ensure!(self.remaining() >= compressed_len, "Truncated compressed data");
      let compressed = &self.data[self.pos..self.pos + compressed_len];
      self.pos += compressed_len;

      let mut decoder = ZlibDecoder::new(compressed);
      let mut decompressed = Vec::with_capacity(decompressed_size);
      decoder
          .read_to_end(&mut decompressed)
          .context("Failed to decompress ESM record")?;
      decompressed
  }
  ```
  The only test covering the prefix,
  `compressed_record_prefix_matches_payload_length` (`reader.rs:1618-1651`),
  builds a *well-formed* record and asserts the round-trip — its own doc
  comment frames the prefix as a "capacity hint … a mismatch here would panic
  or silently truncate on strict allocators", i.e. it tests that a correct
  prefix works, never that a lying one is rejected. There is no test for a
  hostile prefix and no upper bound anywhere in the function.
- **Impact**: Two distinct untrusted-input failures on the same three lines.
  (a) A 24-byte record header plus a 4-byte prefix of `0xFFFFFFFF` requests a
  4 GiB reservation before a single byte is inflated. (b) More seriously,
  `read_to_end` is unbounded independently of the prefix: zlib's practical
  ratio exceeds 1000:1, so a 100 MB compressed record inside an otherwise
  ordinary-looking plugin inflates toward ~100 GB and terminates the process
  on allocation failure. Same trigger model and same unrecoverable outcome as
  `#3237` (crafted plugin → process death in the ESM parser), which is why
  this carries the same severity.
- **Related**: `#3237` (same untrusted-plugin threat model, HIGH); `#990`
  (CLOSED — added the zlib unit tests, but only happy-path ones);
  `crates/bsa`'s decompressors are a separate surface owned by `/audit-nif`.
- **Suggested Fix**: Bound both halves against the compressed length. Clamp
  the capacity hint to `min(decompressed_size, some_multiple_of(compressed_len))`
  so a lying prefix costs nothing, and replace `read_to_end` with
  `Read::take(limit).read_to_end(..)` where `limit` is that same ceiling,
  returning `Err` when the limit is hit so the record is diagnosably rejected
  rather than silently truncated. Add the two missing negative tests (lying
  prefix; over-ratio payload).

### ESM-2026-08-27-D3-01: `SCOL` and `PKIN` child FormIDs bypass the load-order remap — the second and later FO4 DLC's static collections and package-ins lose every child

- **Severity**: HIGH
- **Dimension**: FormID Remap, Load Order & ESL Space
- **Record / Sub-record**: `SCOL` / `ONAM`, `FLTR`; `PKIN` / `CNAM`, `VNAM`
- **Location**: `crates/plugin/src/esm/records/scol.rs:153-158` (`ONAM`) and
  `:197-201` (`FLTR`); `crates/plugin/src/esm/records/pkin.rs:96` (`CNAM`) and
  `:101` (`VNAM`). Consumers:
  `byroredux/src/cell_loader/refr.rs:602-631`
  (`expand_scol_placements_with_depth`) and `:495-505`
  (`expand_packin_placements_with_depth`)
- **Status**: NEW (same defect class as **#3314**, CLOSED — which fixed
  `cell/` and `records/` was never swept)
- **Description**: `#3314` established the crate's rule and made it structural
  for the cell tier: `read_form_id` in `crates/plugin/src/esm/cell/helpers.rs:29`
  now takes `&EsmReader` as a *required* parameter precisely so a sub-record
  FormID cannot be read without being remapped. The record tier was not
  swept. `parse_scol` and `parse_pkin` take no remap at all and store their
  child references raw, and those raw values are then looked up in
  `index.scols` / `index.packins` / `index.statics` — every one of which is
  keyed by the **remapped** id, because `EsmReader::read_record_header`
  (`reader.rs:592`) remaps every record's own FormID.
- **Evidence**: `crates/plugin/src/esm/records/pkin.rs:94-97`
  ```rust
  b"CNAM" => {
      if let Some(form) = read_u32(&sub.data) {
          contents.push(form);
      }
  }
  ```
  `crates/plugin/src/esm/records/scol.rs:151-160` is the same shape for
  `ONAM`. Consumed unremapped at `byroredux/src/cell_loader/refr.rs:611-631`:
  ```rust
  for part in &scol.parts {
      for p in &part.placements {
          ...
          if depth + 1 < MAX_PKIN_DEPTH && index.scols.contains_key(&part.base_form_id) {
  ```
  and `refr.rs:495-505` (`index.packins.get(&base_form_id)` /
  `index.packins.contains_key(&child_form_id)`).

  The remap is non-identity on ordinary multi-DLC load orders. Master lists
  read directly off disk (`mast.py`, outside this parser):
  ```
  Fallout4.esm       []
  DLCRobot.esm       ['Fallout4.esm']
  DLCCoast.esm       ['Fallout4.esm']
  DLCNukaWorld.esm   ['Fallout4.esm']
  ```
  Every FO4 DLC has exactly one master, so its self mod-index is `0x01`.
  `allocate_global_slot` (`byroredux/src/cell_loader/load_order.rs:296+`)
  assigns global slots by position, so in
  `[Fallout4.esm, DLCRobot.esm, DLCCoast.esm]` `DLCCoast` gets slot `0x02`
  while its own forms are authored `0x01…` — `FormIdRemap::remap`
  (`reader.rs:374-419`) rewrites its record keys to `0x02…`, and every raw
  `ONAM`/`CNAM` still says `0x01…`. The same arithmetic makes `HearthFires.esm`
  (slot 3, self index 2) and `Dragonborn.esm` (slot 4, self index 2)
  non-identity in the vanilla five-plugin Skyrim order, and **any** ESL is
  non-identity by construction (`GlobalSlot::Light` composes into `0xFE…`).
- **Impact**: On the second and later DLC/mod plugin of an FO4 load order,
  every `SCOL` child lookup and every `PKIN` `contents` lookup misses.
  `expand_scol_placements_with_depth` emits placements whose base form
  resolves to nothing, and `expand_packin_placements_with_depth` returns
  `None` for the whole package-in — so mod- and DLC-added static collections
  and package-ins (`FalloutNV.esm` alone ships 98 SCOLs behind 1,084 REFRs;
  vanilla FO4 ships 2,617) render as empty space rather than as geometry.
  Silent: the miss is indistinguishable from an unshipped `CM*.NIF`.
- **Related**: `#3314` (CLOSED — the CELL/WRLD/LAND half of exactly this);
  ESM-2026-08-27-D3-02 below (the latent remainder of the same sweep).
- **Suggested Fix**: `parse_scol_group` / `parse_pkin_group`
  (`cell/support.rs:555`, `:624`) already hold a `&mut EsmReader` and their
  sibling `parse_modl_group` already calls `reader.get_form_id_remap()` at
  `:346`. Thread `&Option<FormIdRemap>` into `parse_scol` / `parse_pkin` and
  apply it to `ONAM` / `FLTR` / `CNAM` / `VNAM`, preserving the
  null-is-not-a-form rule `FormIdRemap::remap` already implements. Pin it with
  a two-plugin `merge_from` fixture, the shape `#2991`'s regression test uses.

---

## Findings — MEDIUM (2)

### ESM-2026-08-27-D3-02: ~12 more embedded-FormID sites in `records/` still bypass the remap, including two inside a function that already holds it

- **Severity**: MEDIUM
- **Dimension**: FormID Remap, Load Order & ESL Space
- **Record / Sub-record**: `ACTI`/`SNAM`,`RNAM`,`RADR`; `MOVS`/`LNAM`,`ZNAM`;
  `NAVM`/`DATA`,`NVEX`,`NVDP`,`NVNM`; `FLST`/`LNAM`; `TREE`; `REGN`/`RDWT`,`RDSD`
- **Location**: `crates/plugin/src/esm/records/misc/world.rs:1288-1291`
  (`parse_acti`), `:322` (`NavmRecord::cell_form`), `:594`
  (`NavmExternalConnection::mesh_form`), `:486` + `:539`
  (`NavmDoorTriangle::door_form_id`, both added this delta by `#3300`);
  `crates/plugin/src/esm/records/movs.rs:102-112`;
  `crates/plugin/src/esm/records/list_record.rs:69-73`
- **Status**: NEW (latent half of the **#3314** class; kept separate from
  ESM-2026-08-27-D3-01 because these have no live consumer yet)
- **Description**: The sharpest instance is `parse_acti`, which *receives*
  `remap: &Option<FormIdRemap>`, applies it to the shared named-field helper,
  and then reads two more FormIDs raw twelve lines later:
  ```rust
  pub fn parse_acti(form_id: u32, subs: &[SubRecord], remap: &Option<FormIdRemap>) -> ActiRecord {
      let common = CommonNamedFields::from_subs_with_remap(subs, remap);   // :1276 — remapped
      ...
      b"SNAM" => out.sound_form_id = SubReader::new(&sub.data).u32_or_default(),   // :1288 — raw
      b"RNAM" | b"RADR" => {
          out.radio_form_id = SubReader::new(&sub.data).u32_or_default();          // :1290 — raw
      }
  ```
  `parse_navm` (`world.rs:304`) takes no remap at all and stores three
  cross-record references raw — the parent `CELL` (`DATA` word 0), the
  `NAVM` on the far side of each external connection (`NVEX`/packed
  `mesh_form`), and, new this delta, the door `REFR` per threshold triangle
  (`NVDP` stride-8 row and the packed stride-10 row). `parse_movs`,
  `parse_flst` and `parse_tree` are the same shape. `grep -c remap` returns
  **0** for `list_record.rs`, `scol.rs`, `tree.rs`, `pkin.rs`, `movs.rs`,
  `mswp.rs`, `soun.rs`, `misc/character.rs`, `misc/imagespace.rs`.
- **Impact**: Latent today — I traced consumers for each and found none:
  `sound_form_id` / `radio_form_id` / `loop_sound_form_id` /
  `activate_sound_form_id` have zero references outside `crates/plugin`, and
  `EsmIndex::form_lists` is only populated, never read. The `NAVM` references
  are the ones nearest to a consumer (`#2372` / EX-16 wants exactly the
  cross-tile join and door association these fields carry). Filed so the
  first consumer does not inherit a silently-wrong map key, which is precisely
  how `#3314` came about. Severity is MEDIUM rather than HIGH only because
  nothing reads them yet.
- **Related**: `#3314`; `#2372` (EX-16, the NAVM consumer that will make this
  live); `#3300` (added the `NVDP` fields).
- **Suggested Fix**: Finish the `#3314` sweep on the record tier the same way
  it was finished on the cell tier — make the remap a required parameter
  rather than an optional courtesy. The dispatch routers already resolve
  `reader.get_form_id_remap()` for `NPC_`/`CREA`/`FACT`/`WATR`/`PACK`/`SCEN`/
  `PERK`, so the plumbing exists; the change is per-parser signature work
  plus one grep-based guard test.

### ESM-2026-08-27-D7-01: `merge_from` last-write-wins on `EsmIndex::game` — one failed plugin parse silently re-labels the whole load order as Fallout 3/NV

- **Severity**: MEDIUM
- **Dimension**: `EsmIndex` → ECS Handoff
- **Record / Sub-record**: `TES4` / `HEDR`
- **Location**: `crates/plugin/src/esm/records/index.rs:848`;
  reachable via `byroredux/src/cell_loader/load_order.rs:285-290`
- **Status**: NEW — and explicitly deferred to this audit by the commit that
  fixed the sibling case
- **Description**: `#3384` (`aec3e1ca`) fixed exactly this hazard for
  `character_rules`: `merge_from` used to do an unconditional last-write-wins,
  and the load-order driver merges an `EsmIndex::default()` whenever a plugin
  fails to parse, so a failure in the *last* plugin adopted the default and
  switched the whole character layer off. The fix guarded `character_rules`
  with first-non-`NONE`-wins — and left the identically-shaped `game` field
  one line above it unguarded, with a comment saying so:
  ```rust
  // NOTE: this field is deliberately *not* given the `character_rules`
  // guard below. Callers construct an index and set `game` without ever
  // setting `character_rules` (the FO4 inventory-classification path
  // does exactly that), so gating the two together would silently drop
  // the game. The same empty-index hazard applies to `game` in
  // principle; it is out of scope here and belongs to /audit-esm.
  self.game = other.game;
  ```
  (`index.rs:840-848`.) The reasoning for not reusing the *same* guard is
  sound; the hazard it names is real and is now this audit's to file.
- **Evidence**: `byroredux/src/cell_loader/load_order.rs:285-290`:
  ```rust
  let plugin_records = esm::records::parse_esm_with_load_order(&bytes, Some(remap))
      .unwrap_or_else(|e| {
          log::warn!("Record parse failed for '{}': {}", path, e);
          esm::records::EsmIndex::default()
      });
  merged.merge_from(plugin_records);
  ```
  `EsmIndex::default()` carries `game: GameKind::default()` =
  `GameKind::Fallout3NV` (`reader.rs:96-105`), so the last plugin's parse
  failure overwrites a correctly detected `Skyrim` / `Fallout4` /
  `Starfield` with `Fallout3NV` behind a single `warn`.
- **Impact**: `EsmIndex::game` is a broad dispatch key, not a label. Live
  consumers include `byroredux/src/npc_spawn.rs` (`humanoid_skeleton_path`,
  `humanoid_body_path_biped_mask`, `humanoid_default_idle_kf_path`,
  `sandbox_sit_enter_kf_path`), `byroredux/src/asset_provider/script.rs:279`
  and `:352` (Papyrus `.pex` translation), `byroredux/src/asset_provider/animation.rs:30`
  (the Havok slice's Skyrim gate), `byroredux/src/env_translate.rs`
  (`terrain_lod_layout`), `byroredux/src/cell_loader/placement_lod.rs:313`,
  `byroredux/src/inventory.rs:43` (`player_npc_form_id`) and
  `byroredux/src/streaming.rs:834`. A misclassified load order therefore
  spawns actors with the wrong skeleton, resolves the wrong player base form,
  and picks the wrong terrain-LOD layout — all downstream of a warning about
  an unrelated subject.
- **Related**: `#3384` (the `character_rules` half, fixed); `#2907` (the
  earlier `merge_from` completeness fix).
- **Suggested Fix**: Do not reuse the `character_rules` guard (the comment
  explains why). Instead make the failure site honest: either propagate the
  parse error out of `parse_record_indexes_in_load_order` rather than merging
  a default index, or have `merge_from` skip `game` when `other` is
  observably empty (`other.total() == 0`). The second is a one-line change
  and matches the "a failed parse contributes nothing" intent the `warn!`
  already implies.

---

## Findings — LOW (2)

### ESM-2026-08-27-D2-01: `rows()` silently drops a non-zero stride remainder, unlike every other decoder in the same file

- **Severity**: LOW
- **Dimension**: Sub-Record Byte Accounting
- **Record / Sub-record**: `NAVM` / `NVVX`, `NVTR`, `NVEX`, `NVDP`, `NVCA`; `REGN` / `RDWT`
- **Location**: `crates/plugin/src/esm/records/misc/world.rs:801-803`
- **Status**: NEW
- **Description**: Every repeating-row sub-record in `world.rs` goes through
  ```rust
  fn rows(data: &[u8], stride: usize) -> impl Iterator<Item = &[u8]> {
      data.chunks_exact(stride.max(1))
  }
  ```
  `chunks_exact` discards the remainder with no signal. The file's own
  posture everywhere else is the opposite: `decode_nvgd` and `decode_nvnm`
  both return `None` unless the payload is consumed *exactly*, with the
  rationale written out at `world.rs:545-551` — *"the same all-or-nothing
  posture … so a format revision surfaces as 'not recognised' rather than as
  a half-filled lattice"*. `decode_weather_rows` even has an `exact` flag
  doing the remainder check by hand. `rows()` is the one path with neither.
- **Evidence**: The `#3300` doc comments state the strides were established by
  taking the GCD of observed payload lengths across 11,969 shipped FO3+FNV
  meshes and cross-checking against `DATA` words 1–5, so on shipped data every
  remainder is zero today. That is exactly the condition under which a silent
  drop is invisible: the first game (or the first mod) whose `NVDP` stride is
  not 8 will decode a truncated door list and report success.
- **Impact**: No known incorrect output on shipped data. The gap is
  diagnostic: a stride that is wrong for a future game degrades to partial
  data with no error, no log and no counter.
- **Related**: `#3300`; the skill's Dimension 2 repeating-row checklist item.
- **Suggested Fix**: Give `rows()` a strict sibling (`rows_exact` returning
  `Option`) and use it wherever a `DATA`-header count is available to
  cross-check — `NVDP`/`NVCA` already have one (`DATA` words 4 and 5).

### ESM-2026-08-27-D1-03: `GameKind::from_header`'s sampled-value table says FO76 HEDR is 68.0; both installed FO76 masters ship 266.0

- **Severity**: LOW
- **Dimension**: Header Detection (documentation)
- **Record / Sub-record**: `TES4` / `HEDR`
- **Location**: `crates/plugin/src/esm/reader.rs:126-137` (comment), band at `:145`
- **Status**: NEW
- **Description**: The comment block that justifies every band in
  `from_header` lists *"FO76 = 68.0"* among "HEDR versions sampled from real
  vanilla masters at 2026-04-19". Reading the TES4 header directly off disk
  gives `SeventySix.esm` = **266.0** and `NW.esm` = **266.0**. The band itself
  (`hedr_version >= 60.0 → Fallout76`) still classifies both correctly, so
  there is no misclassification — but the sampled values are the *evidence*
  the band gaps rest on, and one of the six is wrong by a factor of ~4.
- **Evidence**: independent header read (not through this parser), all seven
  installed games:
  ```
  Oblivion.esm    1.0    (20-byte header → EsmVariant::Oblivion)
  Fallout3.esm    0.94   rec_ver 2
  FalloutNV.esm   1.34   rec_ver 2      (DLC masters: 1.32 / 1.33)
  Skyrim.esm      1.71   rec_ver 44
  Fallout4.esm    1.0    rec_ver 131    (DLCs: 0.95 & 1.0, rec_ver 131)
  SeventySix.esm  266.0  rec_ver 209
  Starfield.esm   0.96   rec_ver 581
  ```
  Every one of these classifies correctly through the current bands, which I
  re-derived arm by arm; the FNV DLC values 1.32/1.33 and the FO4 0.95 +
  `rec_ver >= 100` discriminator both behave as documented.
- **Impact**: Documentation only. It matters because the next person widening
  or narrowing a band will reason from this table.
- **Related**: `#439` / FO3-3-01 (the inverted-band incident this comment
  block exists to prevent recurring).
- **Suggested Fix**: Correct the line to `FO76 = 266.0` and note that the
  `>= 60.0` floor is deliberately far below it.

---

## Prior-Audit Findings — Status at HEAD

Every row re-verified against current code (and, where the claim is about
data, against the installed masters) — not carried over from the prior
report's prose.

| Finding | Check at HEAD | Result |
|---|---|---|
| **ESM-2026-08-24-D5-01** / **#3270** (FO4 `WATR.DNAM` 12/16 read as fog near/far) | `decode_dnam_fo4` (`misc/water.rs:1065-1080`) | **FIXED in code** — the two reads are gone and replaced by a comment citing `#3270` and the 1.0-constant evidence; `parse_rate_fo4_esm` passes. **The GitHub issue #3270 is still OPEN** — issue-hygiene gap, not a code gap |
| **#3237** (GRUP recursion depth bound) | `cell/support.rs` ×7, `cell/walkers.rs:665` | **STILL BROKEN, issue now CLOSED** — see ESM-2026-08-27-D1-01 |
| **#3314** (CELL/WRLD/LAND FormIDs bypass remap) | `cell/helpers.rs:29` (`read_form_id` now requires `&EsmReader`), `walkers.rs:1084`/`:1097` (BTXT/ATXT), `support.rs:275`/`:396` | **FIXED for the cell tier** — and made structurally hard to regress by the required-parameter signature. `records/` was not swept: ESM-2026-08-27-D3-01 / -D3-02 |
| **#3300** (NAVM door/cover/grid + cross-tile join) | `decode_nvdp_row`, `decode_nvgd`, `decode_nvnm` (`misc/world.rs:486-591`) | **FIXED** — exact-consumption gate on `NVGD`, `NVNM_MAX_DIVISOR` bound and `divisor == 0` rejection both present; strides carry a written census. The stored `door_form_id` is un-remapped (D3-02) |
| **#3356** (OTFT `INAM` is one array, not one FormID per sub-record) | `outfit.rs:82-89` | **FIXED** — `chunks_exact(4)`, with the fabricated `WhiterunGuardOutfit` fixture replaced by real `Skyrim.esm` records |
| **#3357** (ARMO contributes only the first race-matching ARMA) | `equip.rs:153-231` (`resolve_armor_meshes`) | **FIXED**, both consumers migrated (`npc_spawn.rs:803`, `:918`). See LOW note below on the now-dead singular wrapper |
| **#3384** (empty index erases `character_rules`) | `index.rs:850-871` | **FIXED** — first-non-`NONE`-wins with a disagreement warning. Sibling `game` field left unguarded: ESM-2026-08-27-D7-01 |
| **#3383** (CREA `CNAM` stored as a class FormID) | `dispatch_actor.rs:52-62` | **FIXED** — cleared at the one site that knows it read the CREA group, with a 0/1578 + 0/533 vanilla census in the comment |
| **#3324 / #3325** (WEAP `VATS`, FACT `WMI1`) | `items.rs:324-355`, `actor/mod.rs:639-651`, `dispatch_actor.rs:76-83` | **FIXED** — `VATS` is length-guarded for the three 16-byte vanilla payloads, `WMI1` is remapped through `reader.get_form_id_remap()` |
| **#2636** (SECH/AOPF zero dispatch) | `records/mod.rs:471`, `dispatch_misc_stub.rs:47-65`, `index.rs:294-305` | **FIXED** — routed to the stub bucket with typed maps and `categories()` entries |
| **#2611** (SCOL→PKIN expansion) | `byroredux/src/cell_loader/refr.rs:645-668` | **FIXED** (engine-side, cross-referenced only) |
| **ESM-D4-05** (2026-08-13, never published) — the anonymous catch-all GRUP skip has no telemetry | `records/mod.rs:480-482`; `skipped_unconsumed_groups` is pushed at exactly **one** site (`:380`, `PDCL` only) | **STILL OPEN**, unchanged, not re-filed. Refreshed numbers below make it worse, not better: 95 Starfield / 89 FO76 / 46 FO4 / 34 Skyrim top-level labels vanish with no counter and no log |
| **ESM-D7-02 + ESM-D8-01** (2026-08-13, never published) — `EsmIndex` RAM is unbudgeted and unevicted; parse peak RSS is 2.1–5.0× file size | `docs/engine/memory-budget.md` section list (`## Starfield Component Database` is the only host-RAM section; everything else is VRAM) | **STILL OPEN**, and the numbers have grown — see the refreshed table below. Not re-filed |
| **#3146** (dead post-`return` tail in `decode_data`) | `misc/water.rs:417-460` | **still live**, unchanged, not re-filed (LOW) |
| **#3150** (`_tmp_*` scratch examples committed) | `crates/plugin/examples/_tmp_obl_bsxrefr.rs`, `_tmp_obl_player.rs`, `_tmp_sk_lvli.rs` | **still live**, unchanged, not re-filed (LOW) |
| **#3217** (LVLI `0x02` vs `0x04` multi-pick) | `equip.rs`, plus the new `expand_leveled_nested_tier_ladders_do_not_combinatorially_explode` regression test (`7136e263`) | **FIXED and now issue-closed** — the hygiene gap the 2026-08-24 report flagged is resolved |
| **`resolve_armor_mesh` (singular)** | `equip.rs:122-132` | **Dead outside tests** after `#3357` migrated both engine consumers to `resolve_armor_meshes`. Ten remaining references are all inside `equip.rs`'s own `mod tests`. Noted as LOW housekeeping rather than filed as a finding of its own |

---

## Record Coverage Matrix (refreshed 2026-08-27)

Re-derived this pass with `crates/plugin/examples/esm_dim8_coverage`, against
the 118 top-level GRUP labels the dispatch match in
`crates/plugin/src/esm/records/mod.rs` actually routes (extracted from source,
not hand-listed). Label sets are **de-duplicated** — Fallout 4 and later split
one logical top-level group across several GRUPs, which is why these totals
differ from the prior report's raw counts.

| Master | Distinct top-level GRUPs | Routed | Unrouted (silently skipped) |
|---|---:|---:|---|
| `Oblivion.esm` | 56 | 53 | 3 — `LVSP` `SBSP` `SKIL` |
| `Fallout3.esm` | 84 | 84 | — (full coverage) |
| `FalloutNV.esm` | 101 | 101 | — (full coverage) |
| `Skyrim.esm` (SE) | 117 | 83 | 34 — `AACT ARTO ASTP CLDC CLFM COLL DLBR DLVW DUAL EQUP FSTP FSTS HAZD KYWD LCRT LCTN LVSP MATO MATT MOVT MUST RELA REVB RFCT SCRL SHOU SMBN SMEN SMQN SNCT SNDR SOPM SPGD WOOP` |
| `Fallout4.esm` | 126 | 80 | 46 — the Skyrim set plus `AECH AMDL AORU CMPO DFOB DMGT GDRY INNR KSSM LAYR LENS NOCM OVIS RFGP SCCO SCSN STAG TRNS ZOOM` |
| `SeventySix.esm` | 170 | 81 | 89 — the FO4 set plus `AAMD AAPD ASTM ATXO AUVF AVTR CMPT CNCY CNDF COEN CPRD CSEN CURV DCGF DIST ECAT EMOT ENTM FISH GCVR GMRW LGDI LOUT LVLP LVPC MDSP PACH PCRD PEPF PLYT PMFT PPAK QMDL RESO STHD STMP STND TEPF TRAP UTIL VOLI WAVE WSPR` |
| `Starfield.esm` | 176 | 81 | 95 — incl. `BIOM ATMO SFBK SFPC SFPT SFTR MRPH TRAV FOGV SUNP PNDT INFO DLBR SPCH WTHS WWED` |

Two notes on the deltas from `AUDIT_ESM_2026-08-20.md`'s carried-forward
version of this table:
- The FO4 / FO76 / Starfield "routed" counts drop (93→80, 80→81, 80→81)
  because that table counted duplicated split-group labels; the label
  *sets* are unchanged apart from `SECH`/`AOPF` moving into the routed
  column this delta.
- `Starfield.esm` authors a top-level `INFO` GRUP that is not routed
  (`DIAL` is, and dispatches `INFO` as a child). Worth a look under
  `/audit-starfield` Dim 4 — it is out of scope to resolve here.

---

## Dimension 8 — Parse Cost per Master (refreshed 2026-08-27)

First refresh since 2026-08-13. `cargo run --release --example esm_dim8_bench`,
warm page cache, `/usr/bin/time -f %M` for peak RSS (process high-water with
the resulting `EsmIndex` **and** the source `Vec<u8>` still alive).

| Master | File | Read | `parse_esm` | Peak RSS | RSS / file | `index.total()` | Int cells | Ext cells | NAVM |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| `Oblivion.esm` | 265 MB | 115 ms | 1,454 ms | 1.37 GB | 5.0× | 55,317 | 1,855 | 33,540 | 0 |
| `Fallout3.esm` | 275 MB | 188 ms | 1,247 ms | 1.01 GB | 3.7× | 44,666 | 421 | 41,958 | 7,198 |
| `FalloutNV.esm` | 234 MB | 102 ms | 1,151 ms | 0.82 GB | 3.5× | 77,828 | 388 | 30,096 | 4,771 |
| `Skyrim.esm` (SE) | 238 MB | 93 ms | 1,245 ms | 0.93 GB | 3.9× | 96,322 | 590 | 16,942 | 15,966 |
| `Fallout4.esm` | 315 MB | 144 ms | 1,667 ms | 1.37 GB | 4.4× | 123,311 | 1,195 | 38,965 | 7,894 |
| `SeventySix.esm` | 880 MB | 344 ms | 3,399 ms | **3.35 GB** | 3.8× | 315,355 | 2,653 | 50,487 | 25,603 |
| `Starfield.esm` | 1,389 MB | 539 ms | 4,516 ms | **3.21 GB** | 2.3× | 246,355 | 11,985 | 18,300 | 56,576 |

`SeventySix.esm`'s peak has risen from the 3.2 GB the 2026-08-13 report
measured. This is the ledger ESM-D7-02 asks `docs/engine/memory-budget.md`
to carry and it still does not: that file's only host-RAM section is
`## Starfield Component Database (CPU-side)`. Nothing caps or evicts
`EsmIndex`, and `merge_from` makes a multi-plugin load order additive.

---

## Cross-Audit Pointers

| Topic | Owner | Disposition |
|---|---|---|
| `expand_scol_placements_with_depth` / `expand_packin_placements_with_depth` (`byroredux/src/cell_loader/refr.rs`) as the consumer of ESM-2026-08-27-D3-01 | `/audit-fo4` Dim 1 | The consumers are correct; they look up exactly the key they are handed. The defect is entirely parser-side and the fix belongs in `crates/plugin` |
| `EsmIndex::game` consumers (skeleton path, biped mask, `.pex` translation gate, terrain-LOD layout, player base form) | `/audit-fo4`, `/audit-skyrim`, `/audit-scripting` | ESM-2026-08-27-D7-01's blast radius crosses four subsystems; the fix is a single `merge_from` line and is filed here |
| `NavmRecord` door / cover / grid consumption (`#2372` / EX-16) | `/audit-audio`, exterior streaming | `#3300` decodes them correctly; ESM-2026-08-27-D3-02 flags that `door_form_id` is not remapped **before** a consumer inherits the bug |
| `derive_npc_actor_values`, `effective_actor_level`, `RaceRecord` starting pools, `WeaponVats` → CHARAL | `/audit-character` Dim 4/5 | Byte offsets verified in scope here (`VATS` 0/4/8/12/16, length-guarded for the three 16-byte vanilla payloads). The *consumption* of `effective_actor_level`'s `calc_min.max(1) as i16` narrowing is CHARAL's, not re-derived |
| Starfield top-level `INFO` GRUP is unrouted | `/audit-starfield` Dim 4 | Surfaced by the refreshed coverage matrix; not resolved here |
| `crates/bsa` zlib / LZ4 decompression bounds (sibling of ESM-2026-08-27-D1-02) | `/audit-nif`, `/audit-safety` | Not examined this pass; the ESM-side gap is filed above |
| `docs/engine/memory-budget.md` gaining a host-RAM section | `/audit-performance` | ESM-D7-02, carried forward unpublished since 2026-08-13 |

---

## Not Covered

Stated plainly so this does not read as a full sweep:

- **`equip.rs` biped-slot xEdit citations** — still not byte-verified against
  real `ARMO`/`ARMA` records. Third consecutive pass this has been deferred
  (2026-08-20, 2026-08-24, and now). The `+244` lines this delta added to
  `equip.rs` are `resolve_armor_meshes` and its tests, not the constants.
- **The Redux-native tier** (`manifest.rs` / `record.rs` / `datastore.rs` /
  `resolver.rs`) and `legacy/mod.rs` — `manifest.rs` moved by 2 lines in this
  delta and nothing else changed; the 2026-08-13 rot findings stand,
  not re-derived.
- **`parse_land_record` quadrant/layer strides** — unchanged in the delta,
  not re-derived.
- **`--sf-smoke` Starfield resolve-rate comparison** — not run this pass
  (`crates/plugin/examples/sf_smoke.rs` moved by 9 lines; the parse-rate floor
  was refreshed by `#3397` this delta).
- **Per-record unknown-sub-record census** (the `SUB` half of
  `esm_dim8_coverage`) — the label-level census was run on all seven masters;
  the per-record-type sub-record census was not, for runtime reasons.
- **`actor_value_derive.rs`** (`+265` this delta) — reviewed for byte-level
  decode only; the CHARAL formulas are `/audit-character` Dim 4's.

---

## Method & Hygiene

- **No sub-agents.** All analysis performed directly in one context.
- **`cargo test -p byroredux-plugin`**: 814 passed, 0 failed, 23 ignored;
  doc-tests 2 passed. **`--test parse_real_esm -- --ignored`**: 21 passed,
  0 failed — including `parse_rate_fo4_esm`, the test that failed on the
  2026-08-24 pass and is the evidence that `#3270`'s code fix landed.
- **Ground truth for every data claim is the on-disk master**, read by
  standalone Python (TES4 header located by the `data[20..24] == b"HEDR"`
  test, `MAST` sub-records walked by their own 6-byte headers) or by the
  crate's release-built probe binaries — so a parser bug cannot hide inside
  the measurement.
- **Every prior finding above was re-checked against current code**, not
  taken from the prior report's text — which mattered again this pass:
  `#3237` and `#3270` are both in a state (closed-but-broken,
  fixed-but-open) that reading either the issue tracker or the prior report
  alone would have got wrong.
- **Working tree carries no artifact of this audit outside `docs/audits/`.**
  Probe scratch lived in `/tmp/audit/esm/`.

---

Report ready. Publish with:

```
/audit-publish docs/audits/AUDIT_ESM_2026-08-27.md
```

(domain label: `esm-plugin`; add `game:fo4` to ESM-2026-08-27-D3-01 and
`game:fo76` to ESM-2026-08-27-D1-03.)

TALLY: CRITICAL=0 HIGH=3 MEDIUM=2 LOW=2
