# Fallout 3 Compatibility Audit — 2026-08-30

**Scope**: FO3 compatibility — NIF v20.2.0.7 / BSVER 34, BSA v104, ESM through the
FNV-shared parser. Seven dimensions, all run in-process against real game data at
`/mnt/data/SteamLibrary/steamapps/common/Fallout 3 goty/Data/` (base master + all
five DLC).
**HEAD**: `64f64480` (2026-08-30). Prior report: `docs/audits/AUDIT_FO3_2026-08-27.md`.
**Method**: measurement first. Every number below was produced this run by a live
corpus sweep or an `--ignored` real-data test, not carried forward from a prior
report. Where a measurement contradicted the skill file or an existing issue, the
measurement wins and the discrepancy is stated.

---

## Executive Summary

FO3 remains the healthiest legacy title in the tree. Every parse-tier gate is at
100 % with real headroom behind it, and the two defects found this run are both
*content-completeness* gaps — data authored in the file that never reaches the
engine — not parse failures.

| Signal | Measured this run | Prior claim |
|---|---|---|
| NIF parse rate | **17 172 / 17 172, 100.00 % clean** across all 6 mesh archives | 100 % (17 172) ✅ |
| NIF block dispatch | **526 109 blocks, 0 `NiUnknown`** | 0 ✅ |
| ESM `index.total()` | **44 666** | 44 666 ✅ |
| ESM raw file census | **718 952 records + 89 748 GRUPs across 91 types**, reconciled 1:1 against HEDR `numrec` 808 699 | *never measured before* |
| Cell tree | 42 410 / 42 410 CELLs · 573 610 / 573 610 REFR+ACHR+ACRE · 40 256 / 40 256 LAND · 7 198 / 7 198 NAVM | — |
| Interior baseline | `megatonplayerhouse` = **929 REFRs**, lighting present | 929 ✅ |
| Exterior | 32 worldspaces · 41 958 exterior cells · 336 976 exterior refs | — |
| Collision | **19 229 Classic chains, 0 extraction failures**, 77 phantom | matches ✅ |
| DDS | **16 336 across all 6 texture-bearing archives, 0 failures** | 12 261 (one archive) — widened |
| `.kf` corpus | **3 572 / 3 572 parsed, 3 608 clips, 0 zero-clip files** | *never measured before* |
| B-spline reach on FO3 | **80 341 blocks in 2 980 of 3 572 `.kf` files (83.4 %)** | "reachable" (qualitative) |
| Disney-BSDF gate | **`is_pbr` = 0 / 56 477 meshes** — provably unreachable | ✅ |
| Object LOD | 8 061 baked `\lod\` entries across 29 worldspaces | — |

### Findings

| # | Dimension | New | Notes |
|---|---|---|---|
| 1 | Rendering — inline shaders | 1 LOW | two `texture_registry` doc comments invert the clamp-mode default |
| 2 | NIF parser — FO3 block subset | 1 MEDIUM | a ramp-up birth-rate curve is discarded whole; 20 of 294 emitter meshes fall back to a name guess |
| 3 | ESM record coverage | 1 MEDIUM + 2 LOW | every FO3 placed mine is dropped; two doc premises that invite a real regression |
| 4 | Cell loading end-to-end | 0 | both live issues (#3502, #3508) reproduce unchanged; no FNV hardcoding found |
| 5 | Collision import | 0 | 19 229 / 19 229 extract; #1652 and #1277 both hold on FO3 |
| 6 | BSA v104 + real data | 0 | #3513 verified **fixed** — recommend closing |
| 7 | Animation / NPC spawn | 0 | all three M41.0 guards present; every FO3 body/hand/skeleton asset resolves |

**Totals: 0 CRITICAL · 0 HIGH · 2 MEDIUM · 3 LOW.**

Dimensions **4, 5, 6 and 7 produced no new findings.**

### Stale candidates dropped (5)

1. `TXST` 244 on disk vs 243 indexed — the missing one is form `0x00000028`,
   `NullTextureSet`, an EDID-only record with zero texture slots. Correctly not
   indexed. **Dropped.**
2. `extract_emitter_rate`'s `find_map(…NiPSysEmitterCtlr)` first-controller-only
   early return. Replicated the full tier chain per-controller over all 22
   affected FO3 files: **0** would resolve on a later controller. **Disproved.**
3. Escalating **#3517** on FO3 grounds. Measured: 3 729 FO3 shapes carry the
   mixed chain, but only **8** diverge from the proposed latch policy, all
   `meshes\interface\testloadanim*.nif`, and today's code already picks the more
   specific value. **Not escalated.**
4. **#3513** (ROADMAP cites the pre-#3041 single-archive corpus) — `ROADMAP.md:590`
   now reads the correct 17 172 / 6 archives. **Fixed; recommend closing.**
5. **#3514** (`refraction_strength` last-writer-wins) — re-measured: the entire
   FO3 corpus contains **exactly one** NiNode-borne property (a `NiFogProperty`)
   and **zero** inherited `BSShader*`. Still unreachable on vanilla FO3. **Not re-filed.**

### Skill-file premise discrepancies (reported per instruction, 3)

1. **Dim 5** tells the auditor to confirm `BhkMultiSphereShape` extraction because
   "FO3 uses these in static/clutter collision". Measured: **FO3 authors zero**
   `bhkMultiSphereShape` across all 17 172 NIFs. The `BhkConvexListShape` half of
   #1277 *is* FO3-reachable (71 blocks, all children resolvable) and is verified;
   the MultiSphere half is unfalsifiable on FO3 data.
2. **Dim 2** calls FO3 particle-emitter decode "an UNVERIFIED gap". It is verified
   now (#3286's sweep fix runs FO3 live), but the verification is *presence*-shaped
   and the real defect is *magnitude*-shaped — see FO3-2026-08-30-D2-01.
3. **Dim 4** frames the FO3 exterior gap as "a fresh GPU bench, not a missing
   feature". True, but `GameKind::Fallout3NV` collapses FO3 and FNV into one enum
   variant (`crates/plugin/src/esm/reader.rs:137-141`), so no cell-loader code can
   express an FO3-only behaviour. Worth knowing before anyone proposes "add an FO3 arm".

---

## Dimension Findings

### Dimension 3 — ESM Record Coverage

#### FO3-2026-08-30-D3-01: every FO3 placed mine is dropped — the cell walker skips `PGRE` and no `PROJ` `MODL` reaches `cells.statics`
- **Severity**: MEDIUM
- **Dimension**: 3 — ESM record coverage (defect is in the shared cell walker)
- **Location**: `crates/plugin/src/esm/cell/walkers.rs:1055-1057`;
  `crates/plugin/src/esm/records/dispatch_misc_gameplay_b.rs:101-103`;
  `crates/plugin/src/esm/records/misc/effects.rs:123-146`
- **Status**: NEW. `docs/audits/AUDIT_ESM_2026-08-13.md` counted the records
  ("FO3 | 350 skipped | 0.0 % of file | `PGRE` 350") and `docs/audits/AUDIT_FNV_2026-08-26.md`
  deferred them to `/audit-esm`, but **no GitHub issue exists** and what the 350
  records *are* was never established. The "0.0 % of file" framing is what kept
  them looking like a trivial tail.
- **Description**: the cell child-record loop ends in
  ```rust
  } else {
      // Skip other record types (PGRE, PMIS, etc.)
      reader.skip_record(&header);
  }
  ```
  There is no `PGRE` arm anywhere in the walker. The reconciliation confirms the
  loss exactly: indexed placed refs = 573 610 = REFR 568 107 + ACHR 2 154 +
  ACRE 3 349, with PGRE's 350 accounted for nowhere.
- **Evidence** (measured on `Fallout3.esm`):
  - **350** `PGRE` records; **0** carry the Deleted flag `0x20`; 0 compressed.
  - Sub-records: `NAME` 350, `DATA` 350, `XOWN` 225, `XRGD` 120, `XESP` 19,
    `XSCL` 7, `EDID` 5, `XPWR` 2, `XHLP` 1.
  - `DATA` is **24 bytes on all 350** — `pos[3]f32 + rot[3]f32`, byte-identical to
    the REFR/ACHR/ACRE `DATA` the walker already decodes.
  - `NAME` resolves to just four base forms, every one a `PROJ` with a `MODL`:

    | form | EditorID | MODL | count |
    |---|---|---|---:|
    | `0x0043FA` | `MineFragProjectile` | `Weapons\1handMineDrop\MineFrag.NIF` | 340 |
    | `0x0403D8` | `MinePlasmaProjectile` | `Weapons\1handMineDrop\MinePlasma.NIF` | 7 |
    | `0x033C4B` | `MinePulseProjectile` | `Weapons\1handMineDrop\MinePulse.NIF` | 2 |
    | `0x059449` | `MineBottleCapProjectile` | `Weapons\1handMineDrop\MineBottleCap.NIF` | 1 |

  - All four meshes exist in `Fallout - Meshes.bsa` and parse clean (63 965 /
    298 492 / 95 023 / 125 121 bytes, `parse_nif` OK).
  - **Second half of the gap**: `parse_proj` never reads `MODL`, and the `PROJ`
    dispatch uses `extract_records`, not `extract_records_with_modl`. Probed live:
    `form 0x43fa: in statics=None in projectiles=true` — same for all four. So a
    `PGRE` placement arm alone would still resolve to no mesh.
- **Impact**: **every world-placed mine in Fallout 3 is absent from every loaded
  cell** — 350 live placements of the game's signature environmental hazard.
  Beyond rendering, a future combat/hazard/trap system has no placement data to
  read. The defect is in the shared walker, so the same `else` arm drops FNV 174,
  Skyrim SE 42, FO4 395, FO76 118 and Starfield 1 268 `PGRE` (counts from
  `docs/audits/AUDIT_ESM_2026-08-13.md`); only the FO3 content behind it is measured here.
- **Related**: `docs/audits/AUDIT_ESM_2026-08-13.md` skipped-record table; #1538 (the
  structurally identical `is_fo4_plus` SCOL gate that dropped 98 FNV bases).
- **Suggested Fix**: add `|| &header.record_type == b"PGRE"` to the
  REFR/ACHR/ACRE arm at `walkers.rs:698-701` — the `DATA` layout is identical, so
  the existing `PlacedRef` decode applies unchanged — and switch the `PROJ`
  dispatch to `extract_records_with_modl` so the four base meshes land in
  `cells.statics`. Add a floor to `parse_rate_fo3_esm` asserting ≥ 350 PGRE
  placements so the arm cannot silently regress.

---

#### FO3-2026-08-30-D3-02: the `ACRE` comment says "Oblivion-only … FO3+ folded creature placements into ACHR" — FO3 ships 3 349 `ACRE`, more than its 2 154 `ACHR`
- **Severity**: LOW (doc-rot with a live hazard)
- **Dimension**: 3 — ESM record coverage
- **Location**: `crates/plugin/src/esm/cell/walkers.rs:695-698`
- **Status**: NEW
- **Description**:
  ```rust
  // ACRE — Oblivion-only placed-creature reference (#396). FO3+
  // folded creature placements into ACHR; ACRE's wire layout
  // matches ACHR byte-for-byte on Oblivion (NAME/DATA/XSCL/XESP),
  // so it routes through the same handler.
  ```
  Measured on `Fallout3.esm`: **`ACRE` 3 349, `ACHR` 2 154**. FO3 did not fold
  creature placements into `ACHR`. It splits them exactly the way Oblivion does —
  `CREA` placements to `ACRE`, `NPC_` placements to `ACHR`.
- **Evidence**: the Dim-3 reconciliation only closes because `ACRE` *is* accepted:
  568 107 + 2 154 + 3 349 = 573 610 = the indexed placed-ref total. Remove ACRE and
  the parser loses 3 349 placements.
- **Impact**: no behavioural defect today — the arm is ungated by game. The hazard
  is the premise. A reader trusting "Oblivion-only" would gate the arm on
  `GameKind::Oblivion` and silently delete 3 349 FO3 creature placements (super
  mutants, radroaches, brahmin, mirelurks) plus FNV's equivalent. The project has
  already paid for this exact class of comment once — #1538.
- **Suggested Fix**: "placed-creature reference. Oblivion, FO3 and FNV all author
  it (FO3: 3 349 `ACRE` vs 2 154 `ACHR`); the wire layout matches `ACHR`
  byte-for-byte, so it routes through the same handler."

---

#### FO3-2026-08-30-D3-03: the `parse_rate_fo3_esm` baseline comment describes `index.total()` as 31 101 and calls 44 657 the *file* record count — it is neither
- **Severity**: LOW (doc-rot in a regression-gate rationale)
- **Dimension**: 3 — ESM record coverage
- **Location**: `crates/plugin/tests/parse_real_esm.rs:1476-1482`, and the
  `FO3_TOTAL_FLOOR` doc at `:52`
- **Status**: NEW
- **Description**: the comment reads
  > `index.total()` … Observed 2026-04 on the GOTY master: 31,101; `FO3_TOTAL_FLOOR`
  > (30,000) sits just below it … Distinct from the *file* baseline re-verified
  > 2026-05-26: 44,657 total = 37,459 structured + 7,198 NAVM.

  Both halves are wrong:
  1. `index.total()` measures **44 666** today, not 31 101 — 43 % above the figure
     the 30 000 floor was reasoned from. The floor no longer "sits just below"
     anything: the gate would still pass after losing a third of the index.
  2. The "*file* baseline … 44 657" is not a file quantity. The file holds
     **718 952 records** (validated against HEDR `numrec` 808 699 — records +
     GRUPs = 808 700, delta 1 = the TES4 header itself). 44 657 is the *same*
     index-sum metric measured on 2026-05-26 (44 657 then, 44 666 now), which is
     exactly what the sentence claims it is "distinct from".
- **Impact**: the ROADMAP FO3 compat row and this audit skill both carry
  "44 657 = 37 459 structured + 7 198 NAVMs" as *the FO3 ESM record count*. Read
  literally it understates the file by 16×. More usefully: it cannot detect a
  regression anywhere in the 663 824-record cell tree (REFR / CELL / LAND / ACHR /
  ACRE / PGRE), because none of that tier except NAVM enters `total()` — which is
  precisely the tier FO3-2026-08-30-D3-01 lives in. `index.total()` also
  double-counts by design (`cells.statics` overlaps `items` / `activators` / …),
  which `EsmIndex::categories()`'s own doc states and this comment does not.
  (Sibling nit in the same area: `GameKind`'s enum doc at
  `crates/plugin/src/esm/reader.rs:137` says "Fallout 3 (HEDR 0.85)"; the installed
  GOTY master measures **0.94**, which the table 25 lines below states correctly.)
- **Suggested Fix**: re-cut the comment to "index-sum, observed 44 666 on
  2026-08-30; double-counts the `cells.statics` overlap by design", raise
  `FO3_TOTAL_FLOOR` to ~44 000, and add a separate cell-tier assertion (placed refs
  ≥ 573 000, exterior cells ≥ 41 900) so a regression there is visible at all.

---

### Dimension 2 — NIF Parser (FO3 block subset)

#### FO3-2026-08-30-D2-01: a ramp-up birth-rate curve is discarded whole because `float_interpolator_rate` reads only `keys.first()` — 20 of 294 FO3 emitter meshes run on a name-heuristic guess with the authored peak sitting in the same array
- **Severity**: MEDIUM (severity table: *"Translatable block silently dropped by
  NIFAL (collision shape / particle emitter params) → at least MEDIUM"*)
- **Dimension**: 2 — typed particle emitters (NIFAL particle slice)
- **Location**: `crates/nif/src/import/walk/mod.rs:1060-1071`
  (`float_interpolator_rate`), reached by every tier of `extract_emitter_rate`
  including `sequence_emitter_rate` (`:963-1009`)
- **Status**: NEW. Not #1771 (a *constant* 0.0 rate), not #3329 (the empty-`items`
  manager blend — which fires correctly here and still finds nothing), not #1402
  (first-*emitter* match, not first-*key*). All three closed.
- **Description**: every tier funnels through
  ```rust
  if let Some(first) = scene.get_as::<NiFloatData>(data_idx)
      .and_then(|d| d.keys.keys.first())
  {
      if let Some(r) = sane(first.value) { return Some(r); }
  }
  sane(interp.value)
  ```
  `sane()` rejects `0.0` by design (#1771 — a zero first key means "ramp-up", so
  don't latch it as a permanent zero). But the function then looks **no further
  into `d.keys.keys`**, and on this shape the interpolator's own `value` is the
  `-FLT_MAX` "use the keyed data" sentinel, which `sane()` also rejects. The
  authored curve — already in hand — is dropped and the emitter falls through to
  `byroredux/src/fog.rs:100 particle_preset`, a **name/texture keyword guess** whose
  default arm is `ParticleEmitter::torch_flame()` at **35 /s**
  (`crates/core/src/ecs/components/particle.rs:384-388`).
- **Evidence** — controlled blocks dumped from three affected FO3 meshes
  (`NiControllerSequence` → `ctype = "NiPSysEmitterCtlr"` →
  `NiFloatInterpolator{value = -3.4028235e38}` → `NiFloatData.keys`):

  | mesh | authored key values | rate used today |
  |---|---|---|
  | `meshes\architecture\urban\tenpengate01.nif` (seq `Close`) | `[0.0, 0.0, 600.0, 0.0]` | 35 /s preset — **17× low** |
  | `meshes\effects\ambient\fxfallingrocks01.nif` (seq `Idle`) | `[0.0, 0.0, 22.5, 0.0]`, `[0.0, 0.0, 300.0, 0.0]` | 35 /s preset — **8.6× low** |
  | `meshes\effects\ambient\fxbubblestall01.nif` (seq `Idle`) | `[0.0, 0.0, 30.0, 30.0]`, `[0.0, 60.0, 60.0, 0.0]` | 35 /s preset |

- **Blast radius (measured, whole FO3 corpus)**: 294 emitter-bearing meshes; 65
  yield no rate; **20 of those carry an authored ramp-up curve with a positive
  peak**. Peak histogram `{10.5:1, 45:1, 60:7, 120:4, 150:1, 202.5:1, 300:2,
  450:1, 600:1, 900:1}`. Affected content: Tenpenny Tower's gate FX, the Anchorage
  snow drifts and mesh tubes, the MQ11 vertibird effects, the whole force-field FX
  set, the falling-rock ambients. (The other 45 rate-less files genuinely author
  nothing: 43 have no emitter controller at all, 2 carry a bare `-FLT_MAX`
  interpolator with no keyed data.)
- **Disproved alternative**: I first suspected the first-controller-only
  `find_map`. Replicating the full tier chain per-controller over all 22 files
  with a controller: **0** would resolve on a later controller. Not the cause.
- **Impact**: 6.8 % of FO3's particle library spawns at an invented density. No
  crash, no parse error — the emitters exist and animate, they simply run at the
  wrong rate. This is exactly the class of defect #3343's magnitude floors were
  added to catch, and they still miss it: those floors count `rate.is_some()`, and
  these files legitimately report `None`.
- **Related**: #1771, #3329, #3343, #1402 (all closed); `docs/engine/nifal.md`
  particle slice.
- **Suggested Fix**: when `sane(first.value)` rejects a zero first key, scan the
  remaining keys for the maximum finite positive value and use that as the
  steady-state rate — it is the curve's plateau in every sampled case — instead of
  falling through to `sane(interp.value)` and then to the name preset. Extend
  `real_archive_torch_meshes_surface_particle_emitters` to assert a per-game
  *ratio* of rate-bearing emitters so a regression here turns the build red.

---

### Dimension 1 — Rendering Path (inline shaders)

#### FO3-2026-08-30-D1-01: two `texture_registry` doc comments invert the clamp-mode default — "clamped to `0` (REPEAT)" is wrong in both halves
- **Severity**: LOW
- **Dimension**: 1 — inline-shader material path (documentation)
- **Location**: `crates/renderer/src/texture_registry.rs:611-612` and `:1140-1141`
- **Status**: NEW
- **Description**: `load_dds_with_clamp`'s doc says *"`clamp_mode` values outside
  `0..=3` are clamped to `0` (REPEAT) — defensive default for upstream parser
  garbage"*, and `get_by_path_with_clamp`'s says *"Defaults (`clamp_mode == 0` =
  REPEAT) preserve the legacy single-key shape"*. Both are wrong twice over.
- **Evidence**:
  - `SAMPLER_ADDRESS_MODES` (`:30-46`) index **0** is
    `(CLAMP_TO_EDGE, CLAMP_TO_EDGE)`; index **3** is `(REPEAT, REPEAT)`.
  - The code clamps **up**, not to zero: `let clamp_mode = clamp_mode.min(3);`
    (`:647`, `:1142`), and the regression test is named
    `out_of_range_clamp_mode_falls_back_to_3`
    (`crates/renderer/src/texture_registry_tests.rs:375`).
  - `get_by_path` (`:1132-1134`) passes `3` with the comment *"3 = WRAP_S_WRAP_T
    per nif.xml — the legacy REPEAT cache entry"*, contradicting the doc four
    lines below it.
- **Impact**: no runtime defect — the code is right, the prose is wrong. It
  surfaced now because **#3516** made FO3 the first title whose legacy-chain clamp
  values are non-trivial (see below), so this is the doc the next reader consults
  while reasoning about FO3/FNV sampler addressing. Shared across games.
- **Suggested Fix**: "out-of-range `clamp_mode` is clamped to `3`
  (`WRAP_S_WRAP_T` = REPEAT/REPEAT); mode `0` is `CLAMP_S_CLAMP_T`."

---

## Verified Fixed / Not Re-Filed

- **#3516 (TexDesc clamp nibble) — verified effective on FO3.** Over all 2 077
  `NiTexturingProperty` blocks, the base/detail/normal/glow `TexDesc` clamp
  decode moved from `{0: 2077}` (the pre-fix `flags & 0xF`, i.e. *every*
  descriptor read CLAMP/CLAMP) to `{0: 21, 2: 2, 3: 2054}`. **2 054 descriptors
  flipped to REPEAT/REPEAT**, matching nif.xml `TexturingMapFlags` (Clamp Mode,
  width 4, pos 12). The `apply_mode` census is 100 % `APPLY_MODULATE` (2 077/2 077),
  independently corroborating #3530's claim that Oblivion's `APPLY_HILIGHT2`
  parallax convention is inert on FO3.
- **#3517 (OBL-2026-08-27-02) — live in code, but its FO3 blast radius is 8 shapes.**
  The arm still gates on `info.texture_clamp_mode == 3` and never touches
  `texture_clamp_mode_consumed`, unlike its four `BSShader*` siblings at
  `legacy_properties.rs:458 / :578 / :649 / :663`. Its precondition is common on
  FO3 — **3 729** `NiTriShape`s carry both a `NiTexturingProperty` and a
  `BSShader*` (1 941 NiTexturing-first, 1 788 BSShader-first), pair histogram
  `{(0,0):17, (2,2):2, (3,1):8, (3,3):3702}`. But only **8** diverge from the
  proposed latch policy, all `meshes\interface\testloadanim*.nif`, and today's code
  already picks the shader's more-specific value. Keep the issue as a consistency
  fix; **do not escalate on FO3 grounds**. Note its `:272-276` location and
  `base.flags & 0xF` excerpt are stale post-#3516 — the arm is now `:325-329`
  reading `base.clamp_mode`.
- **#3514 (`refraction_strength` last-writer-wins)** — the whole FO3 corpus holds
  **one** NiNode-borne property (a `NiFogProperty`) and **zero** inherited
  `BSShader*`. Unreachable on vanilla FO3, unchanged.
- **#3513 (ROADMAP FO3 compat cell) — FIXED, still OPEN.** `ROADMAP.md:590` now
  reads `100% (17 172 across 6 archives — base + 5 DLC; measured 2026-08-28)`.
  Recommend closing.
- **#3502, #3508, #3506, #3509, #3511** — all re-checked, all reproduce unchanged,
  none re-filed.
- **#1652 (canonical `hkMotionType`)** — FO3 raw `{2:105, 4:1612, 6:1389, 7:16120,
  8:3}` → `{Dynamic 1720, Keyframed 1389, Static 16120}`. BOX_INERTIA (4) maps to
  Dynamic on all 1 612, so FO3 clutter still falls. Holds.
- **#1277 (`BhkConvexListShape`)** — 71 blocks; child-count and
  resolvable-children histograms are identical (`{2:49, 3:4, 4:3, 5:3, 6:3, 9:1,
  10:3, 11:4, 19:1}`). Every child resolves. Holds.
- **#3286 (FO3 emitter test unreachable)** — the sweep fix runs live; the FO3 line
  now prints (`114 emitters, params=114 rate=100 budget=114`).
- **#772 / #790 / #793 (M41.0 guards)** — all three present and correct on FO3;
  every asset `humanoid_body_paths(Fallout3NV, …)` returns is present, parses, and
  yields skinning (see Dimension 6).

---

## FNV-Shared Surface (verified on FO3 data, not re-filed)

FO3 rides the FNV path for the ESM parser, the cell walker, the collision
importer, the material chain and the animation importer. Verified on FO3 data
this run with no FO3-only divergence: CELL/REFR/LAND/NAVM walking (1:1 on all
four), `SCPT` SCHR `u16` flags at cursor 18 (#1654), the `WATR`/`WTHR`/`CLMT`
tier (53 / 27 / 18, all 1:1), the items family (1 762, exact), the
`bhk*CollisionObject → BhkRigidBody → shape` chain (19 229 / 0 failures), and
`import_kf` (3 572 / 3 572).

The one FO3-measured defect inside that shared surface is
**FO3-2026-08-30-D3-01** — a shared-walker gap, quantified on FO3 content.
`cargo check -p byroredux-plugin` is warning-clean; no `unreachable_patterns` in
`crates/plugin/src/esm/cell/walkers.rs`.

## FO3-Distinctive Gaps (unchanged, not bugs)

- **Inline-shader-only material universe.** Zero BGSM, `is_pbr = 0` over 56 477
  meshes, `emissive_source ∈ {Material, None}` only. The Disney-BSDF lobe and the
  FO4+/Skyrim+ `EmissiveSource` variants are provably unreachable on FO3.
- **Capital Wasteland** is `Wasteland` (38 463 of FO3's 41 958 exterior cells),
  distinct from FNV's `wastelandnv`. No FNV-hardcoded worldspace exists in the
  shared loader.
- **The 1 257-SCPT scripting runtime gap.** Parsed, executed by nothing. FO3's
  largest functional gap; owned by `/audit-scripting`, not filed here.
- **`GameKind::Fallout3NV` collapses FO3 and FNV.** No per-title behaviour is
  expressible below the ESM reader today.

## Validation Status

| Tier | Status |
|---|---|
| NIF parse | ✅ 17 172 / 17 172, 100 % clean, 6 archives |
| NIF dispatch | ✅ 526 109 blocks, 0 `NiUnknown` |
| BSA v104 extract | ✅ 0 failures across 7 archives |
| DDS | ✅ 16 336 / 16 336 valid headers |
| ESM cell tree | ✅ CELL / REFR+ACHR+ACRE / LAND / NAVM all 1:1 — **except `PGRE` 350 → 0** |
| Interior | ✅ Megaton 929 REFRs, lighting present |
| Exterior | ✅ parse-side (32 worldspaces / 41 958 cells / 336 976 refs); GPU bench still pending (R6a-stale-15) |
| Collision | ✅ 19 229 / 19 229 |
| Creature / NPC skinning | ✅ 9 / 9 body, hand and skeleton assets present, parsing, skinned |
| Animation | ✅ 3 572 `.kf` / 3 608 clips / 0 failures; B-splines live on 83.4 % of files |
| Particles | ⚠️ 920 emitters, 920 params — **20 meshes on a guessed rate** (D2-01) |
| Scripting | ❌ 1 257 SCPT parsed, 0 executed (known blocker) |

## Not Investigated

- **GPU / runtime behaviour.** No engine launch this run (`/audit-runtime` owns
  that). The R6a-stale-15 FO3 exterior bench remains unmeasured.
- **FaceGen** (`crates/facegen/`) — un-owned subsystem; only the body/hand/skeleton
  NIF tier was validated here, not `.tri` / `.egt` morph application.
- **SpeedTree** — FO3's 9 `TREE` records are owned by `/audit-speedtree`
  this run (see #3528).
- **The scripting runtime itself** — `/audit-scripting`.

## Methodology Note

Seven dimensions, all executed in-process; per-dimension notes at
`/tmp/audit/fo3/dim_{1..7}.md`. Scratch probes are prefixed
`_tmp_fo3_a0830_*` under `crates/nif/examples/` and `crates/plugin/examples/`.
The raw ESM census is an independent Python GRUP walk cross-checked against the
`TES4` `HEDR` record count (records + GRUPs = 808 700 vs `numrec` 808 699), so
the parser and the reference walk are not the same code.
