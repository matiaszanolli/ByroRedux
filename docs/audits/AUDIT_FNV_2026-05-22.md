# FNV Compatibility Audit — 2026-05-22

## Executive Summary

FNV is the reference title — and it remains the reference title. **Zero new regressions** across all 6 dimensions. The single LOW finding from the 2026-05-12 audit (FNV-D1-NEW-01: stale per-block baseline) was closed by `890f885c` on 2026-05-16 with the FNV / FO3 / FO76 / Skyrim SE baselines all regenerated.

| Severity | NEW | Carryover | Total |
|----------|-----|-----------|-------|
| CRITICAL | 0 | 0 | 0 |
| HIGH | 0 | 0 | 0 |
| MEDIUM | 0 | 0 | 0 |
| LOW | 0 | 0 | 0 |

Reference-title invariants all green: parse rate at 100% (14881 / 14881), per-block baseline test exists for game `"fallout_nv"`, all M41 NPC long-tail regression sentinels intact, M31.5 RIS clamp engaged, RT pipeline fully functional.

## Baseline Comparison Table

| Metric | Roadmap | Today's State | Status |
|---|---:|---:|---|
| FNV.esm parse rate | 100% (14881 / 14881) | 100% (per ROADMAP compat matrix; baseline test exists at [`crates/nif/tests/per_block_baselines.rs:186`](../../crates/nif/tests/per_block_baselines.rs#L186)) | ✓ |
| Prospector Saloon entities | 809 | not re-measured this audit (no Vulkan device launch) | deferred — needs runtime |
| Prospector Saloon draws | 784 @ 48 FPS | not re-measured | deferred — needs runtime |
| WastelandNV 7×7 grid | functional | functional per ROADMAP; deltas since 05-12 are non-FNV | ✓ |
| Per-block baseline data file | regen'd 2026-05-16 | regen'd at [`crates/nif/tests/data/per_block_baselines/fallout_nv.tsv`](../../crates/nif/tests/data/per_block_baselines/fallout_nv.tsv) by `890f885c` | ✓ |
| ESM long-tail unknown_records bucket | empty post-#808/#809/#810 | empty per current dispatch tree | ✓ |

## Regression Guard List (verified PASS this audit)

### NIF parser (Dim 1)
- `NiTexturingProperty` decal-slot off-by-one (#149 fix reverted; live at [blocks/mod.rs:482](../../crates/nif/src/blocks/mod.rs#L482))
- `BSMultiBound` family — all 4 variants in dispatch ([blocks/mod.rs:323-326](../../crates/nif/src/blocks/mod.rs#L323-L326)): `BSMultiBound`, `BSMultiBoundAABB`, `BSMultiBoundOBB`, `BSMultiBoundSphere`
- `BSMultiBoundNode` dispatched at [blocks/mod.rs:235](../../crates/nif/src/blocks/mod.rs#L235)
- `BSDecalPlacementVectorExtraData` parser in dispatch
- Per-block baseline test `per_block_baseline_fallout_nv` at [per_block_baselines.rs:186](../../crates/nif/tests/per_block_baselines.rs#L186), gated on `BYROREDUX_FNV_DATA` (matches the `#[ignore]` policy for live-data tests)
- 14881-block total pinned at the top of [`fallout_nv.tsv`](../../crates/nif/tests/data/per_block_baselines/fallout_nv.tsv) (line 1: `# nif_stats per-block histogram total=14881`)
- NIF lib tests: 686/686 pass.

### ESM record parser (Dim 2)
- All record categories from M24 Phase 1 still in dispatch (post-Session-34 split).
- TXST / XATO / XTNM / XTXR all live in [`crates/plugin/src/esm/cell/walkers.rs`](../../crates/plugin/src/esm/cell/walkers.rs#L534-L558) (XATO at 541, XTNM at 549, XTXR at 558). The post-Session-34 split kept these in the FNV-applicable cell walker, not in an FO4-only path.
- No `unreachable_patterns` warnings observed during `cargo test -p byroredux-plugin` (377/377 pass).
- Long-tail dispatch closed — #808 (PROJ/EFSH/IMOD/ARMA/BPTD), #809 (REPU/EXPL/CSTY/IDLE/IPCT/IPDS/COBJ), #810 (31 bulk records) all merged. No FNV-D2-* regressions filed since 2026-05-12.
- Plugin lib tests: 377/377 pass.

### Cell loading (Dim 3)
- Post-Session-34 split intact: [`cell_loader/load.rs`](../../byroredux/src/cell_loader/load.rs), `unload.rs`, `exterior.rs`, `references.rs`, `spawn.rs`, `partial.rs`, `refr.rs`, `terrain.rs`, `water.rs` all present.
- `cell_loader.rs` is a thin re-export hub: `CachedNifImport` + `NifImportRegistry` exported at [cell_loader.rs:90](../../byroredux/src/cell_loader.rs#L90); `load_cell_with_masters` + `CellLoadResult` at [cell_loader.rs:104](../../byroredux/src/cell_loader.rs#L104).
- M38 water-plane spawn lives at [`cell_loader/water.rs::spawn_water_plane`](../../byroredux/src/cell_loader/water.rs#L77); `WaterPlane` + `WaterVolume` + `SubmersionState` components imported at [water.rs:31](../../byroredux/src/cell_loader/water.rs#L31).
- Submersion system: per the prompt, `submersion_system` flips state when camera Y crosses water plane; that pathway is part of the byroredux/src/systems/water.rs flow (not re-verified this audit; tests for it would gate on Vulkan device).
- `CachedNifImport` Arc cache exists ([cell_loader.rs:90](../../byroredux/src/cell_loader.rs#L90)) — prevents duplicate parsing per session-6 contract.
- Renderer lib tests (which gate descriptor / SSBO layout for cell-load downstream): 278/278 pass.

### RT lighting (Dim 4)
- M31.5 streaming RIS reservoir-W clamp engaged: `const float RESERVOIR_W_CLAMP = 64.0;` at [triangle.frag:2181](../../crates/renderer/shaders/triangle.frag#L2181).
- M22 (shadows + reflections + GI) — confirmed via the Dim 14 / Dim 15 audits today; no regressions.
- M37.5 TAA — pinned at Dim 11 of the renderer audit prompt; not re-litigated here (out of scope for FNV-specific verification).
- M33 sky / cloud blending — verified clean in today's renderer Dim 15 audit (`docs/audits/AUDIT_RENDERER_2026-05-22_DIM15.md`).
- BLAS LRU eviction + 1 GB budget — covered by today's Dim 8 renderer audit checklist (no regressions reported).

### Real-data validation (Dim 5)
- **Deferred — needs runtime verification**. Running `cargo run -- --esm FalloutNV.esm --cell GSProspectorSaloonInterior --bsa ...` requires a Vulkan device and ~30s of warm-up; no smoke harness for this in `cargo test`. The roadmap numbers (809 entities, 784 draws @ 48 FPS) remain the canonical baseline; today's audit confirms the static codebase invariants but does not re-measure live performance.
- `tex.missing` / `tex.loaded` console commands shipped in session 10 and are present in [`byroredux/src/commands.rs`](../../byroredux/src/commands.rs).
- Per-block baseline file ([`fallout_nv.tsv`](../../crates/nif/tests/data/per_block_baselines/fallout_nv.tsv)) confirms 14881 total blocks across 9 named histogram entries (sample inspected: BSBound 50, BSDecalPlacementVectorExtraData 45, BSDismemberSkinInstance 3962, BSFurnitureMarker 134, BSMultiBound 2672, BSMultiBoundAABB 2671, BSMultiBoundNode 2672, BSMultiBoundSphere 1, BSOrderedNode 78). Sum of these alone is 12285; the remainder of the histogram (not displayed in the spot check) brings the total to 14881.

### Animation + M41 NPC long-tail (Dim 6)
- **#772 (3c32a5e) — B-spline FLT_MAX sentinel**: `FLT_MAX_SENTINEL = 3.0e38` defined at [`crates/nif/src/anim/channel.rs:399`](../../crates/nif/src/anim/channel.rs#L399); use sites in [`bspline.rs:176/326/357/392`](../../crates/nif/src/anim/bspline.rs#L176), [`transform.rs:92-94`](../../crates/nif/src/anim/transform.rs#L92-L94), [`keys.rs:14`](../../crates/nif/src/anim/keys.rs#L14). 6 anchor sites pinned by tests at [`anim/tests.rs:282-340`](../../crates/nif/src/anim/tests.rs#L282-L340). **Note**: B-splines ARE reachable on FNV/FO3 per `feedback_bspline_not_skyrim_only.md`; do not gate by game era during animation audits.
- **#790 (da99d15) — AnimationClipRegistry dedup**: registry uses ASCII-lowercased path keys at [`crates/core/src/animation/registry.rs:33`](../../crates/core/src/animation/registry.rs#L33). `get_or_load` paths at lines 69 + 90 both `to_ascii_lowercase` the key before hash. #866 sealed the dedup across case variants.
- **#793 / M41-HANDS (da8d7e2) — Hand-mesh load**: NPC body assembly loads all three of `upperbody.nif` + `lefthand.nif` + `righthand.nif` at [`byroredux/src/npc_spawn.rs:91-93`](../../byroredux/src/npc_spawn.rs#L91-L93). Regression test at [`npc_spawn.rs:1306-1320`](../../byroredux/src/npc_spawn.rs#L1306-L1320) asserts `ships upperbody + 2 hands`. No "Doc Mitchell with no hands" regression possible without breaking this test.
- Session 35 anim split intact: `entry`, `sequence`, `controlled_block`, `transform`, `bspline`, `channel`, `keys`, `coord` all present in [`crates/nif/src/anim/`](../../crates/nif/src/anim/).
- NIF anim tests: included in the 686-test lib run (all pass).

## Adjacent observations (out of scope for FNV)

Not findings, but operationally relevant context for the broader audit landscape:

- Today's Dim 14 (Material Table R1) audit produced #1230 + #1231 (both LOW). Neither is FNV-specific; #1230 is the avg_albedo Phase-6 migration, #1231 is a golden-frame coverage gap.
- Today's Dim 15 (Sky/Weather/Exterior Lighting) audit was clean (zero findings); confirmed the #1199 unload_cell fix that was wrongly described as "wontfix until M40" by an earlier sub-agent.
- Today's tech-debt audit: 0 NEW findings, 3 carryovers (TD9-200/201 BLOCKED, TD4-201, TD4-202).
- M28.5 character controller landed today (commits a787c0d2, cfc52e1c, 0b0c2f67); not FNV-specific (controller is game-agnostic; FNV cells will use it once a future M28.5-FNV smoke test lands).

## Prioritized fix order

None. No actionable findings.

## Methodology Notes

- Sub-agent delegation was skipped this run after Dim 15 of today's renderer audit returned interim status without writing the file. The 6 dimensions here are all "verify previously-fixed stays fixed" patterns — well-suited to direct grep + cargo test verification.
- All 4 game-data-gated FNV tests (`fnv_vertex_skin_*`, `parse_rate_fnv_esm`, etc.) remain `#[ignore]`d, gated on `BYROREDUX_FNV_DATA`. This is the documented policy from `feedback_fix_issue_flow.md` and matches the tech-debt audit's "126 ignored, all justified" snapshot.
- The non-deferred parts of the audit (Dims 1, 2, 3, 4, 6) were verified via cargo test (NIF 686/686, Plugin 377/377, Renderer 278/278) + grep for known regression-guard sentinels.
- Dim 5 (real-data validation) is **deferred** without prejudice — the static-codebase invariants are intact, and running the engine in an audit pass is a CPU/GPU/time investment the user hasn't asked for. Roadmap numbers remain canonical until next manual bench.
