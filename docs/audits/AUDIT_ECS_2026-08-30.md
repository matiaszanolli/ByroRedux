# ECS Audit — 2026-08-30

**Command**: `/audit-ecs` (run inside the `comprehensive` audit suite)
**Repo state**: `main` @ `64f64480`
**Scope**: `crates/core/src/ecs/` (storage backends, queries, world, scheduler,
access analyzer, lock tracker, transform propagation), `crates/core/src/animation/`
(Dimension 10) and its ECS driver `byroredux/src/systems/animation.rs`, plus —
by explicit suite instruction — the **P2 gameplay slice**, which has no owner
audit skill: `byroredux/src/combat.rs`, `byroredux/src/inventory.rs`,
`byroredux/src/settings_io.rs`, and the action half of
`byroredux/src/interaction.rs`. Findings from that slice carry a
`[P2-gameplay]` tag.

**Tests**: `cargo test -p byroredux-core --lib` → 705 passed, 0 failed.
`cargo test -p byroredux --bin byroredux` → 1647 passed, 0 failed, 17 ignored.
`cargo test -p byroredux` → 14 passed, 0 failed, 2 ignored.

## Summary

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 0 |
| MEDIUM | 10 |
| LOW | 8 |
| **Total** | **18** |

16 NEW · 2 already-open (`#3255`, `#3473`) · 0 regressions.
7 of the 18 are `[P2-gameplay]`.

The ECS core itself is in good shape: every invariant this skill names as a
regression target was checked and found intact (see *Verified clean* at the
end). The findings cluster in three places instead — one canonical-lock-order
inversion outside the scheduler, a family of latent defects in the
never-instantiated `AnimationStack` layer system, and the newest,
least-reviewed code in the P2 gameplay slice.

---

# Dimension 1 — Lock Ordering & Deadlock

### ECS-D1-01: `scene_centroid_distance` inverts the canonical `GlobalTransform → MeshHandle` order, closing a live 2-cycle
- **Severity**: MEDIUM
- **Dimension**: Lock Ordering & Deadlock
- **Location**: `byroredux/src/app_step.rs:410-411` (opposing edge at `byroredux/src/render/static_meshes.rs:99-100`)
- **Status**: NEW
- **Description**: `docs/engine/ecs.md` fixes one process-wide acquisition
  order for the hierarchy/skinning/bounds cluster:
  `CharacterController → RapierHandles → Transform → Parent → Children →
  GlobalTransform → SkinnedMesh → MeshHandle → LocalBound → WorldBound →
  Name → StringPool` ("skipping types is fine, reordering them is not").
  `App::scene_centroid_distance` acquires `MeshHandle` first and then
  `GlobalTransform`, holding both across the centroid loop — the inverse of
  that order, and the inverse of the edge `build_render_data`'s static-mesh
  pass establishes on the same two types. Neither guard is scoped or dropped
  between acquisition and use, so both edges are real observations for the
  `BYRO_LOCK_ORDER_CHECK` graph.
- **Evidence**:
```rust
// byroredux/src/app_step.rs:410-411 — MeshHandle → GlobalTransform (inverted)
let meshes = self.world.query::<MeshHandle>()?;
let globals = self.world.query::<GlobalTransform>()?;
```
```rust
// byroredux/src/render/static_meshes.rs:99-100 — GlobalTransform → MeshHandle (canonical)
let tq = world.query::<GlobalTransform>();
let mq = world.query::<MeshHandle>();
```
  Both sites are reachable in one process: `scene_centroid_distance` is
  called from `measure_bench_subject_distance` (`byroredux/src/app_step.rs:390`),
  the bench-camera orbit setup, and a `--bench-frames` / `--bench-hold` run
  also renders frames through `build_render_data`.
- **Impact**: No live deadlock — both sites run on the main thread and cannot
  overlap. The concrete cost is the one `docs/engine/ecs.md` warns about
  directly: "an inverted pair that is *safe* still aborts a debug build once
  both sites run." A debug build with `BYRO_LOCK_ORDER_CHECK=1` (the CI
  lock-order job, `#1410` / `#2137`) panics on whichever observation lands
  second, taking down a bench run for a non-bug. It also erodes the one
  invariant that makes future promotions of either site to a parallel system
  safe by construction.
- **Related**: `#3445` (studio_host `Name → StringPool` inversion, OPEN,
  MEDIUM — same class and same non-scheduler blast radius), `#3446`,
  `#2388`; `#3260` / `#3303` are the scheduler-side HIGH precedents.
- **Suggested Fix**: Swap the two acquisitions in
  `byroredux/src/app_step.rs:410-411` so `GlobalTransform` is taken first,
  matching `byroredux/src/render/static_meshes.rs`. One-line change, no behaviour difference.

### ECS-D1-02: the recursive-read fast path skips `record_and_check`, so a re-entrant read can silently close an ABBA cycle
- **Severity**: LOW
- **Dimension**: Lock Ordering & Deadlock
- **Location**: `crates/core/src/ecs/lock_tracker.rs:94-110`
- **Status**: NEW
- **Description**: `track_read` returns from the recursive-read branch
  *before* the `#[cfg(debug_assertions)]` `record_and_check` block that every
  other acquisition runs. The module doc justifies this as "re-entrant read
  acquires on the same type are handled by the thread-local tracker's count
  and don't add edges" — true for the *outgoing* edge, but the branch also
  skips the **incoming** edges `held_other → T` and, with them, the
  reachability probe. A thread that holds `A`, acquires `B` (recording
  `A → B`), and then re-reads `A` while both are held is establishing
  `B → A`; that closes a cycle the detector would otherwise panic on, and it
  is neither recorded nor tested.
- **Evidence**:
```rust
// crates/core/src/ecs/lock_tracker.rs:94-110
if recursive_read {
    let mut map = locks.borrow_mut();
    let entry = map.get_mut(&type_id).expect("recursive read row vanished");
    if entry.read_count == 1 { log::warn!( … ); }
    entry.read_count = entry.read_count.saturating_add(1);
    return;                       // <- returns before record_and_check below
}
#[cfg(debug_assertions)]
{
    let held_others = locks.borrow().iter() … ;
    global_order::record_and_check(type_id, type_name, &held_others);
}
```
- **Impact**: A narrow blind spot in a debug-only, opt-in detector. Partly
  mitigated: the same branch already emits the `#2386` recursive-read hazard
  warning, so the situation is not silent — but that warning names the type,
  not the cycle, and `#3249` (OPEN) records that it carries no call-site
  information either.
- **Related**: `#2675` (the reachability generalisation this branch bypasses),
  `#3249`, `#2386`.
- **Suggested Fix**: Move the `record_and_check` block above the
  `if recursive_read` return (excluding `type_id` itself from `held_others`,
  which it already is by construction on the non-recursive path), so a
  re-entrant read is checked against the graph even though it adds no
  outgoing edge.

### ECS-P2-01: `combat_input_system` holds the `CombatState` write guard across a component-storage acquisition `[P2-gameplay]`
- **Severity**: MEDIUM
- **Dimension**: P2 Gameplay Slice / Lock Ordering
- **Location**: `byroredux/src/combat.rs:120-135` (helper at `byroredux/src/combat.rs:384-390`)
- **Status**: NEW
- **Description**: The cooldown-arming branch runs inside
  `if let Some(mut state) = world.try_resource_mut::<CombatState>()` and calls
  `attack_cooldown_seconds(world, aggressor)` — which does
  `world.get::<EquippedWeapon>(aggressor)` — while that `ResourceWrite` guard
  is still live. This is exactly the pattern the World house rule forbids
  (`crates/core/src/ecs/world.rs:10-33`: snapshot into an owned local and drop
  your guards *before* calling a helper that locks). It is the only
  resource-**write**-across-component-read site in the slice.
- **Evidence**:
```rust
// byroredux/src/combat.rs:120-129
let attack_ready = if let Some(mut state) = world.try_resource_mut::<CombatState>() {
    state.blocking = block_held;
    state.cooldown_remaining = (state.cooldown_remaining - dt.max(0.0)).max(0.0);
    if attack_pressed && in_character_mode && state.cooldown_remaining <= 0.0 {
        state.cooldown_remaining = aggressor.map_or(MELEE_COOLDOWN_SECONDS, |aggressor| {
            attack_cooldown_seconds(world, aggressor)   // -> world.get::<EquippedWeapon>()
        });
```
- **Impact**: Records a `CombatState(write) → EquippedWeapon(read)` edge in the
  global lock-order graph. No reverse edge exists today (`CombatState`'s other
  readers clone and release, and `combat_input_system` is a `Stage::Update`
  exclusive), so this is a latent edge rather than a live deadlock — hence
  MEDIUM, not HIGH.
- **Suggested Fix**: Resolve the cooldown before opening the guard
  (`let armed_cooldown = aggressor.map_or(…);` above line 120) and assign the
  precomputed value inside, flattening the hold-stack to one lock.

### ECS-P2-02: `collect_candidates` holds the scratch write guard across five component reads, every frame `[P2-gameplay]`
- **Severity**: MEDIUM
- **Dimension**: P2 Gameplay Slice / Lock Ordering
- **Location**: `byroredux/src/interaction.rs:863-873` (helper `populate_candidates` at `byroredux/src/interaction.rs:875-924`)
- **Status**: NEW
- **Description**: `collect_candidates` opens a
  `ResourceWrite<InteractionCandidateScratch>` and calls `populate_candidates`
  *through* it; that helper then acquires five component read guards
  (`DoorTeleport`, `RumbleOnActivate`, `QuestAdvanceOnActivate`,
  `TwoStateActivator`, `MG07LabyrinthianDoor`) with the write guard still
  held. Same class as ECS-P2-01, but on the per-frame path rather than an
  attack edge.
- **Evidence**:
```rust
// byroredux/src/interaction.rs:863-868
fn collect_candidates(world: &World) -> FxHashMap<EntityId, InteractionKind> {
    if let Some(mut scratch) = world.try_resource_mut::<InteractionCandidateScratch>() {
        scratch.candidates.clear();
        populate_candidates(world, &mut scratch.candidates);   // 5 component locks, guard live
        std::mem::take(&mut scratch.candidates)
```
- **Impact**: Five scratch→component edges per frame in the debug lock-order
  graph. No reverse edge exists and `interaction_system` is a `Stage::Update`
  exclusive, so no demonstrable deadlock — MEDIUM.
- **Suggested Fix**: Take the map out of the resource first, populate an owned
  local with no guard held, and hand it back. The `std::mem::take` already
  present proves the map can leave the resource; capacity reuse is preserved.

---

# Dimension 2 — Storage Correctness

### ECS-D2-01: `crates/core/src/ecs/sparse_set.rs`'s two storage-population figures are both stale, and disagree with each other
- **Severity**: LOW
- **Dimension**: Storage Correctness (doc-rot)
- **Location**: `crates/core/src/ecs/sparse_set.rs:11-25` and `crates/core/src/ecs/sparse_set.rs:172-186`
- **Status**: NEW
- **Description**: Two rationale comments in one file quantify the
  sparse-vs-packed population differently, and neither matches the tree. The
  `EMPTY` sentinel doc (`#2148`) says the 4-byte slot "halves that floor
  across all 122 `SparseSetStorage` component declarations"; the
  `remove_entities_erased` early-out (`#2397`) says "147 sparse component
  types vs. 11 packed". Current counts are **167** `type Storage =
  SparseSetStorage` declarations and **10** `type Storage = PackedStorage`
  (of which only four are production components — `Transform`,
  `GlobalTransform`, `WorldBound`, `SceneFlags`; the other six are test
  fixtures).
- **Evidence**:
```
$ rg -c 'type Storage = SparseSetStorage' --type rust .   # -> 167
$ rg -c 'type Storage = PackedStorage'    --type rust .   # -> 10
```
- **Impact**: Documentation only — both comments justify decisions that remain
  correct (and more so, since the ratio has widened). But these are the
  numbers a future maintainer would use to re-derive whether the sentinel and
  the early-out still earn their complexity, and two contradictory figures in
  one file mean at least one is guaranteed wrong.
- **Suggested Fix**: Replace both hard-coded counts with the ratio they are
  arguing for, or state them once with the `rg` incantation that regenerates
  them.

---

# Dimension 8 — Hot-Path & Hierarchy Walks

### ECS-D8-01: no hierarchy walk in the engine bounds its traversal, so a `Parent`/`Children` cycle hangs the frame with no diagnostic
- **Severity**: MEDIUM
- **Dimension**: Hot-Path Performance Invariants / robustness
- **Location**: `crates/core/src/ecs/systems.rs:221-252`; sibling walks at `byroredux/src/systems/bounds.rs:284-289` and `byroredux/src/systems/bounds.rs:302-319`
- **Status**: NEW
- **Description**: Three per-frame hierarchy walks share one unguarded shape —
  none carries a visited set, a depth cap, or an iteration budget:
  1. `make_transform_propagation_system`'s BFS drain
     (`crates/core/src/ecs/systems.rs:221`) — `while let Some(entity) =
     queue.pop_front()` re-enqueues `cq.get(entity)`'s children unconditionally.
  2. `make_world_bound_propagation_system`'s parent-chain climb
     (`byroredux/src/systems/bounds.rs:284-289`) — `while let Some(p) =
     parent_q… { cur = p; }` with no termination condition other than a
     missing `Parent`.
  3. That system's post-order stack walk (`byroredux/src/systems/bounds.rs:302-319`).

  Nothing upstream prevents a cycle from existing. `add_child`
  (`byroredux/src/helpers.rs:139-151`) pushes unconditionally — no dedup, no
  self-parent rejection — and `World::insert(child, Parent(p))` performs no
  acyclicity check. The save path's `validate_world`
  (`crates/save/src/validate.rs:119-190`) checks that `Parent` ⇄ `Children`
  agree and that neither points past `next_entity`, but **not** that the graph
  is acyclic; and on the load side `restore_world`
  (`crates/save/src/driver.rs:168`) runs it through
  `log_validation_warnings` — a warning, after the world is already
  populated, not a rejection.
- **Evidence**:
```rust
// crates/core/src/ecs/systems.rs:221-251 — no visited set, no depth bound
while let Some(entity) = queue.pop_front() {
    let Some(parent) = pq.get(entity) else { continue; };
    …
    if let Some(children) = cq.get(entity) {
        queue.extend(children.0.iter().copied());
    }
}
```
```rust
// byroredux/src/helpers.rs:139-151 — no dedup, no self-parent guard
pub(crate) fn add_child(world: &mut World, parent: EntityId, child: EntityId) {
    …
    if has_children {
        let mut cq = world.query_mut::<Children>().unwrap();
        cq.get_mut(parent).unwrap().0.push(child);
```
- **Impact**: A bidirectionally-consistent cycle reachable from a propagation
  root produces an unbounded loop inside a `Stage::PostUpdate` system: the
  process hangs with no panic, no log line, and no watchdog — strictly worse
  than the fail-fast crash the scheduler's panic policy (`#1412`) deliberately
  chose everywhere else. A duplicated `Children` entry is the milder case:
  exponential re-walk of that subtree rather than a hang.
  **Honest framing**: I found no live construction path. `world.remove::<Parent>`
  appears only in tests, and every production `add_child` caller passes a
  freshly-spawned child. This is a defence-in-depth gap on a corrupt or
  hand-edited save, or on a future spawn-path bug — not a reproducible defect
  today.
- **Suggested Fix**: Cheapest correct guard is a per-frame iteration budget on
  the BFS drain (`queue` pops ≤ `Transform::len()` + `Children` total, then
  `log::error!` and bail) — O(1) cost, converts a hang into a diagnosable
  frame. Adding an acyclicity pass to `validate_world` would additionally stop
  a corrupt save at the door instead of after `restore_world` has cleared the
  live world.

---

# Dimension 10 — Animation Runtime

**Reachability note for D10-01/02/03/06.** `AnimationStack` is never
`world.register`-ed nor inserted by production code: `byroredux/src/boot.rs:1043`
declares `.writes::<AnimationStack>()`, but the registration block
(`byroredux/src/boot.rs:562-667`) registers only `AnimationPlayer` and
`RootMotionDelta`. `World::query_mut` returns `None` for a storage that was
never created (`crates/core/src/ecs/world.rs:494-496`), so
`animation_system_inner`'s stack pass early-returns at
`byroredux/src/systems/animation.rs:799-801` on every real frame. The layer /
crossfade subsystem is fully implemented, save-serialised
(`byroredux/src/save_io.rs:346`) and debug-inspectable
(`crates/debug-server/src/registration.rs:208`), but has no runtime producer.
Verified independently of the sub-agent that reported these. Severities are
set for latent-not-live.

### ECS-D10-01: `AnimationLayer` blend-in contributes exactly zero weight for the whole fade — the crossfade is a hard cut
- **Severity**: MEDIUM
- **Dimension**: Animation Runtime
- **Location**: `crates/core/src/animation/stack.rs:80-99`, `crates/core/src/animation/stack.rs:212-219`
- **Status**: NEW
- **Description**: `with_blend_in` parks `weight` at `0.0` ("Starts at zero,
  ramps up"), but nothing ever ramps it. `effective_weight()` computes the
  blend-in progress and *multiplies* it into `self.weight`, which is still
  `0.0`, so the product is `0.0` for the entire blend-in window.
  `advance_stack` is the only writer of `layer.weight` and it assigns only at
  *completion* (`weight = weight.max(1.0)`), never during the ramp. The
  incoming layer is therefore invisible to `sample_blended_transform` (its
  `ew < 0.001` cull fires) for the whole fade, then snaps to full weight in
  one tick, while the outgoing layer renormalises to `w = 1.0` and holds full
  influence until culled.
- **Evidence**:
```rust
// crates/core/src/animation/stack.rs:80-85
pub fn with_blend_in(mut self, blend_time: f32) -> Self {
    self.blend_in_remaining = blend_time;
    self.blend_in_total = blend_time;
    self.weight = 0.0; // Starts at zero, ramps up.
```
```rust
// crates/core/src/animation/stack.rs:88-95
let mut w = self.weight;                       // 0.0 for the whole blend-in
if self.blend_in_total > 0.0 && self.blend_in_remaining > 0.0 {
    let progress = 1.0 - (self.blend_in_remaining / self.blend_in_total);
    w *= progress;                             // 0.0 * progress == 0.0
}
```
  No test pins mid-blend weight — the only `with_blend_in` assertions check
  `blend_in_total`, never `effective_weight()` mid-fade.
- **Impact**: Every KFM-driven transition (`apply_pending_transition` →
  `AnimationStack::play`) is a hard cut plus a one-tick pop instead of a
  blend. Latent today, but it is the entire point of the layer stack, so the
  first consumer that registers `AnimationStack` inherits a non-functional
  crossfade.
- **Suggested Fix**: Make the blend-in a weight *source* rather than a
  multiplier — keep the ramp target in a dedicated field (default `1.0`) and
  have `advance_stack` write `layer.weight = target * progress` each tick. Add
  a test asserting the two layers' effective weights sum to ~1 at the midpoint.

### ECS-D10-02: a non-playing layer never ticks its blend-out, so `cleanup_finished` can never remove it and it holds full weight forever
- **Severity**: MEDIUM
- **Dimension**: Animation Runtime
- **Location**: `crates/core/src/animation/stack.rs:167-226` (guard at `169-171`), `crates/core/src/animation/stack.rs:152-159`
- **Status**: NEW
- **Description**: `advance_stack`'s `if !layer.playing { continue; }` skips
  the *whole* per-layer body, including the blend timers — not just the clip
  clock. `cleanup_finished` only retains-out layers satisfying
  `blend_out_total > 0.0 && blend_out_remaining <= 0.0`, so a paused layer
  that `play()` scheduled for fade-out (`play` sets `blend_out_remaining` on
  *every* layer regardless of `playing`) keeps `blend_out_remaining ==
  blend_time` forever, is never retained out, and — because
  `effective_weight()` evaluates its blend-out factor as `remaining/total ==
  1.0` — contributes its **full** weight to every subsequent blend
  indefinitely.
- **Evidence**:
```rust
// crates/core/src/animation/stack.rs:168-171
for layer in &mut stack.layers {
    if !layer.playing {
        continue;          // skips the blend-timer decrements at 213-222 too
    }
```
```rust
// crates/core/src/animation/stack.rs:152-159
pub fn cleanup_finished(&mut self) {
    self.layers.retain(|layer| {
        if layer.blend_out_total > 0.0 && layer.blend_out_remaining <= 0.0 {
            return false;  // unreachable for a layer whose timer never ticks
```
- **Impact**: A paused layer permanently pins the blend result and, at max
  channel priority, permanently overrides the live clip; each pause/`play`
  pair adds one immortal layer. Latent today — nothing in-tree sets
  `AnimationLayer::playing = false`.
- **Suggested Fix**: Move the blend-timer advance above the `!layer.playing`
  guard so fades are wall-clock and independent of clip playback, or have
  `cleanup_finished` also drop layers with `effective_weight() < 0.001 &&
  !playing`.

### ECS-D10-03: the stack path writes the accum root's **absolute** position as `RootMotionDelta`; the player path writes a per-tick delta
- **Severity**: MEDIUM
- **Dimension**: Animation Runtime
- **Location**: `byroredux/src/systems/animation.rs:945-964` vs `byroredux/src/systems/animation.rs:85-107` and `byroredux/src/systems/animation.rs:706-713`
- **Status**: NEW
- **Description**: `sampled_root_motion_delta` exists precisely because
  "applying the absolute sample every frame would compound hundreds of units
  of motion", and the `AnimationPlayer` path uses it. The `AnimationStack`
  path never got the same treatment: it feeds `split_root_motion(pos).1` — the
  raw sampled horizontal *position* — into `root_motion`, which
  `write_root_motion` stores into `RootMotionDelta`. The consumer,
  `cinematic_root_motion_system` (`byroredux/src/systems/cinematic.rs:119-130`),
  does `transform.translation += rotation * (local_delta * scale)` **every
  frame** it runs, so an absolute-position payload is integrated repeatedly.
- **Evidence**:
```rust
// byroredux/src/systems/animation.rs:951-953 (stack path) — `pos` is the blended ABSOLUTE translation
let (anim_pos, delta) = split_root_motion(pos);
transform.translation = anim_pos;
root_motion += delta;              // absolute, not a per-tick delta
```
```rust
// byroredux/src/systems/animation.rs:709-712 (player path) — converted to a per-tick displacement
let (anim_pos, _) = split_root_motion(pos);
transform.translation = anim_pos;
root_motion += sampled_root_motion_delta(clip, channel, ps.prev_time, current_time);
```
- **Impact**: An `AnimationStack`-driven actor in the `ExitCartEnd` cinematic
  window would be displaced by the accum root's absolute offset every tick —
  the exact compounding `sampled_root_motion_delta` exists to prevent. Latent
  today.
- **Suggested Fix**: Route the stack path's accum-root contribution through
  `sampled_root_motion_delta` using the dominant layer's
  `(prev_time, local_time)` and clip, mirroring the player path, so both
  honour the per-tick-displacement contract `RootMotionDelta` documents.

### ECS-D10-04: text keys are still dropped when one frame advances more than a full clip period but not an exact multiple
- **Severity**: MEDIUM
- **Dimension**: Animation Runtime
- **Location**: `crates/core/src/animation/text_events.rs:63-117`
- **Status**: NEW — a coverage gap in `#3034`'s fix, not a regression of it
- **Description**: `#3034`'s "fire every key once" arm is gated on
  `curr_time == prev_time`, i.e. a delta that is an *exact* integer multiple
  of `duration`. For `|delta| > duration` with a non-zero residual the
  playhead still traverses one or more complete periods, yet control falls
  into the ordinary forward or wrap branch and only the residual window is
  scanned. Worked example on a `Loop` clip, `duration = 0.5`, `prev = 0.1`,
  `delta = 1.3` → `local_time = 1.4 % 0.5 = 0.4`; `curr (0.4) >= prev (0.1)`
  so only keys in `(0.1, 0.4]` fire — keys in `(0.4, 0.5]` and `(0.0, 0.1]`,
  each crossed twice during the traversal, fire zero times.
- **Evidence**:
```rust
// crates/core/src/animation/text_events.rs:63-67 — the #3034 arm requires exact equality
} else if curr_time == prev_time
    && applied_delta != 0.0
    && clip.duration > 0.0
    && clip.cycle_type == CycleType::Loop
```
  `dt` is raw unclamped wall-clock (`byroredux/src/app_events.rs:513`, `531`,
  `541` — `wall_dt` stored into `DeltaTime` with no ceiling), so a
  streaming/cell-load hitch of several hundred ms against a sub-second looping
  clip is a realistic producer.
- **Impact**: `AnimationTextKeyEvents` feeds `cinematic_animation_event_system`,
  which writes `QuestStageState`. A frame hitch on a short looping clip
  silently swallows the notification, with no counter or log.
- **Related**: `#3034` (CLOSED), `#3470` (CLOSED — the zero-advance guard on
  the same arm).
- **Suggested Fix**: Widen the arm to `applied_delta.abs() >= clip.duration`
  on `Loop` clips (fire every key once, then still scan the residual window),
  keeping the `applied_delta != 0.0` guard `#3470` added.

### ECS-D10-05: clip release is driven by NIF-cache LRU pressure with no liveness check, so live animated REFRs freeze permanently
- **Severity**: MEDIUM
- **Dimension**: Animation Runtime / Component Lifecycles
- **Location**: `crates/core/src/animation/registry.rs:156-191`, `byroredux/src/cell_loader/nif_import_registry.rs:488-530`, `byroredux/src/cell_loader/spawn.rs:747-790`
- **Status**: NEW
- **Description**: `NifImportRegistry::insert` evicts by pure LRU on a default
  2048-entry cap, returns the evicted entry's memoised clip handle, and every
  caller forwards it to `AnimationClipRegistry::release`
  (`byroredux/src/streaming_helpers.rs:544`,
  `byroredux/src/cell_loader/references/mod.rs:136`,
  `byroredux/src/cell_loader/partial.rs:151`). **Nothing consults whether an
  `AnimationPlayer` still holds that handle.** `AnimationPlayer` stores only
  the numeric handle — no path back to the source path — so nothing ever
  re-resolves. Post-release the slot reads as an empty clip (every channel map
  cleared, `duration` zeroed), so the still-loaded REFR's UV scroll /
  emissive pulse / visibility flicker stops for the rest of the session.
- **Evidence**:
```rust
// byroredux/src/cell_loader/nif_import_registry.rs:497-527 — eviction is LRU-only; no liveness test
while self.core.len() > self.max_entries {
    let victim = self.access_tick.iter().min_by_key(|(_, &tick)| tick).map(|(k, _)| k.clone());
    …
    if let Some(handle) = self.clip_handles.remove(&victim_key) {
        freed_clip_handles.push(handle);
    }
```
  The registry's own doc calls this "live handles stay resolvable and just
  stop animating" — safe, but the eviction *trigger* is cache pressure, not
  entity lifetime.
- **Impact**: In a long exterior traversal (>2048 distinct model paths
  streamed while an earlier cell stays resident) animated scenery in older
  cells silently and permanently stops, indistinguishable from "the clip never
  loaded" — no log, no telemetry.
- **Related**: `#863` (CLOSED — added the release path this finding says is
  unguarded), `#2689` (OPEN — the *slot vector* growing monotonically, a
  different defect), `#1854`, `#2524`.
- **Suggested Fix**: Refcount clip handles against live
  `AnimationPlayer`/`AnimationLayer` holders so eviction can only release
  clips nothing is playing; alternatively store the source path on the player
  so a released clip can be lazily rebuilt via `get_or_insert_by_path`.

### ECS-D10-06: `sample_blended_transform` has no single-layer short-circuit
- **Severity**: LOW
- **Dimension**: Animation Runtime
- **Location**: `crates/core/src/animation/stack.rs:368-462`
- **Status**: NEW
- **Description**: The **allocation check passes** — the function allocates
  nothing; all state is scalar/`Vec3`/`Quat` locals. But it always runs both
  passes, so a stack with one layer (the common case once a fade completes)
  pays `registry.get()` twice, `effective_weight()` twice, and
  `clip.channels.get(&channel_name)` (a `HashMap` probe) twice per bone per
  frame. `#288` reduced three passes to two; the degenerate one-layer case was
  never carved out.
- **Evidence**: the same `registry.get(layer.clip_handle)` /
  `layer.effective_weight()` / `clip.channels.get(&channel_name)` triple
  appears at `crates/core/src/animation/stack.rs:379-388` and again at
  `crates/core/src/animation/stack.rs:422-432`.
- **Impact**: ~2× the intended per-bone lookup cost on the steady-state
  single-layer stack. Latent today (see the reachability note).
- **Suggested Fix**: Early-out when `stack.layers.len() == 1`: resolve clip +
  channel once, apply the weight cull, and return the raw `sample_*` triple
  without the normalisation pass (a no-op at `w = ew/ew = 1.0`).

### ECS-D10-07: `write_root_motion`'s zero-motion early return latches the previous tick's delta
- **Severity**: LOW
- **Dimension**: Animation Runtime
- **Location**: `byroredux/src/systems/animation.rs:73-83`
- **Status**: NEW
- **Description**: `write_root_motion` returns before touching
  `RootMotionDelta` when this tick's motion is exactly `Vec3::ZERO`, so the
  component keeps whatever the last non-zero tick wrote. The only drain,
  `cinematic_root_motion_system`, zeroes it only for actors currently awaiting
  `ExitCartEnd`. An actor outside that window carries a stale delta
  indefinitely; when it later enters the window, the first tick has
  `current_time ≈ prev_time`, so the write is skipped and the consumer applies
  the stale value.
- **Evidence**:
```rust
// byroredux/src/systems/animation.rs:73-77
fn write_root_motion(world: &World, entity: EntityId, motion: Vec3) {
    if motion == Vec3::ZERO {
        return;                                   // component keeps the old value
    }
```
- **Impact**: A single-frame position pop of up to one prior frame's root
  displacement at the start of a cart-exit cinematic. Not an integration leak
  — the write is an assignment, not `+=`.
- **Suggested Fix**: Write unconditionally (including `Vec3::ZERO`) for
  entities that already have the component, keeping the `query_mut`/`get_mut`
  guards so entities without it stay untouched.

---

# P2 Gameplay Slice (Dimension 7 — Component Lifecycles)

*(ECS-P2-01 and ECS-P2-02 are filed under Dimension 1 above.)*

### ECS-P2-03: the alive→dead transition never retires the actor from the ambient-package scheduler, so every corpse pays a heap clone per frame forever `[P2-gameplay]`
- **Severity**: MEDIUM
- **Dimension**: P2 Gameplay Slice / Component Lifecycles
- **Location**: `byroredux/src/combat.rs:420-433` (`reconcile_dead_actor`) + `byroredux/src/npc_spawn/ai_package.rs:596-628`, `byroredux/src/npc_spawn/ai_package.rs:687-694`
- **Status**: NEW — residual of CLOSED `#3353`, **not** a regression (the
  once-per-game-minute gate still works for live actors)
- **Description**: `reconcile_dead_actor` leaves `AmbientPackageRuntime` on the
  corpse. The pass-2 gate is `requested.contains(actor) || *last !=
  Some(minute)`, and `last_evaluated_game_minute` is written only in the
  trailing loop over `updates` — which dead actors never reach, because the
  `Dead` skip `continue`s *after* pass 3 has already cloned the runtime. A
  corpse's minute marker therefore freezes at its final live evaluation and it
  is "due" on every frame whose game-minute differs, forever.
- **Evidence**:
```rust
// byroredux/src/npc_spawn/ai_package.rs:598-600 — the gate; no Dead filter
let due: Vec<EntityId> = last_evaluated
    .into_iter()
    .filter(|(actor, last)| requested.contains(actor) || *last != Some(minute))
```
```rust
// byroredux/src/npc_spawn/ai_package.rs:611-626 — pass 3 clones FIRST, then skips the dead
        .map(|query| { due.iter().filter_map(|&actor| query.get(actor).map(|r| (actor, r.clone()))).collect() })
…
for (actor, runtime) in runtimes {
    if world.get::<Dead>(actor).is_some() {
        continue;                                 // never reaches `updates`
```
  `AmbientPackageRuntime` owns `package_candidates: Vec<u32>`, so `r.clone()`
  is a real allocation.
- **Impact**: One heap allocation + one `Dead` lookup per corpse per frame,
  permanently, growing linearly with the session's kill count — exactly the
  cost model `#3353` was closed to remove, with the dead population left
  uncovered. Freed each frame, so churn rather than a leak: MEDIUM.
- **Suggested Fix**: Add `remove_component::<AmbientPackageRuntime>(world,
  actor)` (and `EvaluatePackageRequest`) to the death teardown — a corpse has
  no package to select. Cheaper alternative: move the `Dead` check into pass
  2's `due` filter so the clone never happens.

### ECS-P2-04: `NavPath` survives the death teardown `[P2-gameplay]`
- **Severity**: LOW
- **Dimension**: P2 Gameplay Slice / Component Lifecycles
- **Location**: `byroredux/src/npc_spawn/ai_package.rs:468-483` (reached from `byroredux/src/combat.rs:420-421`)
- **Status**: Existing: `#3255`
- **Description**: Re-verified still live while auditing the death-teardown
  roster. `clear_ambient_behavior` removes `TravelBehavior`/`TravelState`/
  `Traveled` and `GuardBehavior`/`GuardState` but not the `NavPath` cache
  those systems write (`byroredux/src/systems/travel.rs`,
  `byroredux/src/systems/guard.rs`). `NavPath` holds a `VecDeque<Vec3>`.
- **Impact**: Retention only, not re-animation — `travel_system` /
  `guard_system` iterate the behaviour components, which are gone. The stale
  `VecDeque` is held until despawn.
- **Suggested Fix**: Add `remove_component::<NavPath>(world, actor);`
  alongside the Travel/Guard removals.

### ECS-P2-05: the three P2 `Stage::Update` exclusives carry no `Access` declaration `[P2-gameplay]`
- **Severity**: LOW
- **Dimension**: P2 Gameplay Slice / Scheduler Access Declarations
- **Location**: `byroredux/src/boot.rs:851-856`
- **Status**: Existing: `#3473`
- **Description**: `interaction_system`, `combat_input_system` and
  `combat_damage_system` use bare `add_exclusive`, unlike their `Stage::Late`
  sibling `reconcile_pending_dead_actors_system`
  (`byroredux/src/boot.rs:1437-1443`, `add_exclusive_with_access`). Confirmed
  still true. While re-verifying, the hold-stack `#3473`'s title names was
  also re-confirmed: `attack_damage` (`byroredux/src/combat.rs:318-331`) binds
  the `EquippedWeapon` `ComponentRef` in a `match` arm and calls
  `melee_damage_charal_bonus` inside it; that helper stacks
  `MeleeDamageConfig`, `CharacterRuleset` and `ActorValues` on top — and
  `let config = *config;` **shadows the guard without dropping it** (the same
  shadow-is-not-a-drop pattern as OPEN `#3444`). Four guards are
  simultaneously live.
- **Impact**: Runtime-neutral — the analyzer skips exclusives and
  `Scheduler::run` sequences them. The cost is diagnostic: `sys.accesses`
  shows blank rows for the three systems that own the deepest hold-stack in
  the slice.
- **Suggested Fix**: Convert the three to `add_exclusive_with_access`;
  separately replace the shadowing `let config = *config;` with an explicit
  `drop` or a scoped read so the guard actually releases.

### ECS-P2-06: per-actor melee state lives in a global `Resource` `[P2-gameplay]`
- **Severity**: LOW
- **Dimension**: P2 Gameplay Slice / Resource shape
- **Location**: `byroredux/src/combat.rs:45-69`
- **Status**: NEW
- **Description**: `CombatState` mixes genuinely global telemetry
  (`attacks_started` / `hits_landed` / `kills` / `last`) with two
  per-combatant facts (`cooldown_remaining`, `blocking`), both written from
  the player's `ActionState`.
- **Impact**: **No present-day failure** — there is exactly one combatant
  (`combat_input_system` resolves its aggressor from `PlayerEntity` and gates
  on `PlayerMode::Character`), and the code already flags that
  `HitEvent::blocked` has no NPC-side producer. LOW because it is a shape
  issue: the first NPC attacker makes a single global cooldown/block flag
  incorrect.
- **Suggested Fix**: Split the two per-actor fields into a `MeleeState`
  component (`SparseSetStorage`) on the combatant, leaving `CombatState` as
  pure session telemetry.

### ECS-P2-07: two distinct `PlayerEntity` resources describe the same fact with different null semantics `[P2-gameplay]`
- **Severity**: LOW
- **Dimension**: P2 Gameplay Slice / Resource shape
- **Location**: `byroredux/src/systems/character.rs:42` and `crates/scripting/src/papyrus_demo/mod.rs:71`; consumed side-by-side at `byroredux/src/combat.rs:106-108` vs `byroredux/src/interaction.rs:788-790`
- **Status**: NEW
- **Description**: `crate::systems::PlayerEntity(pub Option<EntityId>)` and
  `byroredux_scripting::papyrus_demo::PlayerEntity(pub EntityId)` hold the
  same canonical fact and are used interchangeably for the same purpose in the
  slice: `combat_input_system` excludes the aggressor's Rapier body using the
  first, `target_has_line_of_sight` excludes the player's body using the
  second. Both are written from one site.
- **Impact**: No divergence is reachable today — character mode binds both to
  the same body, and the fly-cam placeholder has no `RapierHandles`, so LOS
  exclusion degrades correctly. LOW because the two have different null
  encodings (`None` vs a live placeholder entity): a consumer migrating from
  one to the other silently changes its "no player" behaviour. It also makes
  the save registry-completeness guard ambiguous — it keys on the short type
  name, so one allowlist row covers both types.
- **Suggested Fix**: Keep one canonical `PlayerEntity` and have the scripting
  crate re-export it; at minimum pick one for all self-exclusion ray casts so
  combat and activation cannot disagree.

---

# Verified clean — checks that produced no finding

These are stated explicitly so a future run can tell "checked and clean" from
"never looked".

**Dimension 1 — Lock ordering.** Every `query` / `query_mut` / `resource` /
`resource_mut` / `try_resource*` site in `crates/core/src/ecs/world.rs` arms a
`TrackedRead`/`TrackedWrite` scope and defuses it only *after* the wrapper
exists (`#2149`); `query_2_mut` / `query_2_mut_mut` / `resource_2_mut` set up
tracker scopes in TypeId-ascending order, matching acquisition (`#313`);
all three `assert_ne!` on `A == B`; every acquisition resolves `PoisonError`
through `storage_lock_poisoned::<T>` / `storage_lock_poisoned_erased` /
`resource_lock_poisoned::<R>`, and `despawn` / `despawn_batch` /
`clear_entities` / `shrink_storages` all feed the `#466` `type_names`
side-table. `try_resource_2_mut` still does both existence checks before
acquiring either lock (`#465`). A mechanical scan of every production `fn(&World)`
for a same-type re-lock inside a live guard scope, and for inversions of the
canonical order documented in `docs/engine/ecs.md`, produced exactly one true
positive (ECS-D1-01); the other ten candidates (`byroredux/src/systems/bounds.rs`,
`byroredux/src/systems/sandbox.rs`, `byroredux/src/systems/character.rs` ×3,
`byroredux/src/commands/view.rs`, `byroredux/src/anim_convert.rs`,
`crates/physics/src/sync.rs`, `crates/physics/src/water.rs`) were
all verified as explicitly scoped or `drop`-ed guards. `crates/debug-server/src/listener.rs`
has **zero** `World` references — the per-client TCP threads only enqueue.

**Dimension 2 — Storage correctness.** `SparseSetStorage`'s swap-remove fixes
the moved entity's sparse pointer, takes the no-swap path on the last element,
and overwrites in place; `structural_gen` is bumped on insert (including
reparent overwrite) and on remove. `PackedStorage` maintains sorted order on
every path, `insert_bulk` uses the append + single-sort + last-writer-wins
dedup (`#467`), `drain_dirty_into` clears `out` while preserving `self.dirty`'s
capacity (`#1371`), and `clear_erased` releases capacity (`#2395`).
`remove_entities_erased` early-outs on an empty victim set (`#2397`) and both
overrides carry the sorted-victims `debug_assert`. `World::insert_batch`'s
adapter iterator still fires the `entity < next_entity` `debug_assert` per item.
Only `Transform` and `GlobalTransform` are `PackedStorage` + `TRACK_CHANGES`,
and both dirty sets have a live production drainer
(`make_transform_propagation_system` and `make_world_bound_propagation_system`
respectively) — no unbounded dirty growth. All seven M42 AI-procedure
components and their `*State` / terminal-marker siblings are
`SparseSetStorage`.

**Dimension 3 / 6 — Query borrow safety and unsafe.** Exactly four `unsafe`
blocks exist in `crates/core/src/ecs/` and `crates/core/src/animation/`, all in
`crates/core/src/ecs/query.rs`, all with a SAFETY comment tying validity to the
live guard, and all four wrappers keep their `guard` field alive.
`ComponentRef` still retains its guard rather than returning a raw pointer.
The `ecs-query-miri` CI job (`.github/workflows/ci.yml:145-157`) is intact and
its skip list has **not** widened — still only
`resource_visible_to_system_via_scheduler`.

**Dimension 4 — Resources.** `ResourceRead` / `ResourceWrite` downcast through
the guard on each `Deref` (deliberately uncached — not the `#1367` hot path);
`insert_resource` and `remove_resource` both resolve poison rather than
swallowing it (`#1837`).

**Dimension 5 / 5b — Scheduler and access declarations.**
`rg '^\s*scheduler\.add_to\(' byroredux/src/boot.rs` → **0 matches**;
`add_to_with_access` → **9**. The boot guard in `install_runtime_registries`
(`byroredux/src/boot.rs:1541-1563`) asserts all three of
`undeclared_parallel_count()`, `known_conflict_count()` and
`unknown_pair_count()` are zero. Note for the skill: these are **release-level
`assert_eq!`**, not `debug_assert_eq!` as `.claude/commands/audit-ecs/SKILL.md`
still says — a deliberate strengthening (`#2690`), so the skill text is stale,
not the code. Stage discriminants are still `Early..=Late` `0..=4`;
`AccessConflict` still has exactly the three variants. Spot-verified two
declarations against their bodies for drift and found none:
`camera_follow_system` (`byroredux/src/systems/character.rs:507-611`) touches
exactly `PlayerMode`/`PlayerEntity`/`ActiveCamera`/`InputState`/`Transform`/
`GlobalTransform`/`CharacterController`, and `reverb_zone_system`
(`byroredux/src/systems/audio.rs:58-94`) exactly `CellLightingRes` + `AudioWorld`.

**Dimension 7 — Component lifecycles.** `prune_seat_reservations`
(`byroredux/src/cell_loader/references/mod.rs:897-918`) keeps **both**
liveness halves — live furniture *and* a claimant still carrying `Seated`
naming that furniture. `event_cleanup_system`'s Pattern-A drain list is
pinned against its own module doc by a source-scanning contract test
(`crates/scripting/src/cleanup.rs`). `timer_tick_system`
(`crates/scripting/src/timer.rs:29-58`) removes expired timers in the same
frame — no negative-time accumulation — and every marker it needs is
pre-registered by `byroredux_scripting::register`
(`byroredux/src/boot.rs:670`). Cell unload releases owned non-ECS state before
`despawn_batch` — `release_victim_item_instances` and
`release_victim_rapier_bodies` (`byroredux/src/cell_loader/unload.rs:307-334`)
— so no orphaned Rapier body or `ItemInstance` slot. Streaming dedups at the
cell level (`byroredux/src/streaming.rs:885`).
`AnimationClipRegistry::get_or_insert_by_path`'s case folding is intact
(`#790`/`#866`), pinned by `get_or_insert_by_path_is_case_insensitive`.
`AnimationController`'s handles degrade to a silent no-op after a release
rather than dangling.

**Dimension 8 — Hot-path invariants.** `#823`: the `held_others` collection in
`lock_tracker`'s `track_read`/`track_write` is `#[cfg(debug_assertions)]`-gated
as one block. `#825`/`#1371`: `make_transform_propagation_system` keys its
cached roots on `(Transform::len, Parent-len, next_entity_id)` plus
`Parent`/`Children` `structural_generation()`, and drains via
`drain_dirty_into`, not `take_dirty`. `#824`: `NameIndex.map` is refilled in
place (`clear` + `reserve` + reinsert). `#828`: the `events`/`seen_labels`
scratches are hoisted into `AnimScratch` and handed over by `clone()`, not
`mem::take`; the extracted helpers (`subtree_cache`, `write_root_motion`,
`apply_bool_channels`) are single-sourced with no DRY-undo drift. *Skill note*:
the `write_lazy!` macro the skill names no longer exists anywhere in the tree —
`#2399` replaced it with `apply_target!` plus fixed-order per-target passes,
and that repetition is a structural requirement of the `#2399` lock-order fix,
not drift. `#932`: `footstep_system` still round-trips `FootstepScratch`
through `mem::take` + restore, including on the audio-missing bail. `#2923`:
every collection on `SkinSlotPool` is `FxHashMap`/`FxHashSet`; the only
`std::collections` hits in that file and in `byroredux/src/render/particles.rs`
are inside `#[cfg(test)]` modules.

**Dimension 9 — NIFAL canonical material.** `Material::metalness` /
`roughness` are plain resolved `f32` (`crates/core/src/ecs/components/material.rs:344-362`),
not `Option`; `resolve_pbr` fills only NaN slots and then clamps, preserving
authored values; `byroredux/src/render/static_meshes.rs` carries no per-draw
`classify_pbr` fallback.

**Dimension 10 — Animation runtime.** Clip-handle validity (bounds-checked
`Option`, every consumer handles `None`, unload despawns the players);
`dt == 0` / negative / NaN / zero-length-clip handling in `advance_time` and
`advance_stack` (`finite_time_delta` folds non-finite to `0.0`; `Loop` guards
the modulo; `Reverse` early-returns on `duration <= 0.0`; `CycleType` is read
per clip, never globally — the residual NaN-`duration` hazard is
**Existing: `#3432`**, OPEN); weight normalisation in
`sample_blended_transform`; no double-emission of text keys (half-open windows,
plus interned-symbol dedup across layers); `find_key_pair` boundary clamping at
both ends and quaternion shortest-path dot-sign flips at every site.

**P2 gameplay slice.** Registration order is correct and load-bearing:
`byroredux/src/boot.rs:851` `interaction_system` → `855` `combat_input_system`
→ `856` `combat_damage_system`, all `Stage::Update` exclusives run in
registration order. The skill's "unbounded `CombatTraceEntry` trace growth"
premise is **stale** — `CombatState.last` is a single
`Option<CombatTraceEntry>`, not a growing `Vec`; same for `InteractionTrace`.
`clear_ambient_behavior` (`byroredux/src/npc_spawn/ai_package.rs:468-483`)
matches the full live seven-procedure roster (16 components; there is no
`SandboxState`) — only `NavPath` is missing (ECS-P2-04). Corpse re-animation
via the package swap is guarded (`#3030` has not regressed). `Dead` storage is
pre-registered unconditionally. The "inventory.rs is a second source of truth"
premise is **not substantiated**: `PlayerInventoryTemplate` is written only by
`install_catalog` and read only by `attach_to_player` at spawn, and
`install_catalog` runs from the single `populate_scene_runtime` funnel with a
fresh `EsmIndex`, so the stale-after-load-order-change failure mode is
unreachable. The four slice files declare **zero** `impl Component` — every
type they own is a `Resource`, and every component they consume is correctly
`SparseSetStorage`. `byroredux/src/settings_io.rs` has no ECS surface and no lock inversion.

---

## Coverage note

Per `.claude/commands/_audit-common.md`'s un-owned-subsystems table, this run
covered the **Gameplay slice (P2)** explicitly (suite instruction) and reached
`crates/sdk`'s engine adapter `byroredux/src/studio_host.rs` incidentally —
`apply_command` / `pick_from_view` / `frame_selection` were checked for guard
overlap and are clean; the known inversion in `snapshot` is already
**OPEN as `#3445`** and is not re-reported here. **Not** covered by this run:
FaceGen, Mod Runtime, FSR3 + FFI, the Havok packfile reader, and the
debug-server *command surface* (only its thread/`World` boundary was checked).

## Next step

```
/audit-publish docs/audits/AUDIT_ECS_2026-08-30.md
```
