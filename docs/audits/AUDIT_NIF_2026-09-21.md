**HEAD**: `f97775ca8` · **Baseline**: `docs/audits/AUDIT_NIF_2026-09-11.md` (committed `f74f8f68a`; that report recorded no HEAD line) · **Audited**: Dims 1–6 · **Unchanged since baseline (skimmed)**: none — every dimension's `Paths:` had commits since `f74f8f68a` (52 commits touch `crates/nif`: D1 17, D2 5, D3 3, D4 22, D5 9, D6 2). `crates/nif/src/blocks/mod.rs` itself is unchanged.

# NIF Parser Audit — 2026-09-21

**Command**: `/audit-nif` (default scope, all games), one leg of `/audit-suite --preset comprehensive`.
**Severity scale**: `.claude/commands/_audit-severity.md`.

## Method

- I ran the six dimensions myself, in order, with no sub-agents. Per-dimension notes are in `/tmp/audit/nif/dim_{1..6}.md`.
- **Default lane**: `cargo test -p byroredux-nif` gave 1,454 passed, 0 failed, 42 ignored (lib: 1,342 passed). This log came from the interrupted earlier run and is valid for this HEAD.
- **dhat lane**: all three `heap_allocation_bounds*` files pass (4 + 1 + 1).
- **The nightly real-data lane has never run** (NIF-D3-2026-09-21-01). So I ran every opt-in corpus gate locally, one title at a time with `-j 4`:
  - `parse_real_nifs`, `per_block_baselines` and `block_coverage_baselines` for all eight corpora;
  - `parse_rate_fo4_all_meshes` and `parse_rate_fo76_all_meshes`;
  - `oblivion_stream_drift_corpus`, `constraint_drift_corpus`, `normal_synthesis_corpus`;
  - `real_archive_torch_meshes_surface_particle_emitters`.
- **Probe**: the harness does not surface drift or opaque-tail histograms, so I built a standalone crate outside the repo (`/tmp/audit/nif/probe`, own target dir) against `byroredux-nif` / `byroredux-bsa`. It re-walks every vanilla mesh archive and:
  - aggregates the `drift_histogram`, `stubbed_drift_histogram` and `opaque_tail_histogram`;
  - re-parses every sized block with `block_size = None` inside a slice bounded to the block, to expose parsers that quietly reconcile themselves against `block_size`;
  - runs targeted censuses: Starfield shader types, `BSSkin::BoneData` sanity, `bhkRigidBody` field plausibility, NiSkinPartition index spaces, and the emitter-rate tier-(d) attribution.
- **Dedup sources**:
  - the cached issue list (`/tmp/audit/issues.json`, 4,458 issues);
  - the baseline report and its 18 published issues, #4148–#4165, all closed and each verified below;
  - today's sibling reports: `AUDIT_NIFAL_2026-09-21.md`, `AUDIT_SAFETY_2026-09-21.md`, `AUDIT_RENDERER_2026-09-21.md`, `AUDIT_ECS_2026-09-21.md`.

---

## Executive Summary

**Parser health is excellent on vanilla content.**
- Every shipped corpus parses **100% clean with 0 truncations, 0 NiUnknown blocks and 0 inferred ("guessed") skips**: 560,775 vanilla NIFs across 8 corpora.
- The only stream drift on any vanilla corpus is the known constraint motor-tail residual (values 1 / 18 / 19 / 26, malleable +4).
- The `block_size = None` re-parse finds no hidden drift outside the four known tail-capturing block types.
- All 18 findings from the 2026-09-11 baseline are fixed and hold.
- The baseline asked for Oblivion's clean rate to be re-measured. It is **100% (9,612 / 9,612)**.

**13 new findings: 0 CRITICAL, 0 HIGH, 6 MEDIUM, 7 LOW.** None duplicates an open issue.

The six MEDIUM findings:
1. **The #3919 nightly corpus lane has never executed** (NIF-D3-2026-09-21-01).
   - The workflow file is invalid: a job-level `if:` references `matrix`, which GitHub does not allow there. The result is 254 failed push runs with 0 jobs, and 0 scheduled or manually dispatched runs.
   - No self-hosted runner carries the `byroredux-game-data` label, so fixing the YAML alone would leave jobs queued.
   - The cost is already visible: two per-block baselines went red with no signal.
2. **Particle emitter tier (d) is a whole-scene first-match** (NIF-D4-2026-09-21-01).
   - Hundreds of vanilla particle systems get a sibling's authored birth rate: 690 on FNV, 386 on FO3, 313 on Skyrim SE.
   - Example: FO3 `explosionlarge01.nif` runs all six systems at 900/s against authored 7.5–90/s.
3. **`BSSkin::BoneData` bind transforms bypass both the #277 rotation sanitizer and #4549's finite gate** (NIF-D4-2026-09-21-02).
   - Affects FO4, FO76 and Starfield.
   - Vanilla exposure is measured at zero; the risk is corrupt or mod content.
4. **BSTriShape's #621 data_size-derived stride has no upper bound** (NIF-D1-2026-09-21-03). On a Sim Settlements 2 mesh it over-reads 216 bytes and decodes garbage vertices; `block_size` realignment hides this from the clean-rate gate.
5. **The #4157 allocation-amplification class survives on other fixed-size elements** (NIF-D6-2026-09-21-01).
   - Worst case is `BSSkin::BoneData`, at 68×.
   - #4156's new CInfo2014 path copied the loose pattern.
6. **`parse_havok_packfile` pre-sizes a Vec straight from an on-disk u32** (NIF-D6-2026-09-21-02). There is no production caller yet; the sibling #4155 in the same function was rated the same.

### Per-game coverage vs `docs/engine/nif-parser.md` § "Per-game NIF coverage"

| Game | Documented | Measured 2026-09-21 (HEAD `f97775ca8`) | Note |
|---|---|---|---|
| Oblivion | 100% (9,612), 0 truncations | **100% (9,612/9,612)**, 0 truncated, 0 guessed; block-count parity 9612/9612; drift detector 0 false positives | baseline's open question answered |
| Fallout 3 | 100% (17,172) | **100% (17,172)**; per-block OK (145 types) | |
| Fallout NV | 100% (20,746) | **100% (20,746)**; per-block OK (150) | |
| Skyrim LE | — (no row) | **100% (22,466)**; per-block OK (146) | new baseline this window (fb8173fe0); doc row missing |
| Skyrim SE | 100% (33,468) | **100% (33,468** incl. 6 optional archives); per-block **RED** (data drift, NIF-D3-2026-09-21-02) | |
| Fallout 4 | 100% vanilla (254,648 incl. third-party) | **100% vanilla (235,082, 8 archives)**; 254,589 across all 86 mesh-bearing archives on this install (incl. Creation Club + mods) at 99.9988% (1 truncated + 2 recovered, all Sim Settlements files) | third-party residue → NIF-D1-2026-09-21-03 |
| Fallout 76 | 100% (58,469 files) | **100% (102,968; Meshes 63,305)**; per-block **RED** (archive re-issued 2026-09-20) | all-meshes gate finds 4/20 listed archives |
| Starfield | 99.98% (120,836) | **100% (120,543, 13 NIF-bearing archives incl. FaceMeshes)**; per-block OK (28) | doc staleness is Existing #4440 |

### Stream-position telemetry (probe, full vanilla corpora)

| Game | `drift_histogram` (type: drift=count) | hidden (`block_size=None`) drift | opaque tails |
|---|---|---|---|
| Oblivion | n/a (no `block_sizes`); drift detector 0 warnings | n/a | — |
| FO3 | LimitedHinge {1:469, 18:6, 26:11}, Ragdoll {1:462, 18:4, 26:23}, Prismatic {1:9}, Malleable {4:1, 5:59} | none | — |
| FNV | LimitedHinge {1:594, 26:2}, Ragdoll {1:707, 26:1}, Prismatic {1:3}, Malleable {5:143} | none | — |
| Skyrim LE / SE | LimitedHinge {1}, Ragdoll {1} | none | all 0 |
| FO4 (vanilla) | none | none | all 0 |
| FO76 | none | none | all 0 |
| Starfield | none | only the known tail-capturers | BLSP {0: 480,757, 30: 3,080} · BSEffect {0: 104, 32: 879} · BSFaceGenNiNode {2: 1,552} · BSWeakReferenceNode {0 … 5,940} |

`stubbed_drift_histogram` is empty on every corpus (see NIF-D1-2026-09-21-02). The `constraint_drift_corpus` gate passes: 4,069 events, all of them known values.

---

## Block Type Coverage Matrix

Fresh count of `parse_block_inner` (`crates/nif/src/blocks/mod.rs`):
- **255 top-level arms**, including the `_` fallback;
- **314 distinct type-name literals**, no duplicates, no guarded arms.

Two independent counting methods agree, and so does `docs/engine/nif-parser.md` after the #4162 recount. The baseline report's "312" was a miscount of this same, unchanged file.

| Game | NIFs (per-block gate) | blocks | distinct wire types | parsed | NiUnknown | truncated |
|---|---:|---:|---:|---:|---:|---:|
| Oblivion | 8,032 (Meshes.bsa; 9,612 all) | 329,933 | 120 | all | 0 | 0 |
| Fallout 3 | 17,172 | 526,109 | 145 | all | 0 | 0 |
| Fallout NV | 20,746 | 662,102 | 150 | all | 0 | 0 |
| Skyrim LE | 22,466 | 686,641 | 146 | all | 0 | 0 |
| Skyrim SE | 32,709 | 856,103 | 145 | all | 0 | 0 |
| Fallout 4 | 41,697 (235,082 all) | 805,148 | 114 | all | 0 | 0 |
| Fallout 76 | 63,305 (102,968 all) | 1,770,751 | 117 | all | 0 | 0 |
| Starfield | 31,058 (120,543 all) | 770,322 | 28 | all | 0 | 0 |

- No block type falls through to NiUnknown on any vanilla corpus, so there is no Oblivion cascade risk.
- Two RTTI-discriminator-in-a-field cases were re-checked: `BhkRigidBody.is_t` is set by the dispatch arm, not the parser; `NiPSysBlock.original_type` is preserved. The baseline harness keys on wire RTTI, and a header-only recount reproduces its numbers exactly.

---

## Findings

IDs are `NIF-D<dim>-2026-09-21-NN`. The dimension notes in `/tmp/audit/nif/dim_*.md` carry the full evidence.

### MEDIUM

#### NIF-D3-2026-09-21-01: The nightly real-data corpus lane (#3919) has never executed
- **Severity**: MEDIUM. A defence-in-depth gap over the HIGH-floor "NIF parse failure" class; same shape as SAFE-D4/D5-2026-09-21-01.
- **Dimension**: Block Dispatch Coverage (baseline harness)
- **Game Affected**: all (the gate covers every title)
- **Location**:
  - `.github/workflows/real-data-gates.yml:36-38`: the job-level `if: … inputs.game == matrix.game`.
  - `:39`: `runs-on: [self-hosted, linux, x64, byroredux-game-data]`.
  - `:44-74`: the matrix filters.
- **Status**: NEW. The fix for #3919 had two halves; part (a), `MIN_CLEAN_RATE`, is live, but part (b), the nightly lane, never took effect.
- **Description**:
  - GitHub evaluates `jobs.<id>.if` before it expands the matrix, and the `matrix` context is not available at that point. The workflow file therefore fails to parse.
  - GitHub registers it under its path instead of its `name:` (workflow id 351792019), and every push since `bd40e91af` (2026-09-06) creates a failed run with 0 jobs.
  - Separately, the repo has **no self-hosted runner** at all (`gh api repos/…/actions/runners` → `total_count: 0`).
  - Even once it runs, the per-title filter substrings miss two gates: `fallout_4` does not match `parse_rate_fo4_all_meshes`, and `fallout_76` does not match `parse_rate_fo76_all_meshes`.
  - The matrix has no `skyrim_le` row for the Skyrim LE gates added this window.
- **Evidence**:
  - `gh api …/workflows/351792019/runs`: 254 runs, all `event=push`, all `conclusion=failure`. There are 0 `schedule` runs and 0 `workflow_dispatch` runs.
  - For run 35658430021 (HEAD), `jobs.total_count` is 0 and `gh run view` reports "This run likely failed because of a workflow file issue".
- **Impact**:
  - Vanilla-content parse regressions are caught only by manual audits. That is the latency #3919 existed to remove; #3918's FO3 clean-rate collapse to 98.29% sat on `main` for two days.
  - It is already costing signal: two per-block baselines are red on the local installs with nobody notified (NIF-D3-2026-09-21-02).
- **Related**: #3919, #3918, #3850; SAFE-D4-2026-09-21-01 and SAFE-D5-2026-09-21-01 (other inert CI gates found today).
- **Suggested Fix**:
  - Move per-game selection into a step-level `if:`, or build the matrix from `inputs.game` with `fromJSON`.
  - Register a runner that carries the label, or point `runs-on` at an existing one.
  - Extend the filters to cover `fo4_all_meshes` and `fo76_all_meshes`, and add a `skyrim_le` row.

#### NIF-D4-2026-09-21-01: `sequence_emitter_rate` (tier d) is a whole-scene first-match; hundreds of vanilla particle systems get a sibling's authored birth rate
- **Severity**: MEDIUM. This is the NIFAL particle-emitter-params row: a translatable parameter silently misattributed, the same class as #4550.
- **Dimension**: Geometry Extraction & Import Handoff (particle emitters)
- **Game Affected**:
  - FO3 / FNV (`bsver` 34);
  - Skyrim LE / SE (`bsver` 83 / 100);
  - any title with manager-driven emitter blends.
- **Location**: `crates/nif/src/import/walk/emitter.rs`:
  - `:581-625`: `sequence_emitter_rate(scene, curves)`, which takes no system or controller argument;
  - `:693-700`: the call site inside `resolve`.
  - Per-system callers: `import/walk/mod.rs:666` and `emitter.rs:818`.
- **Status**: NEW. It is a residual of the first-match class closed by #1402 and #4261. Open #4560 (NIFAL-D5-2026-09-21-02) covers only the legacy `NiPSysEmitterCtlrData` tier below this one.
- **Description**:
  - When a system's own `NiPSysEmitterCtlr` interpolator is a manager-driven `NiBlendFloatInterpolator` with empty `items` (the #3329 shape), tier (d) scans **every** `NiControllerSequence`.
  - It returns the first controlled block whose `controller_type` contains "EmitterCtlr" in the best-ranked sequence, without checking which controller that block drives.
  - As a result, every system in a multi-emitter NIF receives the same rate.
  - The file does carry the per-system answer: each `ControlledBlock.controller_ref` points at exactly one emitter controller, whose `target_ref` is the system.
- **Evidence** (probe TIERD, which mirrors the rejected-curve pass):
  - **FNV**: 1,147 systems reach tier (d). **690 systems in 116 NIFs** get a rate different from their own controlled block's rate.
    - `fxravenrockexplosion01.nif`: all six systems get 60/s; their own rates are 30, 150, 30, 4.5, 90 and 30.
    - `dlc05zetadead.nif`: all systems get 18/s; own rates range from 1.5 to 300.
  - **FO3**: 712 systems reach tier (d); **386 in 99 NIFs** are misattributed.
    - `explosionlarge01.nif`: 900/s for all, against 60 / 7.5 / 90 / 30 / 60 / 21.4.
    - `vgeardoorr01.nif`: 30/s against 6–180.
  - **Skyrim SE**: 1,055 systems reach tier (d); **313 in 150 NIFs** are misattributed.
    - `dlc1vampchangeexplosion`: 180 against 450.
    - `sprigganfxtestunified`: 4.5 against 7.5–99.
  - The opt-in gate `real_archive_torch_meshes_surface_particle_emitters` passes, because it counts only *presence* of a positive rate.
- **Impact**: particle density in multi-emitter explosions, fire, magic, door and vehicle FX is wrong per system, by up to about 120×, on three titles.
- **Related**: #1402, #4261, #4467, #4550 (all closed); #4560 (open, legacy tier); #3329 (introduced tier d).
- **Suggested Fix**:
  - Pass the system's own emitter-controller block index into `sequence_emitter_rate`, and accept only controlled blocks whose `controller_ref` matches it.
  - For controlled blocks without a controller ref, fall back to matching `node_name` against the system's name. Without that fallback, the 47 FNV / 80 FO3 / 193 SSE systems that have no controlled block of their own would drop to the preset.
  - Add a multi-emitter fixture asserting distinct per-system rates.

#### NIF-D4-2026-09-21-02: FO4+/Starfield `BSSkin::BoneData` bind transforms bypass both the #277 rotation sanitizer and #4549's finite gate
- **Severity**: MEDIUM. A defence-in-depth gap on the skinning path; vanilla exposure is measured at zero.
- **Dimension**: Geometry Extraction & Import Handoff (skinning)
- **Game Affected**: Fallout 4 (`bsver` 130), Fallout 76 (155), Starfield (172+)
- **Location**:
  - `crates/nif/src/blocks/skin.rs:494-518`: `BsSkinBoneData::parse`, a raw 17-float slice per bone.
  - `crates/nif/src/import/mesh/skin.rs:677-688`: `bs_bone_to_inverse_matrix`.
  - Consumers: `skin.rs:258-271` (BSTriShape / `BSSkin::Instance`) and `:447-516` (Starfield `BSGeometry`).
- **Status**: NEW. This path is outside the coverage of #4549 (closed today, `3e798cacd`); related to #277.
- **Description**:
  - #4549 put the non-finite gate, and #277 put `sanitize_rotation`, inside `NifStream::read_ni_transform` and `read_ni_transform_struct`. Together those cover `NiAVObject` and the legacy `NiSkinData` bones.
  - `BSSkin::BoneData` never goes through either reader. Its rotation, translation and scale are sliced out of a bulk f32 array and turned into the bind-inverse matrix unchecked.
  - So a NaN, infinite or non-orthonormal bone reaches the bone palette, GPU skinning, and the skinned BLAS refit. That is the chain #4549 closed for static transforms.
- **Evidence** (probe): no vanilla bone is non-finite, and none is changed by `sanitize_rotation`.
  - FO4 vanilla + DLC: 166,323 bones.
  - FO4 including third-party: 267,328 bones.
  - Starfield: 288,208 bones (27 of them carry scale ≠ 1).
- **Impact**: one corrupt bone poisons every vertex it influences. NaN positions make BLAS triangles inactive, ±inf gives unbounded AABBs, and raster shows exploded meshes. The exposure is corrupt or mod content, which was #4549's premise too.
- **Related**: #4549, #4396/#4397 (animation side), #277.
- **Suggested Fix**:
  - Apply `sanitize_rotation` and `sanitize_transform_translation_and_scale` per bone in `BsSkinBoneData::parse`, the parse-time boundary #4549 chose.
  - Add a NaN-bone unit test.

#### NIF-D1-2026-09-21-03: BSTriShape's #621 data_size-derived stride override has no upper bound; a bogus `data_size` over-reads past the block and decodes garbage vertices
- **Severity**: MEDIUM. A consumed-byte mismatch that `block_size` reconciliation covers; the parsed geometry is still wrong.
- **Dimension**: Stream Position Integrity
- **Game Affected**:
  - the mechanism applies to every BSTriShape band (SSE 100, FO4 130, FO76 155);
  - it was observed only on FO4 third-party content.
  - Vanilla shows zero BSTriShape drift: FO4 235,082 NIFs, FO76 102,968, SSE 32,902.
- **Location**:
  - `crates/nif/src/blocks/tri_shape/bs_tri_shape.rs:500-560`: the derived stride, and the #2598 guard `safe_stride = derived_stride.filter(|&s| s >= min_needed)`.
  - `crates/nif/src/blocks/mod.rs:467`: `BsTriShape::parse(stream)` is called without `block_size`.
- **Status**: NEW. #621 introduced the override; #2598 bounded only the under-stride direction.
- **Description**:
  - When `data_size` disagrees with `vertex_desc`, the override divides `data_size` to derive a per-vertex stride. The only check is a lower bound.
  - A `data_size` that overstates the payload therefore produces a stride larger than the descriptor's, and the decode reads past the block.
  - The parser never sees `block_size`, so it cannot notice.
- **Evidence**: probe dump of `meshes\ss2c2\architecture\buildings\bld02addsinglewin01.nif` (`ss2_xpac_chapter2 - main.ba2`, bsver 130):
  ```
  #1 size=226 consumed=442 drift=-216 BSTriShape vdesc=0x0003b00005430206 stride=24 ntri=2 nvert=4 data_size=324 expected=108
  ```
  - The 226-byte block is exactly the 118 bytes of header fields plus the descriptor-stride payload (4 vertices × 24 B + 2 triangles × 6 B = 108 B).
  - The override derives (324 − 12) / 4 = 78 bytes per vertex, reads 216 bytes past the block, and returns `Ok` with vertices 1–3 and both triangles decoded from wrong offsets.
  - The sibling block #3 in the same file over-reads to EOF and falls to NiUnknown.
- **Impact**: silently garbage geometry, visible only in `drift_histogram` and never in the clean-rate gate. Widely installed mods are the realistic trigger.
- **Related**: #621, #2598, #359.
- **Suggested Fix**:
  - Pass `block_size` into `BsTriShape::parse`, as `parse_sub_index` already receives it, and reject a derived stride whose payload exceeds the block.
  - Otherwise, cap the override near the descriptor stride.
  - Pin the dumped 226-byte block as a fixture.

#### NIF-D6-2026-09-21-01: The #4157 amplification class survives on other fixed-size elements; worst is `BSSkin::BoneData` (68×), and #4156's new CInfo2014 path copied the loose pattern
- **Severity**: MEDIUM, the same class and rating as #4157.
- **Dimension**: Allocation Hygiene
- **Game Affected**: FO4 / FO76 / Starfield (`BSSkin::BoneData`, CInfo2014); every era for the other BlockRef lists.
- **Location**:
  - `crates/nif/src/blocks/skin.rs:496-499`: `allocate_vec(num_bones)` for a 68-byte `BsSkinBoneTrans`, *before* `read_f32_array(num_bones * 17)`.
  - `crates/nif/src/blocks/collision/rigid_body.rs:375` (new this window), and its siblings `:181` and `:266`.
  - Other loose fixed-size sites:
    - `skin.rs:45`, `:448` and `:453`;
    - `collision/shape_compound.rs:54`;
    - `controller/sequence.rs:20`, `:45` and `:456`;
    - `particle.rs:1164`;
    - `collision/compressed_mesh.rs:117`, `:126` and `:143`.
- **Status**: NEW. These are siblings of #4157 and #2523, both closed.
- **Description**:
  - `allocate_vec` bounds `count` at 1 byte per element and never goes through `check_alloc`, so the 256 MB `MAX_SINGLE_ALLOC_BYTES` cap does not apply (`stream.rs:278-291`). The function's own doc says to prefer `allocate_vec_sized` for scalar and array structs.
  - `BsSkinBoneData` reserves 68 bytes per claimed bone before the capped bulk read can reject a forged count. Simply reordering those two calls would close the gap.
  - The constraint-ref lists amplify only 4×, but the new CInfo2014 path diverges from its own siblings: `shape_compound.rs:171`, `shape_mesh.rs:44` and `controller/mod.rs:490/594/633` already use `allocate_vec_sized::<BlockRef>`.
- **Impact**: a crafted NIF can request up to 68 × its own size in capacity. Under Linux overcommit those pages stay untouched; it becomes an abort only when the request exceeds RAM + swap. So this is a hygiene regression rather than a live exploit.
- **Related**: #2523, #4157, #408, #3918.
- **Suggested Fix**:
  - Use `allocate_vec_sized::<BsSkinBoneTrans>`, or size the Vec from the flat array after it is read.
  - Use `allocate_vec_sized::<BlockRef>` for the rigid-body and other BlockRef lists.

#### NIF-D6-2026-09-21-02: `parse_havok_packfile` pre-sizes `Vec::with_capacity(num_sections)` straight from an on-disk u32
- **Severity**: MEDIUM.
  - No production caller exists today.
  - The special-rules row for untrusted packfile readers makes this **HIGH** as soon as a loader consumes `BhkSystemBinary` blobs.
- **Dimension**: Allocation Hygiene
- **Game Affected**: FO4 / FO76 / Starfield (`bhkPhysicsSystem` / `bhkRagdollSystem` → `BhkSystemBinary`)
- **Location**: `crates/nif/src/blocks/collision/havok_packfile.rs:389-396`
- **Status**: NEW. A sibling of #4155 in the same function; this is the #408 / #764 class.
- **Description**:
  - `num_sections` is read from the blob header and passed to `Vec::with_capacity` before the per-iteration bounds check.
  - `PackfileSection` is about 128 bytes (a String, 7 u32 fields and 3 Vecs). So `num_sections = 0xFFFF_FFFF` requests roughly 550 GB, and the allocation failure aborts the process. A 64-byte crafted blob is enough.
  - Today only `examples/havok_blob_recon.rs` and tests reach this code. That is the same reachability the baseline used to rate #4155 MEDIUM.
- **Suggested Fix**: bound `num_sections` by `(data.len() - SECTION_TABLE_START) / SECTION_HEADER_SIZE` before reserving (real packfiles carry 3 sections).

### LOW

| ID | Dim | Title | Location | Status |
|---|---|---|---|---|
| NIF-D1-2026-09-21-01 | 1 | Pre-Gamebryo BoundingVolume arm table is incomplete now that #4150 makes unlisted types a hard Err (details below) | `blocks/base.rs:318-367` | NEW (#4150 side effect) |
| NIF-D1-2026-09-21-02 | 1 | `is_havok_constraint_stub` / `stubbed_drift_histogram` are dead after #4212, and three doc sites still describe "~45 stub under-reads per actor" (details below) | `lib.rs:163-198, 341-355, 509-535`; `scene.rs:106-123` | NEW |
| NIF-D2-2026-09-21-01 | 2 | #4156's CInfo2014 branch is gated `bsver >= FALLOUT4`, but nif.xml uses it only at `#BS_FO4#` (== 130), and the code doc claims "derived purely from nif.xml" (details below) | `blocks/collision/rigid_body.rs:67-69, 300-331` | NEW |
| NIF-D3-2026-09-21-02 | 3 | `per_block_baseline_skyrim_se` and `_fallout_76` are red on the current installs from game-data drift, not parser loss (details below) | `tests/data/per_block_baselines/{skyrim_se,fallout_76}.tsv`; `tests/common/mod.rs:999-1033`; `parse_real_nifs.rs` FO76 list | NEW |
| NIF-D3-2026-09-21-03 | 3 | #4154's sibling sweep missed five diagnostics that still filter `.nif` only (details below) | `examples/{unknown_types,locate_unknowns,full_histogram,parse_sweep,d5_unk_ba2}.rs` | NEW |
| NIF-D3-2026-09-21-04 | 3 | audit-nif skill drift (details below) | `.claude/commands/audit-nif/SKILL.md` | NEW; owner tech-debt |
| NIF-D6-2026-09-21-03 | 6 | KFM `read_cstring` allocates `vec![0u8; len]` for any i32 length with no remaining-bytes or cap check; `kfm.rs` has no engine consumer, and the header / stream string readers both check first | `kfm.rs:766-778` | NEW |

**Details for the LOW findings:**

- **NIF-D1-2026-09-21-01** (pre-Gamebryo BoundingVolume arms):
  - `BASE_BV = 0xFFFFFFFF` is a nif.xml-legal value with no body (nif.xml:1279-1286, 2546-2553), and it now errors.
  - OpenMW's Morrowind reader (`components/nif/node.cpp:75-127`) also reads `LOZENGE_BV = 3`, which Gamebryo 3.2 reserves too.
  - OpenMW gates the half-space BV's 12-byte origin on version ≥ 4.2.1.0; the Rust reader always skips 28 bytes.
  - Only v ≤ 4.2.2.0 content reaches this code, and none of the seven target titles.
- **NIF-D1-2026-09-21-02** (dead constraint-stub telemetry):
  - The only type left on the stub list, `bhkGenericConstraint`, has no dispatch arm.
  - It therefore always becomes NiUnknown with an exact `block_size` skip, so the drift branch that consults the list can never run for it.
  - It is absent from nif.xml and from every corpus, and the histogram is empty on every corpus.
- **NIF-D2-2026-09-21-01** (CInfo2014 gate breadth):
  - At bsver 130 the layout is corpus-confirmed: 6 third-party blocks decode nif.xml's defaults with zero drift, where nifly's 39-byte FO4 tail would have drifted 13 bytes.
  - No content exists anywhere at bsver > 130.
- **NIF-D3-2026-09-21-02** (per-block baselines red from data drift):
  - **Skyrim SE**: `BSDynamicTriShape` 21140 → 21054. A parser-independent header recount shows an exact 1:1 swap with `BSTriShape`: +86, totals identical at 856,103. The archives were rewritten 2026-09-02, after the 2026-08-27 baseline.
  - **FO76**: three `NiPSys*Ctlr` types shrank. `SeventySix - Meshes.ba2` was rewritten 2026-09-20 (NIF count 58,469 → 63,305).
  - The comparator ignores the TSV's own `total=` header, so drift reads as "filter or dispatch loss?".
  - The FO76 all-meshes list names 16 `NNUpdateMain.ba2` archives the patched install no longer has, and misses `Startup.ba2` (12 NIFs).
- **NIF-D3-2026-09-21-03** (#4154 siblings):
  - The five examples skip `.bto` / `.btr`, so on FO76 `GeneratedMeshes02.ba2` (2,055 NIFs, all `.bto`) they report "0 NIFs".
- **NIF-D3-2026-09-21-04** (skill drift):
  - (a) Dim 3's first step diffs `nif_stats --tsv` against the baseline TSV. nif_stats' own #4265 doc says the two use different keys and must not be diffed.
  - (b) The Starfield BLSP tail is given as "38 B / {38, 42}"; measured it is {0, 30}.
  - (c) Phase 1 says to "check the latest nightly result"; there is none.
  - (d) The Dim 5 checklist says `bhkGenericConstraint`'s "drift is suppressed", but that path is unreachable.

---

## Baseline fixes and in-window changes verified (not re-reported)

- **#4148–#4165**, the full baseline set, all hold:
  - **Stream position / bounding volumes**:
    - #4148: bounding-volume depth cap of 32, with tests.
    - #4150: an unknown BV type now returns Err.
    - #4158: `skip_animation` overrun falls through to normal dispatch; `skip()` is atomic on error.
    - #4159: realignment is bounds-checked. In production the Err-branch fall-through reaches only truncation, because the size cache is Oblivion-only and `oblivion_skip_sizes` has no production caller.
  - **Version gates and constants**:
    - #4151: the Sky/Water flag gate is `!#BS_GTE_132#`.
    - #4152: `APPLY_MODE_STANDALONE_UNTIL`.
    - #4153: `MATERIAL_COMPACT_COLORS`.
    - #4160: the `OBLIVION` and `FO4_SHADER_GAP` doc changes.
    - #4161: `FO76_STARFIELD_BOUNDARY`.
  - **Collision**:
    - #4155: packfile offsets are widened before adding.
    - #4156: CInfo2014 matches nif.xml field for field (see NIF-D2-2026-09-21-01 for the gate).
    - #4163: `BhkPlaneShape` is counted in `CollisionAuthoringSummary::plane_shapes`.
    - #4164: `read_havok_material` is used.
  - **Tooling and docs**:
    - #4154: the `d5_coverage` and `block_coverage_baselines` filters now use `is_nif_entry`.
    - #4162: the dispatch count is 255 / 314.
  - **Allocation**:
    - #4157: `NiPixelData` uses `allocate_vec_sized`.
    - #4165: `read_pod_vec_from_cursor` is `#[must_use]`.
  - **#4149**: the Starfield tint-tail gate is in place but **corpus-inert**. All 483,837 Starfield BLSP blocks (120,543 NIFs) are shader_type 0, so the "no sample available" comment can now read "none exist in vanilla".
- **Other in-window changes**:
  - #4249: SSE particle-data gate `== 100`.
  - #4257: inline-type-name key is `V5_0_0_1`.
  - #4212: constraint decoders consume the same byte count as before; drift 0 on every corpus.
  - Shader split `eaa94b49d`: mechanically clean (a line-multiset diff shows only `use` / visibility changes), and the tail-guard scan covers all five new files.
  - `8c834e0be`: SSE skin index spaces. Packed indices are widened only. Triangles are global. The body-part gate is exact: all 26,962 SSE partitions carry a global buffer.
  - #4550 and #4467: particle budget and controller attribution are per-instance.
  - #4234 / #4238: every precombine LOD band is decoded, with masked attributes.
  - #4206: the tangent pre-size is bounded.
  - `pre_parse_cell` is still two-phase after #4207.
- **Existing items re-confirmed, not re-filed**:
  - SAFE-D2-2026-09-21-02: `read_pod_vec_from`'s SAFETY argument.
  - #4560: the legacy emitter tier.
  - #4562: a missed reference from the Havok rename.
  - #4440: the Starfield 99.98% doc staleness.

---

## Prioritized Fix Order

1. **NIF-D3-2026-09-21-01**: make the nightly corpus lane actually run: fix the YAML, provide a runner, widen the filters. Every other parse-rate guard depends on it.
2. **NIF-D3-2026-09-21-02**: confirm the SE and FO76 installs, then regenerate their baselines, so the gates can go green once the lane runs. Teach the comparator to name corpus drift.
3. **NIF-D4-2026-09-21-01**: key tier-(d) emitter rates per system. This is visible on three titles and is the largest vanilla-visible defect found.
4. **NIF-D1-2026-09-21-03**: add an upper bound to the BSTriShape stride override. Rendering-blocking for affected mod content.
5. **NIF-D4-2026-09-21-02** and **NIF-D6-2026-09-21-01**: both touch `BsSkinBoneData::parse`. Sanitize each bone and use `allocate_vec_sized` in the same change.
6. **NIF-D6-2026-09-21-02**: bound `num_sections` before `parse_havok_packfile` gains a production caller.
7. The LOW findings. NIF-D1-02 (dead stub telemetry), NIF-D3-03 (example filters), NIF-D3-04 (skill drift) and NIF-D6-03 (KFM string cap) are each a few lines. NIF-D2-01 and NIF-D1-01 need a decision more than code.

## Artifacts

- Per-dimension notes: `/tmp/audit/nif/dim_{1..6}.md`.
- Corpus logs: `/tmp/audit/nif/corpus_*.log`, `corpus_misc.log`, `corpus_allmeshes.log`.
- dhat logs: `/tmp/audit/nif/dhat_*.log`.
- Probe crate and outputs: `/tmp/audit/nif/probe/`, `/tmp/audit/nif/probe_*.txt`.

Suggested next step:

```
/audit-publish docs/audits/AUDIT_NIF_2026-09-21.md
```
