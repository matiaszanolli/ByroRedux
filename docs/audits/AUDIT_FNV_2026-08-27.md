# Fallout: New Vegas Compatibility Audit — 2026-08-27

**HEAD**: `969d81c8` · **Scope**: all 9 dimensions of `/audit-fnv`, run
single-threaded by one auditor (no sub-agents — nested-agent result relay is
unreliable in this project). FNV is the **reference title**; regressions here
outweigh the same defect on any other game.

**Method**: static analysis at HEAD, plus first-party real-data measurement —
a purpose-written Python BSA v104 reader and ESM/GRUP walker run over the
vanilla install at
`/mnt/data/SteamLibrary/steamapps/common/Fallout New Vegas/Data/` (182 177
archive entries across 20 BSAs; `FalloutNV.esm` walked record-by-record), and
the repo's own ignored real-data suites (`cargo test -p byroredux-plugin
--test parse_real_esm -- --ignored`, `cargo test -p byroredux-nif --test
parse_real_nifs -- --ignored`, `scripts/check-shader-artifacts.sh`).
**No engine process was launched** — the user may have their own instance
running — so Dimension 8's live-device rows are explicitly not measured.

**Delta since the last FNV audit** (`docs/audits/AUDIT_FNV_2026-08-26.md`,
HEAD `d6e16c90`): **48 commits**, 253 files, +24 869 / −1 450 lines.

**Dedup baseline**: `gh issue list --limit 300 --state all` (300 records, 87
open) at `/tmp/audit/issues.json`; both prior FNV reports
(`AUDIT_FNV_2026-08-24.md`, `AUDIT_FNV_2026-08-26.md`); and `git log`
`d6e16c90..HEAD`.

---

## Executive Summary

**7 findings: 0 CRITICAL · 3 HIGH · 2 MEDIUM · 2 LOW.**

The 2026-08-26 sweep's 43 findings have been worked hard: its CRITICAL and all
six HIGHs are fixed and **verified still fixed here** (see *Regression Guard
List*), and 25 of its MEDIUM/LOW findings are filed and open. The parse
baselines are unchanged and exact — `[FNV] total=77828`, 20 746 / 20 746 NIFs
clean at 100 %, `check-shader-artifacts.sh` now **green 22/22** where it
reported DRIFT at the last audit's HEAD.

This run's findings therefore come from two places the prior sweeps did not
reach:

1. **The exterior→interior seam.** `LoadedCellIndex` — the resource
   `queue_door_transition` needs to resolve a door's XTEL destination — is
   installed at exactly **one** call site, inside the *interior* loader. An
   FNV exterior boot (`--esm FalloutNV.esm --grid …`, the WastelandNV route)
   never installs it, so every door in the open world fails activation with
   `MissingCellIndex`. `4dcbd187`'s commit body noticed this in passing while
   testing #3323 and explicitly deferred filing it; nothing was filed.

2. **Gender is dropped on both FNV actor-appearance paths.** The engine picks
   an NPC's armor mesh and its race head mesh without consulting the actor's
   gender, on a title where the data authors both distinctly and where the
   *body* path beside them already gets it right:
   - **ARMO `MOD3`** (the female biped mesh) is parsed for `ARMA` records and
     for nothing else. `CommonNamedFields::from_subs` reads only `MODL`, and
     `resolve_armor_meshes`'s pre-Skyrim arm returns that one path while
     ignoring its own `gender` parameter. Measured: **213 of `FalloutNV.esm`'s
     389 ARMO records author a female mesh that differs from the male one**
     (`tuxedo_M.NIF` vs `tuxedo_F.NIF`, `mark2combat.NIF` vs
     `mark2combatf.NIF`, …), and NPC inventories reference ARMO 1 507 times
     directly plus 13 974 LVLI expansions.
   - **The race head** is chosen as `race.body_models.first()` — literally the
     first `MODL` in the RACE record, which is always the male head.
     Measured: **all 22 FNV races author a distinct female head**
     (`HeadOld.NIF` / `HeadOldFemale.NIF`, `HeadHuman.NIF` /
     `HeadFemale.NIF`), the correct path is *already parsed* into
     `RaceRecord::head_parts` carrying its `gender_section` tag, and it is
     never read. 987 of 3 816 FNV NPCs are female.

**What is healthy.** The FNV data pipeline held up under every check this
audit could make without a GPU. ESM record totals, per-category counts, NIF
parse rate, XCLL sizing, the SCOL-era dispatch gate, the WATR classifier's
recorded token decisions, `GameKind` routing for all ten FNV masters, the
seven opt-in AI-package env gates, and the whole `b78749af` / `6aa3d8f6` fix
batch are all verified correct against the shipped archives. The #3321
distant-object-LOD path that landed in this window is right on the data:
`object_lod_atlas_path`'s hardcoded `<world>.buildings.dds` is what all 355
FNV block quads actually name, across all 8 worldspaces, and all 16 atlas
textures resolve out of `Fallout - Textures2.bsa`.

### Excluded by instruction (concurrent audits already filed these)

The GRUP recursion depth bound (#3237, `crates/plugin/src/esm/cell/support.rs`),
the compressed-record zlib bomb (`crates/plugin/src/esm/reader.rs:630-647`),
and the SpeedTree `TREE.ICON` bare-filename resolution failure
(`crates/spt/src/import/mod.rs:137-155`) are not re-filed here.

---

## Baseline Comparison

Compared against the living floor tests and `ROADMAP.md`. HEAD is `969d81c8`.

| Metric | Baseline | Observed this audit | Verdict |
|---|---|---|---|
| ESM record total | `total=77828` (`parse_rate_fnv_esm`, living) | `total=77828` — exact | ✅ match |
| ESM items / NPCs / quests / dialogues / packages | 2 643 / 3 816 / 436 / 18 215 / 4 163 | identical on all five | ✅ match |
| NIF parse rate | 100 % clean / 100 % recoverable | 20 746 / 20 746 clean, 0 truncated, 0 failed across all 11 FNV archives | ✅ match |
| `scripts/check-shader-artifacts.sh` | RED at the 2026-08-26 audit's HEAD | **22/22 match glslang 11:16.2.0** | ✅ **fixed** |
| `cargo test -p byroredux-plugin --test parse_real_esm -- --ignored` | green | 21 passed / 0 failed | ✅ green |
| `cargo test -p byroredux-nif --test parse_real_nifs -- --ignored` | green | 12 passed / 0 failed | ✅ green |
| XCLL sizing | `XCLL_SIZES_FALLOUT_ERA = [28, 36, 40]` | every one of the 388 XCLL in `FalloutNV.esm` is exactly 40 B | ✅ canonical |
| FNV `_far.nif` / `distantlod\` census | 0 / 0 (#3321 premise) | 0 `_far.nif`, 0 `distantlod\` in 182 177 entries across 20 BSAs | ✅ confirmed |
| FNV object-LOD block quads | 295 wastelandnv level-4 (#3321) | 355 across 8 worldspaces in `Fallout - Meshes.bsa` (289 plain + 6 `.high.` for wastelandnv) | ✅ consistent |
| Prospector entity / draw / FPS / fence | ROADMAP row | **not observed — no engine launched** | ⚠️ not measured |

**No performance regression is claimed or refuted by this audit.** The
bench-of-record rows need a live device; the repro command is in *Not
investigated* below. The bench-staleness item (R6a-stale-20) is already folded
and is not re-filed.

---

## Findings

### FNV-2026-08-27-D1-01: `LoadedCellIndex` is installed only by the interior loader, so every door in an FNV exterior worldspace fails to transition

- **Severity**: HIGH
- **Dimension**: 1 — Cell Loading End-to-End
- **Location**: `byroredux/src/cell_loader/load.rs:568` (sole producer) ·
  `byroredux/src/cell_loader/transition.rs:271-281` (consumer) ·
  `byroredux/src/scene.rs:816-858` (the two boot arms)
- **Status**: NEW
- **Description**: `queue_door_transition` — the single producer used by both
  player activation (`interaction.rs:989`) and the `door.teleport` console
  command (`commands/scene.rs:451`) — resolves a door's XTEL destination REFR
  to its parent cell through the `LoadedCellIndex` World resource. That
  resource is inserted at exactly one site in the whole tree, and that site is
  inside `load_cell_with_masters`, the **interior** entry point. `scene.rs`'s
  exterior arm (`--esm <path> --grid <x>,<y>`, i.e. the WastelandNV route)
  drives `WorldStreamingState` instead and never calls it, so on an exterior
  boot the resource is absent for the whole session.
- **Evidence**: The only insert in the tree:

  ```
  $ grep -rn "insert_resource(super::LoadedCellIndex\|LoadedCellIndex(" byroredux/src/
  byroredux/src/cell_loader/index.rs:37:pub struct LoadedCellIndex(pub Arc<EsmCellIndex>);
  byroredux/src/cell_loader/load.rs:568:    world.insert_resource(super::LoadedCellIndex(std::sync::Arc::new(index.cells)));
  ```

  `byroredux/src/cell_loader/transition.rs:271-278`:

  ```rust
  let index = world
      .try_resource::<super::index::LoadedCellIndex>()
      .ok_or(QueueDoorTransitionError::MissingCellIndex)?;
  ```

  and its error text, `transition.rs:236-238`:

  ```rust
  Self::MissingCellIndex => write!(
      formatter,
      "no LoadedCellIndex resource; an ESM-driven cell load is required"
  ),
  ```

  Doors are attached on the **shared** REFR spawn path
  (`byroredux/src/cell_loader/spawn.rs:815-828` stamps `DoorTeleport` whenever
  a REFR carries an XTEL destination), so exterior doors do become interaction
  candidates (`interaction.rs:877-882`) and reach `activate_target`. The
  failure is swallowed as a `log::warn!` (`interaction.rs:998-1005`).

  Independently corroborated by `4dcbd187`'s own commit body: *"the
  exterior -> interior leg could not be driven, because `LoadedCellIndex` is
  installed only by `load_cell_with_masters` and never by the exterior
  streaming path, so activating an exterior door on a `--wrld/--grid` boot
  logs 'no LoadedCellIndex resource; an ESM-driven cell load is required' and
  no transition occurs."* It says the gap is *"worth its own issue"*; no issue
  exists.
- **Impact**: On FNV's reference open-world route the player can walk up to
  any building, saloon, vault or metro door and activate it, and nothing
  happens beyond a warning line. Every exterior→interior traversal on the
  reference title is unreachable, which also means the whole door-walk
  transition orchestrator (`transition.rs`), its cell-swap unload path, and
  `#3323`'s new interior window-portal sky lane are exercised only from an
  interior boot. `LoadedPluginSet` — the *other* prerequisite the same
  function needs — **is** inserted on both arms (`scene.rs:803-806`), so this
  is a single missing insert, not a design gap: the exterior context already
  holds the full parsed index as `wctx.record_index.cells`.
- **Related**: `4dcbd187` (#3323); `byroredux/src/cell_loader/transition.rs:653-672`
  (`queue_door_transition_reports_missing_prerequisites` already asserts the
  `MissingCellIndex` arm, so the error is tested but the production gap is not).
- **Suggested Fix**: Insert `LoadedCellIndex` from the exterior boot as well —
  the `EsmCellIndex` is already resident as `wctx.record_index.cells` and is
  `Arc`-shared, so it is a clone of an `Arc`, not a re-parse. Pin it with a
  test that asserts the resource is present after *both* boot arms, keyed on
  the boot path rather than on this one resource name.

---

### FNV-2026-08-27-D4-01: FO3/FNV ARMO `MOD3` (the female biped mesh) is never parsed — 213 of 389 FalloutNV.esm armors put the male mesh on every female wearer

- **Severity**: HIGH
- **Dimension**: 4 — ESM Record Parser (FNV data through it)
- **Location**: `crates/plugin/src/esm/records/common.rs:288` (only `MODL` is
  read) · `crates/plugin/src/equip.rs:169-178` (the pre-Skyrim arm ignores its
  own `gender` parameter) · consumed at
  `byroredux/src/npc_spawn.rs:803-807` and `:918-923`
- **Status**: NEW
- **Description**: On Oblivion / FO3 / FNV an ARMO carries **two** worn
  meshes: `MODL` (male biped) and `MOD3` (female biped). `MOD3` is parsed in
  exactly one place in the plugin crate — `parse_arma`
  (`crates/plugin/src/esm/records/misc/equipment.rs:120`) — which handles
  **ARMA** records, a Skyrim+ dispatch that FNV NPC inventories never
  reference. For ARMO, `CommonNamedFields::from_subs` captures `MODL` into
  `model_path` and silently drops `MOD3`, and `resolve_armor_meshes`'s legacy
  arm returns that single path regardless of gender.
- **Evidence**: `crates/plugin/src/esm/records/common.rs:285-289`:

  ```rust
  b"EDID" => out.editor_id = read_zstring(&sub.data),
  b"FULL" => out.full_name = read_lstring_or_zstring(&sub.data),
  b"MODL" => out.model_path = read_zstring(&sub.data),
  b"ICON" => out.icon_path = read_zstring(&sub.data),
  ```

  `crates/plugin/src/equip.rs:169-178` — note that `gender` is bound in the
  signature and never used on this branch:

  ```rust
  if !is_skyrim_or_later {
      // Oblivion / FO3 / FNV: ARMO MODL is the worn mesh. One record,
      // one mesh — no ARMA dispatch, so never more than one path.
      let path = armor.common.model_path.as_str();
      return if path.is_empty() {
          Vec::new()
      } else {
          vec![path]
      };
  }
  ```

  Direct census of `FalloutNV.esm` (independent Python GRUP walker, this
  audit): **389 ARMO records; 245 author a `MOD3`; 213 of those differ from
  the record's `MODL`.** Samples:

  | EDID | `MODL` (used) | `MOD3` (dropped) |
  |---|---|---|
  | `ArmorWhiteGloveSociety` | `armor\tuxedo\tuxedo_M.NIF` | `armor\tuxedo\tuxedo_F.NIF` |
  | `ArmorCombatReinforcedMark2` | `armor\combatarmor\m\mark2combat.NIF` | `armor\combatarmor\f\mark2combatf.NIF` |
  | `ChineseStealthArmor` | `dlcanch\armor\chinesestealtharmor\m\outfit.nif` | `dlcanch\armor\chinesestealtharmor\f\outfit.nif` |
  | `OutfitGeneralOliver` | `armor\GeneralOliver\GeneralOliverM.NIF` | `armor\GeneralOliver\GeneralOliverF.NIF` |
  | `NVPapaKhanArmor` | `armor\papakhan\papakhan.NIF` | `armor\papakhan\papakhan_f.NIF` |

  Reach: `FalloutNV.esm`'s NPC_/CREA `CNTO` lists resolve to `ARMO` 1 507
  times directly and to `LVLI` 13 974 times (whose leaves expand through
  `expand_leveled_form_id` into the same ARMO records). 987 of 3 816 `NPC_`
  records set the `ACBS` female bit.
- **Impact**: Every female FNV NPC wearing one of those 213 armors renders the
  male mesh — wrong silhouette, and in the `m\` / `f\` cases a mesh whose
  dismember partitions and UVs were authored against the male body. This is
  not a missing asset (`_F.NIF` is present in `Fallout - Meshes.bsa`) and not
  a renderer issue; it is one unparsed sub-record. It applies to Oblivion
  equally — the same branch serves both — but FNV is the reference title.
- **Related**: `#3357` (`e0d5ec18`) reworked the *Skyrim+* half of this exact
  function into a multi-mesh resolver two days ago and left the legacy arm
  untouched; `resolve_armor_meshes`' Skyrim path is already fully
  gender-aware via `ArmaRecord::{male_biped_model, female_biped_model}`.
- **Suggested Fix**: Parse `MOD3` on the ARMO path into a `female_model_path`
  beside `model_path` (the FO3/FNV `ItemKind::Armor` variant is the natural
  home, so `CommonNamedFields` stays game-neutral), then have
  `resolve_armor_meshes`'s pre-Skyrim arm select on `gender` and fall back to
  `MODL` when `MOD3` is empty (144 of 389 FNV ARMOs author none). Pin it with
  a real-data test over `FalloutNV.esm` asserting a female actor resolves
  `tuxedo_F.NIF`.

---

### FNV-2026-08-27-D4-02: the FNV race head mesh is `body_models.first()` — always the male head, on all 22 races, with the correct female path already parsed and tagged

- **Severity**: HIGH
- **Dimension**: 4 / 6 — ESM record semantics → NPC spawn
- **Location**: `byroredux/src/npc_spawn/resumable.rs:380-382`
- **Status**: NEW
- **Description**: The kf-era (Oblivion / FO3 / FNV) NPC spawner picks the
  head NIF as the first entry of `RaceRecord::body_models`, a flat
  append-ordered list of *every* `MODL` in the RACE record. In the shipped FNV
  layout the first `MODL` is always the male head, because the record opens
  `NAM0` (head-data marker) → `MNAM` (male section) → `INDX 0` → `MODL`. The
  female head follows in the `FNAM` section and is already captured — with a
  gender tag — in `RaceRecord::head_parts`, which the same function reads two
  statements later for the eyes. It is never consulted for the head.
- **Evidence**: `byroredux/src/npc_spawn/resumable.rs:380-382`:

  ```rust
  let head_path = race
      .and_then(|race| race.body_models.first())
      .map(|path| normalize_mesh_path(path).into_owned());
  ```

  Contrast the eye selection 34 lines below, which *does* honour the tag
  (`resumable.rs:411-421`):

  ```rust
  .filter(|(part_idx, path, section)| {
      (*part_idx == ...::head_part::LEFT_EYE
          || *part_idx == ...::head_part::RIGHT_EYE)
          && !path.is_empty()
          && section.is_none_or(|tag| tag == want_gender_tag)
  })
  ```

  Real sub-record sequence of `CaucasianOldAged` (`000987DF`), dumped from
  `FalloutNV.esm` by this audit:

  ```
  EDID  NAM0  MNAM  INDX:0  MODL:Characters\Head\HeadOld.NIF   ...
                    INDX:6  MODL:Characters\Head\EyeLeftHuman.NIF
                    INDX:7  MODL:Characters\Head\EyeRightHuman.NIF
              FNAM  INDX:0  MODL:Characters\Head\HeadOldFemale.NIF  ...
                    INDX:6  MODL:Characters\Head\EyeLeftHumanFemale.NIF
  ```

  Census over all `FalloutNV.esm` RACE records: **22 of 22 races author a
  female head that differs from the male head**, and `body_models[0]` is the
  male head in every one of them (`HeadOld.NIF` / `HeadOldFemale.NIF`,
  `HeadHuman.NIF` / `HeadFemale.NIF`). 987 of 3 816 FNV NPCs are female.
- **Impact**: Every female FNV NPC gets a male head. This is worse than a
  swapped mesh: the runtime-FaceGen path (`spawn_runtime_head`,
  `resumable.rs:852-930`) then applies that NPC's authored `FGGS`/`FGGA`
  morph deltas — sliders authored against the *female* base head — to the male
  base mesh through the `.egm` morph basis, so the resulting face is not the
  male head either. Every named female character on the reference title is
  affected.

  Secondary fragility worth fixing in the same pass: `body_models` is an
  ordering-dependent flat list that also contains the body-section meshes and
  the `.egt` texture paths (`Characters\_Male\UpperBodyHumanMale.egt` is
  `body_models[12]` on `CaucasianOldAged`). `first()` returning a head at all
  is incidental.
- **Related**: FNV-2026-08-27-D4-03 below (the same accumulator's other
  defect); `humanoid_body_paths` (`npc_spawn.rs:348-379`) is the *body*
  sibling and is fully gender-aware, which is why the asymmetry went
  unnoticed.
- **Suggested Fix**: Resolve the head from `race.head_parts` by
  `(head_part::HEAD, gender_section)` with the same
  `section.is_none_or(|tag| tag == want_gender_tag)` rule the eye filter
  already uses, falling back to the untagged/male entry. Add a real-data test
  over `FalloutNV.esm` asserting a female `Caucasian` actor resolves
  `HeadFemale.NIF`.

---

### FNV-2026-08-27-D4-03: `RaceRecord::head_parts` also absorbs the RACE *body* section, so `head_part::HEAD` / `MOUTH` / `TEETH_LOWER` collide with UpperBody / RightHand / an `.egt` path

- **Severity**: MEDIUM
- **Dimension**: 4 — ESM Record Parser
- **Location**: `crates/plugin/src/esm/records/actor/mod.rs:1403-1409`
  (accumulator) · `:436-458` (the field's contract) · `:533-541` (the
  constants)
- **Status**: NEW
- **Description**: The FNV RACE walker pairs every `INDX` with the next `MODL`
  and pushes it into `head_parts` unconditionally. But FO3/FNV RACE records
  re-use `INDX` — and re-use the `MNAM`/`FNAM` gender markers — for the
  **body** section that follows `NAM1`, with its own indices 0..3. The typed
  map documented as "FNV / FO3 `INDX` + `MODL` head-part pairs" therefore also
  contains body meshes and a texture path keyed under head-part indices.
- **Evidence**: `crates/plugin/src/esm/records/actor/mod.rs:1403-1409`:

  ```rust
  b"MODL" => {
      let path = read_zstring(&sub.data);
      if let Some(idx) = pending_indx.take() {
          record.head_parts.push((idx, path.clone(), gender_section));
      }
      record.body_models.push(path);
  }
  ```

  There is no `NAM1` arm anywhere in `parse_race` to close the head section
  (`grep -n 'b"NAM1"' crates/plugin/src/esm/records/actor/mod.rs` → no match).
  Real tail of `CaucasianOldAged`, after `NAM1`:

  ```
  NAM1  MNAM  INDX:0  MODL:characters\_Male\UpperBody.nif
                INDX:1  MODL:characters\_Male\LeftHand.nif
                INDX:2  MODL:characters\_Male\RightHand.nif
                INDX:3  MODL:Characters\_Male\UpperBodyHumanMale.egt
        FNAM  INDX:0  MODL:Characters\_Male\FemaleUpperBody.NIF
                ...
  ```

  So `head_parts` for that race contains `(0, "characters\_Male\UpperBody.nif",
  Some(0))` under `head_part::HEAD = 0`, `(2, "…\RightHand.nif", Some(0))`
  under `head_part::MOUTH = 2`, and `(3, "…UpperBodyHumanMale.egt", Some(0))`
  under `head_part::TEETH_LOWER = 3`.
- **Impact**: Latent today rather than live — the only consumer
  (`resumable.rs:414-421`) filters on `LEFT_EYE = 6` / `RIGHT_EYE = 7`, and
  the body section authors no `INDX` above 3, so no current lookup misfires.
  But `head_part::{HEAD, MOUTH, TEETH_LOWER, TEETH_UPPER, TONGUE}` are public
  constants sitting beside `LEFT_EYE`/`RIGHT_EYE` with a doc comment inviting
  exactly that lookup, and the fix for FNV-2026-08-27-D4-02 is the natural
  first consumer of `head_part::HEAD` — which would resolve to
  `UpperBody.nif` on a first-match search unless the contamination is removed
  first. These two findings must be scheduled together.
- **Related**: FNV-2026-08-27-D4-02; FNV-2026-08-27-D5-01.
- **Suggested Fix**: Track section state in `parse_race` — `NAM0` opens the
  head-data section, `NAM1` opens the body-data section — and push into
  `head_parts` only while inside the head section (`body_models` can keep
  taking everything, or gain a parallel `body_part_models`). Pin with a
  real-data assertion that no `head_parts` entry on any `FalloutNV.esm` RACE
  ends in `.egt` or lives under `characters\_male\`.

---

### FNV-2026-08-27-D6-01: FNV head sub-part meshes (mouth / teeth / tongue) are parsed, indexed and never spawned — only the eyes are consumed

- **Severity**: MEDIUM
- **Dimension**: 6 — Animation, Skinning & NPC assembly
- **Location**: `byroredux/src/npc_spawn/resumable.rs:411-424` (the only
  `head_parts` consumer) · `crates/plugin/src/esm/records/actor/mod.rs:533-541`
  (four dead constants) · `:452-456` (a doc claim the code does not meet)
- **Status**: NEW
- **Description**: `RaceRecord::head_parts` exists so the spawner can select
  head sub-meshes by semantic role, and the `head_part` module publishes
  `HEAD`, `MOUTH`, `TEETH_LOWER`, `TEETH_UPPER`, `TONGUE`, `LEFT_EYE`,
  `RIGHT_EYE`. Only `LEFT_EYE` and `RIGHT_EYE` are ever read; `MOUTH`,
  `TEETH_LOWER`, `TEETH_UPPER` and `TONGUE` have no reference anywhere outside
  their own definition.
- **Evidence**:

  ```
  $ grep -rn "head_part::" byroredux/src/ crates/ | grep -v 'mod.rs:53[0-9]'
  byroredux/src/npc_spawn/resumable.rs:416:  ... head_part::LEFT_EYE
  byroredux/src/npc_spawn/resumable.rs:417:  ... head_part::RIGHT_EYE
  ```

  ```
  $ grep -rn "\.head_parts" byroredux/src/ crates/plugin/src/ | grep -v _tests
  byroredux/src/npc_spawn/resumable.rs:400   # index.head_parts — the HDPT map, unrelated
  byroredux/src/npc_spawn/resumable.rs:414   # race.head_parts — eyes only
  crates/plugin/src/esm/records/actor/mod.rs:1406   # the push site
  ```

  Every FNV race authors the four meshes; `CaucasianOldAged` carries
  `MouthHuman.NIF`, `TeethLowerHuman.NIF`, `TeethUpperHuman.NIF`,
  `TongueHuman.NIF` in both gender sections. The field's own doc
  (`actor/mod.rs:452-456`) frames the pairing as the fix for *"Pre-fix every
  NPC rendered with just the head NIF — no eyes, mouth, teeth, tongue, ears"*,
  a claim only the eyes half of which is realised.
- **Impact**: FNV head meshes model the lips but not the oral cavity — the
  mouth interior, both teeth rows and the tongue are separate NIFs. Every FNV
  NPC therefore has an empty hole behind the lips, visible whenever the jaw
  opens (all dialogue, all idle talk animations) and at grazing angles when
  closed. The data is parsed and one `flat_map` away from the spawn list.
- **Related**: FNV-2026-08-27-D4-03 must land first — until the body section
  stops leaking in, `head_part::MOUTH` also matches `RightHand.nif`.
- **Suggested Fix**: Extend the `eye_paths` filter to the full head-sub-part
  set (mouth / teeth ×2 / tongue), keeping the same gender-tag rule, and spawn
  them alongside the eyes in `spawn_runtime_head`. If the omission is a
  deliberate deferral, say so at the constants and correct the `head_parts`
  doc, which currently reads as though it were done.

---

### FNV-2026-08-27-D5-01: `RaceRecord::head_parts`' doc mis-numbers the FNV head-part table and mis-states the gender tagging — the constants beside it are right

- **Severity**: LOW
- **Dimension**: 5 — NIF/ESM regression guard (doc rot)
- **Location**: `crates/plugin/src/esm/records/actor/mod.rs:436-457`
- **Status**: NEW
- **Description**: Two claims in the field's doc comment are falsified by the
  shipped data. The doc numbers the head parts `0 Head · 1 Ear (male) ·
  2 Ear (female) · 3 Mouth · 4 Teeth (lower) · 5 Teeth (upper) · 6 Tongue ·
  7 Left Eye · 8 Right Eye`, and states that `gender_section` is `None` for
  head entries because *"every 'Head' entry lands here"* — i.e. before any
  `MNAM`/`FNAM` marker.
- **Evidence**: Measured `INDX` values in `FalloutNV.esm`'s RACE head section
  are `0 Head · 1 Ear · 2 Mouth · 3 Teeth lower · 4 Teeth upper · 5 Tongue ·
  6 Left Eye · 7 Right Eye` — one lower than the doc from `Mouth` on. The
  `head_part` constants immediately below (`mod.rs:533-541`) already encode the
  **correct** numbering (`MOUTH = 2`, `LEFT_EYE = 6`, `RIGHT_EYE = 7`), so
  the runtime is right and only the prose is wrong. And the real record opens
  `NAM0` → **`MNAM`** → `INDX:0` → `MODL`, so the head entries carry
  `Some(0)` / `Some(1)`, never `None`.
- **Impact**: No runtime effect. It is a false premise sitting on the exact
  field the two findings above must be fixed through, and it reads as
  authoritative ("per UESP RACE_HeadPart"). An implementer trusting the prose
  over the constants would introduce the off-by-one the constants avoid.
- **Related**: FNV-2026-08-27-D4-02, -D4-03, -D6-01.
- **Suggested Fix**: Renumber the doc table to match the constants, and
  replace the `None` claim with what the archive shows (`None` occurs only for
  a record that authors `INDX`/`MODL` before its first `MNAM` — none of the 22
  FNV races do).

---

### FNV-2026-08-27-D1-02: the `/audit-fnv` skill's own Dimension-1 checklist still sends auditors after `_far.nif`, which FNV does not ship, and has no bullet for the object-LOD scheme it does

- **Severity**: LOW
- **Dimension**: audit infrastructure (doc rot)
- **Location**: `.claude/commands/audit-fnv/SKILL.md`, Dimension 1 checklist,
  final bullet
- **Status**: NEW
- **Description**: The checklist reads *"`_far.nif` distant-object LOD
  (#1726/#1745, Session 52) — verify the placement scheme + real LOD textures
  still resolve on FNV's WastelandNV exterior grid; entry points
  `cell_loader/object_lod.rs`, `cell_loader/placement_lod.rs`."* FNV ships no
  `_far.nif` and no `distantlod\` at all, and `placement_lod_supported` is
  Oblivion-only by construction, so the check can only ever confirm a no-op.
  Meanwhile the scheme FNV *does* ship —
  `ObjectLodScheme::FalloutLegacyBlocks`, landed 2026-08-27 under #3321 — has
  no checklist coverage.
- **Evidence**: Census over all 20 FNV BSAs (182 177 entries) this audit:
  **0 `_far.nif`, 0 `distantlod\` entries.** And
  `byroredux/src/cell_loader/placement_lod.rs:313-315`:

  ```rust
  pub(crate) fn placement_lod_supported(game: GameKind) -> bool {
      game == GameKind::Oblivion
  }
  ```

  pinned by `placement_lod_supported_is_oblivion_only` (`:754-761`), which
  asserts `!placement_lod_supported(GameKind::Fallout3NV)`. The live FNV
  scheme is `object_lod.rs:458-467` / `:491-497` / `:508-514`.
- **Impact**: Wasted auditor effort on a guaranteed-empty check, and — the
  costlier half — no standing instruction to verify the newest, least-reviewed
  FNV LOD code. Same class as FNV-2026-08-26-D8-01 (#3331), whose D8 repro
  command names archives a vanilla install does not have.
- **Related**: #3321 (`e23a9908`), #2086, #3331, `docs/engine/exal.md` §5.
- **Suggested Fix**: Replace the bullet with the `FalloutLegacyBlocks`
  checks that are actually verifiable — quad path shape
  (`meshes\landscape\lod\<world>\blocks\<world>.level<L>.x<qx>.y<qy>.nif`),
  the shared `<world>.buildings.dds` atlas resolving out of
  `Fallout - Textures2.bsa`, and the `LodBandLadder::for_object_game`
  legacy-ladder arm — and keep one line recording that `_far.nif` /
  `distantlod\` are confirmed absent on FNV so nobody re-derives it.

---

## Non-findings — checked this run, deliberately not filed

- **FNV `.high.` object-LOD quads.** `Fallout - Meshes.bsa` ships 6
  `wastelandnv.level4.high.x*.y*.nif` (and `LonesomeRoad - Main.bsa` 19 for
  `nvdlc04dividevistaworld`) that no path builder in
  `object_lod_archive_path` can name. Verified that **every one of them has a
  plain-named sibling at the same `(level, qx, qy)`** — 0 coordinates are
  reachable only through the `.high.` variant — so no content is lost; only an
  optional higher-detail tier is unreachable. Not a defect.
- **Object-LOD normal-map slot.** The FNV block quads name
  `<world>.buildings_n.dds` alongside the diffuse and the loader binds only
  the diffuse. Already recorded as a known follow-up **for both schemes** in
  `object_lod.rs:505-513`, not an FNV-specific gap.
- **WATR `potomac` / `spill` / `fountain` tokens.** Still absent from
  `water_kind_from_name`; confirmed this is a *recorded decision*, not a gap —
  `#3284` was closed with the 10-worldspace blast-radius reasoning and
  `rejected_tokens_stay_calm` pins it. Census reproduced: FNV authors 78 WATR,
  none with an `MNAM`, 5 classify as `River` via `creek`.
- **`#3357`'s ARMA rework on FNV.** `resolve_armor_meshes`' multi-mesh change
  is gated behind `is_skyrim_or_later`; the legacy arm is byte-identical to
  its pre-`e0d5ec18` shape. (Its *pre-existing* gender defect is
  FNV-2026-08-27-D4-01, not a `#3357` regression.)
- **`#2611`'s SCOL→PKIN recursion on FNV.** Guarded by
  `index.packins.contains_key(...)`, and PKIN is `is_fo4_plus`-gated at
  `records/mod.rs:318`, so `index.packins` is empty on every FNV load and the
  new arm is a proven no-op.
- **Disney-BSDF gate.** `MAT_FLAG_PBR_BSDF` is set only from
  `ImportedMaterial::is_pbr`, whose sole production writers are the
  BGSM/BGEM/`.mat` merge sites in `byroredux/src/asset_provider/material.rs`
  (`:1074`, `:1240`, `:1623`). Unreachable on FNV. Gate holds.
- **XCLL decode.** All 388 `FalloutNV.esm` XCLL payloads are exactly 40 B, and
  the reader's field order (3×RGBA, fog near, fog far, rot XY i32, rot Z i32)
  matches the authored layout byte-for-byte.
- **`GameKind` routing for the FNV DLC set.** All ten shipped masters
  (`FalloutNV.esm` HEDR 1.34, the six DLC at 1.32–1.34, the four packs at
  1.32) fall through `from_header`'s bands to `Fallout3NV`. No misroute.
- **FNV `_male` body table.** `humanoid_body_paths`' hardcoded FNV entries were
  checked against every RACE record's `NAM1` body section: all 22 races author
  exactly `characters\_Male\UpperBody.nif` / `FemaleUpperBody.NIF` /
  `Child*UpperBody.NIF` and the matching hands, including `Ghoul`. The
  hardcoded table is correct.
- **ARMA in FNV NPC inventories.** Censused every `NPC_`/`CREA` `CNTO` target
  in `FalloutNV.esm` (5 394 actors): `LVLI` 13 974, `ARMO` 1 507, `WEAP` 738,
  `AMMO` 514, `MISC` 486, `ALCH` 339, `KEYM` 139, `NOTE` 31, `LIGH` 3,
  `CMNY` 3 — **zero ARMA**. The Skyrim-shaped ARMA equip path is genuinely
  unreachable on FNV, so FNV-2026-08-27-D4-01's fix must land on the ARMO arm.

## Already tracked — cited, not re-filed

The 2026-08-26 report's still-open MEDIUM/LOW findings all reproduce at HEAD
and are filed: #3327, #3328, #3329, #3330, #3331, #3332, #3333, #3334, #3335,
#3336, #3337, #3338, #3339, #3340, #3341, #3342, #3343, #3344, #3345, #3346,
#3347, #3348, #3349, #3350, #3351, #3352, #3353, #3354. Also live and not
re-filed: #3255 / #3256 (`NavPath` teardown + navmesh-residency invalidation —
both became *materially* live rather than theoretical when `6aa3d8f6`
registered `NavPath` for the first time), #3237 (GRUP recursion depth),
#3390 (CREA derives no ActorValues), and R6a-stale-20 (bench staleness).

---

## Regression Guard List — verified still correct at `969d81c8`

`b78749af` (the four HIGH+ fixes this audit was asked to re-verify):

| Fix | Guard checked | Result |
|---|---|---|
| #3313 / #3322 — stale SPIR-V | `scripts/check-shader-artifacts.sh` | ✅ `22 shader artifacts match glslang 11:16.2.0` (was DRIFT on `water.frag.spv` + `composite.frag.spv`) |
| #3314 — sub-record FormID remap | `crates/plugin/src/esm/cell/helpers.rs:29-43` | ✅ `read_form_id`/`read_form_id_array` both **take `&EsmReader`** and `.map(\|raw\| reader.remap_form_id(raw))`; the raw `u32::from_le_bytes` sites left in `walkers.rs:343` / `wrld.rs:516` (XPRI) feed `reader.remap_form_id(fid)` on the next line, and `walkers.rs:301-330` / `wrld.rs:477-500` (XCRI) are mesh *hashes*, not FormIDs |
| #3315 — PBR skin classifier | `crates/core/src/ecs/components/material.rs:897-899` | ✅ `contains_any_ci(path, &["skin"]) \|\| (family && contains_any_ci(path, &["body","head","hand","face"]))`; the `1handpistol` / `2handrifle` / `1handmelee` corpus test at `:1349-1391` is present |
| #3316 — posed transform channels | `crates/nif/src/anim/transform.rs:46-66`, `:101` | ✅ null `data_ref` falls back to `constant_transform_channel(&interp.transform)` |

`6aa3d8f6` (the three follow-on HIGHs):

| Fix | Guard checked | Result |
|---|---|---|
| #3317 — capsule midpoint + axis | `crates/nif/src/import/collision/shape.rs:730-780` | ✅ `segment_shape` present; both the capsule (`:160`) and cylinder (`:172`) arms route through it. Consumer chain re-verified: `ragdoll_dynamic_shape` recurses into `Compound` (`crates/physics/src/ragdoll.rs:210-214`) and `flatten_to_parts` scales the child translation identically to the primitive (`crates/physics/src/convert.rs:211-214`), so the new offset is neither dropped nor double-scaled |
| #3318 — `bhkRigidBodyT` is node-local | `byroredux/src/ragdoll.rs` `template_from_imported` | ✅ `(b.translation, b.rotation)` used verbatim under `if b.is_t`; #2447's non-T zero-offset guard retained |
| #3319 — `NavPath` registered | `byroredux/src/boot.rs:667` | ✅ `world.register::<crate::components::NavPath>();` in `build_world`, with the general "every AI component written through a `query_mut` guard is pre-registered" invariant test at `:1975-2045` |

Older guards re-checked against real FNV data:

- **SCOL is FNV-era** — `records/mod.rs:254` `let is_scol_era = is_fo4_plus || matches!(game, GameKind::Fallout3NV);` and `:303` `b"SCOL" if is_scol_era => parse_scol_group(...)`. ✅ still dispatching for FNV.
- **PKIN / MOVS / MSWP stay FO4+** — `records/mod.rs:318-327`. ✅
- **`XCLL_SIZES_FALLOUT_ERA`** — `cell/walkers.rs:53`. ✅ matches the archive exactly (388/388 at 40 B).
- **ESM parse rate + category counts** — `parse_rate_fnv_esm` ✅ exact.
- **NIF parse rate** — `parse_rate_fallout_nv` ✅ 20 746/20 746 clean.
- **`vanilla_archives_have_zero_nisequencestreamhelper`** — ✅ 0 across 47 934 files.
- **AI-package opt-in gates** — all seven `BYRO_*` env checks still present in `boot.rs:1131-1215`; none registered unconditionally. ✅
- **`placement_lod_supported` is Oblivion-only** — ✅ (`placement_lod.rs:313`).
- **FNV terrain-LOD texture translation** — `env_translate.rs:130-142` builds `textures\landscape\lod\<w>\{diffuse,normals}\<w>.n.level<L>.x<qx>.y<qy>.dds`; ✅ 1 360 diffuse + 1 360 normals present for `wastelandnv`, naming matches character-for-character.
- **FNV object-LOD atlas** — `object_lod.rs:508-514`'s `<w>.buildings.dds`; ✅ matches the `BSShaderTextureSet` string in all 355 block NIFs across all 8 worldspaces, and all 16 atlas files (diffuse + `_n`) are present in `Fallout - Textures2.bsa`.
- **`#3385` LOD probe memoisation** — `lod_terrain_available` / `lod_object_available` cleared with the rings in `streaming_helpers.rs:410-411`; ✅ no cross-worldspace staleness.

---

## Not investigated (needs a live device)

Dimension 8's bench-of-record rows. The FNV interior repro, with the CWD
hazard and the `Fallout - ` archive-name correction the skill's own command
still gets wrong (#3331), is:

```bash
cd "/mnt/data/SteamLibrary/steamapps/common/Fallout New Vegas/Data"
cargo run --release --manifest-path /mnt/data/src/gamebyro-redux/Cargo.toml -- \
  --esm FalloutNV.esm --cell GSProspectorSaloonInterior \
  --bsa "Fallout - Meshes.bsa" --textures-bsa "Fallout - Textures.bsa" \
  --bench-frames 300 --bench-hold
# then, in another shell: cargo run -p byro-dbg  →  stats / tex.missing
```

Exterior: `--wrld WastelandNV --grid 0,0 --radius 3` — and that route is now
also the one to use to confirm FNV-2026-08-27-D1-01 (walk to any building
door, activate, and read the `door queue failed: no LoadedCellIndex resource`
warning).

---

## Suggested Next Step

```
/audit-publish docs/audits/AUDIT_FNV_2026-08-27.md
```

Label every finding `game:fnv` + `legacy-compat`, plus its own domain label
(`esm-plugin` for D4-01/-02/-03 and D5-01, `character` for D6-01,
`terrain-exterior` for D1-01 and D1-02, and `doc-rot` / `tech-debt` for the
two LOWs).
