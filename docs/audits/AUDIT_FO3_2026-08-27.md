# Fallout 3 Compatibility Audit — 2026-08-27

**Architecture**: single-auditor sweep, no sub-agents (explicit run constraint — nested-agent
relay is unreliable in this project). All 7 skill dimensions executed directly against the tree
at HEAD `969d81c8` (152 commits after the 2026-08-24 sweep's `048a8bd8`).

**Method**: static reading plus **live measurement**. Every quantitative claim below was
produced this run, either by executing the checked-in `--ignored` real-data gates or by
throwaway probes (`crates/nif/examples/_tmp_fo3_0828_*.rs`,
`crates/plugin/examples/_tmp_fo3_0828_*.rs`, raw-Python GRUP walkers) that were **deleted
after use** — nothing was left in the tree. No engine instance was launched.

```
cargo test -p byroredux-nif    --release --test parse_real_nifs -- --ignored parse_rate_fallout_3   → ok
cargo test -p byroredux-plugin --release --test parse_real_esm  -- --ignored parse_rate_fo3_esm     → ok
cargo test -p byroredux-nif    --release --test parse_real_nifs -- --ignored real_archive_torch_meshes_surface_particle_emitters → ok (now reaches FO3)
```

**Scope discipline**: a concurrent `/audit-fnv` run covers the shared FO3/FNV path. Findings
below are FO3-specific or are places where the *shared* code carries an assumption FO3
falsifies. Shared-path defects are cross-referenced, not re-filed. Per the run constraint, the
GRUP recursion-depth bound (#3237), the compressed-record zlib bomb
(`crates/plugin/src/esm/reader.rs:630-647`) and the SpeedTree `TREE.ICON` resolution failure
are deliberately excluded.

---

## Executive Summary

FO3 remains the best-behaved of the pre-Skyrim titles. Every hard gate this run came back
green and *better* than the documented baseline:

| Signal | Measured this run | Prior claim |
|---|---|---|
| NIF parse rate | **100.00 % over 17 172 NIFs across all 6 vanilla archives** | ROADMAP: 100 % (10 989) |
| NIF dispatch coverage | **0 distinct `NiUnknown` block types** across all 6 archives | — |
| ESM | `index.total() = 44 666`; SCPT 1 257, CREA 533, LVLC 60, SCOL 54, TREE 9 (9/9 `.spt`) | matches |
| Interior baseline | MegatonPlayerHouse = **929 REFRs** | 929 ✓ |
| Interior CELL coverage | 421 indexed / 421 authored interior CELLs (the 31-record delta against a naive GRUP count is the per-worldspace persistent CELLs, correctly routed) | — |
| Collision | **19 229 `Classic` chains, 0 extraction failures**, 77 phantom (`bhkSPCollisionObject` 69 + `bhkAabbPhantom` 8) | matches |
| DDS | 12 261 entries, **0 extract/header failures** (DXT1 8571 / DXT3 717 / DXT5 2951 / 22 uncompressed-RGB, all handled) | — |
| Object LOD | 422 baked quads across 15 worldspaces; atlas `<world>.buildings.dds` present for **all 15** | #3321 (new) |

Two things stood out.

1. **The FO3 corpus is the first to exercise a level-8-only object-LOD worldspace.** Seven of
   FO3's fifteen object-LOD worldspaces bake `blocks\` quads *only* at level 8; FNV bakes only
   level 4 everywhere. `select_lod_quads` subdivides on a missing asset but never coarsens, so
   on those seven worldspaces the near band resolves to level-4 quads that do not exist. The
   engine's *default* `--radius 12` happens to mask it; every smaller radius does not.
2. **The `WorldspaceRecord` DATA flag bit map documented in the ESM parser is the TES5 list
   shifted one bit down** — wrong for FO3's TES4-family layout *and* wrong for TES5. The field
   is parsed and has no consumer today, so this is a live trap rather than a live bug.

No CRITICAL or HIGH findings. **2 MEDIUM, 5 LOW.**

| # | Dimension | New | Notes |
|---|-----------|-----|-------|
| 1 | Rendering — inline shaders | 1 LOW | `refraction_strength` is the one field #2328's precedence fix skipped; measured unreachable on vanilla FO3 |
| 2 | NIF parser — FO3 block subset | 0 | 0 unknown block types over 17 172 NIFs; `fo3nv_f1`/`fo3nv_f2` re-verified field-by-field against `nif.xml`; prior LOW closed by #3286 |
| 3 | ESM record coverage | 1 MEDIUM + 2 LOW | WRLD DATA flag doc; XCLL size-comment; skill's XATO premise |
| 4 | Cell loading end-to-end | 1 MEDIUM + 1 LOW | Object-LOD level-8-only worldspaces; feature-matrix `_far.nif` attribution |
| 5 | Collision import | 0 | 19 229/19 229 classic chains extract; `hkSubPartData` (#2550) verified fixed |
| 6 | BSA v104 + real data | 1 LOW | ROADMAP compat cell cites the pre-#3041 single-archive corpus |
| 7 | Animation / NPC spawn + scripting | 0 | idle/sit KF paths verified present; 1 257-SCPT gap unchanged (known blocker) |

---

## Dimension Findings

### Dimension 4: FO3 Cell Loading

#### FO3-2026-08-27-D4-01: the object-LOD descent only subdivides, so FO3's seven level-8-only worldspaces have no distant objects in the near band
- **Severity**: MEDIUM
- **Dimension**: 4 — Cell loading end-to-end (distant object LOD)
- **Location**: `byroredux/src/cell_loader/lod_bands.rs:278-330` (`select_lod_quads`),
  `byroredux/src/cell_loader/object_lod.rs:216-222` (missing-quad sentinel),
  `byroredux/src/cell_loader/lod_bands.rs:119-120` (`FALLOUT_LEGACY_REFINE_BU` / `FALLOUT_LEGACY_MAX_CELLS`)
- **Status**: NEW (the FO3 half of the surface #3321 opened; not covered by #3203, which is
  fixed — `LodBandLadder::for_object_game` now returns `fallout_legacy()` for `Fallout3NV`)
- **Description**: `select_lod_quads` has exactly one escape for a quad whose baked asset is
  absent — descend:

  ```rust
  //   * it is nearer than its refine threshold (with hysteresis), or
  //   * the game ships no baked asset for it (`available` is false), in which
  //     case descending is what keeps a hole out of the horizon — the finest
  //     level always emits, where the caller's own fallback (synthesized
  //     terrain, or an empty sentinel for objects) takes over.
  ...
              if too_near || !available(level, qx, qy) {
                  let half = level / 2;
                  stack.push((half, qx, qy));
  ```

  That reasoning holds for **terrain**, where the caller synthesizes a heightmap quad when the
  baked one is missing. It does not hold for **objects**: `stream_object_lod_blocks`'s fallback
  is `ObjectLodBlock::empty()` — a "nothing here" sentinel:

  ```rust
              None => {
                  // No baked mesh for this quad — remember so we don't
                  // re-extract on every boundary crossing.
                  blocks.insert((level, qx, qy), ObjectLodBlock::empty());
              }
  ```

  FNV never exposes this because every FNV worldspace bakes its `blocks\` quads at level 4 —
  the finest level, which always emits. **FO3 does not.** Archive census of
  `Fallout - Meshes.bsa` (`meshes\landscape\lod\<world>\blocks\<world>.level<L>.*.nif`,
  measured this run):

  ```
  world           terrain levels                    object levels
  dcworld01       {}                                {8: 3}
  dcworld03       {4: 64, 8: 16}                    {8: 4}
  dcworld06       {}                                {8: 6}
  dcworld12       {}                                {8: 8}
  dcworld17       {}                                {8: 6}
  paradisefalls   {4: 16}                           {8: 1}
  washmontop      {4: 112, 8: 28, 16: 7}            {8: 65}
  ---- (the other eight worldspaces, incl. wasteland 276 quads, are level-4-only) ----
  ```

  Seven of fifteen worldspaces — **93 of FO3's 422 baked object quads, including all 65 of
  `WashMonTop`'s** — sit at level 8 with no level-4 sibling.
- **Evidence**: driving `select_lod_quads` with the FO3 ladder and an availability predicate
  that admits level 8 only (temporary probe test, since reverted):

  ```
  coarsest=32 max_cells=64
  refine_threshold(8) = Some(12)   refine_threshold(16) = Some(18)   refine_threshold(32) = Some(27)
  exclude_within=4 : by_level={4: 55, 8: 105}  nearest={4: 5,  8: 16}
  exclude_within=6 : by_level={4: 48, 8: 105}  nearest={4: 8,  8: 16}
  exclude_within=13: by_level={8: 105}         nearest={8: 16}
  ```

  At `exclude_within = 4` (i.e. `--radius 3`, the radius the exterior smoke/bench recipes use)
  the descent requests **55 level-4 quads spanning cells 5..15**, every one of which is absent
  on these worldspaces and therefore becomes an `empty()` sentinel; the nearest quad that can
  actually draw is the level-8 one at 16 cells. The 12-cell figure is
  `cells_from_bu(FALLOUT_LEGACY_REFINE_BU[0] = 50 000) = 12`.
- **Impact**: on `WashMonTop`, `ParadiseFalls` and the five level-8-only `DCworld*` worldspaces,
  every baked distant building disappears from the ~5..15-cell band whenever the streaming
  radius is below 12 — the visually dominant middle distance, right where the full-detail ring
  ends. The default `--radius 12` (`byroredux/src/scene.rs`, `.unwrap_or(12)`) sets
  `exclude_within = radius_unload = 13 > 12`, so the level-4 request never survives the
  `d > exclude_within` gate and the hole closes. That coupling is accidental and undocumented:
  nothing ties the default radius to `FALLOUT_LEGACY_REFINE_BU[0]`, and lowering either
  re-opens the hole silently.
- **Related**: #3321 (which wired the scheme and did the FNV census — the FO3 per-level split
  was not part of it). Distinct from #3203 (closed). The residual 2-cell gap at
  `exclude_within = 13` (nothing between 14 and 15 cells) follows from the deliberate
  whole-quad containment rule of #1866/#1871 and is cross-game, not an FO3 issue.
- **Suggested Fix**: give the object arm a coarsen fallback the terrain arm does not need —
  when a subdivision produces only unavailable children, emit the parent quad if *it* is
  available, instead of descending into sentinels. A cheaper stopgap is to derive the descent's
  finest level per worldspace from the archive inventory (the availability probe is already
  memoised per `(level, qx, qy)` since #3385) so a level-8-only worldspace never asks for
  level 4 at all.

#### FO3-2026-08-27-D4-02: `docs/feature-matrix.md` attributes `_far.nif` placement LOD to FO3 and calls multi-band selection deferred; both are contradicted by the code
- **Severity**: LOW
- **Dimension**: 4 — Cell loading (doc-rot)
- **Location**: `docs/feature-matrix.md:54`
- **Status**: NEW
- **Description**: the row reads

  ```
  | **Terrain LOD (M35)** | ~ Partial | `.btr` (Skyrim+/FO4) + `.bto` + `_far.nif` (Oblivion/FO3/FNV) shipped; distance-based multi-band selection + `.btr` normal maps deferred |
  ```

  Two claims are false for FO3 at HEAD. (a) `_far.nif` placement LOD is **Oblivion-only**:
  `byroredux/src/cell_loader/placement_lod.rs::placement_lod_supported` gates on
  `GameKind::Oblivion` alone (#2086), and `object_lod.rs`'s own module doc records that FO3
  ships 2 `_far.nif` and **zero** `distantlod\*.lod` — so the scheme is a documented no-op for
  FO3. (b) "distance-based multi-band selection … deferred" was made false by #2371: FO3 runs
  the four-level quadtree ladder (`LodBandLadder::for_terrain_game` /
  `for_object_game` → `fallout_legacy()`), verified above to emit levels 4 and 8.
- **Impact**: doc-only, but it is the row an auditor or contributor reads to decide whether FO3
  distant-object work is already done, and it points at the wrong module. It also mis-credits
  the `_far.nif` scheme to a title that ships none, which is exactly the premise #2086/#3321
  spent two cycles correcting inside the code.
- **Related**: #3321, #2086 (both closed); `_audit-common.md` already flags feature-matrix lag
  generally, but this row is a specific wrong attribution rather than a stale status.
- **Suggested Fix**: split the row: `.btr`/`.bto` for Skyrim+/FO4, `landscape\lod` quadtree
  (terrain + `blocks\` objects) for FO3/FNV, `DistantLOD\*.lod` + `_far.nif` for Oblivion only;
  drop the "multi-band selection deferred" clause.

### Dimension 3: ESM Record Coverage (Fallout3.esm)

#### FO3-2026-08-27-D3-01: the documented `WRLD.DATA` flag bit map is the TES5 list shifted one bit down — wrong for FO3's TES4 layout, and wrong for TES5 too
- **Severity**: MEDIUM
- **Dimension**: 3 — ESM record coverage (wire-layout contract)
- **Location**: `crates/plugin/src/esm/cell/mod.rs:911-913` (doc), read at
  `crates/plugin/src/esm/cell/wrld.rs:188-190` (`record.flags = sub.data[0]`)
- **Status**: NEW
- **Description**: the `WorldspaceRecord` wire-layout docblock states:

  ```rust
  /// - `DATA` — worldspace flags byte (u8): 0x01 small-world, 0x02
  ///   can't fast travel, 0x04 no LOD water, 0x08 no landscape, 0x10
  ///   no sky, 0x20 fixed dimensions, 0x40 no grass.
  ```

  The same docblock names OpenMW `components/esm4/loadwrld.cpp` as its reference. That
  reference (`/mnt/data/src/reference/openmw/components/esm4/loadwrld.hpp:46-56`) gives two
  *different* columns:

  ```cpp
  enum WorldFlags     // TES4                 TES5
  {
      WLD_Small          = 0x01,  // Small World          Small World
      WLD_NoFastTravel   = 0x02,  // Can't Fast Travel    Can't Fast Travel
      WLD_Oblivion       = 0x04,  // Oblivion worldspace
      WLD_NoLODWater     = 0x08,  //                      No LOD Water
      WLD_NoLandscpe     = 0x10,  // No LOD Water         No Landscape
      WLD_NoSky          = 0x20,  //                      No Sky
      wLD_FixedDimension = 0x40,  //                      Fixed Dimensions
      WLD_NoGrass        = 0x80   //                      No Grass
  };
  ```

  The committed comment is the **TES5** name list assigned to bit positions one step *below*
  their TES5 values. Every flag from `No LOD Water` upward is therefore documented at the wrong
  bit for both families, and for FO3 — which is TES4-layout — the errors are semantic, not just
  positional: FO3's `0x04` is *Oblivion worldspace*, and its `0x10` is *No LOD Water*, not
  "no sky".
- **Evidence**: the mislabelling is not academic on real FO3 data. Live WRLD census of
  `Fallout3.esm` (32 worldspaces):

  ```
      MegatonWorld  flags=0x51   →  documented: "no grass + no sky + small world"
                                →  TES4 (OpenMW): bit6 + No LOD Water + Small World
       CitadelWorld flags=0x11 · MonumentWorld 0x11 · WashMonTop 0x11 · tLandscape 0x11
       ParadiseFalls 0x13 · TranquilityLane 0x13 · StatesmanRoofWorld 0x03 · Wasteland 0x00
  ```

  Reading MegatonWorld as "no sky" is plainly wrong — Megaton's exterior renders a sky. Under
  the TES4 column the same byte reads as "No LOD Water", which matches a crater settlement with
  no distant water plane.
- **Impact**: zero today — `WorldspaceRecord::flags` is parsed and **no engine code reads it**
  (grep over `byroredux/src` and `crates/`: only the parser writes it). That is precisely why
  this is worth filing now: the first consumer to land — an EXAL sky/landscape/LOD-water gate
  is the obvious candidate, and `0x10` is the bit such a gate would reach for — will build on a
  wrong bit map and silently suppress the wrong subsystem on 9 of FO3's 32 worldspaces. The
  cost of fixing a wire-layout comment now is a one-line edit; the cost after a consumer exists
  is a per-game rendering bug that looks like a renderer defect.
- **Related**: same class as #3101 / #1887 — a per-game premise recorded in a comment that real
  data falsifies. Owner-audit note: the *mechanism* (`b"DATA"` dispatch) is `/audit-esm`'s; the
  per-game *semantics* is this audit's remit, which is why it is filed here.
- **Suggested Fix**: replace the single list with the two columns OpenMW carries, keyed by
  `EsmVariant`/`GameKind`, and say explicitly that no consumer exists yet so the next one
  reaches for the right column.

#### FO3-2026-08-27-D3-02: the `XCLL_SIZES_FALLOUT_ERA` comment inverts the FO3/FNV split — FO3 ships 40-byte XCLL 96 % of the time
- **Severity**: LOW
- **Dimension**: 3 — ESM record coverage (doc-rot on a wire-layout constant)
- **Location**: `crates/plugin/src/esm/cell/walkers.rs:48-53`
- **Status**: NEW
- **Description**:

  ```rust
  // FO3 ships 36 bytes (dir_fade + fog_clip, NO fog_power); FNV adds the
  // 4-byte Fog Power tail for 40. Both share `GameKind::Fallout3NV`, so the
  // canonical set carries both — the size dispatch already parses 36 fine
  // (per-field gating below), 36 was just missing here and tripped a spurious
  // "lighting may be mis-computed" warn on every FO3 interior (D3-FO3-01).
  const XCLL_SIZES_FALLOUT_ERA: &[usize] = &[28, 36, 40];
  ```

  "FO3 ships 36 bytes … FNV adds the tail for 40" is backwards as a description of the corpus.
- **Evidence**: raw sub-record size census over every CELL in both masters (this run):

  ```
  FO3  XCLL: 40 B → 404,  36 B → 17
  FNV  XCLL: 40 B → 388,  36 B →  0
  ```

  FO3 ships the 40-byte form on 404 of 421 lit cells (95.9 %); the 36-byte form is a 17-record
  minority, not the FO3 norm. (The constant itself is right — both sizes belong in the set —
  and the size-based dispatch handles both. Only the rationale is wrong.)
- **Impact**: doc-only today, but this comment is the stated justification for the contents of a
  size-dispatch table. A future change that "simplifies" it by gating 36 → FO3 and 40 → FNV, on
  the strength of this comment, would mis-parse 404 FO3 interiors' lighting. Same failure shape
  the comment itself was written to prevent.
- **Related**: the closed `D3-FO3-01` finding this comment cites.
- **Suggested Fix**: restate as "FO3 authors both 36 and 40 (404 / 17 on `Fallout3.esm`); FNV
  authors 40 only" and keep the per-field gating rationale.

#### FO3-2026-08-27-D3-03: the audit skill's own Dimension-3 checklist asks auditors to verify a REFR texture-overlay path FO3 never authors
- **Severity**: LOW
- **Dimension**: 3 — audit infrastructure / premise correction
- **Location**: `.claude/commands/audit-fo3/SKILL.md` (Dimension 3, "REFR per-instance texture
  overrides (XATO/XTNM/XTXR — #584)"), against
  `crates/plugin/src/esm/cell/walkers.rs:855-900`
- **Status**: NEW
- **Description**: the skill instructs: *"FO3 cell REFRs can carry per-instance texture-set
  overrides … Confirm FO3 overlays produce distinct resolved paths, not a collapse to the base
  mesh material. Regression tests: `byroredux/src/cell_loader/refr_texture_overlay_tests.rs`."*
  The parser's own comment at the XATO arm already records the opposite, and has since #1887:

  ```rust
  // On FONV, XATO is the Activation-Prompt subrecord
  // (grouped with SCRV/SCVR/SLSD script-vars), a string,
  // not a FormID. FO3 REFRs never carry XATO.
  ```
- **Evidence**: two independent measurements agree. Through the engine's own parser, all
  566 642 indexed FO3 REFRs carry **0** `alt_texture_ref` / `land_texture_ref` /
  `texture_slot_swaps`. A raw byte-level sub-record scan over every `REFR`/`ACHR`/`ACRE` in
  `Fallout3.esm` (573 610 records, decompressing compressed records) finds **0** `XATO`, **0**
  `XTNM`, **0** `XTXR`. FO3's 243 TXST records are reached through `LTEX.TNAM`, not through
  REFR overlays.
- **Impact**: the skill sends every FO3 audit run to verify a behaviour with no vanilla
  instances, and cites an FO4-shaped regression-test file as the FO3 evidence. This is the
  exact failure mode #3101 was filed for (`BSSegmentedTriShape` named as an FO3 divergence
  surface that FO3 authors zero of). It costs auditor time and, worse, invites a
  "verified working" conclusion drawn from FO4 fixtures.
- **Related**: #3101 (same class, closed); #1887 (the parser-side provenance caveat).
- **Suggested Fix**: replace the FO3 Dim-3 XATO bullet with the measured fact and point it at
  the real FO3 TXST consumer (`LTEX.TNAM` → `landscape_texture_sets`), the way the skill's own
  `BSSegmentedTriShape` note was corrected.

### Dimension 6: BSA v104 + Real-Data Validation

#### FO3-2026-08-27-D6-01: the ROADMAP FO3 compat-matrix cell still cites the pre-#3041 single-archive corpus (10 989 vs the live 17 172)
- **Severity**: LOW
- **Dimension**: 6 — real-data validation (doc-rot)
- **Location**: `ROADMAP.md:578`
- **Status**: NEW — sibling of **#3342** (OPEN), which reports the identical defect on the
  *FNV* row of the same table. Filed separately because a fix to #3342 edits a different line
  and would leave this one stale.
- **Description**: the row reads

  ```
  | Fallout 3         | BSA v104      | 100% (10 989)                                | Interior (Megaton, 929 REFRs). Exterior wired; fresh GPU bench pending (R6a). |
  ```

  10 989 is the `Fallout - Meshes.bsa`-only count. #3041 widened `run_game` to walk
  `Game::mesh_archives()` — all six vanilla FO3 archives — and the gate has been measuring the
  full corpus since.
- **Evidence**: `cargo test -p byroredux-nif --release --test parse_real_nifs -- --ignored parse_rate_fallout_3`,
  executed this run:

  ```
  [Fallout 3/Fallout - Meshes.bsa]  10989 NIFs, 10989 clean, 0 truncated, 0 failed (100.00%)
  [Fallout 3/Anchorage - Main.bsa]   1597 NIFs ... (100.00%)
  [Fallout 3/BrokenSteel - Main.bsa]  855 NIFs ... (100.00%)
  [Fallout 3/PointLookout - Main.bsa]1372 NIFs ... (100.00%)
  [Fallout 3/ThePitt - Main.bsa]     1614 NIFs ... (100.00%)
  [Fallout 3/Zeta - Main.bsa]         745 NIFs ... (100.00%)
  [Fallout 3] parsed 17172/17172 NIFs: clean 100.00%
  ```
- **Impact**: the headline understates verified coverage by 36 %, and — the reason it matters —
  it makes a *regression* harder to spot: a future reader comparing a re-measured 17 172 against
  the documented 10 989 has no way to tell growth from drift. The DLC archives are also where
  FO3's rarest collision blocks live (`bhkSPCollisionObject`, `bhkBlendCollisionObject`,
  `bhkConvexTransformShape` — see `crates/nif/tests/common/mod.rs:110-140`), so the number that
  certifies them should be visible.
- **Related**: #3342 (FNV row, OPEN), #3041 (the widening), #3369 (the Skyrim variant of the
  same blind spot, OPEN).
- **Suggested Fix**: `100% (17 172 across all 6 vanilla archives; 10 989 in Fallout - Meshes.bsa)`.

### Dimension 1: FO3 Rendering Path — Inline Shaders

#### FO3-2026-08-27-D1-01: `refraction_strength` is the one write in `apply_pp_lighting_property` #2328 left ungated, so an inherited property can still overwrite the shape's own value
- **Severity**: LOW
- **Dimension**: 1 — inline-shader material path (hardening)
- **Location**: `crates/nif/src/import/material/legacy_properties.rs:452`
- **Status**: NEW
- **Description**: `apply_legacy_property_chain` walks the shape's own properties first, then the
  inherited parent-NiNode ones, and the documented precedence (#208) is that the shape wins.
  #2328 implemented that for this function by converting each scalar write to a `_consumed`
  gate — `texture_clamp_mode` and `env_map_scale` in both the PPLighting and NoLighting arms,
  and again in `apply_misc_shader_properties` / `apply_base_only_shader_property`. One write in
  the same function was not converted:

  ```rust
          info.refraction_strength = shader.refraction_strength;
  ```

  A bare assignment inside a loop over `direct_properties.chain(inherited_props)` is
  last-writer-wins, i.e. inherited-parent-wins — the exact inversion #2328 fixed for its
  siblings, two statements above.
- **Evidence**: contrast with the immediately preceding writes in the same function
  (`legacy_properties.rs:398-412`):

  ```rust
          if !info.texture_clamp_mode_consumed {
              info.texture_clamp_mode = shader.texture_clamp_mode as u8;
              info.texture_clamp_mode_consumed = true;
          }
          ...
          if !info.env_map_scale_consumed {
  ```

  and with the sibling first-wins policies for `texture_path` (`is_none()`) and
  `no_lighting_falloff` (`get_or_insert`).
- **Impact**: **not reachable on vanilla FO3, measured.** A probe over all 17 172 FO3 NIFs
  across all six archives found **0** `NiNode` carrying a `BSShaderPPLightingProperty` or
  `BSShaderNoLightingProperty` in `av.properties` — FO3 binds its BS shader properties
  per-`NiTriShape` without exception, so no chain ever contains two of them and the last-writer
  rule never fires. (For scale: 49 619 PPLighting blocks, of which only 70 carry a non-zero
  `refraction_strength` at all.) This is therefore hardening + consistency, not a live defect:
  the exposure is modded FO3/FNV content that authors an inherited BS shader property, and the
  cost of divergence from its own siblings is that the next reader has to re-derive which of the
  two policies this function uses.
- **Related**: #2328 (FO3-D1-06, closed — this is its remainder); #2321 (which added the write).
  The `material_kind = 103` fire-refraction promotion below it is a monotone latch and is not
  affected the same way.
- **Suggested Fix**: add a `refraction_strength_consumed` gate matching its two neighbours, or
  document in-place why this field is deliberately last-writer-wins.

---

## Verified Fixed Since 2026-08-24

- **FO3-2026-08-24-D2-01 (the particle-emitter real-archive test structurally could not reach
  FO3) — FIXED, verified live.** `30537bf3` (Fix #3286) replaced the return-on-first-success
  loop with a full sweep. Re-run this session:

  ```
  [emitters] 4 archive(s) swept, all non-zero:
    [("Fallout New Vegas", 397), ("Fallout 3", 159), ("Oblivion", 272), ("Skyrim SE", 137)]
  ```

  FO3 now contributes 159 emitters across 5 sampled meshes
  (`fxdrippingsewage02.nif`, `fxexplosionplasma.nif`, `birthdaycake01fx.nif`, …). The
  Dimension-2 "UNVERIFIED on FO3" gap the skill flags is closed for *presence*; see #3343 for
  the remaining assert-strength gap.
- **#3203 (object-LOD ladder returned `None` for FO3) — FIXED.**
  `LodBandLadder::for_object_game` (`lod_bands.rs:175-180`) now returns `fallout_legacy()` for
  `GameKind::Fallout3NV`, with `object_lod_scheme` returning `FalloutLegacyBlocks` and a test
  (`object_lod_scheme_table`) asserting a scheme implies a ladder. Landed with #3321.
- **#2550 (`hkSubPartData` skipped on the FO3+ packed-mesh branch) — FIXED** (`84dbf1bf`).
  Collision survey this run: 11 597 `bhkPackedNiTriStripsShape` / 10 466
  `hkPackedNiTriStripsData` across the six FO3 archives, 0 extraction failures.
- **#3205, #3101, #3005, #2521, #2425, #3158 — all confirmed CLOSED** in the tracker and not
  re-derived.
- **#3146** (`decode_data`'s 144-220 tail) — **still OPEN**, owned by `/audit-esm`, unchanged,
  not re-filed.

---

## FNV-Shared Surface (verified on FO3 data, not re-filed)

These are the shared paths most likely to carry an FNV-only assumption. All were exercised
against FO3 data this run and none diverged; where an FNV issue is already open for the shared
defect it is cross-referenced rather than duplicated.

- **Inline shader flag vocabulary** — `crates/nif/src/shader_flags.rs` `fo3nv_f1` / `fo3nv_f2`
  re-verified **field-by-field against `nif.xml`** (`<bitflags name="BSShaderFlags" prefix="F3SF1" versions="#FO3#">`
  and `BSShaderFlags2`): bits 7/17/21 environment mapping, 11/28 parallax, 15/16
  refraction/fire-refraction, 26/27 decal, F2 bit 21 `Alpha_Decal` all match exactly. The
  documented absence of a `Double_Sided` bit on FO3 F2 is **confirmed by the spec** — F2 bit 4
  is `Refraction_Tint`, and back-face routing via `NiStencilProperty` is correct. FO3 authors
  463 `NiStencilProperty` blocks, all two-sided.
- **`fo3nv_f1::TREE_BILLBOARD` has no production consumer** — but a full-corpus probe found
  **0** FO3 shader properties with bit 22 set across all 17 172 NIFs, so nothing is lost.
  Premise checked and discarded rather than filed.
- **`apply_zbuffer_property` is dead on both FO3 and FNV** — **0** `NiZBufferProperty` blocks
  in either corpus (17 172 FO3 NIFs / 14 881 FNV). FO3 depth state therefore always comes from
  `MaterialInfo`'s defaults. That is *correct*: 46 523 of 49 619 PPLighting properties set both
  `ZBuffer_Test` (F1 bit 31) and `ZBuffer_Write` (F2 bit 0), and the 3 049 that do not are
  exclusively distant-LOD meshes (`f1 = 0x00003000`, `f2 = 0x2`/`0x4` — `LOD_Landscape` /
  `LOD_Building`) which must still write depth. Consuming the bits would be a regression, not a
  fix. No finding.
- **XCLC "Force Hide Land" tail** — FO3 authors 35 929 8-byte and 6 060 12-byte XCLC (FNV is
  almost entirely 12-byte); the parser's `if sub.data.len() >= 8` gate handles both. The
  unread 12-byte tail is **inert**: the low nibble (the four quadrant flags) is `0` on all
  6 060 FO3 records and on 30 097 of 30 098 FNV records, the upper bytes being uninitialised
  fill (`0xCDCDCD00`, `0xFFFFFF00`). No finding.
- **`.egt` face-tint and `.tri` morph-remap parsers have no production consumer.**
  `crates/facegen/src/{egt,tri}.rs` are reachable only from their own tests; `spawn_runtime_head`
  (`byroredux/src/npc_spawn/resumable.rs:872-905`) consumes `.egm` only and its own log calls
  the result a *"best-effort prefix until Phase 3b.x parses .tri remap"*. FO3 ships 240 `.egm`,
  49 `.tri` and its `.egt` files inside `Fallout - Meshes.bsa` (e.g.
  `meshes\characters\head\eyelefthuman.egt`). This is shared with FNV and Oblivion (every
  `has_runtime_facegen_recipe()` game) and is left to the concurrent `/audit-fnv` run rather
  than duplicated here.
- **Havok constraint coverage** — FO3 authors 489 `bhkRagdollConstraint`, 486
  `bhkLimitedHingeConstraint`, 60 `bhkMalleableConstraint`, 9 `bhkPrismaticConstraint`, 6
  `bhkHingeConstraint`, 1 `bhkStiffSpringConstraint`, 1 `bhkBreakableConstraint`. The
  undecoded hinge/prismatic/breakable edges are **#3330** (OPEN, filed against FNV) and apply
  identically here.
- **Particle / animation channel gaps** — **#3327** (abstract `NiFloatInterpolator` naming),
  **#3328** (posed float/point3 channels dropped), **#3329** (authored birth rate dropped on
  manager-controlled emitters), **#3343** (the emitter test asserts presence only), **#3344**
  (`BS Max Vertices` discarded), **#3345** (`AnimationClip.phase` dropped at the NIF→ECS
  boundary) — all OPEN against FNV, all on code with no FO3 gate, all reachable from the 159
  FO3 emitters measured above. Cross-referenced, not re-filed.
- **AI-package / CTDA** — **#3350** (20-byte CTDA rejected), **#3352** (`duration == 0`
  schedule), **#3353**, **#3354** — shared, OPEN against FNV.
- **`GameKind::from_header` classification** — verified against all six FO3 GOTY masters:
  every one reports `HEDR 0.94`, record-header version 2 (`Fallout3.esm`) or 15 (the five DLC),
  landing in the `(0.93..=0.95)` → `Fallout3NV` arm with no overlap into the FO4 (`>= 100`
  record version) or Starfield (`0.955..=0.97`) bands. #439's inversion stays fixed.
- **SCPT `SCHR` u16 flags** (#1654), **`havok_motion_type`** (#1652), **`examine_collision_kind`**
  block-type dispatch — all re-read at HEAD and unchanged; the collision survey's engine motion
  histogram (Dynamic 1 720 / Keyframed 1 389 / Static 16 120 from raw `hkMotionType`
  {2: 105, 4: 1612, 6: 1389, 7: 16120, 8: 3}) shows BOX_INERTIA (4) still mapping to Dynamic.

---

## FO3-Distinctive Gaps (unchanged, not bugs)

- **Inline-shader-only material universe.** FO3 ships 49 619 `BSShaderPPLightingProperty` and
  7 483 `BSShaderNoLightingProperty` blocks and **zero** `BSLightingShaderProperty` / BGSM, so
  `MAT_FLAG_PBR_BSDF` stays unreachable; `Material::resolve_pbr`'s `classify_pbr_keyword`
  backstop (gated on `has_material_data`, #1873) is the whole FO3 PBR story by design.
- **1 257-SCPT scripting runtime gap.** Confirmed unchanged (`scripts=1257` in the live
  `parse_rate_fo3_esm` output). Known M47.2 blocker, owned by `/audit-scripting`.
- **`WRLD.DATA` flags parsed, unconsumed** — see D3-01 above; the gap itself (no consumer) is
  by design, only its documentation is wrong.
- **SpeedTree 5.x** — 9 vanilla FO3 TREE records, 9/9 routed through the `.spt` path
  (`has_speedtree_binary()`), placeholder-billboard fallback. The `TREE.ICON` resolution
  failure is excluded from this run per the known-settled list.

---

## Validation Status

- **NIF**: live-executed `parse_rate_fallout_3` — **100.00 %, 17 172 / 17 172** across all six
  vanilla archives, 0 truncated, 0 hard failures. `unknown_types` over each archive
  independently: **0 distinct `NiUnknown` type names**, i.e. complete block-dispatch coverage
  for the FO3 corpus.
- **ESM**: live-executed `parse_rate_fo3_esm` — `index.total() = 44 666` against the 44 657
  file baseline (37 459 structured + 7 198 NAVM). Independent raw GRUP walk: 452 CELL records
  under interior/unclassified GRUPs (421 true interiors + 31 per-worldspace persistent cells)
  and 41 958 exterior CELLs, all accounted for by the index.
- **Interior**: MegatonPlayerHouse = **929 REFRs**, re-measured through the engine's own
  `EsmCellIndex` this run — matches the ROADMAP baseline exactly. (Largest FO3 interiors for
  reference: `redracer01` 2 581, `arlingtonlibrary01` 2 468, `ntlguarddepot01` 2 376.)
- **Exterior**: Capital Wasteland (`Wasteland`, FormID `0x0000003C`, bounds
  `(-99,-99)..(99,101)`, 38 463 cells, no WNAM parent) resolves correctly; the 13 FO3
  worldspaces that contain grid `(0,0)` are disambiguated by `PREFERRED_WORLDSPACES`'
  `"wasteland"` entry, and the 20 child worldspaces all carry `WNAM = 60 = 0x3C` pointing at
  it. Fresh GPU bench still pending (R6a-stale-15, unchanged — requires a Vulkan device).
- **Distant LOD**: object-LOD path exercised for the first time on FO3 — 422 baked
  `blocks\` quads across 15 worldspaces, atlas `<world>.buildings.dds` present for all 15,
  terrain quads present for 8 worldspaces at levels 4/8/16/32 (`wasteland` = 1024/256/64/16, an
  exact quadtree). The level-8-only subset is D4-01 above.
- **Textures**: `Fallout - Textures.bsa` — 12 261 `.dds`, **0** extract failures, **0** bad
  magic, **0** bad dimensions. Formats DXT1 8 571 / DXT3 717 / DXT5 2 951 / 22 uncompressed
  RGB-mask, the last handled by `dds.rs`'s `expand_uncompressed_rgb` 16/24-bpp path.
- **Collision**: 9 958 of 17 172 FO3 NIFs carry collision authoring; 19 229 `Classic`
  collision-object chains, **0** extraction failures; shapes TriMesh 12 210 / Compound 4 744 /
  ConvexHull 2 096 / Cuboid 166 / Ball 13; all 71 `bhkConvexListShape` children resolve
  (#1277 holds); 753 `bhkBlendCollisionObject`, 753/753 extract.
- **Creature/NPC**: not re-verified end-to-end (needs a Vulkan device). Static checks passed:
  `humanoid_default_idle_kf_path` → `meshes\characters\_male\locomotion\mtidle.kf` and
  `sandbox_sit_enter_kf_path` → `meshes\characters\_male\idleanims\chairskirt_leftenter.kf`
  both confirmed present in `Fallout - Meshes.bsa`; FO3 ships 2 740 `.kf` clips.

---

## Methodology Note

This run's distinguishing move was **archive-inventory census before code reading**. The
Dimension-4 finding is not visible from the source at all — `select_lod_quads` and
`stream_object_lod_blocks` are individually correct, and the FNV census that justified #3321
shows a uniform level-4 corpus. Only enumerating FO3's `blocks\` directory *per worldspace*
exposes the level-8-only shape that the descent has no answer for. Likewise D3-01 and D3-02
came from sub-record size/value censuses over the raw master, not from reading the parser;
both are cases where a committed comment states a per-game fact the shipped data contradicts.
Two candidate findings (`Tree_Billboard` unconsumed, XCLC land-flags unread) were **disproved**
by the same method and are recorded above rather than filed.

Suggest: `/audit-publish docs/audits/AUDIT_FO3_2026-08-27.md`
(label every finding `game:fo3` + `legacy-compat`, plus `terrain-exterior` for D4-01/D4-02,
`esm-plugin` for D3-01/D3-02, `nif` for D1-01, `doc-rot` for D3-02/D4-02/D6-01, and
`tech-debt` for the D3-03 skill-text correction.)
