# NIF Parser Audit — 2026-08-30

**Scope**: general parser pass, all six dimensions, no `--game` narrowing.
**Corpus**: 624,702 NIFs across all seven shipping games — every `.bsa`/`.ba2` in
each `Data/` directory, not just the primary mesh archive, with `.nif` + `.bto` +
`.btr` per `corpus::is_nif_entry`.
**Spec authority**: `/mnt/data/src/reference/nifxml/nif.xml` (precedence over the
Gamebryo 2.3 source). Every byte-layout claim below cites a nif.xml line.
**Out of scope**: canonical translation (`translate_material`, `Imported*` →
`Canonical`) — covered by the parallel `/audit-nifal` run and cross-linked, not
duplicated.

---

## Executive Summary

The parser is in strong shape. **Zero hard parse failures and zero truncations
across all 624,702 files**, 100% recoverable on every game, and exactly **one**
block type in the entire corpus has no dispatch arm — already filed as #3461.

| Game | NIFs | clean | recovered | truncated | hard fail |
|---|---|---|---|---|---|
| Oblivion | 9,612 | 100.00% | 0 | 0 | 0 |
| Fallout 3 | 17,172 | 100.00% | 0 | 0 | 0 |
| Fallout NV | 20,746 | 100.00% | 0 | 0 | 0 |
| Skyrim SE | 33,468 | 100.00% | 0 | 0 | 0 |
| Fallout 4 | 254,648 | 99.996% | 9 | 0 | 0 |
| Fallout 76 | 168,220 | 98.18% | 3,056 | 0 | 0 |
| Starfield | 120,836 | 99.98% | 19 | 0 | 0 |
| **Total** | **624,702** | | | **0** | **0** |

**Stream-position mismatches: 2 in 624,702 files** (both over-reads, both in
third-party FO4 mod archives — `ss2 - main.ba2` and `ss2_xpac_chapter2`). No
vanilla file in any game produces real-parser drift. All remaining drift is the
`bhk*Constraint` motor tail, which this audit characterised byte-for-byte against
nif.xml and found correct in every case.

**The findings are not about broken parsing — they are about the instrumentation
that would notice parsing breaking.** Three of the seven findings describe guards
that exist but cannot do their job: a sizeless-stream drift detector with a 100%
false-positive rate, a constraint-drift bucket that suppresses the signal for
exactly the parsers that now have something to regress, and 16.4% of the Oblivion
corpus behind no gate at all. The `bhkHingeConstraint` case is the proof this
class matters: it sat at a full 128-byte undecoded CInfo on 100% of instances in
three games, inside the suppressed bucket, until #3330 found it by hand.

**Live rates differ materially from `docs/engine/nif-parser.md`**, which is stamped
2026-07-11 and predates the corpus widening — most importantly it advertises FO76
at 100% clean where it measures 98.18% (D3-03).

### Counts

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 0 |
| MEDIUM | 3 |
| LOW | 4 |
| **Total** | **7** |

**Dimensions producing no findings: Dimension 4 (Geometry Extraction & Import
Handoff) and Dimension 6 (Allocation Hygiene).** Both were audited in full; their
existing defects (#3479, #3177, #3176 for D4; #3478 for D6) were reproduced and
deliberately not re-filed.

**15 candidate findings were dropped as stale** after checking the premise against
current code and nif.xml — roughly 2 dropped per finding kept, consistent with the
project's ~1-in-6 historical stale rate being, if anything, optimistic. Each drop
is recorded per dimension below with the reason.

---

## Block Type Coverage Matrix

`NiUnknown` blocks by **wire** type — the complete list across all seven games:

| wire type | game | count | dispatch arm? | verdict |
|---|---|---|---|---|
| `BSDistantObjectExtraData` | FO76 | 112,716 | **NO** | **the only coverage gap in the corpus** — #3461, OPEN |
| `BSWeakReferenceNode` | Starfield | 19 | yes | `Err` → recovery; #3524 / #2105, characterised |
| `bhkRigidBody` | FO4 | 6 | yes | mod archives only (`ss2*`) |
| `BSLightingShaderProperty` | FO4 | 2 | yes | mod archives only |
| `BSSkin::BoneData` | FO4 | 2 | yes | mod archives only |
| `BSTriShape` | FO4 | 2 | yes | mod archives only |
| `BSSkin::Instance` | FO4 | 1 | yes | mod archives only |
| `BSSubIndexTriShape` | FO4 | 1 | yes | mod archives only |

Oblivion, FO3, FNV and Skyrim SE produce **zero** `NiUnknown` blocks of any type.
Every vanilla FO4 archive — including all seven DLC — is 100% clean; every FO4
`NiUnknown` traces to third-party content.

**Live dispatch-arm count** (counted fresh by depth-0 walk of the `match type_name`
in `parse_block_inner`, not quoted from any doc):

```
top-level `=>` arms ....................... 253
distinct block-type-name literals matched .. 321
naive `=>` count (nested-match inflated) ... 269
```

167 distinct parsed struct names across the corpus (Oblivion 88, FO3 101, FNV 102,
Skyrim SE 96, FO4 83, FO76 74, Starfield 24).

**FO76 `GeneratedMeshes` reproduces exactly as documented** and is not re-filed:
`GeneratedMeshes02.ba2` 0.00% clean (all 2,049 files recovered),
`GeneratedMeshes01.ba2` 95.03% (1,007 of 20,245), every other FO76 archive 100%.

---

## Findings

### MEDIUM

#### NIF-2026-08-30-D1-01: the #395 sizeless-stream drift detector fires 4,280 times on an Oblivion corpus with zero real drift
- **Severity**: MEDIUM
- **Dimension**: Stream Position
- **Game Affected**: Oblivion (`bsver <= 11`) — every `no_block_sizes` file; by
  construction it cannot fire on the block-sized games
- **Location**: `crates/nif/src/lib.rs:917-944` (`drift_warning`), armed at
  `crates/nif/src/lib.rs:524-543`
- **Status**: NEW
- **Description**: The `no_block_sizes` path has no header-driven recovery anchor,
  so #395 added a heuristic drift detector as its only guard: for each
  successfully parsed block it compares consumed size against prior parses of the
  same type in the same file and warns when the new value is more than 2 bytes
  from every prior. The heuristic keys on a property that is not stable for most
  Oblivion block types — consumed size varies legitimately with embedded string
  length and child/property counts. Its `max - min > 2` variance escape hatch is
  evaluated over the priors only, so a type whose first two instances happen to
  agree arms the check for every later instance that does not.
- **Evidence**: Debug build (the detector is `#[cfg(debug_assertions)]`), full
  Oblivion corpus of 9,612 NIFs across all nine archives:
  ```
  4,280 "Stream drift suspect" warnings
      0 truncations, 0 hard failures, 0 recovered blocks, 0 real-parser drift
  ```
  Top emitters are exactly the variable-length types: `NiSourceTexture` 1,187 ·
  `NiTriStrips` 1,094 · `NiMaterialProperty` 914 · `NiNode` 342 · `NiTriShape` 119.
  Two representative warnings:
  ```
  block 187 'NiSourceTexture' (offset 203754) consumed 68 bytes,
    but 5 prior parse(s) of this type all consumed 72±1 bytes (median 72)
  block  38 'NiMaterialProperty' (offset 355279) consumed 75 bytes,
    but 3 prior parse(s) of this type all consumed 79±2 bytes (median 80)
  ```
  A 4-byte swing on `NiSourceTexture` is one texture path four characters shorter
  than its siblings; a 4-5 byte swing on `NiMaterialProperty` is the block name.
  Both are correct parses. False-positive rate on this corpus: 100% (4,280
  warnings, 0 true positives).
- **Impact**: The one instrumentation surface guarding the only game where a
  parser drift cascades silently is unusable as shipped — ~0.45 warnings per file,
  so anyone who enables it learns to ignore the bucket, which is precisely what
  would hide a real drift. Defence-in-depth gap, not live corruption: no vanilla
  Oblivion file currently drifts.
- **Related**: #395, #324 (the sibling sizeless-recovery mechanism sharing
  `parsed_size_cache`); D3-01 covers the same blind spot from the test side.
- **Suggested Fix**: Key the detector on something invariant rather than total
  consumed size — the natural candidate is a per-type *fixed-field* byte count
  (consumed minus the summed length of the variable-length fields the parser
  already read), which the parser knows and the heuristic does not. Failing that,
  restrict it to types whose on-disk size genuinely is constant and require
  `prior.len() >= 5` before arming, then assert a count of 0 against this
  9,612-file corpus in a test.

#### NIF-2026-08-30-D3-01: Oblivion's eight DLC archives are the only mesh corpus in the project guarded by no parse gate — on the one game where a dispatch regression truncates instead of recovering
- **Severity**: MEDIUM
- **Dimension**: Block Dispatch Coverage
- **Game Affected**: Oblivion (`bsver <= 11`, NIF v20.0.0.4/5 and the v10.x
  NetImmerse family — the `no_block_sizes` band)
- **Location**: `crates/nif/tests/common/mod.rs:152` (`mesh_archives`, Oblivion
  arm), `crates/nif/tests/common/mod.rs:216-227` (`optional_mesh_archives`, `&[]`
  for Oblivion), `crates/nif/tests/parse_real_nifs.rs:167-174`
  (`parse_rate_oblivion`), `crates/nif/tests/block_coverage_baselines.rs:85`
  (`oblivion_block_count_parity`, opens the primary archive only)
- **Status**: NEW
- **Description**: The #3041 → #3466 → #3369 sequence widened every game's parse
  gate from "the primary mesh archive" to "every mesh-bearing archive", and added
  an `optional_mesh_archives` tier so account-varying content could be gated by
  rate even when it cannot be baselined by count. Oblivion was left out of both.
  1,580 NIFs across eight vanilla DLC archives are covered by no test in the
  repository — not `parse_rate_oblivion`, not `per_block_baselines`, not
  `oblivion_block_count_parity`.
- **Evidence**: Full-corpus sweep of every `.bsa` in Oblivion's `Data/`:
  ```
  Oblivion - Meshes.bsa            8032 nifs   8032 clean   0 trunc   0 fail  <- gated
  DLCShiveringIsles - Meshes.bsa   1438        1438         0         0       <- ungated
  Knights.bsa                        75          75         0         0       <- ungated
  DLCBattlehornCastle.bsa            24          24         0         0       <- ungated
  DLCFrostcrag.bsa                   17          17         0         0       <- ungated
  DLCOrrery.bsa                       9           9         0         0       <- ungated
  DLCVileLair.bsa                     8           8         0         0       <- ungated
  DLCThievesDen.bsa                   5           5         0         0       <- ungated
  DLCHorseArmor.bsa                   4           4         0         0       <- ungated
  ```
  1,580 of 9,612 Oblivion NIFs (16.4%) ungated. `Game::mesh_archives`' doc-comment
  explains the omission — requiring GOTY-only DLC would make the all-or-nothing
  rule skip Oblivion entirely on a base-game install — but that is exactly the
  problem `optional_mesh_archives` was introduced to solve for Skyrim SE's
  Creation Club archives under #3369, and it was not applied here.
  (`Skyrim - Animations.bsa`, 44 NIFs, is likewise in neither list.)
- **Impact**: Oblivion is the **only** supported game with no `block_sizes` table,
  so it is the only game where an undispatched or under-reading block truncates
  the remainder of the scene instead of being absorbed into an `NiUnknown`
  placeholder. `oblivion_block_count_parity` exists specifically to catch that
  cascade (the F-01 `bhkConvexSweepShape`/`bhkMeshShape` class) and is blind to
  16.4% of the content. Shivering Isles alone is 1,438 files of distinctly
  authored Daedric/Mania architecture and creatures — later-authored content of
  exactly the kind #3041 widened the FNV gate to cover. All 1,580 parse clean
  today, so this is an unguarded-corpus gap, not a live defect.
- **Related**: #3041, #3466, #3369 (the same blind spot closed for every other
  game), #1332 (`oblivion_block_count_parity`); D1-01 (the sizeless path's other
  weak guard).
- **Suggested Fix**: Populate `Game::optional_mesh_archives` for Oblivion with the
  eight DLC archives (and `Skyrim - Animations.bsa` for Skyrim SE) — the
  present-only tier is safe for `parse_rate_oblivion` because that gate asserts a
  *rate*. Then widen `oblivion_block_count_parity` to `open_all_mesh_archives` +
  `open_optional_mesh_archives` and regenerate its truncating-file baseline, which
  is count-based and needs its own regen pass.

#### NIF-2026-08-30-D5-01: four constraint types now have real CInfo parsers but remain on the `is_havok_constraint_stub` list, so their drift is routed away from the real-parser signal and nothing asserts it
- **Severity**: MEDIUM
- **Dimension**: Collision/Shader Parsing
- **Game Affected**: Oblivion, Fallout 3, Fallout NV, Skyrim SE (the games whose
  corpora contain `bhk*Constraint` blocks; FO4/FO76/Starfield ship none)
- **Location**: `crates/nif/src/lib.rs:185-199` (`is_havok_constraint_stub`),
  consumed at `crates/nif/src/lib.rs:471-484`; the real parsers it shadows are at
  `crates/nif/src/blocks/collision/constraints.rs:565-576`
- **Status**: NEW
- **Description**: `is_havok_constraint_stub` lists nine constraint type names and
  routes any drift on them into `stubbed_drift_histogram` instead of
  `drift_histogram`, explicitly so that "a future audit running
  `nif_stats --drift-histogram` doesn't see ~45 systematic under-reads per
  skeleton load and falsely conclude constraints parse cleanly"
  (`lib.rs:328-345`). Four of those nine — `bhkRagdollConstraint`,
  `bhkLimitedHingeConstraint`, `bhkHingeConstraint` (#3330) and
  `bhkMalleableConstraint` — are no longer name-only stubs; they have typed CInfo
  parsers whose residual is a small, known, fully-predictable tail. They are still
  on the list, so a genuine regression inside `RagdollCInfo::parse_fo3` or
  `LimitedHingeCInfo::parse_fo3` lands in the same bucket as the intended motor
  tail, indistinguishable from it. The bucket's stated purpose — "spot a new stub
  regression (constraint type drifts from its expected stub size)" — has no
  implementation: no test, gate or baseline reads `stubbed_drift_histogram` or
  asserts any expected value.
- **Evidence**: `is_havok_constraint_stub` returns `true` for all nine names
  including the four with real parsers. **`bhkHingeConstraint` is the proof this
  matters.** A sweep of the same corpora taken 2026-08-27 recorded:
  ```
  bhkHingeConstraint  drift=+128  count=6   (FO3)
  bhkHingeConstraint  drift=+128  count=4   (FNV)
  bhkHingeConstraint  drift=+128  count=8   (Skyrim SE)
  ```
  +128 is exactly the full FO3+ `bhkHingeConstraintCInfo` — 8 × `Vector4`
  (nif.xml:2457-2464) — i.e. the constraint's entire payload was undecoded on
  100% of instances in three games. That is not a motor tail; it is a whole
  missing parser, and it sat inside the suppressed bucket where no gate looks
  until #3330 went hunting by hand. The 2026-08-30 sweep confirms #3330 fixed it
  (`bhkHingeConstraint` now appears only in the *parsed* histogram, with no drift
  row in any game), but the routing that concealed it is unchanged for the three
  remaining real parsers.

  For completeness, the current residual on every constraint type is now
  characterised byte-for-byte against nif.xml, which is what makes it assertable:

  | observed drift | composition | nif.xml sizes |
  |---|---|---|
  | +1 (FNV 1,301 · FO3 931 · SkyrimSE 1,575) | motor type byte, `MOTOR_NONE` | 1 (`hkMotorType` is a `byte`, nif.xml:2370) |
  | +18 (FO3 10) | type byte + `bhkSpringDamperConstraintMotor` | 1 + 17 (`size="17"`) |
  | +26 (FO3 34 · FNV 1) | type byte + `bhkPositionConstraintMotor` | 1 + 25 (`size="25"`) |
  | +5 / +4 (`bhkMalleableConstraint`, FNV 143 · FO3 60) | motor byte + `Strength` f32 / `Strength` alone | 1 + 4 / 4 |
  | +32 (`bhkBallAndSocketConstraint`, SkyrimSE 30) | whole undecoded CInfo | `size="32"` |
  | +36 (`bhkStiffSpringConstraint`) | whole undecoded CInfo | 2×Vec4 + f32 = 36 |
  | +141 (`bhkPrismaticConstraint`, FO3 9 · FNV 3) | whole undecoded CInfo + motor byte | 140 + 1 |
- **Impact**: Instrumentation, not live corruption — the current residuals are all
  correct, and FO3+ files are never sizeless so `block_sizes` keeps the outer
  stream aligned. The exposure is that the one telemetry surface able to notice a
  constraint-decode regression deliberately discards it for exactly the types that
  now have something to regress, and the hinge case shows the blind spot can
  persist across many releases. Constraint CInfo decode is also the sole per-game
  seam in PHYSAL, so a silent drift here mislabels ragdoll joint frames rather
  than failing loudly.
- **Related**: #117 (the original stubs), #3330 (the fix whose absence this blind
  spot hid), #979, NIF-D3-NEW-06 (the audit that created the split bucket); D1-01.
- **Suggested Fix**: Narrow `is_havok_constraint_stub` to the five genuinely
  name-only types (`bhkBallAndSocketConstraint`, `bhkPrismaticConstraint`,
  `bhkStiffSpringConstraint`, `bhkGenericConstraint`,
  `bhkBallSocketConstraintChain`) so the four decoded types report through the
  real `drift_histogram`. Then replace the suppression with an assertion: the
  residual for a decoded constraint is now known to be exactly 1, 18, 26, or 4/5,
  so a per-game gate asserting membership in that set turns the invisible +128
  class into a red build. The values are already measured per game in the table
  above.

### LOW

#### NIF-2026-08-30-D2-01: `NiDynamicEffect`'s pre-4.0.0.2 affected-nodes fields are never read, on the one band where the miss cascades
- **Severity**: LOW
- **Dimension**: Version Gating
- **Game Affected**: any content at NIF version <= 4.0.0.2 — the pre-Gamebryo
  `Morrowind` variant band, which Oblivion's own `Data/` ships (5 files)
- **Location**: `crates/nif/src/blocks/base.rs:193-207`
  (`NiDynamicEffectData::parse`)
- **Status**: NEW
- **Description**: nif.xml gives `NiDynamicEffect` **two** affected-nodes field
  groups; the parser implements only the later one. The `since="10.1.0.0"` pair is
  read; the `until="4.0.0.2"` pair is not read at all, so a `NiLight` or
  `NiTextureEffect` in a v<=4.0.0.2 file under-reads by 4 bytes plus
  `4 × Num Affected Nodes`.
- **Evidence**: nif.xml:3497-3505 —
  ```
  <field name="Num Affected Nodes"     type="uint" until="4.0.0.2" />
  <field name="Affected Nodes"         type="Ptr"  length="Num Affected Nodes" until="3.3.0.13" />
  <field name="Affected Node Pointers" type="uint" length="Num Affected Nodes" since="4.0.0.0" until="4.0.0.2" />
  <field name="Num Affected Nodes"     type="uint" since="10.1.0.0" vercond="#NI_BS_LT_FO4#" />
  <field name="Affected Nodes"         type="Ptr"  length="Num Affected Nodes" since="10.1.0.0" vercond="#NI_BS_LT_FO4#" />
  ```
  The parser's only array read is gated `stream.version() >= NifVersion::V10_1_0_0`
  (`base.rs:200`); there is no `<= V4_0_0_2` arm. The `switch_state` gate
  (`>= V10_1_0_106`) correctly matches its own `since="10.1.0.106"`, and the
  `bsver < FALLOUT4` gate correctly matches `#NI_BS_LT_FO4#` (nif.xml:16).
- **Impact**: Bounded and currently unrealised. Files below v4.0.0.2 have neither a
  `block_sizes` table nor a header block-type table (`inline_type_names`), so there
  is no recovery anchor — a 4-byte under-read there truncates the rest of the scene
  rather than corrupting one block, which makes the potential impact
  disproportionate to the field's obscurity. **Explicitly unattested**: the
  624,702-file corpus contains 5 files in this band (Oblivion's `marker_radius.nif`
  at v3.3.0.13 and four v4.0.0.2 markers), all parsing clean with zero truncations,
  so none carries a `NiLight` or `NiTextureEffect`. Reported as a spec-conformance
  gap on a reachable band, not as a live defect.
- **Related**: #1750/TD2-001 (the consolidation that gave this gate one home — the
  fix belongs in the same function), #721, #1240; D1-01.
- **Suggested Fix**: Add the `until="4.0.0.2"` arm alongside the existing one
  (`Num Affected Nodes` always, then `Affected Nodes` as `Ptr` for `<= 3.3.0.13` /
  `Affected Node Pointers` as `uint` for `4.0.0.0..=4.0.0.2`), plus a synthetic
  fixture at v4.0.0.2 — there is no vanilla sample to regression-test against.

#### NIF-2026-08-30-D2-02: three `NifVersion` constants have no call site, including a `V20_2_0_7_SSE` alias whose only reference is a test asserting it equals `V20_2_0_7`
- **Severity**: LOW
- **Dimension**: Version Gating
- **Game Affected**: none (dead code)
- **Location**: `crates/nif/src/version.rs:111` (`V10_1_0_112`), `:150`
  (`V20_2_0_7_SSE`), `:160` (`V30_1_0_1`); the self-referential assertion at
  `crates/nif/src/version.rs:848`
- **Status**: NEW
- **Description**: `V20_2_0_7_SSE` is defined as `Self(0x14020007)` — bit-identical
  to `V20_2_0_7`. Its only reference anywhere in the workspace is
  `assert_eq!(NifVersion::V20_2_0_7, NifVersion::V20_2_0_7_SSE)`, a test that can
  only fail if someone edits the constant it exists to alias. Two names for one
  value invite a future gate written against the "SSE" spelling in the belief that
  it discriminates Skyrim SE from LE — it does not: the corpus census confirms SE
  and LE share version 20.2.0.7 and differ only by `bsver` 100 vs 83.
  `V10_1_0_112` and `V30_1_0_1` are likewise call-site-less.
- **Evidence**: Exhaustive scan of every `pub const V*` in `version.rs` for
  `NifVersion::<name>` / `Self::<name>` references outside `version.rs` returns
  zero for these three and non-zero for all others.
- **Impact**: No runtime effect. Same dead-constant class that #1511, #1840 and
  #1897 each had to remove once already; the aliasing case is actively misleading
  rather than merely unused.
- **Related**: #1511, #1840, #1897; cross-domain with `/audit-tech-debt`.
- **Suggested Fix**: Delete `V20_2_0_7_SSE` and its tautological assertion; keep
  `V10_1_0_112` / `V30_1_0_1` only if a documented near-term consumer exists.

#### NIF-2026-08-30-D3-02: `block_coverage_baselines.rs` still carries the private `.nif`-only corpus filter that #2587 removed from its sibling harnesses
- **Severity**: LOW
- **Dimension**: Block Dispatch Coverage
- **Game Affected**: none today (Oblivion ships no `.bto`/`.btr`); latent for any
  game the harness is later extended to
- **Location**: `crates/nif/tests/block_coverage_baselines.rs:92`; second site at
  `crates/nif/tests/translation_completeness.rs:313`
- **Status**: Regression of #2587 (partial — the fix landed in `corpus.rs`,
  `nif_stats` and `tests/common` but not in these two harnesses)
- **Description**: #2587 established `byroredux_nif::corpus::is_nif_entry` as the
  single definition of "which archive entry is a NIF", because `.bto`/`.btr` are
  renamed NIFs on the identical parse path and filtering on `.nif` alone left
  10,662 files in `Skyrim - Meshes1.bsa` alone contributing to no baseline.
  `nif_stats` (`:580,608`) and `tests/common` (`:573,948`) were converted;
  `oblivion_block_count_parity` was not — it still builds its file list with an
  inline `.filter(|p| p.to_ascii_lowercase().ends_with(".nif"))`.
- **Evidence**: `block_coverage_baselines.rs:92` versus `tests/common/mod.rs:948`.
  The sized-game path in the *same file* (`measure_coverage`, `:237-251`) routes
  correctly through `common::parse_archive_with_histogram` and therefore through
  `is_nif_entry`, so the file is internally inconsistent.
- **Impact**: **Currently zero** — verified rather than assumed: the full-corpus
  sweep counts exactly 8,032 entries in `Oblivion - Meshes.bsa` under the
  `.nif`+`.bto`+`.btr` rule, identical to the historical `.nif`-only count, so that
  archive contains no `.bto`/`.btr`. The finding is that the second copy of the
  rule survives at all: it is the exact artefact #2587 existed to eliminate, it
  sits in the harness guarding Oblivion's cascade-prone sizeless path, and it will
  silently under-sample the moment D3-01's fix widens this test to the DLC
  archives.
- **Related**: #2587, #2347; D3-01 (the widening that would make this bite).
- **Suggested Fix**: Replace both inline filters with `corpus::is_nif_entry`, and
  consider having `corpus.rs` expose the whole "list a game archive's NIF entries"
  step so a third private copy cannot appear.

#### NIF-2026-08-30-D3-03: the per-game coverage table in `nif-parser.md` understates the swept corpus 3.4x and reports FO76 at 100% clean where it measures 98.18%
- **Severity**: LOW
- **Dimension**: Block Dispatch Coverage
- **Game Affected**: all seven (documentation)
- **Location**: `docs/engine/nif-parser.md:660-680` (§ "Per-game NIF coverage"),
  and the "Cumulative NIFs swept" row at `docs/engine/nif-parser.md:23`
- **Status**: NEW
- **Description**: The table is stamped "summary refreshed 2026-07-11 (#1900 /
  NIF-D3-02)" and predates the #3041/#3466/#3369 corpus widening, so every row
  under-counts. Two rows are wrong in direction, not merely stale.
- **Evidence**:

  | row | documented | measured 2026-08-30 |
  |---|---|---|
  | Oblivion | 99.93% (8,026 / 8,032), "remaining 6 v3.3.0.13 marker files" | **100% (9,612 / 9,612), 0 truncations** |
  | Fallout 3 | 100% (10,989) | 100% (17,172) |
  | Fallout NV | 100% (14,881) | 100% (20,746) |
  | Skyrim SE | 100% (18,862) | 100% (33,468) |
  | Fallout 4 | 100% (34,995 + 124,871) | 100% vanilla (254,648 incl. third-party) |
  | **Fallout 76** | **100% (58,469)** | **98.18% clean (165,164 / 168,220)** |
  | Starfield | 99.64% aggregate | 99.98% clean (120,836) |
  | Cumulative swept | 184,886 | **624,702** |

  The FO76 row is the load-bearing error: `GeneratedMeshes02.ba2` parses 0.00%
  clean and `GeneratedMeshes01` 95.03%, which the `/audit-nif` skill records as
  known-open and deliberately un-baselined pending #3461. The document a reader
  consults for per-game compatibility still advertises 100%. The Oblivion row errs
  the other way, claiming six residual truncations that no longer exist.
- **Impact**: Documentation only, but this is the code-verified reference audits
  are instructed to measure findings against; a wrong clean-rate causes an auditor
  to chase a fixed bug (Oblivion's six files) or miss a real gap (FO76's 3,056
  recovered blocks).
- **Related**: #3461 (the FO76 cause), #3513 (the identical stale-corpus error
  already filed against the ROADMAP's FO3 cell), #3041/#3466/#3369.
- **Suggested Fix**: Refresh the table from a full sweep, split the FO76 row into
  per-archive rates so the `GeneratedMeshes` tail is visible rather than averaged
  away, and note beside the Oblivion row that the #687/#688/#698 marker files now
  parse whole.

---

## Prioritized Fix Order

Rendering-blocking first, then animation, then collision — with the caveat that
**no finding in this audit blocks rendering**: the parser produces correct
geometry on all seven games today.

1. **D3-03** (doc) — refresh `nif-parser.md`'s coverage table first. It is cheap,
   and every subsequent audit measures against it; leaving FO76 at "100% clean"
   actively misdirects the next sweep.
2. **D3-01** (test infra, Oblivion) — highest real risk-reduction. Adds a gate over
   1,580 currently-unguarded files on the only cascade-prone game.
3. **D5-01** (collision instrumentation) — un-suppress the three decoded constraint
   types and assert the now-characterised residual set. The measured per-game
   values are in this report, so the assertion can be written directly.
4. **D3-02** (test infra) — fold into the D3-01 change; it is a two-line fix and
   D3-01's widening is what makes it matter.
5. **D1-01** (Oblivion instrumentation) — the largest design change of the six.
   Worth doing after D3-01, since a working gate reduces the urgency of a working
   warning.
6. **D2-02** (dead code) — trivial deletion, no urgency.
7. **D2-01** (spec conformance) — genuinely unattested; do it when a v4.0.0.2
   sample with a light or texture effect appears, or opportunistically alongside
   any other `NiDynamicEffectData` change.

---

## Verified-Clean Summary

Confirmed intact and **not** re-reported. Every guard below was checked against
current code, not assumed:

**Stream position** — #1606 Starfield tail (consumed to `block_size`, no hardcoded
38) · #2345/#3468 pre-10.1.0.106 `NiSequence`/`ControlledBlock`/
`NiControllerSequence` (both `until`/`since` halves present) · #698 inline
type-name truncation · #1510 Starfield shader over-read · the whole #1509/#1337/
#1329/#1310/#1301/#1302 Oblivion v10.x family · `read_bool` vs `read_byte_bool`
against nif.xml:296-298 · `BSShaderPropertyData::parse_fo3`'s four-caller set.

**Version gating** — `NifVariant::detect` over every shipping tuple in all seven
games (no tuple reaches `Unknown`) · all nine `NifVersion` feature helpers have
live call sites · every `bsver` comparison uses the right named constant and
operator, including `== FO76` which correctly transcribes nif.xml:29's
`#BS_F76# = (#BSVER# #EQ# 155)` · per-game shader-flag storage shapes · #1239
`NiPSysEmitter` · #1240 `NiTextureEffect`.

**Dispatch coverage** — #3326 wire-RTTI baseline keying · #3041/#3466/#3369
per-archive (not global) limit semantics · one gap in 624,702 files, already filed.

**Geometry handoff** — #3355/#3360 SSE global triangle indices (gated on the
partition, not the game) · #786 Bethesda tangent/bitangent swap · #2246 Starfield
UDEC3 W normalisation · typed particle decode end-to-end · #1263/#1265 `mem::take`
and the `BSGeometry` bulk-read fast paths · exact walker arm parity.

**Collision/shader** — bhk*Shape dispatch ↔ `resolve_shape` parity (the
`bhkConvexTransformShape` alias verified byte-identical against nif.xml:3029/3107)
· #2550 `hkSubPartData` decoded not skipped · #3364 Starfield shader-type
translation · #1721 Starfield stub discriminator (both shader classes in lockstep)
· #1652 full `hkMotionType` map · #1592 FO4 model-space-normals + alpha-test ·
`CollisionAuthoring`'s five variants · `CollisionAuthoringSummary` leaks no
per-game detail.

**Allocation hygiene** — complete `#[must_use]` coverage (zero gaps across 12 array
wrappers + 3 allocators + the KFM twin) · `read_pod_vec` overflow check and
big-endian compile gate · `bytemuck` confirmed not a declared dep · #832 counter
allocation · #1245 ragdoll allocators · #1261 `Arc<str>` interning · #877/#1262
`pre_parse_cell` two-phase split with both branches wired · both `dhat` heap-bound
binaries present and correctly separated.

---

## Reproduced but Deliberately Not Re-Filed

Already-open issues confirmed still live by this sweep: **#3461**
(`BSDistantObjectExtraData`, 112,716 `NiUnknown` — reproduced exactly), **#3464**
(`BSFaceGenNiNode` +2 drift, 1,563 instances), **#3524/#2105**
(`BSWeakReferenceNode`, 19 residual), **#3478** (`hkSubPartData` allocation bound),
**#3479 / #3177 / #3176** (geometry-handoff defects), **#3476** (raw version
comparisons in `sequence.rs`). The FO76 `GeneratedMeshes` truncation tail
reproduces as documented and is not re-filed pending #3461.

## Stale Candidates Dropped

15 total, per dimension: D1 3 · D2 4 · D3 2 · D4 2 · D5 2 · D6 2. Notable:

- **`bhkHingeConstraint` under-reading its full 128-byte CInfo on 100% of
  instances in three games** — true on 2026-08-27, fixed by #3330 before this
  audit ran. Retained only as historical evidence inside D5-01.
- **`parse_skyrim_shader_base` reading the typed flag pair inside the
  `is_shader_flag_gap` band** — nif.xml gates Sky/Water shader flags on
  `!#BS_GTE_132#`, unlike the Lighting/Effect blocks' `#NI_BS_LT_FO4#`. Both
  parser gates are correct and correctly different.
- **`extract_morph_targets` producing an index-sparse `Vec`** — traced to
  `flatten_morph_targets`, which deliberately writes at `original_index` and
  leaves zero slots (#3233). The two halves agree.
- **FO4/FO76/Starfield under-gated at one archive each** — false;
  `parse_real_nifs.rs` carries explicit all-archive gates for all three. Only
  Oblivion lacks one (D3-01).

---

*Corpus scratch data: `/tmp/audit/nif/sweep_<game>.txt`,
`/tmp/audit/nif/ver_<game>.txt`. Per-dimension notes: `/tmp/audit/nif/dim_1..6.md`.*
