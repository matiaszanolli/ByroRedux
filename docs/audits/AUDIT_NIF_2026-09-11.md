# NIF Parser Audit — 2026-09-11

**Scope**: `crates/nif/src/` — stream position integrity, version gating, block
dispatch coverage, geometry/import handoff, collision & shader parsing,
allocation hygiene. All 6 dimensions run (`--focus` not restricted, `--game`
not restricted). No `--corpus` override.

**Method**: 6 parallel `general-purpose` dimension agents, each reading
`_audit-common.md` / `_audit-severity.md` for shared protocol, cross-reading
`crates/nif/src/version.rs`/`nif.xml` field-by-field for gates cited, running
targeted corpus checks against the installed vanilla game archives where
useful, and deduping against a fresh `gh issue list` snapshot
(`/tmp/audit/issues.json`). Every dimension also cross-read the most recent
prior NIF audit, `docs/audits/AUDIT_NIF_2026-09-04.md` — which was never
published (no matching GitHub issues exist for any of its findings) — and
independently re-derived each of its still-cited findings from current source
rather than trusting it at face value. The orchestrator (this document)
independently verified the dispatch-arm/type-name counts and cross-referenced
every "carried forward" finding against both the 2026-09-04 report and the
current open-issue list before inclusion.

---

## Executive Summary

**The parser's structural discipline continues to hold up well.** Every
long-standing regression guard across all six dimensions — the Oblivion v10.x
truncation family, the Starfield shader over-read/tail fixes, the
pre-10.1.0.106 `NiSequence` band, SSE global-index skin partitions, the
`#2550`/`#1652`/`#1592`/`#1721`/`#3364`/`#3809` collision/shader guards, and
the `#831`/`#832`/`#833`/`#1245`/`#1261`/`#877`/`#3918`/`#3919` allocation-hygiene
pins — is **intact**. One same-day commit (`4acdf514`, "Fix #3845") landed a
real stream-drift fix unifying the Skyrim+ shader-head gate across all call
sites, verified correctly applied. Dispatch coverage has **no live parser
gap** on vanilla content — the one real dispatch-coverage finding this round
is in **audit/diagnostic tooling** (`d5_coverage.rs`), not the production
parser or its CI gates.

**18 findings, 0 CRITICAL, 2 HIGH, 8 MEDIUM, 8 LOW.** Dimension 4 (Geometry
Extraction & Import Handoff) produced **zero findings** — every checklist
item, including the newly-added `#3231` morph-target path and the
`#3930` Starfield `SkinAttach` fix that landed the day before this audit, was
independently re-verified intact.

A recurring theme this round: **9 of the 18 findings are carry-forwards from
the unpublished `AUDIT_NIF_2026-09-04.md`** — real, still-open issues that
were correctly identified a week ago but never turned into GitHub issues via
`/audit-publish`, and so were at risk of being silently lost. Each was
independently re-derived from source this session (not copy-pasted) and
confirmed still present. **This report should be published this time** —
see Prioritized Fix Order.

### Compatibility-matrix note carried from the 2026-09-04 report

The 2026-09-04 audit measured Oblivion's clean-parse rate at 92.41%
(730 truncated meshes, both lead files skinned), against the 100% figure
`docs/engine/nif-parser.md` records, and attributed it to
`PERF-D6-NEW-01` — a same-week `NiSkinPartition` strip-branch allocation bound
of 6 B/triangle against a true ~2 B/triangle payload. **This dimension's own
Dimension 6 agent confirms `NiSkinPartition::parse`'s strip branch is now
correctly bound at `allocate_vec_min_bytes::<[u16; 3]>(num_triangles, 2)`** —
the fix implied by that finding appears to already be in place. However, no
dimension agent this round re-ran `parse_rate_oblivion` to confirm the clean
rate actually recovered to 100%. **This needs a one-command re-measurement
before the next `docs/engine/nif-parser.md`/`ROADMAP.md` refresh treats either
number as settled** — it was not re-verified as part of this sweep and is
called out here rather than silently assumed fixed.

### Block dispatch coverage

Fresh count this session (`crates/nif/src/blocks/mod.rs:312-1365`, top-level
arms only, excluding four nested `type_name_static` re-derivation matches):
**255 top-level match arms covering 312 distinct block-type-name string
literals** — superseding both stale counts currently in
`docs/engine/nif-parser.md` (one section says "~248/~315", another says
"254/310"; a third count, "251/315", appears in the 2026-07-25 report). No
dispatch gap exists on any measured corpus: all seven checked-in
`per_block_baselines` TSVs show `unknown = 0`, and a live re-run of the
coverage probe against FO76's `SeventySix - GeneratedMeshes01.ba2` (19,238
NIFs) confirms the `#3461` `BSDistantObjectExtraData` fix holds at 100.0000%
coverage. The FO76 `GeneratedMeshes` tail closure (`#3461`) is fully
confirmed CLOSED — the `nif-parser.md` row still describing it as "98.18%...
pending #3461" is stale doc-rot, not a live parser gap.

### Per-game NIF coverage vs. the documented matrix

| Game | Documented (`nif-parser.md`) | This audit | Note |
|------|------|------|------|
| Oblivion | 100% (9,612/9,612), 0 truncations | Not re-measured this round; 2026-09-04 measured 92.41% clean, likely-causal bug (`PERF-D6-NEW-01`) now confirmed fixed by Dim 6 — **needs a re-run to confirm the number, see above** |
| Fallout 3 | 100% (17,172) | Not re-measured; no dimension flagged drift |
| Fallout NV | 100% (20,746) | Not re-measured; reference title, no drift flagged |
| Skyrim SE | 100% (33,468) | Not re-measured; no drift flagged. `NiSkinPartition` SSE global-index guards (#3355/#3360) confirmed intact by Dim 4 |
| Fallout 4 | 100% vanilla (254,648 incl. third-party) | Not re-measured this round; no drift flagged |
| Fallout 76 | 98.18% clean, `GeneratedMeshes02` "0.00%, known-open" (doc is stale) | **Confirmed 100.0000% coverage, 0 uncovered** on `GeneratedMeshes01.ba2` (19,238 NIFs) via live `d5_coverage` run — `#3461` closure re-confirmed; doc needs refresh |
| Starfield | 99.98% clean (120,836) | Not re-measured; Dim 5 flags a new HIGH over-read *risk* on skin/hair-tint shader types (unconfirmed against real corpus — see NIF-D5-2026-09-04-02) |

---

## Block Type Coverage Matrix (summary)

Full per-block-per-game detail lives in the checked-in
`crates/nif/tests/per_block_baselines/*.tsv` (7 games) and
`crates/nif/tests/block_coverage_baselines/*.tsv` — this audit does not
reproduce those tables verbatim.

| Metric | Value |
|---|---|
| Live dispatch arms (`parse_block_inner`) | 255 (fresh count, 2026-09-11) |
| Distinct type-name literals dispatched | 312 |
| Nested `type_name_static` re-derivation match blocks | 4 (not 2 — see NIF-D3-02) |
| Baseline rows keyed on a name with no live arm | 0 across all 7 `per_block_baselines` TSVs |
| Baseline rows with `unknown > 0` | 0 across all 7 TSVs |
| FO76 `GeneratedMeshes01.ba2` live coverage re-check | 100.0000% (0/326,634 instances uncovered) |
| RTTI-discriminator-in-a-field types checked | 5/5 correctly preserve wire-type identity (`BhkRigidBody.is_t`, `BhkCollisionObject.is_blend`, `BsRangeNode.kind`, `NiPSysBlock.original_type`, `NiTriShape::parse_segmented`) |

---

## Findings

All findings below are **Status: NEW** unless noted. IDs are preserved as
issued by each dimension agent (`NIF-D<n>-2026-09-11-NN`, or
`PERF-D6-2026-09-11-NN`) for traceability into `/tmp/audit/nif/dim_*.md`.
Nine findings are carry-forwards from the unpublished
`docs/audits/AUDIT_NIF_2026-09-04.md`, independently re-derived from current
source this session (marked "carried" below) — none has a matching GitHub
issue.

### HIGH

#### NIF-D1-2026-09-11-01 — Unbounded recursion in `read_and_skip_bounding_volume`'s UNION arm
- **Dimension**: 1 — Stream Position Integrity
- **Game Affected**: Pre-Gamebryo/NetImmerse content only (v ≤ 4.2.2.0) — Morrowind-era and non-Bethesda mod content, not reachable by any of the seven vanilla titles Redux targets (all ship at v20.0.0.4+)
- **Location**: `crates/nif/src/blocks/base.rs:306-337` (`read_and_skip_bounding_volume`)
- **Status**: NEW (carry-forward of `NIF-2026-09-04-D1-01`, independently re-derived; no matching GitHub issue)
- **Description**: The `UNION` bounding-volume arm (`bv_type == 4`) recurses once per declared child with no depth limit — 8 on-disk bytes per level (`bv_type` + `count`), so an N-byte crafted file can drive recursion to depth N/8. This is native Rust call-stack recursion, not a bounded loop or heap allocation, so none of the crate's `allocate_vec`/`MAX_SINGLE_ALLOC_BYTES` guards apply.
- **Impact**: Process abort (uncatchable stack overflow, not a returned `Err`) on crafted/fuzzed/untrusted mod content reaching Redux's loose-`.nif` CLI load path. Blast radius: archive sweep harnesses, the loose-NIF CLI arm, third-party mod content in this version band.
- **Related**: NIF-D1-2026-09-11-02 (same function); precedent for a depth cap on recursive NIF structures at `#3237` (ESM GRUP walker, already capped at 64 levels).
- **Suggested Fix**: Thread a `depth: u32` parameter through the function, `Err(InvalidData)` past a small cap (e.g. 32).

#### NIF-D5-2026-09-04-02 (carried) — Starfield `BSLightingShaderProperty` runs the FO76 skin/hair-tint tail after the parser's own comment says the block has ended
- **Severity**: HIGH (raised from the prior report's implicit MEDIUM placement)
- **Dimension**: 5 — Collision/Shader Parsing
- **Game Affected**: Starfield
- **Location**: `crates/nif/src/blocks/shader.rs:1249-1254` (the "ends after the luminance quad" comment), `:1300` (unconditional call), `:1595-1630` (`parse_shader_type_data_fo76`)
- **Status**: NEW (previously identified in the unpublished `AUDIT_NIF_2026-09-04.md`, re-verified against current code, still present; no matching GitHub issue)
- **Description**: `parse_fo76_plus` gates its FO76-only translucency/texture-array tail on `bsver < STARFIELD` with an explicit comment that Starfield's block "ends after the luminance quad... reading these over-ran the block into the NIF footer." Three lines later, `parse_shader_type_data_fo76` is called **unconditionally on `bsver`**, and for `shader_type ∈ {4 (Skin Tint), 5 (Hair Tint)}` reads an additional 12-16 bytes that the adjacent comment says Starfield doesn't have. No test fixture exercises Starfield with `shader_type == 4` or `5` — every Starfield fixture resolves to `shader_type == 0`. Unlike nif.xml's untrustworthy `#BS_F76#` scoping, which the parser has already had to override twice with corpus evidence (`#2616`/`#2622`) for adjacent fields, no equivalent corpus citation exists for this specific tail.
- **Impact**: If real Starfield content authors a skin/hair-material `BSLightingShaderProperty` (common on character NIFs) without this tail on the wire, the parser over-reads into the next block's data, desyncing the **entire remaining block stream** — a cascading misparse, not a locally-contained one. This is the classic `#1510`-class over-read the checklist calls out.
- **Related**: `#2616`/`#2622` (the precedent that nif.xml's Starfield scoping needs corpus verification per-field).
- **Suggested Fix**: Corpus-verify against real Starfield character/hair NIFs with `shader_type ∈ {4,5}`. If the tail is genuinely absent on Starfield, gate the call on `bsver < STARFIELD` like the adjacent translucency/texture-array fields, and let `read_starfield_tail` capture whatever bytes Starfield actually carries there.

### MEDIUM

#### NIF-D1-2026-09-11-02 — Unknown `BoundVolumeType` continues without consuming its body
- **Dimension**: 1 — Stream Position Integrity
- **Game Affected**: Same band as above (pre-Gamebryo, no `block_sizes` recovery anchor)
- **Location**: `crates/nif/src/blocks/base.rs:328-334`
- **Status**: NEW (carry-forward of `NIF-2026-09-04-D1-04`; no matching GitHub issue)
- **Description**: The wildcard match arm for an unrecognized `bv_type` logs "skipping" but performs no `stream.skip(...)` — the stream is left mid-body with no bytes consumed.
- **Impact**: On the no-`block_sizes` band, a wrong `bv_type` silently misaligns every subsequent field in the file with zero reconciliation to catch it.
- **Suggested Fix**: Return `Err(InvalidData)` on the unrecognized-type arm so the caller's existing recovery paths engage.

#### NIF-D2-2026-09-11-01 — `carries_typed_shader_flags`/`carries_crc_shader_flags` mis-gate `BSSkyShaderProperty`/`BSWaterShaderProperty` at the unattested `bsver == 131` stream
- **Dimension**: 2 — Version Gating
- **Game Affected**: None in shipped content today (BSVER 131 is a documented unattested FO4-era dev stream) — but the affected block types are Skyrim+ `BSSkyShaderProperty`/`BSWaterShaderProperty`
- **Location**: `crates/nif/src/version.rs:524-552`; `crates/nif/src/blocks/shader.rs:414-459,497-565`
- **Status**: NEW (deepens the prior report's one-line `NIF-D2-2026-09-04-04`; confirmed against nif.xml directly this round)
- **Description**: `parse_skyrim_shader_base` is shared by four block types, but the `bsver == 131` "neither typed nor CRC flags" gap band is only correct for `BSLightingShaderProperty`/`BSEffectShaderProperty` (nif.xml splits their flags at `#NI_BS_LT_FO4#`/`#BS_FO4#`, union `bsver <= 130`). `BSSkyShaderProperty`/`BSWaterShaderProperty` instead use nif.xml's un-split `vercond="!#BS_GTE_132#"` (`bsver < 132`) — meaning both genuinely carry the typed flags at 131, which the shared reader skips.
- **Impact**: An 8-byte under-read on any `BSSkyShaderProperty`/`BSWaterShaderProperty` at bsver 131, misaligning every field after it in the block. No observed exposure today (131 is unattested in any shipped corpus).
- **Suggested Fix**: Give Sky/Water their own gate (`bsver < 132`, no gap band) instead of routing through the BLSP-derived predicate; add fixtures at bsver 131 for both types.

#### NIF-D2-2026-09-11-02 — `NiTexturingProperty.Apply Mode` gates on `STRING_TABLE_THRESHOLD`, a constant whose own doc reserves it to header/stream code
- **Dimension**: 2 — Version Gating
- **Game Affected**: Oblivion (~30,121 `NiTexturingProperty` instances)
- **Location**: `crates/nif/src/blocks/properties.rs:285`; `crates/nif/src/version.rs:165-174`
- **Status**: NEW (re-verified carry-forward of `NIF-D2-2026-09-04-02`; code unchanged since, no matching GitHub issue)
- **Description**: Numerically correct today (same cut point), but `STRING_TABLE_THRESHOLD`'s own doc comment declares a lockstep contract scoped to `header.rs`/`stream.rs` only — this is an undeclared third consumer of a constant meant for an unrelated concept.
- **Impact**: Latent — a future coordinated rename/renumber of the constant silently flips `Apply Mode`'s read shape with no recovery anchor below v20.2.0.5.
- **Suggested Fix**: Add a distinct `V20_1_0_1`-scoped name for this call site.

#### NIF-D2-2026-09-11-03 — `NiMaterialProperty` reuses `FLAGS_U32_THRESHOLD` for an unrelated field's cut
- **Dimension**: 2 — Version Gating
- **Game Affected**: Fallout 3 dev/mod BSVERs 24-33 (brackets the affected value 26)
- **Location**: `crates/nif/src/blocks/properties.rs:44`; `crates/nif/src/version.rs:432-436`
- **Status**: NEW (re-verified carry-forward of `NIF-D2-2026-09-04-03`; no matching GitHub issue)
- **Description**: `FLAGS_U32_THRESHOLD` is named/documented for the `NiAVObject.flags` u16→u32 widen (`bsver > 26`); `NiMaterialProperty`'s compact-color cut reuses it via `>=` for nif.xml's independent `bsver < 26` condition. Correct only because both constants happen to equal 26 today.
- **Impact**: Latent — a future correction of the flags cut (e.g. to 27) silently flips `NiMaterialProperty` at bsver == 26 with no test pinning the two together.
- **Suggested Fix**: Add a distinct `MATERIAL_COMPACT_COLORS = 26` constant with its own doc and a two-sided test.

#### NIF-D3-01 — `d5_coverage` coverage-probe tool silently skips all `.bto`/`.btr` files
- **Dimension**: 3 — Block Dispatch Coverage
- **Game Affected**: Fallout 4, Fallout 76 (any archive whose content is `.bto`/`.btr`)
- **Location**: `crates/nif/examples/d5_coverage.rs:91,116`; contrast with `crates/nif/src/corpus.rs:20-28` (`is_nif_entry`)
- **Status**: NEW
- **Description**: The tool this project's own doc comments call "the LIVE source of truth for dispatch coverage" hardcodes a `.nif`-only filter instead of the shared `corpus::is_nif_entry`. Live-tested against FO76's `GeneratedMeshes02.ba2` (2,049 files, 100% `.bto`, 0 `.nif`): the tool reports "0 NIFs" and a vacuous "100.0000% coverage" for an archive it never opened a single file from. `crates/nif/tests/block_coverage_baselines.rs:126` has the identical duplicated filter (currently harmless — its one caller is Oblivion-only).
- **Impact**: No production impact (`parse_real_nifs.rs`/`per_block_baselines.rs` correctly use `corpus::is_nif_entry`) — but this specific audit/diagnostic tool produces a clean-looking, vacuously-true coverage report for `.bto`/`.btr`-dominant archives, which is exactly the failure mode that could mislead a future audit pass.
- **Suggested Fix**: Replace both hardcoded filters with `byroredux_nif::corpus::is_nif_entry`.

#### NIF-D5-2026-09-11-01 — `HavokPackfile::content_range`/`absolute_end` add two `u32` fields before widening — overflow panics in non-release builds
- **Dimension**: 5 — Collision/Shader Parsing
- **Game Affected**: Fallout 4, Fallout 76 (the `bhkPhysicsSystem`/`bhkRagdollSystem` → `BhkSystemBinary` path)
- **Location**: `crates/nif/src/blocks/collision/havok_packfile.rs:133-148`
- **Status**: NEW
- **Description**: Unlike every other offset computation in `parse_havok_packfile` (which correctly promotes to `usize` before adding), `PackfileSection::content_range()`/`absolute_end()` add two raw `u32` header fields before casting. A section header whose fields sum past `u32::MAX` triggers Rust's default debug-build overflow panic — live in `cargo test`/`cargo check`/plain `cargo run`, the project's own documented day-to-day commands (no `overflow-checks` override in `Cargo.toml`).
- **Impact**: Currently unreachable — `parse_havok_packfile` has no live caller yet (added by `#3809` as infrastructure for future physics-bridge work). Once wired to a real loader consuming untrusted/moddable `BhkSystemBinary` blobs, a single malformed section header aborts the whole engine process in any non-release build.
- **Suggested Fix**: Widen to `usize` before adding, matching the rest of the file's pattern. Add a unit test with a section header summing past `u32::MAX`.

#### NIF-D5-2026-09-04-01 (carried) — FO4+ `bhkRigidBody` reads the Skyrim CInfo body against a CInfo2014 wire layout, by the parser's own admission
- **Dimension**: 5 — Collision/Shader Parsing
- **Game Affected**: Fallout 4, Fallout 76, Starfield
- **Location**: `crates/nif/src/blocks/collision/rigid_body.rs:107-113,173-178`
- **Status**: NEW (previously identified in the unpublished `AUDIT_NIF_2026-09-04.md`, re-verified unchanged; no matching GitHub issue)
- **Description**: `BhkRigidBody::parse` has no real FO4+ CInfo prefix arm — it skips 4 trailing bytes and reads the remaining fields at the Skyrim field order, with in-code comments explicitly calling the FO4+ (`bhkRigidBodyCInfo2014`) layout "very different" and "knowingly incomplete," preserved post-`#546` specifically to avoid a new regression rather than because the layout was verified.
- **Impact**: Any FO4/FO76/Starfield NIF using the *classic* (non-NP) `bhkRigidBody` chain yields garbage `mass`/`friction`/`motion_type`/`havok_filter` feeding straight into the PHYSAL solver's Static/Dynamic/Keyframed classification. Invisible to `cargo test`.
- **Related**: `#546` (deliberately left this arm alone); `#3809` (the separate NP/`BhkSystemBinary` chain — not the same code path).
- **Suggested Fix**: Decode `bhkRigidBodyCInfo2014` per nif.xml, or at minimum make the assumption measurable via `summarize_collision_authoring`.

#### PERF-D6-2026-09-11-01 — `NiPixelData` mipmap array bypasses the amplification guard `allocate_vec_sized` exists to close
- **Dimension**: 6 — Allocation Hygiene
- **Game Affected**: Oblivion (the only current source of embedded `NiPixelData` textures); mechanism is version-agnostic
- **Location**: `crates/nif/src/blocks/texture.rs:210,288`
- **Status**: NEW
- **Description**: Both `NiPixelData`/`PixelFormatPrelude` mipmap-descriptor loops pre-size with the loose `stream.allocate_vec::<MipMapInfo>(num_mipmaps)` — a 1-byte-per-element bound with no `check_alloc`/`MAX_SINGLE_ALLOC_BYTES` routing at all — for a 12-byte struct (`width`/`height`/`offset`, all `u32`) that `allocate_vec_sized`'s own doc comment names as exactly the case it exists to bound correctly.
- **Impact**: A crafted `NiPixelData` block with an inflated `num_mipmaps` can trigger a `Vec::with_capacity` request up to 12× the remaining file bytes, unconstrained by the crate's 256 MB hard cap — an OOM/large-allocation DoS vector on untrusted input, not covered by any of the three `heap_allocation_bounds*.rs` dhat gates.
- **Suggested Fix**: `stream.allocate_vec_sized::<MipMapInfo>(num_mipmaps)?` at both call sites — the type has no heap indirection or version-dependent size variability, so the exact `size_of`-based bound applies cleanly.

### LOW

| ID | Dim | Title | Location |
|---|---|---|---|
| NIF-D1-2026-09-11-03 | 1 | `skip_animation`'s block-size skip escapes as a whole-parse `Err` via `?` instead of engaging the loop's normal truncation recovery | `lib.rs:459-469` |
| NIF-D1-2026-09-11-04 | 1 | `block_size`-driven realignment (`set_position`) can park the cursor past EOF silently, unlike `skip()` which bounds-checks | `lib.rs:536,629` |
| NIF-D2-2026-09-11-04 | 2 | `bsver::OBLIVION` has no production (non-test) consumer — the same dead-helper class removed twice already (#1511/#1840/#1897), narrower since it's test-fixture-exercised | `version.rs:401-404` |
| NIF-D2-2026-09-11-05 | 2 | `NifVariant::detect`'s FO76 lower bound (155) contradicts its own constant's doc comment ("spans 152..=167"); FO76/Starfield boundary is a bare `170` literal | `version.rs:676-698,487-490` |
| NIF-D3-02 | 3 | `docs/engine/nif-parser.md`'s dispatch-arm/type-name counts are internally inconsistent across sections and stale against the fresh 255/312 count; "two small nested match blocks" undercounts (actually four) | `docs/engine/nif-parser.md:305-306,658` |
| NIF-D5-2026-09-04-03 (carried) | 5 | `bhkPlaneShape` parses fully but hard-resolves to `None` by design (documented, reasonable) — unmeasured, so a future wrong-reason regression is invisible | `import/collision/shape.rs:102-104` |
| NIF-D5-2026-09-04-04 (carried) | 5 | `BhkConvexListShape` reads `HavokMaterial` via raw `read_u32_le` instead of the shared `read_havok_material` helper — verified this round to have zero behavioral effect (block type's version scope never reaches the helper's gate) | `blocks/collision/shape_compound.rs:175` |
| PERF-D6-2026-09-11-02 | 6 | `read_pod_vec_from_cursor` (header parser) lacks the `#[must_use]` its `NifStream` twin carries — no live bug, both current call sites bind correctly | `header.rs:426-449` |

---

## Prioritized Fix Order

1. **NIF-D5-2026-09-04-02** (HIGH) — Starfield skin/hair-tint shader tail
   over-read risk. Corpus-verify against real character/hair Starfield NIFs
   first (cheap — a targeted grep of `Starfield - Meshes*.ba2` for
   `shader_type ∈ {4,5}`); if confirmed, this is a one-line gate fix with
   potentially broad blast radius (any authored skin/hair material).
2. **NIF-D1-2026-09-11-01** (HIGH) — unbounded bounding-volume recursion.
   Process-abort DoS on crafted/mod content; the fix (a depth cap) is small
   and has direct precedent (`#3237`).
3. **Re-measure Oblivion's clean-parse rate** (not itself a finding, but the
   single highest-value action from this audit) — confirm whether
   `PERF-D6-NEW-01`'s apparent fix (visible in this round's Dim 6 sweep)
   actually restored the 100% figure the docs currently claim, before the
   next roadmap sync treats either number as ground truth.
4. **NIF-D5-2026-09-04-01** (MEDIUM) — FO4+ `bhkRigidBody` CInfo2014 gap.
   Feeds the physics solver directly on three shipping games.
5. **PERF-D6-2026-09-11-01** (MEDIUM) — `NiPixelData` mipmap allocation
   amplification. Straightforward `allocate_vec_sized` swap, closes a real
   (if narrow-blast-radius) OOM vector.
6. Remaining MEDIUMs (**NIF-D1-02, NIF-D2-01/02/03, NIF-D3-01,
   NIF-D5-2026-09-11-01**) — mostly latent/guard-hardening fixes with no
   confirmed vanilla-content exposure today; batch as a follow-up sweep.
7. **LOWs** — batch as a single follow-up; **NIF-D3-02**'s doc-count fix and
   the FO76 coverage-row refresh are cheap wins worth folding into the same
   pass since both are already known doc-rot.

**Publish this report.** Unlike prior sweeps, this one deliberately surfaced
and re-verified 9 carry-forward findings from an unpublished predecessor
report specifically because absence-of-issue was silently eroding real,
still-open findings — running `/audit-publish` on this document is the fix
for that erosion, not optional follow-up.

---

## Coverage Notes

- Dimension 4 (Geometry Extraction & Import Handoff) found **zero** issues
  after a full line-level re-verification of all three tangent paths, the
  `.av.` field-access discipline, per-game shape dispatch, SSE skin-partition
  global-index guards, the `#3856` satellite-walker split, and the
  most-recent NIF-adjacent commit (`a4cbe36f`, 2026-09-10, Starfield
  `SkinAttach` precedence) — this is a genuinely clean dimension this round,
  not an under-audited one.
- Per-game **material** classification (`byroredux/src/material_translate.rs`)
  is explicitly out of scope — owned by `/audit-nifal`. No dimension reported
  on it.
- No dimension ran a full 7-game corpus/BSA sweep this round beyond the
  targeted checks named in each dimension's report (FO76 `GeneratedMeshes01`
  re-check in Dim 3). The Oblivion clean-rate re-measurement flagged above is
  the highest-value follow-up corpus run this report identifies.
- Two doc-rot items are flagged for `/audit-tech-debt` rather than fixed here:
  `docs/engine/nif-parser.md`'s internally-inconsistent dispatch-arm counts
  (NIF-D3-02) and its stale FO76 coverage row (98.18%, "pending #3461" —
  confirmed closed this session).

---

*Generated by `/audit-nif`, 6 dimension agents (`general-purpose`),
2026-09-11. Suggested next step:*

```
/audit-publish docs/audits/AUDIT_NIF_2026-09-11.md
```
