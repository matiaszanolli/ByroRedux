# NIF Parser Audit — 2026-07-25

Deep audit of the NIF binary-format parser (`crates/nif/src/`) across the six
dimensions defined in `.claude/commands/audit-nif/SKILL.md`: Stream Position
Integrity, Version Gating, Block Dispatch Coverage, Geometry Extraction &
Import Handoff, Collision & Shader Block Parsing, and Allocation Hygiene. Run
as one leg of a `comprehensive` audit-suite sweep.

Each dimension ran as an independent pass (three concurrent sub-agents
covering two dimensions apiece, plus direct verification by the orchestrating
session), cross-referencing `docs/legacy/nif.xml`, `docs/engine/nif-parser.md`,
the checked-in per-game baseline TSVs, and `gh issue list` for deduplication.
Every finding surfaced by a sub-pass was independently re-verified against
live source by the orchestrating session before inclusion — no claim in this
report rests solely on a sub-agent's say-so.

## Executive Summary

- **1 NEW finding**: 0 CRITICAL, 0 HIGH, 1 MEDIUM, 0 LOW (code). Plus **1 LOW
  meta-finding** about audit-tooling drift (the `audit-nif` skill file itself,
  not the codebase — see NIF-META-01).
- **0 regressions.** Every regression guard cited in the audit brief — spanning
  `#688`, `#698`, `#786`, `#1240`, `#1261`, `#1262`, `#1265`, `#1269`, `#1301`,
  `#1302`, `#1310`, `#1329`, `#1337`, `#1402`, `#1509`, `#1510`, `#1516`,
  `#1592`, `#1606`, `#1652`, `#1721`, `#1840`, `#1897`, plus the allocation-
  hygiene pins `#408`/`#831`/`#832`/`#833`/`#877`/`#1245`/`#1246`, the
  collision-shape-translation completeness pair (`BhkMultiSphereShape`/
  `BhkConvexListShape`), and the `CollisionAuthoring` discriminator — was
  independently re-verified against current source (not just grepped for a
  comment) and found intact.
- **All 10 findings from the prior `AUDIT_NIF_2026-07-16.md` sweep are
  confirmed fixed** in current code (spot-checked directly, not taken on
  faith): `#1999`–`#2004`, `#2008`, and the doc-rot items in
  `docs/engine/nif-parser.md` (which now correctly describes the reverted
  `NifVariant`-gating doctrine, the current arm count, and the `NiFogProperty`
  non-consumption boundary).
- **Clean/recoverable rate**: unchanged from `docs/engine/nif-parser.md` —
  100% recoverable across all seven games, 100% clean on FO3/FNV/Skyrim SE/
  FO4/FO76, 99.93% Oblivion, 99.64% aggregate Starfield. Spot-verified
  directly (not just cited) for Oblivion, FO76, and Starfield against real
  archives this session — see Coverage Matrix below.
- **Block dispatch coverage**: freshly re-measured at **100.0000%** across
  all 7 supported games — **~419,672 files / ~10.93M block instances**
  scanned via the live `d5_coverage` probe, **zero** uncovered types on any
  game. All 246 `nif.xml` block types with no dispatch arm were individually
  triaged and confirmed to be abstract base classes, sub-4-Oblivion NetImmerse
  types below the supported floor, or non-Bethesda Gamebryo-licensee features
  (NiMesh/`NiPS*` streaming particles, NiPhysX, Ni3ds, portal culling, Gamebryo's
  own Fx-UI framework) empirically absent from all seven games' shipped
  content.
- **Full `byroredux-nif` test suite**: 886 unit tests + all integration test
  binaries (`per_block_baselines`, `block_coverage_baselines`,
  `translation_completeness`, `heap_allocation_bounds`,
  `heap_allocation_bounds_geometry`, `synthetic_fixtures`,
  `mtidle_motion_diagnostic`) — **0 failures**, confirmed in both debug and
  `--release` profiles, including the `dhat-heap`-gated allocation-budget
  suites.
- **This is a very mature, well-instrumented corner of the codebase.** Four of
  six dimensions (Block Dispatch, Geometry/Import Handoff, Collision/Shader,
  Allocation Hygiene) returned zero findings after full re-reads and, in
  Dimension 3's case, a fresh 7-game empirical sweep. The one real defect
  found (Dimension 1) is a genuine version-gating omission but has **zero
  practical blast radius** against any currently supported title.

## Block Type Coverage Matrix

Freshly measured this session via the live `d5_coverage` probe
(keyed off `is_block_type_dispatched`, not a cached snapshot) against the
primary mesh archive(s) of all 7 supported games:

| Game | Archive(s) | NIFs | Block instances | Distinct types | Uncovered (`NiUnknown`) |
|---|---|---:|---:|---:|---:|
| Oblivion | `Oblivion - Meshes.bsa` | 8,032 | 329,889 | 119 | **0** |
| Fallout 3 | `Fallout - Meshes.bsa` | 10,989 | 287,331 | 142 | **0** |
| Fallout NV | `Fallout - Meshes.bsa` | 14,881 | 492,796 | 150 | **0** |
| Skyrim SE | `Skyrim - Meshes0/1.bsa` | 22,047 | 758,733 | 143 | **0** |
| Fallout 4 | `Fallout4 - Meshes(Extra).ba2` | 159,866 | 4,804,489 | 114 | **0** |
| Fallout 76 | `SeventySix - Meshes/StaticMeshes/GeneratedMeshes01.ba2` | 95,041 | 2,391,268 | 117 | **0** |
| Starfield | `Starfield - Meshes01/02/Patch/FaceMeshes/LODMeshes(Patch).ba2` | 108,816 | 1,865,182 | 30 | **0** |

**Total: ~419,672 files / ~10.93M block instances, 100.0000% dispatch coverage,
zero uncovered types, on every one of the 7 supported games.**

Independently spot-verified by the orchestrating session (a subset, to
corroborate the sub-agent's figures) via `cargo test --release --test
per_block_baselines -- --ignored`:

| Game | Files parsed | Clean | Recoverable | Distinct types | Unknown |
|---|---:|---:|---:|---:|---:|
| Oblivion | 8,032/8,032 | 99.93% (6 truncated markers) | 100% | 81 | 0 |
| Fallout 76 | 58,469/58,469 (`SeventySix - Meshes.ba2`) | 100% | 100% | 69 | 0 |
| Starfield | 31,058/31,058 (`Meshes01.ba2` only) | 100% | 100% | 22 | 0 |

(The `per_block_baselines` per-archive figures differ from `d5_coverage`'s
figures above because the two probes target different archive subsets per
game — both independently show 0 unknown blocks.)

Live dispatch arm count: **315 distinct type-name literals / ~251 top-level
match arms** in `crates/nif/src/blocks/mod.rs::parse_block_inner` (freshly
counted, not cited from a stale doc figure — matches
`docs/engine/nif-parser.md`'s "~315 distinct type-name literals" almost
exactly; the doc's "~248" arm figure is within the drift the doc itself
warns about, "this figure drifts, count fresh").

**Project-memory note-to-self** (not a report finding — flagging so
`MEMORY.md`'s "NIF Corpus Baseline Tests" entry can be refreshed): the note
that "FO76 currently silently RED on `NiPSysBlock`" is now stale.
`per_block_baseline_fallout_76` was re-run directly against real FO76 data
this session and is green (69 distinct types, 0 unknown, baseline matches
exactly). The underlying fix (typed `NiPSysEmitter*` structs) already
landed; the FO76 corpus is clean today.

## Findings

### MEDIUM

#### NIF-D1-2026-07-25-01: `NiSkinData::parse` reads `Has Vertex Weights` and the inline `Skin Partition` ref without their full nif.xml version gates
- **Severity**: MEDIUM
- **Dimension**: Stream Position Integrity (cross-referenced under Version Gating, same root cause)
- **Location**: `crates/nif/src/blocks/skin.rs:96-108` (`NiSkinData::parse`); the incomplete gate lives on `crates/nif/src/version.rs:220-226` (`NifVersion::has_skin_data_partition_ref`)
- **Game Affected**: Would affect a `NifVariant::Morrowind`-band file at NIF version `< 4.2.1.0` (Skin Partition ref: `< 4.0.0.2`). **No currently supported game ships at this version** — Oblivion is the oldest supported title at v20.0.0.4/5.
- **Status**: NEW
- **Description**: `NiSkinData::parse` reads two fields whose presence nif.xml gates by version, and both gates are incomplete:
  1. **`Has Vertex Weights`** (nif.xml line 5072: `since="4.2.1.0" default="true"`) is read **unconditionally** at `skin.rs:107` (`let has_vertex_weights = stream.read_u8()? != 0;`). No version check exists anywhere nearby — every sibling version-gated field in this same function is properly guarded; this one isn't, despite an inline comment stating the correct gate in prose ("version >= 4.2.1.0") that the code never applies.
  2. **`Skin Partition` ref** (nif.xml line 5071: `since="4.0.0.2" until="10.1.0.0"`) is gated by `NifVersion::has_skin_data_partition_ref`, whose body is `self <= Self::V10_1_0_0` — only the `until` bound is checked. The `since="4.0.0.2"` lower bound is missing, so for `version < 4.0.0.2` (i.e. exactly `V4_0_0_0`) the predicate still returns `true` and the parser reads a 4-byte ref that isn't on disk at that version.

  Both gaps break the codebase's own established doctrine: every other "old
  NetImmerse" boundary in this file (`has_object_group_id`,
  `has_interp_controller_manager_controlled`, `has_quat_transform_trs_valid`)
  is a correct two-sided (or correctly single-sided, when nif.xml itself has
  no `since`) range check. This is the one field-level gate in the file that
  doesn't match its own nif.xml citation.
- **Evidence**:
  ```rust
  // crates/nif/src/blocks/skin.rs:102-107
  if stream.version().has_skin_data_partition_ref() {
      let _skin_partition_ref = stream.read_block_ref()?;
  }
  // has_vertex_weights (version >= 4.2.1.0, always true for Bethesda games)
  let has_vertex_weights = stream.read_u8()? != 0;
  ```
  nif.xml (`docs/legacy/nif.xml`) cross-check, confirmed directly:
  ```
  5071: <field name="Skin Partition" type="Ref" template="NiSkinPartition" since="4.0.0.2" until="10.1.0.0">
  5072: <field name="Has Vertex Weights" type="bool" since="4.2.1.0" default="true">
  ```
  ```rust
  // crates/nif/src/version.rs:220-226
  pub fn has_skin_data_partition_ref(self) -> bool {
      self <= Self::V10_1_0_0   // missing `self >= Self::V4_0_0_2 &&`
  }
  ```
  No test in the crate exercises a pre-4.2.1.0 `NiSkinData` fixture — this
  path has zero coverage in either direction.
- **Impact**: For any NIF at `version < 4.2.1.0` containing an `NiSkinData`
  block, the parser over-reads by 1 byte (5 bytes if also `< 4.0.0.2`),
  misaligning every subsequent float read inside that block's `BoneData`
  array. Versions this old predate the `block_sizes` table (`>= 20.2.0.5`),
  so there is no per-block recovery anchor for this specific era — the drift
  would cascade the same way the historical `#1301`/`#1310`/`#1337` v10.x
  bugs did. **Blast radius is effectively zero for every currently supported
  title**: Oblivion ships at 20.0.0.4/20.0.0.5, FO3/FNV at 20.2.0.7, Skyrim+
  higher still — all far above 4.2.1.0. `ROADMAP.md:488` explicitly places
  Morrowind (the one game that would exercise `< 4.2.1.0` NIFs) **out of
  scope** ("supporting it would double the parser surface for one extra
  game"). Rated MEDIUM rather than HIGH for exactly this reason: real,
  reproducible stream-position bug, but unreachable by any game this engine
  is tested or shipped against today, and the version constants exist in
  this codebase only for defensive parsing of non-Bethesda NetImmerse
  content (Civ IV / Freedom Force, per comments elsewhere in `base.rs`).
- **Related**: Same class of bug as the historical `#1301`/`#1310`/`#1329`/
  `#1337` Oblivion v10.x truncation family (all fixed); this is the one
  remaining unguarded field in the skinning path, one version-era further
  back than any of those.
- **Suggested Fix**: Gate `has_vertex_weights` on `stream.version() >=
  NifVersion::V4_2_1_0` (default `true` when absent, per nif.xml's
  `default="true"`), and add the missing lower bound to
  `has_skin_data_partition_ref` (`self >= NifVersion::V4_0_0_2 && self <=
  Self::V10_1_0_0`). Add a synthetic fixture test at `V4_0_0_2` (has skin
  partition ref, no has-vertex-weights byte) and one below `V4_2_1_0`,
  mirroring the existing `old_oblivion_layout_predicates` test pattern in
  `version.rs`. Given the confirmed-zero blast radius, this is safe to batch
  with other low-urgency parser hardening rather than treat as urgent.

### LOW

#### NIF-META-01: `audit-nif` skill's Dimension 2 checklist cites 7 `NifVariant` helper names that no longer exist (all deleted as dead code)
- **Severity**: LOW
- **Dimension**: Version Gating (audit-tooling meta-finding, not a codebase defect)
- **Location**: `.claude/commands/audit-nif/SKILL.md` (Dimension 2 checklist, the "canonical helpers" sentence); actual code at `crates/nif/src/version.rs`
- **Status**: NEW (tooling drift, not a code regression)
- **Description**: The skill's Dimension 2 checklist states the canonical
  `NifVariant`-keyed feature-flag helper surface "currently include[s]
  `has_properties_list`, `has_effects_list`, `has_culling_mode`,
  `has_shader_alpha_refs`, `has_shader_property_fo3_fields`,
  `uses_bs_tri_shape`, `has_material_crc`" (plus the still-live
  `NifVersion`-side collision/v10.x helpers). **All seven of those
  `NifVariant` names have been deleted** — `#1840` removed six of them,
  `#1897` removed the seventh survivor (`has_shader_property_fo3_fields`),
  per an explicit removal-log comment at `crates/nif/src/version.rs:560-563`.
  `grep -rn "has_properties_list\|has_effects_list\|has_culling_mode\|has_shader_alpha_refs\|has_shader_property_fo3_fields\|uses_bs_tri_shape\|has_material_crc" crates/nif/src/version.rs`
  only matches the removal-log comment, not a live definition. Today
  `impl NifVariant` has exactly two methods: `detect` and `bsver`. The
  8 collision/v10.x-era helpers the checklist lists as living on the same
  surface (`has_mopp_offset`, `has_object_group_id`, etc.) DO still exist —
  correctly — but they live on `NifVersion`, not `NifVariant`, and
  `docs/engine/nif-parser.md`'s own "Version handling" section already
  describes the current (post-revert) doctrine accurately without naming
  any of the seven dead helpers. This is exactly the class of doc-rot the
  prior `AUDIT_NIF_2026-07-16.md` flagged as `NIF-D2-A`/`NIF-D3-01` against
  `docs/engine/nif-parser.md` — except this time the stale reference lives
  in the audit skill's own checklist text, not the architecture doc.
- **Impact**: None to the running engine. Risk is entirely to future audits:
  someone could grep for one of the seven names, find nothing, and
  misdiagnose "a version-gating helper regressed" when in fact its deletion
  was the intentional, already-tracked `#1840`/`#1897` cleanup. Both this
  session's Dimension 2 sub-pass and the orchestrating session independently
  hit this exact trap and had to disprove it before concluding "no finding."
- **Suggested Fix**: Update `.claude/commands/audit-nif/SKILL.md`'s
  Dimension 2 checklist to drop the seven dead `NifVariant` names and either
  (a) point at `docs/engine/nif-parser.md`'s "Version handling" section for
  the current doctrine, or (b) list the 8 `NifVersion`-side helpers that are
  actually live today (`has_object_group_id`, `has_mopp_offset`,
  `has_havok_strips_scale`, `has_skin_data_partition_ref`,
  `has_keyframe_controller_data`, `has_interp_controller_manager_controlled`,
  `has_quat_transform_trs_valid`, `uses_old_rigid_body_layout`).

## Dimension-by-Dimension Detail

| Dimension | New findings | Notes |
|---|---|---|
| 1. Stream Position Integrity | 1 MEDIUM | `NiSkinData` pre-4.2.1.0 gate gap (above). 9 named regression guards independently re-verified intact, incl. the Oblivion v10.x truncation family, Starfield `BSLightingShaderProperty` tail capture, and root-NiNode truncation-not-hard-fail. One candidate (`bhkRigidBody` FO4+ "incomplete" branch) disproven as dead code by cross-checking the real 34,995-file FO4 per-block baseline TSV — `bhkRigidBody` never appears in FO4 content (FO4 uses `bhkNPCollisionObject` instead). |
| 2. Version Gating | 0 (cross-refs Dim 1's finding) | Two checklist premises supplied by the audit brief were explicitly re-tested and **disproven**: (a) "should route through `NifVariant` helpers" — confirmed zero production call sites, doctrine fully enforced post-`#1897`; (b) "Skyrim+ uses single-word shader-flag storage" — false, Skyrim/FO4 use the same two-word `(F1,F2)` pair as FO3/FNV, just a different bit vocabulary; only FO76+/Starfield switches to CRC32-tagged arrays. All 9 shipping `NifVariant`s have dedicated `detect_*` tests plus edge-case/gap-range/ambiguity coverage. 14 named `bsver` boundary constants spot-checked against nif.xml operators — all correct. See also NIF-META-01. |
| 3. Block Dispatch Coverage | 0 | Fresh 7-game empirical sweep (~419,672 files / ~10.93M block instances) found zero uncovered types on any game — see Coverage Matrix above. All 246 "missing" nif.xml block types individually triaged as abstract bases, sub-Oblivion NetImmerse types, or non-Bethesda-licensee features. Both checked-in baseline test suites (`per_block_baselines.rs`, `block_coverage_baselines.rs`) re-run against real game data: 14/14 pass. |
| 4. Geometry Extraction & Import Handoff | 0 | `.av.*` sub-struct access is clean everywhere. Three per-game geometry paths (`NiTriShape`/`BsTriShape`/`BSGeometry`) cleanly downcast-dispatched, consistent between hierarchical and flat walkers. Three tangent-handoff paths (authored-blob `#786` swap, FO4+ inline `VF_TANGENTS && VF_NORMALS` gate, Mikkelsen synthesis) correctly gated and not cross-wired. SSE full-precision decoder traced and confirmed double-gated (parse-layer bsver check + import-layer skin-ref check) against ever misfiring on FO4+ content. One candidate (multi-particle-system parameter cross-contamination) resolved as **Existing: #1402** (closed, documented limitation, unregressed). |
| 5. Collision & Shader Block Parsing | 0 | `BhkRigidBody` old/new layout split, MOPP-offset/Havok-strips-scale gates, per-game constraint `CInfo` decode, `BSLightingShaderProperty`'s 3-way dispatch (each variant reads only its own field band), Starfield tail capture (budget-driven, can't over-read), shader-type trailing-field counts (matched against nif.xml enums exactly), `BSEffectShaderProperty` Starfield stub discriminator (`#1721`), collision-shape translation completeness (`BhkMultiSphereShape`/`BhkConvexListShape`), `CollisionAuthoring` enum, `hkMotionType→MotionType` mapping (`#1652`), and FO4 `Model_Space_Normals`/`Alpha_Test` OR-in (`#1592`, now in `import/material/dedicated_shader.rs` post-reorg) — all confirmed PASS. |
| 6. Allocation Hygiene (PERF) | 0 | `#[must_use]` confirmed on `allocate_vec`/`read_pod_vec`/every `read_*_array` wrapper/KFM's local twin. Confirmed **no `bytemuck` dependency anywhere in the workspace** (contradicts what some past audits apparently claimed) — `AnyBitPattern` is a small local `unsafe trait`. Zero double-allocation regressions crate-wide. `lib.rs`'s per-type counters use the `get_mut()`/`insert` split, not `or_insert(to_string())`. `ragdoll.rs` and siblings use `allocate_vec` throughout. `streaming.rs::pre_parse_cell`'s two-phase pipeline (serial BSA extract → rayon-or-serial parse at threshold 8) confirmed intact. Both `dhat`-gated heap-budget suites pass, confirmed as intentionally separate binaries (process-global `dhat::Profiler` singleton), not a duplication bug. |

## Prior Audit Closure Verification

Every finding from `AUDIT_NIF_2026-07-16.md` was independently re-checked
against current source (not assumed fixed from commit-message titles alone):

| ID | Title | Status |
|---|---|---|
| NIF-D1-02 | `BhkSimpleShapePhantom`/`BhkAabbPhantom` missing `bhkWorldObject` "Unknown Int" for v10.0.1.0–10.0.1.2 | **FIXED** — both parsers now gate `stream.version() <= NifVersion::V10_0_1_2` before the 4-byte skip |
| NIF-D1-03 | `NiGeomMorpherController` reads `Morpher Flags`/`Num Interpolators` without version gates | **FIXED** (`#2000`) — both fields properly gated (`V10_0_1_2`, `V10_1_0_106`) |
| NIF-D1-01 | `NiPersistentSrcTextureRendererData` aliased to `NiPixelData`'s parser | **FIXED** (`#2001`) — dedicated parser + dispatch arm + regression tests now exist in `texture.rs` |
| NIF-D5-01 | `BSLightingShaderProperty::parse_fo4` reads pre-Name Shader Type unconditionally across BSVER 130–154 | **FIXED** (`#2002`/`#2110`) — now gated `bsver < FO4_DLC_UPPER` |
| NIF-D1-04 | `NiShadeProperty.Flags` read unconditionally | **FIXED** (`#2003`) — gated `bsver > FO3_FNV` (Skyrim+ has no on-disk flags) |
| NIF-D1-05 | `NiTexturingProperty` decal-slot count has no upper bound | **FIXED** (`#2004`) — `MAX_DECAL_SLOTS: u32 = 4` enforced, returns `InvalidData` on overflow |
| NIF-D2-A | `nif-parser.md`'s stale `GameVariant` pattern example | **FIXED** — doc now accurately describes the reverted doctrine |
| NIF-D3-01 | `nif-parser.md`'s live-arm-count ~5% stale | **Effectively current** — 315/~251 today vs. doc's "~315/~248", within the doc's own acknowledged drift tolerance |
| NIF-D3-02 | `nif-parser.md` mischaracterizes `NiFogProperty` as a dispatch gap | **FIXED** — doc now correctly attributes the gap to the downstream material walker (`#1224`) |
| NIF-D4-01 | `BsOrderedNodeData.alpha_sort_bound` left in raw Z-up space | **FIXED** (`#2008`) — Y-up conversion applied at extraction (`import/walk/mod.rs:1619-1630`) |

## Prioritized Fix Order

1. **NIF-D1-2026-07-25-01** (MEDIUM) — low urgency given zero blast radius on
   any supported title; batch with other parser-hardening work rather than
   fast-track. No rendering-blocking, animation, or collision content is
   affected on any currently supported game.
2. **NIF-META-01** (LOW, tooling) — a documentation-only fix to
   `.claude/commands/audit-nif/SKILL.md`; convenient to fold into the next
   routine doc-sync pass alongside any other audit-skill maintenance.

No CRITICAL or HIGH severity findings this session. No rendering-blocking,
animation-blocking, or collision-blocking gaps were found in any of the six
dimensions.

## Verification Commands Run This Session

```
cargo test -p byroredux-nif                                          # 886 passed, 0 failed (+ all integration binaries)
cargo test -p byroredux-nif --release                                # same, release profile
cargo test -p byroredux-nif --release --test per_block_baselines -- --ignored     # Oblivion/FO76/Starfield spot-check, 0 unknown
cargo test -p byroredux-nif --features dhat-heap --test heap_allocation_bounds    # 3 passed
cargo test -p byroredux-nif --features dhat-heap --test heap_allocation_bounds_geometry  # 1 passed
cargo test -p byroredux-nif --release --test translation_completeness -- --ignored cross_game  # all 7 games pass fill-rate floors
gh issue list --repo matiaszanolli/ByroRedux --limit 200 --json number,title,state,labels
gh issue view 1402 / 1843 / 1856 / 1827 / 2098 / 2099                 # dedup confirmation
```

## Deduplication — Open Issues Re-Confirmed, Not Re-Filed

`#2109`, `#2108` (renderer/material scope, out of nif-parser boundary but
cited for completeness), `#2099` (Starfield `uvs1` dropped by importer),
`#2098` (BSGeometry bounding-sphere scale not cross-checked), `#1856` (FO3
`WaterShaderProperty.water_shader_flags` dead-ends at `MaterialInfo`),
`#1843` (pre-4.1 NIF bool-width cascade, Morrowind-era — same "out of scope
today" class as this session's NIF-D1-2026-07-25-01), `#1827` (Starfield
`BSGeometry` per-vertex bone data empty, informational). All spot-checked
against current code this session and confirmed still accurately described
by their existing issue text — no status changes.

## Suggested Next Step

```
/audit-publish docs/audits/AUDIT_NIF_2026-07-25.md
```
