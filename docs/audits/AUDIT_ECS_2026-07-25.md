# ECS Audit — 2026-07-25

**Scope**: `crates/core/src/ecs/` (storage backends, queries, world, resources,
system/scheduler, access analyzer, lock tracker, built-in systems) plus the
cross-cutting component-lifecycle and hot-path guards listed in
`.claude/commands/audit-ecs/SKILL.md` dimensions 7–9.

**Run date**: 2026-07-25 · **Branch**: `main` @ `ca7a4e0e` · **Leg of**: `comprehensive` audit-suite sweep

## Test Baseline

| Command | Result |
|---|---|
| `cargo test -p byroredux-core` | **543 passed, 0 failed**, 0 ignored (+ 2 doc-tests passed, 3 ignored) |
| `cargo test -p byroredux` | **693 passed, 0 failed**, 4 ignored (lib) + 9 passed across integration binaries |

Storage, query, scheduler, access-analyzer, lock-tracker and
transform-propagation suites are all green. No regressions found in any of the
regression guards the skill enumerates.

## Executive Summary

The ECS core is in good shape. All nine dimensions were walked top-down against
live code; **every named regression guard from the skill checklist is intact**
(details in the per-dimension pass list below). Four findings emerged, none of
them a regression of a previously-fixed bug:

- **2 MEDIUM** — one real cross-cell correctness bug in the M42 seat-reservation
  lifecycle under exterior grid streaming, and one unbounded RAM-growth
  mechanism in `SparseSetStorage` that compounds with the (deliberately)
  monotonic `EntityId` counter.
- **2 LOW** — a `lock_tracker` scope-defuse ordering inconsistency on an
  unreachable-in-practice panic path, and CI doc rot discovered while verifying
  the ABBA-detector guard.

**No CRITICAL and no HIGH findings.** In particular: no lock-ordering
regression, no unsound `unsafe`, no dropped-guard UAF, no undeclared parallel
system, and no per-draw material re-classification.

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 0 |
| MEDIUM | 2 |
| LOW | 2 |

## Dimension Pass List (verified, no finding)

### 1. Lock Ordering & Deadlock — PASS

- **Tracker-scope arming**: all ten acquisition sites in `world.rs`
  (`get`, `query`, `query_mut`, `query_2_mut`, `query_2_mut_mut`, `resource`,
  `resource_mut`, `resource_2_mut`, `try_resource`, `try_resource_mut`) arm a
  `TrackedRead`/`TrackedWrite` before the real acquisition and defuse after it.
  `World::get` correctly keeps the scope armed on the `None` arm so
  `TrackedRead::Drop` untracks (the #137 pattern). `QueryRead`/`QueryWrite`/
  `ComponentRef`/`ResourceRead`/`ResourceWrite` all impl `Drop` → `untrack_*`.
- **TypeId-sorted scope + lock order (#313)**: `query_2_mut` (`world.rs:457-487`),
  `query_2_mut_mut` (`world.rs:521-551`) and `resource_2_mut`
  (`world.rs:695-725`) each branch on `id_a < id_b` and arm the tracker scopes
  in the *same* order as the real acquisitions. The pre-#313 parameter-order
  arming is not present.
- **Same-type double-lock**: `assert_ne!` guards on all three paired APIs
  (`world.rs:440`, `508`, `672`, plus `try_resource_2_mut` at `796`).
- **ABBA graph**: `lock_tracker::global_order` is `cfg(debug_assertions)` +
  `BYRO_LOCK_ORDER_CHECK`-gated, records `held → new` edges and panics on the
  cycle-closing observation, with a re-check under the write lock. The
  `lock-order-check` CI job (`.github/workflows/ci.yml:75-87`) runs
  `cargo test --workspace` with `BYRO_LOCK_ORDER_CHECK: 1` and is **blocking**
  (no `continue-on-error`) — #1410 is genuinely closed.
- **Poison resolution**: every acquisition resolves `PoisonError` through
  `storage_lock_poisoned::<T>()` / `storage_lock_poisoned_erased()` /
  `resource_lock_poisoned::<R>()`. `despawn` (`world.rs:131-134`) and
  `clear_entities` (`world.rs:234-237`) both feed the erased variant from the
  `type_names` side-table (#466 / #1836). `insert_resource` (#1837) and
  `remove_resource` both re-panic loud rather than `.ok()`-swallowing.
- **`&mut self` fast paths** (`insert`, `insert_batch`, `remove`, `get_mut`,
  `despawn`, `clear_entities`) use `RwLock::get_mut()` and are therefore
  untracked — sound, because `&mut World` structurally excludes any live
  `&self`-borrowing guard.

### 2. Storage Correctness — PASS

- `SparseSetStorage::remove` (`sparse_set.rs:99-107`) fixes the moved entity's
  sparse pointer after the swap; the `dense_idx == last` branch takes the
  no-swap `pop` path; `insert` overwrites in place with no `len` change.
  Pinned by `swap_remove`, `remove_last`, `overwrite`.
- `PackedStorage` maintains the sorted invariant via `binary_search` on every
  `insert`/`remove`; `insert_bulk` (`packed.rs:152-233`) uses the #467
  append + index-sort + last-writer-wins dedup path, pinned by
  `insert_bulk_matches_serial_insert_for_unique_ids`,
  `insert_bulk_duplicate_ids_last_writer_wins`,
  `insert_bulk_extends_non_empty_storage`, `insert_bulk_empty_is_noop`.
- Change tracking: `TRACK_CHANGES` is `false` by default and enabled on exactly
  four types (`Transform`, `GlobalTransform` on `PackedStorage`; `Parent`,
  `Children` on `SparseSetStorage`, plus `LocalBound`). `mark_dirty` fires on
  `insert`/`get_mut`/`iter_mut`/`remove`; `structural_gen` bumps on
  insert (incl. overwrite/reparent) and remove. `drain_dirty_into` clears `out`
  first and preserves `self.dirty` capacity (#1371), pinned by
  `drain_dirty_into_preserves_storage_capacity`.
- `World::insert_batch` (`world.rs:203-211`) wraps the iterator in a `map`
  adapter that still runs the `entity < next_entity` `debug_assert` per item
  before handing off to `insert_bulk`.

### 3. Query Borrow Safety — PASS

- `QueryRead`/`QueryWrite`/`ComponentRef` each hold the `RwLock*Guard` in a
  live `#[allow(dead_code)] guard` field and cache the downcast pointer once in
  `new()` (#1367). All three guards are dropped only with the wrapper.
- `ComponentRef` (`query.rs:231-291`) retains the guard — the unsound #35
  "drop the guard, return a raw pointer" pattern is not present.
- `QueryWrite::Deref`/`DerefMut` route through `storage()`/`storage_mut()`;
  `storage_mut` takes `&mut self`, so a simultaneous `&`/`&mut` into the same
  storage is a borrow-check error.
- `query`/`query_mut` return `None` for a never-created storage (`self.storages.get(&type_id)?`)
  — no lazy empty-storage creation on the read path.

### 4. Resource Lifetimes — PASS

- `resource()`/`resource_mut()` panic naming the type; `try_resource()`/
  `try_resource_mut()` return `None`.
- `ResourceRead`/`ResourceWrite` downcast per-`Deref` (not cached — correct,
  these are not the #1367 hot path); the `expect` is unreachable because the
  map is `TypeId`-keyed.
- `try_resource_2_mut` (`world.rs:803-807`) performs **both** `contains_key`
  checks before delegating to `resource_2_mut` — the #465 partial-acquire
  window is closed.

### 5. System & Scheduler Wiring — PASS

- Blanket `impl<F: FnMut(&World, f32) + Send + Sync> System for F` (`system.rs:49`).
- Cross-stage mutation visibility pinned by `mutation_visible_across_stages`;
  empty-scheduler / empty-stage behaviour pinned by `empty_scheduler_runs_cleanly`
  and `empty_stages_skipped`.
- `system_names()` yields stage order then parallel-before-exclusive;
  `add_*` warns on duplicates, `try_add_*` returns `Err(name)` across the flat
  name space (#312), pinned by four `try_add_*_rejects_duplicate*` tests.
- Fail-fast panic policy is documented at `scheduler.rs:425-452` (TS-08 / #1412).
  `run` takes `&mut self`; `Scheduler` is **not** a `Resource` — the only
  `impl Resource` in `scheduler.rs` is `SchedulerSystemTimings` (#868 intact).
- `#1647`: the per-system timing tracker is `None` unless the
  `SchedulerSystemTimings` resource exists — pinned by
  `system_timings_collected_only_when_resource_present`.

### 5b. Scheduler Access Declarations — PASS

- Stages are exactly `Early(0) → Update(1) → PostUpdate(2) → Physics(3) → Late(4)`
  iterated via `BTreeMap<Stage, _>`; no `ParallelUpdate`/`LateExclusive`.
  Pinned by `all_five_stages_run_in_order` (#1238).
- `Access` is the declaration type (`access.rs`), three states
  (declared-empty / declared-with-claims / `None`), default `None` on both
  `System::access` and the per-entry override.
- `AccessConflict` has exactly three variants (`None`, `Unknown`, `Conflict`) —
  no `Parallel` variant (#1521 wording fix holds). `analyze_pair` returns
  `Unknown` when either side is undeclared, pinned by
  `undeclared_closure_pairs_show_as_unknown`.
- `access_report` pairs only parallel-stage systems; exclusives are listed but
  never paired, pinned by `exclusive_systems_are_listed_but_not_paired`.
  `add_exclusive_with_access` / `try_add_exclusive_with_access` exist (#1236)
  and the live schedule still uses plain `add_exclusive` for most exclusives by
  design — not a finding.
- **Boot guard intact and strengthened**: `byroredux/src/boot.rs:1006-1027`
  runs three `debug_assert_eq!`s after building the schedule —
  `undeclared_parallel_count() == 0` (#1394), `known_conflict_count() == 0`
  (#1602) and `unknown_pair_count() == 0`. Every parallel-batch registration in
  `build_scheduler()` uses `add_to_with_access`. Operator path
  (`sys.accesses` → `SchedulerAccessReport` resource) is wired at
  `boot.rs:1042` / `commands/world_info.rs:308`.

### 6. Unsafe Code Review — PASS

- Exactly four `unsafe` blocks in `crates/core/src/ecs/`, all in `query.rs`
  (lines 64, 135, 143, 289), all #1367 cached-pointer derefs, each with a
  SAFETY comment tying validity to the live guard. No new unguarded `unsafe`.
- `World::spawn` uses `checked_add` + panic on `EntityId` overflow (#36);
  `despawn` does not reclaim IDs and documents why (#372).

### 7. Component Lifecycles — PASS (one finding, see ECS-2507-01)

- **M40 streaming**: `cell_loader/unload.rs:199` calls `World::despawn(eid)`,
  which walks *every* registered storage — no per-type removal list to fall out
  of sync, so no orphaned `CharacterController` / `LightFlicker` / behaviour
  components after a load/unload cycle. Physics handles are released before the
  despawn loop (`unload.rs:174-199`).
- **M42 procedure runtimes**: all seven behaviour/state component pairs are
  `SparseSetStorage`; all seven systems are registered `add_exclusive(Stage::PostUpdate, …)`
  behind their own env-var gate (`boot.rs:752-840`:
  `BYRO_SANDBOX_SIT` / `BYRO_WANDER` / `BYRO_TRAVEL` / `BYRO_FOLLOW` /
  `BYRO_ESCORT` / `BYRO_GUARD` / `BYRO_PATROL`); none is registered
  unconditionally.
- One-shot terminal markers hold: `sandbox.rs:206` skips `Seated`,
  `travel.rs:144` skips `Traveled`, `escort.rs:147` skips `Escorted`, each
  pinned by a "must not move on later ticks" test.
- **Scripting transients**: `event_cleanup_system` (`crates/scripting/src/cleanup.rs:30-50`)
  drains 12 marker types, registered `add_exclusive(Stage::Late, …)`
  (`boot.rs:993`). `timer_tick_system` removes the `ScriptTimer` on expiry
  before inserting `TimerExpired`, so no negative-time accumulation.
- **AnimationClipRegistry** (#790/#866): `get_or_insert_by_path` case-folds the
  key in place and memoises by lowercased path — cell streaming cannot grow the
  registry per load.
- **AudioWorld**: `OneShotSound` is removed after dispatch
  (`crates/audio/src/lib.rs:975-978`) and emitters are pruned on
  `PlaybackState::Stopped` (`:1044-1060`) — no infinite-marker leak.
- **DebugDrainSystem** is `add_exclusive(Stage::Late, …)`; per-client threads
  only enqueue.
- **Particle emitter**: `apply_emitter_params` override semantics still pinned
  by `apply_emitter_params_size_defaults_base_scale_to_one` and
  `apply_emitter_params_overrides_kinematics_and_size_not_color`.

### 8. Hot-Path Performance Invariants — PASS

- **#823**: the `held_others: Vec` build **and** the `record_and_check` call are
  inside a single `#[cfg(debug_assertions)]` block in both `track_read`
  (`lock_tracker.rs:83-92`) and `track_write` (`:125-134`). Release builds skip
  the allocation entirely.
- **#824**: `animation_system` refills `NameIndex.map` in place
  (`byroredux/src/systems/animation.rs:420-425` — `clear()` + `reserve()` +
  reinsert), not `new()` + `swap`.
- **#825 + #1371**: `make_transform_propagation_system`
  (`crates/core/src/ecs/systems.rs:41-253`) keys on
  `(Transform::len, Parent-len-or-0, next_entity_id)` **plus** `Parent`/`Children`
  `structural_generation()`, drains via `drain_dirty_into(&mut transform_dirty)`
  (not `take_dirty`), and early-returns when the dirty set is empty and the full
  state is unchanged. Pinned by `change_detection_skips_when_nothing_moved` and
  `change_detection_reacts_to_reparent_overwrite`. Verified the fast path is not
  silently dead: no system can spawn (`World::spawn` needs `&mut World`, systems
  get `&World`), so `next_entity_id` is stable frame-to-frame in steady state —
  every `world.spawn()` under `byroredux/src/systems/` is inside a `#[cfg(test)]`
  module.
- **#828**: `events` / `seen_labels` / `stack_events` / `channel_names` live on
  the hoisted `AnimScratch` (`animation.rs:320-356`) and are `clear()`ed per
  entity, not re-`Vec::new`'d. `ensure_subtree_cache` / `write_root_motion` /
  `apply_bool_channels` and the 5-arm `write_lazy!` macro are all still present
  — no DRY-undo drift.
- **#932**: `footstep_system` (`byroredux/src/systems/audio.rs:166-200`) takes
  the `FootstepScratch.triggers` Vec via `mem::take` and restores it; no
  per-frame `Vec::new`.
- **#466**: the `type_names` side-table is populated at both storage-creation
  sites (`register`, `storage_write`) and consumed by `despawn` and
  `clear_entities`.
- **GlobalTransform dirty set is drained**: `make_world_bound_propagation_system`
  (`byroredux/src/systems/bounds.rs:57-63`) drains it *before* any early return,
  and is registered unconditionally at `boot.rs:858` — no unbounded dirty-Vec
  growth.

### 9. NIFAL Canonical Material in the Component Layer — PASS

- `Material::metalness` / `Material::roughness` are plain `f32`
  (`crates/core/src/ecs/components/material.rs:245-251`), not `Option<f32>`.
- `Material::resolve_pbr` (`:741-767`) is the only fill-the-gap helper; it uses a
  NaN sentinel, fills only the unset slot, then clamps
  (`metalness ∈ [0,1]`, `roughness ∈ [0.04,1]`) — idempotent. Pinned by
  `resolve_pbr_is_idempotent`, `resolve_pbr_preserves_upstream_translator_values`,
  `resolve_pbr_fills_only_missing_slot`, `resolve_pbr_clamps_authored_out_of_range`.
- No per-draw classifier survives: `byroredux/src/render/static_meshes.rs:317`
  explicitly documents "no per-draw keyword scan / classify_pbr fallback", and
  the only `classify_pbr_keyword` call sites are `resolve_pbr` plus the
  test/`classify_legacy_pbr` helpers in `material.rs` itself.

---

## Findings

### ECS-2507-01: Per-cell wholesale `SeatReservations` clear frees seats still held by actors in other loaded cells

- **Severity**: MEDIUM
- **Dimension**: 7 — Component Lifecycles (M42 seat claims)
- **Location**: `byroredux/src/cell_loader/references/mod.rs:195-197`; consumer at `byroredux/src/systems/sandbox.rs:206-217`
- **Status**: NEW
- **Description**: `load_references` clears the entire `SeatReservations` set on
  every invocation. `load_references` is called **once per cell**, on both the
  interior path (`cell_loader/load.rs:377`) and the exterior grid path
  (`cell_loader/exterior.rs:418`, inside the per-`(gx, gy)` cell loader). On an
  exterior grid load with `--radius 3` that is 49 wholesale clears; during
  boundary-crossing streaming it happens again for every newly-streamed cell
  while previously-loaded cells (and their seated actors) are still resident.
  `Seated` is a one-shot terminal marker, so an actor that already sat never
  re-claims its marker after the clear — the seat is permanently released while
  still physically occupied, and the next unseated actor within
  `SEAT_SEARCH_RADIUS` can claim the same `(furniture entity, marker index)`.
- **Evidence**:
  ```rust
  // byroredux/src/cell_loader/references/mod.rs:195
  if let Some(mut r) = world.try_resource_mut::<crate::components::SeatReservations>() {
      r.0.clear();
  }
  ```
  ```rust
  // byroredux/src/systems/sandbox.rs:206-217 — claims are never re-asserted
  let mut reservations = world.resource_mut::<SeatReservations>();
  for (npc, behavior) in sandbox_q.iter() {
      if seated_q.as_ref().is_some_and(|s| s.contains(npc)) {
          continue; // already seated (one-shot guard) — never re-inserts its claim
      }
      …
      reservations.0.insert(seat_id);
  ```
  The in-code rationale is also factually wrong: the comment at
  `references/mod.rs:189` says "clear stale seat reservations from the previous
  cell (entity ids reset on unload)". Entity IDs are **never** reset or reclaimed
  — `World::despawn` explicitly documents this (`crates/core/src/ecs/world.rs:114-118`,
  #372) and `next_entity` only ever grows. Stale entries can therefore never
  alias a new furniture entity; the clear is only preventing a slow set-growth
  leak, at the cost of correctness.
- **Impact**: Two NPCs occupying the same furniture marker in an exterior
  multi-cell scene. Gated behind `BYRO_SANDBOX_SIT` (off by default), so no
  default-configuration impact today, but it silently breaks the per-marker
  exclusivity that `SeatReservations` exists to provide as soon as M42 seating
  is turned on for exteriors.
- **Related**: #372 (IDs never reclaimed); the M42 seat-claim design in
  `docs/engine/npc-spawn-ai-packages.md`.
- **Suggested Fix**: Replace the wholesale `clear()` with a targeted prune —
  retain only reservations whose furniture entity still exists
  (`r.0.retain(|(furn_e, _)| world.has::<Furniture>(*furn_e))`), which is both
  leak-free and cross-cell-safe. Alternatively, have `sandbox_seat_system`
  re-assert each `Seated` actor's claim each tick (store the claimed `seat_id`
  on the `Seated` marker) so a clear is self-healing. Fix the "entity ids reset
  on unload" comment either way.

---

### ECS-2507-02: `SparseSetStorage::sparse` is sized by the monotonic `EntityId` high-water mark and never shrinks

- **Severity**: MEDIUM
- **Dimension**: 2 / 8 — Storage Correctness + Hot-Path / Memory
- **Location**: `crates/core/src/ecs/sparse_set.rs:11-13, 58-63, 77-110, 146-151`
- **Status**: NEW
- **Description**: `SparseSetStorage.sparse: Vec<Option<u32>>` is indexed
  directly by `EntityId` and grown with `resize(idx + 1, None)` on insert. It is
  **never** truncated or shrunk: `remove` writes `None` into the slot but leaves
  the `Vec` length untouched, and `clear_erased` calls `.clear()` (which keeps
  capacity). Because entity IDs are deliberately never reclaimed
  (`World::despawn`, #372) and `next_entity` only grows, every sparse-set storage
  that ever receives an insert near the current high-water mark permanently
  retains `8 bytes × high_water_mark` of RAM regardless of how few components
  are actually live. There are **122** `SparseSetStorage<Self>` component
  declarations in the workspace; the ones attached to nearly every spawned
  entity (`Name`, `FormIdComponent`, `MeshHandle`, `Parent`, `Children`,
  `RenderLayer`, `CollisionShape`, …) all track the global high-water mark.
- **Evidence**:
  ```rust
  // sparse_set.rs:60-63 — only growth, no counterpart shrink
  if idx >= self.sparse.len() {
      self.sparse.resize(idx + 1, None);
  }
  ```
  ```rust
  // sparse_set.rs:88 — remove clears the slot but not the length
  self.sparse[idx] = None;
  ```
  ```rust
  // sparse_set.rs:147 — clear() retains capacity
  self.sparse.clear();
  ```
  `grep -rn "shrink" crates/core/src/ecs/*.rs` returns nothing — there is no
  compaction API anywhere in the ECS core. Cell unload
  (`cell_loader/unload.rs:199`) goes through `World::despawn`, which never
  touches `next_entity`.
- **Impact**: RAM growth proportional to (cumulative entities ever spawned) ×
  (number of sparse component types touched), *independent of live entity
  count*. `Option<u32>` has no niche, so each slot is 8 bytes: a 2 M-ID
  high-water mark costs ~16 MB per affected storage, i.e. a few hundred MB
  across a dozen commonly-attached sparse components. This is exactly the shape
  of a long exterior-streaming session (repeated cell load → despawn → load at
  ever-higher IDs) and it is invisible to `cargo test`. Against the
  `docs/engine/memory-budget.md` "under ~4 GB total" target this is material,
  though it is a slow accumulation, not a per-frame leak.
- **Related**: #372 (IDs never reclaimed — the documented decision this
  interacts with; do **not** "fix" by reusing IDs). `PackedStorage` is unaffected
  (its `entities`/`data` are sized by live count).
- **Suggested Fix**: Two independent, low-risk mitigations. (a) Halve the
  per-slot cost by replacing `Vec<Option<u32>>` with `Vec<u32>` plus a
  `u32::MAX` sentinel (4 bytes/slot, same O(1) semantics). (b) Add a
  `shrink_sparse_tail()` that truncates trailing `None` slots plus a
  `shrink_to_fit()`, exposed on `DynStorage` and invoked once per cell-unload
  from `unload_cell` — cheap (a backwards scan) and only runs at load
  boundaries. Also make `clear_erased` call `shrink_to_fit()` so a save-load
  actually returns the memory.

---

### ECS-2507-03: `query`/`query_mut`/`query_2_mut*` defuse the tracker scope before the wrapper's fallible downcast, orphaning the tracker row on panic

- **Severity**: LOW
- **Dimension**: 1 — Lock Ordering & Deadlock (tracker hygiene)
- **Location**: `crates/core/src/ecs/world.rs:396-397, 417-418, 466-471, 530-535, 544-550`; panic site `crates/core/src/ecs/query.rs:44-48, 116-120`
- **Status**: NEW
- **Description**: `World::query` calls `scope.defuse()` **before** constructing
  `QueryRead`, and `QueryRead::new` performs
  `downcast_ref::<T::Storage>().expect("storage type mismatch (bug in World)")`.
  If that `expect` ever fired, the tracker row would already be un-owned by the
  `TrackedRead` scope and not yet owned by a `QueryRead` (whose `Drop` is the
  only untrack path), leaving a stale entry in the thread-local `LOCKS` map. A
  later acquisition on the same thread after a `catch_unwind` would then report
  a spurious "ECS deadlock detected". This is the exact failure mode #137 fixed,
  and `World::get` (`world.rs:288-300`) gets the ordering right — it defuses only
  inside the `Some` arm, after `ComponentRef::new` has returned. `query`,
  `query_mut`, `query_2_mut`, `query_2_mut_mut` are inconsistent with it.
- **Evidence**:
  ```rust
  // world.rs:394-397 — defuse precedes the fallible construction
  let scope = lock_tracker::TrackedRead::new(type_id, std::any::type_name::<T>());
  let guard = lock.read().unwrap_or_else(|_| storage_lock_poisoned::<T>());
  scope.defuse();
  Some(QueryRead::new(guard, type_id))   // <- .expect() inside
  ```
  vs. the correct shape in `World::get`:
  ```rust
  match ComponentRef::new(guard, entity, type_id) {
      Some(cr) => { scope.defuse(); Some(cr) }
      None => None,   // scope drops → untrack
  }
  ```
- **Impact**: None in practice — the downcast can only fail if `World.storages`
  maps a `TypeId` to a storage that is not `T::Storage`, which is impossible by
  construction (`storage_write`/`register` create `T::Storage::default()` under
  `TypeId::of::<T>()`, and two distinct types cannot share a `TypeId`). This is
  a defense-in-depth / consistency gap on an unreachable path, not a live bug.
  The poison path *is* handled correctly (the panic happens inside
  `unwrap_or_else`, before `defuse`).
- **Related**: #137 (`TrackedRead`/`TrackedWrite` RAII scopes),
  `lock_tracker::is_clean()` test helper.
- **Suggested Fix**: Move the downcast + `expect` out of `QueryRead::new` /
  `QueryWrite::new` into a fallible `try_new` and defuse only on success, or
  simply reorder so `defuse()` runs after the wrapper is constructed
  (`let q = QueryRead::new(guard, type_id); scope.defuse(); Some(q)`), matching
  `World::get`.

---

### ECS-2507-04: `docs/contributing.md` claims three CI jobs; `ci.yml` has five

- **Severity**: LOW
- **Dimension**: 1 — discovered while verifying the ABBA-detector CI guard (#1410)
- **Location**: `docs/contributing.md:264-273` vs `.github/workflows/ci.yml`
- **Status**: NEW
- **Description**: The CI table states "Three jobs run on every push / PR" and
  lists `cargo-test`, `lock-order-check`, `vulkan-validation`. `ci.yml`
  currently defines five jobs: `shader-artifacts` (line 23, added in `ca7a4e0e`),
  `cargo-test` (45), `lock-order-check` (75), `nif-heap-allocation-bounds` (105)
  and `vulkan-validation` (131). The closing sentence "CI passes if: all unit
  tests pass, no clippy warnings, no ABBA cycles detected, and no Vulkan
  `ERROR`-severity validation messages fire" also omits the shader-artifact
  parity check and the NIF heap-allocation bounds.
- **Evidence**: `grep -n "^  [a-z-]*:$" .github/workflows/ci.yml` →
  `shader-artifacts`, `cargo-test`, `lock-order-check`,
  `nif-heap-allocation-bounds`, `vulkan-validation`.
- **Impact**: Documentation only. A contributor reading `contributing.md` will
  not know that a shader-SPIR-V drift or a NIF allocation-budget regression is a
  hard CI failure. The ECS-relevant guard (`lock-order-check`) is correctly
  documented and correctly blocking.
- **Related**: #1410 (ABBA detector in CI — verified closed and blocking).
- **Suggested Fix**: Update the table to five rows and extend the pass criteria
  sentence. One-line doc edit.

---

## Prioritized Fix Order

1. **ECS-2507-01** (MEDIUM) — seat-reservation cross-cell clear. Real
   correctness bug with a small, well-scoped fix (`retain` instead of `clear`),
   and the misleading "entity ids reset on unload" comment should go with it.
   Do this first because it is the only *behavioural* defect in the report.
2. **ECS-2507-02** (MEDIUM) — `SparseSetStorage` sparse-tail growth. Larger
   blast radius (122 component types, all games, long sessions) but slower to
   bite and needs a design decision between the `u32`-sentinel narrowing and the
   unload-time shrink hook. Ship (a) and (b) independently; (a) alone halves the
   cost with no API change.
3. **ECS-2507-03** (LOW) — tracker-scope defuse ordering. Two-line change per
   site; worth doing while the #137 pattern is fresh, purely to make all
   acquisition sites structurally identical.
4. **ECS-2507-04** (LOW) — `contributing.md` CI table. Trivial doc edit; bundle
   with any other docs pass.

---

*Next step:* `/audit-publish docs/audits/AUDIT_ECS_2026-07-25.md`
