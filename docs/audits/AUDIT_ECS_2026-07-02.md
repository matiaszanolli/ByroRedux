# ECS Core Audit — 2026-07-02

Daily re-run of the ByroRedux ECS audit (`crates/core/src/ecs/`) plus its
cross-cutting lifecycle, hot-path, and NIFAL-material consumers, across all
9 `/audit-ecs` dimensions. Method per `_audit-common.md`: green-test baseline
gate, then each dimension re-verified top-down (lock ordering first) against
**current code** — every candidate finding's premise independently checked
before inclusion (stale-finding hygiene). Baseline for delta focus: the clean
0 CRITICAL / 0 HIGH / 1 MEDIUM / 2 LOW result in `AUDIT_ECS_2026-07-01.md`.

**Delta since last audit**: `crates/core/src/ecs/` itself is byte-identical
to yesterday's snapshot (last touching commit still `49d3745a`, 2026-06-28 —
already reviewed as part of finding `ECS-2026-07-01-01`). Seven commits landed
since then in *adjacent* consumers: `byroredux/src/npc_spawn.rs`,
`byroredux/src/cell_loader/references.rs`, `byroredux/src/commands/`, and
`crates/scripting/src/condition.rs` (CHARAL character-ruleset wiring). This
audit's scrutiny focused on those.

## Executive summary

**4 findings: 0 CRITICAL · 0 HIGH · 1 MEDIUM · 3 LOW.**

The ECS core's high-blast-radius guarantees all hold: lock ordering / deadlock
protection (dim 1), storage correctness (dim 2), query borrow safety (dim 3),
scheduler access declarations (dim 5b), the transform-propagation change-
detection fast path (dim 8), and the NIFAL plain-`f32` material contract
(dim 9) are intact with no regressions. The one MEDIUM (carried over,
unfixed) is the `ActorValues` save-registry gap from yesterday. One NEW LOW
was found: the same "stamped-at-spawn, unregistered-for-save" pattern now
also applies to four CHARAL/faction components (`CharacterLevel`, `Perks`,
`Background`, `FactionRanks`) wired into the live NPC spawn path this week —
currently harmless (write-once from ESM data, deterministically re-derived on
reload) but structurally the same gap that will become a MEDIUM the moment any
runtime mutator (leveling, perk-granting, faction-rank commands) lands, which
CHARAL's own roadmap makes near-term. The two other LOWs are the carried-over
poison-handling diagnostics deviations in `World` (`clear_entities`,
`insert_resource`).

**Test baseline:** `cargo test -p byroredux-core` → **467 passed, 0 failed**
(13 ignored; +2 tests vs. yesterday's 465, from CHARAL-adjacent unit tests —
no regressions). `cargo test -p byroredux` → **521 passed, 0 failed**
(unchanged, GPU/game-data suites ignored as designed).

## Findings

### ECS-2026-07-02-01: `ActorValues` still absent from the M45 save registry — save→load silently reverts actor-value edits
- **Severity**: MEDIUM
- **Dimension**: 7 — Component Lifecycles (save/load)
- **Location**: `byroredux/src/save_io.rs:81-88` (`MUTABLE_DELTA_COLUMNS`), `byroredux/src/save_io.rs:155-186` (`build_save_registry`), `crates/core/src/ecs/components/actor_values.rs:66-69`
- **Status**: Regression of nothing — carried over unfixed from `ECS-2026-07-01-01` (component landed 2026-06-28 in `49d3745a`)
- **Description**: Unchanged from yesterday's audit. `ActorValues` derives only
  `Debug, Clone, Default` (`actor_values.rs:66`) — no `serde` — and is absent
  from both `build_save_registry()`'s 11 registered components and
  `MUTABLE_DELTA_COLUMNS`. It is stamped on every NPC placement root at spawn
  (`byroredux/src/npc_spawn.rs:571`, `stamp_actor_values`) and mutated at
  runtime by the `setav`/`modav` console commands
  (`byroredux/src/commands/actor_value.rs:20-46`), and is now also read by the
  `GetActorValue` condition function (`crates/scripting/src/condition.rs`).
- **Evidence**:
  ```rust
  // actor_values.rs:66 — no serde derive
  #[derive(Debug, Clone, Default)]
  pub struct ActorValues { .. }
  ```
  ```rust
  // npc_spawn.rs:570-572 — stamped at spawn, alongside the new CHARAL stamps
  stamp_faction_ranks(world, placement_root, npc);
  stamp_actor_values(world, placement_root, npc, index, game);
  stamp_character_components(world, placement_root, npc);
  ```
  `save_io.rs:166-184` lists Transform/Name/Parent/Children/Inventory/
  EquipmentSlots/LightSource/LightFlicker/AnimationPlayer/AnimationStack/
  ScriptTimer + 3 resources — no `ActorValues`.
- **Impact**: A save→load cycle drops every permanent/temporary/damage layer
  and any console-edited base; the reloaded cell re-derives only the class
  auto-calc base at NPC respawn. `GetActorValue` conditions evaluate against
  the freshly-rederived (not the edited) value post-load. Blast radius is
  still bounded to the console-command mutation source today, but widens with
  every perk/magic-effect system that composes into `ActorValues` per its
  module docs.
- **Related**: #1663 (component introduction); `ECS-2026-07-01-01` (same
  finding, prior audit); `ECS-2026-07-02-02` below (same pattern, new
  components); `delta_columns_carry_only_session_stable_fields` tripwire test
  (`save_io.rs:716`).
- **Suggested Fix**: Add `#[cfg_attr(feature = "save", derive(serde::Serialize,
  serde::Deserialize))]` (matching `ScriptTimer`) to `ActorValue`/`ActorValues`,
  register it in `build_save_registry()`, and add `"ActorValues"` to
  `MUTABLE_DELTA_COLUMNS` — then extend the tripwire test's pinned set.

### ECS-2026-07-02-02: Four new CHARAL/faction components stamped at NPC spawn share `ActorValues`' save-registry gap (currently dormant, will activate the moment any mutator lands)
- **Severity**: LOW
- **Dimension**: 7 — Component Lifecycles (save/load)
- **Location**: `crates/core/src/character/components.rs:16` (`CharacterLevel`), `:44` (`Perks`), `:91` (`Background`), `crates/core/src/ecs/components/faction_ranks.rs:26` (`FactionRanks`); write sites `byroredux/src/npc_spawn.rs:570,572` (`stamp_faction_ranks`, `stamp_character_components`); registry gap `byroredux/src/save_io.rs:155-186`
- **Status**: NEW
- **Description**: This week's CHARAL wiring (`b4ee7bfe` and follow-ups) made
  `spawn_npc_entity` stamp four more live-shaped components on every NPC
  placement root: `FactionRanks`, `CharacterLevel`, `Background`, and `Perks`
  (the last only when the NPC has FO4+ `PRKR` data). None derive `serde`; none
  are registered in `build_save_registry()`. `crates/scripting/src/condition.rs`
  now reads three of them (`GetLevel`/`GetXPForNextLevel` → `CharacterLevel`,
  `GetIsClass`/`GetIsRace` → `Background`, `GetFactionRank` → `FactionRanks`).
  **Verified against source**: unlike `ActorValues`, none of these four types
  has a runtime write path anywhere in production code today — no
  `world.get_mut::<CharacterLevel/Perks/Background/FactionRanks>` call exists
  outside test modules, and no console command (`setlevel`/`addperk`/
  `setfactionrank`, etc.) exists. They are written exactly once, at spawn,
  from static ESM `NPC_` data, so a save→load cycle re-derives byte-identical
  values from the same ESM on NPC respawn — **the round trip is a no-op, not a
  data-loss bug, today.**
- **Evidence**:
  ```rust
  // components.rs:16 — no serde, same shape as ActorValues
  #[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
  pub struct CharacterLevel { pub level: u16, pub xp: u32 }
  ```
  ```rust
  // npc_spawn.rs:110-141 — stamp_character_components: three world.insert
  // calls (CharacterLevel, Background, conditional Perks), zero world.get_mut
  ```
  `grep` for `get_mut::<CharacterLevel>|get_mut::<Perks>|get_mut::<Background>|
  get_mut::<FactionRanks>` across `byroredux/src/` and `crates/` returns no
  production matches — confirms write-once-at-spawn.
- **Impact**: None today (verified no active mutator). But `CharacterLevel.xp`
  documents itself as "resets on level-up" and `Perks` as "iterated by the
  perk entry-point modifier pipeline" — both signal near-term runtime mutation
  (leveling, perk-granting) is the intended next step for CHARAL, per its own
  module docs and the roadmap direction. The moment either lands, this becomes
  the same class of bug as `ECS-2026-07-02-01`/`ECS-2026-07-01-01` (silent
  save→load revert of player-earned progress), and unlike `ActorValues` there
  would be *four* types to fix at once instead of one.
- **Related**: `ECS-2026-07-02-01` (same pattern, active for `ActorValues`);
  CHARAL character abstraction layer (`docs/engine/charal.md`); the M45 save
  registry's tripwire test (`save_io.rs:716`, `delta_columns_carry_only_
  session_stable_fields`) only guards `MUTABLE_DELTA_COLUMNS` entries already
  in the map — it has no coverage check that would have flagged an
  unregistered-but-spawn-stamped component type, so this class of gap is
  structurally invisible to CI until a save round-trip test exercises it.
- **Suggested Fix**: No urgent action required (no data loss today). When
  wiring the first runtime mutator for any of these four types (leveling XP
  gain, `AddPerk`/perk-rank commands, faction-rank changes), register that
  type in `build_save_registry()` + `MUTABLE_DELTA_COLUMNS` in the *same*
  commit that adds the mutator — do not let the pattern repeat a third time.
  Consider a lint/test that enumerates every component `npc_spawn.rs` stamps
  and cross-checks it against the save registry or an explicit "intentionally
  derived, not saved" allowlist (mirroring how `CharacterRuleset`, a resource
  correctly excluded because it's fully reconstructable from plugin data at
  cell load, would pass such a check while these four would not once they gain
  a mutator).

### ECS-2026-07-02-03: `clear_entities` poison panic drops the component type name (bypasses the #466 side-table)
- **Severity**: LOW
- **Dimension**: 1 — Lock Ordering & Deadlock (poison-on-panic resolution)
- **Location**: `crates/core/src/ecs/world.rs:227-233`
- **Status**: Carried over unfixed from `ECS-2026-07-01-02`
- **Description**: `World::despawn` resolves type-erased poison through the
  `type_names` side-table so the panic names the offending component (#466,
  `world.rs:121-136`). `clear_entities` — used on the M45 restore path, run on
  every full snapshot load — still iterates `self.storages.values_mut()` and
  panics with the generic message `"storage lock poisoned during
  clear_entities"`, discarding the TypeId key it could use to resolve the
  name. It still fails loud (the policy's core guarantee holds), but loses the
  diagnostic the side-table was built to provide.
- **Evidence**:
  ```rust
  // world.rs:230
  .unwrap_or_else(|_| panic!("storage lock poisoned during clear_entities"))
  ```
  vs. `despawn`'s `self.storages.iter_mut()` + `self.type_names.get(type_id)` +
  `storage_lock_poisoned_erased(type_name)`.
- **Impact**: On a poisoned-lock cascade during a save load, the panic cannot
  say *which* storage poisoned — the exact "10× harder bisect" #466 fixed for
  `despawn`. Post-panic-only path, so no runtime correctness impact.
- **Related**: #466; `despawn_poisoned_lock_panics_with_type_name`
  (`world_tests.rs:1256`); `ECS-2026-07-01-02`.
- **Suggested Fix**: Iterate `self.storages.iter_mut()`, resolve the name via
  `self.type_names.get(type_id)`, and call `storage_lock_poisoned_erased(name)`
  — a three-line change mirroring `despawn`.

### ECS-2026-07-02-04: `insert_resource` silently swallows a poisoned prior-value lock instead of re-panicking loud
- **Severity**: LOW
- **Dimension**: 4 — Resource Lifetimes (poison-on-panic resolution)
- **Location**: `crates/core/src/ecs/world.rs:541-550`
- **Status**: Carried over unfixed from `ECS-2026-07-01-03`
- **Description**: The documented poison policy (dim 1 checklist; pinned by
  `poisoned_resource_lock_panics_with_type_name`) is that a post-panic lock
  access re-panics loud with the type name. `remove_resource` follows it
  (`resource_lock_poisoned::<R>()`, `world.rs:557-559`). `insert_resource`
  still does not: when replacing an existing resource whose lock was poisoned,
  the chain `old.and_then(|lock| lock.into_inner().ok().and_then(|boxed|
  boxed.downcast::<R>().ok()).map(|b| *b))` maps the `PoisonError` to `None` —
  indistinguishable from "no prior value existed."
- **Evidence**: `world.rs:541-550` vs. `remove_resource` at `world.rs:555-561`.
- **Impact**: Post-panic-only. No torn-state read occurs (the old value is
  dropped, not dereferenced, and the replacement installs a fresh un-poisoned
  `RwLock`), but a recovery path that replaces a resource after a caught panic
  gets a silent `None` where the sibling API panics loud — masking the
  original failure. Diagnostics/consistency gap under the fail-fast policy.
- **Related**: #466 (loud-poison policy), TS-08/#1412 (fail-fast);
  `ECS-2026-07-01-03`; `ECS-2026-07-02-03`.
- **Suggested Fix**: Resolve the old lock via
  `.unwrap_or_else(|_| resource_lock_poisoned::<R>())` and the downcast via
  `.expect("resource type mismatch")`, matching `remove_resource`.

## Per-dimension verification results

| Dim | Area | Verdict | Key evidence (current code) |
|----:|------|---------|------------------------------|
| 1 | Lock Ordering & Deadlock | ✅ intact (2 LOW diagnostics deviations, carried over) | All single-lock sites in `world.rs` arm `TrackedRead`/`TrackedWrite` before acquiring and `defuse()` after. `query_2_mut`/`query_2_mut_mut`/`resource_2_mut` acquire **and arm tracker scopes** in TypeId-ascending order in both branches (`world.rs:522-536` verified directly) — #313 fix holds. `assert_ne!` same-type guards present on all three pair APIs. Poison resolves through `storage_lock_poisoned*` / `resource_lock_poisoned` at every query/resource acquisition; `despawn` uses the #466 `type_names` side-table (confirmed `world.rs:121-136` unchanged). |
| 2 | Storage Correctness | ✅ intact | SparseSet swap-remove fixes the moved entity's sparse pointer; last-element takes the no-swap path. Packed `binary_search` on every insert/remove; `insert_bulk` = reserve + append + stable index-sort + last-writer-wins in-place dedup — re-read `packed.rs:152-233` directly, dedup swap loop only ever writes at or below the current read index (`write <= last_in_run` invariant), confirmed sound. `structural_gen` bumps on insert/remove/overwrite. `drain_dirty_into` preserves `self.dirty` capacity (#1371). |
| 3 | Query Borrow Safety | ✅ intact | `QueryRead`/`QueryWrite`/`ComponentRef` retain the guard field for the wrapper lifetime and cache the downcast pointer once in `new()` (#1367); all 4 `unsafe` blocks in `query.rs` (lines 64, 135, 143, 289) carry SAFETY comments tying validity to the live guard — grepped and read directly this audit. `query`/`query_mut` return `None` via `.get(&type_id)?` — no lazy storage creation on the read path. |
| 4 | Resource Lifetimes | ✅ intact (1 LOW carried over, above) | `resource`/`resource_mut` panic with type name; `try_*` return `None`. `insert_resource` returns prior value (poison-swallow LOW noted); `remove_resource` resolves poison loud. `try_resource_2_mut` performs BOTH existence checks before acquiring EITHER lock (#465). |
| 5 | System & Scheduler Wiring | ✅ intact | `mutation_visible_across_stages`, `empty_scheduler_runs_cleanly`, `empty_stages_skipped`, `all_five_stages_run_in_order` all present and passing (`scheduler.rs:834,873,978,987`). Fail-fast panic policy intentional (TS-08/#1412), not flagged. |
| 5b | Scheduler Access Declarations | ✅ intact | Stages exactly `Early→Update→PostUpdate→Physics→Late`. `AccessConflict` has exactly `None`/`Unknown`/`Conflict` — no `Parallel` variant, re-read `access.rs:139-152` directly. Boot guard triple-gates `undeclared_parallel_count()==0` + `known_conflict_count()==0` + `unknown_pair_count()==0` (`main.rs:1013-1035`, confirmed unchanged). `undeclared_closure_pairs_show_as_unknown` + `exclusive_systems_are_listed_but_not_paired` pins present (`scheduler.rs:1141,1198`). |
| 6 | Unsafe Code Review | ✅ intact | Exactly 4 `unsafe` expressions in the ECS core, all in `query.rs`, each with a SAFETY comment. `spawn` uses `checked_add` + overflow panic (#36); `despawn` does not reclaim IDs — documented intentional (#372). |
| 7 | Component Lifecycles | ⚠️ 2 findings (1 MEDIUM carried over + 1 NEW LOW, both above); otherwise intact | Streaming (`streaming.rs`), transient markers (`event_cleanup_system`), `ScriptTimer`, `AnimationClipRegistry` case-folding (#790), `DebugDrainSystem`, `AudioWorld` field-drop order, particle emitter param overrides — all re-verified unchanged since 2026-06-27/07-01. `CharacterRuleset` (installed `cell_loader/references.rs`) correctly excluded from the save registry as a derived-at-cell-load resource, not stamped state — confirmed the install site checks `is_none()` before inserting (idempotent per cell). |
| 8 | Hot-Path Perf Invariants | ✅ intact | Lock-tracker `held_others` Vec gated `cfg(debug_assertions)` (#823). `NameIndex.map` refilled in place (#824). Transform propagation fast path: `drain_dirty_into` not `take_dirty` (#825/#1371), re-read `systems.rs:90-93` directly. `footstep_system` `FootstepScratch` `mem::take`+restore (#932). New `condition.rs` read sites use `world.get`/`try_resource` patterns, no per-frame allocation introduced. Open issue #1698 (Dragonsreach scheduler stall) remains the known perf item in this area — Existing, not re-reported. |
| 9 | NIFAL Canonical Material | ✅ intact | `Material.metalness`/`roughness` plain resolved `f32` (`material.rs:217,223`), not `Option`. Exactly 2 `translate_material` call sites (`cell_loader/spawn.rs:880`, `scene/nif_loader.rs:828`) — grepped and confirmed this audit. `resolve_pbr` the only gap-filler; all four pinned tests present. |

## Deduplication

- `gh issue list` (open issues) checked: no open issue covers any of the four
  findings. #1698 (ECS scheduler stall) is the only open ECS-domain issue and
  is orthogonal (noted as Existing under dim 8).
- Prior reports in `docs/audits/` (16 ECS audits, latest `AUDIT_ECS_2026-07-01.md`)
  checked: `ECS-2026-07-02-01`, `-03`, `-04` are the same finding as
  `ECS-2026-07-01-01/02/03` respectively (unfixed, carried over — new IDs
  assigned per this report's date per the standard convention, cross-
  referenced above). `ECS-2026-07-02-02` is genuinely new: it did not exist
  before this week's CHARAL spawn-wiring commits and no prior audit could have
  found it.

## Method notes

- Every finding's premise was independently re-verified against current
  source in this session (not merely trusted from the prior report or the
  research subagent) — including direct reads of `npc_spawn.rs:95-141`,
  `save_io.rs:155-186`, `world.rs:227-233` and `:520-561`, `query.rs` unsafe
  sites, `access.rs:130-165`, `packed.rs:152-233`, and grep sweeps for any
  `get_mut`/console-command mutation path on the four new CHARAL components
  (none found — the basis for classifying `ECS-2026-07-02-02` as LOW rather
  than MEDIUM despite the structural resemblance to the `ActorValues` gap).
- A parallel research pass (ecs-specialist subagent) independently surfaced
  the same four-component gap and initially rated it MEDIUM by analogy to
  `ActorValues`; this report downgrades it to LOW after confirming — by direct
  grep for `get_mut::<CharacterLevel/Perks/Background/FactionRanks>` and for
  any `setlevel`/`addperk`/faction-rank console command — that none of the
  four currently has a runtime mutation path, so no player-caused state is at
  risk of loss today. The escalation trigger (any mutator landing) is
  documented in the finding's Suggested Fix so it doesn't get missed.
- Intentional behaviors explicitly confirmed-and-not-flagged: fail-fast
  scheduler panic policy (TS-08/#1412), non-zero `undeclared_exclusive_count`
  (#1236/#1237), `despawn` ID non-reclaim (#372), `PackedStorage` dirty-set
  duplicates (consumer contract), `CharacterRuleset` resource correctly
  excluded from save (derived/reconstructable at cell load).
- Since-last-audit delta reviewed: 7 commits, all outside
  `crates/core/src/ecs/` proper — `npc_spawn.rs` (CHARAL/faction stamp
  wiring), `cell_loader/references.rs` (idempotent `CharacterRuleset`
  resource install), `commands/` (actor-value + condition console commands,
  read/mutate via documented query APIs, no lock-order violations observed),
  `crates/scripting/src/condition.rs` (new read consumers, no ECS storage
  surface of its own).

## Next step

```
/audit-publish docs/audits/AUDIT_ECS_2026-07-02.md
```
