# NIF Parser Audit — 2026-06-14

**Scope**: Full NIF binary-format parser (`crates/nif/src/`), all 6 dimensions
(Stream Position, Version Gating, Block Dispatch Coverage, Geometry/Import
Handoff, Collision/Shader Parsing, Allocation Hygiene). General all-games audit
(no single `--game` arm), part of a `comprehensive` audit-suite sweep.

**Baseline**: HEAD `435e265d`. Dedup against open GitHub issues
(`/tmp/audit/issues.json`, 23 open) + `docs/audits/`.

**Method**: One Task agent per dimension. Empirical corpus sweeps via the
prebuilt `nif_stats` example over every game's primary mesh archive (all seven
game-data dirs present) plus `trace_block` byte-decodes and the checked-in
full-data-dir baselines. Every checklist premise and regression-guard pin was
re-verified against **current** code (symbol-based) before any finding was
written; each finding was adversarially disproven before inclusion.

> **This audit runs one day after `AUDIT_NIF_2026-06-13.md`.** That audit's
> HIGH cluster (NIF-NEW-01…05) has since been **substantially fixed by
> post-06-13 commits** — re-verified below. This pass focuses on (a) confirming
> those fixes, (b) byte-auditing the brand-new PHYSAL constraint-CInfo decode
> that landed *today* (2026-06-14), and (c) the `#1516` bitangent fix.

---

## Executive Summary

**The NIF parser is in excellent shape and has measurably improved since
yesterday.** Fresh sweeps confirm the prior audit's headline gaps are closed:

- **Starfield: 770,322 blocks / 0 `NiUnknown`** (was 1,036) — `#1510` resolved
  the `BSEffectShaderProperty` / `BSLightingShaderProperty` cluster. The prior
  audit's NIF-NEW-05 is **FIXED**.
- **Oblivion: 56 → 8 truncating files** — the `V10_1_0_108/109/110/111/112`
  version constants + `has_interp_controller_manager_controlled` /
  `has_quat_transform_trs_valid` range predicates landed (`#1506`/`#1508`), the
  `Manager Controlled` bool is now read at the correct offset, and `#1509`
  closed the v10.2 morph over-read. The prior audit's NIF-NEW-01/02/03/04
  cluster is **mostly FIXED**; only a thin residual tail remains (NIF-D1-01).
- **FO3 / FNV / Skyrim SE / FO4 / FO76: a genuine 100.0000%** (millions of
  blocks, 0 `NiUnknown`) — unchanged, re-confirmed.

**Block-type dispatch coverage is 100% for all seven games** — 254 macro-generated
match arms covering 312 distinct block-type names; zero genuinely-missing
dispatch arms. Every `NiUnknown` anywhere is a recovered drift tail in a
*dispatched* type, never a coverage hole.

**The brand-new M41.x / PHYSAL constraint-CInfo decode (`constraints.rs`,
landed today) is byte-exact.** Two independent byte audits (Dim 1 + Dim 5)
cross-checked `RagdollCInfo` / `LimitedHingeCInfo` / `MalleableConstraintCInfo`
field-for-field against nif.xml in *both* the Oblivion (`#NI_BS_LTE_16#`) and
FO3+ (`!#NI_BS_LTE_16#`) eras, and proved correctness empirically: Oblivion
`skeletonbeast.nif` (no `block_sizes` anchor, 426 blocks) parses 100% clean —
any byte error in the new decode would cascade. The brief's worry (a silent
*over*-read of the trailing motor) is disproven: the decoders always *under*-read
(the motor/strength is intentionally left for `block_size` recovery on FO3+,
which always carry the table). **No correctness finding against the new decode.**

**Findings this pass are all MEDIUM/LOW** and cluster around the new code's
*regression-detection surface* (not its bytes), a single pre-existing
mis-dispatched constraint type, a residual Oblivion sub-version tail, a
Starfield LOD shader trailing field, and three minor hygiene/fidelity items.

| Severity | Count | IDs |
|----------|-------|-----|
| CRITICAL | 0 | — |
| HIGH     | 0 | — |
| MEDIUM   | 4 | NIF-D5-01, NIF-D5-02, NIF-D5-05, NIF-D1-01 |
| LOW      | 4 | NIF-D5-03, NIF-D5-04, NIF-D4-01, NIF-D1-02 |
| **TOTAL**| **8** | |

**Clean dimensions** (0 findings): **Dim 6 (Allocation Hygiene)** — all 10 pins
hold; the new constraint code uses fixed-size stack `[f32;4]` reads with no
stream-derived `count`, so it is structurally immune to the `#388` OOM class.
**Dim 4 geometry handoff is effectively clean** — the only finding is a latent
LOW.

**Most severe finding**: NIF-D5-01 — `bhkBallSocketConstraintChain` is dispatched
through `BhkConstraint::parse` but inherits `bhkSerializable` (not
`bhkConstraint`), so it reads a 16-byte base that isn't there. (MEDIUM:
`block_size`-absorbed on its only vanilla home, Skyrim; garbage in unused
fields today; latent sizeless-cascade + future-consumer hazard.)

---

## Block Type Coverage Matrix (refreshed — fresh `nif_stats --tsv` per game)

| Game | Blocks (primary archive) | NiUnknown | Coverage |
|------|--------------------------|-----------|----------|
| Fallout 3   | 287,331   | 0 | 100.0000% |
| Fallout NV  | 492,796   | 0 | 100.0000% |
| Skyrim SE   | 665,846   | 0 | 100.0000% |
| Fallout 4   | 740,562   | 0 | 100.0000% |
| Fallout 76  | 1,548,202 | 0 | 100.0000% |
| **Starfield** | 770,322 | **0** (was 1,036) | **100.0000%** |
| Oblivion    | 329,840   | 2 (recovered tails in dispatched types; **8 files** truncate) | ~99.99% blocks / ~99.9% files |

All `NiUnknown` is parser-stride / version-drift (Dims 1/2), never a missing
dispatch arm. Dispatch: **254 arms / 312 distinct names** (counted fresh from
`parse_block_inner`).

---

## Findings

### MEDIUM

#### NIF-D5-01: `bhkBallSocketConstraintChain` reads the wrong base struct → garbage entity/priority refs + silently-dropped chain constraint
- **Severity**: MEDIUM (`block_size`-absorbed on the only vanilla home game; garbage stored in currently-unused fields; latent sizeless cascade + misuse hazard for future consumers)
- **Dimension**: Collision/Shader Parsing
- **Game Affected**: Skyrim SE / LE (bsver 83–100) — the only vanilla home of this type (~6 instances). Also any FO4+ Bethesda chain content.
- **Location**: `crates/nif/src/blocks/mod.rs:1153-1154` (dispatches `"bhkBallSocketConstraintChain"` → `BhkConstraint::parse`), `crates/nif/src/blocks/collision/constraints.rs:225-231` (`parse_base`), `crates/nif/src/lib.rs:179`.
- **Status**: NEW (pre-existing from `#979`; surfaced by this byte-audit)
- **Description**: nif.xml:3013 declares `bhkBallSocketConstraintChain` with `inherit="bhkSerializable"`, **not** `bhkConstraint`. `BhkConstraint::parse` unconditionally calls `parse_base`, which reads `num_entities:u32 + entity_a:ref + entity_b:ref + priority:u32` (16 B) — fields that do not exist at the head of a `bhkSerializable`-derived chain. The first 16 bytes of the chain's real payload are reinterpreted as those four fields.
- **Evidence**: A traced Skyrim instance declared `block_size=260`, the parser consumed 16 B; the bytes that became `entity_a`/`entity_b`/`priority` are actually the chain's pivot-float / count data. nif.xml confirms the inheritance: `grep -n 'bhkBallSocketConstraintChain' nif.xml` → `inherit="bhkSerializable"`. On Skyrim the 244-byte remainder is seeked past via `block_size`, so the scene survives; the stored entity refs are garbage but no current consumer reads them.
- **Impact**: The chain constraint is dropped (never reaches a future ragdoll consumer) and stores garbage refs. Today: invisible (unused fields, `block_size`-recovered). Tomorrow: if a consumer trusts `entity_a/entity_b`, or if this type appears in any sizeless-format file, it cascades.
- **Related**: `#979`; PHYSAL (this type is on the ragdoll-constraint path the M41.x work targets).
- **Suggested Fix**: Either give `bhkBallSocketConstraintChain` its own parser that reads the `bhkSerializable` chain layout (`num_pivots`, pivots, constraint params, then the `entity` array per nif.xml), or — until a consumer needs it — dispatch it to a name-only opaque stub that reads zero base bytes and relies on `block_size` recovery, rather than fabricating four wrong fields.

#### NIF-D5-02: M41.x typed Ragdoll/LimitedHinge/Malleable decoders are still listed as constraint "stubs" → a future over-read regression is hidden from `--drift-histogram`
- **Severity**: MEDIUM (no mis-parse today; a regression-detection blind spot on the single highest-priority new parser code in the tree)
- **Dimension**: Collision/Shader Parsing (regression surface)
- **Game Affected**: FO3 / FNV / Skyrim / FO4+ (every game with Havok ragdolls).
- **Location**: `crates/nif/src/lib.rs:167-181` (`is_havok_constraint_stub`, still lists `bhkLimitedHingeConstraint`, `bhkRagdollConstraint`, `bhkMalleableConstraint`).
- **Status**: NEW
- **Description**: `is_havok_constraint_stub` suppresses the consumed-vs-`block_size` drift `warn!` for the listed types (so actor-spawn logs aren't drowned, `#462`). Its own doc comment (`lib.rs:165`) states: *"When a full CInfo parser lands for any of these types, remove it from here so the drift detector goes back to catching real mistakes."* A full CInfo parser landed **today** for `bhkRagdollConstraint` / `bhkLimitedHingeConstraint` / `bhkMalleableConstraint`, yet all three remain in the list. Their drift is therefore still suppressed from `nif_stats --drift-histogram` and the reconciliation `warn!`, so a future **over-read** regression in this byte-exact-but-fragile new decode would be invisible to the standard NIF regression signal.
- **Evidence**: `crates/nif/src/lib.rs:172,174,176` list the three now-typed names; `crates/nif/src/blocks/collision/constraints.rs:260+` is the new typed parser for exactly those names.
- **Impact**: Loss of regression coverage on the most safety-critical new parser. The decoders currently *under*-read by design (motor left for recovery), so they can't simply be removed from the list without re-introducing the `#462` warn-spam — but the over-read direction is now unguarded.
- **Related**: `#462`, `#979`, the M41.x PHYSAL commits (`ca631e09`, `0a0bc3ce`).
- **Suggested Fix**: Replace the blanket suppression for the now-typed types with a *signed* tolerance: suppress the expected small **under**-read (the unread motor/strength tail) but still `warn!` on any **over**-read or larger-than-expected under-read. Alternatively, add a dedicated `block_size`-equality assertion on the typed decoders in the existing constraint unit-test suite so an over-read regression fails CI.

#### NIF-D5-05: Starfield LOD `BSLightingShaderProperty` under-reads +38 B on 26 instances (Starfield-specific trailing field unset)
- **Severity**: MEDIUM (stream-position mismatch the `block_size` reconciliation covers — per `_audit-severity.md`; Starfield is a stretch target, LOD only)
- **Dimension**: Collision/Shader Parsing (shader tail)
- **Game Affected**: Starfield (bsver 172) — `Starfield - LODMeshes.ba2`.
- **Location**: `crates/nif/src/blocks/shader.rs` `parse_fo76_plus`, the Starfield branch past the `#1510` `bsver < STARFIELD` gate (~`:1118`).
- **Status**: NEW (not previously filed; cross-links the 06-13 NIF-NEW-05 Starfield-layout cluster — a *different, smaller* signal: a +38 under-read, not a `NiUnknown`)
- **Description**: 26 Starfield LOD shaders consume 38 B fewer than their `block_size` — a Starfield-specific trailing field set the FO76+ parser doesn't read (likely a material/wetness-tail extension). Positive drift (under-read), so it is **not** the `#1510` over-read regression — `#1510` is confirmed fixed (`bsver < STARFIELD` correctly excludes Starfield from the FO76 luminance/translucency/texture-array tail). `block_size`-absorbed; 0 `NiUnknown`.
- **Evidence**: `nif_stats "Starfield - LODMeshes.ba2" --drift-histogram` → `BSLightingShaderProperty drift=+38×26`; same archive `--tsv` → 48,903 parsed / 0 unknown.
- **Impact**: 26 Starfield LOD shaders parse with a 38-byte trailing field defaulted (the LOD material loses whatever that field carries). Bounded.
- **Related**: 06-13 NIF-NEW-05; `#1510`.
- **Suggested Fix**: Byte-audit the Starfield (bsver ≥ 172) `BSLightingShaderProperty` tail against nif.xml `#STARFIELD#` conditionals; add the missing trailing field(s) to the Starfield path of `parse_fo76_plus`.

#### NIF-D1-01: Residual v10.1.0.106 (bsver 5) interpolator/animation stride drift — 2 Oblivion files still truncate
- **Severity**: MEDIUM (sizeless-format truncation the `block_size` reconciliation cannot cover; small blast radius)
- **Dimension**: Stream Position (+ Version Gating, coupled to the v10.1.0.x sub-band)
- **Game Affected**: Oblivion (the v10.1.0.106 / bsver-5 sub-corpus, sizeless format).
- **Location**: the `NiBoolData` / `NiBoolInterpolator` / `NiBlendPoint3Interpolator` chain (`crates/nif/src/blocks/interpolator.rs`); surfaces at the subsequent block in `crates/nif/src/lib.rs::parse_nif_with_options`.
- **Status**: **Confirms doc finding NIF-NEW-03** (the v10.1.0.x interpolator/`NiControllerSequence` stride cluster) — *residual tail*. The prior audit's named root files (`pickold.nif`, `_1stperson\skeleton.nif`) are now **FIXED** by `#1506`/`#1509`; these two siblings remain.
- **Description**: After the v10.1.0.x version-predicate + `Manager Controlled` bool fixes landed, the Oblivion truncation set dropped 56 → 8 files. Two of the residual 8 (`scampswitch01.nif`, `arwelkydclusterfx01.nif`, both file-version 10.1.0.106) still drift inside the bool/blend-interpolator chain.
- **Evidence**: Byte-trace shows a block reading `0x3F800000` (the float `1.0`) as a `u32` array count; downstream error `unknown KeyType: 16744447` confirms a misaligned offset entered upstream in the interpolator chain. Both files are already tracked in `oblivion_truncations.tsv`, so the no-new-truncation gate covers them.
- **Impact**: 2 of the 8 remaining Oblivion truncations — minor scamp-switch + cluster-FX animation rigs lose their tail. No corruption or OOM (guards hold); the head still imports.
- **Related**: 06-13 NIF-NEW-03; `#1506`; `#1509`.
- **Suggested Fix**: Byte-audit the v10.1.0.106 `NiBoolData` / `NiBoolInterpolator` / `NiBlendPoint3Interpolator` stride against nif.xml (the bool-interpolator base and blend-interpolator variants carry version-gated fields that shift between 10.1.x and 20.x). Add a v10.1.0.106 bool-interpolator fixture. Shares no new version-constant prerequisite (those landed in `#1506`).

### LOW

#### NIF-D5-03 / NIF-D2-01: Constraint version gate uses file-version (`version() <= V20_0_0_5`) instead of `bsver <= NI_BS_LTE_16`, diverging from nif.xml's gate and the sibling rigid-body parser
- **Severity**: LOW (no wrong decode on any shipping game or Morrowind; pre-retail FO3 dev band only; architectural fidelity + cross-parser inconsistency)
- **Dimension**: Version Gating / Collision Parsing (one finding, surfaced by both Dim 2 and Dim 5)
- **Game Affected**: None shipping. Diverges only for non-shipping FO3 pre-retail dev builds at bsver 14/16, which would decode the FO3+ constraint layout instead of the Oblivion one.
- **Location**: `crates/nif/src/blocks/collision/constraints.rs:266` (and `:538`) — `let is_oblivion = stream.version() <= NifVersion::V20_0_0_5;`.
- **Status**: NEW (the PHYSAL constraint decode landed 2026-06-14, commit `0a0bc3ce`).
- **Description**: nif.xml defines the `#NI_BS_LTE_16#` verset as `(#BSVER# #LTE# 16)` — a **bsver** test. The new decode expresses it as a **NIF-version** comparison (`version() <= V20_0_0_5`). The two are equivalent on all seven shipping games + Morrowind, but diverge for FO3 pre-retail dev builds (bsver 14/16, NIF version > 20.0.0.5): those would wrongly take the FO3+ path. A named `bsver::NI_BS_LTE_16 = 16` constant already exists and is unused; the sibling `rigid_body.rs` already gates on bsver. This is a deliberate, documented PHYSAL design choice (spec maps the verset to "NIF ≤ 20.0.0.5"), so it is a fidelity gap, not a bug.
- **Evidence**: `constraints.rs:266` vs nif.xml `<version name="NI_BS_LTE_16">(#BSVER# #LTE# 16)</version>`; `bsver::NI_BS_LTE_16` defined and unused; `rigid_body.rs` gates on `bsver`.
- **Related**: PHYSAL spec (`docs/engine/physal.md`); `rigid_body.rs`.
- **Suggested Fix**: One-token swap to `stream.bsver() <= bsver::NI_BS_LTE_16`, matching nif.xml + `rigid_body.rs`; or, if the NIF-version framing is intentional, add a one-line comment citing the PHYSAL spec so it doesn't read as an oversight.

#### NIF-D5-04: FO3+ malleable inner-dispatch leaves undecoded inner-constraint types' bytes for `block_size` recovery only (no size-skip), unlike the Oblivion path
- **Severity**: LOW (`block_size`-absorbed on all FO3+ files, which always carry the table; an asymmetry, not a mis-parse)
- **Dimension**: Collision/Shader Parsing
- **Game Affected**: FO3 / FNV / Skyrim / FO4+ (malleable-wrapped constraints whose inner type is not Ragdoll/LimitedHinge).
- **Location**: `crates/nif/src/blocks/collision/constraints.rs:241-251` (`parse_fo3_malleable_inner` — `_ => BhkConstraintData::Other` reads nothing further).
- **Status**: NEW (acceptable-but-asymmetric; documents a real behaviour)
- **Description**: When a `bhkMalleableConstraint` wraps an inner type other than Ragdoll (7) or LimitedHinge (2), the FO3+ path returns `Other` without consuming the inner CInfo bytes, relying entirely on the outer `block_size` seek. The Oblivion path (which has no `block_sizes` anchor) *does* size-skip the fixed payload of undecoded types. The two paths are asymmetric: the FO3+ path would mis-parse if it ever ran on a sizeless file, and it offers no defence-in-depth.
- **Evidence**: `constraints.rs:248` (`_ => BhkConstraintData::Other`) vs the Oblivion `payload_size` size-skip table (`constraints.rs:~300+`).
- **Impact**: None today (FO3+ always carries `block_sizes`). Latent if the malleable path is ever exercised on a sizeless format.
- **Related**: NIF-D5-03 (same new code).
- **Suggested Fix**: Mirror the Oblivion size-skip: for the `_` arm, `stream.skip()` the fixed FO3+ inner-CInfo payload size by wrapped type so consumption is self-consistent independent of `block_size`.

#### NIF-D4-01: `tint_map` / `inner_layer_map` captured on `MaterialInfo` but dropped at the `ImportedMesh` handoff
- **Severity**: LOW (latent — both texture slots are currently renderer-unconsumed; no visible artifact today)
- **Dimension**: Geometry/Import Handoff
- **Game Affected**: Skyrim / FO4 (FaceTint + MultiLayerParallax shader types; `BSShaderTextureSet` slot 7).
- **Location**: `crates/nif/src/import/material/walker.rs:198-204,225-231` (captures `tint_map` / `inner_layer_map` onto the internal `MaterialInfo`); `crates/nif/src/import/types.rs` (`ImportedMesh` has no field for either).
- **Status**: NEW (undocumented residual of CLOSED `#563` / SK-D3-02 — the fix's "bindless indices to GpuInstance" half never landed)
- **Description**: The material walker extracts the FaceTint tint map and the MultiLayerParallax inner-layer map into `MaterialInfo`, but `ImportedMesh` carries no field for them and no extractor forwards them, so they are silently dropped at the import boundary. Same bug class as the historically-fixed `#214`/`#430`/`#451`/`#1076` "captured-but-dropped" findings.
- **Evidence**: `walker.rs:198-204` / `:225-231` assign the two fields; `grep -n 'tint_map\|inner_layer_map' crates/nif/src/import/types.rs` → no match; no consumer in `material_translate.rs`.
- **Impact**: None today (no renderer consumer reads either slot). Latent: when FaceTint / MultiLayerParallax rendering is implemented, the data is already parsed but won't reach the GPU.
- **Related**: CLOSED `#563`; NIFAL boundary (`material_translate.rs` — cross-link `/audit-nifal`, do not duplicate the per-game material classification there).
- **Suggested Fix**: Either add `tint_map` / `inner_layer_map` to `ImportedMesh` and forward them through the walker (so the data survives to a future consumer), or drop the unused `MaterialInfo` captures and re-add when a consumer lands — and note the deferral on `#563`.

#### NIF-D1-02: NetImmerse v3.3–v4.2 `marker_*` files truncate (inline-type-name + pre-Gamebryo block layout) — 6 files
- **Severity**: LOW (handled by the `#698` truncate-not-Err recovery; the kept head still imports; tiny UI markers below the stated compat span)
- **Dimension**: Stream Position
- **Game Affected**: Oblivion data dir, but the files are pre-Gamebryo NetImmerse v3.3–v4.2 — *below* the documented v10.0.0.3 floor of the supported span.
- **Location**: recovery path `crates/nif/src/lib.rs` (`#698` inline-type-name truncate); the v3/v4 block layouts have no dedicated parsers.
- **Status**: NEW (the prior audit grouped `marker_*` under NIF-NEW-03, but these are NetImmerse v3/v4 — a *distinct* root cause from the v10.1.0.x interpolator drift). All 6 are already in `oblivion_truncations.tsv`, so the no-new-truncation gate tracks them.
- **Description**: 6 `marker_*` UI meshes are NetImmerse v3.3–v4.2, predating the Gamebryo block layout the parser targets. They truncate gracefully via the `#698` inline-type-name recovery rather than hard-`Err`.
- **Evidence**: `sweep_oblivion.tsv` / `oblivion_truncations.tsv` rows; file-version 3.3.0.13 … 4.2.2.0.
- **Impact**: 6 tiny editor-marker meshes (not gameplay content) keep their head and drop the tail. None are rendered in normal play.
- **Related**: `#698`; 06-13 NIF-NEW-03 (mis-grouped there).
- **Suggested Fix**: None required — document as out-of-span. If desired, exclude pre-v10.0.0.3 NetImmerse files from the Oblivion clean-rate denominator so they stop counting against the figure.

---

## Regression Guards — all hold (verified, not re-reported)

- **`#1509`** (NiGeomMorpherController bsver=9 over-read) — FIXED.
- **`#1510`** (Starfield `BSLightingShaderProperty`/`BSEffectShaderProperty` over-read) — FIXED; 48,903 Starfield shaders, 0 over-read, 0 `NiUnknown` corpus-wide.
- **`#1506`/`#1508`** (`V10_1_0_108…112` constants + `has_interp_controller_manager_controlled` / `has_quat_transform_trs_valid` predicates) — LANDED; the `Manager Controlled` bool now read at the correct offset (`crates/nif/src/blocks/controller/mod.rs:84-89`), TRS-Valid bool[3] in `crates/nif/src/stream.rs` (`read_ni_quat_transform`).
- **`#1516`** (inverted bitangent sign on inline tangent paths) — **CORRECT and not over-corrected.** All five sign sites consolidated onto `types::bitangent_sign(n, t, b)`; the two inline paths (`bs_tri_shape.rs:1008`, `sse_recon.rs:369`) genuinely flip (triple-product antisymmetry), the authored-blob (`tangent.rs:100`) and synthesized (`:315`/`:486`) paths were already correct and stay consistent. 7/7 unit tests pass.
- **`#1336`** (bare-decimal bsver literals → named constants) — COMPLETE; `grep -rnE 'bsver\(\)\s*[<>=!]+\s*[0-9]+' crates/nif/src/` returns zero genuine bare-decimal comparisons.
- **`#1239`/`#1240`** (NiPSysEmitter nif.xml gate; NiTextureEffect→NiDynamicEffect base gated `bsver < FALLOUT4`) — HOLD.
- **`#1263`/`#1265`** (BSGeometry `read_pod_vec` fast path; NiTriShape tangent `std::mem::take`) — HOLD.
- **Typed particle decode** (NiPSysEmitter/Ctlr/CtlrData/GrowFadeModifier typed, params flow `extract_emitter_params` → `apply_emitter_params`, color NOT overridden) — HOLD.
- **Collision translation** (`extract_collision` resolves `BhkMultiSphereShape` + `BhkConvexListShape`; `CollisionAuthoring` 5-discriminant enum `None`/`Classic`/`NewPhysicsStub`/`Phantom`/`Unrecognised`) — HOLD; `every_dispatched_bhk_shape_has_resolve_arm` structural guard passes.
- **PHYSAL per-game seam** confined to `constraints.rs` (only the constraint CInfo decode branches per game) — HOLD.
- **Allocation pins** (all 10): `#[must_use]` on `allocate_vec`/`read_pod_vec`/12 array wrappers + KFM twin; `read_pod_vec` single-allocate; big-endian `compile_error!` gate; `bytemuck` genuinely absent from every `Cargo.toml` (only a transitive `Cargo.lock` entry); `get_mut()/insert` counter split; `ragdoll.rs` on `allocate_vec`; `Arc<str>` name interning; `pre_parse_cell` two-phase serial→rayon split + serial fast path (`PRE_PARSE_RAYON_MIN = 8`) — ALL HOLD.
- **NEW constraint decode allocation** — uses fixed-size stack `[f32;4]` reads via `read_vec4`, no stream-derived `count`, structurally immune to the `#388` OOM class.

---

## Confirmed-open, not re-reported (dedup)

- **`#1511`** (NIF-NEW-06 — six `NifVariant` helpers with zero production call sites) — still tests-only; confirmed, not re-filed.
- **`#1335`** (d5_coverage `DISPATCH_KEYS` snapshot stale — 300 vs live 312) — confirmed still accurate over-report; not re-filed.
- **`#1334`** (Skyrim SE `bhkPlaneShape` undispatched, single file, `block_size`-recovered) — confirmed still undispatched, not regressed; not re-filed.
- **`#1333`** (modern `NiParticleSystem` local transform discarded) — not re-checked this pass; remains open.
- **`#1445`** (LC-D9-02 — `extract_emitter_params` omits `planar_angle`) — confirmed still open; not re-filed.
- **`#1381`** (dhat/alloc-counter regression coverage unwired) — confirmed still open; the new constraint code did NOT add an unbounded allocation, so no fresh finding against it.

## Brief-premise correction (verification discipline)

- A `per_block_baselines` test FAIL on 5 games (FO3/FNV/FO4/FO76/Skyrim) is a **stale-baseline false positive**, NOT a regression: the May-16 baselines predate the typed-particle promotions (`#1345`/`#1444`), so `NiPSysBlock`'s parsed count shrank as blocks moved to dedicated typed structs — total parsed and total unknown are byte-identical per game (verified: FO3's −1,484 `NiPSysBlock` exactly matches +1,484 across the new typed types). Fix is mechanical: regenerate the 5 TSVs with `BYROREDUX_REGEN_BASELINES=1`. The `block_coverage_baselines` surface is NOT stale (all 7 pass, regenerated Jun 13). *(Captured here as a tooling note rather than a finding — it tracks the existing baseline-regen workflow, not a parser bug.)*

---

## Prioritized Fix Order

1. **NIF-D5-02** (MEDIUM) — restore over-read drift detection on the now-typed constraint decoders (signed tolerance, or a `block_size`-equality unit-test assertion). Protects the most fragile new code first; small.
2. **NIF-D5-01** (MEDIUM) — give `bhkBallSocketConstraintChain` a `bhkSerializable`-correct parser, or a zero-base opaque stub. Removes the only fabricated-fields constraint dispatch.
3. **NIF-D1-01** (MEDIUM) — byte-audit the v10.1.0.106 `NiBoolData`/`NiBoolInterpolator`/`NiBlendPoint3Interpolator` chain; recovers 2 of the residual 8 Oblivion truncations.
4. **NIF-D5-05** (MEDIUM) — byte-audit the Starfield (bsver ≥ 172) `BSLightingShaderProperty` tail; recovers the +38 B under-read on 26 LOD shaders.
5. **NIF-D5-03/D2-01** (LOW) — one-token swap to `bsver <= NI_BS_LTE_16` (or a clarifying comment).
6. **NIF-D5-04** (LOW) — mirror the Oblivion size-skip in the FO3+ malleable inner-dispatch `_` arm.
7. **NIF-D4-01** (LOW) — add `tint_map`/`inner_layer_map` to `ImportedMesh`, or drop the unused captures and note the deferral on `#563`.
8. **NIF-D1-02** (LOW) — document the 6 NetImmerse v3/v4 marker files as out-of-span (or exclude from the clean-rate denominator).
- **Housekeeping**: regenerate the 5 stale `per_block_baselines` TSVs (`BYROREDUX_REGEN_BASELINES=1`).

---

## Coverage Notes

- **Fully clean**: Dim 6 (Allocation) — 10/10 pins; new constraint code structurally OOM-immune. Dim 4 (Geometry handoff) — only a latent LOW; `#1516` bitangent fix verified correct; `.av.*` access, per-game stride/index selection, three non-cross-wired tangent producers, SSE partition→global bone remap, single coord conversion all hold; the PHYSAL `Option<ImportedRagdoll>` thread is purely additive and doesn't touch the mesh handoff.
- **Empirical baselines**: FO3/FNV/Skyrim-SE/FO4/FO76 + Starfield all parse at a genuine 100.0000% corpus-wide (millions of blocks, 0 `NiUnknown`). Oblivion sits at 8 truncating files (down from 56) — the only remaining real exception, all sizeless v10.x sub-version stride residuals.
- **Per-dimension raw reports**: `/tmp/audit/nif/dim_{1..6}.md` + corpus sweeps `/tmp/audit/nif/sweep_*.{tsv,err}` (scratch).

---

*Next step: `/audit-publish docs/audits/AUDIT_NIF_2026-06-14.md`*
