# ByroRedux Compatibility Audit — Fallout: New Vegas

**Date**: 2026-06-18 · **HEAD**: `2aac5351` · **Game**: Fallout: New Vegas (reference title)
**Reference data**: `/mnt/data/SteamLibrary/steamapps/common/Fallout New Vegas/Data/`
**GPU**: NVIDIA RTX 4070 Ti (Vulkan 1.4)

## Executive Summary

This audit re-examines FNV — the reference title and the reference realization for
the canonical translation layers (NIFAL material/physics, PHYSAL ragdoll) — at HEAD
`2aac5351`, which is **identical to the commit audited on 2026-06-16** (no commits
landed in the intervening window). Rather than transcribe the prior pass, all eight
dimensions were independently re-audited with a deliberately higher skepticism bar,
targeting anything the prior "0 findings above LOW" verdict might have missed and
re-verifying the one substantive change in recent history that touches FNV — the
ReSTIR reservoir-attachment removal (`218b425b`) and the #1649 additive-particle
batching fix (`067a8354`).

**Result: the prior clean verdict holds.** Every high-blast-radius invariant — cell
load + unload hygiene, NIFAL single-boundary material translation, RT lighting gates,
ESM record coverage, NIF version gates, animation/particle/skin pipeline, and the
PHYSAL ragdoll slice — was re-verified correct against the live code. The reservoir
removal is fully consistent (no dangling references, no shader-struct drift; the
committed `triangle.frag.spv` byte-matches a fresh `glslangValidator -V` build), and
the #1649 particle fix is correct end-to-end for FNV additive stacks.

**Totals: 1 finding — 0 CRITICAL, 0 HIGH, 0 MEDIUM, 1 LOW.** The single new item is a
test-coverage gap (the unload-walk "all texture handle components" test omits the
6-slot `ExtraTextureMaps` component); production code is correct, so it is a guard
weakness, not a live leak.

Because HEAD is byte-identical to the 2026-06-16 audit commit, no GPU re-bench was run
— the bench-of-record numbers captured there (87.6 FPS / 11.41 ms / fence 9.02 / 3624
ent / 1224 draws on RTX 4070 Ti) and the 14881/14881 (100.00%) NIF clean-parse rate
remain the authoritative live figures, and no code path that could change them was
touched.

## Baseline Comparison Table

HEAD `2aac5351` == the 2026-06-16 audit commit. No code change → no re-bench;
figures carried from the 2026-06-16 live run, compared against the ROADMAP FNV row at
commit `1c26bc25` (2026-06-03).

| Metric                | ROADMAP (`1c26bc25`) | Observed (`2aac5351`)  | Δ |
|-----------------------|----------------------|------------------------|---|
| Prospector wall FPS   | 76.2                 | 87.6                   | +14.9% (faster) |
| Prospector wall ms    | 13.11                | 11.41                  | −1.70 ms |
| Prospector fence ms   | 11.12                | 9.02                   | −2.10 ms |
| Prospector entities   | 3516                 | 3624                   | +108 |
| Prospector draws      | 1224                 | 1224                   | 0 (identical) |
| NIF clean-parse rate  | 100% (14881)         | 100% (14881)           | 0 |
| Structured records    | 73 054 (ROADMAP doc) | 77 825 (live `total()`)| +4 771 (navmesh drain #1272, doc drift — see prior D4-01) |

No regression on any axis. The ROADMAP bench-of-record remains ~201 commits stale and
understates throughput ~15% (carried forward from prior D8-01); refresh at next
`/session-close`.

## Findings by Severity

### CRITICAL — none
### HIGH — none
### MEDIUM — none

### LOW

#### D1-01: Unload-walk "all texture handle components" test omits `ExtraTextureMaps`
- **Severity**: LOW · **Dimension**: Cell Loading (test coverage)
- **Location**: `byroredux/src/cell_loader/unload_greyscale_lut_tests.rs:65-87`
- **Status**: NEW
- **Description**: The test named for sweeping *all* texture-handle components on unload
  covers `TextureHandle` / `NormalMapHandle` / `DarkMapHandle` / `GreyscaleLutHandle`
  but never constructs `ExtraTextureMaps` — the largest texture-bearing component (6
  slots: glow / detail / gloss / parallax / env / env_mask), each acquired via
  `resolve_texture` at spawn time (`byroredux/src/cell_loader/spawn.rs:916-921`) and
  swept at `byroredux/src/cell_loader/unload.rs:283-292`.
- **Evidence**: The test inserts only the four single-handle components; `ExtraTextureMaps`
  is absent from the fixture. Production code IS correct — all six slots are released on
  unload — so this is a coverage gap, not a runtime leak. A future edit that dropped the
  env / parallax / env_mask arms from the unload walk (leaking up to six texture
  refcounts per env-mapped FNV mesh per cell-unload cycle) would still pass this
  "all components" test.
- **Impact**: Diagnostic / coverage only. No live defect on FNV. Blast radius is the loss
  of a regression guard on a per-cell-cycle texture-refcount leak.
- **Related**: Mirrors the per-cell texture-refcount sweep guards (#1338/#1341/#627).
- **Suggested Fix**: Add an `ExtraTextureMaps { glow, detail, gloss, parallax, env,
  env_mask, .. }` entity to the fixture and assert all six non-zero handles appear in the
  collected `texture_drops`.

## Regression Guard List (re-verified still correct — no defect)

**Dimension 1 — Cell loading**: interior XCLL→LTMP→default lighting routing
(`load.rs::apply_interior_cell_lighting`, all three interior entry points pass
`is_interior=true`); `NiAlphaProperty` decal routing gates on the `alpha_test` *bool*
(`render_layer.rs`), `is_decal`→Decal / alpha-test Architecture→Clutter; exterior LAND
+ LTEX→TXST→diffuse splat with per-layer refcount release on unload; unload sweeps mesh
(BLAS dropped only at refcount-zero), all texture-handle components incl. the 6-slot
`ExtraTextureMaps`, item-instance pool (#896), Rapier bodies + Ragdoll (#1520/#1531),
skin slots (#1003/#1004); worldspace sky/weather NOT over-released per-cell (#1199);
`drop_blas` safely no-ops on BLAS-less meshes (water); M38 water + submersion hysteresis.
All four `NifImportRegistry::insert` production sites except `precombined.rs` (the known
FO4-only D1-01 from the prior audit) capture + release evicted clip handles.

**Dimension 2 — NIFAL FNV slice**: single `translate_material` boundary, exactly two call
sites (`spawn.rs`, `nif_loader.rs`); PBR resolved once as plain `f32`, no per-draw
`classify_pbr` in `render/static_meshes.rs`; `EmissiveSource::Material` gated behind
`!has_material_data` so Skyrim+ `Lighting` / FO4+ `Effect` cannot bleed into FNV;
collision no-drop guard `BhkMultiSphereShape` / `BhkConvexListShape` → `CollisionShape`
with finite-guards and trimesh fallback; no fabricated PBR beyond the sanctioned,
regression-tested legacy-specular→metalness translation (capped ≤0.4).

**Dimension 3 — RT lighting**: ReSTIR reservoir-attachment removal (`218b425b`) fully
consistent — `GBuffer` carries 5 attachments, render pass/framebuffer/clear-values/blend
state all 6-color, `triangle.frag` outputs locations 0..=5, reflection test
`triangle_frag_declares_six_color_outputs` green, zero `reservoir` grep hits in
`crates/renderer/src/`, committed `triangle.frag.spv` byte-identical to a fresh
`glslangValidator -V` build; `MAT_FLAG_PBR_BSDF` (=32u) is 0 for all FNV materials
(`is_pbr` set only on BGSM/.mat/CDB paths) → Disney lobe unreachable, Cook-Torrance
dielectric path taken; `#1125` skyTint interior gate present at both miss sites
(reflection + refraction); ReSTIR W estimator `W = resWSum/(K·w_sel)` correct including
the #1369 pass-2 radiance recompute; sun-sprite mip-0 force (`textureLod(...,0.0)`);
SVGF temporal uses motion vectors + `mesh_id` disocclusion with no orphaned binding;
BLAS budget `device_local_bytes/3` floored at 256 MB.

**Dimension 4 — ESM parser**: SCOL era gate (`is_fo4_plus || Fallout3NV`) intact — FNV's
98 SCOL bases / 1084 REFRs dispatch via `parse_scol_group`, MOVS/PKIN/MSWP correctly
FO4+-gated with explicit FNV-skip arms; no `unreachable_patterns` warning in
`cell/walkers.rs`; XCLL `fog_far_color` length-gated optional read (FNV 40-byte XCLL →
`None`, no over-read); `equip.rs::expand_leveled_form_id` resolves LVLI → ARMO/WEAP with
nested-list + `LVLI_MAX_DEPTH=8` cycle guard; all 491 plugin tests pass.

**Dimension 5 — NIF parser**: `version.rs` raw-`bsver` gates correct — FNV (`bsver` 34)
sits `≤ FO3_FNV (34)`, `> NI_BS_LTE_16 (16)`, `< RIGID_BODY_FLAGS16 (76)` /
`< SKYRIM_LE` / `< FALLOUT4` on every gate (no flipped/off-by-one compare);
`examine_collision_kind` → `Classic` for FNV bhk chains; `MAX_NIF_NODE_DEPTH=128` never
tripped on legit scenes; `NiTexturingProperty` decal-slot, `BSMultiBound*`,
`BSDecalPlacementVectorExtraData` all still fixed.

**Dimension 6 — animation/skin/particles**: #1649 additive-particle fix (`067a8354`)
correct — `gamebryo_to_vk_blend_factor` 0→ONE/1→ZERO, ADD blend op gives true additive
accumulation, four additive presets `dst_blend:0` / smoke `dst_blend:7`, sort-key routes
additive mesh-before-depth for batch contiguity, per-instance model matrices keep
batched additive particles correct; CycleType Clamp/Loop/Reverse map intact NIF→core,
honored in both `player.rs::advance_time` and `stack.rs::advance_stack`; NPC hand-mesh
load (#793) — `humanoid_body_paths(Fallout3NV)` returns upperbody + lefthand + righthand,
hands load even when torso armor suppresses upperbody; typed-emitter pin reads authored
`NiPSysEmitter.params` / `NiFloatData` / `NiPSysEmitterCtlrData.birth_rate_first` (finite
guards reject only corrupt values, never overwrite authored kinematics); FixedString
interning at clip-load; B-spline FLT_MAX pose-fallback; AnimationClipRegistry
lowercase-path dedup.

**Dimension 7 — PHYSAL ragdoll**: no per-game branch in `ragdoll.rs` or
`crates/physics/` (only PHYSAL seam = constraint CInfo byte-layout decode in
`constraints.rs`, collapsing to game-agnostic `BhkConstraintData`); malleable-wrapped
Ragdoll (`wrapped_type==7`) decodes via `parse_fo3_malleable_inner` to
`BhkConstraintData::Ragdoll` — a jointed body, not a rigid blob; writeback is
index-aligned (`spec`/`template` order), inverts the body-local offset (#1616),
preserves scale, and skips non-finite poses before they reach the bone palette / GPU
skinning (#1534).

**Dimension 8 — real data**: HEAD identical to 2026-06-16 audit commit; bench-of-record
and 14881/14881 NIF clean-parse carried forward (no code path touched); CWD gotcha and
`tex.missing` / `tex.loaded` behavior unchanged.

## Existing-Issue Cross-References (not re-reported)

- **#1333** (OPEN) — Modern NiParticleSystem local transform discarded (Dimension 6).
- **#1539** (OPEN) — Non-Ragdoll/non-LimitedHinge constraints dropped silently (Dimension 7).
- **#1540** (OPEN) — Dynamic trimesh ragdoll body degenerate inertia (Dimension 7).
- **#1583** (OPEN per cache) — Write-only ReSTIR reservoir attachment; verified already
  removed by `218b425b` (attachment gone, this audit confirms no debris) — the issue
  should be closeable.
- Prior LOW findings D1-01/02/03, D3-01, D4-01/02, D7-01/02, D8-01/02 from
  `docs/audits/AUDIT_FNV_2026-06-16.md` still stand (same commit) and are not re-listed.

---
*Generated by `/audit-fnv`. Next: `/audit-publish docs/audits/AUDIT_FNV_2026-06-18.md`*
