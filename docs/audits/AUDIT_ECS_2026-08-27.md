# ECS Audit — 2026-08-27

**Command**: `/audit-ecs` (run inside an `/audit-suite --preset comprehensive` sweep)
**Repo**: `/mnt/data/src/gamebyro-redux` @ `969d81c8`
**Prior report**: `docs/audits/AUDIT_ECS_2026-08-24.md` (dedup base, at `048a8bd8`)

## Scope

Standard `/audit-ecs` scope — `crates/core/src/ecs/` (storage backends, queries,
world, resources, scheduler, access analyzer, lock tracker), plus
`crates/core/src/animation/` (Dimension 10), plus the un-owned **P2 gameplay
slice** the comprehensive preset explicitly assigns here:
`byroredux/src/combat.rs`, `byroredux/src/inventory.rs`,
`byroredux/src/settings_io.rs`, and the action/ray/activation half of
`byroredux/src/interaction.rs`.

HEAD is **142 commits** past the prior report's baseline
(`048a8bd8..969d81c8`), spanning the tail of Session 74 and Session 75 (FNV /
Skyrim / Starfield per-game fix batches, the NAVM path-cache wiring, the
`#3112` weapon-slot split, the `#3033`/`#3034` combat + text-key fixes, the
`#3258` animation NaN guard, and the `#2395`/`#2397` storage capacity work).

**In-scope files that changed in this delta** (`git diff --stat`):
`crates/core/src/ecs/{world,lock_tracker,packed,sparse_set,world_tests}.rs`,
`crates/core/src/ecs/resources/mod.rs`,
`crates/core/src/ecs/components/{actor_values,camera,inventory,material}.rs`,
`crates/core/src/animation/{mod,player,stack,text_events}.rs`,
`byroredux/src/{boot,combat,inventory,interaction}.rs`, and fifteen
`byroredux/src/systems/*.rs`. `crates/core/src/ecs/{query,resource,scheduler,
access,storage,systems}.rs` are byte-identical to the prior baseline (verified
with an empty `git diff`), so Dimensions 3, 5, 5b's analyzer core, and 6 were
re-verified against unchanged code rather than re-derived.

**Tests** (run for this report, both green):
- `cargo test -p byroredux-core` — 689 passed, 0 failed, 3 ignored.
- `cargo test -p byroredux` — 1575 + 18 + 14 + 1 passed across 7 binaries,
  0 failed, 33 ignored (Vulkan / game-data gated).

**Dedup base**: `gh issue list --limit 400 --state all` (`/tmp/audit/issues.json`,
fetched this run) plus a scan of `docs/audits/AUDIT_ECS_*.md` and
`AUDIT_CONCURRENCY_*.md`.

## Summary

Four findings: **0 CRITICAL, 0 HIGH, 1 MEDIUM, 3 LOW**.

The ECS core itself is in good shape and got *better* this delta: every prior
HIGH is closed and verified fixed in code (see "Verified fixed" below), the
`World` guard-then-defuse ordering is now uniform across the resource API as
well as the storage API, and both storage backends gained the capacity-release
and empty-storage early-out they were missing. `install_runtime_registries`
still asserts `undeclared_parallel_count() == 0`, `known_conflict_count() == 0`
and `unknown_pair_count() == 0` as **release** assertions, and
`rg '^\s*scheduler\.add_to\(' byroredux/src/boot.rs` is empty — the M27
invariant holds.

The one MEDIUM is a genuine interaction between two fixes that landed in the
same commit (`a5ed4bf5` / `bbfd742f`): `#3034`'s new "a full loop period
elapsed" text-key arm cannot distinguish a full period from *no* advance, and
`#3258`'s NaN guard now routes non-integrable rates into exactly that
zero-advance shape.

## Findings

### ECS-2026-08-27-01: `#3034`'s full-period text-key arm also matches a **zero**-advance step, so the `dt == 0` startup priming tick fires every text key of every looping clip

- **Severity**: MEDIUM
- **Dimension**: 10 (Animation Runtime — text keys)
- **Location**: `crates/core/src/animation/text_events.rs:55-72`; producer
  `crates/core/src/animation/player.rs:112-133`; consumer path
  `byroredux/src/systems/animation.rs:621-658`; trigger
  `byroredux/src/app_events.rs:182`
- **Status**: NEW
- **Description**: `visit_text_key_events` gained an arm for the case where a
  `CycleType::Loop` clip advances by an exact multiple of `duration` and lands
  back on the instant it started from. Its guard is
  `curr_time == prev_time && clip.duration > 0.0 && clip.cycle_type == CycleType::Loop`
  — but `prev == curr` on a `Loop` clip has **two** causes, and the `(prev, curr)`
  pair carries no period count to tell them apart:
  1. N ≥ 1 full periods elapsed (the intended case), and
  2. **zero** advance — `delta == 0.0`, which `advance_time`'s `Loop` arm turns
     into `local_time += 0.0; local_time %= duration`, leaving
     `prev_time == local_time`.

  Case 2 is live. `App::resumed` runs `self.scheduler.run(&self.world, 0.0)`
  immediately after `self.setup_scene()` (`app_events.rs:182`) to prime
  transform state, so every `AnimationPlayer` that exists after the cell/NIF
  load takes a `dt == 0.0` tick. Every looping clip on that tick reports
  `prev == curr` and fires **all** of its text keys at once, at engine start,
  before any of them was actually crossed. The same shape is reachable through
  `BYROREDUX_FIXED_DT=0`.

  The fix's own sibling guard covers only `Clamp`
  (`text_key_settled_clamp_stays_silent_at_prev_eq_curr`,
  `crates/core/src/animation/mod.rs:716`); there is no test pinning a `Loop`
  clip at zero advance, and `text_key_full_period_advance_fires_every_key_once`
  (`mod.rs:679`) asserts the *firing* behaviour for the identical
  `(0.5, 0.5)` pair.
- **Evidence**:
  ```rust
  // crates/core/src/animation/text_events.rs:55
  } else if curr_time == prev_time && clip.duration > 0.0 && clip.cycle_type == CycleType::Loop {
      …
      for (t, sym) in &clip.text_keys {
          visit(*t, *sym);
      }
  ```
  ```rust
  // crates/core/src/animation/player.rs:118 (advance_time)
  player.prev_time = player.local_time;
  let delta = finite_time_delta(dt * player.speed * clip.frequency);
  …
      CycleType::Loop => {
          player.local_time += delta;
          if clip.duration > 0.0 {
              player.local_time %= clip.duration;
  ```
  ```rust
  // byroredux/src/app_events.rs:181-182
  // M41.0 Phase 1b.x — Prime the scene's transform state
  // BEFORE the event loop starts.
  self.scheduler.run(&self.world, 0.0);
  ```
  `advance_stack` (`crates/core/src/animation/stack.rs:154-172`) carries the
  byte-identical `Loop` arm and reaches the same visitor through
  `visit_stack_text_events`.
- **Impact**: Every text key of every looping clip in the scene is delivered as
  an `AnimationTextKeyEvents` component on the priming tick
  (`byroredux/src/systems/animation.rs:656`). The consumer is
  `cinematic_animation_event_system` (`byroredux/src/systems/cinematic.rs:150`),
  a `Stage::Update` exclusive that writes `ActorCinematicState`,
  `CinematicPresentationState` **and** `QuestStageState` — so a spurious batch
  can advance quest state and fire presentation events for keys that were never
  crossed. Blast radius is one tick per engine launch today.

  There is a worse latent variant worth naming while fixing this: `#3258`'s
  `finite_time_delta` (`player.rs:102-108`) folds a non-finite
  `dt * speed * frequency` to `0.0`, which is precisely case 2 — so a clip
  reaching the registry with a NaN/±inf `frequency` from **any** producer other
  than `byroredux::anim_convert` (whose `sanitized_clip_frequency` guards the
  NIF path, `byroredux/src/anim_convert.rs:299`) would fire every text key on
  **every frame, forever**. `crates/core/src/animation/mod.rs:495`
  (`advance_stack_survives_a_non_finite_clip_frequency`) builds exactly that
  clip; it has no `text_keys`, so the interaction is invisible to the suite.
- **Related**: #3258 (`advance_time` NaN latch, CLOSED), #3034 (the arm this
  finding is about, CLOSED), #2082 (the `Reverse` sibling arm).
- **Suggested Fix**: Carry the advance explicitly instead of inferring it from
  the `(prev, curr)` pair. Cheapest form: have `advance_time` / `advance_stack`
  record the applied `delta` (or a `wrapped_periods: u32`) alongside
  `prev_time`, and gate the new arm on `delta.abs() > 0.0`. A narrower stopgap
  is to have the callers in `byroredux/src/systems/animation.rs` skip text-key
  emission when the frame's `dt` is non-positive, but that leaves the
  zero-`frequency` / zero-`speed` variants open. Add a
  `text_key_loop_at_zero_advance_stays_silent` test next to the existing
  `Clamp` sibling.

### ECS-2026-08-27-02: `sample_blended_transform`'s blend pass never re-applies the weight pass's empty-key filter, so a keyless max-priority channel pollutes the blend it was excluded from

- **Severity**: LOW
- **Dimension**: 10 (Animation Runtime — blend weights)
- **Location**: `crates/core/src/animation/stack.rs:328-360` (pass 1) vs
  `crates/core/src/animation/stack.rs:367-403` (pass 3)
- **Status**: NEW
- **Description**: Pass 1 (which elects `max_priority` and accumulates
  `total_weight`) skips any layer whose channel for this bone has all three key
  lists empty. Pass 3 (which does the actual blend) re-walks the layers with
  only a `channel.priority != max_priority` filter — the empty-key check is not
  repeated. A layer that pass 1 excluded from `total_weight` therefore still
  contributes to the blend, with `sample_translation`/`sample_rotation`/
  `sample_scale` all returning `None` and falling back to
  `Vec3::ZERO` / `Quat::IDENTITY` / `1.0`.

  The two effects are: `blended_scale` gains a spurious `+1.0 * w` (a bone at
  scale 1 blends to ~2), and `blended_rot` is slerped toward identity by
  `w / (accumulated_weight + w)`. `accumulated_weight` also exceeds 1.0, since
  the denominator `total_weight` never counted the offending layer.
- **Evidence**: pass 1 —
  ```rust
  // crates/core/src/animation/stack.rs:341
  // Only inspect key presence here. Sampling is deferred to the blend
  // pass below so interpolation happens once per channel (#3031).
  if channel.translation_keys.is_empty()
      && channel.rotation_keys.is_empty()
      && channel.scale_keys.is_empty()
  {
      continue;
  }
  ```
  pass 3 has no counterpart —
  ```rust
  // crates/core/src/animation/stack.rs:385
  if channel.priority != max_priority {
      continue;
  }

  let t = sample_translation(channel, layer.local_time).unwrap_or(Vec3::ZERO);
  let r = sample_rotation(channel, layer.local_time).unwrap_or(Quat::IDENTITY);
  let s = sample_scale(channel, layer.local_time).unwrap_or(1.0);
  ```
  All-empty channels are producible: `constant_transform_channel`
  (`crates/nif/src/anim/transform.rs:128-177`) emits empty key vectors for every
  axis whose pose is the `FLT_MAX` "no static pose" sentinel, and
  `extract_transform_channel_at` (`crates/nif/src/anim/transform.rs:69-81`)
  passes `convert_*_keys` output through verbatim, so a `NiTransformData` with
  no keys yields the same. Neither `sequence.rs:49` nor `entry.rs:401/420`
  filters an empty channel out before `channels.insert`.
- **Impact**: Wrong bone scale and rotation during a crossfade whenever one of
  the blended clips carries a keyless channel for that bone at the winning
  priority. **Not live today**: nothing in the engine inserts an
  `AnimationStack` — `AnimationStack::new()` appears only in
  `crates/core/src/animation/controller.rs`'s test module, and
  `apply_pending_transition` (the sole `stack.play` caller,
  `controller.rs:278`) has no production caller. The bug is latent in a `pub`
  core API that `boot.rs:1043` already declares a write for, and it will go live
  the moment the KFM controller is wired.
- **Related**: #3031 (the commit that introduced the pass-1 filter, `136254e9`,
  and did not mirror it), #3316 (the FNV finding that made
  `constant_transform_channel` a high-traffic path).
- **Suggested Fix**: Hoist the three-`is_empty` predicate into a small
  `fn channel_has_keys(channel: &TransformChannel) -> bool` and call it from
  both passes, so the two filters cannot drift again. Pin with a two-layer
  fixture where one layer's channel is keyless at the same priority.

### ECS-2026-08-27-03: `settings_io::save_to_path` writes-then-renames without an `fsync`, diverging from the project's own atomic-write doctrine one crate over

- **Severity**: LOW
- **Dimension**: 7 (P2 gameplay-slice lifecycles — `settings_io.rs`)
- **Location**: `byroredux/src/settings_io.rs:118-148`
- **Status**: NEW
- **Description**: The settings writer creates a `.tmp` sibling with
  `fs::write`, then `fs::rename`s it over the target. It never flushes the temp
  file to disk before the rename, never re-reads to verify the bytes landed, and
  never syncs the parent directory afterward. `crates/save/src/disk.rs` — the
  M45 save path, whose module doc calls this "the crash-safe dance" — does all
  three (`f.sync_all()?` at `disk.rs:43`, readback verify at `disk.rs:48-51`,
  `dir_file.sync_all()?` at `disk.rs:66` with an explicit SAVE-D3-01 comment
  saying a successful rename is not durable until the parent directory is
  synced). Two writers in the same binary, two different durability contracts,
  and only one of them documents which it is.
- **Evidence**:
  ```rust
  // byroredux/src/settings_io.rs:136-138
  let temp_path = temporary_path(path);
  fs::write(&temp_path, source)?;
  if let Err(rename_error) = fs::rename(&temp_path, path) {
  ```
  versus
  ```rust
  // crates/save/src/disk.rs:40-43
  let mut f = fs::File::create(&tmp_path)?;
  …
  f.sync_all()?;
  ```
- **Impact**: A crash or power loss in the window between the rename hitting the
  directory journal and the temp file's data reaching the platter leaves a
  zero-length or truncated `settings.toml`. The loader degrades gracefully (a
  `toml::from_str` failure is logged and skipped, `settings_io.rs:85`), so the
  user loses their control bindings and preferences rather than the session —
  hence LOW, not the save path's severity.
- **Related**: #1714 / SAVE-D2-01 (the save-format compatibility doctrine),
  SAVE-D3-01 (the parent-directory sync this file lacks).
- **Suggested Fix**: Replace the `fs::write` + `fs::rename` pair with the same
  `File::create` → `write_all` → `sync_all` → `rename` → parent `sync_all`
  sequence `crates/save/src/disk.rs` already implements, or factor that helper
  into a shared `atomic_write` used by both. If the weaker contract is
  deliberate, say so in the module doc and name `disk.rs` as the contrast.

### ECS-2026-08-27-04: `combat_input_system` builds the gameplay slice's deepest guard hold-stack (5 types) and is registered with a bare `add_exclusive`, so `sys.accesses` shows a blank row exactly where the dispute is

- **Severity**: LOW
- **Dimension**: 1 (Lock Ordering) + 5b (Scheduler Access Declarations)
- **Location**: `byroredux/src/combat.rs:318-330` and `byroredux/src/combat.rs:348-367`;
  registration `byroredux/src/boot.rs:855-856`
- **Status**: NEW
- **Description**: `attack_damage` matches on `world.get::<EquippedWeapon>()`,
  which yields a `ComponentRef` whose read guard is bound for the whole match
  arm — and the arm's body calls `melee_damage_charal_bonus(world, aggressor)`,
  which then acquires, and holds to function end, `MeleeDamageConfig` (read),
  `CharacterRuleset` (read), `ActorValues` (read) and `CharacterLevel` (read).
  Note that `let config = *config;` **shadows** but does not drop the
  `MeleeDamageConfig` guard — the original binding lives to the end of the
  function scope. That is a five-deep nested hold-stack, established across a
  helper call, which is the exact pattern `crates/core/src/ecs/world.rs:10-31`'s
  "snapshot before you iterate" house rule was written to prohibit.

  This is the same shape as #2153's `pool_regen_tick_system` (a 3-deep stack:
  `PoolRegenConfig` read → `PoolRegenAccumulator` write → `CharacterRuleset`
  read → `ActorValues` write). That one was resolved under #2391 by moving it to
  `add_exclusive_with_access` **specifically so the disputed types appear on the
  `sys.accesses` report instead of a blank row** — the rationale is spelled out
  in `byroredux/src/boot.rs:984-993`. The three P2 gameplay exclusives
  (`interaction_system`, `combat_input_system`, `combat_damage_system`) were
  added afterwards with plain `add_exclusive` and got no such treatment.
- **Evidence**:
  ```rust
  // byroredux/src/combat.rs:326-330
  match world.get::<EquippedWeapon>(aggressor) {
      …
      Some(weapon) => weapon.damage.max(0.0) + melee_damage_charal_bonus(world, aggressor),
      None => UNARMED_DAMAGE,
  }
  ```
  ```rust
  // byroredux/src/combat.rs:348-360
  fn melee_damage_charal_bonus(world: &World, aggressor: EntityId) -> f32 {
      let Some(config) = world.try_resource::<MeleeDamageConfig>() else { return 0.0; };
      let config = *config;                                   // shadows; guard still live
      let Some(ruleset) = world.try_resource::<CharacterRuleset>() else { return 0.0; };
      let Some(avs) = world.get::<ActorValues>(aggressor) else { return 0.0; };
      let level = world.get::<CharacterLevel>(aggressor).map_or(1, |level| level.level);
  ```
  ```rust
  // byroredux/src/boot.rs:855-856
  scheduler.add_exclusive(Stage::Update, crate::combat::combat_input_system);
  scheduler.add_exclusive(Stage::Update, crate::combat::combat_damage_system);
  ```
- **Impact**: No live deadlock. Both combat systems are `Stage::Update`
  exclusives, so they hold the world serially and no parallel partner exists;
  the `CharacterRuleset → ActorValues` ordering also matches
  `pool_regen_tick_system`'s, so no reverse edge exists in-tree today. The cost
  is diagnostic: the deepest hold-stack in the newest, least-reviewed subsystem
  is invisible to `sys.accesses`, and `BYRO_LOCK_ORDER_CHECK` will record four
  new edges out of `EquippedWeapon` with no declaration to compare them against.
  A future move of either combat system into a parallel lane would silently
  bypass the analyzer.
- **Related**: #2153 (the identical 3-deep stack), #2391 / ECS-D5B-03 (the
  `add_exclusive_with_access` remedy and its stated rationale), #2270 (the
  `world.rs` house rule).
- **Suggested Fix**: Drop the `EquippedWeapon` guard before computing the CHARAL
  bonus — snapshot `weapon.damage` into an `f32` and let the `ComponentRef` die
  at the end of that statement — and give `melee_damage_charal_bonus` the same
  treatment for `MeleeDamageConfig` (`drop(config_guard)` after the copy).
  Then register all three P2 exclusives via `add_exclusive_with_access` with
  their real read/write sets, matching `pool_regen_tick_system`.

## Verified fixed since the prior report (re-derived from code, not trusted from commit messages)

| Prior finding | Issue | Verification |
|---|---|---|
| ECS-2026-08-24-01 (two `ComponentRef<Transform>` guards in `fragment.rs`) | #3250 CLOSED | `crates/scripting/src/fragment.rs` no longer holds a second `Transform` read across the write; `fragment/tests.rs` gained a guard. |
| ECS-2026-08-24-04 (`make_animation_system()` writes `AnimatedTextureFlip` undeclared) | #3252 CLOSED | `byroredux/src/boot.rs:1039` now carries `.writes::<byroredux_core::ecs::AnimatedTextureFlip>()`. |
| ECS-2026-08-24-05 (`physics_sync_system` reads `ActorBoneCollider` / `Parent` undeclared) | #3253 CLOSED | `byroredux/src/boot.rs:1294-1295` now declares `.reads::<…Parent>()` and `.reads::<byroredux_physics::ActorBoneCollider>()`. |
| ECS-2026-08-24-10 (`submersion_system` per-frame `Vec::new()`) | #3257 CLOSED | `WaterDisturbanceScratch` resource added (`byroredux/src/components.rs:1591`), inserted at `boot.rs:531`, taken/restored by the system. |
| ECS-2026-08-24-11 (`advance_time` `Loop` NaN latch) | #3258 CLOSED | `finite_time_delta` (`player.rs:102`) applied in both `advance_time` and `advance_stack`. **But see ECS-2026-08-27-01** — the guard's zero-delta output now feeds `#3034`'s new text-key arm. |
| ECS-2026-08-20-01 (`WindField` undeclared on `player_controller_system`) | #3111 CLOSED | Declared at `boot.rs:746`; `weather_system` moved to a `Stage::Early` exclusive with its own declaration (`boot.rs:764-780`). |
| ECS-2026-08-20-03 (`WEAPON_EQUIP_SLOT = 31` collides with BOD2 body-part 61) | #3112 CLOSED | `EquipmentSlots.weapon` is now a separate field outside `occupants` (`crates/core/src/ecs/components/inventory.rs:204`), with `equip_weapon`/`unequip_weapon`/`equipped_indices`/`is_equipped`/`release` covering it; `EquipTarget::{BipedSlots,Weapon}` makes the collision unrepresentable (`byroredux/src/inventory.rs:31-37`). |

Storage-side improvements also verified in code, not just in the log:
`PackedStorage::clear_erased` now `shrink_to_fit`s all three vectors (#2395,
`packed.rs:296-302`), `SparseSetStorage::remove_entities_erased` early-outs on
an empty `dense` (#2397, `sparse_set.rs:174-181`), and `World`'s resource
accessors now construct the wrapper **before** `scope.defuse()`, matching the
storage accessors' #2149 ordering (`world.rs:709-712`, `739-742`, `796-806`,
`836-840`, `857-861`).

## Prior findings — dedup status (open, not re-numbered)

| Finding | Issue | Status at HEAD |
|---|---|---|
| ECS-2026-08-24-02 (`#2386` recursive-read warning unbounded, no call site) | #3249 OPEN | Unchanged; `lock_tracker.rs` gained only a doc block this delta. |
| ECS-2026-08-24-03 (`AnimatedTextureFlip::handle_for_slot` aliases out-of-range to handle 0) | #3251 OPEN | Unchanged. |
| ECS-2026-08-24-06 (cinematic unload-retention orphans entities out of cell ownership) | #3254 OPEN | Unchanged. |
| ECS-2026-08-24-07 (`NavPath` missing from `clear_ambient_behavior`) | #3255 OPEN | **Re-confirmed at HEAD**: `byroredux/src/npc_spawn/ai_package.rs:421-436` removes all fourteen behavior/state/terminal components and still no `NavPath`, even though #3319 made `NavPath` live by registering it at `boot.rs:667`. The teardown gap is now reachable in the shipped binary, where before it was inert. |
| ECS-2026-08-24-08 (`resolve_cached_waypoints` has no navmesh-residency invalidation) | #3256 OPEN | Unchanged. |
| `Effect::Disable` writes `ReferenceEnableState`, which has no production reader | #3278 OPEN | Re-confirmed: `is_enabled` (`crates/scripting/src/fragment.rs:72`) has no caller outside `fragment/tests.rs`. Owned by `/audit-scripting`; recorded here because it is a write-only `Resource`, not re-filed. |

## Dimensions with no findings — what was checked

1. **Lock ordering & deadlock** — every `query`/`query_mut`/`get`/`resource*`
   site in `world.rs` arms a `TrackedRead`/`TrackedWrite`, defuses only after
   the wrapper exists, and resolves poison through
   `storage_lock_poisoned::<T>()` / `…_erased` / `resource_lock_poisoned::<R>()`.
   `query_2_mut`, `query_2_mut_mut` and `resource_2_mut` all branch on
   `id_a < id_b` and arm the tracker scopes in TypeId order (the #313 fix), and
   all three `assert_ne!` on `A == B`. `try_resource_2_mut` still does both
   existence checks before touching either lock (#465). The one finding in this
   area is ECS-2026-08-27-04, filed as LOW because the site is exclusive.
2. **Storage correctness** — `SparseSetStorage` swap-remove / remove-last /
   overwrite paths and `structural_generation` bumps intact; `PackedStorage`
   `binary_search` sorted invariant, `insert_bulk`'s stable-sort-then-keep-last
   dedup (verified it reproduces single-`insert` last-writer-wins for a
   pre-existing entity re-inserted in the same batch), `mark_dirty` on
   insert/`get_mut`/`remove`/`iter_mut`/`remove_entities_erased`, and
   `drain_dirty_into` preserving `self.dirty`'s capacity while `take_dirty`
   hands it away. `World::insert_batch`'s per-item `debug_assert` adapter
   (`world.rs:250-262`) still fires through the bulk path.
3. **Query borrow safety** — `query.rs` unchanged. All four `unsafe` derefs
   (`query.rs:64`, `135`, `143`, `289`) still carry SAFETY comments tying
   validity to a live `guard` field; `QueryWrite::storage_mut` still requires
   `&mut self`; `ComponentRef` still retains the guard and untracks in `Drop`.
4. **Resource lifetimes** — `resource`/`resource_mut` panic with the type name;
   `insert_resource` / `remove_resource` re-panic on a poisoned prior value
   (#1837 / #466) rather than swallowing it. Every `world.resource::<R>()`
   (panicking form) in the systems layer was traced to an unconditional
   `insert_resource` in `boot.rs`, or is inside a test module.
5. **System & scheduler wiring** — `scheduler.rs`/`system.rs` unchanged; stage
   chain is still `Early → Update → PostUpdate → Physics → Late`; fail-fast
   panic policy (TS-08 / #1412) intact and deliberately not reported.
6. **Scheduler access declarations** — 9 `add_to_with_access`, **0** plain
   `add_to`. Each of the nine parallel systems' declaration was diffed against
   the live body: `player_controller_system` (largest — union of
   `fly_camera_system` + `character_controller_system`, including the water /
   drowning delta's `WaterFlow`, `ActorVitals`, `ActorValues`, `Dead`,
   `PendingDeathReconciliations` and `WindField`-via-`weather_wave_adjustment`),
   `timer_tick_system`, `make_animation_system`,
   `make_transform_propagation_system`, `physics_sync_system`,
   `camera_follow_system`, `reverb_zone_system`, `log_stats_system`,
   `metrics_sample_system` — all complete. The three release `assert_eq!`s in
   `install_runtime_registries` (`boot.rs:1540-1565`) are intact.
7. **Unsafe code review** — no new `unsafe` anywhere in `crates/core/src/ecs/`;
   `World::spawn` still `checked_add`s (#36) and `despawn` still does not
   reclaim ids (#372).
8. **Component lifecycles** — `clear_ambient_behavior` covers all seven M42
   procedures' behavior + state + terminal markers (`NavPath` excepted, #3255
   open); `prune_seat_reservations`
   (`byroredux/src/cell_loader/references/mod.rs:860-881`) still checks **both**
   liveness halves (`live_furniture.contains(furniture)` **and**
   `seated_claimants.get(claimant) == Some(furniture)`); all seven AI systems
   remain env-gated and out of the default schedule; `event_cleanup_system`'s
   import list covers every `impl Component` in
   `crates/scripting/src/events.rs`; `AnimationClipRegistry`'s
   lowercased-path memoisation (#790/#866) is intact and both direct `add`
   callers on the cell path are deduped through `NifImportRegistry`.
   P2-specific checks: `CombatState.last` is a single `Option<CombatTraceEntry>`
   (bounded — the trace-growth failure mode the skill names does not exist),
   `PendingDeathReconciliations.actors` is `mem::take`-drained every Late tick,
   `combat_damage_system` snapshots `HitEvent` into an owned `Vec` before
   mutating (house rule respected), and `Inventory.items` has **no** removal
   path anywhere in the workspace, so `InventoryIndex` cannot be invalidated
   under `EquipmentSlots`.
9. **Hot-path performance invariants** — #823 (lock-tracker held-set behind
   `cfg(debug_assertions)`), #824 (`NameIndex.map` in-place refill,
   `systems/animation.rs:576-587`), #825 + #1371 (transform-propagation fast
   path keyed on the roots triple + `Parent`/`Children` `structural_generation`,
   draining via `drain_dirty_into`), #828 (animation scratch hoisting), #932
   (`FootstepScratch` `mem::take` + restore), #3059
   (`InteractionCandidateScratch` handed back on every path through
   `select_interaction_target`), #2923 (`FxHashMap`/`FxHashSet` end-to-end on
   the render/skinning path) — all spot-checked at HEAD and intact. The new
   per-frame closures added this delta (`make_water_interaction_system`'s five
   scratch collections, `make_billboard_system`'s `last_wind`/`last_cam`,
   `make_world_bound_propagation_system`'s five vectors) are all hoisted into
   closure state; `WaterAudioState.ripple_cooldowns` self-prunes by `retain`.
10. **NIFAL canonical material in the component layer** — `Material.metalness`
    / `roughness` are still plain `f32` (not `Option`), `resolve_pbr` remains
    the sole fill-the-gap helper, and this delta's +629 lines are new authored
    fields (`soft_lighting`, glass optics, blend modes) plus `sanitize_finite`
    and the `path_indicates_ice` classifier — no per-draw reclassification
    reintroduced.

## Premises checked and disproved (not reported)

- **"`NameIndex` / `SubtreeCache` invalidation keyed on `Name` component count
  can go stale across an equal-count unload/load."** Structurally true
  (`byroredux/src/systems/animation.rs:544-585`), but `SubtreeCache` is keyed by
  root `EntityId` and ids are never recycled, so a stale entry is a miss, never
  a wrong hit; and a cell swap that leaves the `Name` count *exactly* unchanged
  is not demonstrable on real data. Not filed.
- **"`AnimationStack::play` called every tick with a nonzero blend time leaks
  layers."** The leak shape the skill names is real in principle but bounded by
  `blend_time / dt`, and the only `play` caller (`apply_pending_transition`,
  `controller.rs:278`) clears `pending_request` on the same call, so it cannot
  fire per tick.
- **"`collect_candidates` holds an `InteractionCandidateScratch` write guard
  across five component acquisitions."** True
  (`byroredux/src/interaction.rs:864-874`), but the scratch has exactly one
  access site in the workspace and `interaction_system` is a `Stage::Update`
  exclusive — no partner exists to invert the order.
- **"`combat::disable_actor_ai` is a divergent copy of
  `clear_ambient_behavior`."** Stale — #3029 already collapsed it;
  `byroredux/src/combat.rs:410-412` is a one-line delegation.
- **"`timer_tick_system` allocates a `Vec` per frame."** `Vec::new()` does not
  allocate, and the vector stays empty on every frame with no expiry.

## Next Step

```
/audit-publish docs/audits/AUDIT_ECS_2026-08-27.md
```
