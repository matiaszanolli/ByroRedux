# Fallout 4 Compatibility Audit — 2026-08-27

**Scope**: all 9 `/audit-fo4` dimensions, full comprehensive run (no `--focus`
filter). Executed solo, single-agent, no sub-agent fan-out (nested-agent result
relay is unreliable in this project).

**Game data**: `/mnt/data/SteamLibrary/steamapps/common/Fallout 4/Data/`
(`Fallout4.esm`, `Fallout4 - Meshes.ba2`, `- MeshesExtra.ba2`,
`- Materials.ba2`, `Fallout4 - Geometry.csg`).

**Delta since the prior report** (`docs/audits/AUDIT_FO4_2026-08-24.md`, commit
`147daae7`): 152 commits. 98 files under the skill's dimension entry-points
changed (+6 987 / −1 094). Every one of that report's regression pins was
re-read against current code rather than assumed; three new findings below are
NOT delta regressions — they are pre-existing gaps this run's real-data probes
surfaced for the first time.

**Build**: `cargo check --workspace --all-targets` is clean at HEAD (warnings
only, all in pre-existing `_tmp_*` scratch examples). The `E0004` break in
`crates/scripting/examples/fragment_coverage.rs` the 2026-08-24 report worked
around is **gone**.

**Parse rate (re-run, not cited)**:
`cargo test --release -p byroredux-nif --test parse_real_nifs -- --ignored parse_rate_fo4_all_meshes`

```
[Fallout 4/Fallout4 - Meshes.ba2]      41 697/41 697 NIFs: clean 100.00% (0 truncated / 0 failed)
[Fallout 4/Fallout4 - MeshesExtra.ba2] 124 871/124 871 NIFs: clean 100.00% (0 truncated / 0 failed)
```

---

## Executive Summary

**The 2026-08-24 HIGH is fixed.** `FO4-2026-08-24-D6-01` /
`ESM-2026-08-24-D5-01` (the `WATR.DNAM` offsets 12/16 misread that collapsed
every vanilla FO4 water body's above-water fog ramp to ~1 world unit) was
closed by `#3270`: `decode_dnam_fo4` no longer reads those offsets at all and
`fog_near`/`fog_far` fall back to the canonical 80.0/600.0 defaults, matching
the FO76/Starfield arms
(`crates/plugin/src/esm/records/misc/water.rs:1068-1082`). The synthetic
fixture that encoded the false premise was rewritten with the real
`ExtOceanWater` bytes and now asserts the defaults
(`water.rs:2063-2119`). Verified by reading the diff, not the commit message.

**Three NEW findings, all MEDIUM, plus one LOW.** None is a delta regression;
all three are long-standing gaps that only surface under real-data measurement:

1. **`BSLightingShaderProperty.texture_clamp_mode` is parsed and then dropped**
   at the FO4/Skyrim import boundary. Measured over both vanilla mesh archives:
   **63 865 of 810 489** non-stub FO4 lit shader properties (7.9%) author a
   non-`WRAP_S_WRAP_T` address mode, and every one of them renders REPEAT/REPEAT.
   The sibling BGSM `tile_u`/`tile_v` pair is dropped too (31/6 899 materials).
   This is exactly the defect `#610` fixed for Oblivion/FO3/FNV — the Skyrim+
   lit path was never given the same treatment.
2. **Precombine shared geometry is uploaded once per placed instance.**
   `build_precombine_meshes` clones the decoded object per
   `BSPackedGeomDataCombined` transform, and the mesh cache keys on the
   flattened sub-mesh index, so N instances of one shared object become N
   distinct GPU vertex/index buffers **and N distinct BLASes**, with no
   instanced-draw batching. Worst single vanilla `_oc.nif` measured:
   13.6 MiB of unique geometry uploaded as 62.3 MiB across 2 257 meshes.
3. **`CsgArchive::chunk_bytes` inflates a `.csg` chunk into an unbounded
   `Vec`** and only rejects an over-sized result *after* it has been
   materialised — a hostile or corrupt `.csg` is a memory-exhaustion vector on
   the one FO4-only archive format.

**Everything else holds.** All seven M49 CSG pins, the BA2 exhaustive version
match, both FO4 collision arms, `FO4_ENV_SCALE` (zero occurrences tree-wide),
every FO4 slot→role arm, the metalness-from-saturation rule (`#1476`), the
blend-factor identity pass-through (`#1823`), the narrowed
`merge_external_material` signature, `MAX_PKIN_DEPTH`, the `BSConnectPoint`
attach-graph spawn, and the `#1592` Model_Space_Normals / Alpha_Test OR-in were
re-read directly and are unchanged. The `#973` per-shape MSWP consumer the
2026-08-24 report verified is still in place and correctly threaded through
`spawn_synth_child` (the `mat_provider.as_deref_mut()` → `mat_provider`
narrowing in `synth_child.rs:668` is a borrow cleanup, not a behaviour change).

**Total findings this cycle: 4** — 0 CRITICAL, 0 HIGH, 3 MEDIUM, 1 LOW.
Prior-cycle HIGH: closed.

### Per-dimension summary

| # | Dimension | Findings | Notes |
|---|---|---:|---|
| 1 | M49 precombined geometry — CSG + decode + spawn | 2 MEDIUM | all seven pins hold; per-instance upload amplification + unbounded CSG inflate are new |
| 2 | BGSM/BGEM consumption + metalness-from-chromaticity | 0 | `#1476`/`#1823`/narrowed-signature hold; `#2607`/`#2608`/`#3373` are correct additions |
| 3 | BA2 reader — GNRL + DX10 exhaustive match | 0 | exhaustive `match` intact; delta is Starfield/LZ4 hardening only |
| 4 | NIF BSVER 130 + half-float + FO4 collision | 0 | both collision arms live; `#2343` replaced the bare `130..=139` with named constants, band identical |
| 5 | FO4 shader flags + BGSM PBR routing (slot→role) | 1 MEDIUM + 1 LOW | all 8 FO4 slots route (0 unrouted, measured); `texture_clamp_mode` is the dropped field |
| 6 | ESM records (FO4 data through the parser) | 0 | prior HIGH closed by `#3270`; TXST `DecalData` + category index intact |
| 7 | NIFAL canonical material translation | 0 | single boundary intact and *widened* correctly by `#2490` |
| 8 | FO4 cell load end-to-end (SCOL/PKIN expansion) | 0 | `#2611` closed the SCOL→PKIN direction; depth cap shared and bounded |
| 9 | Real-data validation + forward scope | 0 | 100.00% on 166 568 NIFs, re-run this session |

---

## Findings

### FO4-2026-08-27-D5-01: `BSLightingShaderProperty.texture_clamp_mode` is parsed then dropped — 7.9% of vanilla FO4 lit materials lose their authored texture-address mode

- **Severity**: MEDIUM
- **Dimension**: 5 (FO4 shader flags & BGSM PBR routing) ∩ NIFAL translatable-field drop
- **Location**: `crates/nif/src/import/material/dedicated_shader.rs:310-325`
  (`apply_bs_lighting_shader`); field parsed at
  `crates/nif/src/blocks/shader.rs:722`; sibling consumer that *does* work at
  `crates/nif/src/import/material/dedicated_shader.rs:512-518`
  (`BSEffectShaderProperty`)
- **Status**: NEW
- **Description**: nif.xml declares `Texture Clamp Mode` as an unconditional
  `BSLightingShaderProperty` field for every Skyrim-and-later BSVER
  (`nif.xml:6597`, no `vercond`), and the parser reads it into
  `BSLightingShaderProperty::texture_clamp_mode: u32`
  (`crates/nif/src/blocks/shader.rs:722`). `apply_bs_lighting_shader` — the
  importer arm that owns this property class for FO4, Skyrim, FO76 and
  Starfield — copies **`uv_offset`, `uv_scale`, `alpha`, `specular_color`,
  `specular_strength`, `glossiness`** and the whole FO4 PBR/DLC tail out of the
  block, but never touches `texture_clamp_mode`:
  ```rust
  info.specular_color = shader.specular_color;
  info.specular_authored = true;
  info.specular_strength = shader.specular_strength;
  info.glossiness = shader.glossiness;
  info.uv_offset = shader.uv_offset;
  info.uv_scale = shader.uv_scale;
  info.has_uv_transform = true;
  info.alpha = shader.alpha;
  ```
  The three sites that *do* write `MaterialInfo::texture_clamp_mode` are all
  legacy: `apply_texturing_property` (`NiTexturingProperty`, Oblivion/FO3/FNV,
  `legacy_properties.rs:272-276`), `apply_pp_lighting_property` /
  `apply_no_lighting_property` / `apply_misc_shader_properties`
  (`legacy_properties.rs:405-407`, `:525-527`, `:596-598`, `:609-611`), and the
  `BSEffectShaderProperty` arm (`dedicated_shader.rs:518`). FO4 authors none of
  those on ordinary lit architecture, so `MaterialInfo::texture_clamp_mode`
  keeps its `3` (`WRAP_S_WRAP_T`) default (`import/material/mod.rs:1075`,
  `:1202`) and is carried verbatim through `into_imported_material`
  (`mod.rs:1455`) → `translate_material` (`material_translate.rs:561`) →
  `resolve_texture_with_clamp` (`cell_loader/spawn/mesh_instance.rs:648-653`,
  `scene/nif_loader.rs:930`), which is the *only* input to the renderer's
  4-sampler `TexClampMode` table (`crates/renderer/src/texture_registry.rs:171-183`).
  The **BGSM half is dropped too**: `BaseMaterial` parses `tile_u`/`tile_v`
  from the tile-flags word (`crates/bgsm/src/base.rs:173-175`), and
  `merge_external_material` forwards `u_offset`/`v_offset`/`u_scale`/`v_scale`
  into `uv_offset`/`uv_scale`
  (`byroredux/src/asset_provider/material.rs:1527-1528`, `:1727-1728`) but
  never maps the tile pair onto `texture_clamp_mode`. So neither of FO4's two
  authoring channels reaches the sampler.
- **Evidence** (measured this session against both vanilla mesh archives; probe
  walked every `.nif`, downcast every `BSLightingShaderProperty`, skipped
  `material_reference` stubs, histogrammed `texture_clamp_mode`):
  ```
  files parsed 159 866, non-stub BSLightingShaderProperty 810 489
    texture_clamp_mode 0 (CLAMP_S_CLAMP_T):  57 365
    texture_clamp_mode 1 (CLAMP_S_WRAP_T):        41
    texture_clamp_mode 2 (WRAP_S_CLAMP_T):     6 459
    texture_clamp_mode 3 (WRAP_S_WRAP_T):    746 624
  ```
  63 865 / 810 489 = **7.88%** author a non-default mode; zero values fell
  outside the `0..=3` enum, which independently confirms the field is being read
  at the right stream position. Sample authors are exactly the asset classes
  `#610` was filed about — architecture wall-kit trim and their LOD siblings:
  `meshes\interiors\building\brick\med_wallkit\bldbrickmdwalltophole05.nif`,
  `meshes\architecture\buildings\hightech\lobby\hitextlobbycornerccapbottom01.nif`,
  `meshes\architecture\buildings\neoclassical\nca1x1trime01.nif`,
  `meshes\lod\buildings\decokit\decomaina1x2wall01_lod_0.nif`.
  BGSM side, over `Fallout4 - Materials.ba2`:
  ```
  materials parsed 6 899 (0 errors)
    tile_u && tile_v (WRAP/WRAP): 6 862
    tile_u == false:                  31
    tile_v == false:                  28
    both false (CLAMP/CLAMP):         22
      e.g. materials\landscape\plants\vinedecals.bgsm, materials\props\lab\beakers.bgem,
           materials\landscape\trees\elmprewaratlas.bgsm, materials\landscape\grass\meadowgrassobj01.bgsm
  ```
- **Impact**: every FO4 surface whose UVs leave `[0,1]` — atlas-packed foliage
  cards, decals, wall-kit trim pieces, LOD atlases — samples with REPEAT
  instead of the authored CLAMP, so the atlas neighbour bleeds across the card
  edge instead of the border texel holding. The failure is invisible where UVs
  stay inside the unit square (most walls), which is why it survived: it shows
  up as a thin wrong-colour fringe on foliage/decal edges, not as a broken
  scene. Blast radius is FO4 **and Skyrim/FO76/Starfield** — the same import arm
  owns all four — but this report scopes only the FO4 measurement. Not a
  regression: this path has never consumed the field.
- **Related**: `#610` (the identical fix for `NiTexturingProperty` +
  `BSEffectShaderProperty`); `#2328` / FO3-D1-06 (the `_consumed` precedence
  gate the new writer should reuse); `#2571` / OBL-D5-01 (put
  `texture_clamp_mode` on canonical `Material`, which is what makes the drop
  observable end-to-end); FO4-2026-08-27-D5-02 below (the divergent default).
  **Distinct from `OBL-2026-08-27-01`** (`docs/audits/AUDIT_OBLIVION_2026-08-27.md`,
  HIGH), which is the *legacy* `NiTexturingProperty` nibble mis-decode: that
  report correctly notes FO4 is unaffected by it "because [it carries] no
  `NiTexturingProperty`" — which is exactly why FO4's clamp authoring has to
  come from the lit shader property, and exactly the arm that never reads it.
  The two findings are complementary halves of the same field's coverage and
  should be fixed together.
- **Suggested Fix**: in `apply_bs_lighting_shader`, beside the existing
  `info.uv_offset`/`info.uv_scale` copy, add
  `if !info.texture_clamp_mode_consumed { info.texture_clamp_mode = shader.texture_clamp_mode as u8; info.texture_clamp_mode_consumed = true; }`
  — the same `_consumed`-gated shape `legacy_properties.rs` already uses, so a
  `NiAlphaProperty`-era inherited property cannot overwrite the shape's own
  value. Separately, map BGSM's `(tile_u, tile_v)` onto the `TexClampMode`
  encoding in `merge_external_material` (`tile_u` → S-wrap bit,
  `tile_v` → T-wrap bit; `WRAP_S_WRAP_T = 3`), gated so a `false/false` BGSM
  cannot silently clobber an authored NIF value on a mesh that has both.

---

### FO4-2026-08-27-D1-01: precombine shared geometry is uploaded once per placed instance — up to 4.6× VRAM and one BLAS per instance on a single `_oc.nif`

- **Severity**: MEDIUM
- **Dimension**: 1 (M49 precombined geometry — spawn)
- **Location**: `byroredux/src/cell_loader/precombined.rs:762-771`
  (`build_precombine_meshes`); cache-key consumer at
  `byroredux/src/cell_loader/spawn/mesh_instance.rs:429-443`; cache-key
  definition at `crates/renderer/src/mesh.rs:229-235`
- **Status**: NEW
- **Description**: `build_precombine_meshes` decodes each
  `BSPackedGeomObject`'s shared geometry **once** and then materialises one full
  `ImportedMesh` per `BSPackedGeomDataCombined` instance transform:
  ```rust
  for inst in &geom.instances {
      let mesh = decoded
          .clone()
          .into_imported_mesh(&inst.transform, geom.material.clone());
      meshes.push(mesh);
  }
  ```
  `decoded.clone()` deep-copies `positions` / `normals` / `tangents` / `uvs` /
  `colors` / `indices`; `into_imported_mesh` then differs only in
  `translation` / `rotation` / `scale`
  (`crates/nif/src/import/precombine.rs:66-85`). Downstream, the GPU-mesh dedup
  layer keys on `MeshCacheKey = (String, u32)` — *lowercased model path* plus
  *sub-mesh index* (`crates/renderer/src/mesh.rs:229-235`) — and
  `prepare_mesh_uploads` uses the mesh's position in the flattened `Vec`
  (`mesh_instance.rs:429-437`, `for (sub_mesh_index, mesh) in imported.iter().enumerate()`).
  Each instance therefore claims a **distinct** cache slot: distinct
  `GpuMesh` (vertex + index buffers), distinct BLAS build, and — because the
  draw batcher batches on mesh handle — no instanced draw merging either.
  Nothing on the path recognises that N of these meshes are byte-identical
  geometry differing only by transform, which is precisely the case
  `MeshCacheKey` exists to collapse for ordinary REFR placements (`#879`).
- **Evidence** (measured this session over a 4 000-file sample of the 120 387
  `meshes\precombined\*_oc.nif` entries in `Fallout4 - MeshesExtra.ba2`,
  resolving every object against `Fallout4 - Geometry.csg`, sizing at the
  engine's 104-byte `Vertex` + `u32` index):
  ```
  nifs 4 000, objects 41 574, instances 67 585        (1.63 instances / object)
  unique geometry 1 904.1 MiB, uploaded-as-placed 2 617.2 MiB   (x1.37 overall)
  max instances on one object: 146
  instances-per-object histogram (16 = 16+):
    {1: 30160, 2: 6654, 3: 2173, 4: 1131, 5: 397, 6: 299, 7: 168,
     8: 165, 9: 93, 10: 53, 11: 35, 12: 52, 13: 30, 14: 21, 15: 18, 16+: 125}
  worst single _oc.nif: meshes\precombined\0004b2c0_00120a99_oc.nif
    -> 2 257 meshes, unique 13.6 MiB, uploaded 62.3 MiB   (x4.6)
  ```
  The distribution matters as much as the mean: 72% of objects are placed once
  and cost nothing extra, but the tail is where the cells are — a single
  precombine tile can carry 2 257 mesh entities and 2 257 BLAS builds where
  ~500 distinct geometries would do.
- **Impact**: three compounding costs on FO4 cell load, all in the tail cells
  (dense settlements, the Institute, Diamond City) rather than uniformly:
  VRAM for duplicated vertex/index buffers (up to 4.6× on the measured worst
  tile), redundant BLAS builds against the `AccelerationManager`'s reserve
  floors and eviction thresholds, and a lost instanced-draw batch (N draws
  where 1 instanced draw would serve). It is a performance/VRAM defect, not a
  correctness one — the geometry rendered is right.
- **Related**: `#879` / CELL-PERF-01 (established the refcounted
  `(path, sub_mesh_index)` dedup this path bypasses); `a30c088a` (the LOD-tier
  selection just above, which is correct — one tier per object, verified:
  `precombined.rs:719-722` still takes `max_by_key(|&(c, _)| c)` over the three
  `lod_counts`).
- **Suggested Fix**: give the precombine path a cache key that separates the
  *object* from its *placement* — e.g. key the shared geometry as
  `(oc_nif_path, object_ordinal)` and hand every instance of that object the
  same handle, letting the transform live only on the spawned entity's
  `Transform`. That collapses the uploads and the BLASes, and lets the existing
  draw batcher instance them. `PrecombineGeometry` would need to be uploaded
  once and placed N times rather than cloned N times, i.e. the split belongs
  between `build_precombine_meshes` and `spawn_placed_instances`, not inside
  the mesh registry.

---

### FO4-2026-08-27-D1-02: `CsgArchive::chunk_bytes` inflates a `.csg` chunk into an unbounded `Vec` and range-checks it only afterwards

- **Severity**: MEDIUM
- **Dimension**: 1 (M49 precombined geometry — CSG reader) ∩ safety
- **Location**: `crates/bsa/src/csg.rs:274-290`
- **Status**: NEW
- **Description**: `chunk_bytes` caps the **compressed** read via
  `checked_chunk_size(entry.compressed_size, …)` (`csg.rs:271`, bound
  `MAX_CHUNK_BYTES = 1 GiB`, `crates/bsa/src/safety.rs:36`) but places no bound
  on the *decompressed* size — it inflates into a `Vec` that only had
  `CHUNK_SIZE` reserved and then rejects the result after the fact:
  ```rust
  let mut raw = Vec::with_capacity(CSG_CHUNK_SIZE);
  ZlibDecoder::new(&comp[..]).read_to_end(&mut raw)?;
  if raw.len() > CSG_CHUNK_SIZE {
      return Err(io::Error::new(
          io::ErrorKind::InvalidData,
          format!("CSG chunk {idx} inflated to {} > {CSG_CHUNK_SIZE}", raw.len()),
      ));
  }
  ```
  `read_to_end` grows the buffer until the deflate stream ends, so the guard
  can only fire once the whole thing is already resident. Every other
  attacker-controlled inflate in `crates/bsa` at least pre-caps the target size
  (`decompress_chunk`'s `checked_chunk_size_usize` on `unpacked_size`,
  `ba2.rs:719`), and the module docstring for `safety.rs` names decompressed
  payload sizes as one of the three classes it exists to bound
  (`safety.rs:5-9`) — this call site was simply not wired to it.
- **Evidence**: deflate's maximum expansion ratio is ~1032:1. A crafted
  64 MiB `.csg` chunk — well inside the 1 GiB compressed cap, and small enough
  to ship inside an ordinary mod archive — inflates to ~66 GiB before
  `raw.len() > CSG_CHUNK_SIZE` is ever evaluated. The path is reached from
  `read_psg` (`csg.rs:241`), which `build_precombine_meshes` calls for every
  precombine object of every FO4 cell
  (`byroredux/src/cell_loader/precombined.rs:736`), and the `.csg` is opened
  from the load order by name, so a mod supplying
  `<Plugin> - Geometry.csg` reaches it with no further gate.
- **Impact**: OOM-kill / allocator abort during an FO4 cell load on a hostile
  or merely corrupt `.csg`. `.csg` is FO4-only, and modded FO4 is precisely the
  case where third-party `<Plugin> - Geometry.csg` blobs appear, so the input
  is genuinely untrusted. No memory-safety violation (this is `flate2`'s safe
  API into a `Vec`) — the failure is resource exhaustion, not corruption, which
  is why this is MEDIUM and not HIGH.
- **Related**: `#586` / FO4-DIM2-01 (the BA2-side allocation-safety sweep that
  produced `safety.rs`); `#1986` / FO4-D1-01 (the *short* interior-chunk guard
  immediately below, `csg.rs:292-300` — added for exactly the same
  "reject rather than return wrong bytes" reason, but only in the under-size
  direction); the sibling `Ba2Compression::Zlib` arm at `ba2.rs:721-727`, which
  has the same `read_to_end` shape but does at least pre-validate its declared
  `unpacked_size`.
- **Suggested Fix**: bound the reader instead of the result —
  `ZlibDecoder::new(&comp[..]).take(CSG_CHUNK_SIZE as u64 + 1).read_to_end(&mut raw)?;`
  keeps the existing `> CSG_CHUNK_SIZE` check as the error path (the `+1` makes
  an over-long stream still trip it) and caps the allocation at 64 KiB + 1. One
  line, no behaviour change on any well-formed archive.

---

### FO4-2026-08-27-D5-02: the same `texture_clamp_mode` field carries two different defaults across the three material tiers

- **Severity**: LOW
- **Dimension**: 5 (FO4 shader flags) ∩ 7 (NIFAL canonical translation)
- **Location**: `crates/nif/src/import/material/mod.rs:1075` and `:1202`
  (`MaterialInfo`, default `3`) vs. `crates/nif/src/import/types.rs:643`
  (`ImportedMaterial`, default `0`) vs.
  `crates/core/src/ecs/components/material.rs:551` (`Material`, default `0`)
- **Status**: NEW
- **Description**: `3` is `WRAP_S_WRAP_T`, the Gamebryo default that `#610`
  established and that `resolve_texture` hardcodes for its clamp-unaware
  variant (`byroredux/src/asset_provider/texture.rs:285-297`, "3 = WRAP_S_WRAP_T
  per nif.xml — the legacy REPEAT default"). `0` is `CLAMP_S_CLAMP_T`, the
  *opposite* end of the enum. `MaterialInfo` — the tier the NIF walker actually
  fills — uses `3`; the two tiers below it default to `0`, and
  `Material::texture_clamp_mode`'s own doc rationalises this as mirroring
  "that struct's own `0` (CLAMP_S_CLAMP_T) parser-stub default"
  (`material.rs:388-390`). Today the divergence is inert on every real path
  because `into_imported_material` overwrites the field verbatim
  (`mod.rs:1455`) and the only production `ImportedMesh::from_geometry`
  consumers that keep `ImportedMaterial::default()` are the fog volumes
  (`byroredux/src/fog.rs:1151`, `:1176`), which are untextured. The FO4
  precombine path — named in `from_geometry`'s own doc as its other production
  consumer (`types.rs:844-846`) — is safe only because
  `into_imported_mesh` reassigns `mesh.material` immediately afterwards
  (`crates/nif/src/import/precombine.rs:84`).
- **Evidence**: the three literals above; `resolve_texture_with_clamp`'s
  registry contract, where `0` selects the CLAMP/CLAMP sampler and out-of-range
  values fall back to `3` (`texture.rs:298-305`,
  `crates/renderer/src/texture_registry.rs:171-183`).
- **Impact**: latent. The next synthetic-geometry producer that builds through
  `ImportedMesh::from_geometry` and *does* bind a tiling texture — distant
  object LOD, terrain LOD, a future `_precomb.nif` collision-visual path — will
  silently get CLAMP/CLAMP on a tiling atlas and read as one stretched edge
  texel per axis. The wrong default is also load-bearing documentation: the
  `Material` doc currently teaches the reader that `0` is the field's default,
  which contradicts `#610`'s rule.
- **Related**: FO4-2026-08-27-D5-01 (the live half of the same field); `#610`;
  `#2571` / OBL-D5-01 (which propagated the `0` down to `Material`).
- **Suggested Fix**: set `ImportedMaterial::default().texture_clamp_mode = 3`
  and `Material::default().texture_clamp_mode = 3`, matching `MaterialInfo` and
  `resolve_texture`'s own hardcoded fallback, and correct the two doc comments.
  Note this is a saved `Material` field (`FORMAT_MAJOR` 6 made it required,
  `crates/save/src/snapshot.rs:65-67`) but changing a *default* does not change
  the serialised shape, so no format bump is needed.

---

## Regression pins re-verified (0 findings)

Read directly against current code at HEAD, not carried forward from the prior
report.

**Dimension 1 — M49 precombines.** `absorbed_refs_or_empty` still gates
suppression on `pc_spawned > 0` (`precombined.rs:76-85`) and both loaders route
through it (interior `load.rs:475-476`, exterior `exterior.rs:1830`). LOD tier
selection still takes exactly one tier per object
(`precombined.rs:719-726`). The opaque-architecture alpha-blend restore
(`887aae52`) is intact — `precombined.rs:302-324` snapshots
`(has_alpha, src_blend_mode, dst_blend_mode)` before
`merge_external_material` and writes the pre-merge triple back after, so FO4's
"Standard" blend on Institute metal cannot promote precombined walls to
`MATERIAL_KIND_GLASS`. Owning-plugin CSG routing (`#2369` / `#1590`) is
unchanged. `csg.rs`, `precombine.rs` and `precombined.rs`'s decode/spawn logic
are byte-identical to `147daae7`.

**Dimension 2 — BGSM/BGEM.** `bgsm_metalness`
(`byroredux/src/asset_provider/material.rs:265-278`) still branches
`pbr → luminance` / `non-pbr → saturation (max−min)/max` — `#1476` holds.
`merge_external_material` still takes `&mut ImportedMaterial`, not
`&mut ImportedMesh`. The blend pass-through is still an identity narrowing, and
no `gl_to_gamebryo_blend` exists anywhere in the tree — `#1823` holds. Two
additions this cycle are correct, not regressions: `forward_bgsm_rim_subsurface`
(`#2607`, `material.rs:76-97`) and `forward_bgsm_env_map_scale` (`#2608`), both
gated on the authored enable bit precisely because the underlying group is only
parsed on the `version < 8` / `version < 10` branch — verified against
`crates/bgsm/src/bgsm.rs:205-241`, where `rim_lighting`, `rim_power`,
`back_light_power`, `subsurface_lighting` and `back_lighting` are all inside
`else { … }` on `version >= 8` (so a modern BGSM leaves them false and the
forward correctly no-ops, rather than fabricating the struct's 2.0/0.3
never-parsed defaults). Vanilla FO4 BGSMs are version 2, so the forward is live
for FO4 content. `#3373` (`59b85565`) closed a real hole by wiring the four
BGEM glass-optics scalars into `Material::sanitize_finite`; `FORMAT_MAJOR` 7
correctly covers them on the save path.

**Dimension 3 — BA2.** The exhaustive `match` over `{1, 2, 3, 7, 8}` with an
`other =>` bail is intact (`ba2.rs:227-280`); `BA2_V_FO4`,
`BA2_V_FO4_NEXT_GEN_TEX`, `BA2_V_FO4_NEXT_GEN_MESH` all still force
`Ba2Compression::Zlib` on one arm (`:227`). The whole 258-line delta is
Starfield-side hardening (`#2360` v3 header-boundary log ordering, `#2097`
`catch_unwind` on the LZ4 arm, `#3392` `safe-decode` feature pin, `#3393`
char-boundary slicing in the source-order pins) and touches no FO4 code path.

**Dimension 4 — NIF BSVER 130 / collision.** `BhkMultiSphereShape`
(`crates/nif/src/import/collision/shape.rs:110`) and `BhkConvexListShape`
(`:235`) both still resolve to `CollisionShape`. `FO4_ENV_SCALE` has zero
occurrences tree-wide. `#2343`/`#2597` replaced the DLC-tail gate's bare
`(130..=139)` with `(bsver::FALLOUT4..bsver::FO4_DLC_UPPER)`
(`crates/nif/src/blocks/shader.rs:1064-1090`) — `130..140` is the identical
band, and the `Rimlight Power >= f32::MAX && is_finite()` sentinel gate on
`Backlight Power` is unchanged. `crates/nif/src/blocks/skin.rs`'s SSE window is
now `bsver::SKYRIM_SE..bsver::FALLOUT4` (`skin.rs:227`), which correctly
excludes FO4 — nif.xml's `#BS_SSE#` is `BSVER == 100` exactly
(`nif.xml:22`), and the widening to `100..130` is the documented `#126` /
NIF-206 variant-classifier accommodation, not an FO4 over-read. `#3360`'s new
`triangles_are_global` gate in `triangle_body_parts`
(`crates/nif/src/import/mesh/skin.rs:44`) keys on
`partition.global_vertex_data.is_some()`, which is `None` for FO4, so FO4's
`BSDismemberSkinInstance` remap is unchanged.

**Dimension 5 — slot→role.** All four FO4 arms are present and unchanged:
`(Fallout4, 2)` tint-or-emissive (`slot_role.rs:250-256`),
`(Fallout4 | Fallout76, 3) → GreyscaleLut` (`:279-281`),
`(Fallout4, 4) → Environment` / `(Fallout4, 5) → Wrinkle | EnvironmentMask`
(`:310-317`), `(Fallout4, 7) → Specular` (`:355`). This cycle added
`TextureRole::LightingMask` / `BackLighting` and three feature-flag inputs to
`TextureSlotContext` (`:66-69`, `:127-129`) — those route **Skyrim/Starfield**
slots 2 and 7 only, and the flag extraction that feeds them is explicitly
`matches!(slot_layout, TextureSlotLayout::Skyrim)`
(`dedicated_shader.rs:142-154`), so FO4 correctly gets `(false, false, false)`
and its own slot arms are untouched. `#1592`'s Model_Space_Normals /
Alpha_Test OR-in is live (`dedicated_shader.rs:134-140`, `:290-300`); the
`skyrim_slsf1::MODEL_SPACE_NORMALS` constant serving both games is correct and
pinned — nif.xml puts `Model_Space_Normals` at bit 12 in *both*
`SkyrimShaderPropertyFlags1` and `Fallout4ShaderPropertyFlags1`, and
`crates/nif/src/shader_flags.rs:466-467` asserts the two constants are equal.
**Measured**: over `Fallout4 - Meshes.ba2`, every populated FO4 texture-set slot
routes to a canonical role — 0 unrouted bindings across all 8 slots
(occupancy 67 934 / 66 438 / 3 216 / 5 456 / 24 238 / 1 021 / 0 / 63 314).

**Dimension 6 — ESM architecture records.** TXST's `DecalData` sink is present
(`crates/plugin/src/esm/cell/support.rs:514`). SCOL / PKIN / MOVS / MSWP all
appear in `categories()` (`crates/plugin/src/esm/records/index.rs:614-618`) and
all four are FO4-gated at the GRUP dispatch with an explicit skip log
(`records/mod.rs:301-357`). The prior cycle's `WATR.DNAM` HIGH is closed (see
Executive Summary).

**Dimension 7 — NIFAL.** `translate_material` is still the single
`ImportedMesh → Material` boundary, and `#2490` *widened* the boundary
correctly rather than eroding it: the blend/decal/facing marker derivation that
had been copy-pasted at both spawn sites is now
`attach_blend_and_facing_markers` (`material_translate.rs:604-640`), with a
source-scan test that fails if either site re-introduces an inline
`AlphaBlend {` literal (`material_translate.rs:1043-1063`). `resolve_pbr` is
still resolve-once and idempotent. The two Phase-2 post-texture resolvers
(`resolve_normal_alpha_spec_roughness`, `resolve_msn_z_source`) are documented
as such in the module header (`#2330`) and called from both sites.

**Dimension 8 — cell load.** `MAX_PKIN_DEPTH = 4` (`refr.rs:449`), and `#2611`
added the symmetric SCOL→PKIN fan-out (`refr.rs:645-668`) under the same shared
depth counter as the three existing cross-recursion guards (`:505`, `:525`,
`:631`) — all four use `depth + 1 < MAX_PKIN_DEPTH`, so SCOL↔PKIN
cross-recursion is bounded. The `BSConnectPoint` attach-graph stamp is present
(`cell_loader/spawn.rs:454`). `#973`'s per-shape MSWP consumer still
re-evaluates the `FNAM` filter and swap table against each shape's own
`material_path` (`spawn/mesh_instance.rs`), and `#3320` moved the interior
water-plane spawn above `load_references` with both a `debug_assert_eq!` on
`pending_dds_upload_count()` and a CI-reachable source-order test
(`load.rs:588-599`, `:744-771`) — that reordering does not sit between the
precombine spawn and the absorbed-REFR gate, which still evaluate in the
correct order.

---

## Disproved / non-findings investigated this session

1. **"FO4 precombines inherit `ImportedMaterial::default()`'s
   `texture_clamp_mode = 0`, so every precombined wall samples CLAMP/CLAMP."**
   False. `PrecombineGeometry::into_imported_mesh` builds through
   `ImportedMesh::from_geometry` (which does use the `0` default) but then
   assigns `mesh.material = material` from
   `precombine_material_from_shape` → `into_imported_material`
   (`precombine.rs:84`, `:338-353`), which carries `MaterialInfo`'s `3`. The
   `0` default survives only on the fog-volume path — filed as the LOW above,
   not as a live precombine defect.
2. **"FO4 skin partitions desync because `is_sse` was widened to a range."**
   False. `bsver::SKYRIM_SE..bsver::FALLOUT4` excludes 130+, matching nif.xml's
   `#BS_SSE# = (BSVER == 100)` for the `Vertex Desc` / `Triangles Copy` fields
   and `#BS_GT_FO3#` for the `LOD Level` / `Global VB` pair that *is* read on
   FO4. The 100% parse rate on 166 568 FO4 NIFs corroborates.
3. **"`#3360`'s `triangles_are_global` gate changes FO4 body-part
   assignment."** False. The gate is `partition.global_vertex_data.is_some()`,
   which the parser only populates inside the SSE window, so FO4 keeps the
   `vertex_map` remap it had.
4. **"FO4 texture-set slot 6 is authored and unrouted (the FO76 slot-6 defect,
   `#3085`, in its FO4 form)."** False, measured: FO4 slot 6 occupancy is
   **0** across `Fallout4 - Meshes.ba2`. The `(Skyrim | Fallout4 | Starfield, 6)`
   MultiLayerParallax-only arm is correct for FO4 by absence.
5. **"The new `soft_lighting` / `rim_lighting` / `back_lighting` context fields
   change FO4 slot routing, since `merge_external_material` sets them from
   BGSM."** False in effect. `merge_external_material` does set them
   (`asset_provider/material.rs:84-97`) and `mesh_instance.rs:193-195` does feed
   them into `TextureSlotContext`, but the FO4 arms for slots 2 and 7 never
   read those inputs — only the `Skyrim | Starfield` arms do. Worth knowing if
   an FO4 slot arm is ever made flag-sensitive.
6. **"`BSLightingShaderProperty`'s `wetness` block is another parsed-then-
   dropped FO4 field."** True as stated but *not* a finding — the wet-surface
   system is unimplemented by design (ROADMAP M61), and the BGSM
   `wetness_control_*` sextet is equally unconsumed, so there is no renderer
   sink to feed. Listed under forward scope instead.
7. **"The delta's `Material` growth broke save compatibility."** False.
   `FORMAT_MAJOR` was bumped to 7 for the glass-optics + authored
   lighting-response fields and to 8 for the unrelated `EquipmentSlots` split
   (`crates/save/src/snapshot.rs:68-78`), so old snapshots are rejected rather
   than silently default-filled — the `SAVE-D2-01` rule was followed.

---

## BGSM/BGEM consumption table (delta since 2026-08-24)

Only rows that changed this cycle or that a finding touches; the full table is
in `docs/audits/AUDIT_FO4_2026-08-20.md`.

| BGSM/BGEM field | merged into `ImportedMesh.material` | surfaced on canonical `Material` |
|---|---|---|
| `rim_lighting`, `rim_power` | `rim_lighting`, `rimlight_power` (v<8 gate, `#2607`) | `rim_lighting`, `rimlight_power` |
| `back_lighting`, `back_light_power` | `back_lighting`, `backlight_power` (shares the rim enable bit) | `back_lighting`, `backlight_power` |
| `subsurface_lighting`, `subsurface_lighting_rolloff` | `soft_lighting`, `subsurface_rolloff` (v<8 gate) | `soft_lighting`, `subsurface_rolloff` |
| `environment_mapping_mask_scale` | `env_map_scale` (v<10 enable-bit gate, `#2608`) | drives `resolve_pbr`'s reflection-intent arm |
| glass optics (`glass_fresnel_color`, `glass_refraction_scale`, `glass_blur_scale`, `glass_blur_scale_factor`) | BGEM v21+/v22+ | copied verbatim (`material_translate.rs:546-549`); now covered by `sanitize_finite` (`#3373`) |
| `u_offset` / `v_offset` / `u_scale` / `v_scale` | `uv_offset`, `uv_scale` | `uv_offset`, `uv_scale` |
| **`tile_u` / `tile_v`** | **dropped** | **dropped** — see FO4-2026-08-27-D5-01 |
| `specular_color` (+ `specular_mult`, `pbr`) | `metalness_override` via `bgsm_metalness` (saturation on the non-pbr branch) | `metalness` |
| `smoothness` | `roughness_override = (1.0 - smoothness).clamp(0.04, 1.0)`, applied once at parse | `roughness` |
| texture paths | `MaterialTextureSet` roles (`base_color`, `normal`, `emissive`, `greyscale_lut`, `environment`, `environment_mask`, `wrinkle`, `specular`, `smooth_spec`, `lighting`, `flow`, `inner_layer`, `back_lighting`, `lighting_mask`, `decals[4]`) | bindless handles via `MaterialTextureHandles` |
| `wetness_control_*` (6 fields) | dropped | dropped — no renderer sink, ROADMAP M61 |

---

## Forward scope chain

Unchanged in shape, updated in detail:

`_precomb.nif` collision (blocked behind the `BhkSystemBinary` Havok NP blob —
the sibling file is `<cell_formid:08x>_physics.nif`, verified against real
`MeshesExtra.ba2` data 2026-08-23, and the same blocker PHYSAL's FO4+
constraint decode sits behind) → `.uvd` occlusion volumes → MOVS physics
runtime → deeper cell coverage (LIGH power state / CONT leveled items / NPC_
face morph) → morph-target GPU consumer (`#3231` follow-up; the NIF-import half
shipped 2026-08-23) → wet-surface consumption of the already-parsed NIF
`WetnessParams` + BGSM `wetness_control_*` sextet (ROADMAP M61).

**Newly documented this cycle, deliberately not filed as a finding**: FO4
navmesh geometry decodes at **0 of 7 894** `Fallout4.esm` `NAVM` records — the
header decodes and the cell/grid is known for all 7 894, but the packed `NVNM`
body diverges from Skyrim's layout after the shared header and the reader
retains the blob verbatim rather than guessing
(`crates/plugin/src/esm/records/misc/world.rs:71-80`, `:255-259`; OpenMW skips
the FO4 form for the same reason). This is correct, deliberate, and documented
behaviour with the bytes kept for a future decoder — but it means FO4 has no
navmesh-backed pathing at all, which belongs on this chain rather than buried
in a doc comment.

**Shipped — never list as pending**: the `.csg` reader and the whole M49
precombine pipeline, the BGSM/BGEM parser, SCOL/PKIN expansion (both recursion
directions as of `#2611`), the per-shape MSWP consumer (`#973`), and the
morph-target import plumbing (`#3231` data half).

---

## Method notes (for reproduction)

The three real-data censuses in this report were run from throwaway
`crates/*/examples/_tmp_*` probes, deleted afterwards:

1. **Clamp-mode histogram** — walk every `.nif` in `Fallout4 - Meshes.ba2` +
   `- MeshesExtra.ba2`, `downcast_ref::<BSLightingShaderProperty>()`, skip
   `material_reference` stubs, histogram `texture_clamp_mode`.
2. **BGSM tile flags** — walk every `.bgsm`/`.bgem` in
   `Fallout4 - Materials.ba2` through `byroredux_bgsm::parse`, count
   `base.tile_u` / `base.tile_v`.
3. **Precombine instance duplication** — walk `meshes\precombined\*_oc.nif` in
   `MeshesExtra.ba2`, `collect_precombine_geom_refs`, pick the max-triangle LOD
   tier, size each object at `num_verts × 104 + tris × 3 × 4`, and compare the
   unique total against the per-instance total.
4. **Slot occupancy vs. routed role** — same walk as (1), resolving each
   non-empty `BSShaderTextureSet` slot through
   `byroredux_nif::import::slot_to_role` with
   `TextureSlotLayout::Fallout4`.

---

## Suggested next step

```
/audit-publish docs/audits/AUDIT_FO4_2026-08-27.md
```

Label all four `game:fo4` + `legacy-compat`, plus:
`FO4-2026-08-27-D5-01` → `nif-parser` `import-pipeline` `nifal` `medium` `bug`;
`FO4-2026-08-27-D1-01` → `performance` `memory` `import-pipeline` `medium` `bug`;
`FO4-2026-08-27-D1-02` → `safety` `import-pipeline` `medium` `bug`;
`FO4-2026-08-27-D5-02` → `import-pipeline` `nifal` `tech-debt` `low` `bug`.

TALLY: CRITICAL=0 HIGH=0 MEDIUM=3 LOW=1
