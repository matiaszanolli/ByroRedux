# Fallout 3 Compatibility Audit — 2026-08-03

**Engine**: ByroRedux (Rust + Vulkan Gamebryo/Creation-engine rebuild)
**Scope**: Readiness of the shared FNV code path for **Fallout 3** content, hunting
the FO3-vs-FNV **divergences** (not a re-run of `/audit-fnv`). 7 dimensions.
Dimensions 1, 3, 4, 6 ran as delegated sub-agents; Dimensions 2, 5, 7 were
completed both by delegated sub-agents (which finished asynchronously) and,
in parallel, by direct orchestrator analysis after the sub-agent completion
notifications proved unreliable mid-run — the richer of the two results was
kept for each. Real FO3 GOTY data validated throughout via static code
reading plus live `nif_stats`/`bsa_grep`/test-suite runs against
`/mnt/data/SteamLibrary/steamapps/common/Fallout 3 goty/Data/` (base game +
all five DLC BSAs). Run as one leg of a `comprehensive` audit-suite sweep.

---

## Executive Summary

**Compatibility level: MEDIUM-HIGH — mostly parity with FNV, but this cycle
surfaced two real HIGH-severity defects a prior audit cycle had dismissed or
missed.** FO3 ships at `bsver = 34`, the identical NIF/BSA/ESM stamp FNV
uses, and rides the shared code path end-to-end almost everywhere. Most
checklist items across all 7 dimensions verified clean against real data. But
two dimensions (1 — inline shaders, 5 — collision import) each turned up a
HIGH finding with quantified real-data blast radius, one of which **directly
contradicts a "non-bug, informational" dismissal in
`docs/audits/AUDIT_FO3_2026-07-16.md`** whose premise ("non-T rigid bodies
always ship identity translation in vanilla content") this audit empirically
disproved on the real archive (9.5% of `Fallout - Meshes.bsa` carries a
non-identity transform on a non-T body).

**Delta vs FNV**: the two HIGH findings (FO3-D1-01 env-map misclassification,
FO3-D5-01 rigid-body T-suffix collapse) are both **shared code paths**, so
they equally affect FNV/Oblivion/Skyrim — they are not FO3-exclusive bugs,
but this audit is what quantified their FO3-specific blast radius. The
genuinely FO3-distinctive functional gap remains the **Papyrus scripting
runtime** (1 257 SCPT records parse; M47.2 compiled-Papyrus recognizer slice
is in-progress, not a bug to file).

**Findings: 14 new** (after merging one exact cross-dimension duplicate —
Dimensions 1 and 2 both independently found the FO3 fire-refraction
field-drop bug). Zero CRITICAL. **2 HIGH, 5 MEDIUM, 7 LOW.**

### Findings by severity

| Severity | Count | IDs |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 2 | FO3-D1-01, FO3-D5-01 |
| MEDIUM | 5 | FO3-D1-02, FO3-D1-03, FO3-D1-04, FO3-D1-05/D2-01 (merged), FO3-D2-02 |
| LOW | 7 | FO3-D1-06, FO3-D2-03, FO3-D2-04, FO3-D5-02, FO3-D5-03, FO3-D5-04, FO3-D6-NEW-01 |

### Real-data validation (measured this audit, 2026-08-03)

| Axis | Result |
|------|--------|
| `Fallout - Meshes.bsa` NIF parse | **100.00% clean / 10 989 NIFs** (0 truncated, 0 failures, 96 distinct block types, 0 drift) |
| `Fallout - Textures.bsa` DDS | 12 261/12 261 extract clean, all valid `"DDS "` FourCC headers |
| Folder/file hash collisions | 0 across 14 167 real entries (Bethesda hash reimplemented + checked) |
| Fallout3.esm parse | `total=44 666` (navm=7 198 exact match; structured=37 468, +9 benign drift vs the 37 459 ROADMAP figure — already-flagged GOTY-patch content, not a regression) |
| Megaton interior baseline | **929 REFRs** exact match, live-tested |
| Collision census (new this cycle) | `Fallout - Meshes.bsa`: 6 903 `bhkRigidBody` (non-T) vs 2 054 `bhkRigidBodyT`; 2 704 non-T bodies carry a nonzero translation; 1 041 files (9.5%) affected — see FO3-D5-01 |
| Havok motion-type census | 6 842 FIXED→Static, 1 061 BOX_INERTIA→Dynamic, 964 KEYFRAMED→Keyframed, 87 SPHERE_INERTIA→Dynamic, 3 THIN_BOX→Dynamic — #1652 fix confirmed intact, 0 zero-mass-Dynamic misclassifications |
| `bhkConvexListShape` (#1277) | 55 real occurrences in base game (FNV baseline has zero — FO3-distinctive), all translate to `Compound` |
| `bhkMultiSphereShape` (#1277) | 0 occurrences in FO3 base+DLC — translator correct but FO3-inert (Oblivion-only content in practice) |
| Deathclaw `NiSkinData` | 5 skinned sub-meshes/`BSDismemberSkinInstance`, correctly dispatched |
| Particle emitters | 422 `NiPSysEmitter`, 361 `NiPSysEmitterCtlr`, 282 `NiPSysGrowFadeModifier`, all 0 unknown — the checklist's "unverified gap" framing does not hold |
| `BSSegmentedTriShape` | Parses correctly but 0 real occurrences across base+DLC — checklist premise ("FO3 biped body parts") doesn't hold against real data |
| NPC hand-mesh paths | `skeleton.nif`/`upperbody.nif`/`lefthand.nif`/`righthand.nif` all confirmed present at hardcoded paths in `Fallout - Meshes.bsa` |
| FaceGen `headhuman.*` | Byte-identical to already-tested FNV fixture; parses (test coverage gap noted, FO3-D6-NEW-01) |
| SpeedTree `.spt` | 10/10 real files, 100% TLV coverage, 0 unknown tags |

---

## Dimension Findings

### Dimension 1 — Inline Shaders / Material Import → Render Path

#### FO3-D1-01: `env_map_scale` forwarded from FO3 inline shaders with no `Environment_Mapping` flag gate, driving the PBR classifier's env arm on every lit mesh
- **Severity**: HIGH
- **Location**: `crates/nif/src/import/material/legacy_properties.rs:325` (PPLighting), `:386` (NoLighting), `:445`/`:452`/`:458`/`:469` (Tile/Sky/TallGrass/Water), `:490` (BaseOnly); consumed at `crates/core/src/ecs/components/material.rs:647-671`
- **Status**: NEW (sibling of CLOSED #1873 "chrome flyer" — that fix added `specular_authored`; this is the un-gated *input* reaching the same arm)
- **Description**: `apply_pp_lighting_property` writes `info.env_map_scale = shader.shader.env_map_scale` unconditionally. Per nif.xml the field defaults to 1.0 and is only meaningful when F3SF1 bit 7 `Environment_Mapping` (or bit 21/17 window/eye variants) is set — none of which `fo3nv_f1` even declares. Essentially every FO3 `BSShaderPPLightingProperty` mesh exits import with `env_map_scale ≈ 1.0` regardless of authored intent.
- **Impact**: FO3 statics carrying both `NiMaterialProperty` + `BSShaderPPLightingProperty` (the common vanilla shape) classify as 0.4-metalness/0.55-roughness semi-conductors — under the RT reflection gate they pick up environment response on plaster, concrete, painted metal, wood. Same visual class as closed #1873, reached via the un-gated input. Also permanently disables the Gamebryo normal-alpha-as-spec path (`normal_alpha_spec_applies` requires `env_map_scale <= 0.3`). Shared with FNV; FO3's heavier `NiMaterialProperty`+PPLighting pairing makes it the dominant population.
- **Related**: #1873 (CLOSED, `634873db`), `feedback_chrome_means_missing_textures.md`, FO3-D1-02
- **Suggested Fix**: Add `ENVIRONMENT_MAPPING`/`WINDOW_ENVIRONMENT_MAPPING`/`EYE_ENVIRONMENT_MAPPING` (bits 7/21/17) to `shader_flags::fo3nv_f1` and gate the `env_map_scale` write on them; leave at 0.0 otherwise.

#### FO3-D1-02: FO3 parallax POM enabled by texture-slot presence, never by authored flags; height scale passed through un-converted at nif.xml's 1.0 default
- **Severity**: MEDIUM
- **Location**: `crates/nif/src/import/material/legacy_properties.rs:265-302`; `crates/nif/src/blocks/shader.rs:90-97`; `crates/renderer/shaders/triangle.frag:186-198`; `crates/renderer/shaders/include/material_sampling.glsl:18-23,74`
- **Status**: NEW
- **Description**: Three compounding gaps — (a) no gate on bits 11 (`Parallax_Shader_Index_15`)/28 (`Parallax_Occulsion`), POM driven purely by texture-slot-3 presence; (b) `parallax_max_passes`/`parallax_height_scale` written unconditionally, unlike the sibling `NiTexturingProperty` branch which is gated + uses engine defaults; (c) no unit conversion — the shader's own contract says `heightScale` is typically 0.02–0.08, but the FO3 `bsver<=24` fallback delivers 1.0 (~25× mismatch), producing texture swimming at grazing angles.
- **Impact**: FO3 parallax architecture (Pitt/Point Lookout/Megaton brick+concrete) gets either texture swimming or unauthored POM.
- **Related**: #453, #452, #725/NIF-D4-06, FO3-D1-01
- **Suggested Fix**: Add the two flag bits to `fo3nv_f1`, gate the parallax writes on them + `parallax_map.is_some()`, and define the FO3 Parallax Scale → engine `heightScale` conversion at the import boundary.

#### FO3-D1-03: `fo3nv_f1::OWN_EMIT` is mislabeled — nif.xml bit 22 is `Tree_Billboard`, not `Own_Emit`; a unit test cements the wrong fact
- **Severity**: MEDIUM
- **Location**: `crates/nif/src/shader_flags.rs:34-37,331-339`; use site `crates/nif/src/import/material/dedicated_shader.rs:526-532`
- **Status**: NEW
- **Description**: FO3/FNV `BSShaderFlags` bit 22 is `Tree_Billboard` per nif.xml; there is no `Own_Emit` bit in the FO3/FNV enum. The module's own purpose is pinning per-game bit semantics, yet it declares this constant as a "cross-game constant" and a passing test asserts the false claim.
- **Impact**: Latent — the only production read of this constant is inside a genuinely Skyrim+ code path (`apply_bs_effect_shader`), so no live miscompute today. But the wrong fact is protected by a green test — exactly the failure mode `shader_flags.rs` exists to prevent — and would silently additive-composite every FO3 tree/foliage billboard if ever reached from the FO3 module.
- **Related**: #414, #592
- **Suggested Fix**: Rename to `fo3nv_f1::TREE_BILLBOARD`, move the Own_Emit alias to `skyrim_slsf1::OWN_EMIT`, rewrite the test.

#### FO3-D1-04: FO3 `BSShaderProperty.shader_type` is parsed and never consumed — Skin/Water/Sky/Tile/Lighting30 inline materials indistinguishable from Default
- **Severity**: MEDIUM
- **Location**: `crates/nif/src/blocks/base.rs:227-271` (parsed); `crates/nif/src/import/material/legacy_properties.rs:239-336` (never read); doc-rot at `crates/nif/src/import/material/mod.rs:421-426`
- **Status**: NEW
- **Description**: No importer arm reads `shader.shader.shader_type` for the FO3 legacy path. `MaterialInfo.material_kind` stays 0 for every `BSShaderPPLightingProperty` mesh; the doc-comment attributing "shader_type = 3/7" parallax kinds to FO3 is actually describing Skyrim's `BSLightingShaderType` enum, not FO3's.
- **Impact**: FO3 `SHADER_SKIN`(14)/`SHADER_WATER`(17)/`SHADER_SKY`(10) materials all import as generic lit `material_kind=0`, blocking any future skin-SSS/sky-fullbright/water-dispatch branch. Root cause of FO3 water having no distinct GPU material kind (see also checklist item 7 under Dim 1).
- **Related**: #1856, #977
- **Suggested Fix**: Capture `shader.shader.shader_type` into a new `MaterialInfo::legacy_shader_type` field (do not alias onto `material_kind`, whose range is the Skyrim enum), correct the doc.

#### FO3-D1-05 / FO3-D2-01 (merged — found independently by both dimensions): FO3/FNV fire-refraction heat-haze never classified — `refraction_strength`/`refraction_fire_period` decoded then dropped at the NIFAL boundary
- **Severity**: MEDIUM
- **Location**: `crates/nif/src/import/material/legacy_properties.rs:237-336` (`apply_pp_lighting_property`, no refraction mirroring); decode site `crates/nif/src/blocks/shader.rs:80-88`; the Skyrim-only classifier at `crates/nif/src/import/material/dedicated_shader.rs:307,330-338`; missing constants at `crates/nif/src/shader_flags.rs:33-44`
- **Status**: NEW
- **Description**: `BSShaderPPLightingProperty::parse` correctly decodes `refraction_strength`(f32)/`refraction_fire_period`(i32) for all FO3 content (bsver 34 > the `#BSVER# #GT# 14` gate). Neither value is ever mirrored into `MaterialInfo` — the only writer of `MaterialInfo::refraction_strength` is the Skyrim+ `BSLightingShaderProperty` path, and the only site that sets `material_kind = 103` (`MATERIAL_KIND_FIRE_REFRACTION`) tests Skyrim-era `skyrim_slsf1` bits that have no FO3/FNV equivalent declared in `fo3nv_f1`. Corroborating evidence: nif.xml declares two FO3-exclusive controllers (`BSRefractionStrengthController`, `BSRefractionFirePeriodController`) whose entire purpose is animating these fields, both already dispatched by this codebase.
- **Impact**: FO3/FNV fire, explosion, and plasma heat-haze proxies render as flat opaque lit slabs, potentially occluding flame cards authored behind them. Shared FO3+FNV blast radius. Kept at MEDIUM rather than HIGH because neither sub-agent could quantify how many *vanilla* FO3 meshes actually author the `Refraction | Fire_Refraction` bit pair without adding new counting tooling (the one name-matching vanilla asset checked, `testrefractcloud01.nif`, is a dev test cloud) — escalate to HIGH if a bit-pair census against `meshes\fire\*` shows it's authored on shipped content.
- **Related**: #2232 (REN-D6-01, the `ior` field overload), #2249, #452/#773
- **Suggested Fix**: Add `REFRACTION`/`FIRE_REFRACTION` (bits 15/16) to `fo3nv_f1`, mirror `shader.refraction_strength` into `info.refraction_strength` in `apply_pp_lighting_property`, gate `material_kind = 103` on the FO3/FNV flag pair matching the Skyrim path's promotion. First step should be the bit-pair census to settle severity.

#### FO3-D1-06: Inherited-property precedence inversion — `texture_clamp_mode`/`env_map_scale` writes are unconditional inside a direct-then-inherited loop
- **Severity**: LOW
- **Location**: `crates/nif/src/import/material/legacy_properties.rs:22,313,325,385,386,444-445,451-452,458,469,490`
- **Status**: NEW
- **Description**: `apply_legacy_property_chain`'s documented intent is "shape properties first so they take priority" (#208), implemented via `is_none()`/`has_…` guards everywhere except `texture_clamp_mode` and `env_map_scale`, which use a bare `=` in all seven shader branches — an inherited parent-NiNode property silently overwrites the shape's own authored value, the opposite of the stated rule.
- **Impact**: Only fires when a parent NiNode carries a `BSShader*Property` while the child shape also carries its own — uncommon in vanilla FO3, hence LOW. Compounds FO3-D1-01 when it does fire.
- **Related**: #208, #773, #1201/NIF-D4-NEW-05
- **Suggested Fix**: Add `texture_clamp_mode_consumed`/`env_map_scale_consumed` bools mirroring `alpha_property_consumed`, gate both writes on them.

---

### Dimension 2 — NIF v20.2.0.7 Parser — FO3 Block Subset

(FO3-D2-01 merged into FO3-D1-05 above.)

#### FO3-D2-02: `nif_stats` per-block histogram keys by parsed Rust type, not header-advertised type — its own module doc claims the opposite, and the FO3 baseline gate inherits the blind spot
- **Severity**: MEDIUM
- **Location**: `crates/nif/examples/nif_stats.rs:33-41` (doc claim) vs `:176-184` (impl); consumed by `crates/nif/tests/per_block_baselines.rs` + `crates/nif/tests/data/per_block_baselines/fallout_3.tsv`
- **Status**: NEW
- **Description**: The doc says blocks attribute to "header-advertised type name, not parsed Rust type"; the success path actually keys by `block.block_type_name()` (the Rust struct's name). Every FO3 block family whose dispatch arm returns a shared struct collapses into one histogram row: `BSSegmentedTriShape`/`NiTriStrips` both fold into `NiTriShape`; `BSFadeNode` folds into `NiNode`; five distinct `NiPSysEmitter` subtypes fold into one row; ~24 particle modifier/controller/collider types fold into opaque `NiPSysBlock`; `Lighting30ShaderProperty` folds into `BSShaderPPLightingProperty`.
- **Impact**: A composition shift inside a collapsed family is invisible to the FO3 baseline regression gate — e.g. a change that silently reroutes `BSSegmentedTriShape` to the plain `NiTriShape` arm (dropping its trailer) keeps the aggregate row byte-identical and passes `per_block_baselines`. Bounded blind spot: total dispatch loss still surfaces as an `unknown` row — this only masks "still parses, parses *differently*", which is precisely the FO3-vs-FNV divergence class this audit was asked to hunt.
- **Related**: #2216 (different harness)
- **Suggested Fix**: Attribute by the header-advertised type name already in scope at the dispatch site (or minimally emit `original_type` for the `NiPSysBlock`/`NiPSysEmitter` families); regenerate all seven baseline TSVs in the same commit; correct the module doc.

#### FO3-D2-03: `BSSegmentedTriShape` segment table consumed and discarded with no bounds check and no downstream consumer
- **Severity**: LOW
- **Location**: `crates/nif/src/blocks/tri_shape/ni_tri_shape.rs:183-192`
- **Status**: NEW
- **Description**: Wire layout is correct (verified against nif.xml and zero drift across 17,172 real FO3 NIFs). Two residual gaps: (a) every field is read into `let _`, so the per-segment body-part table never reaches `ImportedMesh` (benign today — FO3 dismemberment is driven by `BSDismemberSkinInstance` instead, which *is* imported); (b) `num_segments` has no sanity bound before the loop, unlike the sibling `parse_mesh_emitter`'s `check_alloc`.
- **Impact**: (a) benign, nothing visible lost today. (b) bounded robustness gap only (corrupt count walks to EOF and errors, no UB/DoS).
- **Related**: #146, FO3-D2-02
- **Suggested Fix**: Add `stream.check_alloc(num_segments as usize * 9)?` before the loop for parity.

#### FO3-D2-04: `BSShaderNoLightingProperty` falloff-absent default for `falloff_start_angle` disagrees with nif.xml (`0.0` vs `1.0`)
- **Severity**: LOW
- **Location**: `crates/nif/src/blocks/shader.rs:172-182`
- **Status**: NEW
- **Description**: When `bsver <= 26`, the parser substitutes `(0.0, 0.0, 1.0, 0.0)` for the four falloff floats; nif.xml specifies defaults `(1.0, 0.0, 1.0, 0.0)` — `start_angle` is inverted.
- **Impact**: Unreachable on retail FO3/FNV (both ship bsver 34, always above the gate, so on-disk values are always read). Only reachable on transitional/dev/modded exports at bsver ≤ 26.
- **Related**: #1331, #451
- **Suggested Fix**: Change the fallback tuple to `(1.0, 0.0, 1.0, 0.0)`.

---

### Dimension 3 — ESM Record Coverage (Fallout3.esm)

**No findings.** Live-parsed `Fallout3.esm`: `total=44 666` (`navm=7 198` exact
match; `structured=37 468`, +9 benign drift vs the 37 459 ROADMAP figure —
already-flagged GOTY-patch content). `ACBS`/`CTDA`/`DIAL`/`INFO` subform
handling confirmed cross-game-safe by design. `SCPT SCHR` flags confirmed
still `u16_or_default()` at offset 18 (#1654 intact). `CELL XCLL`/`RCLR` use
one shared `GameKind::Fallout3NV` size table, no FO3-only branch. REFR
texture overrides (XATO/XTNM/XTXR): moot per already-closed #1887 — FO3 REFRs
never emit these sub-records at all. Zero `unreachable_patterns` warnings in
`cargo build -p byroredux-plugin`.

Non-finding noted for a general (cross-game, not FO3-specific) ESM audit:
`wrld.rs`'s `NAM2`/`NAM3`/`ZNAM` FormIDs skip `FormIdRemap` while `INFO`/`DIAL`
apply it, pre-existing across every `GameKind`.

---

### Dimension 4 — FO3 Cell Loading End-to-End (interior + exterior)

**No findings.** Megaton interior baseline confirmed exactly 929 REFRs via a
live parse. `exterior.rs` + `crates/plugin/src/esm/cell/wrld.rs` read
WRLD/CELL sub-records fully generically — no hardcoded worldspace name, form
ID, or coordinate found in either file (751 + 525 lines read in full). Zero
`GameKind::FalloutNV`/`GameKind::Fallout3` branches anywhere in the shared
cell loader. WTHR→CLMT resolution is shared/game-agnostic. `CachedNifImport`
cache has no leak path (LRU-capped, evictions correctly forwarded to
`AnimationClipRegistry::release`). FO3's absence of a distant-object LOD
scheme is a correctly-gated non-gap: `.bto`/placement-LOD is Skyrim|FO4/
Oblivion-only by design, and real-data checks (`_far.nif` grep) confirm only
2 landmark files + 0 `distantlod` entries across base game + all 5 DLCs —
matches already-closed #2086, fix verified still in place, not regressed.

---

### Dimension 5 — FO3 Collision Import (Havok → CollisionShape)

#### FO3-D5-01: `bhkRigidBody` (non-T) CInfo transform applied unconditionally — 1,041 FO3 mesh files (9.5% of the archive) get colliders displaced by a median of 160 engine units
- **Severity**: HIGH
- **Location**: `crates/nif/src/import/collision/mod.rs:269-282` (`extract_from_classic`); parser side `crates/nif/src/blocks/mod.rs:1147` + `crates/nif/src/blocks/collision/rigid_body.rs:295`
- **Status**: NEW. **Directly contradicts the "informational (non-bug)" dismissal in `docs/audits/AUDIT_FO3_2026-07-16.md:189-193`**, whose premise ("non-T bodies always ship identity translation/rotation in vanilla content") this audit found to be empirically false.
- **Description**: nif.xml declares `bhkRigidBodyT` as a distinct type whose only semantic difference from plain `bhkRigidBody` is that the "T" suffix marks the body as active for translation/rotation — a non-T body's CInfo `Translation`/`Rotation` fields must be treated as identity. ByroRedux collapses both type names onto one struct at dispatch (`"bhkRigidBody" | "bhkRigidBodyT" => BhkRigidBody::parse`), destroying the T-ness at parse time. `extract_from_classic` then applies the CInfo transform to *every* body whenever it's non-identity, T or not.
- **Evidence**: Real FO3 data (`Fallout - Meshes.bsa`, block-type name from the header table joined to the parsed block by index): `bhkRigidBodyT` = 2,054 blocks (1,998 nonzero translation); `bhkRigidBody` (non-T) = 6,903 blocks, **2,704 with nonzero translation, 1,649 with nonzero rotation**; **1,041 files (9.5% of the archive) carry a non-T body with a nonzero transform**. Offset magnitude actually applied (×7.0 havok scale, engine units): p50=160.7, p90=2820.9, max=20932.8. By motion type, 1,007 of the displaced bodies are `FIXED` — walkable static world geometry (e.g. `meshes\architecture\megaton\megatonrampturn45sml.nif`, `meshes\dungeons\utility\catwalks\utlcat3way01.nif`). Two independent reference implementations (NifSkope, nifly) gate this transform on the type name; this codebase does not. Same shape confirmed in the DLC archives (8,783 non-T vs 1,489 T bodies).
- **Impact**: ~9.5% of FO3 meshes get a collider translated away from its render geometry by a median of 160 units. Symptoms: invisible walls, walk-through architecture, falling through catwalks/ramps, clutter resting mid-air. Not masked by the `spawn.rs` synthesized-trimesh fallback since `extract_collision` returns `Some`. Shared with Oblivion/FNV/Skyrim (identical code path) — the FO3 numbers above are what was measured this cycle.
- **Related**: `docs/audits/AUDIT_FO3_2026-07-16.md:189-193` (the stale dismissal this finding corrects); #1230 (havok scale); #1832/#1874 (zero-mass reclassification, adjacent code)
- **Suggested Fix**: Retain T-ness at parse — add `pub is_t: bool` to `BhkRigidBody` set from the dispatch arm, gate the `has_offset` wrap in `extract_from_classic` on it. Add a regression test pinning "non-T body with non-identity translation ⇒ shape is NOT wrapped in a Compound".

#### FO3-D5-02: `bhkSPCollisionObject` classified `CollisionAuthoring::Classic` although it's a phantom wrapper (25 blocks in FO3 DLC)
- **Severity**: LOW
- **Location**: `crates/nif/src/import/collision/mod.rs:69-85`; dispatch `crates/nif/src/blocks/mod.rs:1139-1141`
- **Status**: NEW
- **Description**: The dispatcher byte-wise-correctly folds `bhkSPCollisionObject` into `BhkCollisionObject`, but nif.xml declares it inherits `bhkPCollisionObject` (a phantom wrapper). Because the parsed struct is `BhkCollisionObject`, `examine_collision_kind` reports `Classic` and routes it into `extract_from_classic`, which fails to resolve the phantom body and returns `None` via the generic debug log rather than the deliberate `extract_from_phantom` path.
- **Evidence**: Live scan of the 5 FO3 DLC archives: `bhkSPCollisionObject` 25 parsed/0 unknown, and exactly 25 of 10,297 `BhkCollisionObject`-typed blocks fail extraction — matching the 25 `bhkSimpleShapePhantom` blocks 1:1.
- **Impact**: No runtime behavior difference today (both paths yield `None`) — diagnostic-only. A future `TriggerVolume` importer keying off `CollisionAuthoring::Phantom` would silently skip these 25 blocks.
- **Related**: FO3-D5-03
- **Suggested Fix**: Give `bhkSPCollisionObject` its own dispatch arm producing `BhkPCollisionObject`.

#### FO3-D5-03: `examine_collision_kind`/`CollisionAuthoring` have no callers — doc comment names consumers that don't exist
- **Severity**: LOW
- **Location**: `crates/nif/src/import/collision/mod.rs:40-85`
- **Status**: NEW
- **Description**: The doc claims the discriminator exists so callers (telemetry, the trimesh fallback in `cell_loader/spawn.rs`) can distinguish authoring kinds. A repo-wide search finds both symbols referenced only inside their own module and its tests — `spawn.rs` never calls either, and its NP-fallback comment refers to `extract_collision` instead.
- **Impact**: Dead public API plus doc drift that would mislead a future reader into believing FO3/FO4 collision telemetry is wired up.
- **Related**: FO3-D5-02
- **Suggested Fix**: Wire it into the `spawn.rs` fallback, or delete it and correct the docstring.

#### FO3-D5-04: FO3 collision baselines only sample `Fallout - Meshes.bsa` — three collision block types exist only in DLC archives and are ungated
- **Severity**: LOW
- **Location**: `crates/nif/tests/common/mod.rs:110`; consumed by `per_block_baselines.rs`/`block_coverage_baselines.rs`
- **Status**: NEW
- **Description**: FO3 ships 5 DLC mesh archives with collision types absent from the sampled baseline archive: `bhkSPCollisionObject` (25), `bhkBlendCollisionObject` (171), `bhkConvexTransformShape` (38) — all parse cleanly (0 unknown) but no checked-in baseline pins their parsed/unknown split.
- **Impact**: A regression in the blend-collision-object tail or `bhkConvexTransformShape` decode would not be caught by any FO3 gate.
- **Related**: FO3-D5-02
- **Suggested Fix**: Extend the FO3 (and FNV) test archive list to a `mesh_archives()` set covering all DLC, regenerate + check in baselines.

---

### Dimension 6 — BSA v104 + Real-Data Validation

#### FO3-D6-NEW-01: `parse_real_facegen.rs` docstring claims FNV+FO3 coverage but the test only ever exercises FNV by default
- **Severity**: LOW
- **Location**: `crates/facegen/tests/parse_real_facegen.rs:1-41`
- **Status**: NEW
- **Description**: The module doc claims FNV/FO3 coverage; the actual data-dir resolution + BSA filename are FNV-only, with no `BYROREDUX_FO3_DATA` fallback (unlike the sibling NIF/`.spt` real-data tests, which do have dedicated FO3 arms).
- **Evidence**: Manually pointed the FNV env var at the FO3 install — all 3 tests pass; FO3's `headhuman.{egm,egt,tri}` are byte-for-byte identical to the hardcoded FNV baselines (asset-reuse coincidence, not something the test structurally guarantees).
- **Impact**: No functional bug — FaceGen parsing genuinely works on real FO3 data. Pure test-coverage/CI-signal gap: nothing would catch a future FO3-only face asset (ghoul/super-mutant/robot) diverging from the FNV-shared assets.
- **Related**: analogous to already-closed #1452/#2090 (same class, doc-overclaim)
- **Suggested Fix**: Parametrize the existing 3 tests over `[("FNV", …), ("FO3", …)]` mirroring the `Game` enum pattern in `parse_real_nifs.rs`/`parse_real_spt.rs`.

---

### Dimension 7 — FO3 Animation / NPC Spawn + Scripting Gap

**No findings.** All three M41.0 regression guards hold: B-spline pose
fallback and `AnimationClipRegistry` dedup are both structurally
version/game-agnostic (no gate exists to regress independently for FO3). NPC
hand-mesh load is provably shared between FO3 and FNV because
`GameKind::Fallout3NV` is a single enum variant covering both games — a
regression cannot selectively break one without the other. All four
hardcoded mesh paths (`skeleton.nif`/`upperbody.nif`/`lefthand.nif`/
`righthand.nif`) confirmed present in real `Fallout - Meshes.bsa`.

**Scripting gap (FO3-distinctive, not a bug)**: 1,257 FO3 SCPT records
parse; M47.0 event-hook + M47.1 condition-eval have landed; the M47.2
compiled-Papyrus recognizer slice is in progress. This remains FO3's largest
functional gap for quest/world interactivity — owned by `/audit-scripting`,
not deep-audited here per this skill's scope.

---

## FNV-Shared Surface

Nearly the entire audited surface is literally shared code with FNV, not
merely similar:

- **ESM parser**: `GameKind::Fallout3NV` is one enum variant for both games
  everywhere the parser's per-game record layout matters (ARMO/WEAP/AMMO
  DATA, NPC_/DIAL/INFO subforms, CELL XCLL/RCLR sizing). A regression here
  cannot distinguish FO3 from FNV by construction.
- **NIF parser**: identical `bsver=34`/`v20.2.0.7` stamp; block dispatch,
  particle-emitter decode, and B-spline handling are all version-agnostic —
  no FO3-specific branch exists to diverge independently of FNV's.
- **Cell loader**: zero `GameKind`-branching found anywhere in
  `cell_loader.rs`/`cell_loader/*.rs`; WRLD/CELL decode, WTHR→CLMT
  resolution, and the NIF import cache are fully generic.
- **NPC spawn hand-mesh path**: `humanoid_body_paths`/`humanoid_skeleton_path`
  match `GameKind::Fallout3NV` as one combined arm — same reasoning as above.
- **Collision**: `examine_collision_kind`/`havok_motion_type`/`resolve_shape`
  have no per-game branch; FO3-D5-01 (the T-suffix bug) and FO3-D5-02/03/04
  equally affect FNV, Oblivion, and Skyrim LE.
- **Shader material path** (Dim 1): FO3-D1-01/02/03/05/06 all sit in
  `legacy_properties.rs`, shared verbatim with FNV.

**FO3-only gaps found *inside* this shared surface**: `bhkConvexListShape`
is FO3-distinctive real content (55 occurrences vs zero in the FNV baseline)
and correctly translates. `BSSegmentedTriShape` and `BhkMultiSphereShape` are
correctly-implemented paths that turn out to have **zero real FO3
occurrences** (base game + all 5 DLCs) — both checklist items assumed FO3
relevance that the real data doesn't support; noted as premise corrections,
not filed as gaps.

## FO3-Distinctive Gaps

1. **Inline-shader-only material universe** — confirmed zero FO3 content can
   ever set `MAT_FLAG_PBR_BSDF` (Disney-BSDF gate), since `is_pbr` is only
   ever set `true` behind a `material_path` guard that FO3 never populates.
   No live risk found here this cycle (Dim 1's HIGH/MEDIUM findings are all
   about *legacy*-path classification bugs, not BGSM leakage).
2. **Capital Wasteland worldspace** — confirmed generic; no FNV-hardcoded
   worldspace name/coordinate found in the exterior/WRLD code (Dim 4).
3. **1,257-SCPT scripting runtime gap** — the largest FO3-distinctive
   functional gap; tracked, in-progress (M47.2), not filed here (Dim 7).
4. **FO3-vs-FNV collision content difference** — FO3 authors more
   `bhkConvexListShape` (55 vs 0 in the FNV baseline sample) and DLC-only
   `bhkBlendCollisionObject`/`bhkConvexTransformShape`/`bhkSPCollisionObject`
   content not present in the FNV test corpus (Dim 5).

## Validation Status

- **Interior**: Megaton, 929 REFRs — confirmed exact match via live ESM
  parse (Dim 4, Dim 6).
- **Exterior**: wired, generic WRLD/CELL decode confirmed with no FNV
  hardcodes (Dim 4). A fresh GPU bench was not re-run this cycle (prior
  cycle's 2026-07-25 report already captured 7,132 entities @ 91.7 FPS for
  the 3×3 Capital Wasteland grid; not independently re-verified here).
- **NIF corpus**: 100.00% clean, 10,989 NIFs, 96 distinct block types, 0
  drift (Dim 2, Dim 6, cross-confirmed).
- **Creature/NPC**: deathclaw `NiSkinData` (5 sub-meshes) confirmed correctly
  dispatched (Dim 6); NPC hand-mesh + skeleton paths confirmed present on
  disk (Dim 7).
- **Collision**: full Havok census taken this cycle for the first time —
  9.5% of the mesh corpus is affected by the newly-found FO3-D5-01 collider
  displacement bug; motion-type mapping (#1652) confirmed correct and
  zero-mass reclassification confirmed inert on FO3.

---

## Deduplication Notes

One exact cross-dimension duplicate found and merged: **FO3-D1-05 /
FO3-D2-01** (both independently discovered the same
`refraction_strength`/`refraction_fire_period` decode-then-drop bug from
different entry points — Dim 1 via the render-consumption angle, Dim 2 via
the parser-completeness angle). Listed once under Dimension 1 with a
cross-reference from Dimension 2.

One existing-issue confirmation: FO3-D5-01 **contradicts** (does not merely
relate to) the dismissal in `docs/audits/AUDIT_FO3_2026-07-16.md:189-193` —
that prior audit's premise is empirically disproven by this cycle's data and
should be treated as superseded, not as a duplicate to skip.

All other findings checked against `/tmp/audit/issues.json` (dedup baseline
captured at audit start) — no OPEN issue matched on keyword search; two
CLOSED issues (#1887 REFR texture overrides, #2086 FO3 LOD placement) were
independently re-verified as still-correctly-fixed, not regressed, and are
cited under their respective dimensions rather than re-filed.

---

Suggest: `/audit-publish docs/audits/AUDIT_FO3_2026-08-03.md`
