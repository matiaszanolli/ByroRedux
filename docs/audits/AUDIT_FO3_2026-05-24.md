# Fallout 3 Compatibility Audit — 2026-05-24

Full 6-dimension sweep. Baseline: 2026-05-01 audit shipped 5 NEW findings (2 HIGH + 1 MEDIUM + 2 LOW). Today's sweep verifies those closures and audits past-week changes that touch the FO3 path.

## Executive Summary

- **0 NEW findings**. All 5 of the 2026-05-01 audit findings (FO3-4-01, FO3-4-02, FO3-1-01, plus the 2 LOWs) verified **FIXED** in current source.
- **4 past-week wins for the FO3 path**:
  - **#1243** WaterShaderProperty consumer wired at import (`walker.rs:856`) — FO3 water surfaces (Megaton water, Wasteland rivers, Springvale ponds) now plumb `env_map_scale` into `MaterialInfo`
  - **#1244** BSShaderPropertyBaseOnly consumer (`walker.rs:872`) — Hair / VolumetricFog / DistantLOD / DistantTree subclasses route through MaterialInfo
  - **#1235** SceneFlags parity at cell-loader spawn — FO3 cell loads see the same `SceneFlags` as steady-state draws (shared with FNV)
  - **M40 Phase 2 Stage 3a/3b** interior↔exterior cell-swap orchestration — FO3 will benefit once Megaton ↔ Wasteland exterior streaming lands
- **Compatibility level**: Tier-1 holding firm. Same shared FNV path; no FO3-specific regressions.
- **Delta vs FNV baseline**: Fully shared on BSA / ESM / NIF-block / shader-property / cell-loader paths. Per-game divergence isolated to the `BSShaderPPLightingProperty` vs `BSLightingShaderProperty` shader-property branch (FO3 uses PPLighting; FO4+ uses BSLighting). No PBR / BGSM authoring (Disney lobe unreachable — verified per Dim 21 regression guard).

| Severity | NEW | Carryover | Total | INFO (verified-fixed) |
|----------|-----|-----------|-------|----------------------:|
| HIGH     | 0   | 0         | 0     | 2 (FO3-4-01, FO3-4-02) |
| MEDIUM   | 0   | 0         | 0     | 1 (FO3-1-01) |
| LOW      | 0   | 0         | 0     | 2 (the LOWs from 2026-05-01) |
| INFO     | —   | —         | —     | 14 (this sweep) |

## Verifications

### 2026-05-01 audit findings — all CLOSED

| Finding | Site | Status |
|---|---|---|
| **FO3-4-01** (HIGH) — `texture_clamp_mode` not propagated to MaterialInfo | `walker.rs` PPLighting branch + struct at `material/mod.rs:693` | ✓ FIXED |
| **FO3-4-02** (HIGH) — `env_map_scale` not propagated to MaterialInfo | `walker.rs:362` + struct at `material/mod.rs:564, 812` | ✓ FIXED |
| **FO3-1-01** (MEDIUM) — Parallax `>= 24` off-by-one vs nif.xml `#GT# 24` | `crates/nif/src/blocks/shader.rs:93` — `if bsver > crate::version::bsver::FO3_PARALLAX` (strict greater) with comment citing the prior `>= 24` regression | ✓ FIXED |
| 2026-05-01 LOWs — texture-path/material-path mirrors | rolled into the same `walker.rs` consumer pass | ✓ FIXED |

### Past-week FO3 path fixes — verified live

#### V1 — #1243 WaterShaderProperty consumer at import (FO3/FNV legacy water)

- **Site**: `crates/nif/src/import/material/walker.rs:828-858`
- **Found**: branch at `:856` — `if let Some(shader) = scene.get_as::<WaterShaderProperty>(idx) { info.env_map_scale = shader.shader.env_map_scale; }`
- **Doc comment** at `:828-835` explicitly cites #1243 / NIF-DIM4-NEW-02 and explains the rationale (omitted by the #940 pass on stale "shader base not yet plumbed" reasoning; in fact the Tile / Sky / TallGrass siblings already reach env_map_scale through the same field).
- FO3 water surfaces (Megaton water, Wasteland river/pond REFRs) now distinguish themselves from glass / opaque siblings in the GpuMaterial dedup. **No regression** — single new branch in an `else if` cascade of property-type tests.
- ✓

#### V2 — #1244 BSShaderPropertyBaseOnly consumer (FO3 hair / grass / volumetric fog)

- **Site**: `walker.rs:860-874`
- **Found**: branch at `:872` covering Hair / VolumetricFog / DistantLOD / DistantTree subclasses (all FO3-era shader stubs that only carry `BSShaderPropertyData` base, no `file_name`).
- **Doc comment** at `:860-871` cites #1244 / NIF-DIM4-NEW-03 and explains the parser shape (the subclasses do NOT inherit BSShaderLightingProperty per nif.xml lines 6346/6350/6359/6363, so only `env_map_scale` is plumbable here).
- FO3 NPC hair and Wasteland tall-grass surfaces now have their authored env_map_scale modulator (was zero pre-fix; reflective hair lost its environment contribution).
- ✓

#### V3 — #1235 SceneFlags parity at cell-loader spawn boundary

- **Status**: closed `dae0e9b2`; FO3 inherits the fix via shared cell_loader. Verified in today's renderer Dim 5 audit (mentioned at `audit-renderer.md` Dim 5).
- ✓

#### V4 — M40 Phase 2 Stage 3a/3b interior↔exterior cell-swap

- **Status**: shipped (`1e92a471` + `a7cc9184`, 2026-05-22). The orchestrator + door.teleport wiring is in place. FO3 will benefit once Megaton ↔ Wasteland exterior streaming becomes a smoke-test target.
- ✓ (architecturally available; FO3 e2e exterior smoke deferred)

#### V5 — Disney BSDF gate UNREACHABLE for FO3 (regression guard)

- **Status**: per today's `AUDIT_RENDERER_2026-05-24_DIM21.md`, the Disney lobe at `triangle.frag:1652/2447/2669` gates on `MAT_FLAG_BGSM_PBR` which is FO4+ BGSM authoring only. FO3 ships zero BGSM materials.
- **Audit-side regression guard**: scan an FO3 cell with `tex.material_kinds` / `mem.stats`, confirm 0 materials report PBR flag set. If any do, the gate has regressed.
- ✓ Unreachable by construction; regression test path documented.

### Baseline metrics holding

| Metric | 2026-05-01 baseline | Today | Drift |
|---|---|---|---|
| Fallout3.esm parse | 32,450 records / 8.0 s | not re-run today (shared FNV parser; the 8-week-stable path) | likely unchanged |
| `Fallout - Meshes.bsa` NIF parse rate | 100% (10,989 / 10,989) | not re-run today (cargo test gated on `BYROREDUX_FO3_DATA`) | last verified clean |
| All 16 vanilla BSAs | 159,155 files open / round-trip | not re-run today | last verified clean |
| Megaton REFR count | 929 (per ROADMAP) | 929 in ROADMAP (line 22+ M40 reference) | unchanged |
| Worldspace EDID priority order | `["wastelandnv", "wasteland", "tamriel", "skyrim"]` | same at `byroredux/src/cell_loader/exterior.rs:96` | unchanged |

## Dimension Findings (this sweep)

### Dim 1 — NIF v20.2.0.7 Parser Correctness (FO3 subset): PASS

- BSShaderPPLightingProperty walker branch at `walker.rs:683` covers refraction / parallax / bump map / normal map slots
- BSShaderNoLightingProperty (UI/sky) walker branch at `walker.rs:775`
- Parallax `>= 24` boundary error from 2026-05-01 audit (FO3-1-01) now fixed at `shader.rs:93` (strict-greater gate)
- Normal-map slot routing: PPLighting uses `BSShaderTextureSet[1]` (FO3+) — confirmed via the slot-1 references in `walker.rs`
- ✓ Clean.

### Dim 2 — BSA v104 Archive: PASS (no past-week BSA changes)

- BSA v104 implementation unchanged since the 2026-04-19 sweep; FO3 + FNV share the path.
- ✓ Clean (re-verify with `BYROREDUX_FO3_DATA=... cargo test -p byroredux-bsa -- --ignored` if a smoke run is wanted).

### Dim 3 — ESM Record Coverage (Fallout3.esm): PASS

- Shared FNV ESM parser; FO3 inherits the full 23-category dispatch.
- Past-week ESM additions (M47.0 Phase 3a `EsmIndex::base_record_script` lookup; M47.1 CTDA / ConditionFunction parser) are dispatch-additive — they extend coverage without changing FO3-shared paths.
- No FO3-unique record-type drift detected.
- ✓ Clean.

### Dim 4 — Rendering Path for FO3 Shaders: PASS

- BSShaderPPLightingProperty flag bits correctly distinguished from Skyrim/FO4 layouts (different bit positions for decal / alpha-test / two-sided / glow / window)
- Normal map from slot 1 (NOT bump slot — Oblivion trap avoided)
- Parallax / cubemap / refraction strength routes intact
- #1243 WaterShaderProperty + #1244 BSShaderPropertyBaseOnly consumers wired (see V1 + V2)
- Disney BSDF unreachable for FO3 (V5)
- ✓ Clean.

### Dim 5 — Real-Data Validation: DEFERRED (no on-disk smoke run this sweep)

- `BYROREDUX_FO3_DATA=... cargo test -p byroredux-nif --test parse_real_nifs -- --ignored fo3` not re-run today; last green at 2026-05-01.
- Megaton interior cell load (929 REFRs) is the canonical FO3 smoke test; ROADMAP references it as the validation demo at 42 FPS.
- Recommended action: re-run the FO3 NIF parse rate + Megaton load before the next ship gate.

### Dim 6 — Blockers & Game-Specific Quirks: PASS

- No FO3-only hardcoded paths in cell_loader (verified by `grep -rnE "Fallout3\.esm|fallout3|fo3" byroredux/src/cell_loader/`)
- Worldspace EDID preference list `["wastelandnv", "wasteland", "tamriel", "skyrim"]` at `exterior.rs:96` covers FO3's `"wasteland"` EDID
- M41.0 long-tail regression guards (#772 B-spline pose-fallback, #790 AnimationClipRegistry dedup, #793 NPC hand-mesh load) are all live — FO3 NPCs (Megaton dwellers, Springvale settlers) inherit the same fixes as FNV
- Cell-loader stale-comment cleanup (#822 / `ca6be24`) shipped; no audit citing pre-NIF-expand stats remains
- ✓ Clean.

## FNV-Shared Surface

Confirmed fully shared with FNV (no FO3-only gaps):

- **BSA v104** — identical code path for `Fallout - Meshes.bsa`, `Fallout - Textures.bsa`, all 16 vanilla + DLC archives
- **ESM parser** — 23-category dispatch handles Fallout3.esm to 32,450 records on the same code that handles FalloutNV.esm
- **NIF v20.2.0.7 dispatcher** — all 30 Havok block types skip via `block_size`; recovery works
- **BSShaderPPLightingProperty + NoLighting + TileShader + SkyShader + TallGrassShader + WaterShaderProperty + BSShaderPropertyBaseOnly** — all walker-side consumers wired
- **CELL XCLL/RCLR lighting** — same `CellLightingRes` resource
- **M41 NPC spawn** (B-spline fallback, AnimationClipRegistry dedup, hand-mesh load)
- **M40 streaming** orchestrator (Phase 2 Stage 3a/3b cell-swap)
- **Disney BSDF gate** — unreachable for both FNV and FO3 (no BGSM authoring)
- **Sun-arc + CLMT TNAM tod_hours** — FO3 Capital Wasteland and FNV Mojave each ship their own `tod_hours` (Wasteland sunrise ~0.3 hr earlier); both honored via the `weather_system::compute_sun_arc(hour, tod_hours)` path

## Validation Status

- **Interior cell load**: ✓ Megaton Player House (929 REFRs) — ROADMAP-validated at 42 FPS
- **Exterior cell load**: ✓ Single-cell load works (`--esm Fallout3.esm --grid <x>,<y>` for Wasteland) — multi-cell streaming inherits M40 Phase 2 wiring landed this week
- **NPC / creature**: ✓ shared with FNV (M41 hand-mesh load applies to Megaton dwellers)
- **Sky / weather**: ✓ FO3 Capital Wasteland CLMT TNAM honored; tod_hours per-worldspace
- **FaceGen**: parses but may not render fully (audit not extended to FaceGen — same gap as FNV)
- **Water surfaces**: NOW ✓ post-#1243 — Megaton water / Wasteland rivers / Springvale ponds plumb env_map_scale to renderer

## Notes

- This sweep is largely a regression-guard pass; FO3 inherits FNV's stability + past-week additive fixes. No new issues to file.
- The Disney BSDF gate is the only "new" thing FO3 needs to guard against — and the gate's unreachability for FO3 is structural (no BGSM authoring), so the regression risk is low.
- No `/audit-publish` follow-up needed.
