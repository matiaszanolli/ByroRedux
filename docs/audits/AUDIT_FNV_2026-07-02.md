# Fallout: New Vegas Compatibility Audit — 2026-07-02

**Command:** `/audit-fnv` (reference-title regression sweep)
**Repo:** ByroRedux · branch `main` · HEAD at Session 53 closeout (`1b4e8e84`)
**Scope:** all 8 dimensions — cell load, NIFAL translation, RT lighting, ESM parser,
NIF parser, animation/skinning/particles, PHYSAL ragdoll, real-data validation.
**Method:** each dimension run as a Task agent; every regression guard re-verified
against current source; findings independently corroborated by the orchestrator
before inclusion. No engine launch (no GPU in this session — real-data dimension
was executed as static verification of the code paths the bench-of-record depends on).

---

## Executive Summary

FNV is the **reference title** — the most-validated end-to-end path and the reference
realization for the canonical translation layers (NIFAL material/physics, PHYSAL
ragdoll). This sweep hunted regressions and unshipped polish, not missing foundations.

**No CRITICAL or HIGH findings.** Every named regression guard across all eight
dimensions is present, correct, and (where applicable) test-covered in current source.
The FNV foundation is intact: the single NIFAL material boundary holds, the SCOL/SCPT/XCLL
ESM guards hold, the `havok_motion_type` full-enum mapping holds, the Disney-BSDF gate is
unreachable for FNV, the skyTint interior gate is in place at both raytrace miss sites, and
the classic-bhk ragdoll chain decodes malleable-wrapped Ragdolls into jointed bodies with no
per-game branch leaking into the solver.

**6 NEW findings** (in the newly-shipped PHYSAL ragdoll slice + the cell-loader / NIF-import
cache): 1 MEDIUM (silent breakable-constraint drop), 5 LOW (ragdoll test-coverage floor,
ragdoll scale-drift hardening, a stale doc comment on FNV worldspace water, a latent
clip-handle cache ordering gap not reachable on FNV, and an exterior terrain/water spawn
observability gap). None breaks a shipped foundation; the reference slice is sound.

### Findings by severity

| Severity  | Count | IDs |
|-----------|-------|-----|
| CRITICAL  | 0     | — |
| HIGH      | 0     | — |
| MEDIUM    | 1     | FNV-D7-02 |
| LOW       | 5     | FNV-D7-03, FNV-D7-04, FNV-D1-01, FNV-D1-02, FNV-D1-03 |
| **Total** | **6** | |

---

## Baseline Comparison (vs ROADMAP FNV row, `1c26bc25` R6a-stale-14 2026-06-03)

No new bench was run (no GPU). Baselines are transcribed here for reference; nothing
in this static sweep contradicts them, and no code change since the recorded commit
was found that would move the numbers against FNV.

| Metric              | ROADMAP baseline                     | This audit |
|---------------------|--------------------------------------|------------|
| Prospector entities | 3516                                 | not re-measured (no GPU) |
| Prospector draws    | 1224                                 | not re-measured |
| Prospector FPS      | 76.2 FPS / 13.11 ms                   | not re-measured |
| Prospector fence    | 11.12 ms                             | not re-measured |
| FalloutNV.esm parse | 100% (14 881 forms)                  | dispatch guards verified intact (SCOL/SCPT/XCLL/MOVS-PKIN-MSWP gating) |
| Plugin test suite   | —                                    | 522 passed / 0 failed |

> Note: the full pre-collider FNV fence baseline (161.4 FPS / 2.62 ms @ ~2564 ent) is
> still unrecovered per ROADMAP Known Issues — not a fresh regression from this sweep.

---

## Dimension Findings

### FNV-D7-02: `bhkBreakableConstraint` is invisible to the ragdoll graph — silent edge drop, no warning
- **Severity**: MEDIUM
- **Dimension**: PHYSAL Ragdoll
- **Location**: `crates/nif/src/import/collision.rs:357-360` (constraint loop downcasts only `BhkConstraint`); separate block dispatch at `crates/nif/src/blocks/collision/constraints.rs:486` / `crates/nif/src/blocks/mod.rs:28`
- **Status**: NEW
- **Description**: `extract_ragdoll` builds the constraint graph by downcasting each block to `BhkConstraint` only. `bhkBreakableConstraint` parses into a **separate struct** (`BhkBreakableConstraint`) which the loop never downcasts. Its `wrapped_type` can be a Ragdoll (7) or LimitedHinge (2) — a genuine articulation joint — but the block is skipped entirely. Unlike the `BhkConstraintData::Other` arm (#1539, `collision.rs:390`), which `log::warn!`s before dropping, this drop is **completely silent**: no warning, no telemetry. The downstream forest-detection in `build_ragdoll` only warns after the fact about the resulting disconnected component, without naming the culprit block.
- **Evidence**:
  ```rust
  // crates/nif/src/import/collision.rs:357-360
  for block in scene.blocks.iter() {
      let Some(c) = block.as_any().downcast_ref::<BhkConstraint>() else {
          continue;   // BhkBreakableConstraint falls through here — no warn
      };
  ```
  `BhkBreakableConstraint` already decodes `entity_a`/`entity_b`/`wrapped_type` (`constraints.rs:503+`), so the data to surface a wrapped Ragdoll/LimitedHinge exists but is unreachable from `extract_ragdoll`.
- **Impact**: A breakable-wrapped ragdoll joint (rare on vanilla FNV skeletons — malleable-Ragdoll dominates per `docs/engine/physal.md`, but present on some creature/modded skeletons) silently disconnects a limb: `orient_tree` yields a forest and the detached limb builds as an independent free-floating multibody that free-falls. This is the exact failure class #1539 raised to make *loud*, but it bypasses that warning because the block type is never examined. Distinct from OPEN #1718 (bone-name miss) and from the #1539 `Other` drop.
- **Related**: #1718 (bone-name-miss silent drop), #1539 (`Other` constraint drop — warns)
- **Suggested Fix**: In the `extract_ragdoll` constraint loop, also downcast `BhkBreakableConstraint`; when `wrapped_type` is 7/2 surface the inner joint, or — as a minimal one-line safety net — emit the same `log::warn!` as the #1539 arm naming the two bones it would have linked.

### FNV-D7-03: Real-data ragdoll test does not pin FNV body/joint counts — a silent joint-drop regression would pass
- **Severity**: LOW
- **Dimension**: PHYSAL Ragdoll
- **Location**: `crates/nif/tests/ragdoll_import.rs:43-80` (`assert_structural`)
- **Status**: NEW
- **Description**: `docs/engine/physal.md` §7 calls FNV "the measured 18-body / 17-joint reference," but the `#[ignore]` real-data test only asserts structural invariants (`bodies >= 2`, `!constraints.is_empty()`, in-range body indices, `ragdoll_joints + hinge_joints == constraints.len()`). It counts only joints that **survived** decode. A future drop (the FNV-D7-02 breakable path, an `Other`-decode regression, or a field-order slip that fails a `finite` guard) would simply shrink `constraints.len()` and every assertion would still pass.
- **Evidence**: `assert_eq!(ragdoll_joints + hinge_joints, ragdoll.constraints.len(), ...)` is a self-consistency check on the surviving set, not a completeness check against the 18/17 reference. No `assert!(ragdoll.bodies.len() >= 17)` floor for the FNV arm.
- **Impact**: The measured reference figure that gives FNV its "reference realization" status is unenforced; joint-loss regressions in the reference slice are invisible to CI (test is `#[ignore]`, but even on manual run it wouldn't catch a drop). Test-coverage gap only; code works today.
- **Suggested Fix**: Add a game-specific floor for the FNV arm (e.g. `>= 17` bodies, `>= 16` joints, leaving slack for archive/version drift) so a silent joint-loss trips the test on the next manual real-data run.

### FNV-D7-04: Ragdoll writeback uses live `gt.scale` in the inverse while the seed captured scale at activation — drift if a bone is rescaled mid-sim
- **Severity**: LOW
- **Dimension**: PHYSAL Ragdoll
- **Location**: `byroredux/src/ragdoll.rs:190` (seed) vs `byroredux/src/ragdoll.rs:327` (writeback inverse)
- **Status**: NEW
- **Description**: The activation seed composes the body world pose with the scale read *at activation*: `translation = gt.translation + gt.rotation * (b.local_translation * gt.scale)`. The per-frame writeback inverts it using the bone's *current* `gt.scale`: `gt.translation = t - bone_rotation * (tb.local_translation * gt.scale)`. If a ragdoll bone's `GlobalTransform.scale` changes between activation and a later frame, the offset term de-composes with a different scale than it was composed with, displacing the bone by `local_translation * Δscale`.
- **Evidence**: seed at `ragdoll.rs:190` reads `gt.scale`; writeback at `ragdoll.rs:327` reads `gt.scale` again from a fresh `get_mut` — not guaranteed equal across frames. The #1616 round-trip test only validates the same-scale case (`scale: 1.0`, no step), so it can't surface this.
- **Impact**: Latent — vanilla ragdoll bones ship uniform, constant scale, so the two reads agree and the term is exact. Only a gameplay system that animates ragdoll-bone scale during simulation (shrink/enlarge FX) on a body authored with a non-zero `bhkRigidBodyT` local offset would surface a small positional wobble. Correctness hardening, not an active bug.
- **Suggested Fix**: Snapshot the seed-time scale into the body spec at activation and use that stored value in the writeback inverse, so compose/decompose always use the identical scale regardless of live `GlobalTransform` mutation. Alternatively, document the "ragdoll bones must not be rescaled while active" invariant next to the writeback math.

### FNV-D1-01: Stale doc comment claims FO3/FNV worldspace water default is unimplemented
- **Severity**: LOW
- **Dimension**: Cell Loading
- **Location**: `byroredux/src/cell_loader/exterior.rs:41-50`
- **Status**: NEW
- **Description**: The `default_water_height` field doc says FO3/FNV/Skyrim+ "are excluded pending DNAM parsing." That state no longer exists: the WRLD parser now reads DNAM's second f32 into `record.default_water_height` (`crates/plugin/src/esm/cell/wrld.rs:131-138`, regression-tested by `wrld_dnam_captures_default_water_height`), and `default_water_for_worldspace` (`byroredux/src/env_translate.rs`) consumes it for all non-Oblivion games. FNV coastal/sea cells without XCLW DO inherit the worldspace default water height today.
- **Evidence**: doc (stale) `exterior.rs:48-49`; parser (live) `wrld.rs:131` reads DNAM `[4..8]` into `default_water_height`; consumer (live) `env_translate.rs::default_water_for_worldspace` matches on `w.default_water_height`.
- **Impact**: Misleads maintainers into thinking FNV exterior water inheritance is a gap when it is shipped + tested. No runtime effect.
- **Suggested Fix**: Rewrite the field doc to state FO3/FNV/Skyrim+ resolve `default_water_height` from the DNAM second f32; drop the obsolete "excluded pending parsing" clause.

### FNV-D1-02: NIF-cache clip-handle commit ordered after insert-driven LRU eviction (latent leak; unreachable on FNV)
- **Severity**: LOW
- **Dimension**: Cell Loading (NIF import registry)
- **Location**: `byroredux/src/cell_loader/references.rs:745-757`; `byroredux/src/cell_loader/nif_import_registry.rs:221-223`
- **Status**: NEW
- **Description**: In the end-of-load batched commit, `pending_new` entries are `insert()`ed first (running LRU eviction), and `pending_clip_handles` are committed via `set_clip_handle` **afterward**. `set_clip_handle` (`nif_import_registry.rs:221`) inserts unconditionally with no check the key is still resident in `core.cache`. If a key inserted earlier in the same loop were evicted by a later insert in the same loop, its clip handle would be set for an already-evicted key: never `release()`d (keyframe-array leak) plus a dangling `clip_handles` entry lingering until `clear()`.
- **Evidence**: `references.rs:749` `freed.extend(reg.insert(key, entry));` then `references.rs:755-757` `for (key, handle) in pending_clip_handles { reg.set_clip_handle(key, handle); }` — no residency check. `set_clip_handle` body is a bare `self.clip_handles.insert(key, handle)`.
- **Impact**: Reachable only when a single `load_references` call inserts more than `BYRO_NIF_CACHE_MAX` (default 2048) unique NIFs so its own inserts evict each other. `load_references` runs per-cell (`exterior.rs:403`; the pre-M40 "all 49 cells in one call" path is gone), and no single FNV cell approaches 2048 unique models. **Not reachable on vanilla FNV**; latent hardening only.
- **Suggested Fix**: Commit `pending_clip_handles` before the insert loop, or guard `set_clip_handle` on `self.core.get(&key).is_some()` and release the handle otherwise.

### FNV-D1-03: Exterior terrain/water spawn results dropped without cell-level acknowledgement
- **Severity**: LOW
- **Dimension**: Cell Loading
- **Location**: `byroredux/src/cell_loader/exterior.rs:309, 335`
- **Status**: NEW
- **Description**: `terrain::spawn_terrain_mesh(...)` and `water::spawn_water_plane(...)` both return `Option<usize>` but are called with `let _ = ...`. The callees log internally on failure (terrain warns on upload failure; `water.rs:175` warns on mesh-upload failure), so a failure is not fully silent, but the exterior cell-load site gives no correlated "cell (gx,gy) had no terrain/water" signal, making a partial-cell-render harder to diagnose from the cell's own log line.
- **Evidence**: `exterior.rs:309` `let _ = terrain::spawn_terrain_mesh(...)`; `exterior.rs:335` `let _ = water::spawn_water_plane(...)`.
- **Impact**: Observability only; no correctness effect. Callee-side warnings already exist — mild redundancy gap, not a silent failure.
- **Suggested Fix**: Optional — `if ....is_none() { log::warn!("cell ({gx},{gy}): terrain/water spawn failed") }` for per-cell correlation.

---

## Regression Guards VERIFIED (attempted to disprove; all held)

### Dimension 1 — Cell Loading
- **Rapier body/collider release on unload (#1520, `34c7a218`)** — `cell_loader/unload.rs` releases per-cell GPU + physics resources; `remove_ragdoll` teardown at the unload path; covered by `rapier_release_tests.rs` / `inventory_release_tests.rs` / `unload_skin_cleanup_tests.rs` siblings.
- **NPC hand-mesh triple-load (#793)** — `npc_spawn.rs:233-235` loads `upperbody.nif` + `lefthand.nif` + `righthand.nif` for kf-era NPCs.
- **AnimationClipRegistry dedup lowercased (#790/#866)** — `crates/core/src/animation/registry.rs:212` keys the dedup map on `to_ascii_lowercase()`; cell streaming does not grow it unboundedly.

### Dimension 2 — NIFAL Canonical Translation
- **Single `ImportedMesh → Material` boundary** — `translate_material` (`byroredux/src/material_translate.rs:73-170`) is the only game-content Material site; both production callers (`scene/nif_loader.rs:828`, `cell_loader/spawn.rs:880`) route through it. Only other literals are the synthetic `cornell.rs` RT test scene.
- **Old render-time `Material::classify_pbr` deleted** — `render/static_meshes.rs:300-310` reads resolved `m.roughness`/`m.metalness`; zero `classify_pbr(` calls remain; only the free `classify_pbr_keyword` survives (import-time + one `resolve_pbr`).
- **Plain-`f32` resolved PBR** — `Material.metalness`/`roughness` are non-`Option`; `resolve_pbr` fills NaN from the classifier exactly once at translate.
- **EmissiveSource no-bleed** — BSLighting→`Lighting`, BSEffect→`Effect` (only under `!has_material_data`), NiMaterialProperty→`Material` (FNV path); FNV effect surfaces use `BSShaderNoLightingProperty` which never sets `Effect`.
- **Collision no-drop (`9c6096aa`)** — `resolve_shape_inner` maps `BhkMultiSphereShape`→`Compound` of `Ball` children and `BhkConvexListShape`→`Compound`/`ConvexHull`; both formerly-dropped shapes now surface.

### Dimension 3 — RT Lighting
- **BLAS budget dynamic (`compute_blas_budget`)** — `acceleration/predicates.rs:547-553` = `(device_local_bytes / 3).max(MIN_BLAS_BUDGET_BYTES)`, `MIN = 256 MB` (`constants.rs:61`), i.e. ~4 GB on a 12 GB box — not a stale 1 GB.
- **Disney-BSDF gate unreachable for FNV (#1248–#1252)** — `MAT_FLAG_PBR_BSDF = 32u` (`shader_constants.glsl:74`); the Rust flag `PBR_BSDF = 1<<5` is set only via `is_pbr` (`material.rs:461,689`), which the BGSM/BGEM merge flips — FNV authors no BGSM, so the flag is 0 across the FNV universe.
- **skyTint interior gate (#1125)** — `raytrace.glsl:87-88` computes `missCol` via `_isExt ? (skyTint*0.5 + sceneFlags.yzw*0.5) : cell-ambient`, returned at both miss sites (reflection `:133`, refraction `:148`); FNV interiors drop to cell ambient, not zenith blue.
- **GpuInstance satellite layout** — all 4 standalone GLSL `GpuInstance` copies match the 112-byte `#[repr(C)]` layout (verified against `scene_buffer/gpu_types.rs`).

### Dimension 4 — ESM Parser
- **SCOL FNV/FO3 dispatch (#1538)** — `esm/records/mod.rs:198` `is_scol_era = is_fo4_plus || Fallout3NV`; `b"SCOL" if is_scol_era` dispatches `parse_scol_group` for FNV; MOVS/PKIN/MSWP stay `is_fo4_plus`-gated and do not steal FNV dispatch.
- **SCPT SCHR flags u16 (#1654, `590351c1`)** — `esm/records/script.rs:144-145` reads `flags` via `u16_or_default` after the `script_type` u16 (cursor 18, 2 bytes left) on the 20-byte SCHR; regression test pins `flags == 0x0002`.
- **CELL XCLL `fog_far_color` optional** — FNV 40-byte XCLL has no fog_far_color (Skyrim+ `>= 92` ambient-cube addition); resolves to `None` on the FNV path.
- **TXST/XATO/XTNM/XTXR arms clean** — `esm/cell/walkers.rs`; `cargo build -p byroredux-plugin` emits zero `unreachable_patterns` warnings.
- **LVLI flattening** — `equip.rs::expand_leveled_form_id` resolves direct + recursive LVLI refs, depth-capped (`LVLI_MAX_DEPTH=8`), deterministic highest-eligible pick.

### Dimension 5 — NIF Parser
- **`havok_motion_type` full enum (#1652, `dc33ec7d`)** — `import/collision.rs:145` maps the raw byte per the canonical `hkMotionType` enum (1–5/8→Dynamic, 6→Keyframed, 7→Static, 9→CharacterKinematic, 0/other→Static); test `havok_motion_type_maps_full_enum` pins it. The pre-fix `4=>Keyframed / _=>Static` collapse is gone.
- **`examine_collision_kind` → Classic for FNV** — FNV bhk chains classify as `CollisionAuthoring::Classic`, not `NewPhysicsStub`/`Phantom`/`Unrecognised`.
- **version gates** — `bsver::FO3_FNV = 34 > NI_BS_LTE_16 = 16` places FNV on the post-Oblivion side of every field-layout gate.
- **walker depth (#1269)** — `MAX_NIF_NODE_DEPTH = 128` (`import/walk/mod.rs:162`) guards both hierarchical (`:190`) and flat (`:861`) walkers; a legit FNV scene does not trip it.

### Dimension 6 — Animation / Skinning / Particles
- **Clip registry populated at load (not per-frame)** — `scene.rs:298-301` uses `resource_mut` (structural, one-time); `convert_nif_clip` interns via the pool there — no per-frame StringPool locks (#340).
- **Interpolator channels** — `NiTransformInterpolator → NiTransformData` is the primary transform path with B-spline fallback reachable on FNV/FO3.
- **B-spline pose-fallback (#772)** — `NiBSplineCompTransformInterpolator` gated on the `FLT_MAX` sentinel; not ruled out by game era.
- **Typed-emitter particle pin (`5708b5b9`/`9db60714`)** — `NiPSysEmitter`/`Ctlr`/`CtlrData`/`GrowFadeModifier` are typed structs in `blocks/particle.rs`; `walk/mod.rs::extract_emitter_params`/`::extract_emitter_rate` feed `systems/particle.rs::apply_emitter_params` from authored data.

### Dimension 7 — PHYSAL Ragdoll
- **No per-game branch in `ragdoll.rs` or `crates/physics/`** — the only live per-game code is the byte-layout dispatch in `blocks/collision/constraints.rs` (`is_oblivion = bsver() <= NI_BS_LTE_16`); doctrine seam confined to the CInfo decode.
- **Malleable-wrapped Ragdoll → jointed body** — `parse_fo3_malleable_inner` surfaces the outer entity refs; byte-exact test `fnv_malleable_wrapping_ragdoll_surfaces_as_ragdoll` pins `entity_a=1/entity_b=2`. Not a single rigid blob.
- **Writeback ↔ skin-palette bone identity** — ragdoll template and skin bones resolve against the *same* skeleton entity map; no aliasing. NaN triple-guarded (import CInfo + writeback belt-and-suspenders #1534). Body↔template index parity safe.
- **Teardown** — keyframed-follower teardown on activation (#1772) and cell-unload ragdoll teardown (#1531) both present; stale-handle writeback returns `None` safely.
- **Scheduler** — `ragdoll_writeback_system` is `add_exclusive(Stage::Late)`, disjoint from camera GlobalTransform writes (#1601).

### Dimension 8 — Real-Data Validation (static verification)
- **CWD-relative archive resolution** — `asset_provider.rs` resolves bare `--bsa`/`--textures-bsa` names against CWD; the numeric-sibling auto-loader still drags `Textures2.bsa` for FNV.
- **`tex.missing`/`tex.loaded`** — command paths present in `commands/assets.rs`.
- FNV data dir confirmed present at `/mnt/data/SteamLibrary/steamapps/common/Fallout New Vegas/Data/` (FalloutNV.esm + `Fallout - Textures.bsa`/`Textures2.bsa` + DLC archives).

---

## Open FNV Issues NOT Re-Reported (already tracked)

- **#1718** — FNV-D7-01: ragdoll body + dependent constraints dropped silently on bone-name miss (no telemetry). FNV-D7-02 above is a *distinct* silent-drop (block-type-based in `extract_ragdoll`, not name-resolution-based) sharing the "no telemetry on a graph-edge drop" theme.
- **#1711** — SPT OBND-derived bs_bound discarded on the cell-loader route.
- **#1698** — RT-1: Skyrim Dragonsreach scheduler stall (not FNV-specific).

---

## Method Note

Dimensions 1/8, 2/4, 5/6, and 7 produced complete consolidated reports. Dimension 3
(RT lighting) exhausted its agent turn budget mid-analysis, so its verdict was
reconstructed from its transcript and **independently re-verified by the orchestrator
against current source** — the BLAS-budget (`compute_blas_budget`), PBR-BSDF gate
(`is_pbr`), and skyTint interior-gate (`raytrace.glsl` `missCol`) guards were all directly
confirmed by the orchestrator, not taken on the sub-agent's word. The MEDIUM ragdoll finding
(FNV-D7-02) and the FNV-D1-02 clip-handle finding were likewise re-verified directly in
source before inclusion. No CRITICAL/HIGH regression surfaced in any dimension.

---

**Suggested next step:**
```
/audit-publish docs/audits/AUDIT_FNV_2026-07-02.md
```
