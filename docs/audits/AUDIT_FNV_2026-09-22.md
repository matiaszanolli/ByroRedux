# FNV Compatibility Audit — 2026-09-22

**HEAD**: `ee6d3fb39` · **Baseline**: `docs/audits/AUDIT_FNV_2026-09-11.md` (HEAD `b3db49fa`) ·
**Audited**: 1, 2, 3, 4, 5, 6 (all, per the current 6-dimension `/audit-fnv` skill) ·
**Unchanged since baseline (skimmed)**: none — every dimension's `Paths:` saw
commits since 2026-09-11 (20-100+ commits per dimension) and was re-verified
in full.

Command: `/audit-fnv` (no `--focus`, default scope), run as one leg of
`/audit-suite --preset comprehensive`. Per the suite's constraint, **no
sub-agents were spawned**; all 6 dimensions were analysed serially by this
agent, one at a time, with per-dimension scratch notes written to
`/tmp/audit/fnv/dim_<N>.md` before moving to the next.

Dedup baseline: `gh issue list --repo matiaszanolli/ByroRedux --limit 6000
--state all` (4,561 issues; written to `/tmp/audit/fnv/issues_fnv.json` — the
shared `/tmp/audit/issues.json` was found truncated to 0 bytes mid-run,
apparently clobbered by a concurrent sibling audit in this suite re-running
the same refresh command, so this dimension used a private copy instead).

## Executive Summary

Fallout: New Vegas remains the healthiest title in the compatibility matrix.
This cycle's window (`b3db49fa`..`ee6d3fb39`) is unusually large — roughly
150 commits touched at least one FNV-audit dimension's paths — but nearly
all of it is either (a) general-mechanism work already freshly audited by
sibling dimension owners in this same suite run (`AUDIT_NIF_2026-09-21.md`,
`AUDIT_NIFAL_2026-09-21.md`, `AUDIT_ESM_2026-09-21.md`,
`AUDIT_PHYSICS_2026-09-21.md`, `AUDIT_GAMEPLAY_2026-09-21.md`,
`AUDIT_EXTERIOR_2026-09-21.md`, `AUDIT_CHARACTER_2026-09-21.md`,
`AUDIT_UI_2026-09-21.md`), or (b) fix-cycle commits that closed findings
from the 2026-09-11 FNV report itself.

**0 NEW findings this cycle.** All FNV-specific real-data regression tests
across all 6 dimensions pass against the real `FalloutNV.esm` and mesh/BSA
corpus (`/mnt/data/SteamLibrary/.../Fallout New Vegas/Data/`). Of the
2026-09-11 report's 6 findings:

- **Fixed** since baseline: FNV-D6-01 (Dim 4 — B-spline rotation/colour
  finite-value guard; fixed by `d53be91be`/`4c49eb7fd`, #4396/#4397/#4406),
  FNV-D2-01 (Dim 3 — material_translate.rs Phase-2 resolver doc undercount;
  fixed, doc now correctly says "all three"), FNV-D2-02 (Dim 3 — glass
  classification vs pre-computed PBR overlay-path asymmetry; fixed, closed
  as #4229).
- **Still open, unchanged, already tracked** (not re-filed): FNV-D4-2026-09-11-01
  / **#4232 (MEDIUM)** — `effective_actor_level` still returns `0` verbatim
  for authored-level-0 `NPC_` records, still emptying every owned leveled
  list. `#1887`/`#3511` (FNV `XATO` misread) — both CLOSED as accepted
  remediation-in-place, unchanged.
- One test-gap self-correction already landed at HEAD before this audit
  started: `1087ad7fa` (#4579, 2026-09-22) restored an orphaned `#[test]`
  attribute on `material_translate.rs`'s core NIFAL copy-fidelity guard
  (`translate_material_copies_every_canonical_field`), which had silently
  stopped running for two audit cycles. Already fixed; not a live defect.

Four FNV-relevant defects surfaced during this cycle's checks were all
**already found by sibling audits in this same suite run** and are cited
below rather than re-reported (per this skill's dedup rule and the
orchestrator's explicit citation list):

- `AUDIT_UI_2026-09-21.md` **UI-D7-2026-09-21-03** (MEDIUM) — `--hud`'s
  FO3-vs-FNV split reads only the `--esm` filename, never `--master`, so
  the documented FNV-DLC invocation shape and any mod-plugin launch get the
  **FO3** HUD profile (wrong texture archive) → a fully transparent HUD.
- `AUDIT_UI_2026-09-21.md` **UI-D7-2026-09-21-04** — `hud.rs`,
  `scaleform_hud.rs`, `commands/hud.rs` carry **zero** `#[test]`, confirmed
  live (`grep -c` → 0/0/0), including the new FO3/FNV profile code.
- `AUDIT_GAMEPLAY_2026-09-21.md` **GAME-D5-2026-09-21-01** (HIGH) —
  `resolve_entity_by_global_form_id` can never resolve `0x14` (PlayerRef),
  misrouting 75 FNV ambient-AI packages (Follow 52 / Escort 8 / Travel 13 /
  Guard 2) across 29 `NPC_` records.
- `AUDIT_CHARACTER_2026-09-21.md` **CHAR-2026-09-21-D4-01** (MEDIUM) — the
  FO3/FNV player template is stamped through the NPC population path, so the
  `PlayerOnly` `ActionPoints` ruleset row is never evaluated for the player
  — FO3/FNV players carry **no Action Points**.

Per the orchestrator's brief, also confirmed present and unchanged but not
independently re-derived: `AUDIT_PARSERS_2026-09-21.md` **PAR-D5-2026-09-21-01**
(FaceGen `.egm` half-float misdecode — affects every FNV runtime-FaceGen NPC
head) and **PAR-D4-2026-09-21-03** (no FNV MenuXml corpus test — confirmed
live via `find crates/menuxml/tests -iname "*fnv*"` → no hits).
`AUDIT_ESM_2026-09-21.md`'s FO76-specific HEDR finding (ESM-D1-01) is
confirmed not relevant to FNV; its D6-04/#4171 TERM-subrecord finding
explicitly states "FNV is correct" (the defect is FO4-only).

## Baseline Comparison Table

Compared against `ROADMAP.md` (compat matrix + bench-of-record tables) and
`docs/feature-matrix.md`, both re-read at HEAD `ee6d3fb39`; the FNV rows are
**byte-identical** to the ones the 2026-09-11 report cited at `b3db49fa` —
only one commit touched `ROADMAP.md` in this window (`d574d9bd1`, HUD
profiles) and it did not touch the FNV table.

| Metric | ROADMAP baseline | Observed this audit | Delta |
|---|---|---|---|
| NIF parse rate (clean) | 100% — 20,746/20,746 across 11 archives | **100.00%**, live re-run (`parse_rate_fallout_nv`) | No change |
| ESM `EsmIndex::total()` | 77,923 | **pass**, live re-run (`parse_rate_fnv_esm`) | No change (test asserts the count; not independently re-derived this cycle) |
| FNV SCOL bases / REFRs | 98 bases / 1,084 REFRs (#1538) | `is_scol_era` gate confirmed intact for `Fallout3NV`; not independently re-counted | No change |
| Prospector Saloon (interior) — entity/FPS/fence/draws | 3,626 ent / 145.1 FPS / 6.90 ms / fence=5.06 / 1,224 draws (TAA-native, `4c9a5b36`) | **Not re-benched live** — no Vulkan device session available to this audit | No comparison — coverage gap carried from prior cycle, not a regression |
| `creatures\protectron\skeleton.nif` ragdoll | 12/12 joints, 1 component (#3792) | `fnv_protectron_skeleton_is_one_connected_component` pass | No change |
| FNV ragdoll constraint-edge surfacing | 1,396/1,396 (100%), measured 2026-09-11 | Not re-measured corpus-wide this cycle (no sweep harness run); `fnv_humanoid_skeleton_threads_ragdoll` + Protectron guard both pass | No change (not independently re-derived) |
| Archive precedence (`Update.bsa` last-listed, all 3 pools) | Fixed (#3790/#3896/#3916) | `archive_precedence::*` (`--ignored`, real FNV data) 4/4 pass | No change |

No regressions were found against any baseline figure.

## Dimension Findings

### Dimension 1 — Cell Loading & Streaming
**0 findings.** `cargo test -p byroredux --bin byroredux cell_loader::` →
529 passed, 0 failed, 8 ignored. All FNV-cited regression guards
(`unload_greyscale_lut_tests`, `unload_skin_cleanup_tests`,
`rapier_release_tests`, `inventory_release_tests`,
`reference_state::tests::*`, `default_land_textures_exist_in_vanilla_archives`
`--ignored`, LIGH falloff sentinel) verified intact. See `/tmp/audit/fnv/dim_1.md`.

### Dimension 2 — ESM Data Slice (FalloutNV.esm)
**0 new findings.** `parse_rate_fnv_esm` and 5 other FNV-tagged real-master
tests (`--ignored fnv`) → 6/6 pass. `is_scol_era` (SCOL FNV-era dispatch,
#1538) and `SCHR` `u16` flags (#1654) gates confirmed intact; no
`unreachable_patterns` warning in `esm/cell/walkers.rs`. `effective_actor_level`
(MEDIUM, #4232, open since 2026-09-11) reconfirmed unfixed —
**Status: Existing #4232**, not re-filed. See `/tmp/audit/fnv/dim_2.md`.

### Dimension 3 — NIF / NIFAL Slice
**0 new findings.** Real-corpus tests (`parse_rate_fallout_nv`,
`per_block_baseline_fallout_nv`, `unknown_ceiling_fallout_nv`,
`real_archive_torch_meshes_surface_particle_emitters`) → 4/4 pass, including
the test that was **red on main 2026-09-12..19** (#4467, fixed 2026-09-19,
now confirmed green — no regression). `EmissiveSource`, `havok_motion_type`,
`examine_collision_kind`, `MAX_NIF_NODE_DEPTH` guards all pass (143+ + 8 + 1 +
56 unit tests). Two previously-open LOWs (FNV-D2-01, FNV-D2-02) confirmed
fixed. See `/tmp/audit/fnv/dim_3.md`.

### Dimension 4 — Animation, Skinning & PHYSAL Ragdoll
**0 new findings.** `fnv_protectron_skeleton_is_one_connected_component` and
`fnv_humanoid_skeleton_threads_ragdoll` (`--ignored`) → 2/2 pass. The
previously-open LOW (FNV-D6-01, B-spline rotation/colour finite-value guard)
is **fixed** by `d53be91be`/`4c49eb7fd` (#4396/#4397/#4406) — verified live
(`bspline` 29/29, `sanitize` 10/10). BallAndSocket/StiffSpring constraint
drop-with-warn behavior is structurally unchanged by the new CInfo-decode
commit (`5172f5e26`) — matches prior audit's documented open state, not
re-filed. See `/tmp/audit/fnv/dim_4.md`.

### Dimension 5 — Ambient AI, Consumables & HUD
**0 new findings; 4 cited from sibling 2026-09-21 audits** (UI-D7-2026-09-21-03,
UI-D7-2026-09-21-04, GAME-D5-2026-09-21-01, CHAR-2026-09-21-D4-01) — all
confirmed still present in current source during this dimension's checks,
none re-reported. Ambient-locomotion default-on, walk-clip resolution
(per-gender/child), walk-speed derivation, and consumable/hardcore real-data
tests all pass. See `/tmp/audit/fnv/dim_5.md`.

### Dimension 6 — Real-Data Validation, Bench-of-Record & Smoke Gates
**0 findings.** Archive-precedence mechanism (`Update.bsa` last-listed, all
3 pools) verified against real FNV data, 4/4 `--ignored` tests pass.
`fnv_meshes_bsa_v104_extracts_nif_with_gamebryo_magic` passes. No live
engine bench performed (no Vulkan device session available to this audit;
consistent with the no-parallel-engine-launch convention) — carried
coverage gap, not a new finding. See `/tmp/audit/fnv/dim_6.md`.

## Regression Guard List

Previously-fixed issues/invariants re-verified as still correct in current
source this cycle:

| Issue / Guard | Dimension | Status |
|---|---|---|
| #1538 (SCOL FNV-era dispatch gate) | 2 | Intact |
| #1654 (`SCHR` flags `u16` not `u32`) | 2 | Intact |
| #1887/#3511 (FNV `XATO` misread) | 2, 3 | Accepted remediation-in-place, unchanged |
| FNV-D2-01 (material_translate.rs Phase-2 resolver doc undercount) | 3 | **Fixed since 2026-09-11** |
| FNV-D2-02 / #4229 (glass classification vs overlay-path PBR asymmetry) | 3 | **Fixed since 2026-09-11** |
| #4467 (real_archive_torch emitter-rate test, red on main 2026-09-12..19) | 3 | **Fixed** (`ee3e3a4c8`, 2026-09-19), confirmed green |
| #4579 (orphaned `#[test]` on `translate_material_copies_every_canonical_field`) | 3 | **Fixed at HEAD** (`1087ad7fa`, same-day) |
| #790/#793/#772/#340 (registry dedup / hand-mesh / bspline sentinel / interning) | 4 | Intact |
| FNV-D6-01 (B-spline rotation/colour finite-value guard) | 4 | **Fixed since 2026-09-11** (#4396/#4397/#4406) |
| #1539/#3330/#3792 (dropped-constraint-kind warn) | 4 | Intact; residual occupancy unchanged |
| M42.10/M42.11 (ambient locomotion default-on, per-gait walk clips) | 5 | Intact |
| #3790/#3896/#3916 (`Update.bsa` last-listed, all 3 pools) | 6 | Intact, reconfirmed live against real data |
| #2560 / FNV-D8-01 (TAA-vs-FSR bench comparison trap) | 6 | Tracked, still open, no new regression |

## Deduplication Notes

- Fresh `gh issue list` snapshot (4,561 issues, `/tmp/audit/fnv/issues_fnv.json`)
  used for all dedup checks in this report (the shared
  `/tmp/audit/issues.json` was found empty mid-run — see header note).
- #4232 is the only OPEN issue this cycle's checks reconfirmed still
  reproduces; not re-filed.
- The four sibling-audit findings cited in Dimension 5 (UI-D7-2026-09-21-03,
  UI-D7-2026-09-21-04, GAME-D5-2026-09-21-01, CHAR-2026-09-21-D4-01) were
  independently reconfirmed present in current source by this audit's own
  checks before being cited, not merely copied from the sibling reports'
  text.

## Coverage Notes

- No live engine launch / GPU bench was performed this cycle, per this
  audit run's explicit no-engine-launch constraint (stricter than the
  "no-parallel-engine-launch" convention alone). Dimension 6's bench-of-record
  comparison and Dimension 1's smoke-test items (`p0-door-interaction.sh fnv`,
  `p5-save-restart.sh fnv`) were therefore verified via source/test-suite
  re-trace only, not a live run.
- Dimension 7's real-corpus ragdoll constraint-edge surfacing measurement
  (1,396/1,396, 2026-09-11) was not re-swept corpus-wide this cycle (no
  standalone sweep harness exists to run outside a full engine session);
  the two targeted real-data ragdoll tests that do exist as `cargo test`
  targets both pass.

---

**Findings by severity (NEW + regression, this report)**: CRITICAL 0 ·
HIGH 0 · MEDIUM 0 · LOW 0 · **Total 0 new**.

**Matched to existing open issues**: 1 (`#4232`, reconfirmed unfixed, not
re-filed).

**Cited from sibling 2026-09-21/22 audits (not this report's findings, not
re-filed)**: 6 (`UI-D7-2026-09-21-03`, `UI-D7-2026-09-21-04`,
`GAME-D5-2026-09-21-01`, `CHAR-2026-09-21-D4-01`, `PAR-D5-2026-09-21-01`,
`PAR-D4-2026-09-21-03`).

Suggested next step:
```
/audit-publish docs/audits/AUDIT_FNV_2026-09-22.md
```
(This report files 0 new issues — every finding it touches is either
already open (#4232) or already owned by a sibling 2026-09-21/22 report.
Nothing new to label.)
