# NIF Parser Audit — 2026-08-03

Deep audit of the NIF binary-format parser (`crates/nif/src/`) across all six
dimensions: stream position integrity, version gating, block dispatch
coverage, geometry extraction/import handoff, collision & shader block
parsing, and allocation hygiene. Full sweep, all games (Morrowind/Oblivion →
Starfield). Ran against the live tree with real game-data archives available
for all seven Steam-installed titles.

## Executive Summary

The NIF parser remains in very good health. Of six dimensions audited, three
(Stream Position, Block Dispatch Coverage, Collision/Shader Parsing) came back
completely clean — every regression guard from prior audit cycles is still
correctly in place, and no new defects survived disproof attempts. The other
three dimensions produced one HIGH, one MEDIUM, and two LOW findings.

The standout finding is **NIF-D2-01**: `NiMaterialProperty`'s Bethesda-compact
ambient/diffuse default is `0.5` where nif.xml specifies `1.0`
(`VEC3_ONE`), and — unusually — the renderer's own shader comments already
document and rely on the *correct* `1.0` assumption, meaning this is a
verified, live, systemic ~50% ambient-darkening bug across effectively all
Fallout 3 / Fallout New Vegas content using the legacy material-property path.
This was independently re-verified against `nif.xml` and the shader source
before inclusion (see Methodology note below) rather than taken at face value
from the sub-agent that surfaced it.

Per-game clean/recoverable rates are unchanged from the
`docs/engine/nif-parser.md` baseline (Oblivion 99.93%/100%, FO3/FNV/SkyrimSE/
FO4/FO76 100%/100%, Starfield 99.64% aggregate/100%) — none of this audit's
findings affect parse success or block-recovery rate; they affect *decoded
value correctness* (D2-01, D4-01) or code hygiene (D2-02, D6-01).

Dispatch table freshly counted at **249 top-level match arms** covering
**313 distinct block-type-name literals** in `parse_block_inner`
(`crates/nif/src/blocks/mod.rs`) — negligibly different from the doc's
self-disclaimed "~248/~315" 2026-07-18 snapshot, not worth a doc-drift
finding.

## Block Type Coverage Matrix

Coverage is measured against the checked-in per-game TSV baselines
(`crates/nif/tests/data/per_block_baselines/*.tsv`,
`crates/nif/tests/data/block_coverage_baselines/*.tsv`), last regenerated
2026-07-05/07-11. No `blocks/mod.rs` dispatch-table change has landed since
then (the one intervening commit, `f33edcf4`, touched version-gating inside
already-dispatched arms — Dimension 1/2 territory, not the dispatched-type
set), so the baselines are current for this report.

| Family | Oblivion | FO3 | FNV | SkyrimSE | FO4 | FO76 | Starfield |
|---|---|---|---|---|---|---|---|
| Nodes/geometry (NiNode, NiTriShape/Strips, …) | parsed | parsed | parsed | parsed | parsed | parsed | parsed |
| `BSGeometry` (Starfield container) | n/a | n/a | n/a | n/a | n/a | n/a | parsed |
| Shaders (PPLighting / BSLighting / BSEffect) | parsed (aliased variants) | parsed | parsed | parsed (per-variant) | parsed | parsed (CRC32) | parsed (CRC32 + stub) |
| Properties (Material/Alpha/Texturing/…) | parsed | parsed | parsed | parsed | parsed | parsed | parsed |
| Textures | parsed | parsed | parsed | parsed | parsed | parsed | parsed |
| Lights (NiLight hierarchy) | parsed | parsed | parsed | parsed | n/a¹ | n/a¹ | n/a¹ |
| Extra data | parsed | parsed | parsed | parsed | parsed | parsed | parsed |
| Controllers/interpolators | parsed | parsed | parsed | parsed | parsed | parsed | parsed |
| Skinning | parsed | parsed | parsed | parsed | parsed | parsed | parsed |
| Particle systems (~48 types) | parsed (legacy stack) | parsed | parsed | parsed | parsed | parsed² | parsed |
| Havok collision (14 `bhk*Shape` variants) | parsed | parsed | parsed | parsed | parsed | parsed | parsed |
| Spatial/palettes | parsed | parsed | parsed | parsed | parsed | parsed | parsed |

Unknown-block count: **0** across all six sized games (FO3/FNV/SkyrimSE/FO4/
FO76/Starfield, 287k–1.55M blocks each per baseline). Oblivion has no
`block_sizes` table; its residual 6/8032 truncating files are pre-Gamebryo
NetImmerse v3.x–v4.x debug marker meshes (`marker_arrow/divine/map/radius/
temple/travel.nif`, tracked under #1843) — an explicitly out-of-scope wire
format, not a missing v10+ dispatch arm.

¹ FO4+ (BSVER ≥130) reparents `NiLight` directly onto `NiAVObject`; not yet
implemented (tracked, see nif-parser.md "Open items"), not a new finding.
² FO76 `NiPSysBlock` aggregation is coarse-grained in the coverage harness by
design (union-keyed histogram across ~33 shared struct names) — detection
still works, only per-type attribution is coarse; resolved under #1883,
closed. The project-memory note flagging this as "silently RED" is stale.

## Findings

### NIF-D2-01: `NiMaterialProperty` compact-form ambient/diffuse default (0.5) contradicts nif.xml's declared default (1.0) and the renderer's own tuned assumption — systemic ~50% ambient darkening across FO3/FNV content

- **Severity**: HIGH
- **Dimension**: Version Gating
- **Game Affected**: Fallout 3, Fallout New Vegas (any content using the legacy `NiMaterialProperty` in its Bethesda-compact form, `bsver >= FLAGS_U32_THRESHOLD (26)` — true for essentially 100% of retail FO3/FNV content, which ships at `bsver = 34`). Oblivion (`bsver = 11`, reads the field explicitly) and Skyrim+ (uses `BSLightingShaderProperty`, not this legacy property) are unaffected.
- **Location**: `crates/nif/src/blocks/properties.rs:44-63` (parser default); contradicted by `crates/renderer/shaders/triangle.frag:2054-2108` (consumer assumption)
- **Status**: NEW
- **Description**: nif.xml gates `NiMaterialProperty.Ambient Color` / `.Diffuse Color` on `vercond="#BSVER# #LT# 26"` with `default="#VEC3_ONE#"` — verified directly against `/mnt/data/src/reference/nifxml/nif.xml` lines 63 and 4366-4367, `default="1.0, 1.0, 1.0"`. The parser correctly detects the field-absent case (`bethesda_compact = stream.bsver() >= FLAGS_U32_THRESHOLD`, matching the `#LT# 26` vercond with the right operator — `FLAGS_U32_THRESHOLD == 26` confirmed at `version.rs:358`), but substitutes `NiColor { r: 0.5, g: 0.5, b: 0.5 }` instead of `(1.0, 1.0, 1.0)`. Independently re-verified: the shader's own `AMBIENT_FILL` tuning comment (`triangle.frag:2092-2098`, REND-#1452 "Ulysses Temple floor" fix) explicitly reasons that "FO3/FNV and Skyrim interiors … have `mat_ambient=(1,1,1)`" — the renderer already assumes and depends on the spec-correct value, which the parser has never actually supplied for retail FO3/FNV content.
- **Evidence**:
  ```rust
  // crates/nif/src/blocks/properties.rs:44-63
  let bethesda_compact = stream.bsver() >= crate::version::bsver::FLAGS_U32_THRESHOLD;
  let ambient = if bethesda_compact {
      NiColor { r: 0.5, g: 0.5, b: 0.5 }   // nif.xml default is VEC3_ONE = (1.0,1.0,1.0)
  } else {
      stream.read_ni_color()?
  };
  // (diffuse mirrors the same pattern)
  ```
  ```glsl
  // crates/renderer/shaders/triangle.frag:2084-2108
  vec3 dielectricAmbient = sceneFlags.yzw * vec3(mat.ambientR, mat.ambientG, mat.ambientB) * (1.0 - metalness);
  ...
  const float AMBIENT_FILL = 0.5;
  vec3 ambientFill = sceneFlags.yzw * AMBIENT_FILL;
  vec3 ambient = max(dielectricAmbient + metallicAmbient, ambientFill);
  ```
  With `mat_ambient = 0.5` (bugged) and `metalness ≈ 0` (typical dielectric): `dielectricAmbient = 0.5 × cell_ambient`, numerically equal to `ambientFill = 0.5 × cell_ambient`, so `max()` collapses to `0.5 × cell_ambient` instead of the intended `1.0 × cell_ambient`.
- **Impact**: Every FO3/FNV static mesh carrying a legacy `NiMaterialProperty` (the norm for `BSShaderPPLightingProperty`/`BSShaderNoLightingProperty` content, i.e. most non-Skyrim+ shapes) renders at roughly half the intended ambient-fill brightness outside direct N·L light contribution — a systemic under-lighting bias across two full games' worth of content, in the exact code path that was already tuned once under the false belief the parser default was correct. The same wrong `0.5` also feeds `diffuse_color` consumers (fog tint, conductor-tint path in `asset_provider/material.rs`), though the ambient-term path is the dominant visible effect.
- **Suggested Fix**: Change the compact-form defaults in `NiMaterialProperty::parse` from `(0.5, 0.5, 0.5)` to `(1.0, 1.0, 1.0)` for both `ambient` and `diffuse`, matching nif.xml's `VEC3_ONE` default and the shader's already-tuned assumption. Add a regression test pinning `bsver=34` (FNV) ambient/diffuse to `(1.0,1.0,1.0)` alongside the existing `#323`/`#938` emissive-mult tests in the same file.

---

### NIF-D4-01: `BsTriShapeKind::LOD` triangle-count cutoffs are unreachable — `ImportedMesh.bs_lod_cutoffs` is always `None` on every real NIF

- **Severity**: MEDIUM
- **Dimension**: Geometry Handoff
- **Game Affected**: SkyrimSE / FO4 (the two games shipping distant-LOD tri-shape wire blocks)
- **Location**: `crates/nif/src/blocks/mod.rs:453-456`; `crates/nif/src/blocks/tri_shape/bs_tri_shape.rs:568-591`; `crates/nif/src/import/mesh/bs_tri_shape.rs:204-207`; `crates/nif/src/import/walk/mod.rs:477-500,1113-1138`
- **Status**: Regression of #1207 (the fix landed but never fires on real content)
- **Description**: Two distinct wire block names dispatch to two different Rust representations. `"BSLODTriShape"` (SkyrimSE) parses as `NiLodTriShape`, a classic-`NiTriShape`-bodied struct carrying its own `lod0_size`/`lod1_size`/`lod2_size` fields that are never read past the parser struct. `"BSMeshLODTriShape"` (FO4/SkyrimSE-DLC) parses via `BsTriShape::parse_lod()` — the only producer of `BsTriShapeKind::LOD{lod0,lod1,lod2}` anywhere in the crate — but the dispatcher immediately overwrites that value with `.with_kind(BsTriShapeKind::MeshLOD)` one line later. So `BsTriShapeKind::LOD` is constructed and discarded in the same expression on every real parse and can never be the persisted `kind` the importer sees. `import/mesh/bs_tri_shape.rs`'s `extract_bs_tri_shape` matches on exactly that unreachable variant to populate `bs_lod_cutoffs`; `NiLodTriShape`-typed shapes never even reach that function (the walker unwraps to the inner classic `NiTriShape` and calls the `ni_tri_shape.rs` extractor, which hardcodes `bs_lod_cutoffs: None`).
- **Evidence**: `grep -rn "BsTriShapeKind::LOD" crates/nif/src` shows the only non-doc, non-test production hit outside the enum definition is `bs_tri_shape.rs:589` inside `parse_lod`, whose sole caller (`blocks/mod.rs:454-456`) discards the value via `.with_kind(MeshLOD)`. The #1207 regression test (`lod_kind_surfaces_three_cutoffs`) hand-constructs a `BsTriShape` fixture with `kind: BsTriShapeKind::LOD{...}` directly, bypassing the real block dispatcher entirely — so it passes without ever exercising the path that would catch the gap. The #988 regression test (`bs_lod_tri_shape_imports_geometry_not_dropped`) builds a `NiLodTriShape` fixture with populated LOD sizes but only asserts the mesh renders, never that the values reach `ImportedMesh`.
- **Impact**: No present-day rendering regression — `grep -rn "bs_lod_cutoffs" byroredux/src` returns no consumers yet; the field is documented as "for an eventual M35 LOD selector." The blast radius is entirely forward-looking: when that selector is built, it will silently receive `None` for every mesh on both games, with no authored cutoff data to work from — the exact gap #1207 was opened to close, still open in practice.
- **Suggested Fix**: Either thread `NiLodTriShape`'s own `lod0_size`/`lod1_size`/`lod2_size` into `bs_lod_cutoffs` from the `NiLodTriShape` walker branch (the wire type that actually carries them on real SkyrimSE content), or drop the `.with_kind(MeshLOD)` override / add a `MeshLOD{lod0,lod1,lod2}` variant so `BSMeshLODTriShape`'s parsed cutoffs survive to import. Either way, rewrite the #1207/#988 regression tests to drive the real dispatcher on a synthetic `"BSLODTriShape"`/`"BSMeshLODTriShape"` block rather than hand-built fixtures, so they can't keep passing against an unreachable code path.

---

### NIF-D2-02: Bare `bsver` literals in two sites bypass the crate's own "named constants over decimal literals" doctrine

- **Severity**: LOW
- **Dimension**: Version Gating
- **Game Affected**: None functionally today (values agree with the equivalent named constants) — maintainability/drift-risk only.
- **Location**: `crates/nif/src/version.rs:529-548` (`NifVariant::detect`); `crates/nif/src/blocks/controller/sequence.rs:177`
- **Status**: NEW
- **Description**: `version.rs`'s own `bsver` module doc comment (lines 311-314) mandates using named constants over bare decimal literals in raw `bsver` comparisons. `NifVariant::detect`'s match arms hardcode `34`/`83`/`100`/`130`/`155`/`170` instead of `bsver::FO3_FNV`/`SKYRIM_LE`/`SKYRIM_SE`/`FALLOUT4`/`FO76`, defined a few dozen lines above in the same file. Today the literals agree with the constants (both `detect_*` unit tests and `bsver_values()` pin them independently), so there is no live misclassification — but the two are wired independently, so a future correction to e.g. `bsver::FALLOUT4` (the kind of fix #937/#1901/#2168 made elsewhere in this file) would silently desync `detect()`'s routing boundary from the constant every downstream feature gate uses. Separately, `controller/sequence.rs:177` uses `bsver > 0` where three other call sites in the crate use the `bsver > crate::version::bsver::PRE_BETHESDA` idiom (functionally identical, `PRE_BETHESDA == 0`).
- **Evidence**:
  ```rust
  // version.rs:532-538
  (11, uv2) if uv2 < 34 => Self::Fallout3,     // bsver::FO3_FNV == 34
  (12, uv2) if uv2 <= 83 => Self::SkyrimLE,    // bsver::SKYRIM_LE == 83
  (12, uv2) if uv2 <= 100 => Self::SkyrimSE,   // bsver::SKYRIM_SE == 100
  (12, uv2) if uv2 < 130 => Self::SkyrimSE,    // bsver::FALLOUT4 == 130
  (12, uv2) if uv2 < 155 => Self::Fallout4,    // bsver::FO76 == 155
  ```
- **Impact**: No current parse-correctness impact; pure drift-risk between `detect()`'s hardcoded boundaries and the named constants they're supposed to mirror.
- **Suggested Fix**: Rewrite `detect()`'s match arms to reference the named `bsver::*` constants instead of literal values; change `sequence.rs:177` to `bsver > crate::version::bsver::PRE_BETHESDA`.

---

### NIF-D6-01: `parse_particle_system`'s `modifier_refs` bypasses `allocate_vec`, duplicating its bound-check logic by hand

- **Severity**: LOW
- **Dimension**: Allocation Hygiene
- **Game Affected**: all (any NIF with a `NiParticleSystem`/`BSStripParticleSystem` block)
- **Location**: `crates/nif/src/blocks/particle.rs:1130-1139`
- **Status**: NEW
- **Description**: Every other file-driven `Vec<T>` allocation in the crate routes through `NifStream::allocate_vec` (bound-check + `with_capacity` in one `#[must_use]`-pinned call, per #831/#408). This one site instead hand-rolls the same pattern: `stream.check_alloc(...)?` followed by `reserve_exact(...)` and a manual push loop — precisely the pre-`#831` idiom the rest of the codebase (including all three `ragdoll.rs` sites re-verified this cycle) migrated away from. Appears to have been missed when the `allocate_vec` migration swept through `particle.rs`.
- **Evidence**:
  ```rust
  let num_modifiers = stream.read_u32_le()?;
  stream.check_alloc((num_modifiers as usize).saturating_mul(4))?;
  modifier_refs.reserve_exact(num_modifiers as usize);
  for _ in 0..num_modifiers {
      modifier_refs.push(stream.read_block_ref()?);
  }
  ```
  (The other `check_alloc`-only sites at `particle.rs:682`, `:1075`, `:1393` are read-and-discard skip loops with no backing `Vec` — not instances of this pattern.)
- **Impact**: None measurable — `check_alloc` + `reserve_exact` is allocation-equivalent to `allocate_vec`. Pure duplication/consistency issue, flagged because it's the one remaining template a future contributor could copy to reintroduce the old idiom elsewhere.
- **Suggested Fix**: Replace with `stream.allocate_vec::<BlockRef>(num_modifiers)?` followed by the same push loop, matching `read_block_ref_list` and every other bulk-ref site in the crate.

## Prioritized Fix Order

1. **NIF-D2-01** (HIGH, rendering-blocking in the sense of visibly wrong lighting) — one-line default change in `properties.rs`, immediately improves FO3/FNV visual fidelity. Fix first.
2. **NIF-D4-01** (MEDIUM, forward-looking/animation-and-LOD-adjacent) — no current runtime impact, but should be fixed before M35's LOD selector is built on top of the currently-dead field, and its own regression tests need rewriting to actually guard the real dispatch path.
3. **NIF-D2-02** and **NIF-D6-01** (LOW, hygiene/drift-risk) — no live impact, safe to batch with other tech-debt cleanup.

## Non-Findings (dimensions verified clean)

- **Dimension 1 (Stream Position Integrity)**: no new findings. All six named regression guards (Oblivion v10.x truncation family, Starfield `BSLightingShaderProperty` over-read #1510 + trailing tail #1606, root-NiNode truncation #688/#698) confirmed still fixed; `BSShaderPropertyData::parse_fo3` confirmed byte-correct with FO3-era-only call sites. Empirically corroborated by zero unknown-block counts across all seven games' checked-in baselines.
- **Dimension 3 (Block Dispatch Coverage)**: 249 top-level arms / 313 literals counted fresh. Zero unknown blocks across all six sized-game baselines. The Oblivion sizeless-truncation residual (6/8032 files) is confirmed out-of-scope pre-Gamebryo NetImmerse content, not a missing arm. FO76 `NiPSysBlock` coarse-attribution note in project memory confirmed stale (resolved under closed #1883). One dead-code overlap with existing open issue **#2268** (TD8-003) found and correctly not re-reported.
- **Dimension 5 (Collision & Shader Block Parsing)**: no new findings across all 912 crate unit tests including 44 shader and 94 collision tests. Every `bhk*` parser cross-checked field-by-field against nif.xml; `BSLightingShaderProperty`'s three-way dispatch confirmed to read only its own field set per band; all five named regression guards (Starfield stub discriminator #1721, `BhkMultiSphereShape`/`BhkConvexListShape` coverage, `CollisionAuthoring::Unrecognised`, `hkMotionType` mapping #1652, FO4 model-space-normals/alpha-test #1592) confirmed intact. One suspicious-looking asymmetry (`BSEffectShaderProperty` reading its FO76 trailing block unconditionally at `bsver >= FO76`, including Starfield, unlike its `BSLightingShaderProperty` sibling) investigated and ruled a deliberate, independently-tested difference between the two block types, not contamination.

## Methodology Note

The HIGH finding (NIF-D2-01) was independently re-verified by the orchestrating session — not merely accepted from the reporting sub-agent — by directly re-reading `properties.rs`, cross-checking the exact `FLAGS_U32_THRESHOLD` value against `version.rs`, and confirming the `nif.xml` default plus the shader's own tuning comment, per the project's audit-finding-hygiene practice of validating a premise before it's written up.

All four findings were checked against `/tmp/audit/issues.json` (47 open issues, fetched fresh this session) for duplication; none collided with an existing open issue under the same premise. One adjacent overlap (dead particle-modifier code, #2268) was correctly identified by the Dimension 3 pass and excluded as already tracked.

Suggest: `/audit-publish docs/audits/AUDIT_NIF_2026-08-03.md`
