# NIF Parser Audit — 2026-08-23

Scope: all 6 dimensions, all games (no `--focus`/`--game`/`--corpus` restriction).
Preset: `nif-deep` (part of `/audit-suite --preset nif-deep`).

## Executive Summary

Clean sweep. All 6 dimensions re-verified against current HEAD (`5f4dea46`,
post-#3231 morph-target GPU plumbing) rather than trusting the prior
2026-08-20 report's PASS marks at face value. **Zero new findings.** Three
dimensions ran live corpus/test-suite verification (not just static review):
Dimension 3 swept every vanilla mesh-bearing archive for all 7 games
including several archives outside the checked-in baseline gates; Dimension 6
ran `heap_allocation_bounds`, `heap_allocation_bounds_geometry`, and the
`bs_geometry` unit suite (all green); Dimension 3 additionally ran the
opt-in `per_block_baseline_*` tests live.

All previously-tracked regression guards (Oblivion v10.x truncation family,
Starfield `BSLightingShaderProperty` over-read #1510 + trailing-tail capture
#1606, root-NiNode inline-type-name truncation #688/#698, the `#[must_use]`
allocation discipline, `Arc<str>` block-name interning, the
`pre_parse_cell` serial/rayon split) remain intact.

No corpus directory needed staging — all 7 game data directories already
present on this machine.

## Findings Carried Forward (not re-reported — dedup confirmed unchanged)

| ID | Dim | Severity | One-line |
|----|-----|----------|----------|
| #3174 | 1 | — | `NiPSys*Ctlr` family byte-identical to tracked issue; still open |
| #3175 | 3 | MEDIUM | `NiSingleInterpController` baseline false-positive from benign `e32e2b1f` reclassification (counts conserve exactly) |
| #2365 | 3 | LOW | `nif-parser.md` cites wrong closed issues (#746/#747) for the Starfield `BSWeakReferenceNode` residual 6-file truncation |
| #3176, #3177 | 4 | LOW | `crates/nif/src/import/mesh/tangent.rs`, unregressed |

## Dimension Results

| Dim | Name | New Findings | Notes |
|-----|------|---------------|-------|
| 1 | Stream Position Integrity | 0 | 4 commits since last sweep touch none of `crates/nif/` stream-position code |
| 2 | Version Gating | 0 | Zero raw `bsver` literals outside `version.rs`; all 9 `NifVersion` helpers have live call sites; the one in-window change (`fa515b9c`, #3186) is a correctly-gated addition |
| 3 | Block Dispatch Coverage | 0 | 253 top-level match arms / 315 type-name literals counted fresh; full 7-game corpus sweep incl. archives outside baseline gates; 2 hypothesized gaps (`BSDistantObjectExtraData`, bare `BSExtraData`) disproved against real data before filing |
| 4 | Geometry Extraction & Import Handoff | 0 | New `#3231` `extract_morph_targets` surface given 6 independent disproof attempts (coordinate conversion, `relative_targets`, positional-index drift, vertex-count consistency, controller-chain cost, all 6 `ImportedMesh` construction sites) — none produced a live defect |
| 5 | Collision & Shader Block Parsing | 0 | Only in-window change is a doc-comment-only commit (`920f4db9`); fresh field-by-field cross-check of `BhkCompressedMeshShapeData` (the one file with no prior report coverage) matches nif.xml byte-for-byte |
| 6 | Allocation Hygiene (PERF) | 0 | Live test run: `heap_allocation_bounds` 3/3, `heap_allocation_bounds_geometry` 1/1, `bs_geometry` unit suite 50/50 incl. #2032/PERF-D8-01 hostile-input guards |

## Cross-Reference

Note: the NIFAL sweep run in parallel (`AUDIT_NIFAL_2026-08-23.md`) found a
**HIGH** finding (NIFAL-D8-2026-08-23-01) whose root cause sits in
`byroredux/src/cell_loader/refr.rs` (a BGSM texture-role resolver) — this is
a *translate*-side defect (wrong canonical role assignment), not a *parse*
byte-correctness defect, so it correctly belongs to NIFAL Dimension 8 and is
not duplicated here.

## Prioritized Fix Order

No new NIF-parser work items from this sweep. Carried-forward items in
priority order: #3175 (baseline false-positive, cosmetic test drift, low
urgency) → #2365 / #3176 / #3177 (doc/LOW cosmetic).

Suggest: `/audit-publish docs/audits/AUDIT_NIF_2026-08-23.md` (no new findings
to publish — this report is a clean-sweep confirmation).
