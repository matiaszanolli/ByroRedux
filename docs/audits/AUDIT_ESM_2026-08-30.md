# ESM / Plugin Parser Audit — 2026-08-30

**Scope**: `crates/plugin/src/` — the ESM/ESP record layer as a *parser*: header
detection, GRUP walk, sub-record byte accounting, per-record schema dispatch,
the FormID load-order remap, the CELL/WRLD walkers, the localized-strings
tables, the `EsmIndex` → ECS handoff, and the Redux-native plugin tier.
Depth: **deep** (byte-level trace against real masters). Games: **all seven**
installed titles.

**Test baseline**: `cargo test -p byroredux-plugin` — **863 passed, 0 failed,
51 ignored** (the ignored set is the real-data-gated corpus tests; gating them
is deliberate, `bdc0d84e`). No pre-existing failure to contextualise.

---

## Executive Summary

| Severity | Count |
|---|---|
| CRITICAL | **0** |
| HIGH | **1** |
| MEDIUM | **4** new + 1 existing (#3403, re-verified unfixed) |
| LOW | **8** |
| INFO | **1** |

**The one HIGH** (D3-01) is a live continuation of the #3400 defect class the
last audit opened and the `05bdb969` sweep was believed to have closed: the
sweep's touched-file list does not include `items.rs`, `misc/equipment.rs` or
`actor/mod.rs::parse_race`, so the entire **armor-equip reference chain** —
`RACE.WNAM` → `ARMO.MODL` (armature list) → `ARMA.RNAM` (race match) — still
reads plugin-local FormIDs while the maps it looks them up in are keyed in
global space. Measured on shipped FO4 DLC: **326 of 408** ARMO armature
references, 67 ARMA race references and 20 RACE default-skin references are
self-referencing (`0x01…`), so DLC- and mod-added armor resolves to **zero**
meshes on a real `--master Fallout4.esm --esm DLC*.esm` load order.

**The most concrete MEDIUM** (D8-01) closes a "cause not yet identified" note
that has sat in `cell/walkers.rs` since #385: FNV `LAND 0x00150FC0` fails to
inflate because its zlib **Adler-32 trailer is wrong**, not its stream — raw
DEFLATE recovers exactly the declared 4385 bytes of well-formed
`DATA`/`VNML`/`VHGT`. The tile is The Strip at grid (-6, 26).

**Dimensions that produced no findings: Dimension 6 (Localized Strings) only.**
Every other dimension produced at least one. Dimension 6's clean bill includes a
disproved hypothesis worth recording (see below).

### Verification discipline
Seven candidate findings were **verified away** before writing — a stale rate of
7 / 24 (29%), higher than the historical ~1-in-6, mostly because the skill's own
checklist carries three items that landed since it was last synced:

1. *NAVM shared row decoder / stride mismatch* — `rows(…, 8)` and `rows(…, 10)`
   both appeared to feed `decode_nvdp_row`. Reading the bodies shows the packed
   path uses an inline closure with its own field order + census citation.
2. *`parse_refr_group` still recurses unbounded* — the skill's Dim-5 checklist
   states this as "the live regression case, not a hypothetical". It was closed
   in `fa511bbf` (#3503); `walkers.rs:678` bounds it and `:689` threads `depth + 1`.
3. *Starfield ships an unrouted top-level `INFO`/`DLBR` GRUP* — both groups exist
   but contain **0 records**.
4. *`StringTableSet::resolve` is type-agnostic and can return the wrong string* —
   measured the three shipped `skyrim_english` tables (30 301 / 2 686 / 34 427
   entries): **zero ID overlap** in all three pairs. First-match is correct.
5. *`weather.rs:704` indexes `sub.data[14]` unguarded* — the guard is
   `len() >= SKYRIM_DATA_SIZE` (19), a named constant a numeric scan misses.
6. *FO4/FO76 walk drops ~80 k records* — the file is walked to EOF with zero
   errors and two independent walkers agree on the counts; it is `HEDR.count`
   semantics, demoted to INFO (D8-02).
7. *`rows()` silently drops a stride remainder* (prior ESM-2026-08-27-D2-01) —
   now carries an explicit corpus-evidence rationale. Deliberate; not re-filed.

Additionally, four items the skill lists as settled were re-verified and are
**not** re-litigated: the WATR `DATA` 0..28 prefix map, the CELL/WRLD tri-state
`XCLW` decode, the GRUP nesting bound across all 13 recursive walkers, and
FO76 `HEDR = 266.0`. `CLAS` SPECIAL-in-`ATTR` was not touched.

---

## Record Coverage Matrix

Routing coverage over every installed vanilla master. "Routed" = the record sits
under a top-level GRUP label the dispatcher has an arm for (118 labels: 9
cell-tier + 8 `dispatch_*` routers). Measured by an independent walker.

| game | records | routed | % | unrouted | unrouted labels | top unrouted (records) |
|---|---|---|---|---|---|---|
| Oblivion   | 1 167 016 | 1 166 656 | **100.0** |     360 |  3 | LVSP 306, SBSP 33, SKIL 21 |
| FO3        |   718 951 |   718 951 | **100.0** |       0 |  0 | — |
| FNV        |   465 016 |   465 016 | **100.0** |       0 |  0 | — |
| Skyrim SE  |   869 687 |   858 308 | 98.7 |  11 379 | 34 | DLBR 3061, SNDR 2453, DLVW 1126, KYWD 825, RELA 642 |
| FO4        | 1 549 276 | 1 524 756 | 98.4 |  24 520 | 46 | RFGP 6116, SNDR 5475, LAYR 3832, KYWD 3539, TRNS 949 |
| FO76       | 5 635 950 | 5 528 732 | 98.1 | 107 218 | 89 | RFGP 29599, SNDR 15475, LAYR 11540, KYWD 10544, LCRT 7051 |
| Starfield  | 3 829 246 | 3 682 200 | 96.2 | 147 046 | 95 | RFGP 80584, LMSW 12966, LAYR 6348, AVMD 6154, LCTN 6017 |

The parser is **complete** on the Gamebryo-Fallout era it was built against and
progressively thinner up the Creation-Engine lineage. Every modern gap falls
into four families: keyword/relationship metadata (`KYWD` `AACT` `RELA`), the
Creation-era audio graph that replaced `SOUN` (`SNDR` `SOPM` `SNCT` `MUST`),
the Story Manager (`SMBN` `SMEN` `SMQN`), and reference groups (`RFGP` — the
single largest unrouted type on FO4/FO76/Starfield).

### Sub-record coverage (a code never matched anywhere in the crate)

Lower bound — a code handled for one record type counts as handled for all.

| master | sub-record occurrences | never-matched | share | distinct (rec, sub) pairs |
|---|---|---|---|---|
| Oblivion.esm  | 4 574 981 | 227 872 |  4.98% |  64 |
| Fallout3.esm  | 2 634 084 | 110 675 |  4.20% | 217 |
| FalloutNV.esm | 2 321 453 | 113 114 |  4.87% | 243 |
| Skyrim.esm    | 4 131 332 | 414 334 | 10.03% | 306 |
| Fallout4.esm  | 6 742 917 | 888 497 | 13.18% | 501 |

**Per-record-type decode-rate drops across games** — the schema-split signal
this dimension exists to compute (unknown-share per record type, ≥25 pp swing):

| record | OBL | FO3 | FNV | SSE | FO4 |
|---|---|---|---|---|---|
| `RACE` |  2.3% |  7.0% |  7.0% | **90.7%** | **92.2%** |
| `NPC_` |  7.8% | 11.3% | 10.2% | **57.4%** | 18.8% |
| `ARMA` |   —   | 11.9% |  9.1% | 18.8% | **56.3%** |
| `FACT` |  0.0% |  0.0% |  0.0% | **41.3%** | **41.5%** |
| `WEAP` |  0.0% | 16.7% | 19.6% | 21.2% | **41.2%** |
| `SOUN` |  0.1% |  9.3% | 19.2% | **39.4%** | 37.3% |

### Header detection & walk integrity

| master | HEDR | variant | `GameKind` | groups | walker errors | walked to EOF |
|---|---|---|---|---|---|---|
| Oblivion.esm   | 1.00 | Oblivion | Oblivion   |  85 079 | 0 | yes |
| Fallout3.esm   | 0.94 | Tes5Plus | Fallout3NV |  89 748 | 0 | yes |
| FalloutNV.esm  | 1.34 | Tes5Plus | Fallout3NV |  77 000 | **1** (D8-01) | yes |
| Skyrim.esm     | 1.71 | Tes5Plus | Skyrim     |  50 494 | 0 | yes |
| Fallout4.esm   | 1.00 | Tes5Plus | Fallout4   | 112 381 | 0 | yes |
| SeventySix.esm | **266.0** | Tes5Plus | Fallout76 | 123 351 | 0 | yes |
| Starfield.esm  | 0.96 | Tes5Plus | Starfield  | 101 341 | 0 | yes |

### Parse cost + peak RSS (release build)

| master | file | parse | peak RSS | index ≈ RSS − file |
|---|---|---|---|---|
| Oblivion.esm   | 265 MB | 1.41 s | 1 441 MB | ~1.18 GB |
| Fallout3.esm   | 275 MB | 1.23 s | 1 059 MB | ~0.78 GB |
| FalloutNV.esm  | 234 MB | 1.17 s |   861 MB | ~0.63 GB |
| Skyrim.esm     | 238 MB | 1.27 s |   980 MB | ~0.74 GB |
| Fallout4.esm   | 315 MB | 1.69 s | 1 440 MB | ~1.13 GB |
| SeventySix.esm | 880 MB | 3.41 s | 3 509 MB | ~2.63 GB |
| Starfield.esm  | 1.39 GB | not run — no safe headroom on this host | | |

### Starfield resolve-rate baseline
`sf_smoke` on `Starfield.esm`: 176 distinct top-level FourCCs, **78 handled /
1 254 763 936 bytes = 86.1% of GRUP bytes**. Byte-identical to
`docs/engine/starfield-esm-phase0-baseline.md`. **No regression.**

---

## Findings

### HIGH

#### ESM-2026-08-30-D3-01: `parse_armo`, `parse_arma` and `parse_race` all bypass the load-order remap — every DLC-/ESL-added FO4 armor resolves to zero meshes
- **Severity**: HIGH
- **Dimension**: FormID & Load Order
- **Record / Sub-record**: `ARMO`/`MODL`, `ARMA`/`RNAM`+`MODL`, `RACE`/`WNAM`
- **Location**:
  - `crates/plugin/src/esm/records/items.rs:538-541` — `ARMO.MODL` armature FormIDs pushed raw
  - `crates/plugin/src/esm/records/misc/equipment.rs:67` — `parse_arma` takes no `remap` parameter at all; `:99` (`RNAM`), `:105` (`MODL` additional races)
  - `crates/plugin/src/esm/records/actor/mod.rs:1428` — `parse_race` takes no `remap`; `:1739` (`WNAM` default skin), `:1722` (`XNAM`)
  - Consumers: `crates/plugin/src/equip.rs:245-285`; `byroredux/src/npc_spawn.rs:806, :819, :935`
- **Status**: NEW — same defect class as **#3400** (CLOSED, filed HIGH). The
  `05bdb969` sweep's touched-file list contains neither `items.rs` nor
  `misc/equipment.rs`, and `parse_race` was untouched, so the premise that "the
  remap sweep is finished for the `records/` tier" is incomplete.
- **Description**: `EsmReader::read_record_header` remaps every record's own
  FormID, so `EsmIndex.armor_addons` / `.items` / `.races` are keyed in global
  space. The three parsers above read *embedded* FormIDs raw. Every lookup in
  the armor-equip chain therefore misses the moment the remap is non-identity —
  which it is for the second and later plugin of any multi-master load order and
  for every ESL by construction.
- **Evidence**:
  - `actor/mod.rs:1012` — NPC side is remapped: `record.race_form_id = remap_fid(raw, remap);`
  - `equipment.rs:99` — ARMA side is not: `out.race_form_id = SubReader::new(&sub.data).u32_or_default();`
  - `equip.rs:246`/`:277` — `index.armor_addons.get(&arma_fid)` with the **raw** id from `ARMO.MODL`; on a miss the loop `continue`s, and `resolve_armor_meshes` ends `Vec::new()` (`equip.rs:285`).
  - `equip.rs:250` — `arma.race_form_id == race_form_id`, where `race_form_id` is the **remapped** `npc.race_form_id`.
  - Census over the shipped FO4 DLC masters (each declares exactly one master, so its own forms are authored `0x01…` while `allocate_global_slot` keys them `0x02…` — the exact non-identity case #3400 names):

    | plugin | ARMO `MODL` armature refs, self-ref / total | ARMA `RNAM` self-ref / total | RACE `WNAM` self-ref / total | `NPC_` `RNAM` self-ref / total |
    |---|---|---|---|---|
    | DLCRobot.esm     |  26 / 35  |  4 / 31  |  2 / 6  |  58 / 235 |
    | DLCCoast.esm     |  97 / 137 | 31 / 94  | 11 / 15 |  83 / 378 |
    | DLCNukaWorld.esm | 203 / 236 | 32 / 196 |  7 / 15 | 115 / 691 |

  - **326 of 408** armature references across the three DLCs point at an ARMA the same plugin adds.
- **Impact**: on `--master Fallout4.esm --esm DLCNukaWorld.esm` (and on every
  ESL, where the remap is non-identity by construction) DLC- and mod-added armor
  renders as **empty space** — indistinguishable from an unshipped mesh, the same
  user-visible symptom #3400 was filed for. Where the ARMA *does* resolve, the
  un-remapped `ARMA.RNAM` still fails the race match for all 67 DLC-added-race
  addons, dropping the actor into the deliberately single-valued pass-2 fallback
  — the exact multi-addon collapse #3357 fixed. References to *base-game*
  races/ARMAs are unaffected (`mod_index` 0 → slot 0, identity), which is why
  single-plugin vanilla loads look correct.
- **Related**: #3400, #3401, #3314, #3357; D3-02 below.
- **Suggested Fix**: thread `remap: &Option<FormIdRemap>` into `parse_arma` and
  `parse_race`, wrap `RNAM` / `WNAM` / `XNAM` / the additional-race `MODL` push,
  and wrap `items.rs:539`'s `armatures.push(id)` in the `remap_fid` `parse_armo`
  already receives. Then add all three parsers to the
  `record_parsers_with_embedded_form_ids_take_a_remap` allowlist.

---

### MEDIUM

#### ESM-2026-08-30-D2-01: Skyrim `BOOK.DATA` is decoded with the 10-byte FNV layout against a 16-byte record — `value` and `weight` are read 2 bytes early and are garbage
- **Severity**: MEDIUM
- **Dimension**: Sub-Record Byte Accounting
- **Record / Sub-record**: `BOOK` / `DATA`
- **Location**: `crates/plugin/src/esm/records/items.rs:834-857` (`parse_book`)
- **Status**: NEW
- **Description**: The arm groups `Oblivion | Fallout3NV | Skyrim | Fallout76 |
  Starfield` and decodes `flags(u8), skill(u8), value(u32), weight(f32)` = 10
  bytes, with **no length guard**. Its own comment concedes the shape:
  *"Preserve the existing decode for the other families until their BOOK schemas
  receive dedicated coverage."* Skyrim's record is 16 bytes.
- **Evidence**: census over `Skyrim.esm` — all **821** BOOK `DATA` sub-records
  are exactly 16 bytes. Sample payloads
  `04000000 ecdd1000 da020000 0000803f` and `04000000 eef71000 ee020000 0000803f`.
  Decoding all 821 both ways:
  - current (`value`@2, `weight`@6): value ranges 0 … **4 294 901 760** (`0xFFFF0000`); weight ranges 0 … 4.74e-33 (denormal noise).
  - offsets 8 / 12: value ranges 0 … **2 500**; weight ranges 0 … **20.0**.

  The 8/12 reading is self-evidently the authored one — book prices and weights.
  The bytes at 4..8 are FormID-shaped (`0x0010DDEC`, `0x0010F7EE`): the skill-book
  "Teaches" reference, which the current arm swallows as part of `value`. No spec
  citation is needed; the two candidate offsets differ by six orders of magnitude
  in plausibility. **No test pins the current behaviour**, so this is an open gap,
  not a settled decode.
- **Impact**: every Skyrim/SSE book in `EsmIndex.items` carries a 4-billion-scale
  `value` and a ~0 `weight`. Any consumer doing economy or carry-weight math on
  Skyrim data gets nonsense, and a `u32` near `u32::MAX` will wrap or saturate
  downstream.
- **Suggested Fix**: give `GameKind::Skyrim` its own arm gated on `len() >= 16`
  (`flags u8, type u8, unknown u16, teaches u32, value u32, weight f32`), routing
  `teaches` into the existing `teaches_skill` field — currently fed only by
  `SKIL`, which Skyrim does not emit. Add a length guard to the 10-byte arm so a
  16-byte record can never take it. FO76 / Starfield need their own census before
  being moved.

#### ESM-2026-08-30-D3-02: the `#3400/#3401` remap source guard is a hardcoded 8-parser allowlist, and 11 more embedded-FormID reads sit outside it
- **Severity**: MEDIUM
- **Dimension**: FormID & Load Order
- **Record / Sub-record**: `WEAP`/`AMMO`+`ETYP`, `AMMO`/`DATA`+`DAT2`+`DNAM`, `NOTE`/`SNAM`, `COBJ`/`CNAM`+`BNAM`, `MGEF`, `PERK`, `ECZN`/`DATA`
- **Location**: guard at `crates/plugin/src/esm/records/tests.rs:2159`
  (`record_parsers_with_embedded_form_ids_take_a_remap`). Un-swept sites:
  `items.rs:371, 407` (`ammo_form`), `:411` (`skill_form`), `:654, 668, 677`
  (`projectile_form`), `:670` (`casing_form`), `:893` (`topic_form`);
  `misc/equipment.rs:216, 219` (`COBJ`); `misc/magic.rs:120` (`MGEF` light),
  `:348, 356` (`PERK` quest/spell); `misc/world.rs:1121` (`ECZN` owner —
  `parse_eczn` takes no remap at all).
- **Status**: NEW (successor to ESM-2026-08-27-D3-02 / #3401, which named a
  different, now-fixed set; #3401's title claim of "~12 more" was not exhaustive)
- **Description**: the guard enumerates exactly the 8 parsers the sweep touched
  (`parse_scol`, `parse_pkin`, `parse_movs`, `parse_flst`, `parse_tree`,
  `parse_acti`, `parse_navm`, `parse_regn`) and asserts each *source* contains a
  `remap: &Option<FormIdRemap>` parameter. A parser simply absent from the list is
  invisible to it — so the guard cannot detect the drift it was written to
  prevent. `parse_arma`, `parse_cobj`, `parse_eczn` and `parse_race` do not even
  take the parameter.
- **Impact**: latent for the sites listed here (none has a live cross-record
  consumer today; `topic_form` is closest — `crates/scripting/src/dialogue.rs:349`
  keys a topic lookup by FormID). Their cost is that each is one consumer away
  from becoming D3-01, which is exactly how D3-01 happened.
- **Suggested Fix**: invert the guard — assert that no `records/**.rs` file
  contains a FormID-shaped read outside a `remap_fid(` / `read_form_id(` call.
  Failing that, extend the allowlist to every `parse_*` that reads an embedded
  FormID and let the compiler force the parameter through (40 of 73 parsers
  currently take none; most legitimately read no FormIDs, ~10 do).

#### ESM-2026-08-30-D7-02: `EsmIndex` residency is 0.6–2.6 GB of RAM per master and appears nowhere in `docs/engine/memory-budget.md`
- **Severity**: MEDIUM
- **Dimension**: ESM→ECS Handoff
- **Record / Sub-record**: —
- **Location**: `crates/plugin/src/esm/records/index.rs` (92 session-lifetime
  `HashMap`s); `docs/engine/memory-budget.md`
- **Status**: NEW
- **Evidence**: the parse-cost/RSS table above, measured with
  `examples/esm_dim8_bench` under `/usr/bin/time -f %M`. `Starfield.esm`
  (1.39 GB on disk) was **not** run — extrapolating the FO76 ratio puts it near
  4 GB, and this host did not have safe headroom. That is itself the point.
- **Impact**: nothing evicts these maps; they are held for the session and
  `merge_from` accumulates them across a load order (vanilla FO4 is base + 7 DLC
  masters). `memory-budget.md` documents VRAM to the byte and has exactly one
  CPU-side section (the Starfield CDB) — the ESM index, the largest single CPU
  allocation in a normal run, is absent. Survivable on the 29 GB dev box
  (*user_hardware*); not on a 16 GB machine with a modded FO76/Starfield order.
- **Suggested Fix**: add an "ESM Index (CPU-side)" section to
  `memory-budget.md` with these measured numbers and the dominant maps. Separate
  follow-up worth scoping: most of the 92 maps (`camera_shots`, `menu_icons`,
  `voice_types`, the 30 `MinimalEsmRecord` stub maps) are `EDID`-only stubs with
  no consumer, each retaining a `String` per record.

#### ESM-2026-08-30-D8-01: the "cause not yet identified" FNV `LAND 0x00150FC0` failure is an Adler-32 mismatch on an otherwise-perfect zlib stream — The Strip's terrain tile is discarded over a checksum byte
- **Severity**: MEDIUM
- **Dimension**: Real-Data Validation
- **Record / Sub-record**: `LAND` / `DATA`, `VNML`, `VHGT`
- **Location**: `crates/plugin/src/esm/reader.rs:717-720` (the
  `ZlibDecoder … read_to_end` in `read_sub_records`); soft-fail site
  `crates/plugin/src/esm/cell/walkers.rs:1027-1044`
- **Status**: NEW — root cause for the long-standing note at `walkers.rs:1029`:
  *"At least one vanilla FNV LAND record (form `0x00150FC0`) reliably fails the
  body read on every ESM open — cause not yet identified"* (#385 / D5-F5)
- **Evidence**: the record sits at file offset 185 401 092; `data_size` 4088,
  `flags` `0x00040000` (compressed); the 4-byte prefix declares 4385 uncompressed
  bytes over 4084 compressed. `zlib.decompress` fails with
  `Error -3: incorrect data check` — an **Adler-32 trailer mismatch**, not a
  malformed stream. Re-inflating the identical payload as raw DEFLATE
  (`decompressobj(-15)` after the 2-byte zlib header) yields **exactly 4385 bytes**
  with `eof == True`, and those bytes parse cleanly as `DATA`(4) + `VNML`(3267) +
  `VHGT`(1096) — a structurally perfect LAND payload. The #3399 ceilings are not
  involved: the ratio ceiling here is 2 091 008 bytes, ~477× the declared size.
- **Which cell**: worldspace `TheStripWorldNew`, exterior grid **(-6, 26)**,
  parent CELL FormID `0x0014F622` — The Strip, visitable content, not a dev cell.
- **Impact**: one exterior tile of a major FNV location renders with no heightmap
  and no vertex normals (the flat/untextured symptom the existing comment
  predicts), visible only at `log::debug`. It is also the sole non-zero entry in
  the walker-error column across all seven masters, so it is the one place where
  "the ESM parse is clean" is not literally true.
- **Suggested Fix**: on an inflate error whose recovered output length already
  equals the validated declared size, accept the buffer and log once at `warn`
  naming the FormID — a checksum-only failure is exactly recoverable, and
  Bethesda's own loader evidently tolerates it since the tile renders in the
  shipped game. Concretely: keep the `ZlibDecoder` path and, on `Err`, retry with
  a raw-DEFLATE decoder, accepting only when the byte count matches the declared
  size. That preserves every #3399 bound. Add a regression fixture built from a
  valid stream with a corrupted trailer.

#### ESM-2026-08-30-D7-01: `merge_from` last-write-wins on `EsmIndex::game`
- **Severity**: MEDIUM
- **Dimension**: ESM→ECS Handoff
- **Location**: `crates/plugin/src/esm/records/index.rs:863`
- **Status**: **Existing: #3403** (OPEN) — re-verified unfixed this run, not
  re-filed.
- `self.game = other.game;` is unconditional, directly under a comment that names
  the hazard and defers it to this audit. The exploit path is live:
  `byroredux/src/cell_loader/load_order.rs:369-372` merges
  `EsmIndex::default()` when a plugin's record parse fails, and `GameKind`'s
  `#[default]` is `Fallout3NV`, so a failed parse of the **last** plugin
  re-labels the whole load order. Contrast `character_rules` immediately below,
  which #3384 already converted to first-non-`NONE`-wins for the same reason.

---

### LOW

#### ESM-2026-08-30-D1-01: a nested group's `sub_end` is never clamped to the parent group's end
- **Severity**: LOW · **Dimension**: Header & GRUP Walk · **Record**: —
- **Location**: `crates/plugin/src/esm/reader.rs:859-877` (`bounded_group_content_end`), every call site
- **Status**: NEW
- `bounded_group_content_end` returns `pos + (total_size - header_size)` with no
  `.min(parent_end)` and no `.min(data.len())`. A corrupt or hostile child GRUP
  declaring a `total_size` larger than its parent's remaining content makes the
  child walker consume records belonging to the parent (or to the next top-level
  group); the parent loop then exits early because `position() > end`. Not a
  memory-safety issue — every loop also tests `reader.remaining() > 0` and
  `read_record_header` returns `Err` on truncation — but the result is silent
  mis-attribution rather than a diagnosable failure. No vanilla master triggers
  it. Fix: `Some(self.group_content_end(header).min(parent_end))`, threading the
  parent end (already in scope as `end` at every call site).

#### ESM-2026-08-30-D1-02: unknown top-level GRUP labels are skipped with no telemetry
- **Severity**: LOW · **Dimension**: Header & GRUP Walk · **Record**: —
- **Location**: `crates/plugin/src/esm/records/mod.rs:472` (`_ => reader.skip_group(&group)`)
- **Status**: NEW
- The behaviour is correct (skip by declared size, continue, no per-record warn
  spam on a 5.6 M-record master), but the catch-all neither logs nor records into
  `index.skipped_unconsumed_groups` — a field that exists and is used for exactly
  one label (`PDCL`). The coverage signal in the matrix above (95 unrouted labels
  on Starfield, 89 on FO76) is unavailable at runtime and had to be computed with
  an external walker. Fix: push the label on the catch-all; cost is O(distinct
  labels).

#### ESM-2026-08-30-D2-02: Skyrim `AMMO.DATA` is 20 bytes on disk, not the 16 the decoder's comment claims
- **Severity**: LOW · **Dimension**: Sub-Record Byte Accounting · **Record**: `AMMO`/`DATA`
- **Location**: `crates/plugin/src/esm/records/items.rs:648-657`
- **Status**: NEW
- The comment reads *"Skyrim AMMO DATA (16 bytes): projectile_form(u32),
  flags(u32), damage(f32), value(u32)"*. Census over `Skyrim.esm`: **35/35** are
  **20** bytes. The decoded first 16 are correct; bytes 16..20 are an undecoded
  `f32` whose value is `0.1` in every one of the 35 records — consistent with an
  authored per-arrow weight, which `common.weight` currently leaves at `0`
  (`CommonItemFields` sets weight from no sub-record on this path). Small and
  uniform, but the comment is factually wrong about the on-disk size, which is
  the kind of stale schema note that misleads the next field addition. Fix:
  correct the comment and read the trailing `f32` behind a `remaining() >= 4`
  check so a genuine 16-byte Skyrim LE record still decodes.

#### ESM-2026-08-30-D2-03: the item `DATA` arms dispatch on `GameKind` alone with no length validation
- **Severity**: LOW · **Dimension**: Sub-Record Byte Accounting · **Record**: `WEAP`/`ARMO`/`AMMO`/`BOOK` `DATA`, `CRDT`, `DNAM`, `ENIT`
- **Location**: `crates/plugin/src/esm/records/items.rs:260, 413, 492, 558, 621, 780, 834`
- **Status**: NEW (hardening)
- These are the only fixed-layout multi-field arms in the crate with no length
  guard at all; every other decoder gates on `sub.data.len()`. They rely entirely
  on `*_or_default` leniency, which is safe against truncation-to-zero (a failed
  read does not advance the cursor) but **not** against a partial truncation
  followed by a narrower field: a 13-byte FO3/FNV `WEAP DATA` makes
  `damage = r.u16_or_default()` fail and then `clip_size = r.u8_or_default()`
  consume `damage`'s stray byte. A static scan found 31 such narrowing pairs in
  the crate; 23 sit in arms guarded to the exact struct width and are
  unreachable, 8 are in these unguarded arms. No vanilla master triggers it (all
  lengths are exact — see the Dim-2 table), so this is hardening. Adding the
  measured guards also makes a wrong-`GameKind` dispatch fail visibly instead of
  silently, which is D4-01's concern.

#### ESM-2026-08-30-D3-03: `parse_esm_with_load_order`'s docstring says the CLI is single-plugin only; it has been multi-plugin since M46.0
- **Severity**: LOW (doc-rot) · **Dimension**: FormID & Load Order · **Record**: —
- **Location**: `crates/plugin/src/esm/records/mod.rs:165-175`
- **Status**: NEW
- *"The current CLI entry point (`--esm <path>`) only wires a single plugin …
  The multi-plugin wiring is tracked as follow-up work."* `--master` is
  repeatable, `byroredux/src/cell_loader/load_order.rs:361` builds a real
  `FormIdRemap` per plugin and `:369` passes it to this very function, and
  `CLAUDE.md`'s usage section documents
  `--master Skyrim.esm --master Update.esm --esm Dawnguard.esm`. The stale note
  is precisely what makes a reader dismiss the remap as not-yet-live — the
  reasoning that leaves D3-01/D3-02 unfixed.

#### ESM-2026-08-30-D4-01: `GameKind::from_header`'s "latent" note is stale — the FO3↔FO4 schema split it says does not exist has since landed in three arms
- **Severity**: LOW (doc-rot) · **Dimension**: Record Schema Dispatch · **Record**: `ARMO`/`WEAP`/`BOOK` `DATA`
- **Location**: `crates/plugin/src/esm/reader.rs:183-188`
- **Status**: NEW
- The comment states, present tense: *"Latent because WEAP/ARMO/AMMO DATA arms in
  items.rs bucket Fallout4 with Fallout3NV/Oblivion."* That bucketing is gone:
  `items.rs:507` gives `ARMO DATA` a dedicated `GameKind::Fallout4` arm that
  **swaps** the last two fields (`value, weight, health` vs FO3NV's
  `value, health, weight`) at the same 12-byte length; `items.rs:296` makes
  `WEAP DATA` an empty FO4 arm (confirmed: 0 WEAP `DATA` in `Fallout4.esm`);
  `items.rs:838` gives `BOOK` its own 8-byte FO4 arm. No live mis-band exists —
  every band was re-verified end-to-end against the sampled values this run —
  but the comment is the reasoning a future band edit is checked against, and it
  now understates the cost: a FO3↔FO4 mis-band would silently swap armor weight
  and health and zero every weapon's value/weight/damage.

#### ESM-2026-08-30-D5-01: a REFR's persistent / temporary / visible-distant group membership is discarded
- **Severity**: LOW · **Dimension**: CELL / WRLD Walkers · **Record**: `CELL` child GRUPs 8/9/10 — `REFR`/`ACHR`/`ACRE`
- **Location**: `crates/plugin/src/esm/cell/walkers.rs:141-159`, `:672-700`;
  `crates/plugin/src/esm/cell/mod.rs:372` (`PlacedRef`)
- **Status**: NEW
- `parse_cell_group_inner` matches `6 | 8 | 9` with one arm and passes the same
  `&mut refs` for all of them; `parse_wrld_children_inner` does the same;
  `parse_refr_group_inner` recurses into any nested group with no record of which
  one it entered. `PlacedRef` has no field for it — `is_persistent` does not exist
  anywhere in the workspace. No consumer is wrong today (the engine's only
  "persistent" concept is the worldspace-level persistent CELL,
  `streaming.rs:608`), which is why this is LOW; the gap is structural, since the
  persistent/temporary split is what a streaming system keeps resident across
  cell transitions and what a save system must distinguish on restore, and the
  information is thrown away at parse time. Fix: carry a `group_type: u8` on
  `PlacedRef`, threaded through `parse_refr_group`'s recursion the same way
  `depth` already is.

#### ESM-2026-08-30-D7-03: ESM parse cost is 1.2–3.4 s per master, on the critical path of the first cell load, with no owner
- **Severity**: LOW · **Dimension**: ESM→ECS Handoff · **Record**: —
- **Location**: `crates/plugin/src/esm/records/mod.rs::parse_esm_with_load_order`
- **Status**: NEW
- See the cost table. A vanilla FO4 load order (base + 7 DLC masters) is ~2.6 s of
  single-threaded ESM parsing before the first cell can load; FO76 alone is
  3.4 s. `/audit-performance` Dim 8 owns NIF parse cost; nothing owns this. The
  walk is inherently sequential per plugin, but plugins are independent up to the
  `merge_from`, and each plugin's `FormIdRemap` is computed from its header alone
  *before* the record walk — nothing in the design prevents parsing them in
  parallel.

---

### INFO

#### ESM-2026-08-30-D8-02: `FileHeader::record_count` is not a completeness gate — its meaning differs by game
- **Severity**: LOW/INFO · **Dimension**: Real-Data Validation · **Record**: `TES4`/`HEDR`
- **Location**: `crates/plugin/src/esm/reader.rs::read_file_header`
- **Status**: NEW
- Measured with the full file walked to EOF and zero walker errors in every case:
  - Oblivion / FO3 / FNV / Skyrim: `HEDR.count == records + groups` **exactly**
    (1 252 095 / 808 699 / 542 016 / 920 181 — all four match to the unit).
  - Starfield: `HEDR.count == records + 1` (3 829 247 vs 3 829 246) — records
    only, groups excluded.
  - FO4 and FO76: neither formula, and both short by **exactly 80 196**
    (1 741 853 vs 1 661 657; 5 839 497 vs 5 759 301). The identical delta across
    two unrelated files is unexplained and recorded here for whoever next reaches
    for this field.
- Today it is used only in a `log::info!`, so nothing is wrong. Recorded so it is
  never promoted into a parse-completeness assertion — on FO4/FO76 that assertion
  would fire on clean vanilla data.

---

## Verified Clean (no findings)

**Dimension 6 — Localized Strings** produced nothing, and it is worth saying why
rather than leaving a blank:

- The lstring branch is **flag-driven** (`is_localized_plugin()` from the TES4
  `0x80` flag, installed via the RAII `LocalizedPluginGuard`), never a
  "looks like a small integer" heuristic; the `data.len() == 4` test only
  disambiguates *within* an already-localized plugin.
- All 37 localizable call sites route through `read_lstring_or_zstring` —
  `FULL`, `DESC` (RACE/CLAS/MGEF/SPEL/EXPL/IMOD/MESG), `NAM1` (INFO response),
  QUST log/objective text, BPTD part names. Every remaining bare
  `read_zstring(&sub.data)` was checked and decodes a non-localizable field
  (paths, editor IDs, `SCTX` source, alias names, MSWP material names).
- Per-kind parse form is extension-driven, not content-sniffed, and
  `StringsTable::parse` scans for the NUL rather than trusting the length
  prefix, so a prefix/terminator disagreement cannot shift the decode.
- Header/bounds hardening is complete: <8-byte header rejected,
  `checked_mul`/`checked_add` on directory and blob offsets, file-length check
  before slicing, per-entry skip on an out-of-blob offset. No panic path.
- Missing files degrade to `None` → `<lstring 0xNNNNNNNN>` placeholder at one
  diagnosable site, not an empty `String` at 40 call sites. No per-form warn spam.
- **Disproved hypothesis, recorded so it is not re-derived**:
  `StringTableSet::resolve` is type-agnostic (tries `.STRINGS`, then
  `.DLSTRINGS`, then `.ILSTRINGS`, first hit wins) whereas the game picks the
  file from the sub-record's declared string type. Extracted
  `strings\skyrim_english.{strings,dlstrings,ilstrings}` from
  `Skyrim - Interface.bsa` (30 301 / 2 686 / 34 427 entries) and intersected the
  ID sets: **overlap is 0 in all three pairs**. The lstring ID space is global
  across the three files, so first-match resolution is correct.

Also verified clean, with no finding:

- **GRUP nesting bound (#3237/#3503)** — universal and correctly threaded across
  all 13 recursive walkers (`grup_walker` ×4, `cell/walkers` ×2, `cell/wrld` ×1,
  `cell/support` ×7); every one both calls `bounded_group_content_end(…, depth, …)`
  **and** passes `depth + 1` into its `_inner` recursion. The only raw
  `group_content_end` production call sites are the two documented non-recursive
  top-level entries (`records/mod.rs:276`, `cell/wrld.rs:28`). Mutual recursion
  cannot reset the counter: `parse_refr_group` never calls back into the cell/wrld
  walkers.
- **Compressed-record bounds (#3399)** — `ensure!(data_size >= 4)` precedes the
  subtraction; ceiling `min(64 MiB, max(64 KiB, compressed_len × 512))` checked
  *before* `Vec::with_capacity`; `take(declared + 1)` plus a post-check. Still in
  lockstep with `byroredux_bsa::safety::inflate_bounded`.
- **`GameKind` bands** re-verified end-to-end against every sampled value,
  including FO76 = 266.0 re-read from disk. The #439 FO3↔FO4 inversion is absent.
- **`FormIdRemap`** — all four arms intact, ESL masks 12-bit on both sides, the
  standalone-no-masters arm still passes through rather than clamping (the #1308
  rationale is intact), missing-master produces a loud actionable `Err`.
- **No unguarded raw slicing** anywhere in the record decoders: a static scan of
  every match arm's maximum `sub.data[n]` index against its guaranteed length
  found **zero** sites.
- **`rgb_color` vs `rgba_color`** are both 4-byte reads, so the checklist's
  "swapped pair shifts every later field" hazard is structurally impossible here.
- **WATR `DATA` 0..28 prefix** and **CELL `XCLW` tri-state** hold; not
  re-litigated.
- **`parse_race` TES5 `DATA`** offsets 36/40/44 are produced by sequential reads
  under a `len() == 128 | 164` guard, so a future field lands at 48 by
  construction.
- **`CTDA`** FormID parameters go through `push_ctda` → `remap_condition_form_ids`;
  the raw `parse_ctda` has no non-test caller outside that wrapper.
- **Redux-native tier** (`manifest`/`record`/`datastore`/`resolver`) and
  `legacy/mod.rs`: compile clean, struct shapes and lifecycle match
  `docs/engine/plugin-loading.md` exactly, no stale markers. Unused-but-documented
  is by design and not reported.
- **Resolver identity**: the live path is
  `resolve_entity_by_global_form_id` (`byroredux/src/systems/travel.rs:103`), not
  `World::find_by_form_id`.
- **`equip.rs`** biped-slot constants carry 8 `wbDefinitions*` citations; no
  uncited slot bits.

---

## Cross-Audit Pointers

- **`/audit-skyrim` Dim 1 / `/audit-fo4` Dim 1** — the `RACE` (90.7% / 92.2%)
  and `NPC_` (57.4% Skyrim) unknown-sub-record shares are the face/tint model
  (`TINC`/`TINV`/`TINI`/`TIAS`/`TIRS`, 195 k occurrences on Skyrim). The parser
  tier only measures the gap; whether it matters is a per-game call.
- **`/audit-fo4`** — D3-01 is fixed in this crate but its *symptom* is FO4 NPC
  outfit rendering; the `docs/smoke-tests/m41-equip.sh` harness should grow a
  DLC-master case, which would have caught it.
- **`/audit-fnv`** — D8-01's terrain hole is at `TheStripWorldNew` (-6, 26); a
  visual check there is the confirmation.
- **`/audit-scripting` Dim 7** — `VMAD` translation was not audited here.
  `NOTE.SNAM` (`topic_form`, D3-02) feeds `crates/scripting/src/dialogue.rs:349`.
- **`/audit-character` Dim 4/5** — `actor_value_derive.rs` and the `RaceRecord`
  starting-pool floats were cross-referenced, not re-audited; only their byte
  offsets were checked (they hold).
- **`/audit-physics`** — D5-01 (persistent/temporary REFR membership) is
  placement metadata a collision/streaming consumer would want.
- **`/audit-performance`** — D7-03 (ESM parse cost) has no owner; consider
  adopting it alongside NIF parse cost in Dim 8.

---

*Report path: `docs/audits/AUDIT_ESM_2026-08-30.md`. Publish with
`/audit-publish docs/audits/AUDIT_ESM_2026-08-30.md` — domain label
`esm-plugin`, plus `game:fnv` (D8-01), `game:fo4` (D3-01), `game:skyrim`
(D2-01, D2-02); `doc-rot` for D3-03 / D4-01; `memory` for D7-02.*
