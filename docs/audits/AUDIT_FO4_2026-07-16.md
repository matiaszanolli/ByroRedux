# Fallout 4 Compatibility Audit — 2026-07-16

- **HEAD**: `c3e09bb5` (branch `main`) · **Prior FO4 audit baseline**: `docs/audits/AUDIT_FO4_2026-07-13.md` (HEAD `21b79aee`, baseline `1c26bc25`)
- **Dimensions run**: all 9 · **Method**: code/shader audit + headless real-data validation (BA2 parse, CSG decode, ESM parse, NIF parse-rate re-run). No windowed engine launch (standing no-parallel-launch instruction).
- **Reference data**: `/mnt/data/SteamLibrary/steamapps/common/Fallout 4/Data/` (base BA2s + DLC `.csg` geometry present).
- **Dedup**: `gh issue list --repo matiaszanolli/ByroRedux --limit 200` → `/tmp/audit/issues.json`, cross-checked per dimension against issue titles/labels and against all 15 prior `docs/audits/AUDIT_FO4_*.md` reports.

---

## Executive Summary

**FO4 remains in strong shape — zero CRITICAL, zero HIGH, zero MEDIUM, one new LOW, zero regressions.** Every regression guard pinned by the prior 2026-07-13 audit re-verified intact against current code, and the two headless measurables match the ROADMAP baseline exactly:

- **NIF parse rate: 100.00% / 159,866** (`Fallout4 - Meshes.ba2` 34,995 + `Fallout4 - MeshesExtra.ba2` 124,871, 0 truncated, 0 failed) — re-measured live in this pass, matches baseline exactly.
- **ESM: SCOL 2,617 records / 40,330 placements, PKIN 872/872, MSWP 2,537, TXST 380** — re-run against live `Fallout4.esm`, matches baseline exactly.

Only three source-level commits touched FO4-relevant code in the three days since the prior audit (`21b79aee..c3e09bb5`), and both are direct fixes for that audit's own findings:
- `6072bb7a` — Fix #1986 (FO4-D1-01: CSG short-chunk rejection). **Verified landed and regression-tested.**
- `441186fb` — Fix #1985 (FO4-D5-01, prior MEDIUM: FO4 shader-flag alpha-test threshold seed). **Verified landed and regression-tested** — the common case (no `NiAlphaProperty`) is fixed. This pass found one narrow residual sub-case the fix didn't cover (see below).

All the recently-churned FO4 surfaces continue to hold: the **M49 CSG precombine pipeline** (CSG reader → decode → spawn → owning-plugin resolution → alpha-blend restore → LOD/texture), the **BGSM/BGEM parser + metalness-from-saturation** (#1476), the **exhaustive BA2 version match**, **half-float BSTriShape + BSSubIndexTriShape + FO4 collision**, **SCOL/PKIN expansion + BSConnectPoint attach graph** (183/183 `cell_loader` tests), and the **single-boundary NIFAL translate**.

Findings: **1 LOW NEW** (a residual edge of the closed #1985 fix, not itself a regression), plus two prior LOW findings reconfirmed still open/accurate (not re-filed).

| Dimension | CRIT | HIGH | MED | LOW | Verdict |
|-----------|:---:|:---:|:---:|:---:|---------|
| 1 · M49 Precombines (CSG) | 0 | 0 | 0 | 0 | Clean — #1986 fix confirmed landed |
| 2 · BGSM/BGEM + metalness | 0 | 0 | 0 | 0 | Clean — #1476 intact |
| 3 · BA2 reader | 0 | 0 | 0 | 0 | Clean — v8 DX10 case newly verified |
| 4 · NIF BSVER 130 + collision | 0 | 0 | 0 | 0 | Clean |
| 5 · FO4 shader flags + PBR | 0 | 0 | 0 | 1 | #1985 fix confirmed; 1 residual sub-case |
| 6 · ESM arch records | 0 | 0 | 0 | 0 | Clean |
| 7 · NIFAL translation | 0 | 0 | 0 | 0 | Clean |
| 8 · Cell load / SCOL-PKIN | 0 | 0 | 0 | 0 | Clean (183/183 tests) |
| 9 · Real-data / forward scope | 0 | 0 | 0 | 0 | Clean (100% parse, exact match) |
| **NEW actionable** | **0** | **0** | **0** | **1** | |

---

## Baseline Comparison Table

Compared against the ROADMAP FO4 compat-matrix row and the 2026-07-13 audit's re-measurement.

| Metric | 2026-07-13 baseline | Observed (2026-07-16) | Status |
|--------|------------------|----------|--------|
| NIF parse rate (both base mesh archives) | 100.00% / 159,866 | 100.00% / 159,866 (0 trunc, 0 fail) | ✅ MATCH |
| — `Fallout4 - Meshes.ba2` | 34,995 | 34,995 clean | ✅ MATCH |
| — `Fallout4 - MeshesExtra.ba2` | 124,871 | 124,871 clean | ✅ MATCH |
| SCOL records / placements | 2,617 / 40,330 | 2,617 / 40,330 | ✅ MATCH |
| PKIN | 872 / 872 | 872 / 872 | ✅ MATCH |
| MSWP / TXST | — | 2,537 / 380 | ℹ️ (newly recorded, no prior baseline row) |
| `cell_loader` test suite | 20/20 (targeted) | 183/183 (full module) | ✅ green, broader run |

---

## Dimension Findings (by severity)

### LOW

#### FO4-D5-01(residual) — FO4 shader-flag alpha-test still inert when a blend-only/opaque `NiAlphaProperty` was consumed · NEW
- **Severity**: LOW
- **Dimension**: 5 · FO4 Shader Flags & BGSM PBR Routing
- **Location**: `crates/nif/src/import/material/walker.rs:346-351`
- **Status**: NEW — a narrow residual edge of the closed #1985 fix (which resolved the prior MEDIUM FO4-D5-01), not a regression of it
- **Description**: The FO4 `F4SF2::Alpha_Test` consumption block sets `info.alpha_test = true` unconditionally, but seeds the usable cutout threshold (`128.0/255.0`) only when `!info.alpha_property_consumed`. #1985 closed the common case (no `NiAlphaProperty` present at all). It does not cover the case where a `NiAlphaProperty` *was* consumed but authored no test threshold — i.e. blend-only (`flags & 0x200 == 0`) or explicit-opaque (`flags == 0`). `apply_alpha_flags` (`crates/nif/src/import/material/mod.rs:1091-1093`) only writes a threshold when the property's own test bit is set, and always sets `alpha_property_consumed = true` (`mod.rs:1107`) regardless.
- **Evidence**: For a FO4 inline NIF whose `NiAlphaProperty` has `flags=0` (explicit opaque, processed first at `walker.rs:118`): result is `alpha_test=false`, `alpha_threshold=0.0`, `alpha_property_consumed=true`. The FO4 flag block then sets `alpha_test=true` but skips the threshold seed because the guard (`!alpha_property_consumed`) is false. Net: `alpha_test=true` with `alpha_threshold=0.0`. `triangle.frag` gates the discard on `aThresh > 0.0`, so the cutout is inert and the surface renders solid — the same symptom class as the original #1985 finding, in a sub-case the fix didn't reach. (For a blend-only property the mesh still alpha-blends, so the visual miss there is cutout→blend rather than cutout→opaque.)
- **Impact**: A FO4 mesh that signals cutout via the shader flag while also binding a non-test `NiAlphaProperty` renders without the discard. This is contradictory authoring (cutout intent split across a shader flag and an opaque/blend alpha property) with no known vanilla-FO4 trigger — vanilla cutout content either ships a proper `NiAlphaProperty` test bit or a BGSM that supplies the threshold via `merge_bgsm_into_mesh`. Blast radius is effectively hypothetical/modded content only; hence LOW.
- **Related**: Prior FO4-D5-01 (#1985, closed — this is a residual, not a reopen); #1592 (flag consumption); #1201/#1202 (explicit-alpha-property-wins precedence).
- **Suggested Fix**: In the `F4SF2::Alpha_Test` arm, seed `128.0/255.0` whenever the resolved `alpha_threshold == 0.0` regardless of `alpha_property_consumed` (not just when no property was consumed at all). Alternatively, apply the #1201/#1202 principle and have an explicit-opaque `NiAlphaProperty { flags: 0 }` suppress the shader-flag alpha-test entirely (defer to the property's own intent). Either way, add a regression case to `fo4_shader_flag_tests` covering a blend-only/opaque `NiAlphaProperty` combined with the `Alpha_Test` shader flag.

### Existing issues reconfirmed (not re-filed)

- **#1987** (FO4-D2-01, OPEN, LOW) — the #1476 metalness-from-saturation formula (`byroredux/src/asset_provider/material.rs:728-748`) remains inlined in `merge_bgsm_into_mesh` rather than extracted into a standalone, unit-testable helper the way `conductor_diffuse_tint` (#1591) and `bgsm_blend_to_gamebryo` (#1823) are. Re-verified: formula is still correct, still untested in isolation. Defense-in-depth gap only.
- **#1761** (OPEN, LOW) — `Dx10Chunk::start_mip` is read (monotonicity check), but its `#[allow(dead_code)]` is redundant; `end_mip` genuinely unused beyond an M40-streaming placeholder comment. Re-verified accurate against current `ba2.rs`.

### Findings closed since the prior audit (verified landed)

- **#1986** (was FO4-D1-01, LOW) — CSG middle-chunk length now pinned to exactly `CSG_CHUNK_SIZE` for non-final chunks (`crates/bsa/src/csg.rs:247-261`), with regression test `rejects_short_non_final_chunk` (`csg.rs:356-372`). Confirmed landed exactly as suggested.
- **#1985** (was FO4-D5-01, MEDIUM) — FO4 shader-flag-only alpha-test now seeds Bethesda's conventional `128.0/255.0` threshold for the common no-`NiAlphaProperty` case (`walker.rs:346-351`), pinned by `fo4_shader_flag_tests::fo4_alpha_test_flag_sets_field`. Confirmed landed. See the NEW LOW finding above for the one sub-case the fix didn't reach.

### Informational — FO76-scope, not FO4 defects (carried forward, not re-verified this pass)

- **FO4-D4-INFO-01**: `bs_tri_shape.rs` 24-byte `Bound Min Max` skip gated on exact `bsver == FO76` (155); zero FO4 impact.
- **FO4-D5-L01**: `walker.rs` per-`shader_type` texture-set slot match keys on raw `shader_type`; latent-wrong only for FO76's `BSShaderType155` remap. Zero FO4 impact.

Both remain FO76-audit-backlog items, not FO4 issues.

---

## BGSM Consumption Table

How each BGSM/BGEM field flows: parsed by `crates/bgsm/` → merged into `ImportedMesh` by `merge_bgsm_into_mesh` → surfaced on canonical `Material` post-NIFAL translate. **Verified intact, unchanged since 2026-07-13.**

| BGSM field | Merged into `ImportedMesh` | Surfaced on canonical `Material` |
|------------|----------------------------|----------------------------------|
| Diffuse/albedo tint | albedo/diffuse tint | base color |
| Specular color | metalness via saturation (`(max−min)/max`, #1476), NOT luminance (luminance confined to `leaf.pbr==true`) | `metalness` (resolve-once f32) |
| Smoothness | `roughness_override = (1.0 − smoothness).clamp(0.04,1.0)`, inverted once at parse | `roughness` (resolve-once f32) |
| Emissive | emissive color/mult | emissive |
| Translucency suite (#1147) | translucency fields → `BGSM_TRANSLUCENCY` flag | `MAT_FLAG_*` via `pack_bgsm_material_flags` |
| Model-space normals bit | `model_space_normals` → `BGSM_MODEL_SPACE_NORMALS` | `MAT_FLAG_MODEL_SPACE_NORMALS` |
| PBR flag | `is_pbr = true` unconditionally for all resolved BGSM/BGEM (#1352) | `MAT_FLAG_PBR_BSDF` (Disney lobe) — routes through all three FO4 spawn paths (REFR, loose-NIF, precombined) |
| src/dst blend | `bgsm_blend_to_gamebryo` (plain `u8` cast, #1823) — Standard (6,7)/Additive (6,0)/Mult (4,1) | VK blend factors verbatim |
| Texture-slot paths | texture/normal/etc. slots | texture bindings |
| Template refs | cycle-aware `resolve` (visited-set + `DEPTH_LIMIT=16`, #1148) | resolved before merge |

---

## Regression Guard List (verified still correct, 2026-07-16)

**M49 Precombines (D1)** — vertex stride (`psg_vertex_stride`, half4-on-disk vs full-precision runtime), Y-up composition (one `cell_origin` application at the placement-root, per-mesh local transform composed as a normal ECS child — no double-apply), graceful CSG-missing fallback (0 spawns, no panic), owning-plugin CSG + `_oc.nif` subdir resolution (#1590), `pc_spawned > 0` absorption gate (interior/exterior symmetric), single-LOD selection (`max_by_key` over tri counts, `a30c088a`), owning-REFR shape-slot texturing (`2900de70`), opaque-architecture alpha-blend restore (`887aae52` — snapshot/restore of `has_alpha`/blend-mode around the BGSM merge), exterior `cell_grid_to_world_yup` origin. **#1986 short-chunk fix confirmed landed with regression test.**

**BGSM/BGEM (D2)** — metalness-from-saturation (#1476, luminance confined to `leaf.pbr==true` — single grep hit confirms no reversion), template cycle-break (#1148, `DEPTH_LIMIT=16` + visited-set, 6 dedicated tests), blend pass-through (#1651/#1823, `bgsm_blend_to_gamebryo` = plain `as u8`, no `gl_to_gamebryo_blend` symbol anywhere in the crate), magic-over-extension dispatch with logged mismatch (#758), single-source merge strictly before all four `translate_material` call sites, smoothness→roughness inverted exactly once (the separate `normal_alpha_spec_roughness` path, #1480, is a distinct narrowly-gated Skyrim-convention derivation, not a double-invert).

**BA2 reader (D3)** — exhaustive `match {1,2,3,7,8}` with unknown-major bail (#811, regression test `unknown_version_rejected` pins `version=5` failing at `open()`); GNRL byte-exact (35k+ NIF brute-force zero errors, manually-verified PEX magic `0xFA57C0DE`); DX10 DDS reconstruction byte-correct for BC1-BC7 + uncompressed formats, cubemap `arraySize=6`, mip assembly; **v8 DX10 case (`Fallout4 - TexturesPatch.ba2`) newly verified this pass** — previously untested, now confirmed correct DDS magic/headers. All three FO4-relevant BTDX majors (1, 7, 8) confirmed present in the live install via on-disk header bytes, all force zlib correctly.

**NIF BSVER 130 (D4)** — `VF_FULL_PRECISION` half-default, IEEE-754 binary16 decode (denormals/NaN, 16 edge-case asserts), `BSSubIndexTriShape` segment walk (SSE + FO4+ both fully decoded) + packed skin layout (12-byte, renormalised weights), DLC trailing fields gated `130..=139` + Next-Gen 132/139 dispatch inside the band, #1242 `FO4_ENV_SCALE→FO4_DLC_UPPER=140` rename complete (zero stale refs anywhere in `crates/nif`/`crates/plugin`), both `BhkMultiSphereShape` + `BhkConvexListShape` still translate to `CollisionShape::Compound` (no drop to the unsupported-log path).

**FO4 shader flags (D5)** — u32 flag-pair decode as two separate fields (`bsver<=130` only, gap at 131, CRC arrays `>=132`), FO4 bit positions pinned vs nif.xml with compile-time cross-game assertions, #1592 MSN (F1 bit12) + Alpha_Test (F2 bit25) OR'd into `MaterialInfo` (typed-word + FO76+ CRC checked), #1733 Glow_Map correctly NOT gated, BGSM Name/`root_material_path` stopcond unconditional, #563 SkinTint/HairTint slot routing skips slots 4/5, PBR-not-Lambert (`is_pbr` unconditional on resolved BGSM, #1352), #1244 `BSShaderPropertyBaseOnly` — FO4 always uses the full `BSLightingShaderProperty` so no fallback-mesh gap. **#1985 threshold-seed fix confirmed landed for the common case; one residual sub-case newly found (LOW, above).**

**ESM arch records (D6)** — TXST DODT (36-byte decal-data → `DecalData`, correct field offsets) + DNAM flags (#813/#814, FO4 2-byte vs Skyrim 1-byte), category exposure of all 5 FO4-architecture maps (#817, `categories().len() == 95` pinned), SCOL/MOVS/PKIN/MSWP wire layouts + era-gate dispatch, `cargo check -p byroredux-plugin --tests` clean (no `unreachable_patterns`), real-ESM harness re-run against live `Fallout4.esm` (SCOL 2617/40330, PKIN 872, MSWP 2537, TXST 380 — exact match), MSWP `XMSP` → REFR wiring traced end-to-end into `cell_loader/refr.rs`, not dead data.

**NIFAL translation (D7)** — single `translate_material` boundary; all three FO4 spawn paths (cell-REFR, loose-NIF, **and precombined/CSG**) route through it — only `cornell.rs` synthetic literals and `#[cfg(test)]` helpers bypass it, both legitimate. `pack_bgsm_material_flags` sets `PBR_BSDF` for every `mesh.is_pbr` (unconditional on all `from_bgsm` content, not just the rare authored-`pbr` case). Resolve-once f32 metalness/roughness seeded from overrides or NaN, `resolve_pbr` idempotent, render path (`render/static_meshes.rs`) reads resolved scalars directly with no draw-time classifier anywhere in the render/upload path. Glass classified after `resolve_pbr` (forced roughness wins).

**Cell load / SCOL-PKIN (D8)** — transform composition `outer_rot*(outer_scale*local) + outer_pos` confirmed against a non-identity-rotation regression test, `MAX_PKIN_DEPTH=4` shared across all 3 recursion arms with cycle-termination tests, first-expander-wins dispatch (`expand_pkin.unwrap_or_else(expand_scol)`), `absorbed_refs` double-draw de-dup gate runs strictly before expansion on both interior and exterior (Phase-3a) paths, BSConnectPoint attach-graph materialized per-synthetic-child via `stamp_attach_components` (called once per `spawn_placed_instances` invocation, correct per-entity data, no sibling cross-contamination). **183/183 `cell_loader::` tests pass** (broader than the prior audit's 20/20 targeted run).

**Real-data (D9)** — 100.00%/159,866 parse re-measured live this pass, exact match to baseline; `byroredux-nif` unit suite 12/12 green; forward-scope items independently re-verified as genuinely honest: `_precomb.nif` collision and `.uvd` occlusion explicitly documented as deferred with working fallbacks, MOVS explicitly record-parses-only with a doc comment stating no physics wiring exists, `BSBehaviorGraphExtraData` parsed and stored but confirmed via repo-wide grep to have **zero consumers** outside `crates/nif/src/blocks/` — genuinely inert, not a misleading half-implementation.

---

## Forward Scope Chain (do NOT list CSG/BGSM/SCOL as pending — all shipped)

Precombine **collision** (`_precomb.nif`/`_oc.nif`) + `.uvd` occlusion/previs volumes → **MOVS physics runtime** (record parses; Havok movement unwired) → deeper cell coverage (LIGH power state / CONT leveled items / NPC_ face morph) → `BSBehaviorGraphExtraData` driving (currently parsed-and-stored, zero consumers). The BGSM parser, SCOL/PKIN expansion, and the M49 CSG geometry pipeline are all **shipped and exercised** — confirmed again this pass across dimensions 1, 2, 6, 7, 8.

---

## Suggested next step

`/audit-publish docs/audits/AUDIT_FO4_2026-07-16.md` — files the one NEW LOW (FO4-D5-01 residual, `walker.rs:346-351`). No other actionable findings this pass.
