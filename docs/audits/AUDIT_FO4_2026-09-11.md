# Fallout 4 Compatibility Audit — 2026-09-11

**Scope**: NIF (BSVER 130 + Next-Gen), BA2 (GNRL/DX10), BGSM/BGEM external
materials, M49 precombined geometry (CSG), ESM architecture records
(SCOL/MOVS/PKIN/TXST), NIFAL canonical material translation, FO4 cell-load
end-to-end (SCOL/PKIN expansion + BSConnectPoint), and real-data validation.
9 dimensions, run per `.claude/commands/audit-fo4/SKILL.md`.

**Dedup baseline**: `gh issue list --repo matiaszanolli/ByroRedux --limit 200`
snapshot at session start (56 open issues at time of dedup; each dimension
additionally checked `issues_all.json` where available).

## Executive Summary

FO4 remains one of the most complete game paths in the engine. All of the
"shipped" subsystems named in the skill's baseline — NIF BSVER 130/half-float
geometry, the exhaustive BA2 GNRL+DX10 reader, the BGSM/BGEM external
material parser (including the #1476 metalness-from-chromaticity fix), SCOL/
PKIN prefab expansion, and the M49 CSG precombine pipeline — were
re-verified against current code and **hold with no regressions**. Real-data
validation re-measured the NIF parse rate at **235,082 / 235,082 (100.00%
clean, 100.00% recoverable)** across all 8 mesh-bearing archives (2 base +
6 DLC `Main.ba2`s), an exact match to the ROADMAP-cited figure with no drift.

The audit surfaced **one real HIGH-severity functional bug**: the FO4
precombined-geometry LOD-band selection (`select_finest_lod`) picks a single
LOD band as if the three bands were alternative triangulations of one
surface, when in fact (confirmed empirically against 28,190 real
shared-geometry objects) the three bands are vertex-disjoint sub-meshes whose
union is the full-detail model — matching how NifSkope's own renderer treats
the identical `BSMeshLODTriShape` construct (additive fall-through, not
selection). This silently drops up to 58% of an individual precombined
object's triangles and averages 12.45% of triangles dropped across the FO4
precombine corpus, with no log, no test, and no visible symptom beyond
missing geometry in baked architecture.

Three MEDIUM findings round out the substantive gaps: a shader-flags-parser
gate that was unified too broadly for two adjacent (non-FO4) block types
(latent, not live on retail FO4 content); the FO4/Skyrim baked `.bto` object
LOD path routing through the texture-only NIFAL boundary function while
holding a full `ImportedMaterial` it partially reads but does not translate
(loses alpha-test/two-sided/PBR routing on distant LOD); and placed FO4
containers never expanding leveled-list (`LVLI`) loot entries the way NPC
inventories already do, producing unresolvable phantom items in any
container using a leveled loot table. The remaining six findings are LOW —
five are doc-rot (stale status lines, wrong sub-record citations) and one is
a latent bit-mask omission with no live effect today.

**Forward scope** (confirmed still open, correctly documented, not newly
discovered): `_precomb.nif`/`_physics.nif` collision (blocked on Havok
`hknp` mesh-shape decode), `.uvd` occlusion-volume payload decode, MOVS
physics runtime, LIGH power-state. FaceGen NIF truncation tail is
**confirmed gone** (0 truncated, re-measured). NPC_ face morph is **not** a
gap for FO4 specifically — FO4 uses the working pre-baked FaceGeom path, not
the runtime-morph path the general checklist framing implied. Do **not**
re-list the BGSM parser, SCOL/PKIN expansion, or the M49 CSG pipeline as
pending — all three are shipped and re-confirmed this session.

## Severity Summary

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 1 |
| MEDIUM | 3 |
| LOW | 6 |
| **Total** | **10** |

## Dimension Results Overview

| # | Dimension | Checklist items HOLD | Findings |
|---|---|---|---|
| 1 | M49 Precombined Geometry (CSG + Decode + Spawn) | 12/12 | 1 LOW (doc-rot) |
| 2 | BGSM/BGEM Consumption + Metalness-from-Chromaticity | 10/10 | 0 |
| 3 | BA2 Reader (GNRL + DX10) | 4/4 | 0 |
| 4 | NIF BSVER 130 + Half-Float + FO4 Collision | 5/5 | 1 HIGH, 2 LOW |
| 5 | Shader Flags & BGSM PBR Routing | 6/6 | 1 MEDIUM |
| 6 | ESM Architecture Records (SCOL/MOVS/PKIN/TXST) | 6/6 | 2 LOW (doc-rot) |
| 7 | NIFAL Canonical Material Translation (FO4) | 4/4 | 1 MEDIUM, 1 LOW |
| 8 | FO4 Cell Load End-to-End (SCOL/PKIN Expansion) | 4/4 | 0 |
| 9 | Real-Data Validation + Forward Scope | — | 1 MEDIUM (new) |

All regression-guard checklist items across all 9 dimensions **HOLD** — no
prior fix (#811, #1147, #1148, #1180, #1182, #1188, #1242, #1244, #1352,
#1476, #1592, #1594, #1651/#1823, #2369, #2609, #2700, #2701, #3400, #3412,
#3466, #3510, #3637, #3639, #3641, #3758, #3845, #3905, and others cited
per-dimension) has regressed.

---

## Dimension Findings

### Dimension 1 — M49 Precombined Geometry

#### FO4-D1-01: `fo4-csg-format.md` "Implementation status" section contradicts its own "Reading an object" section on BSCRC32
- **Severity**: LOW
- **Location**: `docs/engine/fo4-csg-format.md:110-124` vs `:213-214`
- **Status**: NEW
- **Description**: The doc's "Reading an object" section fully documents the
  `BSCRC32`/`csg_name_hash` algorithm and states plainly that the cell's
  owning plugin is *not* a reliable substitute for resolving the `.csg`
  blob. The "Implementation status" section near the bottom still says the
  hash is "not yet reproduced" and that owning-plugin resolution is used
  "meanwhile" — the pre-#2369 state. `e9df743f` ("EX-15 (#2369): route FO4
  precombines by their own CSG name hash") landed after `d6bf8437` (#1590)
  and the hash-based resolution has been fully implemented, tested against
  real Far Harbor rebake data, and in production since.
- **Impact**: Low — self-correcting for a careful reader, but a contributor
  skimming only the status section could try to "fix" already-fixed #2369
  by reintroducing owning-plugin-only resolution.
- **Related**: #1590, #2369
- **Suggested Fix**: Update the status bullet to state the BSCRC32 hash is
  implemented and resolves the `.csg` blob per-object (#2369), while the
  `_oc.nif` filename path separately keys off the owning plugin (#1590).

### Dimension 2 — BGSM/BGEM Consumption

No findings. All 10 checklist items HOLD, including the #1476
metalness-from-chromaticity fix, the narrowed `&mut ImportedMaterial` merge
signature, the two disjoint neutral-roughness fallback arms (#3639/#3905),
archive last-listed-wins precedence (#3637), magic-vs-extension
reconciliation (#758), cycle-aware template resolve (#1148), and the
blend-factor pass-through with no 0↔1 swap (#1823).

### Dimension 3 — BA2 Reader (GNRL + DX10)

No findings. Exhaustive version dispatch (#811) confirmed via both static
read and the crate's own `unknown_version_rejected` test; GNRL mesh +
compiled-script extraction and DX10 DDS/BC1-7 reconstruction verified live
against the installed FO4 Steam library (including the v8-DX10
`TexturesPatch.ba2` edge case the module doc calls out); v1/v7/v8 all force
zlib as a single match arm.

### Dimension 4 — NIF BSVER 130 + Half-Float + FO4 Collision

#### FO4-2026-09-11-D4-01: FO4 precombines render one of three LOD bands, dropping 12.45% of baked triangles — the "alternative triangulations" premise is empirically false
- **Severity**: HIGH
- **Location**: `byroredux/src/cell_loader/precombined.rs:704-777`
  (`select_finest_lod` + call site); premise mirrored in
  `crates/nif/src/import/precombine.rs:110-114`
- **Status**: NEW (#3641 introduced `select_finest_lod` and is the code being
  questioned, not a prior report of this)
- **Description**: The FO4 precombined-geometry path treats a shared-geometry
  object's three LOD triangle bands as alternative triangulations of the same
  surface and renders only the largest by triangle count. That premise is
  wrong: the three bands are **disjoint sub-meshes** whose union is the full
  model, matching NifSkope's own renderer for the identical in-NIF construct
  (`BSMeshLODTriShape`, additive `switch` fall-through in
  `tools/nifskope/src/gl/bsshape.cpp:426-450`).
- **Evidence**: Measured against retail FO4 data. (1) `lod0+lod1+lod2 ==
  num_triangles` for 12,096/12,096 `BSMeshLODTriShape` blocks (100%) — the
  bands partition the index buffer. (2) Vertex-set intersection across bands
  is **exactly zero** for 5,292/5,292 multi-band shapes — alternative
  triangulations would share vertices; these do not. (3) Across
  `Fallout4 - MeshesExtra.ba2`'s 28,190 shared-geometry objects, 6,668
  (23.7%) have ≥2 populated bands, and picking the max band discards
  1,468,471 of 11,793,978 triangles (**12.45%**), with individual objects
  losing up to 58% of their triangles.
- **Impact**: Baked FO4 architecture renders with visible geometry missing.
  This is the default render path for FO4 exteriors and most interiors, so
  it affects essentially every FO4 cell with precombines, silently (no log,
  no parse-rate signal, no test). The z-fighting the removed code's comment
  cites as justification cannot occur, since the bands share no vertices.
- **Related**: #3641 (introduced `select_finest_lod`); FO4-2026-09-11-D4-02
  (the in-NIF sibling of the same idiom).
- **Suggested Fix**: Render the union of all three bands as max-detail
  (`tri_start = 0`, `tri_count = lod_counts.iter().sum()`), deleting
  `select_finest_lod` and the "alternative triangulations" comment. Distance
  LOD, if wanted, must *drop* tail bands (Level1 = LOD0+LOD1, Level0 = LOD0),
  never pick one. Add a regression test asserting a 3-band fixture yields
  `sum(lod_counts)` triangles.

#### FO4-2026-09-11-D4-02: `ImportedMesh::bs_lod_cutoffs` is write-only — three producers, zero consumers
- **Severity**: LOW
- **Location**: `crates/nif/src/import/mesh/bs_tri_shape.rs:273-276`,
  `crates/nif/src/import/walk/mod.rs:574,950`,
  `crates/nif/src/import/types.rs:941`
- **Status**: NEW
- **Description**: `bs_lod_cutoffs` is populated from FO4
  `BSMeshLODTriShape` and Skyrim `NiLodTriShape` but has no reader anywhere
  outside its own tests. Not a correctness bug per D4-01's finding (drawing
  all bands is the correct max-detail render), but a missing distance-LOD
  optimization whose input data is parsed then discarded.
- **Impact**: 12,096 FO4 shapes (plus Skyrim's population) pay full
  triangle cost at all distances with no way to shed detail.
- **Related**: FO4-2026-09-11-D4-01; #2283 (fixed the cutoffs being
  discarded at parse time, one layer below this).
- **Suggested Fix**: Either wire a distance test trimming the index range
  per the NifSkope rule, or add a one-line doc note that the field is
  parsed-but-unconsumed pending that pass.

#### FO4-2026-09-11-D4-03: `psg_vertex_stride` reads the vertex-attribute field without the 12-bit mask its sibling two lines away applies
- **Severity**: LOW
- **Location**: `crates/nif/src/import/precombine.rs:93-101` (vs `:122`)
- **Status**: NEW
- **Description**: `psg_vertex_stride` computes `attrs = (vertex_desc >>
  44) as u16` (unmasked), while `decode_shared_geom_object` twenty lines
  later correctly masks with `& 0xFFF`. `BSVertexDesc`'s Vertex Attributes
  field is 12 bits at position 44; the unmasked form leaks 4 bits of the
  adjacent `Unused 2` field.
- **Impact**: None today — every `VF_*` constant is below `0x800`, so the
  leaked bits can't change the current `attrs & VF_FULL_PRECISION` result.
  Latent trap for the first consumer testing a bit ≥ `0x1000`.
- **Suggested Fix**: Apply `& 0xFFF` at the stride function too.

**Collision regression pin confirmed intact**: `BhkMultiSphereShape` and
`BhkConvexListShape` both still resolve to `CollisionShape`
(`crates/nif/src/import/collision/shape.rs`), guarded by a structural test
that fails CI if a dispatched `bhk*Shape` ever loses its resolve arm. `#1242`
constant rename (`FO4_DLC_UPPER`) confirmed with zero `FO4_ENV_SCALE` hits
tree-wide.

### Dimension 5 — Shader Flags & BGSM PBR Routing

#### FO4-2026-09-11-D5-01: `parse_skyrim_shader_base` applies `BSLightingShaderProperty`'s BSVER-131 gap-band gate to `BSSkyShaderProperty` / `BSWaterShaderProperty`, which nif.xml gates differently
- **Severity**: MEDIUM
- **Location**: `crates/nif/src/blocks/shader.rs:414-429`
  (`parse_skyrim_shader_base`), consumed by `BSSkyShaderProperty::parse`
  (`:498`), `BSWaterShaderProperty::parse` (`:549`),
  `BSLightingShaderProperty::parse_fo4` (`:1040`),
  `BSEffectShaderProperty::parse_inner` (`:1803`)
- **Status**: NEW (regression introduced by the fix for CLOSED #3845)
- **Description**: The shared Skyrim+ shader head reads the typed
  `(shader_flags_1, shader_flags_2)` pair behind one predicate,
  `carries_typed_shader_flags(bsver)` == `bsver <= 130`. That is the correct
  nif.xml gate (`#BS_FO4#`, `BSVER == 130`) for `BSLightingShaderProperty`
  and `BSEffectShaderProperty`, but nif.xml gates `BSSkyShaderProperty` and
  `BSWaterShaderProperty`'s typed pair on `!#BS_GTE_132#`
  (`BSVER < 132`, which **includes 131**). #3845 unified all four consumers
  onto the `<=130` predicate, moving Sky/Water off their spec-correct gate.
- **Evidence**: nif.xml field vercond citations for both block types
  (`!#BS_GTE_132#`) vs. the single Rust predicate `bsver <=
  FALLOUT4 (130)` used for all four consumers.
- **Impact**: Latent, not live — BSVER 131 is an unshipped dev-stream band,
  so no vanilla corpus reaches it and FO4 retail (130) is unaffected. At
  BSVER 131 specifically, a Sky/Water block would under-read 8 bytes and
  drift every subsequent field (for Sky, an unbounded-length `SizedString`
  read before the allocation guard). The durable cost is recording "131
  carries no typed flags" as a global truth when nif.xml makes it
  block-specific.
- **Related**: #3845 (CLOSED, the unification being partially undone here),
  #2603, #409/FO4-D1-H1, #713.
- **Suggested Fix**: Give `parse_skyrim_shader_base` a per-block-type gate
  parameter (or two named predicates) reflecting each caller's own nif.xml
  vercond, and extend `bsver_shader_flag_band_tests` to assert the two gates
  disagree at exactly 131. Do not revert #3845's unification of the
  duplicated head — only the shared-vercond assumption is wrong.

All other Dimension 5 checklist items (render-affecting bit consumption
per #1592, BGSM Name stopcond, `BSShaderTextureSet` slot routing per #563,
PBR-not-Lambert per #1352/#2700, #1244 base-only-shader consumer) HOLD.

### Dimension 6 — ESM Architecture Records

#### FO4-D6-01: `mswp.rs` module doc describes XMSP consumption as future work; it already shipped
- **Severity**: LOW
- **Location**: `crates/plugin/src/esm/records/mswp.rs:30-38`
- **Status**: NEW
- **Description**: The module doc phrases `XMSP` material-swap parsing and
  consumption as pending ("the cell loader will consult once … parsed").
  Both the parse (`esm/cell/walkers.rs:1015-1030`) and the consumer
  (`byroredux/src/cell_loader/refr.rs:401-438`) already exist and are
  regression-tested.
- **Impact**: None on behavior — stale doc only, risk of a future
  contributor re-implementing shipped work.
- **Suggested Fix**: Update the doc's "Downstream use" paragraph to state
  XMSP resolution is implemented.

#### FO4-D6-02: TXST override doc comments cite `XPRD` as a texture-override REFR sub-record; it is not one
- **Severity**: LOW
- **Location**: `crates/plugin/src/esm/cell/support.rs:474`,
  `crates/plugin/src/esm/cell/mod.rs:868,1182`
- **Status**: NEW
- **Description**: Three comments (inherited from #357) say "REFR
  XTNM/XPRD overrides". `XPRD` is "Patrol Data" (unrelated to textures) and
  has no parse arm anywhere in the crate. The real FO4 override mechanism is
  `XATO`/`XTXR` (both implemented and tested), plus `XTNM` (also real).
- **Impact**: None on behavior — purely a misleading citation that could
  send a contributor looking for a nonexistent parsing gap.
- **Suggested Fix**: Replace "XTNM/XPRD" with "XTNM/XATO/XTXR" in the three
  comments.

All other Dimension 6 checklist items (SCOL/PKIN/MOVS/TXST parsing, TXST
DODT+DNAM per #813/#814 with zero `unreachable_patterns` warnings, category
index exposure per #817) HOLD. Real-data re-run against `Fallout4.esm`
matched documented counts closely: 2,617 SCOL records / 15,878 parts /
40,330 placements, 872 PKIN records, 380 TXST sets (207 with DODT, matching
the module doc's cited 207/382 exactly).

### Dimension 7 — NIFAL Canonical Material Translation (FO4)

#### FO4-D7-01: FO4/Skyrim baked `.bto` object LOD materializes through the texture-only boundary while holding a real `ImportedMaterial`
- **Severity**: MEDIUM
- **Location**: `byroredux/src/cell_loader/object_lod.rs:327,396-404,448-456`
- **Status**: NEW
- **Description**: `object_lod.rs` (the FO4/Skyrim baked `.bto` distant
  object LOD path) runs a full `import_nif_scene_with_resolver`, so each
  sub-mesh arrives with a populated `ImportedMaterial` — and since #3412
  the spawner *reads* `mesh.material.textures.base_color` to pick the
  sub-mesh's authored diffuse — but then discards the rest of it, inserting
  `translate_texture_only_material(tex_path)` instead of
  `translate_material(&mesh.material, …)`.
  `translate_texture_only_material`'s own doc defines its population as
  draws with "no source material record" — false here, since there is one
  and the neighbouring lines already consume part of it. Everything else
  the sub-mesh authored is dropped: `alpha_test`/`alpha_threshold`,
  `two_sided`, `material_kind`, `effect_shader_flags` (no `PBR_BSDF` /
  `MODEL_SPACE_NORMALS` routing), emissive, vertex-color mode, `z_write`,
  UV scale/offset, normal map — and no `AlphaBlend`/`TwoSided`/
  `IsDecalMesh` marker is attached either.
- **Evidence**: Contrast `placement_lod.rs:542` (the Oblivion-only sibling
  scheme), which *does* call `translate_material` on its imported LOD
  sub-mesh material — the two LOD schemes disagree, and the one that says
  "no material to translate" is the FO4/Skyrim one.
- **Impact**: Every FO4/Skyrim baked object-LOD draw shades from keyword
  classification of the diffuse path alone. The consequential loss is alpha
  coverage: an alpha-tested cutout baked into a `.bto` (fences, railings,
  billboard foliage) renders as an opaque quad at LOD distance — a
  silhouette change, not a subtle shading pop. Secondary: lost `two_sided`
  flips backface culling for single-plane LOD geometry, and the missing
  `PBR_BSDF` bit routes FO4 LOD through the legacy lighting lobe while the
  full-res mesh beside it takes the Disney one. Blast radius is the whole
  exterior LOD ring on both titles — distant geometry, hence MEDIUM rather
  than HIGH; escalates to HIGH if a census of real `.bto` alpha-test/
  two-sided usage comes back nonzero (verification recipe in the source
  dimension report, not run this session — needs targeted archive
  extraction).
- **Related**: #2444/MAT-D3-02, #3412, `translate_texture_only_material`'s
  doc block and its #4043 correction.
- **Suggested Fix**: Call `translate_material(&mesh.material, …)` here, the
  same shape `placement_lod.rs` already uses, keeping the atlas-fallback
  texture resolution as-is, and add `attach_blend_and_facing_markers`.

#### FO4-D7-02: the NIFAL two-phase boundary doc omits the third (BGSM-only) Phase-2 resolver, and its `placement_lod` exemption rationale is false for it
- **Severity**: LOW
- **Location**: `byroredux/src/material_translate.rs:28-80`
- **Status**: NEW
- **Description**: The module doc's "two-phase boundary" table lists only
  two Phase-2 resolvers, but a third (`resolve_unresolved_gloss_neutral_
  roughness`, #3905) has existed since 2026-09-05 and is precisely the
  FO4-relevant one (gated on `bgsm_pbr_scalars_authored`). The doc's
  `placement_lod` exemption rationale ("both resolvers early-return without
  the component, so calling them there would be a no-op") is true of the
  two listed resolvers but **false** of the third: its default
  `gloss_map_index = 0` when `MaterialTextureHandles` is absent is exactly
  the value that *satisfies* its predicate, so wiring it at a handle-less
  site per the doc's own stated rationale would silently neutralize
  near-mirror BGSM roughness.
- **Impact**: No runtime effect today (`placement_lod` is Oblivion-only and
  calls none of the three resolvers). Latent trap in exactly the place the
  file tells a future author it's safe to add a call.
- **Related**: #3905/NIFAL-2026-09-05-D1-01 (added the resolver without
  extending this table), #3465 (wrote the exemption when only two existed),
  #3370 (prior correction of this same doc block).
- **Suggested Fix**: Add the third resolver to the phase table, correct
  "Both" to the actual set, and restate the `placement_lod` exemption as "no
  texture handles and no BGSM merge runs there" rather than "all resolvers
  early-return".

All other Dimension 7 checklist items (single `translate_material` boundary,
BGSM PBR flag routing, resolve-once `f32` metalness/roughness contract with
idempotent `resolve_pbr`, glass classified after `resolve_pbr`) HOLD.

### Dimension 8 — FO4 Cell Load End-to-End (SCOL/PKIN Expansion)

No findings. `MAX_PKIN_DEPTH = 4` recursion bound confirmed correct at
exactly 4 call levels (0→3); composed-transform formula matches the
documented TRS composition end-to-end; the `absorbed_refs` precombine
de-dup gate runs strictly before SCOL/PKIN expansion and is shared (not
reimplemented) between interior and exterior paths; `BSConnectPoint` attach
graph is materialized identically for ordinary REFRs and every SCOL/PKIN
synthetic child via the single `spawn_placed_instances` entry point. One
candidate gate-bypass (`PersistentCellApplyJob` hard-coding an empty
`absorbed_refs`) was investigated and disproved — that path never runs a
precombine-spawn phase, so an empty set is the structurally correct value,
not a drifted copy.

### Dimension 9 — Real-Data Validation + Forward Scope

#### FO4-D9-01: `attach_container_inventory` never expands leveled-list (LVLI) entries — placed containers with leveled loot get unresolvable phantom items
- **Severity**: MEDIUM
- **Location**: `byroredux/src/cell_loader/references/attach.rs:196-220`
  (`attach_container_inventory`)
- **Status**: NEW (searched all GitHub issue states for LVLI/leveled/
  container-inventory keywords — no match; #1359 built this function but
  never added leveled-list expansion)
- **Description**: `attach_container_inventory` pushes each
  `ContainerRecord.contents` entry's `item_form_id` directly into the ECS
  `Inventory` with no leveled-list expansion. Vanilla Bethesda CONT records
  commonly reference an `LVLI` form ID in place of a concrete item — the
  same pattern `npc_spawn.rs`'s `build_npc_equip_state` already handles
  correctly via `byroredux_plugin::equip::expand_leveled_form_id`. No
  equivalent call exists in the container path.
- **Evidence**: `LVLI` records index separately
  (`crates/plugin/src/esm/records/index.rs:564`,
  `index.leveled_items` vs. `index.items`); any downstream item-metadata
  lookup keyed by `index.items.get(&form_id)` on an unresolved LVLI form ID
  returns `None`. Existing unit tests for this function cover only concrete
  item IDs and negative-count clamping — no LVLI case.
- **Impact**: Any placed container (FO4, and this function is game-agnostic
  so also FNV/FO3/Skyrim) whose loot table uses a leveled list — a common
  vanilla pattern for ammo boxes, footlockers, safes — spawns with at least
  one unresolvable phantom inventory entry instead of real looted items.
  Does not crash the engine; silently produces wrong loot contents across a
  potentially large fraction of the world's containers. Squarely in the
  project's current "Gameplay slice (P2)" focus.
- **Related**: Closed #1359 (built the base function); distinct from the
  already-closed NPC-side leveled-list bugs (#1180, #1182, #3069, #3217,
  #3341, #3356, #3365).
- **Suggested Fix**: Route `entry.item_form_id` through
  `expand_leveled_form_id`, mirroring `build_npc_equip_state`. Open design
  question: containers have no natural `actor_level` the way NPCs do (loot
  tables conventionally roll against player level) — needs either the
  player's level threaded to the cell-loader attach path, or a documented
  default-level policy for cold cell-load before the player entity exists.

**Real-data parse rate** (re-measured, not cited):
`parse_rate_fo4_all_meshes` — **235,082 / 235,082 NIFs, 100.00% clean,
100.00% recoverable**, across all 8 mesh-bearing archives (`Fallout4 -
Meshes.ba2`, `MeshesExtra.ba2`, and the six DLC `Main.ba2`s). Exact match to
ROADMAP.md's cited figure, no drift since the 2026-08-29 (#3466)
measurement. FaceGen truncation tail re-confirmed gone (0 truncated).
`import_nif_scene` traced correctly across a deathclaw, a super mutant, a
power-armor frame (confirmed heavy skinning + `BSConnectPoint::Parents` for
its battery-mod attach point), a modular weapon (confirmed both
`BSConnectPoint` directions — 7 parent attach points and 1 child
connection), and two settlement workshop items (one confirming
`BSConnectPoint` also drives workshop snap-points). Absolute build-server
material paths observed on one file are already correctly normalized
downstream (`strip_build_prefix`) — not a gap. NPC_ face morph forward-scope
item is **resolved for FO4** specifically — FO4 uses the working pre-baked
FaceGeom path, not the runtime-morph model the general checklist framing
implied; the other forward-scope items (`_precomb.nif`/`.uvd`/MOVS
physics/LIGH power state/`BSBehaviorGraphExtraData`) remain open and
honestly documented, with no misrepresentation found.

---

## BGSM Consumption Table

| BGSM field | Merged into `ImportedMaterial` (`merge_external_material`) | Surfaced on canonical `Material` (post-NIFAL) |
|---|---|---|
| Diffuse tint / albedo | `conductor_diffuse_tint` (gated `metalness > 0.5`) | `Material` albedo path |
| Specular color/strength | `material.specular_color` / `specular_strength` | feeds `bgsm_metalness` (saturation for non-PBR, luminance for PBR) |
| Smoothness | `roughness_override = (1 - smoothness).clamp(0.04,1.0)` | `Material::roughness` (resolve-once `f32`) |
| Emissive | gated on `emit_enabled` | `Material` emissive lane |
| Translucency suite (v≥8) | `translucency_subsurface_color` / `_thick_object` / `_mix_albedo` | `pack_imported_material_flags` → `TRANSLUCENCY` bit |
| Model-space normals | `forward_bgsm_phase1_flags` | `MODEL_SPACE_NORMALS` bit |
| PBR flag | `material.is_pbr = true` unconditionally on any BGSM resolve (#1352/#2700) | `PBR_BSDF` bit → Disney lobe (`lighting.glsl`) |
| Blend factors | `bgsm_blend_to_gamebryo` (plain `u8` cast, no swap) | `Material` blend state |
| Textures (22 named roles) | `MaterialTextureSet<T>` via `fill()` — role-keyed, no FO4 slot numbers | bindless handles via `MaterialTextureHandles` |
| Glass (BGEM `glass_enabled`) | `bgem_glass` authoritative flag | classified **after** `resolve_pbr` — forces `metalness 0.0 / roughness 0.10 / IOR 1.45` |

## Forward Scope Chain

`_precomb.nif`/`_physics.nif` collision (blocked on Havok `hknp`
mesh-shape decode — same blocker as general FO4+ physics/ragdoll) → `.uvd`
occlusion-volume payload decode (envelope cracked, visibility-set payload
still bit-packed and uncracked) → MOVS physics runtime (record parses fully,
no Havok import path exists yet since a physics ECS consumer doesn't exist
for it) → deeper REFR/LIGH/CONT coverage (LIGH power state unimplemented;
CONT leveled-item expansion is the new FO4-D9-01 finding this session; NPC_
face morph is **not** part of this chain for FO4** — already resolved via
pre-baked FaceGeom).

Do **not** list the CSG reader, BGSM parser, or SCOL/PKIN expansion as
pending — all three are shipped and re-verified this session (Dimensions 1,
2, 8).

---

## Coverage Notes

- Dimension 3 declined to re-litigate the concurrency-adjacent finding
  covering `Ba2Archive::extract`'s mutex hold across inflate (#3659,
  `CONC-D7-2026-08-30-01`) — out of this dimension's checklist scope,
  belongs to `/audit-concurrency`.
- Dimension 4 noted `bs_geometry.rs` (Starfield `BSGeometry` extractor) has
  no FO4/BSVER-130 code path and is owned by `/audit-starfield`.
- Dimension 6 noted 6 `EsmCellIndex` fields (WRLD/LTEX-architecture) have no
  `cell_category!` row — out of this dimension's SCOL/MOVS/PKIN/TXST scope,
  flagged for awareness only.
- Dimension 7's escalation trigger for FO4-D7-01 (HIGH if a `.bto`
  alpha-test/two-sided census comes back nonzero) was not run this session
  — needs targeted archive extraction against `Fallout4 - Meshes.ba2`'s
  `.bto` corpus.

## Suggested Publish

```
/audit-publish docs/audits/AUDIT_FO4_2026-09-11.md
```
Label every finding `game:fo4` + `legacy-compat`, plus its own domain label
(`import-pipeline` for D1/D4-03, `nif-parser`/`legacy-compat` for D4-01/02,
`nif-parser` for D5-01, `esm-plugin`+`doc-rot` for D6-01/02, `nifal` for
D7-01/02, `inventory`+`gameplay` for D9-01, `doc-rot` for D1-01).
