# Fallout: New Vegas Compatibility Audit — 2026-08-07

**Scope**: `/audit-fnv` comprehensive sweep, all 9 dimensions. FNV is the
engine's reference title — the most-validated end-to-end path and the
reference realization for NIFAL (material/collision translation) and PHYSAL
(ragdoll) canonical layers.

**Engine HEAD at audit time**: `c0f3cda3` (dimension 8 measured explicitly
against this commit; other dimensions read the same working tree).

**Dedup baseline**: `gh issue list --repo matiaszanolli/ByroRedux --limit 200`
(94 open issues, `/tmp/audit/issues.json`), grepped per candidate finding by
each dimension agent.

**Real-data availability**: `/mnt/data/SteamLibrary/steamapps/common/Fallout
New Vegas/Data/` (full vanilla + all DLC archives) and a working Vulkan
device (RTX 4070 Ti) were both present in this environment. Dimensions 4, 5,
and 8 exercised real FalloutNV.esm / BSA / bench-of-record data directly, not
just code reading and fixtures.

---

## Executive Summary

Nine dimensions were audited; **9 findings total** (0 CRITICAL, 2 HIGH, 1
MEDIUM, 6 LOW). Both HIGH findings are pre-existing, silently-live rendering
divergences — not new regressions introduced since the last audit — but both
meet the "regression against the reference title" bar the `/audit-fnv`
skill treats as HIGH-or-above by default, because they measurably and
invisibly (to `cargo test`) degrade what every FNV scene renders:

1. **FNV-D2-01 (HIGH)** — Canonical `Material.specular_color` carries FNV's
   universally-black `NiMaterialProperty.specular`, zeroing the entire
   direct-specular BRDF term on every FNV surface, worst on
   keyword-classified metals (near-black under direct light).
2. **FNV-D3-01 (HIGH)** — `shadowFade` multiplies the whole ReSTIR-DI
   direct-light estimate to zero past 12000 BU instead of fading the
   *shadow* term, so every shadow-casting FNV light (100% of them — no
   light ever uses `SHADOW_POLICY_NONE`) switches fully off beyond ~171 m.
   The small Prospector Saloon bench-of-record scene never crosses this
   distance, which is why it hasn't shown up in bench numbers.

Both are genuine engine-code findings requiring a fix at their respective
architectural boundary (NIFAL translation for D2-01, the ReSTIR finalize/
streaming gate for D3-01) — not renderer-side per-game hacks
(`feedback_format_translation.md`).

The remaining 7 findings are LOW/MEDIUM: one MEDIUM is a stale-premise doc
comment on a classifier arm whose reachability changed after an unrelated
closed fix (#2315); the six LOWs are documentation drift, a stale test
fixture needing regeneration after a same-day correct dispatch fix, and one
maintainability (code-duplication) nit. No CRITICAL findings. No functional
regressions were found in ESM record parsing, NIF parsing, animation/
skinning/particles, PHYSAL ragdoll, or the seven AI-package procedure
runtimes — all five of those dimensions came back clean or near-clean
against real FNV content.

One previously-open issue, **#2339**, was investigated by Dimension 7 and
found to be **stale** — every silent-drop site the issue names now carries
a `log::warn!` in the live tree (apparently fixed by a later commit without
a closing reference). **Recommend closing #2339.**

---

## Baseline Comparison Table

| Metric | ROADMAP baseline (commit) | Observed (HEAD `c0f3cda3`) | Δ | Verdict |
|---|---|---|---|---|
| Prospector Saloon entities | 3626 (`28155b79`, 2026-08-04) | 3664 | +1.0% | flat, within noise — no FNV-content commits in the gap |
| Prospector Saloon FPS, TAA-native | 136.4 | 135.9 (`--upscaler taa` forced) | −0.4% | no regression |
| Prospector Saloon frame ms, TAA-native | 7.33 | 7.36 | +0.4% | no regression |
| FalloutNV.esm total records | 77,828 (ROADMAP) | 77,828 (live re-parse) | 0 | exact match |
| FNV NIF mesh parse rate | 100% (14,881 meshes) | 100.00% (14,881/14,881, live re-parse) | 0 | exact match |
| FNV SCOL bases / REFRs | 98 / 1084 | 98 (live-asserted) | 0 | exact match, #1538 gate intact |

**Note (FNV-D8-01, see below)**: the ROADMAP's bare headline figure "145.1
FPS" (`8a668eff`, 2026-07-18) is not reproducible with its own cited bare
repro command as of `5c7acfe2` (2026-07-24, FSR 3.1 Quality became the
engine-default upscaler) — running the literal command today measures
254.0 FPS because it now silently exercises FSR instead of native TAA. This
is a documentation-currency issue, not an engine regression; forcing
`--upscaler taa` reproduces the intended baseline within noise (see table
above). Exterior grid streaming (`--grid 0,0 --radius 3`, 7×7 WastelandNV)
was also run fresh: 12,884 entities / 80.9 FPS / 4244 draws at close of the
120-frame timed window, with no archive/parse errors and correct terrain +
object streaming across a cell-boundary crossing mid-run. No ROADMAP
baseline exists yet for this specific exterior configuration to diff
against, so it stands as a new data point, not a regression check.

---

## Dimension Findings

### Dimension 1 — Cell Loading End-to-End
**No new findings.** One previously-tracked issue re-encountered
(`spawn_water_plane`'s dead `blas_specs` parameter — **Existing #2076**,
not re-reported). One checklist item (`_far.nif` distant-object LOD)
investigated and confirmed to be correct, intentional, already-documented
behavior for FO3/FNV (both LOD schemes are gated Oblivion-only /
Skyrim+/FO4-only by design; FNV's landmark LOD lives in the terrain-LOD
block tree instead, which does run). Interior load (Prospector), exterior
7×7 grid, `NifImportRegistry` Arc cache, cell unload hygiene (incl. #1520
Rapier release), M38 water spawn/submersion all read end-to-end and found
correct.

### Dimension 2 — NIFAL Canonical Translation (FNV slice)

#### HIGH

**FNV-D2-01**: Canonical `Material.specular_color` carries FNV's
universally-black `NiMaterialProperty.specular`, annihilating every direct
specular highlight on the reference title.
- **Severity**: HIGH
- **Location**: `crates/nif/src/import/material/legacy_properties.rs:142`,
  `byroredux/src/material_translate.rs:124`,
  `crates/renderer/shaders/include/lighting.glsl:168`
- **Status**: NEW
- On FNV (BSVER 34), `NiMaterialProperty` serializes only Specular +
  Emissive (no Ambient/Diffuse). Vanilla FNV authors Specular = (0,0,0)
  essentially everywhere, because the source engine's lit pipeline
  (`BSShaderPPLightingProperty`) sources specular from the shader/normal-map
  alpha, not the material property. `apply_material_property` faithfully
  copies that authored zero through the NIFAL boundary into the canonical
  `Material.specular_color`, and the shader's BRDF multiplies the entire GGX
  lobe by it with no floor. Byte-level trace (`metalbox01.nif`) confirms the
  parse is correct and the data really is black; `material_dump` across 6
  vanilla NIF files / 18 meshes shows `specClum = 0.00` on every single one
  (with `specS = 1.00`, ruling out the disabled-specular path). Worst on
  keyword-classified conductors — `MetalBox01:0` (metalness 0.90) collapses
  both diffuse (~10%) and specular (0) simultaneously, rendering near-black
  under direct light.
- **Related**: #696, #1873 (`specular_authored`), FNV-D2-02 below (same
  zeros feeding the PBR classifier's dead metalness-lift arm).
- **Suggested Fix**: Resolve at the NIFAL boundary
  (`legacy_properties.rs`), not the shader: treat an all-zero
  `NiMaterialProperty.specular` co-bound with `BSShaderPPLightingProperty`/
  `BSShaderNoLightingProperty` as *unauthored* rather than *authored-off* —
  leave `specular_color` neutral `[1,1,1]` with `specular_authored = false`.
  Keep the explicit `NiSpecularProperty{flags:0}` → zero path intact (that
  IS an authored disable). Verify the neutral-value choice against Gamebryo
  2.3 source per the no-guessing policy before implementing.

#### MEDIUM

**FNV-D2-02**: `classify_pbr_keyword`'s env-map arm is documented as the FNV
majority path but is unreachable on ~83% of sampled FNV meshes post-#2315,
and its specular-metalness lift is dead on FNV.
- **Severity**: MEDIUM
- **Location**: `crates/core/src/ecs/components/material.rs:681-727`
- **Status**: NEW
- The arm's in-source comment claims `env_map_scale = 1.0` is FNV's neutral
  default on "nearly every FNV surface." That premise was invalidated by
  #2315 (CLOSED), which forces `env_map_scale` to 0.0 unless an explicit
  environment-mapping shader flag is authored. Measured: `env = 0.00` on 15
  of 18 sampled FNV meshes. Compounding it, the arm's metalness lift reads
  `spec_lum`, which is 0.0 on all 18 sampled meshes per FNV-D2-01 — so even
  meshes that do reach the arm cannot produce `metalness > 0` from it. Not a
  wrong output on its own (matte fallback is defensible), but the single PBR
  decision point for all legacy content now documents a false reachability
  story, risking future audits/fixes reasoning from a stale premise.
- **Related**: #2315 (CLOSED), #1873, #2352, FNV-D2-01.
- **Suggested Fix**: Correct the comment to state post-#2315 reachability;
  decide explicitly whether to retire or re-source the specular-luminance
  conductor lift. If FNV-D2-01 is fixed as suggested, this arm stays
  correctly inert and only the comment needs updating.

#### LOW

**FNV-D2-03**: `EmissiveSource::None`'s doc contradicts `Material::default()`
- **Severity**: LOW
- **Location**: `crates/core/src/ecs/components/material.rs:453-458` vs `:359-362` —
the variant doc says `emissive_mult` defaults to 0.0, but
`Material::default()` sets it to 1.0. No production impact on the NIF path
(translation always overwrites it from `ImportedMaterial`, whose own
default is 0.0); bites only direct `Material::default()` call sites
(`cornell.rs`, save/load fixtures). **Status: NEW.**

**Checklist items verified CLEAN**: single `translate_material` boundary
(no competing production construction sites), plain-`f32` PBR with no
per-draw keyword scan (one known tracked exception, #2330, not
re-reported), `EmissiveSource` guard (Skyrim+/FO4 sources structurally
unreachable on FNV — verified `emSrc = mat` on all 18 sampled meshes),
collision no-drop guard for `BhkMultiSphereShape`/`BhkConvexListShape`
(dispatch↔resolve parity confirmed), no-fabrication invariant.

### Dimension 3 — RT Lighting Pipeline (FNV Scenes)

#### HIGH

**FNV-D3-01**: `shadowFade` multiplies the whole ReSTIR-DI direct-light
estimate, so every shadow-casting light switches OFF (not "un-shadows")
past 12000 BU.
- **Severity**: HIGH
- **Location**: `crates/renderer/shaders/triangle.frag:2378-2385,
  2583-2585, 2657, 2931`
- **Status**: NEW
- Lights needing visibility (`shadowPolicyNeedsVisibility`) skip the
  streaming-loop `Lo` contribution under ReSTIR, expecting their entire
  direct term to arrive later via the shadowed ReSTIR sample — but both the
  reservoir-streaming gate (`shadowFade > 0.01`) and the finalize scale
  (`frameContribution = rad * restirW * transmissionFrame * shadowFade`)
  are gated/scaled on the *same* `shadowFade`, which ramps 1→0 between
  8000–12000 BU distance. So distance fades the *light itself* to zero
  instead of fading only the *shadow term* (the legacy, now-compiled-out WRS
  arm had the correct semantics: unconditional radiance add, `shadowFade`
  applied only to the shadow subtraction). `byroredux/src/render/lights.rs`
  never emits `SHADOW_POLICY_NONE` — the cell directional and every LIGH
  point/spot are FULL/STRUCTURE, all `needsVisibility == true` — so **100%
  of FNV lights, including the sun, route exclusively through the faded
  estimate**.
- **Impact**: FNV exteriors (`--grid`, radius ≥ 2) lose all sun + point
  direct lighting on terrain/architecture beyond ~171 m from camera; a
  distance-graded darkening band tracks the camera between ~114–171 m.
  Large FNV interiors (Hoover Dam, Vault corridors) can reach the 8000-BU
  ramp. The reservoir is also never streamed past the fade end, biasing the
  temporal/spatial history. Prospector Saloon (bench-of-record) is small
  enough to sit entirely under `SHADOW_FADE_START`, explaining why bench
  numbers don't expose this.
- **Related**: `docs/engine/lighting-from-cells.md:255-256`; ReSTIR rewrite
  `6b061120`; legacy-arm gate #1799. No open issue matches.
- **Suggested Fix**: Keep streaming the reservoir regardless of
  `shadowFade`; change finalize to
  `frameContribution = rad * restirW * mix(vec3(1.0), transmissionFrame, shadowFade)`
  (or equivalently add unshadowed radiance scaled by `1 - shadowFade`) so
  the estimator converges to the unshadowed BRDF value at range, matching
  retired-WRS semantics. Add a shader-reflection/unit pin asserting
  `shadowFade == 0 ⇒ direct light == unshadowed BRDF value`.

**Checklist items verified CLEAN** (TLAS frustum culling, BLAS
compaction/LRU eviction at dynamic VRAM budget, SVGF temporal + TAA Halton/
YCoCg, M33 sky/cloud blending, Disney BSDF gate unreachable on FNV
[#1248–#1252], #1125 skyTint interior gate at both glass miss fallbacks,
sun-sprite mip-0 force) — see `/tmp/audit/fnv/dim_3.md` for evidence detail.

### Dimension 4 — ESM Record Parser
Real-data verified against live `FalloutNV.esm` (77,828 total records,
exact ROADMAP match) plus independent raw-byte Python decodes cross-checked
against the parser. **2 LOW findings, both documentation-only:**

- **FNV-D4-01**: `docs/feature-matrix.md:152` mislabels its SCPT record
  count ("1,257") as "FO3/FNV" — that figure is FO3-only; real FalloutNV.esm
  ships 2,576 SCPT records (parser correctly captures all of them; no
  functional gap).
  - **Severity**: LOW
  - **Status**: NEW.
- **FNV-D4-02**: Stale/incorrect example FormID in an NCR-faction
  spot-check test comment (`crates/plugin/src/esm/records/tests.rs:492-493`)
  — cites `0x0011E662` (actually a `REPU` record) instead of the real NCR
  main faction `0x000A46E7`. The test itself passes correctly via a loose
  match unaffected by the wrong comment.
  - **Severity**: LOW
  - **Status**: NEW.

Varmint Rifle WEAP stats, NCR faction `XNAM` relations (independently
byte-decoded and semantically cross-checked against known lore), VATS AVIF
entries (64 real records, correct presence-gated category/skill-scaling
decode), CELL `XCLL` `fog_far_color` optional-field handling, SCOL FNV-era
gate (#1538, exact 98/1084 live-asserted), LVLI leveled-list flattening
(8 unit tests + wired into `npc_spawn.rs` spawn path), and SCPT `SCHR`
u16-flags fix (#1654) were all verified CLEAN against real data. Full
`byroredux-plugin` test suite: 635 passed, 0 failed; real-ESM suite: 8
passed, 0 failed; clean rebuild: 0 warnings.

### Dimension 5 — NIF Parser FNV Regression Guard
**1 LOW finding**, all other guards intact:

- **FNV-D5-01**: The checked-in per-block baseline TSV
  (`crates/nif/tests/data/per_block_baselines/fallout_nv.tsv`) is stale
  after the same-session commit `8ee151e0` correctly split the
  `bhkSPCollisionObject` dispatch arm out of `bhkCollisionObject` (fixing
  #2332 — a real, verified-correct parser improvement, not a regression).
  - **Severity**: LOW
  - **Status**: NEW (stems from #2332, same session).
  Root-caused and confirmed conserved (39 blocks move to a new
  `bhkPCollisionObject` line, 0 unknowns) by regenerating and reverting
  locally. `cargo test --test per_block_baselines -- --ignored` (the R3
  regression gate) currently fails loud for FNV until the baseline is
  regenerated and checked in. **No runtime/gameplay impact** — parser
  behavior is correct. **Suggested fix**: regenerate + check in the FNV
  (and check FO3, which shares the block family) baseline TSV.

Parse rate re-verified live: 14,881/14,881 (100.00%) against real `Fallout
- Meshes.bsa`, exact ROADMAP match. All #1277 collision/version guards
(`examine_collision_kind` classification, #1652 `havok_motion_type` full
canonical-enum mapping, `version.rs` bsver gates), #1269
`MAX_NIF_NODE_DEPTH` walker guard, `NiTexturingProperty` decal-slot
accounting, `BSMultiBound*`/`BSDecalPlacementVectorExtraData` dispatch all
confirmed intact. Full `byroredux-nif` suite: 960 lib tests + all
integration suites, 0 failures.

### Dimension 6 — Animation, Skinning & Particles (FNV)
**No findings — all guards intact.** `.kf` BSA load/parse/registry chain,
interpolator dispatch (`NiTransformInterpolator`/`NiFloatInterpolator`/
`NiBoolInterpolator`), text-key collection, Clamp/Loop/Reverse cycle
handling, FixedString interning (confirmed zero `StringPool` references in
the per-frame hot path), NiSkinData sparse-weight densification (u16 bone
slot, no >255-bone drop), GPU bone-palette slot pool, B-spline `FLT_MAX`
pose-fallback sentinel (#772, all three TRS channels, reachable on FNV/FO3
as documented), AnimationClipRegistry path-keyed dedup (#790, case-fold +
reverse-map scrub on eviction), NPC hand-mesh load (#793, `lefthand.nif`/
`righthand.nif` confirmed wired through the resumable spawn state machine,
never filtered), and the typed-emitter particle pin (finite/positivity
sentinel guards, single overlay boundary at both scene-build call sites) —
all read end-to-end and confirmed correct.

### Dimension 7 — PHYSAL Ragdoll (FNV Reference Slice)
**No findings — all guards intact.** Full import→template→activation→
writeback pipeline read end-to-end: bone-name resolution, root-space→
bone-local pose conversion (#2336, exactly once), Rapier body seeding with
seed-time scale snapshot (#1852), prior-ragdoll teardown on reactivation
(#2083), keyframed-follower teardown (#1772), and `Stage::Late` writeback
ordering (confirmed it never clobbers the frame's render-data read) were
all verified live. The full silent-drop guard family (#1718/#1539/#1540/
#1772/#1850/#1534) was re-confirmed present and test-pinned. The PHYSAL
per-game-seam invariant holds — zero `GameKind`/`bsver` branches in
`ragdoll.rs` or `crates/physics/src/ragdoll.rs`; the only per-game code in
the whole path is the documented Oblivion-vs-FO3+ field-order branch inside
`BhkConstraint::parse`. FNV's dominant `bhkMalleableConstraint`-wrapped
Ragdoll form (14 of 17 joints) was confirmed to decode into the identical
typed `Ragdoll`/`LimitedHinge` arms a bare constraint would, producing a
jointed body, not a rigid blob. Downstream solver-bridge fixes #2337
(seeded forward-kinematics pose) and #2338 (per-multibody self-contact
exclusion) re-verified live. Writeback-vs-GPU-skin-palette ordering
cross-checked against Dimension 6's independent conclusion — no hazard.

**Issue hygiene**: existing **#2339** ("`extract_ragdoll` has silent drop
sites alongside loud warnings") was investigated directly against its
stated line numbers — every site it names now carries a `log::warn!` in the
live tree. The issue's premise no longer holds. **Recommend closing #2339**
as stale/already-fixed.

### Dimension 8 — Real-Data Validation & Bench-of-Record
Full checklist run against real game data + a live Vulkan device (RTX 4070
Ti). **1 LOW finding:**

- **FNV-D8-01**: The ROADMAP compat-matrix headline Prospector figure
  ("145.1 FPS", captured `8a668eff` 2026-07-18) is no longer reproducible
  with its own cited bare repro command, because `5c7acfe2` (2026-07-24)
  made FSR 3.1 Quality the engine-default upscaler — six days *after* the
  baseline was captured under native TAA.
  - **Severity**: LOW
  - **Status**: NEW, related to but distinct from open **#2367** (a
    different, real, still-unexplained ~2× speedup investigated across a
    different, later commit window).
  Running the literal ROADMAP repro command today measures 254.0 FPS (FSR
  default), not ~145 FPS, which nearly registered as a false "+75% FPS"
  regression-that-wasn't before a `git log` cross-check identified the
  confound. Forcing `--upscaler taa` reproduces the intended baseline
  within noise (135.9 vs 136.4 FPS). **Suggested fix**: annotate
  `ROADMAP.md:432,1117` noting the pre-FSR-default capture date and
  pointing to the already-correct config-labeled "TAA (native)"/"FSR
  Quality" columns.

Bench-of-record (interior, matched TAA-native config) reproduced within
noise across 45 commits — no regression (see Baseline Comparison Table).
Exterior 7×7 WastelandNV grid streaming confirmed clean (no archive/parse
errors, correct terrain+object streaming across a cell-boundary crossing).
`tex.missing`/`tex.loaded` both verified live via `byro-dbg` against real
FNV content — correct output, zero missing textures on Prospector Saloon.
The documented CWD gotcha was reproduced and confirmed to now fail loudly
(ERROR-level logs, already fixed by closed #1776) rather than silently, as
ROADMAP's "silently fails" framing implies — checklist item verified CLEAN
with a note that the doc framing is stale in the *pessimistic* direction.
Process hygiene: all bench engine processes explicitly killed after
capture, zero stray processes at session end.

### Dimension 9 — AI Packages & Procedure Runtimes (M42.2–M42.8)
Heavily self-audited corner of the codebase (per-procedure test modules,
precise module docs). **1 LOW finding:**

- **FNV-D9-01**: `guard_system::resolve_anchor`
  (`byroredux/src/systems/guard.rs:70-79`) duplicates
  `travel_system::resolve_destination`'s (`travel.rs:81-96`)
  `NearReference`-FormID-resolve logic instead of sharing it, despite that
  function's own doc comment stating it was generalized specifically so
  Guard could reuse it (Escort does reuse it, `escort.rs:57`, via an
  aliased import).
  - **Severity**: LOW
  - **Status**: NEW.
  `boot.rs:920`'s comment additionally overstates this as
  "reuses travel_system's anchor-resolution logic," which is true of Escort
  but not Guard. Purely a maintainability nit — the two functions are
  behaviorally correct and intentionally divergent on their fallback today
  (Guard falls back to `home`, not Travel's hash-pick, by design, previously
  tried and reverted) — but nothing forces the shared resolve-half to stay
  in sync if it ever needs a fix. **Suggested fix**:
  extract the shared `NearReference → GlobalTransform.translation` resolve
  into a small `Option<Vec3>`-returning helper both functions call before
  applying their own fallback; correct `boot.rs:920`'s wording from
  "reuses" to "mirrors" for Guard.

CTDA fail-open (M42.2, all seven procedures — verified `Unknown` condition
functions never flip to fail-closed), schedule gating / single-Behavior
mutual exclusion (`active_package`'s single-resolve `is_*` chain), PLDT
radius/PTDT distance fallback defaults (six-of-seven + Follow), the
by-design NearReference resolution differences across procedures (Sandbox
never resolves / Travel-Escort-Guard resolve lazily on first tick / Guard's
`home` fallback / Follow's no-fallback), seat reservation keyed by
`(furniture, marker index)` (independent per-marker reservation confirmed
by test), the documented legacy-marker over-match (confirmed still
documented, not silently "fixed" by a heuristic), live-tracking vs. frozen
destination (Follow/Escort re-read live; Travel/Guard freeze once), Patrol
calling Wander's shared core function (not duplicating its state machine),
and the seven-procedures-all-opt-in env-var gating (none in the default
scheduler) were all verified CLEAN with direct test/code cross-checks.

---

## Regression Guard List (previously-fixed issues verified still correct)

- #1520/#1531 — Rapier body/collider/ragdoll-joint release on cell unload
- #613 — NiSkinData bone-index widened to u16
- #772 — B-spline `FLT_MAX` pose-fallback sentinel
- #790 — AnimationClipRegistry path-keyed dedup
- #793 — kf-era NPC hand-mesh load (lefthand/righthand)
- #340 — FixedString interning at clip-load, no per-frame lock
- #1277/#1652 — collision authoring classification + canonical `hkMotionType` mapping
- #1269 — `MAX_NIF_NODE_DEPTH` walker guard
- #1538 — SCOL FNV-era dispatch gate (not FO4-only)
- #1654 — SCPT `SCHR` flags read as u16
- #1718/#1539/#1540/#1772/#1850/#1534 — PHYSAL silent-drop-to-warning guard family
- #1852/#2083/#1979/#1981/#2336/#2337/#2338 — ragdoll activation/writeback/solver-bridge fixes
- #1776 — loud (not silent) error on zero-archives-opened CWD misconfiguration
- #2315 — FNV env-map-scale gate (created the now-stale-comment condition in FNV-D2-02)
- #2332 — `bhkSPCollisionObject` dispatch split (created the stale-baseline condition in FNV-D5-01)

## Issue Hygiene Recommendation

- **Close #2339** — "extract_ragdoll has silent drop sites alongside loud
  #1539/#1850 warnings." Dimension 7 confirmed every site the issue names
  now warns in the live tree; the issue appears to have been fixed by a
  later commit without a closing reference.

---

## Severity Totals

| Severity | Count |
|---|---|
| CRITICAL | 0 |
| HIGH | 2 |
| MEDIUM | 1 |
| LOW | 6 |
| **Total** | **9** |

Per-dimension: Dim1: 0 · Dim2: 3 (1 HIGH, 1 MEDIUM, 1 LOW) · Dim3: 1 (HIGH)
· Dim4: 2 (LOW) · Dim5: 1 (LOW) · Dim6: 0 · Dim7: 0 · Dim8: 1 (LOW) · Dim9: 1
(LOW).

Suggest: `/audit-publish docs/audits/AUDIT_FNV_2026-08-07.md`
