# NIF Parser Audit — 2026-05-23

**Scope:** Full six-dimension sweep per `/audit-nif`.

**Predecessors:**
- [AUDIT_NIF_2026-05-22_DIM5.md](AUDIT_NIF_2026-05-22_DIM5.md) (dim 5 deep audit — 24 h old).
- [AUDIT_NIF_2026-05-19_DIM4.md](AUDIT_NIF_2026-05-19_DIM4.md) (dim 4 — closed via #1201 / #1202 / #1223).
- [AUDIT_NIF_2026-05-12.md](AUDIT_NIF_2026-05-12.md) (last full sweep).

---

## Executive Summary

The NIF parser is in **near-steady-state** with one structural HIGH-impact
gap surfaced today.

The five-day window since the 2026-05-19 sweep landed the #1201 / #1202
(alpha-property cascade), #1215 / #1216 / #1217 (zero-mesh observability),
#1223 (BSLightingShaderProperty env_map_scale over-read),
#1232 (BSGeometry no-tangent fallback), and #1235 (SceneFlags
cell-loader parity) closures — all re-verified in code on this sweep.

Today's six parallel dimension agents surfaced **8 NEW findings** (1 HIGH
+ 2 MEDIUM + 5 LOW). The headline is **NIF-D3-NEW-07**: a single wrong
version gate in `skip_emitter_base` truncates 219 Oblivion scenes /
15 182 dropped blocks (~2.7 pp of the 96.24% Oblivion baseline). Fix is
a one-line gate change.

### Severity rollup

| Severity | Count | Dimension(s) |
|----------|-------|--------------|
| CRITICAL | 0     | — |
| HIGH     | 1     | D3 (Oblivion emitter base under-read) |
| MEDIUM   | 2     | D1 (NiTextureEffect FO4+ asymmetry), D4 (BSLightingShaderProperty PBR scalars dropped) |
| LOW      | 5     | D2 (FO4_ENV_SCALE docstring), D4 ×2 (WaterShaderProperty, BSShaderPropertyBaseOnly), D6 ×2 (ragdoll style, read_pod_vec must_use) |
| INFO     | 1     | D6 (dhat-infra gap re-flagged) |
| Steady   | D5    | zero new findings (24-h delta from yesterday) |

### Recommended fix order

1. **NIF-D3-NEW-07** (HIGH, dim 3) — one-line gate change in
   `crates/nif/src/blocks/particle.rs:181`. Quantified Oblivion impact;
   expect 219 → 0 truncated scenes after the fix.
2. **NIF-D1-2026-05-23-01** (MEDIUM, dim 1) — 5-LOC NiTextureEffect
   parser gate mirroring `NiLightBase` (#721).
3. **NIF-DIM4-2026-05-23-01** (MEDIUM, dim 4) — extend `MaterialInfo`
   with 7 PBR scalar fields parsed but never forwarded to the renderer
   on Skyrim+/FO4/FO76.
4. LOW + INFO findings — bundle into the next hygiene sweep.

Suggested publish: `/audit-publish docs/audits/AUDIT_NIF_2026-05-23.md`

---

## Per-Dimension Status

| Dim | Subject | Status | NEW findings |
|-----|---------|--------|--------------|
| 1 | Block Parsing Correctness | Near-steady | 1 MEDIUM |
| 2 | Version Handling | Steady-state | 1 LOW |
| 3 | Stream Position Integrity | **REGRESSION** | 1 HIGH (Oblivion) |
| 4 | Import Pipeline Correctness | Near-steady | 1 MEDIUM + 2 LOW |
| 5 | Coverage Gaps | Steady-state | 0 (24-h delta zero) |
| 6 | Allocation Hygiene | Steady-state | 2 LOW + 1 INFO |

---

## Block Type Coverage Matrix (unchanged from 2026-05-22)

| Game | Clean-parse rate | Hard fails | Source |
|------|-----------------:|-----------:|--------|
| FO3 | 100.00% | 0 | 2026-04-27 ROADMAP sweep |
| FNV | 100.00% | 0 | 2026-04-27 ROADMAP sweep |
| Skyrim SE | 100.00% | 0 | 2026-04-27 ROADMAP sweep |
| Oblivion | 96.24% | 0 (recoverable via realign) | 2026-04-27 — **expected to rise ~3 pp after NIF-D3-NEW-07 fix** |
| FO4 | 96.46% | 0 | 2026-04-27 ROADMAP sweep |
| FO76 | 97.34% | 0 | 2026-04-27 ROADMAP sweep |
| Starfield | 98.60% | 0 | 2026-04-27 ROADMAP sweep |

Dispatch arm count: **249** string-literal arms / **195** `Box::new`
entries in [`crates/nif/src/blocks/mod.rs`](../../crates/nif/src/blocks/mod.rs)
— byte-identical to yesterday's baseline.

Live corpus spot-check on `Oblivion - Meshes.bsa` (dim 3 agent): **97.27%**
clean parse rate today (vs 96.24% baseline — minor improvement; 0 hard
failures).

---

## NEW Findings

### NIF-D3-NEW-07 (HIGH): NiPSysEmitter base under-reads 8 bytes on Oblivion — 219 truncated scenes in `Oblivion - Meshes.bsa`

- **Severity**: HIGH
- **Dimension**: 3 (Stream Position Integrity)
- **Location**: [`crates/nif/src/blocks/particle.rs:165-185`](../../crates/nif/src/blocks/particle.rs#L165-L185) (`skip_emitter_base`)
- **Status**: NEW. Related to closed #383 (which fixed the same shortfall for FNV/FO3 `bsver ≥ 34` but did not extend to Oblivion `bsver = 11`).
- **Game Affected**: Oblivion (NifVariant::Oblivion, bsver=11)

**Description**: `skip_emitter_base` reads 12 base floats unconditionally
(48 B) and adds 2 trailing floats (`Radius Variation` + `Life Span
Variation`, 8 B) only when `stream.bsver() >= 34`. Per nif.xml's
`NiPSysEmitter` definition, both trailing floats apply to every NIF with
`version >= 10.4.0.1`, which Oblivion (v20.0.0.5) satisfies. The current
`bsver >= 34` gate excludes Oblivion, so every `NiPSys*Emitter` subclass
on Oblivion under-reads by exactly 8 bytes.

**Evidence** — `meshes\magiceffects\fireball.nif` trace via `cargo run --release -p byroredux-nif --example trace_block`:

```
[167] @  37751  NiPSysAgeDeathModifier   consumed=34         (correct)
[168] @  37785  BSPSysArrayEmitter       consumed=68         (should be 76)
[169] @  37853  NiPSysSpawnModifier      consumed=4 → ERR
      NIF requested 1 065 353 216-byte allocation, exceeds hard cap (268 435 456)
```

The 4-byte name-length field that `parse_spawn_modifier` reads at offset
37853 is actually a float `0x3f800000` (= 1.0) — i.e. the last of two
floats `BSPSysArrayEmitter` should have consumed. The hex at 37861
(= 37853 + 8) is `15 00 00 00 4e 69 50 53 79 73 53 70 61 77 6e ...` =
`[len=21]"NiPSysSpawnModifier:1"`, confirming the 8-byte shift. Second
confirmed reproduction in `meshes\fire\firearcanemedium01.nif`:
`NiPSysCylinderEmitter` consumes 96 B at offset 3116, next block starts
at 3220 — same 8-byte shortfall.

**Impact**: Oblivion-only and cascading. Every emitter under-read shifts
the next block's read by 8 bytes, which usually lands on a float
(reinterpreted as a u32 count) that trips `check_alloc`'s 256 MB cap or
EOF, killing the entire NIF via the no-block-sizes truncation path at
[`lib.rs:632-646`](../../crates/nif/src/lib.rs#L632-L646). Aggregate in
`Oblivion - Meshes.bsa`: **219 truncated scenes, 15 182 blocks dropped**
(= 2.73 pp of the 97.27% clean rate). Affected NIFs include every
torch/fire/spell-impact effect (`fireball.nif`, `firearcanemedium01.nif`,
`fxdustcloudfaint01.nif`, dozens more in `magiceffects\`, `effects\`,
`fire\`).

**Related**: #383 (FNV/FO3 8-byte fix, closed — same root cause, opposite
gate). #687 / #688 (Oblivion alloc-cap-truncation symptoms, closed — both
surfaced the same cascading-truncation pattern but were attributed to
specific particle types rather than the emitter base). #395 (Oblivion
stream drift detector, closed). The 2026-04-26 blessed `Oblivion 96.24%`
baseline already reflects this bug; a fix would push the clean rate
toward ~99%+.

**Suggested Fix**: Tighten the gate at `particle.rs:181`. Per nif.xml the
two trailing floats are unconditional once `version >= 10.4.0.1`
(Oblivion qualifies). Replace `if stream.bsver() >= 34` with
`if stream.version() >= NifVersion::V10_4_0_1`. Confirm against
Gamebryo 2.3 `NiPSysEmitter::LoadBinary` before landing — if the source
disagrees with nif.xml, fall back to a Bethesda-specific predicate that
includes Oblivion (`bsver >= 11 && version >= V20_0_0_5`). Run
`cargo run --release -p byroredux-nif --example nif_stats -- "Oblivion - Meshes.bsa"`
after the fix; expect truncated count to drop from 219 toward 0.

---

### NIF-D1-2026-05-23-01 (MEDIUM): `NiTextureEffect` reads `NiDynamicEffect` base unconditionally on FO4+ (BSVER ≥ 130)

- **Severity**: MEDIUM
- **Dimension**: 1 (Block Parsing Correctness)
- **Location**: [`crates/nif/src/blocks/texture.rs:719-737`](../../crates/nif/src/blocks/texture.rs#L719-L737)
- **Status**: NEW
- **Game Affected**: FO4 / FO76 / Starfield (BSVER ≥ 130)

**Description**: `NiTextureEffect::parse` reads the `NiDynamicEffect`
base fields (`Switch State`, `Num Affected Nodes`, `Affected Nodes[]`)
gated only on **NIF version** (`>= V10_1_0_106`, `>= V10_1_0_0`). nif.xml
lines 3499 + 3504 carry an additional `vercond="#NI_BS_LT_FO4#"` clause:
both fields are **absent** at BSVER ≥ 130 (FO4 / FO76 / Starfield),
because FO4 reparented `NiTextureEffect`'s parent class
`NiDynamicEffect` straight onto `NiAVObject` and dropped the
dynamic-effect plumbing. The sibling parser `NiLightBase::parse` at
[`crates/nif/src/blocks/light.rs:60-102`](../../crates/nif/src/blocks/light.rs#L60-L102)
already honours this — see the `pre_fo4 = stream.bsver() < bsver::FALLOUT4`
guard landed in #721. `NiTextureEffect` was missed in that fix.

**Impact**: On any FO4+ NIF that ships a `NiTextureEffect`, the parser
over-reads by 5 bytes (`switch_state` u8 + `num_affected_nodes` u32),
then either consumes more bytes as `affected_nodes` body or — far more
likely — reads a wildly large `count` and fails the `allocate_vec`
budget. FO3+ ships `block_sizes`, so the outer loop recovers via skip
and the block lands as a corrupted (but silently-recovered) record — no
hard error in the parse-rate gate. The source texture ref + projection
matrix never reach the importer; any projected env map / gobo / fog
projector authored on FO4+ content silently drops.

**Suggested Fix**: 5-LOC change mirroring `NiLightBase`:

```rust
let pre_fo4 = stream.bsver() < crate::version::bsver::FALLOUT4;
let switch_state = if pre_fo4 && stream.version() >= NifVersion::V10_1_0_106 { … };
let affected_nodes = if pre_fo4 && stream.version() >= NifVersion::V10_1_0_0 { … };
```

Add a regression test at `texture.rs` constructing a BSVER=130 fixture
with no `Switch State` / `Affected Nodes` bytes between the
`NiAVObjectData` tail and the projection matrix; assert the parser
reads the matrix without drift.

---

### NIF-DIM4-2026-05-23-01 (MEDIUM): BSLightingShaderProperty FO4+ PBR scalars parsed but never consumed

- **Severity**: MEDIUM
- **Dimension**: 4 (Import Pipeline — silent feature drop on Skyrim+ / FO4 / FO76)
- **Location**: [`crates/nif/src/import/material/walker.rs:291-307`](../../crates/nif/src/import/material/walker.rs#L291-L307) (capture site)
- **Status**: NEW
- **Game Affected**: Skyrim / FO4 / FO76 (BSVER < FALLOUT4 for soft-lighting; BSVER 130–139 for rim/backlight/subsurface; BSVER ≥ 130 for grayscale/fresnel)

**Description**: The Skyrim+ `BSLightingShaderProperty` branch reads
`emissive_color`, `emissive_multiple`, `specular_color`,
`specular_strength`, `glossiness`, `uv_offset/scale`, and `alpha` into
`MaterialInfo`, then delegates to `apply_shader_type_data` for the
type-tagged trailer. But seven scalar PBR fields on every
BSLightingShaderProperty body — none of them shader-type gated — are
captured by the parser at [`crates/nif/src/blocks/shader.rs:683-695`](../../crates/nif/src/blocks/shader.rs#L683-L695)
and then dropped on the floor:

- `refraction_strength` (`shader.rs:679`)
- `lighting_effect_1` / `lighting_effect_2` (`shader.rs:683-684`) — Skyrim subsurface/backlight scalars (BSVER < FO4) gated by `SLSF2_Soft_Lighting` / `SLSF2_Back_Lighting`
- `subsurface_rolloff` (`shader.rs:687`) — FO4 BSVER 130–139
- `rimlight_power` (`shader.rs:689`) — FO4 BSVER 130–139
- `backlight_power` (`shader.rs:691`) — FO4 BSVER 130–139
- `grayscale_to_palette_scale` (`shader.rs:693`) — FO4+ BSVER ≥ 130
- `fresnel_power` (`shader.rs:695`) — FO4+ BSVER ≥ 130

Each has been the subject of past parse-correctness bugs (#1175 backlight
gate inversion, #115 backlight conditional, #403 wetness gate at BSVER
130), confirming the corpus is large enough to matter — yet none reach
`MaterialInfo` or `ImportedMesh`, so the import-side investment in
parser correctness can't surface in the renderer.

**Evidence**: `grep -rn "lighting_effect\|subsurface_rolloff\|rimlight\|backlight_power\|grayscale_to_palette_scale\|fresnel_power\|refraction_strength" crates/nif/src/import/ crates/renderer/src/ byroredux/src/`
returns zero hits outside test files.

**Impact**: Skyrim-era subsurface-skin / soft-cloth surfaces render as
flat-lit (no SSS approximation, no soft backlight); FO4 power-armor /
NPC skin / cloth render without the FO4 rimlight + grayscale-palette PBR
contribution; FO76 inherits the same gaps. The renderer's PBR ladder in
`triangle.frag` has Fresnel + spec + emissive paths but no fed values
for the per-material rolloff/rim/backlight modulators, so the fallback
constants apply to every BSLightingShaderProperty surface. Silent feature
drop on three games.

**Related**: #1175 (closed parser fix), #403 (closed parser fix), #1147
(BGSM v≥8 translucency suite — same family of PBR scalars, but
BGSM-sourced instead of NIF-sourced).

**Suggested Fix**: Extend `MaterialInfo` (`crates/nif/src/import/material/mod.rs`)
with the 7 fields. Copy them at `walker.rs:291-307` from `shader.*` to
`info.*` alongside the existing `glossiness` / `alpha` lines. Forward to
`ImportedMesh` in all three mesh extractors (`ni_tri_shape.rs`,
`bs_tri_shape.rs`, `bs_geometry.rs`) — they already pass through ~30
`mat.*` fields each. Renderer consumption can land separately as part of
a Skyrim+ PBR pass once BGSM v≥8 path (#1147) is in place.

---

### NIF-D2-2026-05-23-01 (LOW): `bsver::FO4_ENV_SCALE` docstring contradicts the wire format and the constant's actual usage

- **Severity**: LOW
- **Dimension**: 2 (Version Handling — doc hygiene)
- **Location**: [`crates/nif/src/version.rs:234-237`](../../crates/nif/src/version.rs#L234-L237)
- **Status**: NEW (sibling-class to closed NIF-D2-NEW-09 from 2026-05-12, but the docstring was made stale by the more recent #1223 fix rather than the original #937).

**Description**: The constant `bsver::FO4_ENV_SCALE = 140` carries a
docstring claiming `env_map_scale "moves inside an extended wetness block"`
at `bsver >= FO4_ENV_SCALE`. The comment at
[`shader.rs:943-957`](../../crates/nif/src/blocks/shader.rs#L943-L957)
(added by #1223 on 2026-05-19) explicitly refutes this claim: gating the
wetness env_map_scale on `>= 140` dropped Starfield Meshes01 parse rate
from 97.21% → 95.77%, so the gate was forced to `false` for both
`>= 130` (FO4) and `>= 155` (Starfield/FO76). The constant is now
repurposed as the upper bound of the FO4-DLC SSR/skin-tint range — at
`shader.rs:1205` and `shader.rs:1218` it gates
`bsver >= FALLOUT4 && bsver < FO4_ENV_SCALE` (i.e. BSVER 130..=139 only).

**Suggested Fix**: Rewrite the docstring to describe the actual semantic
("Upper bound of the FO4-DLC range that authors `BSEffectShader`
SSR/skin-tint trailing bools. The previous interpretation was empirically
wrong — see #1223."). Optionally rename to `FO4_DLC_UPPER`.

---

### NIF-DIM4-2026-05-23-02 (LOW): `WaterShaderProperty` (FO3/FNV legacy) parsed but never consumed at import

- **Severity**: LOW (orphan-parse)
- **Dimension**: 4 (Import Pipeline)
- **Location**: [`crates/nif/src/blocks/mod.rs:460`](../../crates/nif/src/blocks/mod.rs#L460) (dispatch), [`crates/nif/src/blocks/shader.rs:279-290`](../../crates/nif/src/blocks/shader.rs#L279-L290) (block), [`crates/nif/src/import/material/walker.rs:818-837`](../../crates/nif/src/import/material/walker.rs#L818-L837) (consumers — only Tile/Sky/TallGrass)
- **Status**: NEW
- **Game Affected**: FO3 / FNV

**Description**: The FO3/FNV-era `WaterShaderProperty` (non-BS variant)
is parsed via its dedicated arm (added in #474), but
`walker.rs` has no `get_as::<WaterShaderProperty>(idx)` site. The legacy
walker consumes `TileShaderProperty`, `SkyShaderProperty`, and
`TallGrassShaderProperty` (#940) but `WaterShaderProperty` was missed.
Its `env_map_scale` never reaches `MaterialInfo`.

**Suggested Fix**: 4-line consumer after the `TallGrassShaderProperty`
arm:

```rust
if let Some(shader) = scene.get_as::<WaterShaderProperty>(idx) {
    info.env_map_scale = shader.shader.env_map_scale;
}
```

---

### NIF-DIM4-2026-05-23-03 (LOW): `BSShaderPropertyBaseOnly` subclasses (Hair / VolumetricFog / DistantLOD / DistantTree) — base-data fields never consumed

- **Severity**: LOW (orphan-parse)
- **Dimension**: 4 (Import Pipeline)
- **Location**: [`crates/nif/src/blocks/shader.rs:302-328`](../../crates/nif/src/blocks/shader.rs#L302-L328) (block), [`crates/nif/src/blocks/mod.rs:426-429`](../../crates/nif/src/blocks/mod.rs#L426-L429) (dispatch arms)
- **Status**: NEW

**Description**: Four legacy shader-property types share the
`BSShaderPropertyBaseOnly` struct (#717): `HairShaderProperty`,
`VolumetricFogShaderProperty`, `DistantLODShaderProperty`,
`BSDistantTreeShaderProperty`. Each carries `NiObjectNETData` +
`BSShaderPropertyData` base (which has `env_map_scale` and
`texture_clamp_mode`). None have a consumer arm in `walker.rs`.

**Suggested Fix**: Single shared consumer in `walker.rs`:

```rust
if let Some(shader) = scene.get_as::<BSShaderPropertyBaseOnly>(idx) {
    info.env_map_scale = shader.shader.env_map_scale;
    if info.texture_clamp_mode == 3 {
        info.texture_clamp_mode = shader.shader.texture_clamp_mode as u8;
    }
}
```

---

### NIF-D6-NEW-01 (LOW): `collision/ragdoll.rs` uses pre-#408 `check_alloc + Vec::with_capacity` idiom instead of `allocate_vec`

- **Severity**: LOW (style consistency)
- **Dimension**: 6 (Allocation Hygiene)
- **Location**: [`crates/nif/src/blocks/collision/ragdoll.rs:59-60, :87-88, :95-96, :132-133`](../../crates/nif/src/blocks/collision/ragdoll.rs)
- **Status**: NEW. Not a regression — file was added in commit 226f43d3 closing #980, predates the #408 architectural pin's strict re-statement.

**Description**: Four sites manually pair
`stream.check_alloc(n.saturating_mul(K))?` with `Vec::with_capacity(n)`.
Functionally identical to `stream.allocate_vec::<T>(n as u32)?` but:
1. Misses the `#[must_use]` warning if a future edit deletes the binding;
2. Hardcodes element-size constants (`40` for BoneTransform, `4` for BlockRef);
3. Makes a future blanket grep harder to interpret.

**Suggested Fix**: Replace each pair with
`let mut transforms = stream.allocate_vec::<BoneTransform>(n as u32)?;`.

---

### NIF-D6-NEW-02 (LOW): 11 `read_*_array` wrappers + KFM `allocate_vec` lack `#[must_use]`

- **Severity**: LOW (latent-regression guard)
- **Dimension**: 6 (Allocation Hygiene)
- **Location**: [`crates/nif/src/stream.rs:337-417`](../../crates/nif/src/stream.rs#L337-L417) (11 wrappers), [`crates/nif/src/kfm.rs:686-698`](../../crates/nif/src/kfm.rs#L686-L698)
- **Status**: NEW. Not a regression of #831 (which targeted the `allocate_vec` callsites).

**Description**: `read_pod_vec` and the 11 public typed wrappers all
return `io::Result<Vec<T>>`. A caller writing `stream.read_u32_array(n)?;`
(no binding) silently runs the bulk `read_exact` *and* drops the Vec —
that's a `(read_count × sizeof::<T>())`-byte zero-init + read + drop,
strictly worse than the `allocate_vec` misuse. All 13 candidate
non-binding sites surfaced by grep turned out to be expression-position
uses — so no current call site triggers the misuse. The pin holds
empirically but is undefended structurally.

**Suggested Fix**: Annotate `read_pod_vec` with
`#[must_use = "read_pod_vec returns a populated Vec; bind it or use stream.skip() to advance the cursor without reading"]`.
Annotate `kfm.rs:686` similarly.

---

### NIF-D6-INFO-03 (INFO): dhat-infra gap still open — no allocation-counter regression test for NIF-PERF-01/02/03 or #408

- **Severity**: INFO (visibility flag, not a defect)
- **Dimension**: 6 (Test Infrastructure)
- **Location**: workspace-wide (no test file exists)
- **Status**: KNOWN-OPEN. Re-flagged from earlier audits.

**Description**: All four architectural pins shipped as code-review-only
fixes. There is no test that, given a fixed corpus NIF, asserts an upper
bound on heap allocations. A regression that re-introduces e.g. the
`or_insert(name.to_string())` pattern would compile, pass every existing
test, and only manifest as a multi-megabyte `htop`-visible RSS bump on
long Oblivion cell walks.

**Suggested Fix**: Add `dhat = "0.3"` as an optional workspace dep gated
on a `dhat-heap` feature; wire a `#[cfg(feature = "dhat-heap")]` test
that parses a representative NIF and asserts `dhat::HeapStats::total_blocks() < N`
and `dhat::HeapStats::total_bytes() < M`. Out of scope to ship from a
dim-6 audit; file as a backlog issue.

---

## Re-Verified Closures (since 2026-05-19)

All carry-overs from the prior sweep re-verified in source on this pass:

| Issue | Subject | Verification site |
|-------|---------|-------------------|
| #1201 | Inherited NiAlphaProperty cascade gate | [`material/walker.rs:514`](../../crates/nif/src/import/material/walker.rs#L514) |
| #1202 | BSEffectShader implicit alpha_blend gate | [`material/walker.rs:446`](../../crates/nif/src/import/material/walker.rs#L446) |
| #1215 | Zero-mesh import warn | [`cell_loader/references.rs:913-925`](../../byroredux/src/cell_loader/references.rs#L913-L925) |
| #1216 | BSTriShape zero-vertex non-skinned debug log | [`tri_shape/bs_tri_shape.rs:378-386`](../../crates/nif/src/blocks/tri_shape/bs_tri_shape.rs#L378-L386) |
| #1217 | Precombined-spawn cache-hit zero-mesh log | [`cell_loader/precombined.rs:103-120`](../../byroredux/src/cell_loader/precombined.rs#L103-L120) |
| #1219 | NifVariant `(V20_0_0_4, u=11)` routing | [`version.rs:314-327`](../../crates/nif/src/version.rs#L314-L327) |
| #1223 | BSLightingShaderProperty env_map_scale gate | [`shader.rs:939-963`](../../crates/nif/src/blocks/shader.rs#L939-L963) |
| #1224 | NiFogProperty carve-out (intentional skip) | [`material/walker.rs:924-934`](../../crates/nif/src/import/material/walker.rs#L924-L934) |
| #1232 | BSGeometry no-tangent synthesis fallback | [`mesh/bs_geometry.rs:113-126`](../../crates/nif/src/import/mesh/bs_geometry.rs#L113-L126) |
| #1235 | SceneFlags cell-loader parity | [`spawn.rs:725, 207`](../../byroredux/src/cell_loader/spawn.rs#L725) + [`extract_root_flags`](../../crates/nif/src/import/mod.rs#L344-L358) |

---

## Out-of-Scope Carve-Outs (not re-filed)

- **#1206** (OPEN): `BsTriShapeKind::SubIndex` segmentation payload reaches `ImportedMesh.bs_sub_index` ([`mesh/bs_tri_shape.rs:296-299`](../../crates/nif/src/import/mesh/bs_tri_shape.rs#L296-L299)) — the parser → import → ImportedMesh chain is wired; the issue stays OPEN because the **dismemberment system** that would consume it is unbuilt. Not a NIF parser finding.
- **#1224** (CLOSED): NiFogProperty carve-out — confirmed and not re-filed per the explicit skill text.
- **Band-B deferred orphan-parse types** under #986 (closed but explicitly deferred): `BsTreadTransfInterpolator`, `BsEyeCenterExtraData`, `BsBehaviorGraphExtraData`, `BsDistantObjectLargeRefExtraData`. Re-raising would be a stale finding per [Audit Finding Hygiene](../../../../home/matias/.claude/projects/-mnt-data-src-gamebyro-redux/memory/feedback_audit_findings.md).
- **#877** (OPEN): `pre_parse_cell` BSA mutex contention — performance, not parser scope.

---

## Summary

| Severity | Count | Disposition |
|----------|-------|-------------|
| CRITICAL | 0     | — |
| HIGH     | 1     | NIF-D3-NEW-07 (Oblivion emitter base under-read — 219 truncated scenes) |
| MEDIUM   | 2     | NIF-D1-2026-05-23-01 (NiTextureEffect FO4+ asymmetry), NIF-DIM4-2026-05-23-01 (7 PBR scalars dropped) |
| LOW      | 5     | NIF-D2 docstring, NIF-DIM4 ×2 (WaterShaderProperty + BSShaderPropertyBaseOnly), NIF-D6 ×2 (ragdoll style + must_use) |
| INFO     | 1     | NIF-D6-INFO-03 (dhat-infra gap re-flagged) |

**Headline:** Oblivion has a single one-line wrong-version-gate that
costs ~3 pp of clean parse rate (219 truncated scenes). Land
NIF-D3-NEW-07 first; everything else is style / orphan-parse / doc
hygiene that can bundle into the next hygiene sweep. Dim 5 confirms
the dispatch surface is byte-identical to yesterday — no coverage
regression, the 2026-04-27 ROADMAP-pinned per-game parse-rate matrix
remains authoritative until a fresh sweep is scheduled.
