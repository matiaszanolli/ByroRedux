# FO4 Compatibility Audit — 2026-06-18

**Branch**: main @ 2aac5351
**Auditor**: Multi-agent /audit-fo4 (Opus 4.8 1M) — incremental delta sweep over the 2026-06-16 FO4 commits + standard dimension regression guards.
**Scope**: Focused on the four recently-churned FO4 areas — `efd3c41b` (BGSM flags on shared-precombine meshes), `022cac83` (precombine decals → opaque-black fix), `2aac5351` (DXT1/BC1 → BC1_RGBA for 1-bit punch-through alpha), `067a8354` (additive particle blend-enum fix) — plus regression checks on M49 precombine CSG, BGSM/BGEM consumption, BA2 reader, and NIFAL material translation.
**Baseline**: This audit is a delta on `docs/audits/AUDIT_FO4_2026-06-14.md` (the last full 9-dimension sweep, which found 0 CRITICAL · 0 HIGH · 7 MEDIUM · 7 LOW). Several of those findings are now CLOSED (see "Prior-Finding Reconciliation").

---

## Executive Summary

The FO4 pipeline remains structurally sound. The three precombine/material commits from 2026-06-16 are **correct and well-scoped** — the BGSM-merge wiring onto the shared-precombine path, the `is_decal` propagation, and the BC1 punch-through-alpha decode all do what they claim, and the precombine pipeline (CSG decode → spawn → material merge → translate) holds together end-to-end (verified borrow scoping, caching order, flag precedence, decal escalation, `resolve_pbr` idempotency).

However, the audit surfaced **one genuine HIGH** correctness bug. The 2026-06-16 particle fix (`067a8354` / #1649) corrected a Gamebryo-vs-Vulkan blend-enum confusion on the *particle* path — but **the identical bug class still lives on the BGSM/BGEM material path**, and the `efd3c41b` precombine wiring just widened its blast radius. `merge_bgsm_into_mesh` forwards the BGSM **GL-style** src/dst blend enum *verbatim* into the **Gamebryo NiAlphaProperty** enum fields the renderer consumes. The two enums are inverted at exactly values 0 and 1, so **additive BGEM effect/glow cards `(One, One)` are reinterpreted as `(Zero, Zero)` and render invisible/black** instead of blooming. The "Standard" alpha mode `(6,7)` coincides between the enums, which is why this stayed hidden — and a unit test currently *pins the wrong behavior*.

Secondary: the BC1 → BC1_RGBA decode change is safe for opaque content (full gate chain verified) but introduces a bounded MEDIUM regression risk on the pure-alpha-blend path, where a BC1 diffuse encoded with legal 3-colour blocks can now trip the implicit-discard.

| Area | State |
|------|-------|
| M49 CSG precombine (decode + spawn + BGSM merge + decal) | PASS — recent commits correct; index range-check now landed (#1533) |
| BC1/DXT1 → BC1_RGBA punch-through alpha (`2aac5351`) | PASS for opaque; one bounded MEDIUM on the alpha-blend path |
| BGSM/BGEM blend-enum forwarding | **FAIL — HIGH, additive BGEM cards invert to (Zero,Zero)** |
| Additive particle blend fix (`067a8354` / #1649) | PASS — presets + batch key + enum mapping all correct |
| BGSM metalness / smoothness / template resolve | PASS — #1476 + #1591 intact, idempotent |
| BA2 reader (exhaustive match, DX10 recon) | PASS — DXGI 88 pitch now landed (#1596) |
| NIFAL precombine → translate_material → GpuMaterial | PASS — flags reach GPU, resolve-once held |

**New actionable findings**: **0 CRITICAL · 1 HIGH · 1 MEDIUM · 0 LOW** (excluding INFO and findings deduped/closed against the 2026-06-14 sweep).

**Most severe**: FO4-D2-HIGH-01 — BGSM/BGEM blend factors forwarded verbatim from a GL-style enum into Gamebryo-enum fields; additive effect/glow cards render invisible. Same bug class as the just-fixed #1649 particle path, now reaching both REFR and precombine spawns.

---

## Dimension Findings

### Dimension 2: BGSM / BGEM Consumption — 1 HIGH

#### FO4-D2-HIGH-01: BGSM/BGEM blend factors forwarded with an inverted enum — additive effect/glow cards render invisible
- **Severity**: HIGH
- **Dimension**: BGSM/BGEM Consumption (parser → Material boundary; format-translation invariant)
- **Location**: `byroredux/src/asset_provider.rs:1436-1439` (BGSM), `byroredux/src/asset_provider.rs:1529-1532` (BGEM); comment `byroredux/src/asset_provider.rs:1431-1434`; enum source `crates/bgsm/src/base.rs:46-47`; renderer consumer `crates/renderer/src/vulkan/pipeline.rs:108-123`; wrong-behavior test `byroredux/src/asset_provider.rs:2398-2413`
- **Status**: NEW
- **Description**: `merge_bgsm_into_mesh` forwards the BGSM/BGEM `alpha_blend_mode.src_blend`/`dst_blend` **verbatim** (`as u8`) into `mesh.src_blend_mode`/`dst_blend_mode`. But the BGSM field is documented as a **GL-style enum** (`base.rs:46`: `Zero=0, One=1, SrcColor=2, SrcAlpha=6, InvSrcAlpha=7`), whereas the renderer's `gamebryo_to_vk_blend_factor` (`pipeline.rs:110-111`) reads `mesh.*_blend_mode` as the **Gamebryo NiAlphaProperty** enum (`0=ONE, 1=ZERO`, then 2..=10 follow the D3D ordering). The two enums are **inverted exactly at values 0 and 1**. The in-code comment at `:1431-1434` claims they "align 1:1" and lists "0=Zero, 1=One" — which is the GL table, contradicting the renderer's mapping. This is the identical bug class fixed for the *particle* presets in #1649 (`067a8354`), still present on the *material* path.
- **Evidence**:
  - BGSM enum (GL): `crates/bgsm/src/base.rs:46` — `src_blend / dst_blend: GL-style enum (Zero=0, One=1, ...)`.
  - Renderer enum (Gamebryo): `crates/renderer/src/vulkan/pipeline.rs:110-111` — `0 => ONE, 1 => ZERO`. Cross-checked against the NIF walker which writes `0 // ONE` into the same `*_blend_mode` fields (`crates/nif/src/import/material/walker.rs:508-509`).
  - Verbatim forward, no conversion: `asset_provider.rs:1438-1439`, `:1531-1532`.
  - Downstream chain (confirmed live on REFR + precombine static-mesh paths): `mesh.dst_blend_mode` → `AlphaBlend{src,dst}` (`spawn.rs:953-954`) → `DrawCommand.dst_blend` (`render/static_meshes.rs:183-184, 550-551`) → `gamebryo_to_vk_blend_factor`.
  - **Test pins the wrong behavior**: `asset_provider.rs:2398-2413` ("additive blend (function=2) with One/One factors — common on FO4 effect / glow card BGEMs") asserts `src==1, dst==1` are forwarded unchanged. Under the renderer's enum that is `(ZERO, ZERO)` → the surface contributes nothing to the framebuffer.
- **Impact**: Cross-game (every game whose materials go through BGSM/BGEM — FO4, FO76, Starfield) and now widened to the FO4 precombine path by `efd3c41b`. The common "Standard" mode `(6,7)` is unaffected (6/7 coincide), which masks the bug for the bulk of glass/decal content — but **additive** BGEM glow/effect/holotape/screen cards (`function=2`, `(One, One)` = `(1,1)`) invert to `(ZERO, ZERO)` and render black/invisible instead of additive, and any BGSM authoring src/dst `0` (Zero) or `1` (One) is corrupted. Bounded to the additive/Zero/One blend population, but the failure is "visible FX surface disappears," not a subtle shift.
- **Related**: #1649 / `067a8354` (the particle-side twin, fixed by flipping presets to `dst_blend:0`); `efd3c41b` (routed the precombine path through this merge, widening exposure). Project invariant: *Format Translation Layer — translate at the parser→Material boundary, never forward a foreign enum.*
- **Suggested Fix**: Convert the BGSM/BGEM GL-style src/dst into the Gamebryo NiAlphaProperty enum at the merge boundary (swap only `0↔1`; values 2..=10 coincide) — e.g. a `gl_to_gamebryo_blend(u32) -> u8` helper applied at `:1438-1439` and `:1531-1532`. Fix the misleading comment at `:1431-1434` and update the test at `:2398-2413` to assert the *converted* `(One,One)` → Gamebryo `(0,0)` (which maps to VK `ONE/ONE` = additive). Mirror the #1649 fix's "renderer speaks the Gamebryo enum" discipline.

---

### Dimension 3 (cross-cutting): BC1/DXT1 → BC1_RGBA decode — 1 MEDIUM

#### FO4-XCUT-MEDIUM-01: Alpha-blend-flagged BC1 meshes with 3-colour-block diffuse now spuriously discard texels
- **Severity**: MEDIUM
- **Dimension**: BA2/DDS Reader → Renderer (cross-cutting)
- **Location**: `crates/renderer/shaders/triangle.frag:223` interacting with the new decode at `crates/renderer/src/vulkan/dds.rs:322` (FourCC DXT1) and `crates/renderer/src/vulkan/dds.rs:361` (DXGI BC1_UNORM/_SRGB)
- **Status**: NEW (introduced by `2aac5351`)
- **Description**: Commit `2aac5351` decodes BC1/DXT1 as `BC1_RGBA_SRGB_BLOCK` instead of `BC1_RGB_SRGB_BLOCK` so the 1-bit punch-through alpha (3-colour block mode, `color0 <= color1`, index-3 = transparent) reaches the shader — required for FO4 alpha-test cutouts. The "opaque content unchanged" claim is **verified correct** (see Verification below). But BC1's 3-colour mode is a legal *RGB-fidelity* encoder choice, not necessarily intent-to-be-transparent. For a mesh flagged `INSTANCE_FLAG_ALPHA_BLEND` with `alphaThreshold == 0` (the FNV picture/table case explicitly noted at `triangle.frag:216-222`) whose BC1 diffuse uses 3-colour blocks in opaque regions, those texels now sample `a == 0.0` and trip the implicit blend-discard at `triangle.frag:223` (`texColor.a < 1/255`). Before this commit that branch was unreachable for any BC1 mesh because BC1_RGB pinned alpha to 1.0.
- **Evidence**: Line 223 condition `ALPHA_BLEND && aThresh==0 && texColor.a < 1/255` — pre-change BC1 alpha was constant 1.0 → never true; post-change a 3-colour index-3 texel yields 0.0 → discards. `INSTANCE_FLAG_ALPHA_BLEND` derives from the NIF `NiAlphaProperty` blend bit (`render/static_meshes.rs:182,549`), entirely independent of texture encoding. The same texels also feed `decalWeight` (`triangle.frag:1580`, `smoothstep` → 0) and `finalAlpha` (`:2562`) for glass-classified blend meshes.
- **Impact**: Bounded, visual, cross-game (single DDS decode path serves all games). Affects only meshes that are *both* alpha-blend-flagged *and* textured with a BC1 diffuse an encoder authored with 3-colour blocks in opaque regions. Bethesda blend surfaces are predominantly BC3/DXT5 or BC1 4-colour, so corpus exposure is small but non-zero; symptom is pinhole/speckle dropout on blended surfaces (the inverse of the FO4 bug this commit fixed). The opaque majority is unaffected.
- **Related**: `2aac5351`; implicit-discard rationale at `triangle.frag:216-222` (#263 era). Distinct from the 2026-06-14 FO4-XCUT-MEDIUM-01 (DXGI 88, different issue, now landed via #1596).
- **Suggested Fix**: Plumb a per-material `diffuseHasPunchAlpha` bit set CPU-side only when the source material is genuinely cutout (NIF/BGSM alpha-test flag set), and require it in the line-223 implicit-discard condition — confining the new behavior to intended cutout content. This mirrors the existing `format_has_alpha`/`NORMAL_ALPHA_SPEC_BIT` plumbing pattern. Lower-effort interim: validate on real FNV/FO3 framed-picture interiors (`--esm FalloutNV.esm --cell <gallery interior> --bench-hold`) and only invest if speckle appears.

---

## Verified-Correct (recent commits — regression guards held)

These are NOT findings — they document the disproof work so the next audit can skip re-deriving it.

- **`efd3c41b` BGSM-merge on shared-precombine** — borrow scoping is clean (`csg` resolved to `Arc` before the loop; merge borrows `StringPool` + `mat_provider` from disjoint sources, no double-borrow); merge runs per-mesh *before* `geometry_only_cached` so cached entries carry merged flags and re-enter doesn't double-merge; precedence is correct (NIF material `apply()` first, then BGSM OR-escalates flags / fill-only textures, identical to the REFR path); `merge_bgsm_into_mesh` no-ops for meshes without `material_path`. `byroredux/src/cell_loader/precombined.rs:169-205`.
- **`022cac83` precombine decals** — `PrecombineMaterial.is_decal` (`precombine.rs:214`) → `apply()` stamps `mesh.is_decal` (`:232`) → sourced from `mat.is_decal` (`:341`); the BGSM merge only OR-escalates it; `spawn.rs:996` `render_layer_with_decal_escalation` promotes the explicit `Architecture` base to `Decal`. End-to-end correct; `apply_propagates_is_decal` passes.
- **`2aac5351` BC1 opaque-path safety** — opaque main pipeline `blend_enable(false)` on all 6 attachments (`pipeline.rs:278-288`); alpha-test discard gated `aThresh>0` (`triangle.frag:201`); implicit discard gated on `ALPHA_BLEND` (`:223`); glass/window/decalWeight/finalAlpha gated on CPU `materialKind`/`ALPHA_BLEND`; gloss-in-alpha reads the *normal/gloss* map's alpha, never the BC1 diffuse, and `format_has_alpha` (`dds.rs:68`) correctly excludes both BC1 variants so a BC1 can never be sampled as gloss. `block_size` stays 8; mip/total-size math keys on `block_size` not the format enum, so staging/mip sizing is byte-identical. BA2 DX10 recon needs no matching edit (it emits dxgi codes that flow through `map_dxgi_format`).
- **`067a8354` / #1649 additive particles** — `gamebryo_to_vk_blend_factor` maps `0→ONE, 1→ZERO` (`pipeline.rs:110-111`); the four additive presets now `dst_blend:0` with `src_blend:6` (`crates/core/src/ecs/components/particle.rs:299,333,405,432`); smoke preset correctly stays `dst_blend:7`. The batch-merge key embeds both factors (`PipelineKey::Blended{src,dst,wireframe}`, `context/draw.rs:2009-2014`) so additive (dst=0) and alpha-over (dst=7) emitters cannot merge into one instanced draw.
- **NIFAL precombine → GPU** — precombine meshes route through the single `translate_material` at `spawn.rs:857`; `pack_bgsm_material_flags` → `Material.effect_shader_flags` → `GpuMaterial.material_flags` (PBR_BSDF/TRANSLUCENCY/MODEL_SPACE_NORMALS reach the GPU); `resolve_pbr` idempotent, smoothness→roughness applied exactly once at merge (before caching), no draw-time reclassification.

---

## Prior-Finding Reconciliation (2026-06-14 sweep)

| 2026-06-14 ID | Then | Now |
|---|---|---|
| FO4-D1-LOW-01 (precombine index range-check) | NEW LOW | **FIXED** — `decode_shared_geom_object` rejects `index >= num_verts` (`precombine.rs:156-165`, #1533); test `out_of_range_triangle_index_is_rejected` passes |
| FO4-D2-MEDIUM-01 (conductor tint folds in `specular_mult`) | NEW MEDIUM | **FIXED** — #1591 / `50666bbb`; blends toward mult-free `leaf.specular_color` (`asset_provider.rs:1229`), test `conductor_tint_is_mult_free` |
| FO4-D3-LOW-01 (DXGI 88 absent from BA2 pitch table) | NEW LOW | **FIXED** — #1596; `88` added to 4-bpp arm (`ba2.rs:944`) with regression test |
| FO4-D1-MED-01 (DLC wrong-CSG / remapped form-id) | NEW MEDIUM | **OPEN as #1590** — unaddressed by the 2026-06-16 commits; not re-reported |
| FO4-D5-MEDIUM-01 (shader-flag bits unconsumed) | NEW MEDIUM | **OPEN as #1592** — consumers partly shipped (Phase 2b, `cell_loader.rs:186-190`); not re-reported |
| FO4-D9-MEDIUM-02 (BSConnectPoint unconsumed) | NEW MEDIUM | **OPEN as #1594** — unchanged; not re-reported |
| FO4-D6-MEDIUM-01 (CONT unqueried) | Existing #1359 | Still open; not re-reported |
| FO4-D7-LOW-01 (PBR_BSDF "dead gate" doc rot) | NEW LOW | Not re-verified this sweep (doc-only) |

#1476 metalness-from-saturation, the exhaustive BA2 version match, and the BGSM template cycle-break + `DEPTH_LIMIT=16` are all confirmed intact (no reversion).

---

## Test Status

- `cargo test -p byroredux-bgsm` → 27 unit + 2 doc pass, 0 fail (2 corpus tests need game data, ignored).
- `cargo test -p byroredux-bsa` → 53 pass, 0 fail (corpus/CSG tests need game data, ignored).
- dds.rs suite → 23 pass.
- Precombine test set → 5/5 pass.
- `cargo check -p byroredux` clean.

---

## Forward Scope Chain (unchanged from 2026-06-14 — do NOT re-file as blockers)

1. Precombine collision (`_precomb.nif`) + `.uvd` occlusion volumes.
2. Record-consumer arc — CONT (#1359), BSConnectPoint (#1594), MOVS (#1598): parsed, unconsumed.
3. OMOD / material-swap assembly (#973), depends on (2).
4. Deeper cell coverage — LIGH power state, CONT leveled items, NPC_ face morph.
5. DLC/multi-master CSG resolution by `filename_hash` (#1590).

> The M49 CSG reader, BGSM/BGEM parser, and SCOL/PKIN expansion are all shipped — never list them as pending.

---

## Finding Index

| ID | Severity | Status | Title |
|----|----------|--------|-------|
| FO4-D2-HIGH-01 | HIGH | NEW | BGSM/BGEM blend factors forwarded with inverted enum — additive effect cards render invisible |
| FO4-XCUT-MEDIUM-01 | MEDIUM | NEW | Alpha-blend BC1 meshes with 3-colour-block diffuse spuriously discard texels (BC1_RGBA decode side-effect) |

**Counts (NEW, excluding INFO/verified-correct):** 0 CRITICAL · 1 HIGH · 1 MEDIUM · 0 LOW · 2 TOTAL. Both NEW.

---

*Suggested next step:* `/audit-publish docs/audits/AUDIT_FO4_2026-06-18.md`
