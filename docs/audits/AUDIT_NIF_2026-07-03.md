# NIF Parser Audit — 2026-07-03

**Scope**: Default (general parser-correctness) sweep of `crates/nif/src/` across all six
`/audit-nif` dimensions — Stream Position, Version Gating, Block Dispatch Coverage, Geometry
Handoff, Collision/Shader Parsing, Allocation Hygiene. No `--game`/`--corpus` restriction
(one dimension additionally ran a live corpus spot-check as a bonus, see Dim 3).

**HEAD**: `8498e559` · **Baseline for delta review**: `1b4e8e84` (`docs/audits/AUDIT_NIF_2026-07-02.md`)
**Dedup source**: `gh issue list --repo matiaszanolli/ByroRedux --limit 200 --json number,title,state,labels` (71 open issues fetched), cross-checked against all prior `docs/audits/AUDIT_NIF_*.md` (27+ reports) and `docs/audits/AUDIT_NIFAL_*.md`.

## Executive Summary

**Zero new findings across all six dimensions.** This is the second consecutive clean sweep
(2026-07-02 also found nothing new in Dim 2; today extends that result to all six dimensions).
The NIF parser and its parse→import handoff are in a stable, heavily-instrumented state: nearly
every non-obvious line already carries an issue-number comment and a defensive guard from prior
audit cycles.

Two commits landed in `crates/nif/`/`byroredux/` territory since yesterday's audit and had not
yet been through a full dimension-by-dimension pass — both were independently re-derived and
verified sound by more than one reviewer in this cycle:

- **`ba728882`** (Fix #1828/#1829 — keep iterating past sentinel `BSGeometry` mesh slots,
  `crates/nif/src/import/mesh/bs_geometry.rs`): both Stage A (inline) and Stage B (external
  `.mesh`) slot-acceptance guards now match the function's final guard exactly; all-sentinel and
  sentinel-first-ordering edge cases are covered by 4 new tests, all passing. No stream-position,
  dispatch, or allocation implication (it operates entirely on already-decoded data).
- **`61b8cf7f`** (Fix #1333 — retain `NiParticleSystem` local transform, composed into both
  emitter walkers, `crates/nif/src/blocks/particle.rs` + `crates/nif/src/import/walk/mod.rs` +
  `byroredux/src/scene/nif_loader.rs`): the coordinate-conversion order-of-operations was
  specifically checked (this is the exact bug class the project has hit before) — the flat
  walker composes in Z-up space then converts once; the hierarchical walker converts each
  transform independently and relies on ECS `GlobalTransform` propagation to compose, which is
  mathematically valid because `zup_matrix_to_yup_quat` implements a true conjugation
  (`R_yup = C·R_zup·Cᵀ`), not a naive per-field remap. Both paths verified equivalent.

Per-game clean/recoverable rates are unchanged from `docs/engine/nif-parser.md` §"Per-game NIF
coverage" (last refreshed 2026-04): Oblivion 96.24%/99.99% recoverable, FO3 100%, FNV 100%,
Skyrim SE 100%, FO4 96.46%/100% recoverable, FO76 97.34%/100% recoverable, Starfield 98.6%
aggregate/100% recoverable. Dimension 3's fresh corpus spot-check (below) independently
corroborates 100% block-dispatch coverage on four of these archives with zero uncovered types
and zero sizeless-cascade risk.

Severity counts this cycle: **CRITICAL 0 · HIGH 0 · MEDIUM 0 · LOW 0** (no new findings to
grade). Existing open issues re-confirmed accurate (not counted as new): 6 (see below).

## Method note on this run

All six dimensions were run as independent passes (five completed as background sub-agents;
Dimension 3 — and the initial re-verification of a subset of Dimension 1/2's claims — was
finished by direct inline source review after a mid-run status check found the orchestrator's
wait loop had lost track of live agent state). Every dimension independently re-read the current
source rather than trusting prior reports' text, per the audit's "be skeptical" methodology; where
two reviewers touched overlapping ground (e.g. Dim 1 and Dim 4 both examined `ba728882`; Dim 2 and
Dim 5 both examined the Starfield stub-discriminator lockstep), their conclusions agreed.

## Existing Open Issues Re-Verified (not new; not double-counted)

All six were checked against **current source**, not just re-cited from the prior report:

| Issue | Title | Dimension | Status this cycle |
|---|---|---|---|
| #1838 (NIF-D2-01, MEDIUM) | `#1277` refactor reverted `#982` fix — variant-helper gates reintroduced at both `tri_shape` sites | Version Gating | Confirmed unchanged, still open, still accurately described |
| #1839 (NIF-D2-02, LOW) | MOPP Build-Type / `BsMultiBoundNode` culling-mode gate helper substitutions | Version Gating | Confirmed unchanged |
| #1840 (NIF-D2-03, LOW) | Four call-site-less `NifVariant` helpers survive prunings | Version Gating | Confirmed still zero production call sites |
| #1841 (NIF-D3-01, LOW) | 5 of 7 per-block baseline TSVs stale (opt-in regression gate false-positive) | Block Dispatch Coverage | Re-verified via git-blame dates on all 7 TSVs; still accurate |
| #1842 (NIF-D2-04, LOW) | `FLAGS_U32_THRESHOLD` doc cites nonexistent nif.xml token | Version Gating | Confirmed unchanged |
| #1843 (NIF-D1-01, LOW) | Pre-4.1 NIF bool fields read as 1 byte (Morrowind-era, out of tier) | Stream Position | Confirmed unchanged, out-of-tier, no regression |
| #1850 (FNV-D7-02, MEDIUM) | `bhkBreakableConstraint` invisible to ragdoll graph — silent edge drop | Collision/Shader | Explicitly re-checked against `db2c3a72` (Fix #1539/#1540) — **not** fixed by that commit, still stands exactly as filed |
| #1851 (FNV-D7-03, LOW) | Real-data ragdoll test doesn't pin FNV body/joint counts | Collision/Shader | Explicitly re-checked — still stands |

No same-day fix landed for any of these; all remain open and continue to accurately describe
current code.

## Per-Dimension Findings

### Dimension 1: Stream Position Integrity
**Result: no new findings.** Every commit touching `crates/nif/src/` since 2026-07-02 was
individually re-verified for stream-consumption correctness: `6a6b89b9` (`#1672` three-phase
`parse_nif_with_options` split) is byte-for-byte behavior-preserving; `3863fe40` (`#1750`
`NiDynamicEffectData` consolidation) reproduces both former inline copies' gates and reads
verbatim; `2b780a2c`/`61b8cf7f`/`05f3342b`/`73592e33`/`fe1d5b1b` are import/walker-layer changes
with zero stream-read impact; `f9cc691b` is an ESM (not NIF) record fix, out of crate scope. All
named regression guards (Oblivion v10.x truncation family, `#1509`/`#1337`/`#1329`/`#1310`/
`#1301`/`#1302`, Starfield `BSLightingShaderProperty` over-read/tail `#1510`/`#1606`, root-NiNode
truncation `#688`/`#698`) re-verified GREEN. `#1843` (pre-4.1 bool width) re-confirmed open,
unchanged, out-of-tier.

A candidate concern was raised and **disproven**: whether `ba728882`'s sentinel-slot fix implied
a stream-position bug in `BSGeometryMeshData::parse`'s `scale <= 0.0` early return. Refuted —
`BSGeometryMeshData` is parsed either as a whole standalone `.mesh` buffer (no `block_sizes`
concept applies) or inline where the sentinel's short read is the correct, complete consumption
for that slot (nothing follows it to misread).

### Dimension 2: Version Gating
**Result: no new findings.** The only commit touching `crates/nif/` since 2026-07-02
(`ba728882`) does not touch `version.rs`, `shader_flags.rs`, or any `bsver`/`variant()` gate — it
is Dimension 4 territory. `NifVariant::detect` test coverage (all seven shipping games + the
FO3-dev edge case) unchanged. Five named regression-guard commits (`0d009cf7` `#1721`, `dc33ec7d`
`#1652`, `497700e7` `#1606`, `3863fe40` `#1750`, `f7fbbed5` `#1592`) all spot-checked intact.

### Dimension 3: Block Dispatch Coverage
**Result: no new findings.** `ba728882` confirmed to have zero Dim-3 implication (post-parse
slot selection only, no `parse_block` match-arm or `impl_ni_object!` change). Fresh dispatch-arm
count from source: **249 top-level match arms / 313 distinct type-name literals** in
`parse_block_inner` — independently reproduces the same figure `AUDIT_NIF_2026-07-01.md` already
filed as a non-finding against `docs/engine/nif-parser.md`'s self-disclaimed "254/310" figure (a
doc that explicitly says "verify the live count from source — it grows"); not re-filed. `#1841`
re-verified via TSV git-blame dates, still accurate. A structural orphan check (185
`NiObject`-implementing types across 25 submodules cross-referenced against the dispatch match)
found **zero orphans** — every typed parser is reachable.

**Bonus**: a live-corpus spot-check (`d5_coverage` example, `#1335`-era live-dispatch probe) was
run against one real archive per game as an independent, non-doc-derived data point:

| Game | Archive | NIFs | Block instances | Distinct types | Uncovered | Sizeless-cascade risk |
|---|---|---|---|---|---|---|
| Oblivion | `Oblivion - Meshes.bsa` | 8,032 | 329,889 | 119 | 0 | 8,032 sizeless files, 0 cascades |
| Skyrim SE | `Skyrim - Meshes1.bsa` | 3,185 | 92,887 | 125 | 0 | 0 sizeless |
| Fallout NV | `Fallout - Meshes.bsa` | 14,881 | 492,796 | 150 | 0 | 1 sizeless file, 0 cascades |
| Starfield | `Starfield - Meshes02.ba2` | 7,552 | 9,740 | 2 | 0 | 0 sizeless |

100% instance coverage on every archive checked, corroborating that #1841's staleness is confined
to the per-type TSV *histogram-key shape* (the `#1345` typed-promotion moved
`BSPSysSimpleColorModifier` out of the opaque `NiPSysBlock` aggregate), not actual dispatch
coverage.

### Dimension 4: Geometry Extraction & Import Handoff
**Result: no new findings.** Both flagged recent commits verified sound in detail:

- `ba728882`: cross-checked against the actual on-disk sentinel semantics in
  `crates/nif/src/blocks/bs_geometry.rs::BSGeometryMeshData::parse` (triangles are read *before*
  the `scale <= 0.0` sentinel check, so a sentinel slot always has non-empty `triangles` + empty
  `vertices` — the format has no complementary sentinel shape). All-sentinel and
  sentinel-first-ordering edge cases covered by new tests; all pass.
- `61b8cf7f`: order-of-operations for the Z-up→Y-up conversion relative to transform composition
  was the specific risk checked (per this project's history of coordinate-conversion bugs) —
  both the flat walker (compose-in-Z-up-then-convert-once) and the hierarchical walker
  (convert-independently-then-let-ECS-compose-in-Y-up) were confirmed mathematically equivalent,
  because the conversion is implemented as a genuine similarity/conjugation. `compose_transforms`
  uses the standard scene-graph TRS composition formula in both paths — no divergence. The
  `nif_loader.rs` identity-offset fast path (zero-cost attach for the common vanilla case) and the
  intentional non-handling of `BSMasterParticleSystem`'s second discarded transform (resolves to
  the opaque `NiPSysBlock`, never matched by either walker) were both confirmed correct.

Remaining Dim-4 entry points (`skin.rs`, `sse_recon.rs`, `tangent.rs`, `ni_tri_shape.rs`,
`bs_tri_shape.rs`, `material/walker.rs`) were swept for anything not already tracked by the open
issue list (#1855, #1854, #1830, #1827, #1826, #1659) — none found; no stray `.unwrap()`/
`.expect()` or `TODO`/`FIXME` markers in production code under these paths.

### Dimension 5: Collision & Shader Block Parsing
**Result: no new findings.** Six recent commits in this dimension's territory were independently
re-derived line-by-line against their stated fix intent, with non-tautological regression tests
confirmed for each:

- `dc33ec7d` (`#1652`) — `havok_motion_type` full canonical enum mapping cross-checked against
  the actual Havok `hkpMotion::MotionType` value ordering; exact match.
- `2b780a2c` (`#1777`) — genuinely per-axis (not uniform) Scale fold on `bhkPackedNiTriStripsShape`,
  confirmed with a distinct-xyz-scale-factor test.
- `39992b32` (`#1744`) — confirmed architecturally sound rather than an overcorrection:
  `bhkNiTriStripsShape`/`bhkMeshShape` correctly excluded from `havok_scale` (they share the
  render-mesh `NiTriStripsData`, already in game units) while primitive/packed shapes that ARE in
  genuine Havok units still correctly receive it.
- `db2c3a72` (`#1539`/`#1540`) — dropped-constraint logging and trimesh→convex-hull inertia fix
  both verified real (non-cosmetic) behavior changes; **explicitly re-confirmed this commit does
  NOT fix #1850 or #1851**, which both stand exactly as filed (`BhkBreakableConstraint` is a
  distinct struct never matched by the ragdoll constraint loop's downcast, so it still falls
  through with zero logging, unlike the `Other`-arm bhk* types which do warn).
- `0d009cf7` (`#1721`) — `BSEffectShaderProperty`/`BSLightingShaderProperty::parse_fo76_plus`
  Starfield stub-discriminator lockstep confirmed identical on both sides, with cross-referencing
  comments and a real (non-smoke-test) assertion on stub-only byte consumption.
- `497700e7`/`f7fbbed5` (`#1606`/`#1592`) — Starfield tail capture still reads to `block_size`;
  FO4 model-space-normals/alpha-test flag consumption in `walker.rs` intact.

All other Dim-5 checklist items (rigid-body flag width, MOPP/Havok-strips-scale gates,
per-game constraint `CInfo` confinement, `CollisionAuthoring` discriminator, `extract_collision`
shape coverage, per-variant shader field isolation) verified intact with no drift.

### Dimension 6: Allocation Hygiene (PERF)
**Result: no new findings.** All six checklist items independently re-verified against current
source: `#[must_use]` present on `allocate_vec`/`read_pod_vec`/all `read_*_array` wrappers;
`bytemuck` confirmed genuinely absent from every workspace `Cargo.toml` (the `AnyBitPattern`
bound is a crate-local trait); per-block counters still use the `get_mut()`/`insert` split
(`#832`); `ragdoll.rs` still uses `allocate_vec`, not `check_alloc`; `Arc<str>` block-name
interning and `pre_parse_cell`'s two-phase serial/rayon pipeline (with the `PRE_PARSE_RAYON_MIN`
serial fast path) both intact. Both flagged recent commits (`ba728882`, `61b8cf7f`) confirmed to
introduce no new clone/reallocation patterns. **TD9-001 (#1763)** re-confirmed accurate:
`.github/workflows/ci.yml` never passes `--features dhat-heap`, so the two dhat-gated
heap-allocation-bounds test files still never execute in CI.

## Block Type Coverage Matrix

See `docs/engine/nif-parser.md` §"Block coverage" for the full per-family type listing (this
audit's fresh recount — 249 arms / 313 literals — confirms that table's figures are close, with
only self-disclosed drift). See Dimension 3 above for the fresh live-corpus per-game
instance/type coverage table (Oblivion/Skyrim SE/Fallout NV/Starfield, all 100% covered, zero
uncovered types).

## Prioritized Fix Order

No new findings this cycle, so there is nothing to prioritize from this report. Carried-forward
priority order from the existing open issues most relevant to this domain (unchanged from
2026-07-02, re-verified still applicable):

1. **#1838 (NIF-D2-01, MEDIUM)** — restore raw-bsver reads at the two `tri_shape` variant-helper
   sites (the only substantive open finding in this domain with a functional-correctness angle).
2. **#1850 (FNV-D7-02, MEDIUM)** — surface `bhkBreakableConstraint` drops in the ragdoll
   constraint loop (silent today; the sibling `Other`-arm path already logs, so this is a
   straightforward parity fix).
3. **#1839 / #1840 / #1842 / #1841 / #1843 / #1851 (LOW)** — cleanup/doc/test-infra items, no
   urgency.

## Conclusion

This is a saturated audit surface: six consecutive dimension passes, five of them on fresh
material (the two most-recently-landed commits) and one wholly independent (a live corpus
coverage spot-check), converged on the same result — the NIF parser, its version gating, its
block dispatch, its geometry/import handoff, its collision/shader parsing, and its allocation
discipline are all currently sound, with no undiscovered regressions from the last 48 hours of
commits. The eight open issues carried forward from prior cycles remain the accurate and
complete list of known gaps in this domain.

---

*Generated by `/audit-nif` on 2026-07-03. Next step:*
`/audit-publish docs/audits/AUDIT_NIF_2026-07-03.md`
