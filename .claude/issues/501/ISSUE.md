{"body":"## Severity: MEDIUM (latent; blocks future parallel scheduler)\n\n**Location**: `byroredux/src/render.rs::build_render_data`\n\n## Problem\n\n`build_render_data` opens ~15 `QueryRead` guards (MeshHandle, GlobalTransform,\nAnimatedVisibility/Alpha/Color, AlphaBlend, TwoSided, Decal, Parent, Name,\nSkin, FormIdComponent, LightSource, Camera, plus marker queries) and holds\nthem all live across the outer `for (e, mh) in mesh_q.iter()` loop. Each\nguard is an `RwLockReadGuard` on a distinct `TypeId`-keyed storage, so no\nlock contention exists today. But the scheduler is parallel-ready per\nCLAUDE.md architecture invariants — once any system runs concurrently with\nrender-data build, any write to any of those 15 storages will block for the\nentire ~1.5–2 ms build window.\n\n## Impact\n\n- **Today** (single-threaded): 0 ns extra.\n- **Future** (M40 parallel scheduler): up to 2 ms stall per frame if any\n  parallel system writes Transform / MeshHandle / AnimatedAlpha mid-build.\n\n## Suggested Fix\n\nIntroduce a `RenderExtract` stage that snapshots\n`(entity, GlobalTransform, MeshHandle, material_flags, animated_overrides)`\ninto a `Vec<RenderInstance>` resource in one pass, then `build_render_data`\niterates the Vec with zero locks held. Equivalent to Bevy's extract stage.\n\nDefer until M40 parallel scheduler. File now so the constraint is captured\nbefore the parallel migration.\n\n## Completeness Checks\n\n- [ ] **LOCK_ORDER**: Extract stage must acquire storages in TypeId-sorted\n      order (see `world.rs::query_2_mut*`)\n- [ ] **TESTS**: Equivalence test — rendered output of\n      `build_render_data(world)` must equal\n      `build_render_data(extracted_snapshot)` on a reference scene\n- [ ] **PERF**: Measure extract overhead (~0.5 ms/frame budget) before\n      committing to the pattern\n\n## Source\n\nAudit: `docs/audits/AUDIT_PERFORMANCE_2026-04-20.md` (D4-M1)\nRelated: #287 (scheduler), #249 (transform dirty bit), #46 (transform\npropagation lock reduction — same-spirit, different site)","labels":[{"id":"LA_kwDORzgFWc8AAAACd30_8w","name":"ecs","description":"Entity Component System","color":"0e8a16"},{"id":"LA_kwDORzgFWc8AAAACeFy0Lg","name":"medium","description":"Medium severity","color":"FBCA04"},{"id":"LA_kwDORzgFWc8AAAACeGdDkw","name":"performance","description":"Performance optimization","color":"fbca04"}],"state":"OPEN","title":"PERF-2026-04-20 D4-M1: build_render_data holds 15 concurrent read locks — blocks parallel scheduler"}
