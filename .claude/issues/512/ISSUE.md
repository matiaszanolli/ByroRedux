{"body":"## Severity: MEDIUM (ENHANCEMENT — measurable cell-load win)\n\n**Location**: `byroredux/src/scene.rs::load_nif_*`,\n`byroredux/src/cell_loader.rs::load_cell`, `crates/core/src/ecs/world.rs::insert`\n\n## Problem\n\nNIF scene import + cell load inserts components one entity at a time via\n`world.insert(entity, Transform { .. })`. Each insert takes a write lock on\nthe target storage. For a 5000-entity cell load inserting\n~5 components per entity, that's **25,000 write-lock acquire/release\npairs** during load.\n\nNot a frame-time issue (load is off critical path), but dominates\ncell-load time: ~180 ms measured total in CLAUDE.md Session 12, of which\nan estimated ~30 ms is attributable to lock thrash across these serial\ninserts.\n\n## Impact\n\nCell-load time ~180 ms currently. ~16% attributable to this pattern.\nUser-visible pop-in delay on cell transition.\n\n## Suggested Fix\n\nAdd a batch-insert method to `World`:\n\n```rust\nimpl World {\n    pub fn insert_batch<T: Component>(\n        &mut self,\n        items: impl IntoIterator<Item = (EntityId, T)>,\n    ) {\n        // Single write-lock acquisition on T's storage; drain into items\n        // one guarded access.\n    }\n}\n```\n\nApply to scene import first (Transform + GlobalTransform + MeshHandle +\nTextureHandle + Parent insertions). Cell-loader follow-up wires the rest.\n\n## Completeness Checks\n\n- [ ] **LOCK_ORDER**: Batch insert takes one write lock per type; sequence\n      the batches in TypeId order to avoid deadlock with concurrent readers\n- [ ] **SIBLING**: `insert_batch` should work identically for SparseSet and\n      Packed storage backends\n- [ ] **TESTS**: Equivalence test — 5000 serial `insert` produces identical\n      World state to one `insert_batch` with the same items\n- [ ] **PERF**: Measure cell-load time before/after; expect ~30 ms drop\n\n## Source\n\nAudit: `docs/audits/AUDIT_PERFORMANCE_2026-04-20.md` (D4-E1)","labels":[{"id":"LA_kwDORzgFWc8AAAACdLDhYg","name":"enhancement","description":"New feature or request","color":"a2eeef"},{"id":"LA_kwDORzgFWc8AAAACd30_8w","name":"ecs","description":"Entity Component System","color":"0e8a16"},{"id":"LA_kwDORzgFWc8AAAACeFy0Lg","name":"medium","description":"Medium severity","color":"FBCA04"},{"id":"LA_kwDORzgFWc8AAAACeGdDkw","name":"performance","description":"Performance optimization","color":"fbca04"}],"state":"OPEN","title":"PERF-2026-04-20 D4-E1: Cell-load does per-entity single-component inserts — ~30 ms of lock thrash"}
