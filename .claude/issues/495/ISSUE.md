{"body":"## Severity: MEDIUM\n\n**Location**: `crates/renderer/src/vulkan/acceleration.rs:106, 206-212, 416-432, 659-675`\n\n## Problem\n\n`scratch_needs_growth` (line 207) is strictly monotonic — it returns `true`\nonly when the required size is **larger** than the current capacity. Once a\nbatched BLAS build encounters a mesh requiring e.g. 80 MB of scratch (Skyrim\ndraugr skeletons, FO4 LOD terrain, Starfield `Saturn.nif`), `blas_scratch_buffer`\nis pinned at 80 MB DEVICE_LOCAL for the rest of the process lifetime.\nEvicting the mesh via `drop_blas` / `evict_unused_blas` frees the BLAS but\nnever the scratch. Same pattern on the two per-frame `scratch_buffers[i]`\nused by the TLAS rebuild path.\n\n```rust\nfn scratch_needs_growth(current_capacity: Option<vk::DeviceSize>, required: vk::DeviceSize) -> bool {\n    match current_capacity {\n        Some(cap) => cap < required,   // ← never shrinks\n        None => true,\n    }\n}\n```\n\n## Impact\n\nAfter a single \"big\" cell load or NIF peek, ~80–200 MB of VRAM is permanently\nreserved for the rest of the session, even when the player is back in an\ninterior cell whose peak scratch requirement is <2 MB.\n\nOn the stated 6 GB RT minimum target, that's 3–5% of the entire VRAM budget\npermanently burned after one peek at a large mesh. Compounds across cell\ntransitions if the engine ever samples a 2000+-triangle BLAS once.\n\n## Suggested Fix\n\n1. After `evict_unused_blas` runs, compute the new maximum `build_scratch_size`\n   across the surviving BLAS map and shrink `blas_scratch_buffer` if current\n   capacity exceeds it by a threshold (e.g. 2× or >16 MB slack).\n2. Same for per-frame TLAS scratch — on a frame where the TLAS instance count\n   drops by >50% for `MAX_FRAMES_IN_FLIGHT` frames running, reallocate to the\n   new `build_scratch_size`.\n3. Simpler alternative: add a `shrink_scratch_to(&mut self)` method called\n   from `unload_cell` that forces the scratch down to the post-unload\n   minimum on a known \"big transition\" boundary.\n\n## Completeness Checks\n\n- [ ] **UNSAFE**: No new unsafe expected; shrink uses the existing `GpuBuffer::destroy` + recreate path\n- [ ] **SIBLING**: Apply the same shrink policy to both `blas_scratch_buffer` AND `scratch_buffers[i]` (TLAS path)\n- [ ] **DROP**: Verify the old `GpuBuffer` is destroyed before the new one is allocated (no leak)\n- [ ] **TESTS**: Add a test that builds a BLAS requiring 80 MB scratch, drops all BLAS, asserts scratch shrinks to <16 MB\n- [ ] **TIMING**: `VK_EXT_memory_budget` can expose whether VRAM is actually under pressure before shrinking\n\n## Source\n\nAudit: `docs/audits/AUDIT_PERFORMANCE_2026-04-20.md` (D2-M1)","labels":[{"id":"LA_kwDORzgFWc8AAAACdLDhWQ","name":"bug","description":"Something isn't working","color":"d73a4a"},{"id":"LA_kwDORzgFWc8AAAACd30_-Q","name":"renderer","description":"Vulkan renderer","color":"e99695"},{"id":"LA_kwDORzgFWc8AAAACeFy0Lg","name":"medium","description":"Medium severity","color":"FBCA04"},{"id":"LA_kwDORzgFWc8AAAACeFy0QA","name":"vulkan","description":"Vulkan API","color":"B60205"},{"id":"LA_kwDORzgFWc8AAAACeFy0YQ","name":"memory","description":"GPU/CPU memory","color":"C2E0C6"},{"id":"LA_kwDORzgFWc8AAAACeGdDkw","name":"performance","description":"Performance optimization","color":"fbca04"}],"state":"OPEN","title":"PERF-2026-04-20 D2-M1: BLAS scratch buffer never shrinks — 80–200 MB permanent VRAM pin"}
