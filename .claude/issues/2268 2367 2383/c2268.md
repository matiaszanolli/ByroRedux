Fixed in 7019eb84. Both shims deleted; nothing referenced them.

Confirmed the premise and then ran the discovery method across the whole file rather than only the two named, since the thing that made these invisible — `pub fn` suppresses rustc's dead-code lint — applies to every sibling equally:

    for fn in $(grep -oP '^pub fn \K\w+' crates/nif/src/blocks/particle.rs); do
      ... count call sites outside the definition ...
    done

That returns exactly `parse_color_modifier` and `parse_simple_color_modifier`. Every other `parse_*_modifier` in the file is reached from the block dispatcher, so the scope you filed was already the complete set — no widening needed, and worth recording so the next sweep doesn't redo it.

Also checked they were not re-exported (`blocks/mod.rs`, `lib.rs`) before deleting, so this is not a public-API change even nominally.

No test added: the deleted functions had no callers and no behaviour of their own — they forwarded to `NiPSysColorModifier::parse` / `BSPSysSimpleColorModifier::parse`, which the dispatcher calls directly and which keep their own coverage. A test asserting their absence would pin the deletion rather than anything a user can observe.
