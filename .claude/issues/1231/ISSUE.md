{"body":"**Severity**: LOW (coverage gap, not a correctness defect)\n**Dimension**: Material Table (R1)\n**Audit**: \\`docs/audits/AUDIT_RENDERER_2026-05-22_DIM14.md\\` → REN-D14-NEW-02\n**Related** (closed batch): #1050 / TD6-006 (\"composite (R1) has no per-pass golden\")\n\n## Finding\n\nNo automated regression test pins the R1 identity invariant: **rendering output for N copies of the same material must be byte-identical pre/post R1 dedup**.\n\nExisting coverage:\n\n| Test | Pins |\n|---|---|\n| \\`gpu_material_size_is_260_bytes\\` (\\`material.rs:727\\`) | struct size byte-pin |\n| \\`gpu_material_field_offsets_match_shader_contract\\` (\\`material.rs:810\\`) | 65 named-field offsets |\n| \\`material_hash_matches_gpu_material_field_hash\\` (\\`context/mod.rs:2578\\`) | hash == byte-equality |\n| \\`intern_by_hash_dedups_identical_materials\\` (\\`context/mod.rs:2596\\`) | two identical DrawCommands → same id |\n\n**Missing link**: rendering the resulting frame produces byte-identical pixels to the pre-R1 path.\n\n## Why this matters\n\nRisk surface is narrow — the hash equality already covers byte-equality of `GpuMaterial` itself, and the shader-side reads dereference an SSBO indexed by `material_id`. But a future edit to `to_gpu_material` (`context/mod.rs:320-407`) could silently change rendered output for materials that still dedup to the same id. The hash-equality contract doesn't catch:\n\n- Changes in `to_gpu_material` that emit semantically different but byte-equal `GpuMaterial`s\n- Shader-side regression in how the indexed read is consumed (`materials[id].field` interpretation)\n- Descriptor-set binding regression (the `ui.vert` MaterialBuffer offset bug was a recurring trap — pinned by `ui_vert_reads_texture_index_from_instance_not_material_table` but only at the layout level)\n\n## Fix\n\n**Deferred — needs runtime verification.** Two viable approaches:\n\n1. **RenderDoc capture diff** — manually verify byte-identical output on a fixed scene pre/post R1 changes. Not an automated regression guard.\n2. **Golden-frame harness extension** (preferred) — extend `byroredux/tests/golden_frames.rs` (currently cube_demo only) with a per-scene R1 identity test: load a fixed scene with N≥2 identical-material placements, snapshot, and assert hash equality vs baseline. Closes part of TD6-006 from closed batch #1050.\n\nThis is **narrower than TD6-006**: TD6-006 also called out TAA, GPU skinning, and alpha-blend decal coverage. This issue tracks only the material-table layer.\n\n## Completeness Checks\n\n- [ ] **TESTS**: Test added that compares pixel output for fixed scene pre-/post-dedup\n- [ ] **SIBLING**: TD6-006 sibling scenes (TAA static-vs-jitter, single-bone skin, alpha-blend decal) considered for the same harness\n- [ ] **DROP**: Test fixture loads/unloads cleanly without GPU resource leak\n\n## Out of scope\n\n- Adding golden-frame coverage for TAA / skinning / decals (separate scope, see TD6-006 in closed #1050)\n- Replacing RenderDoc usage with a fully automated harness (engineering investment, separate)","title":"REN-D14-NEW-02: R1 identity-invariant has no automated regression test (golden-frame coverage gap)"}
