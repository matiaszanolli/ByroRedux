Fixed in e578363f.

The comment now names `initialize_layouts` as the owner of the `UNDEFINED -> GENERAL` discard, states that this barrier is `GENERAL -> GENERAL` on every frame including frame 0, and — since the impact you describe is someone deleting `initialize_layouts` as redundant — says not to do that explicitly, with the VUID.

Added one thing the finding did not: the reason it *cannot* be the discarding transition, which is that `old_layout` is a compile-time constant here. That is the argument that settles it without needing to trace the init path, and it is what makes the two comments' disagreement resolvable by inspection rather than by trust.

Verified the sibling claim held: `grep -rn "First use of this slot" crates/renderer/src/vulkan/` returns nothing now, and the five other `initialize_layouts` owners carry no equivalent.

**On the `Existing: #3442` sub-note in this issue.** It asked for a one-line correction to that issue because the `(f + 1) % MAX_FRAMES_IN_FLIGHT` expression had moved under #3282 while the pin still covered only `taa.rs` / `svgf.rs` / `restir.rs` / `volumetrics.rs`. No correction posted, because it is no longer actionable: `ac48ab63` closed #3442 on 2026-09-07 and extended that pin to cover both `context/draw.rs` and `context/sync_and_acquire_frame.rs`. The note was accurate when written on 2026-09-06 and went stale the next day.

No code change.
