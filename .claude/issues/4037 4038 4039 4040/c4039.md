Fixed in e578363f.

Confirmed the premise exactly: `ensure_screenshot_staging` has one caller (`screenshot_record_copy`, during recording), `destroy_screenshot_staging` has two (that function's grow branch, and `teardown.rs`), and `grep screenshot crates/renderer/src/vulkan/context/resize.rs` returns nothing. There is no resize path.

Took the second half of your suggested fix rather than the first. Copying the corrected sentence across would have left two identical frees under two comments — which is the arrangement that produced this issue, since #3628 corrected one copy and could not know about the other. So `helpers::destroy_staging_buffer` now owns the code *and* the contract, and both methods delegate to it. The bodies were byte-identical, so nothing is lost.

Pinned by `both_staging_frees_delegate_to_the_shared_helper`: each destroy fn body must contain the delegation and must not re-inline a raw `destroy_buffer`, and neither file may carry the retired "only reached between frames" claim. Verified by re-inlining the screenshot free with its old comment — fails.

**One correction to the finding.** It says the both-slots fence wait is "the one correct reason [that] is the one currently unguarded", citing #3442. True on 2026-09-06; not true from 2026-09-07, when `ac48ab63` closed #3442 and extended `temporal_history_indexing_uses_the_general_previous_slot_form` to cover `draw.rs` and `sync_and_acquire_frame.rs`. The helper's doc records this, so the next reader is not sent looking for a gap that is already closed.
