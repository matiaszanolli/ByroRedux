{"body":"## Severity: MEDIUM (ENHANCEMENT — ties to 6 GB RT minimum OOM risk)\n\n**Location**: `crates/renderer/src/vulkan/acceleration.rs:1467-1528`\n(`evict_unused_blas`) + the batched build call site\n\n## Problem\n\n`evict_unused_blas` correctly checks `total_blas_bytes <= budget` at entry\nand early-returns if under. But the function is currently only called from\nthe main frame path (`VulkanContext::draw_frame`). During a synchronous\ncell load (currently ~50–200 ms block on the main thread), the engine can\nrun `build_blas` several hundred times in sequence and blow well past the\nbudget before frame N+1's eviction check runs.\n\nIf VRAM is tight (stated 6 GB RT minimum), this is the failure mode — OOM\nduring the load burst itself, not the steady state.\n\n## Impact\n\nStability/reliability risk on the 6 GB minimum target. Not yet observed in\npractice, but the failure mode is real and documented in the BLAS budget\ncomment.\n\n## Suggested Fix\n\nCall `evict_unused_blas` inside the batched-build path every N meshes\n(e.g. every 100 BLAS builds) when the running `total_blas_bytes`\napproaches the budget. The scan is O(live BLAS count) and the eviction\nonly runs when needed — cheap insurance.\n\n```rust\nfor (i, p) in prepared.iter().enumerate() {\n    if i > 0 && i % EVICTION_CHECK_INTERVAL == 0 && self.total_blas_bytes >= self.blas_budget * 9 / 10 {\n        self.evict_unused_blas(/* in-batch mode */);\n    }\n    // … existing build_blas step …\n}\n```\n\n## Completeness Checks\n\n- [ ] **SIBLING**: In-batch eviction must not drop a BLAS referenced by\n      the current build — track the set of \"currently-building\" handles and\n      exclude them from eviction\n- [ ] **DROP**: Pending-destroy countdown interactions with in-batch drops\n- [ ] **TESTS**: Synthetic test that builds enough BLAS to trigger the\n      mid-batch path, asserts total stays under budget\n\n## Source\n\nAudit: `docs/audits/AUDIT_PERFORMANCE_2026-04-20.md` (D2-E1)","labels":[{"id":"LA_kwDORzgFWc8AAAACdLDhYg","name":"enhancement","description":"New feature or request","color":"a2eeef"},{"id":"LA_kwDORzgFWc8AAAACeFy0Lg","name":"medium","description":"Medium severity","color":"FBCA04"},{"id":"LA_kwDORzgFWc8AAAACeFy0QA","name":"vulkan","description":"Vulkan API","color":"B60205"},{"id":"LA_kwDORzgFWc8AAAACeFy0YQ","name":"memory","description":"GPU/CPU memory","color":"C2E0C6"},{"id":"LA_kwDORzgFWc8AAAACeGdDkw","name":"performance","description":"Performance optimization","color":"fbca04"}],"state":"OPEN","title":"PERF-2026-04-20 D2-E1: BLAS eviction doesn't run mid-batch during cell-load burst"}
