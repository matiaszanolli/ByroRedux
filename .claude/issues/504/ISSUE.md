{"body":"## Severity: LOW\n\n**Location**: `crates/renderer/src/vulkan/acceleration.rs:109, 1017, 1451`\n\n## Problem\n\n`tlas_instances_scratch: Vec<vk::AccelerationStructureInstanceKHR>` uses\n`mem::take` → `clear` → `reserve(draw_commands.len())` → build →\n`mem::replace` back. The `reserve` is unbounded-monotonic: after once\nprocessing a frame with 10,000 draw commands (exterior open cell at peak),\nthe Vec retains capacity 10,000 × 64 B = 640 KB forever.\n\nSame pattern as D2-M1 (BLAS scratch) but at the CPU side.\n\n## Impact\n\nUp to 640 KB RSS retention after a peak exterior frame. Trivial by itself;\nworth fixing alongside the broader scratch-shrinkage story in D2-M1.\n\n## Suggested Fix\n\nAfter the `mem::replace` restore, if\n`self.tlas_instances_scratch.capacity() > 2 * instance_count.max(512)`,\ncall `shrink_to(2 * instance_count.max(512))`. Keeps steady-state near the\nworking set.\n\n## Completeness Checks\n\n- [ ] **SIBLING**: Apply the same shrink policy to any other `mem::take` +\n      `reserve` pattern in the codebase\n- [ ] **TESTS**: Unit test that grows to 10,000 + shrinks back to expected\n      cap after enough low-instance-count frames\n\n## Source\n\nAudit: `docs/audits/AUDIT_PERFORMANCE_2026-04-20.md` (D2-L3)\nRelated: D2-M1 #495 (GPU-side scratch)","labels":[{"id":"LA_kwDORzgFWc8AAAACeFy0Mw","name":"low","description":"Low severity","color":"0E8A16"},{"id":"LA_kwDORzgFWc8AAAACeFy0QA","name":"vulkan","description":"Vulkan API","color":"B60205"},{"id":"LA_kwDORzgFWc8AAAACeFy0YQ","name":"memory","description":"GPU/CPU memory","color":"C2E0C6"},{"id":"LA_kwDORzgFWc8AAAACeGdDkw","name":"performance","description":"Performance optimization","color":"fbca04"}],"state":"OPEN","title":"PERF-2026-04-20 D2-L3: tlas_instances_scratch Vec never shrinks after peak frames"}
