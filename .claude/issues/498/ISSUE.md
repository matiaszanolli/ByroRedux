{"body":"## Severity: MEDIUM (really MEDIUM-LOW; ~10 µs/frame CPU)\n\n**Location**: `crates/renderer/src/vulkan/buffer.rs:522-524, 561-563, 627-629`\n\n## Problem\n\n`write_mapped`, `flush_if_needed`, and `flush_range` each call\n`alloc.memory_properties().contains(HOST_COHERENT)` on every invocation.\nThe result is fixed at bind time — once a `GpuBuffer` is allocated, its\nmemory type index (and therefore its coherent-ness) does not change.\n\nOn a typical frame:\n- ~10 per-frame mapped writes (camera, lights, bones, instances, indirect,\n  SSAO, TAA, caustic, SVGF, composite)\n- Each invokes `memory_properties()` (a `DescriptorSetLayoutBinding`-sized\n  heap query via gpu-allocator)\n- Shows up as visible `gpu_allocator::vulkan::Allocation::memory_properties`\n  self-time in `perf record`\n\n## Impact\n\n~5–15 µs/frame CPU. Not game-breaking. Appears in Linux `perf` traces.\n\n## Suggested Fix\n\nCache `is_coherent: bool` on `GpuBuffer` at construction:\n\n```rust\npub struct GpuBuffer {\n    pub buffer: vk::Buffer,\n    pub allocation: Option<Allocation>,\n    // ... existing fields ...\n    is_coherent: bool,  // ← set once at create, read everywhere else\n}\n```\n\nSet from `allocation.memory_properties()` at bind time.\n`write_mapped` / `flush_if_needed` / `flush_range` all read `self.is_coherent`.\n\n## Completeness Checks\n\n- [ ] **SIBLING**: All 3 flush call sites (write_mapped, flush_if_needed,\n      flush_range) must read the cached flag\n- [ ] **DROP**: No Drop impact (bool field, zero cleanup)\n- [ ] **TESTS**: Existing flush tests cover both coherent and non-coherent\n      paths — verify both still pass\n\n## Source\n\nAudit: `docs/audits/AUDIT_PERFORMANCE_2026-04-20.md` (D2-M4)","labels":[{"id":"LA_kwDORzgFWc8AAAACdLDhWQ","name":"bug","description":"Something isn't working","color":"d73a4a"},{"id":"LA_kwDORzgFWc8AAAACd30_-Q","name":"renderer","description":"Vulkan renderer","color":"e99695"},{"id":"LA_kwDORzgFWc8AAAACeFy0Lg","name":"medium","description":"Medium severity","color":"FBCA04"},{"id":"LA_kwDORzgFWc8AAAACeFy0QA","name":"vulkan","description":"Vulkan API","color":"B60205"},{"id":"LA_kwDORzgFWc8AAAACeGdDkw","name":"performance","description":"Performance optimization","color":"fbca04"}],"state":"OPEN","title":"PERF-2026-04-20 D2-M4: write_mapped re-queries memory_properties per call — cache is_coherent on GpuBuffer"}
