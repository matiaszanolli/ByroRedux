{"body":"## Severity: LOW\n\n**Location**: `byroredux/src/render.rs:133`\n\n## Problem\n\n`palette_scratch: Vec<Mat4>` is a stack-local fresh `Vec::new()` at the top\nof `build_render_data`:\n\n```rust\nlet mut palette_scratch = Vec::new();\nfor (entity, skin) in skin_q.iter() {\n    // … populate palette_scratch, consume, implicitly keep capacity\n}\n```\n\nAcross skinned-mesh loop iterations inside the same call, it's reused\n(cleared each iteration), but it doesn't survive across frames. One\n`Vec::new()` + one growth-to-capacity per frame. ~60 KB allocation churn\nper frame in a scene with skinned actors.\n\n## Impact\n\nBelow the signal floor of profilers (~60 KB/frame is noise against a ~2 ms\n`build_render_data`). Trivially avoidable.\n\n## Suggested Fix\n\nHoist onto the caller — either onto engine state, or as an owned field on\na `RenderDataResource` — and pass by `&mut` to `build_render_data`. Same\npattern `draw_commands` / `gpu_lights` already use.\n\n## Completeness Checks\n\n- [ ] **SIBLING**: All other `Vec::new()` / `HashMap::new()` sites at the\n      top of `build_render_data` — are any also refill-once-per-frame?\n- [ ] **TESTS**: Existing render_data tests should pass unchanged\n\n## Source\n\nAudit: `docs/audits/AUDIT_PERFORMANCE_2026-04-20.md` (D6-L1)\nRelated: #243 (established this scratch-buffer pattern)","labels":[{"id":"LA_kwDORzgFWc8AAAACd30_8w","name":"ecs","description":"Entity Component System","color":"0e8a16"},{"id":"LA_kwDORzgFWc8AAAACd30_-Q","name":"renderer","description":"Vulkan renderer","color":"e99695"},{"id":"LA_kwDORzgFWc8AAAACeFy0Mw","name":"low","description":"Low severity","color":"0E8A16"},{"id":"LA_kwDORzgFWc8AAAACeGdDkw","name":"performance","description":"Performance optimization","color":"fbca04"}],"state":"OPEN","title":"PERF-2026-04-20 D6-L1: palette_scratch fresh-allocated each build_render_data call"}
