{"body":"## Severity: MEDIUM\n\n**Location**: `byroredux/src/render.rs:597-619`\n\n## Problem\n\nThe blended branch of the sort key is\n\n```\n(1u8, is_decal, two_sided, !sort_depth, pack_depth_state, texture_handle, mesh_handle)\n```\n\n`(src_blend, dst_blend)` are absent entirely. Within a given two-sided\ncohort, additive vs alpha vs modulate vs premultiplied draws interleave by\ndepth. Each alternation produces a `cmd_bind_pipeline` through the\n`blend_pipeline_cache` lookup.\n\n## Impact\n\n~20–40 extra blend-pipeline switches per frame in worst-case smoke-heavy\ninteriors (Megaton firelight, Vault 101 steam — ~50 particle quads + ~20\nglass panes interleaving by depth produce ~30–60 pipeline switches instead\nof the ~3–4 unique blend keys actually in use). ~0.02–0.08 ms GPU.\n\nAlso blocks same-blend-same-mesh particle instance merging: billboards at\ndifferent depths within one emitter cannot merge through interleaved\nalpha-blended geometry.\n\n## Suggested Fix\n\nAdd `(src_blend, dst_blend)` between slots 2 and 3 of the blended key\n(after `two_sided`, before `!sort_depth`):\n\n```rust\n// Current:\n(1u8, is_decal, two_sided, !sort_depth, depth_state, texture_handle, mesh_handle)\n// Fix:\n(1u8, is_decal, two_sided, src_blend, dst_blend, !sort_depth, depth_state, mesh_handle)\n```\n\n**Correctness preserved**: alpha compositing only requires back-to-front\norder *within one pipeline state*, not across them. Draws sharing exact\n`(mesh, src, dst, two_sided)` still sort back-to-front and remain correctly\nordered; draws with different blend factors are already visually separable\n(additive over alpha is commutative in the bloom/glow case; glass panes\nrarely co-mingle with smoke).\n\n## Completeness Checks\n\n- [ ] **TESTS**: Existing `build_render_data` tests cover the opaque path —\n      add a case with two emitters at different depths sharing blend\n      factors, assert they merge into a single batch\n- [ ] **SIBLING**: `debug_assert!` in `draw.rs:472-479` should also be\n      updated (see D3-M2 #NNN)\n- [ ] **LOCK_ORDER**: No lock changes\n\n## Source\n\nAudit: `docs/audits/AUDIT_PERFORMANCE_2026-04-20.md` (D3-M1)","labels":[{"id":"LA_kwDORzgFWc8AAAACdLDhWQ","name":"bug","description":"Something isn't working","color":"d73a4a"},{"id":"LA_kwDORzgFWc8AAAACd30_-Q","name":"renderer","description":"Vulkan renderer","color":"e99695"},{"id":"LA_kwDORzgFWc8AAAACeFy0Lg","name":"medium","description":"Medium severity","color":"FBCA04"},{"id":"LA_kwDORzgFWc8AAAACeGdDkw","name":"performance","description":"Performance optimization","color":"fbca04"}],"state":"OPEN","title":"PERF-2026-04-20 D3-M1: Blended sort key omits (src_blend, dst_blend) — pipeline thrash in particle scenes"}
