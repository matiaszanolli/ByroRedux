Fixed in d9a7e75c.

The scan now covers `context/build_and_upload_instances.rs` (where the per-frame UBO section lives) as well as `context/draw.rs`, and the doc comment says where that section actually is and why a re-added call would land there.

Added beyond the suggested fix: an assertion that `composite.upload_params`, `svgf.upload_params`, `taa.upload_params` and `water.upload_params` are all still present in the file being scanned. Re-pointing the pin fixes it for today, but the failure mode this issue reports is precisely that the section can move again — and when it does, the pin should fail loudly rather than quietly return to being vacuous. That check is the difference between fixing the instance and fixing the class.

Verified by neutering: a `bloom.upload_params` string placed where a real re-add would land now fails the test, and did not before.

No production change.
