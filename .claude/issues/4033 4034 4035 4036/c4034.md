Fixed in d9a7e75c.

The pin now scans `sync_and_acquire_frame.rs` (where #3282 moved the call site) alongside `draw.rs` — keeping the old entry costs nothing and catches the comment migrating back, as suggested.

Added beyond the suggested fix, because the negative-only form is what let this rot in the first place: a **positive** assertion that the live call site still contains `collect_image_health` and the phrase "device-side access scope". The negative scan only catches the retired claim being *re-added*; without the positive half, deleting the #2740 correction outright would still pass. Every other `include_str!("context/draw.rs")` scan in the crate already pairs its negative with a positive (`sync.rs` requires `render_finished[img]` present, `context/mod.rs` requires `FxHashSet<EntityId>` present) — which is precisely why those did not rot and this one did.

Verified by neutering rather than by observing green: re-adding a "provably idle" claim at the live call site fails the test now and did not before.

No production change.
