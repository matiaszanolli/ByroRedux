Fixed in d9a7e75c.

Added four rows to `shader-pipeline.md`'s submission-order block, placed against the code rather than the audit's description:

- **4b** `AS_WRITE -> AS_READ` — verified it sits between the TLAS build and `cluster_cull` inside `dispatch_skin_and_cluster`, emitted before the `if !tlas_build_failed` branch (so both arms, as filed), and publishing `record_skinned_blas_refit`'s writes as well as the TLAS.
- **5b** the bulk `HOST_WRITE` fold at the tail of `build_and_upload_instances`, naming its four dst stages, the four UBOs folded onto it (#909 / #961 / #1397), and — the part a reader actually needs — that this is why step 17 composite carries no HOST barrier of its own.
- **1a** `flush_pending_morph_weights`, which runs *before* step 1 in `sync_and_acquire_frame`, after the both-slots fence wait that makes it safe.
- **6b** the selected-ray-probe publish, confirmed to sit between `record_geometry_pass` and `copy_depth_to_history`.

One thing beyond the filed scope, because it defeats the fix otherwise: the block's intro still sent readers to `draw.rs` alone for pass ordering. Post-#3282 that is where `draw_frame` orchestrates, but 4b and 5b are both recorded in sibling files — so a reader following that pointer would not have found the two barriers just documented. The intro now names the phase files.

Deliberately not added: `cluster_cull`'s own two barriers. They are real and also undocumented, but they are not what this issue is about and filing them here would blur it. Worth a separate finding if the next sweep agrees.

No code change.
