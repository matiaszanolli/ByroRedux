{"body":"## Severity\n**LOW** (meta-bug: hides higher-severity bugs from the gate). This is why #546 (SK-D5-01) slipped through the parse-rate gate showing 100%.\n\n## Location\n- `crates/nif/src/lib.rs:302-317` (block recovery path)\n- `crates/nif/examples/nif_stats.rs:73-99` (`record_success` vs `record_truncated`)\n\n## Description\nWhen `bhkRigidBody::parse` (or any other block parser) under-consumes its block, the block-level recovery path in `lib.rs:302` emits a `log::warn!`, replaces the block with a `NiUnknown` placeholder, and continues. Crucially, it does **not** flag the scene as truncated, so `Stats::record_success` is called — the headline rate stays at 100%.\n\nHeadline rate for Meshes0 is 100.00% clean / 0 truncated despite 12,866 bhkRigidBody fall-throughs. The truncated/dropped-blocks telemetry that #393 added is blind to per-block recovery.\n\n## Impact\n- Regressions like SK-D5-01 (#546) can land and ship without ever turning the parse-rate gate red.\n- Any future block parser misalignment will be silently absorbed.\n- Cannot trust \"100% parse rate\" as a release gate.\n\n## Suggested Fix\nOption A — bump truncation counter:\n```rust\n// in lib.rs:302 recovery branch\nscene.truncated = true;\nscene.dropped_block_count += 1;\n```\nThis makes #393's metric catch per-block recovery.\n\nOption B — separate counter:\nAdd `recovered_blocks: u32` to `NifScene`. In `nif_stats.rs::record_success`, check `scene.recovered_blocks > 0` and route through `record_truncated` instead. This preserves truncated-vs-recovered as distinct categories.\n\nOption B is probably better for diagnosis (you can still see at a glance whether a failure is \"stream truncated\" vs \"parser misaligned but stream recovered\").\n\n## Completeness Checks\n- [ ] **SIBLING**: All callers of `record_success` in `nif_stats.rs` updated, not just the happy path.\n- [ ] **SIBLING**: Grep for `Box<NiUnknown>` construction in other recovery paths — all should bump the counter.\n- [ ] **TESTS**: Add a Skyrim bhkRigidBody regression asserting `recovered_blocks > 0` before fix #546, and `recovered_blocks == 0` after.\n- [ ] **TESTS**: Update existing stat tests to assert on the new counter.\n- [ ] After this lands, #546 can be gated on a parse-rate regression test rather than manual inspection.\n\n## Source\nAudit `docs/audits/AUDIT_SKYRIM_2026-04-22.md` finding **SK-D5-06**.","labels":[{"id":"LA_kwDORzgFWc8AAAACdLDhWQ","name":"bug","description":"Something isn't working","color":"d73a4a"},{"id":"LA_kwDORzgFWc8AAAACd30_7A","name":"nif-parser","description":"NIF file parsing","color":"5319e7"},{"id":"LA_kwDORzgFWc8AAAACeFy0Mw","name":"low","description":"Low severity","color":"0E8A16"}],"state":"OPEN","title":"SK-D5-06: nif_stats clean metric counts NiUnknown-recovered blocks as clean parses"}
