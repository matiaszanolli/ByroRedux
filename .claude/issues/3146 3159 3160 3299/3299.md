author:	matiaszanolli
association:	owner
edited:	false
status:	none
--
Unblocked: #2369's step 2 (EX-14/15 item C2's reconcile half — skip the persistent-CELL drain+rebuild when a worldspace crossing resolves to the same persistent CELL) landed in a47dcf0c. Per the design doc's §7 sequencing, this issue (step 3, the snapshot/restore mechanism) can now proceed, pending step 2 "holding up in practice" as this issue's own sequencing note asks — no regressions expected (4 new unit tests, full workspace suite green at 6000 passing), but no live-gameplay validation has happened yet since this environment has no game data on hand to exercise a real worldspace crossing.
--
author:	matiaszanolli
association:	owner
edited:	false
status:	none
--
Cross-linking from #2369's split (EX-14/15 criterion 3: "persistent and
temporary refs retain correct ownership across parent worldspaces and
boundary crossings"). This issue is the live-state-carryover half of that
criterion — the persistent-CELL *identity-skip* optimization (avoid a
redundant despawn/respawn when the resolved persistent CELL is unchanged
across a crossing) landed separately in `a47dcf0c`, but this issue's own
scope (a temporary ref's package/behavior state actually surviving an
ordinary radius-streaming despawn/respawn cycle) is the real remaining
gap #2369's criterion 3 was pointing at. No new issue filed for it — this
one already covers it precisely and is now unblocked (its step-2 blocker
landed).
--
