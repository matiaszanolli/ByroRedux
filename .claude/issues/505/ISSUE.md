{"body":"## Severity: LOW\n\n**Location**: `crates/renderer/src/vulkan/allocator.rs:71-79`\n\n## Problem\n\nWarn threshold is a compile-time `const 2 * 1024 * 1024 * 1024`. Two\nproblems:\n\n- On the stated 6 GB VRAM minimum (see memory note), 2 GB is 33% — too low\n  for Fallout 4 exteriors (has exceeded 1.8 GB historically).\n- On a 12 GB dev GPU (user's 4070 Ti), 2 GB is 16% — triggers an unhelpful\n  warn on every large cell load.\n\nThe comment at line 72 already acknowledges \"deferred to streaming\nmilestone\".\n\n## Suggested Fix\n\nEnable `VK_EXT_memory_budget` at device creation (promoted to core in 1.1,\nso available on every Vulkan 1.1+ driver).\n\n```rust\next::memory_budget::MemoryBudget::new(&instance, physical_device)\n    .get_physical_device_memory_budget_properties_ext(...)\n```\n\nSet threshold to 0.8 × smallest DEVICE_LOCAL heap budget. Warn when\nexceeded. Same `VRAM / 3` pattern the BLAS budget computation\n(`acceleration.rs:214-232`) already uses.\n\n## Completeness Checks\n\n- [ ] **SIBLING**: The BLAS budget path already queries heap size — factor\n      out a `vulkan::device::smallest_device_local_budget()` helper so both\n      sites share one query\n- [ ] **TESTS**: Headless integration test that asserts the reported\n      threshold is within 0.7–0.9 × the true heap size\n\n## Source\n\nAudit: `docs/audits/AUDIT_PERFORMANCE_2026-04-20.md` (D2-L4)","labels":[{"id":"LA_kwDORzgFWc8AAAACeFy0Mw","name":"low","description":"Low severity","color":"0E8A16"},{"id":"LA_kwDORzgFWc8AAAACeFy0QA","name":"vulkan","description":"Vulkan API","color":"B60205"},{"id":"LA_kwDORzgFWc8AAAACeFy0YQ","name":"memory","description":"GPU/CPU memory","color":"C2E0C6"},{"id":"LA_kwDORzgFWc8AAAACeGdDkw","name":"performance","description":"Performance optimization","color":"fbca04"}],"state":"OPEN","title":"PERF-2026-04-20 D2-L4: log_memory_usage warn threshold hard-coded 2 GB — use VK_EXT_memory_budget"}
