{"body":"## Severity: LOW (ENHANCEMENT — depends on #239)\n\n**Location**: `crates/renderer/src/vulkan/buffer.rs:16`\n(`DEFAULT_STAGING_BUDGET_BYTES`)\n\n## Problem\n\nThe 64 MB default is good for mesh streaming (average mesh ~50 KB). For\ntextures (once #239 lands and the pool is wired in), 2K BC7 with full mips\nis ~5.3 MB and 4K BC7 is ~22 MB per texture. A 20-texture burst during an\ninterior → exterior transition can momentarily need ~100 MB of staging\ncapacity.\n\nGoing over the budget forces eviction mid-load, which destroys buffers\nthat could have been reused for the next texture and forces a fresh\nallocation — **defeating the pool's purpose exactly when it's needed most**.\n\n## Impact\n\nDependent on #239 shipping first. Once texture uploads use the pool, the\n64 MB cap becomes the bottleneck.\n\n## Suggested Fix\n\nEither:\n\n(a) Bump the default to 128 or 256 MB. The budget only caps *retained*\n    capacity, not in-flight — it's effectively a \"how much memory are we\n    willing to hold after the burst\" cap. Bumping is cheap.\n\n(b) Expose `StagingPool::reserve(total)` called once at the start of a\n    bulk load so the budget scales with the known working set.\n\nPrefer (a) as the default, with (b) as an escape hatch for extreme loads\n(Fallout 4 exterior transitions).\n\n## Completeness Checks\n\n- [ ] **DEPENDENCIES**: Must land AFTER #239 (texture staging pool wiring)\n- [ ] **TESTS**: Synthetic texture-burst test that exceeds the current\n      64 MB cap; assert no mid-burst evictions occur after the fix\n\n## Source\n\nAudit: `docs/audits/AUDIT_PERFORMANCE_2026-04-20.md` (D2-E2)\nDepends on: #239 (texture staging pool)","labels":[{"id":"LA_kwDORzgFWc8AAAACdLDhYg","name":"enhancement","description":"New feature or request","color":"a2eeef"},{"id":"LA_kwDORzgFWc8AAAACeFy0Mw","name":"low","description":"Low severity","color":"0E8A16"},{"id":"LA_kwDORzgFWc8AAAACeFy0QA","name":"vulkan","description":"Vulkan API","color":"B60205"},{"id":"LA_kwDORzgFWc8AAAACeFy0YQ","name":"memory","description":"GPU/CPU memory","color":"C2E0C6"},{"id":"LA_kwDORzgFWc8AAAACeGdDkw","name":"performance","description":"Performance optimization","color":"fbca04"}],"state":"OPEN","title":"PERF-2026-04-20 D2-E2: StagingPool 64 MB budget too small for texture bursts (depends on #239)"}
