{"body":"## Severity: MEDIUM\n\n**Location**: `crates/renderer/src/vulkan/scene_buffer.rs:336, 371-372, 412-417`\n\n## Problem\n\nIntroduced by #470. The terrain tile SSBO (binding 10) is allocated **once\nper frame-in-flight** (`MAX_FRAMES_IN_FLIGHT = 2`), sized at\n`MAX_TERRAIN_TILES * 32 B = 32 KB`, and lives in HOST_VISIBLE memory. The\ndata is static until the next cell transition — `terrain_tiles_dirty_frames`\nis decremented on each upload so the buffer is rewritten exactly twice after\na cell load, then never touched until the next load.\n\n**Per-frame-in-flight double-buffering is the wrong pattern here**: the\nbuffer is read-only from the GPU's side, so a single DEVICE_LOCAL buffer\nwith one-time staging upload at cell-load time would suffice (same pattern\nas the global vertex/index SSBOs).\n\n## Impact\n\n32 KB × 2 = 64 KB of HOST_VISIBLE memory permanently pinned to the BAR heap\nwhen 32 KB of DEVICE_LOCAL would do. Tiny in absolute terms, but wasteful on\nthe scarce BAR heap (~256 MB on NVIDIA desktop).\n\n## Suggested Fix\n\nPromote to single DEVICE_LOCAL buffer, upload via `StagingPool` at cell\nload. Delete the `tiles_dirty_frames` countdown. Binding 10 in the scene\ndescriptor layout points at the same buffer for every frame-in-flight.\nDescriptor writes already don't care about the frame index — the fragment\nshader reads the same data regardless.\n\n## Completeness Checks\n\n- [ ] **DROP**: Ensure the old per-frame buffers are destroyed (currently\n      dropped via `SceneBuffers::destroy`)\n- [ ] **SIBLING**: Pattern matches the global VB/IB (#294) — DEVICE_LOCAL,\n      one-time upload, many-frame read\n- [ ] **LOCK_ORDER**: Descriptor write happens at cell-load time, outside\n      frame rendering — no per-frame descriptor churn\n- [ ] **TESTS**: Terrain visual regression on an FNV exterior cell (HELIOS\n      One `-3, -12`) to confirm correctness after promotion\n\n## Source\n\nAudit: `docs/audits/AUDIT_PERFORMANCE_2026-04-20.md` (D2-M3). Pairs with D2-M2.\nRelated: #470 (introduced this path)","labels":[{"id":"LA_kwDORzgFWc8AAAACdLDhWQ","name":"bug","description":"Something isn't working","color":"d73a4a"},{"id":"LA_kwDORzgFWc8AAAACd30_-Q","name":"renderer","description":"Vulkan renderer","color":"e99695"},{"id":"LA_kwDORzgFWc8AAAACeFy0Lg","name":"medium","description":"Medium severity","color":"FBCA04"},{"id":"LA_kwDORzgFWc8AAAACeFy0QA","name":"vulkan","description":"Vulkan API","color":"B60205"},{"id":"LA_kwDORzgFWc8AAAACeFy0YQ","name":"memory","description":"GPU/CPU memory","color":"C2E0C6"},{"id":"LA_kwDORzgFWc8AAAACeGdDkw","name":"performance","description":"Performance optimization","color":"fbca04"}],"state":"OPEN","title":"PERF-2026-04-20 D2-M3: Terrain tile SSBO should be DEVICE_LOCAL single buffer, not per-frame HOST_VISIBLE"}
