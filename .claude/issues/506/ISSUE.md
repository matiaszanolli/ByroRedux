{"body":"## Severity: LOW\n\n**Location**: `byroredux/src/render.rs:597` (`par_sort_unstable_by_key`)\n\n## Problem\n\n`par_sort_unstable_by_key` is deliberately unstable for speed. The sort key\nis a 7-tuple with ~88 bits of entropy (mesh_handle + texture_handle +\nsort_depth), so full-tuple ties are rare but not impossible. When they do\noccur, rayon's work-stealing can reorder the tied entries non-deterministically\nacross frames.\n\nBecause the bindless texture array means texture ordering does not affect\nrendering, this is benign for the frame output — but it does mean\ncapture/replay tools (screenshot diffs, RenderDoc captures) cannot assume\nframe N and frame N+1 record the same draw order for structurally-identical\nscene state.\n\n## Impact\n\nZero frame correctness impact. Affects:\n- `screenshot` debug command comparing pre/post-fix frames\n- RenderDoc-captured draw ordering\n- Any future test that compares DrawCommand lists byte-exact\n\n## Suggested Fix\n\nOption A — Append entity ID as secondary tiebreaker:\n\n```rust\n// In both branches of the sort closure:\n(…existing tuple…, cmd.entity.id())\n```\n\nMakes full-tuple ties impossible. Zero perf cost.\n\nOption B — Use serial `sort_unstable_by_key` below a threshold (~500\ncommands). Single-threaded sort is deterministic AND faster than rayon's\nfork overhead at small sizes.\n\nPrefer Option A.\n\n## Completeness Checks\n\n- [ ] **TESTS**: Add a determinism test — sort the same `Vec<DrawCommand>`\n      twice, assert identical output\n- [ ] **PERF**: Confirm entity-ID tiebreaker does not regress the sort hot\n      path (expected: free)\n\n## Source\n\nAudit: `docs/audits/AUDIT_PERFORMANCE_2026-04-20.md` (D3-L3)","labels":[{"id":"LA_kwDORzgFWc8AAAACd30_-Q","name":"renderer","description":"Vulkan renderer","color":"e99695"},{"id":"LA_kwDORzgFWc8AAAACeFy0Mw","name":"low","description":"Low severity","color":"0E8A16"},{"id":"LA_kwDORzgFWc8AAAACeGdDkw","name":"performance","description":"Performance optimization","color":"fbca04"}],"state":"OPEN","title":"PERF-2026-04-20 D3-L3: Unstable sort with no tiebreaker breaks capture/replay determinism"}
