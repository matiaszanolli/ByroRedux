{"body":"**Severity**: LOW (documented technical debt)\n**Dimension**: Material Table (R1)\n**Audit**: `docs/audits/AUDIT_RENDERER_2026-05-22_DIM14.md` → REN-D14-NEW-03\n**Predecessors** (closed): #1098 (REN-D13-001), #1111 (TD1-004). #1111 explicitly suggested filing this followup tracker.\n\n## Finding\n\n`GpuInstance.avg_albedo_r / _g / _b / _pad_albedo` (offsets 96..112, 16 B/instance) live on the per-instance struct rather than on `GpuMaterial`. Two read sites currently rely on this:\n\n- `crates/renderer/shaders/caustic_splat.comp:75-93` — reads `inst.avgAlbedoR/G/B` via the caustic descriptor set (set 0 binding 5)\n- `crates/renderer/shaders/triangle.frag` (GI-miss path) — samples them off the rasterizer's set 1 binding 4\n\nSource comments at `gpu_types.rs:97-108` and `caustic_splat.comp:75-81` document the deferral and call out the migration scope.\n\n## Why this matters\n\n- 16 B × `MAX_INSTANCES (262144)` = **~4 MB per frame** of SSBO bandwidth on per-material data that dedups in practice (Prospector: 1200 placements → 87 unique materials → ~14× dedup hit rate would also apply to `avg_albedo`).\n- Carries Phase-6 closeout debt — R1's stated goal was \"drop per-instance copies that inflate GpuInstance from 112 B to 400 B\"; the 112 B figure still contains 16 B of per-material data.\n- Blocks future GpuInstance shrink below 112 B.\n\n## Fix\n\n1. Add an `avg_albedo: vec3` (or 3 scalar f32 + pad) field to `GpuMaterial` — coordinate offset assertions with `gpu_material_field_offsets_match_shader_contract` (#806), `gpu_material_size_is_260_bytes`, and the GLSL mirror in `triangle.frag:89-126`.\n2. Populate it in `to_gpu_material` (`context/mod.rs:320-407`) from the existing `avg_albedo` source (same place that fills the GpuInstance field today).\n3. Add a `MaterialBuffer` binding to the caustic compute descriptor set; rewrite `caustic_splat.comp` to read `materials[inst.materialId].avgAlbedo*`.\n4. Update the `triangle.frag` GI-miss read to go through `materials[hitInst.materialId]`.\n5. Drop `avg_albedo_r/g/b` + `_pad_albedo` from `GpuInstance` — shrinks the struct 112 → 96 B.\n6. Update all 5 GpuInstance shader mirrors in lockstep (`triangle.vert`, `triangle.frag`, `ui.vert`, `water.vert`, `caustic_splat.comp`) per `feedback_shader_struct_sync.md`.\n7. Update `gpu_instance_layout_tests.rs` byte pin + per-field offset pin to the new size.\n\n## Completeness Checks\n\n- [ ] **SIBLING**: All 5 `struct GpuInstance` shader mirrors updated in lockstep (`feedback_shader_struct_sync.md`)\n- [ ] **DROP**: No Vulkan-object changes; descriptor set layout change requires `recreate_descriptors` on swapchain rebuild\n- [ ] **TESTS**: New `gpu_material_field_offsets_match_shader_contract` assertions for `avg_albedo_*` offsets; updated `gpu_instance_is_112_bytes_std430_compatible` → 96 B\n- [ ] **TESTS**: Regression test that intern-rate on a fixed scene doesn't degrade post-migration (dedup of avg_albedo)\n\nPair this with #973 (MSWP migration) so both GpuInstance shrink steps ride a single shader-struct-sync refresh.","title":"REN-D14-NEW-03: GpuInstance.avg_albedo_* migration to GpuMaterial (R1 Phase 6 followup)"}
